warning: LF will be replaced by CRLF in dist/public/js/client.js.
The file will have its original line endings in your working directory
warning: LF will be replaced by CRLF in src/public/js/client.js.
The file will have its original line endings in your working directory
warning: LF will be replaced by CRLF in src/public/styles/scss/utilities/_helpers.scss.
The file will have its original line endings in your working directory
[1mdiff --git a/dist/public/js/client.js b/dist/public/js/client.js[m
[1mindex 262448d..0745098 100644[m
[1m--- a/dist/public/js/client.js[m
[1m+++ b/dist/public/js/client.js[m
[36m@@ -1,8 +1,8 @@[m
[31m-!function(n){var r={};function a(t){if(r[t])return r[t].exports;var e=r[t]={i:t,l:!1,exports:{}};return n[t].call(e.exports,e,e.exports,a),e.l=!0,e.exports}a.m=n,a.c=r,a.d=function(t,e,n){a.o(t,e)||Object.defineProperty(t,e,{enumerable:!0,get:n})},a.r=function(t){"undefined"!=typeof Symbol&&Symbol.toStringTag&&Object.defineProperty(t,Symbol.toStringTag,{value:"Module"}),Object.defineProperty(t,"__esModule",{value:!0})},a.t=function(e,t){if(1&t&&(e=a(e)),8&t)return e;if(4&t&&"object"==typeof e&&e&&e.__esModule)return e;var n=Object.create(null);if(a.r(n),Object.defineProperty(n,"default",{enumerable:!0,value:e}),2&t&&"string"!=typeof e)for(var r in e)a.d(n,r,function(t){return e[t]}.bind(null,r));return n},a.n=function(t){var e=t&&t.__esModule?function(){return t.default}:function(){return t};return a.d(e,"a",e),e},a.o=function(t,e){return Object.prototype.hasOwnProperty.call(t,e)},a.p="",a(a.s=0)}([function(module,exports,__webpack_require__){"use strict";var _typeof="function"==typeof Symbol&&"symbol"==typeof Symbol.iterator?function(t){return typeof t}:function(t){return t&&"function"==typeof Symbol&&t.constructor===Symbol&&t!==Symbol.prototype?"symbol":typeof t};!function(n){var r={};function a(t){if(r[t])return r[t].exports;var e=r[t]={i:t,l:!1,exports:{}};return n[t].call(e.exports,e,e.exports,a),e.l=!0,e.exports}a.m=n,a.c=r,a.d=function(t,e,n){a.o(t,e)||Object.defineProperty(t,e,{enumerable:!0,get:n})},a.r=function(t){"undefined"!=typeof Symbol&&Symbol.toStringTag&&Object.defineProperty(t,Symbol.toStringTag,{value:"Module"}),Object.defineProperty(t,"__esModule",{value:!0})},a.t=function(e,t){if(1&t&&(e=a(e)),8&t)return e;if(4&t&&"object"===(void 0===e?"undefined":_typeof(e))&&e&&e.__esModule)return e;var n=Object.create(null);if(a.r(n),Object.defineProperty(n,"default",{enumerable:!0,value:e}),2&t&&"string"!=typeof e)for(var r in e)a.d(n,r,function(t){return e[t]}.bind(null,r));return n},a.n=function(t){var e=t&&t.__esModule?function(){return t.default}:function(){return t};return a.d(e,"a",e),e},a.o=function(t,e){return Object.prototype.hasOwnProperty.call(t,e)},a.p="/",a(a.s=0)}({"./node_modules/jquery/dist/jquery.js":[m
[32m+[m[32m!function(n){var r={};function a(t){if(r[t])return r[t].exports;var e=r[t]={i:t,l:!1,exports:{}};return n[t].call(e.exports,e,e.exports,a),e.l=!0,e.exports}a.m=n,a.c=r,a.d=function(t,e,n){a.o(t,e)||Object.defineProperty(t,e,{enumerable:!0,get:n})},a.r=function(t){"undefined"!=typeof Symbol&&Symbol.toStringTag&&Object.defineProperty(t,Symbol.toStringTag,{value:"Module"}),Object.defineProperty(t,"__esModule",{value:!0})},a.t=function(e,t){if(1&t&&(e=a(e)),8&t)return e;if(4&t&&"object"==typeof e&&e&&e.__esModule)return e;var n=Object.create(null);if(a.r(n),Object.defineProperty(n,"default",{enumerable:!0,value:e}),2&t&&"string"!=typeof e)for(var r in e)a.d(n,r,function(t){return e[t]}.bind(null,r));return n},a.n=function(t){var e=t&&t.__esModule?function(){return t.default}:function(){return t};return a.d(e,"a",e),e},a.o=function(t,e){return Object.prototype.hasOwnProperty.call(t,e)},a.p="",a(a.s=0)}([function(module,exports,__webpack_require__){"use strict";var _typeof="function"==typeof Symbol&&"symbol"==typeof Symbol.iterator?function(t){return typeof t}:function(t){return t&&"function"==typeof Symbol&&t.constructor===Symbol&&t!==Symbol.prototype?"symbol":typeof t},w,x;function y(t){if(x[t])return x[t].exports;var e=x[t]={i:t,l:!1,exports:{}};return w[t].call(e.exports,e,e.exports,y),e.l=!0,e.exports}w={"./node_modules/jquery/dist/jquery.js":[m
 /*!********************************************!*\[m
     !*** ./node_modules/jquery/dist/jquery.js ***![m
     \********************************************/[m
[31m-/*! no static exports found */function node_modulesJqueryDistJqueryJs(module,exports,__webpack_require__){eval('var __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;/*!\n * jQuery JavaScript Library v3.3.1\n * https://jquery.com/\n *\n * Includes Sizzle.js\n * https://sizzlejs.com/\n *\n * Copyright JS Foundation and other contributors\n * Released under the MIT license\n * https://jquery.org/license\n *\n * Date: 2018-01-20T17:24Z\n */\n( function( global, factory ) {\n\n\t"use strict";\n\n\tif (  true && typeof module.exports === "object" ) {\n\n\t\t// For CommonJS and CommonJS-like environments where a proper `window`\n\t\t// is present, execute the factory and get jQuery.\n\t\t// For environments that do not have a `window` with a `document`\n\t\t// (such as Node.js), expose a factory as module.exports.\n\t\t// This accentuates the need for the creation of a real `window`.\n\t\t// e.g. var jQuery = require("jquery")(window);\n\t\t// See ticket #14549 for more info.\n\t\tmodule.exports = global.document ?\n\t\t\tfactory( global, true ) :\n\t\t\tfunction( w ) {\n\t\t\t\tif ( !w.document ) {\n\t\t\t\t\tthrow new Error( "jQuery requires a window with a document" );\n\t\t\t\t}\n\t\t\t\treturn factory( w );\n\t\t\t};\n\t} else {\n\t\tfactory( global );\n\t}\n\n// Pass this if window is not defined yet\n} )( typeof window !== "undefined" ? window : this, function( window, noGlobal ) {\n\n// Edge <= 12 - 13+, Firefox <=18 - 45+, IE 10 - 11, Safari 5.1 - 9+, iOS 6 - 9.1\n// throw exceptions when non-strict code (e.g., ASP.NET 4.5) accesses strict mode\n// arguments.callee.caller (trac-13335). But as of jQuery 3.0 (2016), strict mode should be common\n// enough that all such attempts are guarded in a try block.\n"use strict";\n\nvar arr = [];\n\nvar document = window.document;\n\nvar getProto = Object.getPrototypeOf;\n\nvar slice = arr.slice;\n\nvar concat = arr.concat;\n\nvar push = arr.push;\n\nvar indexOf = arr.indexOf;\n\nvar class2type = {};\n\nvar toString = class2type.toString;\n\nvar hasOwn = class2type.hasOwnProperty;\n\nvar fnToString = hasOwn.toString;\n\nvar ObjectFunctionString = fnToString.call( Object );\n\nvar support = {};\n\nvar isFunction = function isFunction( obj ) {\n\n      // Support: Chrome <=57, Firefox <=52\n      // In some browsers, typeof returns "function" for HTML <object> elements\n      // (i.e., `typeof document.createElement( "object" ) === "function"`).\n      // We don\'t want to classify *any* DOM node as a function.\n      return typeof obj === "function" && typeof obj.nodeType !== "number";\n  };\n\n\nvar isWindow = function isWindow( obj ) {\n\t\treturn obj != null && obj === obj.window;\n\t};\n\n\n\n\n\tvar preservedScriptAttributes = {\n\t\ttype: true,\n\t\tsrc: true,\n\t\tnoModule: true\n\t};\n\n\tfunction DOMEval( code, doc, node ) {\n\t\tdoc = doc || document;\n\n\t\tvar i,\n\t\t\tscript = doc.createElement( "script" );\n\n\t\tscript.text = code;\n\t\tif ( node ) {\n\t\t\tfor ( i in preservedScriptAttributes ) {\n\t\t\t\tif ( node[ i ] ) {\n\t\t\t\t\tscript[ i ] = node[ i ];\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t\tdoc.head.appendChild( script ).parentNode.removeChild( script );\n\t}\n\n\nfunction toType( obj ) {\n\tif ( obj == null ) {\n\t\treturn obj + "";\n\t}\n\n\t// Support: Android <=2.3 only (functionish RegExp)\n\treturn typeof obj === "object" || typeof obj === "function" ?\n\t\tclass2type[ toString.call( obj ) ] || "object" :\n\t\ttypeof obj;\n}\n/* global Symbol */\n// Defining this global in .eslintrc.json would create a danger of using the global\n// unguarded in another place, it seems safer to define global only for this module\n\n\n\nvar\n\tversion = "3.3.1",\n\n\t// Define a local copy of jQuery\n\tjQuery = function( selector, context ) {\n\n\t\t// The jQuery object is actually just the init constructor \'enhanced\'\n\t\t// Need init if jQuery is called (just allow error to be thrown if not included)\n\t\treturn new jQuery.fn.init( selector, context );\n\t},\n\n\t// Support: Android <=4.0 only\n\t// Make sure we trim BOM and NBSP\n\trtrim = /^[\\s\\uFEFF\\xA0]+|[\\s\\uFEFF\\xA0]+$/g;\n\njQuery.fn = jQuery.prototype = {\n\n\t// The current version of jQuery being used\n\tjquery: version,\n\n\tconstructor: jQuery,\n\n\t// The default length of a jQuery object is 0\n\tlength: 0,\n\n\ttoArray: function() {\n\t\treturn slice.call( this );\n\t},\n\n\t// Get the Nth element in the matched element set OR\n\t// Get the whole matched element set as a clean array\n\tget: function( num ) {\n\n\t\t// Return all the elements in a clean array\n\t\tif ( num == null ) {\n\t\t\treturn slice.call( this );\n\t\t}\n\n\t\t// Return just the one element from the set\n\t\treturn num < 0 ? this[ num + this.length ] : this[ num ];\n\t},\n\n\t// Take an array of elements and push it onto the stack\n\t// (returning the new matched element set)\n\tpushStack: function( elems ) {\n\n\t\t// Build a new jQuery matched element set\n\t\tvar ret = jQuery.merge( this.constructor(), elems );\n\n\t\t// Add the old object onto the stack (as a reference)\n\t\tret.prevObject = this;\n\n\t\t// Return the newly-formed element set\n\t\treturn ret;\n\t},\n\n\t// Execute a callback for every element in the matched set.\n\teach: function( callback ) {\n\t\treturn jQuery.each( this, callback );\n\t},\n\n\tmap: function( callback ) {\n\t\treturn this.pushStack( jQuery.map( this, function( elem, i ) {\n\t\t\treturn callback.call( elem, i, elem );\n\t\t} ) );\n\t},\n\n\tslice: function() {\n\t\treturn this.pushStack( slice.apply( this, arguments ) );\n\t},\n\n\tfirst: function() {\n\t\treturn this.eq( 0 );\n\t},\n\n\tlast: function() {\n\t\treturn this.eq( -1 );\n\t},\n\n\teq: function( i ) {\n\t\tvar len = this.length,\n\t\t\tj = +i + ( i < 0 ? len : 0 );\n\t\treturn this.pushStack( j >= 0 && j < len ? [ this[ j ] ] : [] );\n\t},\n\n\tend: function() {\n\t\treturn this.prevObject || this.constructor();\n\t},\n\n\t// For internal use only.\n\t// Behaves like an Array\'s method, not like a jQuery method.\n\tpush: push,\n\tsort: arr.sort,\n\tsplice: arr.splice\n};\n\njQuery.extend = jQuery.fn.extend = function() {\n\tvar options, name, src, copy, copyIsArray, clone,\n\t\ttarget = arguments[ 0 ] || {},\n\t\ti = 1,\n\t\tlength = arguments.length,\n\t\tdeep = false;\n\n\t// Handle a deep copy situation\n\tif ( typeof target === "boolean" ) {\n\t\tdeep = target;\n\n\t\t// Skip the boolean and the target\n\t\ttarget = arguments[ i ] || {};\n\t\ti++;\n\t}\n\n\t// Handle case when target is a string or something (possible in deep copy)\n\tif ( typeof target !== "object" && !isFunction( target ) ) {\n\t\ttarget = {};\n\t}\n\n\t// Extend jQuery itself if only one argument is passed\n\tif ( i === length ) {\n\t\ttarget = this;\n\t\ti--;\n\t}\n\n\tfor ( ; i < length; i++ ) {\n\n\t\t// Only deal with non-null/undefined values\n\t\tif ( ( options = arguments[ i ] ) != null ) {\n\n\t\t\t// Extend the base object\n\t\t\tfor ( name in options ) {\n\t\t\t\tsrc = target[ name ];\n\t\t\t\tcopy = options[ name ];\n\n\t\t\t\t// Prevent never-ending loop\n\t\t\t\tif ( target === copy ) {\n\t\t\t\t\tcontinue;\n\t\t\t\t}\n\n\t\t\t\t// Recurse if we\'re merging plain objects or arrays\n\t\t\t\tif ( deep && copy && ( jQuery.isPlainObject( copy ) ||\n\t\t\t\t\t( copyIsArray = Array.isArray( copy ) ) ) ) {\n\n\t\t\t\t\tif ( copyIsArray ) {\n\t\t\t\t\t\tcopyIsArray = false;\n\t\t\t\t\t\tclone = src && Array.isArray( src ) ? src : [];\n\n\t\t\t\t\t} else {\n\t\t\t\t\t\tclone = src && jQuery.isPlainObject( src ) ? src : {};\n\t\t\t\t\t}\n\n\t\t\t\t\t// Never move original objects, clone them\n\t\t\t\t\ttarget[ name ] = jQuery.extend( deep, clone, copy );\n\n\t\t\t\t// Don\'t bring in undefined values\n\t\t\t\t} else if ( copy !== undefined ) {\n\t\t\t\t\ttarget[ name ] = copy;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\t// Return the modified object\n\treturn target;\n};\n\njQuery.extend( {\n\n\t// Unique for each copy of jQuery on the page\n\texpando: "jQuery" + ( version + Math.random() ).replace( /\\D/g, "" ),\n\n\t// Assume jQuery is ready without the ready module\n\tisReady: true,\n\n\terror: function( msg ) {\n\t\tthrow new Error( msg );\n\t},\n\n\tnoop: function() {},\n\n\tisPlainObject: function( obj ) {\n\t\tvar proto, Ctor;\n\n\t\t// Detect obvious negatives\n\t\t// Use toString instead of jQuery.type to catch host objects\n\t\tif ( !obj || toString.call( obj ) !== "[object Object]" ) {\n\t\t\treturn false;\n\t\t}\n\n\t\tproto = getProto( obj );\n\n\t\t// Objects with no prototype (e.g., `Object.create( null )`) are plain\n\t\tif ( !proto ) {\n\t\t\treturn true;\n\t\t}\n\n\t\t// Objects with prototype are plain iff they were constructed by a global Object function\n\t\tCtor = hasOwn.call( proto, "constructor" ) && proto.constructor;\n\t\treturn typeof Ctor === "function" && fnToString.call( Ctor ) === ObjectFunctionString;\n\t},\n\n\tisEmptyObject: function( obj ) {\n\n\t\t/* eslint-disable no-unused-vars */\n\t\t// See https://github.com/eslint/eslint/issues/6125\n\t\tvar name;\n\n\t\tfor ( name in obj ) {\n\t\t\treturn false;\n\t\t}\n\t\treturn true;\n\t},\n\n\t// Evaluates a script in a global context\n\tglobalEval: function( code ) {\n\t\tDOMEval( code );\n\t},\n\n\teach: function( obj, callback ) {\n\t\tvar length, i = 0;\n\n\t\tif ( isArrayLike( obj ) ) {\n\t\t\tlength = obj.length;\n\t\t\tfor ( ; i < length; i++ ) {\n\t\t\t\tif ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {\n\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t}\n\t\t} else {\n\t\t\tfor ( i in obj ) {\n\t\t\t\tif ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {\n\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn obj;\n\t},\n\n\t// Support: Android <=4.0 only\n\ttrim: function( text ) {\n\t\treturn text == null ?\n\t\t\t"" :\n\t\t\t( text + "" ).replace( rtrim, "" );\n\t},\n\n\t// results is for internal usage only\n\tmakeArray: function( arr, results ) {\n\t\tvar ret = results || [];\n\n\t\tif ( arr != null ) {\n\t\t\tif ( isArrayLike( Object( arr ) ) ) {\n\t\t\t\tjQuery.merge( ret,\n\t\t\t\t\ttypeof arr === "string" ?\n\t\t\t\t\t[ arr ] : arr\n\t\t\t\t);\n\t\t\t} else {\n\t\t\t\tpush.call( ret, arr );\n\t\t\t}\n\t\t}\n\n\t\treturn ret;\n\t},\n\n\tinArray: function( elem, arr, i ) {\n\t\treturn arr == null ? -1 : indexOf.call( arr, elem, i );\n\t},\n\n\t// Support: Android <=4.0 only, PhantomJS 1 only\n\t// push.apply(_, arraylike) throws on ancient WebKit\n\tmerge: function( first, second ) {\n\t\tvar len = +second.length,\n\t\t\tj = 0,\n\t\t\ti = first.length;\n\n\t\tfor ( ; j < len; j++ ) {\n\t\t\tfirst[ i++ ] = second[ j ];\n\t\t}\n\n\t\tfirst.length = i;\n\n\t\treturn first;\n\t},\n\n\tgrep: function( elems, callback, invert ) {\n\t\tvar callbackInverse,\n\t\t\tmatches = [],\n\t\t\ti = 0,\n\t\t\tlength = elems.length,\n\t\t\tcallbackExpect = !invert;\n\n\t\t// Go through the array, only saving the items\n\t\t// that pass the validator function\n\t\tfor ( ; i < length; i++ ) {\n\t\t\tcallbackInverse = !callback( elems[ i ], i );\n\t\t\tif ( callbackInverse !== callbackExpect ) {\n\t\t\t\tmatches.push( elems[ i ] );\n\t\t\t}\n\t\t}\n\n\t\treturn matches;\n\t},\n\n\t// arg is for internal usage only\n\tmap: function( elems, callback, arg ) {\n\t\tvar length, value,\n\t\t\ti = 0,\n\t\t\tret = [];\n\n\t\t// Go through the array, translating each of the items to their new values\n\t\tif ( isArrayLike( elems ) ) {\n\t\t\tlength = elems.length;\n\t\t\tfor ( ; i < length; i++ ) {\n\t\t\t\tvalue = callback( elems[ i ], i, arg );\n\n\t\t\t\tif ( value != null ) {\n\t\t\t\t\tret.push( value );\n\t\t\t\t}\n\t\t\t}\n\n\t\t// Go through every key on the object,\n\t\t} else {\n\t\t\tfor ( i in elems ) {\n\t\t\t\tvalue = callback( elems[ i ], i, arg );\n\n\t\t\t\tif ( value != null ) {\n\t\t\t\t\tret.push( value );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\t// Flatten any nested arrays\n\t\treturn concat.apply( [], ret );\n\t},\n\n\t// A global GUID counter for objects\n\tguid: 1,\n\n\t// jQuery.support is not used in Core but other projects attach their\n\t// properties to it so it needs to exist.\n\tsupport: support\n} );\n\nif ( typeof Symbol === "function" ) {\n\tjQuery.fn[ Symbol.iterator ] = arr[ Symbol.iterator ];\n}\n\n// Populate the class2type map\njQuery.each( "Boolean Number String Function Array Date RegExp Object Error Symbol".split( " " ),\nfunction( i, name ) {\n\tclass2type[ "[object " + name + "]" ] = name.toLowerCase();\n} );\n\nfunction isArrayLike( obj ) {\n\n\t// Support: real iOS 8.2 only (not reproducible in simulator)\n\t// `in` check used to prevent JIT error (gh-2145)\n\t// hasOwn isn\'t used here due to false negatives\n\t// regarding Nodelist length in IE\n\tvar length = !!obj && "length" in obj && obj.length,\n\t\ttype = toType( obj );\n\n\tif ( isFunction( obj ) || isWindow( obj ) ) {\n\t\treturn false;\n\t}\n\n\treturn type === "array" || length === 0 ||\n\t\ttypeof length === "number" && length > 0 && ( length - 1 ) in obj;\n}\nvar Sizzle =\n/*!\n * Sizzle CSS Selector Engine v2.3.3\n * https://sizzlejs.com/\n *\n * Copyright jQuery Foundation and other contributors\n * Released under the MIT license\n * http://jquery.org/license\n *\n * Date: 2016-08-08\n */\n(function( window ) {\n\nvar i,\n\tsupport,\n\tExpr,\n\tgetText,\n\tisXML,\n\ttokenize,\n\tcompile,\n\tselect,\n\toutermostContext,\n\tsortInput,\n\thasDuplicate,\n\n\t// Local document vars\n\tsetDocument,\n\tdocument,\n\tdocElem,\n\tdocumentIsHTML,\n\trbuggyQSA,\n\trbuggyMatches,\n\tmatches,\n\tcontains,\n\n\t// Instance-specific data\n\texpando = "sizzle" + 1 * new Date(),\n\tpreferredDoc = window.document,\n\tdirruns = 0,\n\tdone = 0,\n\tclassCache = createCache(),\n\ttokenCache = createCache(),\n\tcompilerCache = createCache(),\n\tsortOrder = function( a, b ) {\n\t\tif ( a === b ) {\n\t\t\thasDuplicate = true;\n\t\t}\n\t\treturn 0;\n\t},\n\n\t// Instance methods\n\thasOwn = ({}).hasOwnProperty,\n\tarr = [],\n\tpop = arr.pop,\n\tpush_native = arr.push,\n\tpush = arr.push,\n\tslice = arr.slice,\n\t// Use a stripped-down indexOf as it\'s faster than native\n\t// https://jsperf.com/thor-indexof-vs-for/5\n\tindexOf = function( list, elem ) {\n\t\tvar i = 0,\n\t\t\tlen = list.length;\n\t\tfor ( ; i < len; i++ ) {\n\t\t\tif ( list[i] === elem ) {\n\t\t\t\treturn i;\n\t\t\t}\n\t\t}\n\t\treturn -1;\n\t},\n\n\tbooleans = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped",\n\n\t// Regular expressions\n\n\t// http://www.w3.org/TR/css3-selectors/#whitespace\n\twhitespace = "[\\\\x20\\\\t\\\\r\\\\n\\\\f]",\n\n\t// http://www.w3.org/TR/CSS21/syndata.html#value-def-identifier\n\tidentifier = "(?:\\\\\\\\.|[\\\\w-]|[^\\0-\\\\xa0])+",\n\n\t// Attribute selectors: http://www.w3.org/TR/selectors/#attribute-selectors\n\tattributes = "\\\\[" + whitespace + "*(" + identifier + ")(?:" + whitespace +\n\t\t// Operator (capture 2)\n\t\t"*([*^$|!~]?=)" + whitespace +\n\t\t// "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"\n\t\t"*(?:\'((?:\\\\\\\\.|[^\\\\\\\\\'])*)\'|\\"((?:\\\\\\\\.|[^\\\\\\\\\\"])*)\\"|(" + identifier + "))|)" + whitespace +\n\t\t"*\\\\]",\n\n\tpseudos = ":(" + identifier + ")(?:\\\\((" +\n\t\t// To reduce the number of selectors needing tokenize in the preFilter, prefer arguments:\n\t\t// 1. quoted (capture 3; capture 4 or capture 5)\n\t\t"(\'((?:\\\\\\\\.|[^\\\\\\\\\'])*)\'|\\"((?:\\\\\\\\.|[^\\\\\\\\\\"])*)\\")|" +\n\t\t// 2. simple (capture 6)\n\t\t"((?:\\\\\\\\.|[^\\\\\\\\()[\\\\]]|" + attributes + ")*)|" +\n\t\t// 3. anything else (capture 2)\n\t\t".*" +\n\t\t")\\\\)|)",\n\n\t// Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter\n\trwhitespace = new RegExp( whitespace + "+", "g" ),\n\trtrim = new RegExp( "^" + whitespace + "+|((?:^|[^\\\\\\\\])(?:\\\\\\\\.)*)" + whitespace + "+$", "g" ),\n\n\trcomma = new RegExp( "^" + whitespace + "*," + whitespace + "*" ),\n\trcombinators = new RegExp( "^" + whitespace + "*([>+~]|" + whitespace + ")" + whitespace + "*" ),\n\n\trattributeQuotes = new RegExp( "=" + whitespace + "*([^\\\\]\'\\"]*?)" + whitespace + "*\\\\]", "g" ),\n\n\trpseudo = new RegExp( pseudos ),\n\tridentifier = new RegExp( "^" + identifier + "$" ),\n\n\tmatchExpr = {\n\t\t"ID": new RegExp( "^#(" + identifier + ")" ),\n\t\t"CLASS": new RegExp( "^\\\\.(" + identifier + ")" ),\n\t\t"TAG": new RegExp( "^(" + identifier + "|[*])" ),\n\t\t"ATTR": new RegExp( "^" + attributes ),\n\t\t"PSEUDO": new RegExp( "^" + pseudos ),\n\t\t"CHILD": new RegExp( "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\\\(" + whitespace +\n\t\t\t"*(even|odd|(([+-]|)(\\\\d*)n|)" + whitespace + "*(?:([+-]|)" + whitespace +\n\t\t\t"*(\\\\d+)|))" + whitespace + "*\\\\)|)", "i" ),\n\t\t"bool": new RegExp( "^(?:" + booleans + ")$", "i" ),\n\t\t// For use in libraries implementing .is()\n\t\t// We use this for POS matching in `select`\n\t\t"needsContext": new RegExp( "^" + whitespace + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\\\(" +\n\t\t\twhitespace + "*((?:-\\\\d)?\\\\d*)" + whitespace + "*\\\\)|)(?=[^-]|$)", "i" )\n\t},\n\n\trinputs = /^(?:input|select|textarea|button)$/i,\n\trheader = /^h\\d$/i,\n\n\trnative = /^[^{]+\\{\\s*\\[native \\w/,\n\n\t// Easily-parseable/retrievable ID or TAG or CLASS selectors\n\trquickExpr = /^(?:#([\\w-]+)|(\\w+)|\\.([\\w-]+))$/,\n\n\trsibling = /[+~]/,\n\n\t// CSS escapes\n\t// http://www.w3.org/TR/CSS21/syndata.html#escaped-characters\n\trunescape = new RegExp( "\\\\\\\\([\\\\da-f]{1,6}" + whitespace + "?|(" + whitespace + ")|.)", "ig" ),\n\tfunescape = function( _, escaped, escapedWhitespace ) {\n\t\tvar high = "0x" + escaped - 0x10000;\n\t\t// NaN means non-codepoint\n\t\t// Support: Firefox<24\n\t\t// Workaround erroneous numeric interpretation of +"0x"\n\t\treturn high !== high || escapedWhitespace ?\n\t\t\tescaped :\n\t\t\thigh < 0 ?\n\t\t\t\t// BMP codepoint\n\t\t\t\tString.fromCharCode( high + 0x10000 ) :\n\t\t\t\t// Supplemental Plane codepoint (surrogate pair)\n\t\t\t\tString.fromCharCode( high >> 10 | 0xD800, high & 0x3FF | 0xDC00 );\n\t},\n\n\t// CSS string/identifier serialization\n\t// https://drafts.csswg.org/cssom/#common-serializing-idioms\n\trcssescape = /([\\0-\\x1f\\x7f]|^-?\\d)|^-$|[^\\0-\\x1f\\x7f-\\uFFFF\\w-]/g,\n\tfcssescape = function( ch, asCodePoint ) {\n\t\tif ( asCodePoint ) {\n\n\t\t\t// U+0000 NULL becomes U+FFFD REPLACEMENT CHARACTER\n\t\t\tif ( ch === "\\0" ) {\n\t\t\t\treturn "\\uFFFD";\n\t\t\t}\n\n\t\t\t// Control characters and (dependent upon position) numbers get escaped as code points\n\t\t\treturn ch.slice( 0, -1 ) + "\\\\" + ch.charCodeAt( ch.length - 1 ).toString( 16 ) + " ";\n\t\t}\n\n\t\t// Other potentially-special ASCII characters get backslash-escaped\n\t\treturn "\\\\" + ch;\n\t},\n\n\t// Used for iframes\n\t// See setDocument()\n\t// Removing the function wrapper causes a "Permission Denied"\n\t// error in IE\n\tunloadHandler = function() {\n\t\tsetDocument();\n\t},\n\n\tdisabledAncestor = addCombinator(\n\t\tfunction( elem ) {\n\t\t\treturn elem.disabled === true && ("form" in elem || "label" in elem);\n\t\t},\n\t\t{ dir: "parentNode", next: "legend" }\n\t);\n\n// Optimize for push.apply( _, NodeList )\ntry {\n\tpush.apply(\n\t\t(arr = slice.call( preferredDoc.childNodes )),\n\t\tpreferredDoc.childNodes\n\t);\n\t// Support: Android<4.0\n\t// Detect silently failing push.apply\n\tarr[ preferredDoc.childNodes.length ].nodeType;\n} catch ( e ) {\n\tpush = { apply: arr.length ?\n\n\t\t// Leverage slice if possible\n\t\tfunction( target, els ) {\n\t\t\tpush_native.apply( target, slice.call(els) );\n\t\t} :\n\n\t\t// Support: IE<9\n\t\t// Otherwise append directly\n\t\tfunction( target, els ) {\n\t\t\tvar j = target.length,\n\t\t\t\ti = 0;\n\t\t\t// Can\'t trust NodeList.length\n\t\t\twhile ( (target[j++] = els[i++]) ) {}\n\t\t\ttarget.length = j - 1;\n\t\t}\n\t};\n}\n\nfunction Sizzle( selector, context, results, seed ) {\n\tvar m, i, elem, nid, match, groups, newSelector,\n\t\tnewContext = context && context.ownerDocument,\n\n\t\t// nodeType defaults to 9, since context defaults to document\n\t\tnodeType = context ? context.nodeType : 9;\n\n\tresults = results || [];\n\n\t// Return early from calls with invalid selector or context\n\tif ( typeof selector !== "string" || !selector ||\n\t\tnodeType !== 1 && nodeType !== 9 && nodeType !== 11 ) {\n\n\t\treturn results;\n\t}\n\n\t// Try to shortcut find operations (as opposed to filters) in HTML documents\n\tif ( !seed ) {\n\n\t\tif ( ( context ? context.ownerDocument || context : preferredDoc ) !== document ) {\n\t\t\tsetDocument( context );\n\t\t}\n\t\tcontext = context || document;\n\n\t\tif ( documentIsHTML ) {\n\n\t\t\t// If the selector is sufficiently simple, try using a "get*By*" DOM method\n\t\t\t// (excepting DocumentFragment context, where the methods don\'t exist)\n\t\t\tif ( nodeType !== 11 && (match = rquickExpr.exec( selector )) ) {\n\n\t\t\t\t// ID selector\n\t\t\t\tif ( (m = match[1]) ) {\n\n\t\t\t\t\t// Document context\n\t\t\t\t\tif ( nodeType === 9 ) {\n\t\t\t\t\t\tif ( (elem = context.getElementById( m )) ) {\n\n\t\t\t\t\t\t\t// Support: IE, Opera, Webkit\n\t\t\t\t\t\t\t// TODO: identify versions\n\t\t\t\t\t\t\t// getElementById can match elements by name instead of ID\n\t\t\t\t\t\t\tif ( elem.id === m ) {\n\t\t\t\t\t\t\t\tresults.push( elem );\n\t\t\t\t\t\t\t\treturn results;\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\treturn results;\n\t\t\t\t\t\t}\n\n\t\t\t\t\t// Element context\n\t\t\t\t\t} else {\n\n\t\t\t\t\t\t// Support: IE, Opera, Webkit\n\t\t\t\t\t\t// TODO: identify versions\n\t\t\t\t\t\t// getElementById can match elements by name instead of ID\n\t\t\t\t\t\tif ( newContext && (elem = newContext.getElementById( m )) &&\n\t\t\t\t\t\t\tcontains( context, elem ) &&\n\t\t\t\t\t\t\telem.id === m ) {\n\n\t\t\t\t\t\t\tresults.push( elem );\n\t\t\t\t\t\t\treturn results;\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t// Type selector\n\t\t\t\t} else if ( match[2] ) {\n\t\t\t\t\tpush.apply( results, context.getElementsByTagName( selector ) );\n\t\t\t\t\treturn results;\n\n\t\t\t\t// Class selector\n\t\t\t\t} else if ( (m = match[3]) && support.getElementsByClassName &&\n\t\t\t\t\tcontext.getElementsByClassName ) {\n\n\t\t\t\t\tpush.apply( results, context.getElementsByClassName( m ) );\n\t\t\t\t\treturn results;\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Take advantage of querySelectorAll\n\t\t\tif ( support.qsa &&\n\t\t\t\t!compilerCache[ selector + " " ] &&\n\t\t\t\t(!rbuggyQSA || !rbuggyQSA.test( selector )) ) {\n\n\t\t\t\tif ( nodeType !== 1 ) {\n\t\t\t\t\tnewContext = context;\n\t\t\t\t\tnewSelector = selector;\n\n\t\t\t\t// qSA looks outside Element context, which is not what we want\n\t\t\t\t// Thanks to Andrew Dupont for this workaround technique\n\t\t\t\t// Support: IE <=8\n\t\t\t\t// Exclude object elements\n\t\t\t\t} else if ( context.nodeName.toLowerCase() !== "object" ) {\n\n\t\t\t\t\t// Capture the context ID, setting it first if necessary\n\t\t\t\t\tif ( (nid = context.getAttribute( "id" )) ) {\n\t\t\t\t\t\tnid = nid.replace( rcssescape, fcssescape );\n\t\t\t\t\t} else {\n\t\t\t\t\t\tcontext.setAttribute( "id", (nid = expando) );\n\t\t\t\t\t}\n\n\t\t\t\t\t// Prefix every selector in the list\n\t\t\t\t\tgroups = tokenize( selector );\n\t\t\t\t\ti = groups.length;\n\t\t\t\t\twhile ( i-- ) {\n\t\t\t\t\t\tgroups[i] = "#" + nid + " " + toSelector( groups[i] );\n\t\t\t\t\t}\n\t\t\t\t\tnewSelector = groups.join( "," );\n\n\t\t\t\t\t// Expand context for sibling selectors\n\t\t\t\t\tnewContext = rsibling.test( selector ) && testContext( context.parentNode ) ||\n\t\t\t\t\t\tcontext;\n\t\t\t\t}\n\n\t\t\t\tif ( newSelector ) {\n\t\t\t\t\ttry {\n\t\t\t\t\t\tpush.apply( results,\n\t\t\t\t\t\t\tnewContext.querySelectorAll( newSelector )\n\t\t\t\t\t\t);\n\t\t\t\t\t\treturn results;\n\t\t\t\t\t} catch ( qsaError ) {\n\t\t\t\t\t} finally {\n\t\t\t\t\t\tif ( nid === expando ) {\n\t\t\t\t\t\t\tcontext.removeAttribute( "id" );\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\t// All others\n\treturn select( selector.replace( rtrim, "$1" ), context, results, seed );\n}\n\n/**\n * Create key-value caches of limited size\n * @returns {function(string, object)} Returns the Object data after storing it on itself with\n *\tproperty name the (space-suffixed) string and (if the cache is larger than Expr.cacheLength)\n *\tdeleting the oldest entry\n */\nfunction createCache() {\n\tvar keys = [];\n\n\tfunction cache( key, value ) {\n\t\t// Use (key + " ") to avoid collision with native prototype properties (see Issue #157)\n\t\tif ( keys.push( key + " " ) > Expr.cacheLength ) {\n\t\t\t// Only keep the most recent entries\n\t\t\tdelete cache[ keys.shift() ];\n\t\t}\n\t\treturn (cache[ key + " " ] = value);\n\t}\n\treturn cache;\n}\n\n/**\n * Mark a function for special use by Sizzle\n * @param {Function} fn The function to mark\n */\nfunction markFunction( fn ) {\n\tfn[ expando ] = true;\n\treturn fn;\n}\n\n/**\n * Support testing using an element\n * @param {Function} fn Passed the created element and returns a boolean result\n */\nfunction assert( fn ) {\n\tvar el = document.createElement("fieldset");\n\n\ttry {\n\t\treturn !!fn( el );\n\t} catch (e) {\n\t\treturn false;\n\t} finally {\n\t\t// Remove from its parent by default\n\t\tif ( el.parentNode ) {\n\t\t\tel.parentNode.removeChild( el );\n\t\t}\n\t\t// release memory in IE\n\t\tel = null;\n\t}\n}\n\n/**\n * Adds the same handler for all of the specified attrs\n * @param {String} attrs Pipe-separated list of attributes\n * @param {Function} handler The method that will be applied\n */\nfunction addHandle( attrs, handler ) {\n\tvar arr = attrs.split("|"),\n\t\ti = arr.length;\n\n\twhile ( i-- ) {\n\t\tExpr.attrHandle[ arr[i] ] = handler;\n\t}\n}\n\n/**\n * Checks document order of two siblings\n * @param {Element} a\n * @param {Element} b\n * @returns {Number} Returns less than 0 if a precedes b, greater than 0 if a follows b\n */\nfunction siblingCheck( a, b ) {\n\tvar cur = b && a,\n\t\tdiff = cur && a.nodeType === 1 && b.nodeType === 1 &&\n\t\t\ta.sourceIndex - b.sourceIndex;\n\n\t// Use IE sourceIndex if available on both nodes\n\tif ( diff ) {\n\t\treturn diff;\n\t}\n\n\t// Check if b follows a\n\tif ( cur ) {\n\t\twhile ( (cur = cur.nextSibling) ) {\n\t\t\tif ( cur === b ) {\n\t\t\t\treturn -1;\n\t\t\t}\n\t\t}\n\t}\n\n\treturn a ? 1 : -1;\n}\n\n/**\n * Returns a function to use in pseudos for input types\n * @param {String} type\n */\nfunction createInputPseudo( type ) {\n\treturn function( elem ) {\n\t\tvar name = elem.nodeName.toLowerCase();\n\t\treturn name === "input" && elem.type === type;\n\t};\n}\n\n/**\n * Returns a function to use in pseudos for buttons\n * @param {String} type\n */\nfunction createButtonPseudo( type ) {\n\treturn function( elem ) {\n\t\tvar name = elem.nodeName.toLowerCase();\n\t\treturn (name === "input" || name === "button") && elem.type === type;\n\t};\n}\n\n/**\n * Returns a function to use in pseudos for :enabled/:disabled\n * @param {Boolean} disabled true for :disabled; false for :enabled\n */\nfunction createDisabledPseudo( disabled ) {\n\n\t// Known :disabled false positives: fieldset[disabled] > legend:nth-of-type(n+2) :can-disable\n\treturn function( elem ) {\n\n\t\t// Only certain elements can match :enabled or :disabled\n\t\t// https://html.spec.whatwg.org/multipage/scripting.html#selector-enabled\n\t\t// https://html.spec.whatwg.org/multipage/scripting.html#selector-disabled\n\t\tif ( "form" in elem ) {\n\n\t\t\t// Check for inherited disabledness on relevant non-disabled elements:\n\t\t\t// * listed form-associated elements in a disabled fieldset\n\t\t\t//   https://html.spec.whatwg.org/multipage/forms.html#category-listed\n\t\t\t//   https://html.spec.whatwg.org/multipage/forms.html#concept-fe-disabled\n\t\t\t// * option elements in a disabled optgroup\n\t\t\t//   https://html.spec.whatwg.org/multipage/forms.html#concept-option-disabled\n\t\t\t// All such elements have a "form" property.\n\t\t\tif ( elem.parentNode && elem.disabled === false ) {\n\n\t\t\t\t// Option elements defer to a parent optgroup if present\n\t\t\t\tif ( "label" in elem ) {\n\t\t\t\t\tif ( "label" in elem.parentNode ) {\n\t\t\t\t\t\treturn elem.parentNode.disabled === disabled;\n\t\t\t\t\t} else {\n\t\t\t\t\t\treturn elem.disabled === disabled;\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\t// Support: IE 6 - 11\n\t\t\t\t// Use the isDisabled shortcut property to check for disabled fieldset ancestors\n\t\t\t\treturn elem.isDisabled === disabled ||\n\n\t\t\t\t\t// Where there is no isDisabled, check manually\n\t\t\t\t\t/* jshint -W018 */\n\t\t\t\t\telem.isDisabled !== !disabled &&\n\t\t\t\t\t\tdisabledAncestor( elem ) === disabled;\n\t\t\t}\n\n\t\t\treturn elem.disabled === disabled;\n\n\t\t// Try to winnow out elements that can\'t be disabled before trusting the disabled property.\n\t\t// Some victims get caught in our net (label, legend, menu, track), but it shouldn\'t\n\t\t// even exist on them, let alone have a boolean value.\n\t\t} else if ( "label" in elem ) {\n\t\t\treturn elem.disabled === disabled;\n\t\t}\n\n\t\t// Remaining elements are neither :enabled nor :disabled\n\t\treturn false;\n\t};\n}\n\n/**\n * Returns a function to use in pseudos for positionals\n * @param {Function} fn\n */\nfunction createPositionalPseudo( fn ) {\n\treturn markFunction(function( argument ) {\n\t\targument = +argument;\n\t\treturn markFunction(function( seed, matches ) {\n\t\t\tvar j,\n\t\t\t\tmatchIndexes = fn( [], seed.length, argument ),\n\t\t\t\ti = matchIndexes.length;\n\n\t\t\t// Match elements found at the specified indexes\n\t\t\twhile ( i-- ) {\n\t\t\t\tif ( seed[ (j = matchIndexes[i]) ] ) {\n\t\t\t\t\tseed[j] = !(matches[j] = seed[j]);\n\t\t\t\t}\n\t\t\t}\n\t\t});\n\t});\n}\n\n/**\n * Checks a node for validity as a Sizzle context\n * @param {Element|Object=} context\n * @returns {Element|Object|Boolean} The input node if acceptable, otherwise a falsy value\n */\nfunction testContext( context ) {\n\treturn context && typeof context.getElementsByTagName !== "undefined" && context;\n}\n\n// Expose support vars for convenience\nsupport = Sizzle.support = {};\n\n/**\n * Detects XML nodes\n * @param {Element|Object} elem An element or a document\n * @returns {Boolean} True iff elem is a non-HTML XML node\n */\nisXML = Sizzle.isXML = function( elem ) {\n\t// documentElement is verified for cases where it doesn\'t yet exist\n\t// (such as loading iframes in IE - #4833)\n\tvar documentElement = elem && (elem.ownerDocument || elem).documentElement;\n\treturn documentElement ? documentElement.nodeName !== "HTML" : false;\n};\n\n/**\n * Sets document-related variables once based on the current document\n * @param {Element|Object} [doc] An element or document object to use to set the document\n * @returns {Object} Returns the current document\n */\nsetDocument = Sizzle.setDocument = function( node ) {\n\tvar hasCompare, subWindow,\n\t\tdoc = node ? node.ownerDocument || node : preferredDoc;\n\n\t// Return early if doc is invalid or already selected\n\tif ( doc === document || doc.nodeType !== 9 || !doc.documentElement ) {\n\t\treturn document;\n\t}\n\n\t// Update global variables\n\tdocument = doc;\n\tdocElem = document.documentElement;\n\tdocumentIsHTML = !isXML( document );\n\n\t// Support: IE 9-11, Edge\n\t// Accessing iframe documents after unload throws "permission denied" errors (jQuery #13936)\n\tif ( preferredDoc !== document &&\n\t\t(subWindow = document.defaultView) && subWindow.top !== subWindow ) {\n\n\t\t// Support: IE 11, Edge\n\t\tif ( subWindow.addEventListener ) {\n\t\t\tsubWindow.addEventListener( "unload", unloadHandler, false );\n\n\t\t// Support: IE 9 - 10 only\n\t\t} else if ( subWindow.attachEvent ) {\n\t\t\tsubWindow.attachEvent( "onunload", unloadHandler );\n\t\t}\n\t}\n\n\t/* Attributes\n\t---------------------------------------------------------------------- */\n\n\t// Support: IE<8\n\t// Verify that getAttribute really returns attributes and not properties\n\t// (excepting IE8 booleans)\n\tsupport.attributes = assert(function( el ) {\n\t\tel.className = "i";\n\t\treturn !el.getAttribute("className");\n\t});\n\n\t/* getElement(s)By*\n\t---------------------------------------------------------------------- */\n\n\t// Check if getElementsByTagName("*") returns only elements\n\tsupport.getElementsByTagName = assert(function( el ) {\n\t\tel.appendChild( document.createComment("") );\n\t\treturn !el.getElementsByTagName("*").length;\n\t});\n\n\t// Support: IE<9\n\tsupport.getElementsByClassName = rnative.test( document.getElementsByClassName );\n\n\t// Support: IE<10\n\t// Check if getElementById returns elements by name\n\t// The broken getElementById methods don\'t pick up programmatically-set names,\n\t// so use a roundabout getElementsByName test\n\tsupport.getById = assert(function( el ) {\n\t\tdocElem.appendChild( el ).id = expando;\n\t\treturn !document.getElementsByName || !document.getElementsByName( expando ).length;\n\t});\n\n\t// ID filter and find\n\tif ( support.getById ) {\n\t\tExpr.filter["ID"] = function( id ) {\n\t\t\tvar attrId = id.replace( runescape, funescape );\n\t\t\treturn function( elem ) {\n\t\t\t\treturn elem.getAttribute("id") === attrId;\n\t\t\t};\n\t\t};\n\t\tExpr.find["ID"] = function( id, context ) {\n\t\t\tif ( typeof context.getElementById !== "undefined" && documentIsHTML ) {\n\t\t\t\tvar elem = context.getElementById( id );\n\t\t\t\treturn elem ? [ elem ] : [];\n\t\t\t}\n\t\t};\n\t} else {\n\t\tExpr.filter["ID"] =  function( id ) {\n\t\t\tvar attrId = id.replace( runescape, funescape );\n\t\t\treturn function( elem ) {\n\t\t\t\tvar node = typeof elem.getAttributeNode !== "undefined" &&\n\t\t\t\t\telem.getAttributeNode("id");\n\t\t\t\treturn node && node.value === attrId;\n\t\t\t};\n\t\t};\n\n\t\t// Support: IE 6 - 7 only\n\t\t// getElementById is not reliable as a find shortcut\n\t\tExpr.find["ID"] = function( id, context ) {\n\t\t\tif ( typeof context.getElementById !== "undefined" && documentIsHTML ) {\n\t\t\t\tvar node, i, elems,\n\t\t\t\t\telem = context.getElementById( id );\n\n\t\t\t\tif ( elem ) {\n\n\t\t\t\t\t// Verify the id attribute\n\t\t\t\t\tnode = elem.getAttributeNode("id");\n\t\t\t\t\tif ( node && node.value === id ) {\n\t\t\t\t\t\treturn [ elem ];\n\t\t\t\t\t}\n\n\t\t\t\t\t// Fall back on getElementsByName\n\t\t\t\t\telems = context.getElementsByName( id );\n\t\t\t\t\ti = 0;\n\t\t\t\t\twhile ( (elem = elems[i++]) ) {\n\t\t\t\t\t\tnode = elem.getAttributeNode("id");\n\t\t\t\t\t\tif ( node && node.value === id ) {\n\t\t\t\t\t\t\treturn [ elem ];\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\treturn [];\n\t\t\t}\n\t\t};\n\t}\n\n\t// Tag\n\tExpr.find["TAG"] = support.getElementsByTagName ?\n\t\tfunction( tag, context ) {\n\t\t\tif ( typeof context.getElementsByTagName !== "undefined" ) {\n\t\t\t\treturn context.getElementsByTagName( tag );\n\n\t\t\t// DocumentFragment nodes don\'t have gEBTN\n\t\t\t} else if ( support.qsa ) {\n\t\t\t\treturn context.querySelectorAll( tag );\n\t\t\t}\n\t\t} :\n\n\t\tfunction( tag, context ) {\n\t\t\tvar elem,\n\t\t\t\ttmp = [],\n\t\t\t\ti = 0,\n\t\t\t\t// By happy coincidence, a (broken) gEBTN appears on DocumentFragment nodes too\n\t\t\t\tresults = context.getElementsByTagName( tag );\n\n\t\t\t// Filter out possible comments\n\t\t\tif ( tag === "*" ) {\n\t\t\t\twhile ( (elem = results[i++]) ) {\n\t\t\t\t\tif ( elem.nodeType === 1 ) {\n\t\t\t\t\t\ttmp.push( elem );\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\treturn tmp;\n\t\t\t}\n\t\t\treturn results;\n\t\t};\n\n\t// Class\n\tExpr.find["CLASS"] = support.getElementsByClassName && function( className, context ) {\n\t\tif ( typeof context.getElementsByClassName !== "undefined" && documentIsHTML ) {\n\t\t\treturn context.getElementsByClassName( className );\n\t\t}\n\t};\n\n\t/* QSA/matchesSelector\n\t---------------------------------------------------------------------- */\n\n\t// QSA and matchesSelector support\n\n\t// matchesSelector(:active) reports false when true (IE9/Opera 11.5)\n\trbuggyMatches = [];\n\n\t// qSa(:focus) reports false when true (Chrome 21)\n\t// We allow this because of a bug in IE8/9 that throws an error\n\t// whenever `document.activeElement` is accessed on an iframe\n\t// So, we allow :focus to pass through QSA all the time to avoid the IE error\n\t// See https://bugs.jquery.com/ticket/13378\n\trbuggyQSA = [];\n\n\tif ( (support.qsa = rnative.test( document.querySelectorAll )) ) {\n\t\t// Build QSA regex\n\t\t// Regex strategy adopted from Diego Perini\n\t\tassert(function( el ) {\n\t\t\t// Select is set to empty string on purpose\n\t\t\t// This is to test IE\'s treatment of not explicitly\n\t\t\t// setting a boolean content attribute,\n\t\t\t// since its presence should be enough\n\t\t\t// https://bugs.jquery.com/ticket/12359\n\t\t\tdocElem.appendChild( el ).innerHTML = "<a id=\'" + expando + "\'></a>" +\n\t\t\t\t"<select id=\'" + expando + "-\\r\\\\\' msallowcapture=\'\'>" +\n\t\t\t\t"<option selected=\'\'></option></select>";\n\n\t\t\t// Support: IE8, Opera 11-12.16\n\t\t\t// Nothing should be selected when empty strings follow ^= or $= or *=\n\t\t\t// The test attribute must be unknown in Opera but "safe" for WinRT\n\t\t\t// https://msdn.microsoft.com/en-us/library/ie/hh465388.aspx#attribute_section\n\t\t\tif ( el.querySelectorAll("[msallowcapture^=\'\']").length ) {\n\t\t\t\trbuggyQSA.push( "[*^$]=" + whitespace + "*(?:\'\'|\\"\\")" );\n\t\t\t}\n\n\t\t\t// Support: IE8\n\t\t\t// Boolean attributes and "value" are not treated correctly\n\t\t\tif ( !el.querySelectorAll("[selected]").length ) {\n\t\t\t\trbuggyQSA.push( "\\\\[" + whitespace + "*(?:value|" + booleans + ")" );\n\t\t\t}\n\n\t\t\t// Support: Chrome<29, Android<4.4, Safari<7.0+, iOS<7.0+, PhantomJS<1.9.8+\n\t\t\tif ( !el.querySelectorAll( "[id~=" + expando + "-]" ).length ) {\n\t\t\t\trbuggyQSA.push("~=");\n\t\t\t}\n\n\t\t\t// Webkit/Opera - :checked should return selected option elements\n\t\t\t// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked\n\t\t\t// IE8 throws error here and will not see later tests\n\t\t\tif ( !el.querySelectorAll(":checked").length ) {\n\t\t\t\trbuggyQSA.push(":checked");\n\t\t\t}\n\n\t\t\t// Support: Safari 8+, iOS 8+\n\t\t\t// https://bugs.webkit.org/show_bug.cgi?id=136851\n\t\t\t// In-page `selector#id sibling-combinator selector` fails\n\t\t\tif ( !el.querySelectorAll( "a#" + expando + "+*" ).length ) {\n\t\t\t\trbuggyQSA.push(".#.+[+~]");\n\t\t\t}\n\t\t});\n\n\t\tassert(function( el ) {\n\t\t\tel.innerHTML = "<a href=\'\' disabled=\'disabled\'></a>" +\n\t\t\t\t"<select disabled=\'disabled\'><option/></select>";\n\n\t\t\t// Support: Windows 8 Native Apps\n\t\t\t// The type and name attributes are restricted during .innerHTML assignment\n\t\t\tvar input = document.createElement("input");\n\t\t\tinput.setAttribute( "type", "hidden" );\n\t\t\tel.appendChild( input ).setAttribute( "name", "D" );\n\n\t\t\t// Support: IE8\n\t\t\t// Enforce case-sensitivity of name attribute\n\t\t\tif ( el.querySelectorAll("[name=d]").length ) {\n\t\t\t\trbuggyQSA.push( "name" + whitespace + "*[*^$|!~]?=" );\n\t\t\t}\n\n\t\t\t// FF 3.5 - :enabled/:disabled and hidden elements (hidden elements are still enabled)\n\t\t\t// IE8 throws error here and will not see later tests\n\t\t\tif ( el.querySelectorAll(":enabled").length !== 2 ) {\n\t\t\t\trbuggyQSA.push( ":enabled", ":disabled" );\n\t\t\t}\n\n\t\t\t// Support: IE9-11+\n\t\t\t// IE\'s :disabled selector does not pick up the children of disabled fieldsets\n\t\t\tdocElem.appendChild( el ).disabled = true;\n\t\t\tif ( el.querySelectorAll(":disabled").length !== 2 ) {\n\t\t\t\trbuggyQSA.push( ":enabled", ":disabled" );\n\t\t\t}\n\n\t\t\t// Opera 10-11 does not throw on post-comma invalid pseudos\n\t\t\tel.querySelectorAll("*,:x");\n\t\t\trbuggyQSA.push(",.*:");\n\t\t});\n\t}\n\n\tif ( (support.matchesSelector = rnative.test( (matches = docElem.matches ||\n\t\tdocElem.webkitMatchesSelector ||\n\t\tdocElem.mozMatchesSelector ||\n\t\tdocElem.oMatchesSelector ||\n\t\tdocElem.msMatchesSelector) )) ) {\n\n\t\tassert(function( el ) {\n\t\t\t// Check to see if it\'s possible to do matchesSelector\n\t\t\t// on a disconnected node (IE 9)\n\t\t\tsupport.disconnectedMatch = matches.call( el, "*" );\n\n\t\t\t// This should fail with an exception\n\t\t\t// Gecko does not error, returns false instead\n\t\t\tmatches.call( el, "[s!=\'\']:x" );\n\t\t\trbuggyMatches.push( "!=", pseudos );\n\t\t});\n\t}\n\n\trbuggyQSA = rbuggyQSA.length && new RegExp( rbuggyQSA.join("|") );\n\trbuggyMatches = rbuggyMatches.length && new RegExp( rbuggyMatches.join("|") );\n\n\t/* Contains\n\t---------------------------------------------------------------------- */\n\thasCompare = rnative.test( docElem.compareDocumentPosition );\n\n\t// Element contains another\n\t// Purposefully self-exclusive\n\t// As in, an element does not contain itself\n\tcontains = hasCompare || rnative.test( docElem.contains ) ?\n\t\tfunction( a, b ) {\n\t\t\tvar adown = a.nodeType === 9 ? a.documentElement : a,\n\t\t\t\tbup = b && b.parentNode;\n\t\t\treturn a === bup || !!( bup && bup.nodeType === 1 && (\n\t\t\t\tadown.contains ?\n\t\t\t\t\tadown.contains( bup ) :\n\t\t\t\t\ta.compareDocumentPosition && a.compareDocumentPosition( bup ) & 16\n\t\t\t));\n\t\t} :\n\t\tfunction( a, b ) {\n\t\t\tif ( b ) {\n\t\t\t\twhile ( (b = b.parentNode) ) {\n\t\t\t\t\tif ( b === a ) {\n\t\t\t\t\t\treturn true;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn false;\n\t\t};\n\n\t/* Sorting\n\t---------------------------------------------------------------------- */\n\n\t// Document order sorting\n\tsortOrder = hasCompare ?\n\tfunction( a, b ) {\n\n\t\t// Flag for duplicate removal\n\t\tif ( a === b ) {\n\t\t\thasDuplicate = true;\n\t\t\treturn 0;\n\t\t}\n\n\t\t// Sort on method existence if only one input has compareDocumentPosition\n\t\tvar compare = !a.compareDocumentPosition - !b.compareDocumentPosition;\n\t\tif ( compare ) {\n\t\t\treturn compare;\n\t\t}\n\n\t\t// Calculate position if both inputs belong to the same document\n\t\tcompare = ( a.ownerDocument || a ) === ( b.ownerDocument || b ) ?\n\t\t\ta.compareDocumentPosition( b ) :\n\n\t\t\t// Otherwise we know they are disconnected\n\t\t\t1;\n\n\t\t// Disconnected nodes\n\t\tif ( compare & 1 ||\n\t\t\t(!support.sortDetached && b.compareDocumentPosition( a ) === compare) ) {\n\n\t\t\t// Choose the first element that is related to our preferred document\n\t\t\tif ( a === document || a.ownerDocument === preferredDoc && contains(preferredDoc, a) ) {\n\t\t\t\treturn -1;\n\t\t\t}\n\t\t\tif ( b === document || b.ownerDocument === preferredDoc && contains(preferredDoc, b) ) {\n\t\t\t\treturn 1;\n\t\t\t}\n\n\t\t\t// Maintain original order\n\t\t\treturn sortInput ?\n\t\t\t\t( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :\n\t\t\t\t0;\n\t\t}\n\n\t\treturn compare & 4 ? -1 : 1;\n\t} :\n\tfunction( a, b ) {\n\t\t// Exit early if the nodes are identical\n\t\tif ( a === b ) {\n\t\t\thasDuplicate = true;\n\t\t\treturn 0;\n\t\t}\n\n\t\tvar cur,\n\t\t\ti = 0,\n\t\t\taup = a.parentNode,\n\t\t\tbup = b.parentNode,\n\t\t\tap = [ a ],\n\t\t\tbp = [ b ];\n\n\t\t// Parentless nodes are either documents or disconnected\n\t\tif ( !aup || !bup ) {\n\t\t\treturn a === document ? -1 :\n\t\t\t\tb === document ? 1 :\n\t\t\t\taup ? -1 :\n\t\t\t\tbup ? 1 :\n\t\t\t\tsortInput ?\n\t\t\t\t( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :\n\t\t\t\t0;\n\n\t\t// If the nodes are siblings, we can do a quick check\n\t\t} else if ( aup === bup ) {\n\t\t\treturn siblingCheck( a, b );\n\t\t}\n\n\t\t// Otherwise we need full lists of their ancestors for comparison\n\t\tcur = a;\n\t\twhile ( (cur = cur.parentNode) ) {\n\t\t\tap.unshift( cur );\n\t\t}\n\t\tcur = b;\n\t\twhile ( (cur = cur.parentNode) ) {\n\t\t\tbp.unshift( cur );\n\t\t}\n\n\t\t// Walk down the tree looking for a discrepancy\n\t\twhile ( ap[i] === bp[i] ) {\n\t\t\ti++;\n\t\t}\n\n\t\treturn i ?\n\t\t\t// Do a sibling check if the nodes have a common ancestor\n\t\t\tsiblingCheck( ap[i], bp[i] ) :\n\n\t\t\t// Otherwise nodes in our document sort first\n\t\t\tap[i] === preferredDoc ? -1 :\n\t\t\tbp[i] === preferredDoc ? 1 :\n\t\t\t0;\n\t};\n\n\treturn document;\n};\n\nSizzle.matches = function( expr, elements ) {\n\treturn Sizzle( expr, null, null, elements );\n};\n\nSizzle.matchesSelector = function( elem, expr ) {\n\t// Set document vars if needed\n\tif ( ( elem.ownerDocument || elem ) !== document ) {\n\t\tsetDocument( elem );\n\t}\n\n\t// Make sure that attribute selectors are quoted\n\texpr = expr.replace( rattributeQuotes, "=\'$1\']" );\n\n\tif ( support.matchesSelector && documentIsHTML &&\n\t\t!compilerCache[ expr + " " ] &&\n\t\t( !rbuggyMatches || !rbuggyMatches.test( expr ) ) &&\n\t\t( !rbuggyQSA     || !rbuggyQSA.test( expr ) ) ) {\n\n\t\ttry {\n\t\t\tvar ret = matches.call( elem, expr );\n\n\t\t\t// IE 9\'s matchesSelector returns false on disconnected nodes\n\t\t\tif ( ret || support.disconnectedMatch ||\n\t\t\t\t\t// As well, disconnected nodes are said to be in a document\n\t\t\t\t\t// fragment in IE 9\n\t\t\t\t\telem.document && elem.document.nodeType !== 11 ) {\n\t\t\t\treturn ret;\n\t\t\t}\n\t\t} catch (e) {}\n\t}\n\n\treturn Sizzle( expr, document, null, [ elem ] ).length > 0;\n};\n\nSizzle.contains = function( context, elem ) {\n\t// Set document vars if needed\n\tif ( ( context.ownerDocument || context ) !== document ) {\n\t\tsetDocument( context );\n\t}\n\treturn contains( context, elem );\n};\n\nSizzle.attr = function( elem, name ) {\n\t// Set document vars if needed\n\tif ( ( elem.ownerDocument || elem ) !== document ) {\n\t\tsetDocument( elem );\n\t}\n\n\tvar fn = Expr.attrHandle[ name.toLowerCase() ],\n\t\t// Don\'t get fooled by Object.prototype properties (jQuery #13807)\n\t\tval = fn && hasOwn.call( Expr.attrHandle, name.toLowerCase() ) ?\n\t\t\tfn( elem, name, !documentIsHTML ) :\n\t\t\tundefined;\n\n\treturn val !== undefined ?\n\t\tval :\n\t\tsupport.attributes || !documentIsHTML ?\n\t\t\telem.getAttribute( name ) :\n\t\t\t(val = elem.getAttributeNode(name)) && val.specified ?\n\t\t\t\tval.value :\n\t\t\t\tnull;\n};\n\nSizzle.escape = function( sel ) {\n\treturn (sel + "").replace( rcssescape, fcssescape );\n};\n\nSizzle.error = function( msg ) {\n\tthrow new Error( "Syntax error, unrecognized expression: " + msg );\n};\n\n/**\n * Document sorting and removing duplicates\n * @param {ArrayLike} results\n */\nSizzle.uniqueSort = function( results ) {\n\tvar elem,\n\t\tduplicates = [],\n\t\tj = 0,\n\t\ti = 0;\n\n\t// Unless we *know* we can detect duplicates, assume their presence\n\thasDuplicate = !support.detectDuplicates;\n\tsortInput = !support.sortStable && results.slice( 0 );\n\tresults.sort( sortOrder );\n\n\tif ( hasDuplicate ) {\n\t\twhile ( (elem = results[i++]) ) {\n\t\t\tif ( elem === results[ i ] ) {\n\t\t\t\tj = duplicates.push( i );\n\t\t\t}\n\t\t}\n\t\twhile ( j-- ) {\n\t\t\tresults.splice( duplicates[ j ], 1 );\n\t\t}\n\t}\n\n\t// Clear input after sorting to release objects\n\t// See https://github.com/jquery/sizzle/pull/225\n\tsortInput = null;\n\n\treturn results;\n};\n\n/**\n * Utility function for retrieving the text value of an array of DOM nodes\n * @param {Array|Element} elem\n */\ngetText = Sizzle.getText = function( elem ) {\n\tvar node,\n\t\tret = "",\n\t\ti = 0,\n\t\tnodeType = elem.nodeType;\n\n\tif ( !nodeType ) {\n\t\t// If no nodeType, this is expected to be an array\n\t\twhile ( (node = elem[i++]) ) {\n\t\t\t// Do not traverse comment nodes\n\t\t\tret += getText( node );\n\t\t}\n\t} else if ( nodeType === 1 || nodeType === 9 || nodeType === 11 ) {\n\t\t// Use textContent for elements\n\t\t// innerText usage removed for consistency of new lines (jQuery #11153)\n\t\tif ( typeof elem.textContent === "string" ) {\n\t\t\treturn elem.textContent;\n\t\t} else {\n\t\t\t// Traverse its children\n\t\t\tfor ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {\n\t\t\t\tret += getText( elem );\n\t\t\t}\n\t\t}\n\t} else if ( nodeType === 3 || nodeType === 4 ) {\n\t\treturn elem.nodeValue;\n\t}\n\t// Do not include comment or processing instruction nodes\n\n\treturn ret;\n};\n\nExpr = Sizzle.selectors = {\n\n\t// Can be adjusted by the user\n\tcacheLength: 50,\n\n\tcreatePseudo: markFunction,\n\n\tmatch: matchExpr,\n\n\tattrHandle: {},\n\n\tfind: {},\n\n\trelative: {\n\t\t">": { dir: "parentNode", first: true },\n\t\t" ": { dir: "parentNode" },\n\t\t"+": { dir: "previousSibling", first: true },\n\t\t"~": { dir: "previousSibling" }\n\t},\n\n\tpreFilter: {\n\t\t"ATTR": function( match ) {\n\t\t\tmatch[1] = match[1].replace( runescape, funescape );\n\n\t\t\t// Move the given value to match[3] whether quoted or unquoted\n\t\t\tmatch[3] = ( match[3] || match[4] || match[5] || "" ).replace( runescape, funescape );\n\n\t\t\tif ( match[2] === "~=" ) {\n\t\t\t\tmatch[3] = " " + match[3] + " ";\n\t\t\t}\n\n\t\t\treturn match.slice( 0, 4 );\n\t\t},\n\n\t\t"CHILD": function( match ) {\n\t\t\t/* matches from matchExpr["CHILD"]\n\t\t\t\t1 type (only|nth|...)\n\t\t\t\t2 what (child|of-type)\n\t\t\t\t3 argument (even|odd|\\d*|\\d*n([+-]\\d+)?|...)\n\t\t\t\t4 xn-component of xn+y argument ([+-]?\\d*n|)\n\t\t\t\t5 sign of xn-component\n\t\t\t\t6 x of xn-component\n\t\t\t\t7 sign of y-component\n\t\t\t\t8 y of y-component\n\t\t\t*/\n\t\t\tmatch[1] = match[1].toLowerCase();\n\n\t\t\tif ( match[1].slice( 0, 3 ) === "nth" ) {\n\t\t\t\t// nth-* requires argument\n\t\t\t\tif ( !match[3] ) {\n\t\t\t\t\tSizzle.error( match[0] );\n\t\t\t\t}\n\n\t\t\t\t// numeric x and y parameters for Expr.filter.CHILD\n\t\t\t\t// remember that false/true cast respectively to 0/1\n\t\t\t\tmatch[4] = +( match[4] ? match[5] + (match[6] || 1) : 2 * ( match[3] === "even" || match[3] === "odd" ) );\n\t\t\t\tmatch[5] = +( ( match[7] + match[8] ) || match[3] === "odd" );\n\n\t\t\t// other types prohibit arguments\n\t\t\t} else if ( match[3] ) {\n\t\t\t\tSizzle.error( match[0] );\n\t\t\t}\n\n\t\t\treturn match;\n\t\t},\n\n\t\t"PSEUDO": function( match ) {\n\t\t\tvar excess,\n\t\t\t\tunquoted = !match[6] && match[2];\n\n\t\t\tif ( matchExpr["CHILD"].test( match[0] ) ) {\n\t\t\t\treturn null;\n\t\t\t}\n\n\t\t\t// Accept quoted arguments as-is\n\t\t\tif ( match[3] ) {\n\t\t\t\tmatch[2] = match[4] || match[5] || "";\n\n\t\t\t// Strip excess characters from unquoted arguments\n\t\t\t} else if ( unquoted && rpseudo.test( unquoted ) &&\n\t\t\t\t// Get excess from tokenize (recursively)\n\t\t\t\t(excess = tokenize( unquoted, true )) &&\n\t\t\t\t// advance to the next closing parenthesis\n\t\t\t\t(excess = unquoted.indexOf( ")", unquoted.length - excess ) - unquoted.length) ) {\n\n\t\t\t\t// excess is a negative index\n\t\t\t\tmatch[0] = match[0].slice( 0, excess );\n\t\t\t\tmatch[2] = unquoted.slice( 0, excess );\n\t\t\t}\n\n\t\t\t// Return only captures needed by the pseudo filter method (type and argument)\n\t\t\treturn match.slice( 0, 3 );\n\t\t}\n\t},\n\n\tfilter: {\n\n\t\t"TAG": function( nodeNameSelector ) {\n\t\t\tvar nodeName = nodeNameSelector.replace( runescape, funescape ).toLowerCase();\n\t\t\treturn nodeNameSelector === "*" ?\n\t\t\t\tfunction() { return true; } :\n\t\t\t\tfunction( elem ) {\n\t\t\t\t\treturn elem.nodeName && elem.nodeName.toLowerCase() === nodeName;\n\t\t\t\t};\n\t\t},\n\n\t\t"CLASS": function( className ) {\n\t\t\tvar pattern = classCache[ className + " " ];\n\n\t\t\treturn pattern ||\n\t\t\t\t(pattern = new RegExp( "(^|" + whitespace + ")" + className + "(" + whitespace + "|$)" )) &&\n\t\t\t\tclassCache( className, function( elem ) {\n\t\t\t\t\treturn pattern.test( typeof elem.className === "string" && elem.className || typeof elem.getAttribute !== "undefined" && elem.getAttribute("class") || "" );\n\t\t\t\t});\n\t\t},\n\n\t\t"ATTR": function( name, operator, check ) {\n\t\t\treturn function( elem ) {\n\t\t\t\tvar result = Sizzle.attr( elem, name );\n\n\t\t\t\tif ( result == null ) {\n\t\t\t\t\treturn operator === "!=";\n\t\t\t\t}\n\t\t\t\tif ( !operator ) {\n\t\t\t\t\treturn true;\n\t\t\t\t}\n\n\t\t\t\tresult += "";\n\n\t\t\t\treturn operator === "=" ? result === check :\n\t\t\t\t\toperator === "!=" ? result !== check :\n\t\t\t\t\toperator === "^=" ? check && result.indexOf( check ) === 0 :\n\t\t\t\t\toperator === "*=" ? check && result.indexOf( check ) > -1 :\n\t\t\t\t\toperator === "$=" ? check && result.slice( -check.length ) === check :\n\t\t\t\t\toperator === "~=" ? ( " " + result.replace( rwhitespace, " " ) + " " ).indexOf( check ) > -1 :\n\t\t\t\t\toperator === "|=" ? result === check || result.slice( 0, check.length + 1 ) === check + "-" :\n\t\t\t\t\tfalse;\n\t\t\t};\n\t\t},\n\n\t\t"CHILD": function( type, what, argument, first, last ) {\n\t\t\tvar simple = type.slice( 0, 3 ) !== "nth",\n\t\t\t\tforward = type.slice( -4 ) !== "last",\n\t\t\t\tofType = what === "of-type";\n\n\t\t\treturn first === 1 && last === 0 ?\n\n\t\t\t\t// Shortcut for :nth-*(n)\n\t\t\t\tfunction( elem ) {\n\t\t\t\t\treturn !!elem.parentNode;\n\t\t\t\t} :\n\n\t\t\t\tfunction( elem, context, xml ) {\n\t\t\t\t\tvar cache, uniqueCache, outerCache, node, nodeIndex, start,\n\t\t\t\t\t\tdir = simple !== forward ? "nextSibling" : "previousSibling",\n\t\t\t\t\t\tparent = elem.parentNode,\n\t\t\t\t\t\tname = ofType && elem.nodeName.toLowerCase(),\n\t\t\t\t\t\tuseCache = !xml && !ofType,\n\t\t\t\t\t\tdiff = false;\n\n\t\t\t\t\tif ( parent ) {\n\n\t\t\t\t\t\t// :(first|last|only)-(child|of-type)\n\t\t\t\t\t\tif ( simple ) {\n\t\t\t\t\t\t\twhile ( dir ) {\n\t\t\t\t\t\t\t\tnode = elem;\n\t\t\t\t\t\t\t\twhile ( (node = node[ dir ]) ) {\n\t\t\t\t\t\t\t\t\tif ( ofType ?\n\t\t\t\t\t\t\t\t\t\tnode.nodeName.toLowerCase() === name :\n\t\t\t\t\t\t\t\t\t\tnode.nodeType === 1 ) {\n\n\t\t\t\t\t\t\t\t\t\treturn false;\n\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t// Reverse direction for :only-* (if we haven\'t yet done so)\n\t\t\t\t\t\t\t\tstart = dir = type === "only" && !start && "nextSibling";\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\treturn true;\n\t\t\t\t\t\t}\n\n\t\t\t\t\t\tstart = [ forward ? parent.firstChild : parent.lastChild ];\n\n\t\t\t\t\t\t// non-xml :nth-child(...) stores cache data on `parent`\n\t\t\t\t\t\tif ( forward && useCache ) {\n\n\t\t\t\t\t\t\t// Seek `elem` from a previously-cached index\n\n\t\t\t\t\t\t\t// ...in a gzip-friendly way\n\t\t\t\t\t\t\tnode = parent;\n\t\t\t\t\t\t\touterCache = node[ expando ] || (node[ expando ] = {});\n\n\t\t\t\t\t\t\t// Support: IE <9 only\n\t\t\t\t\t\t\t// Defend against cloned attroperties (jQuery gh-1709)\n\t\t\t\t\t\t\tuniqueCache = outerCache[ node.uniqueID ] ||\n\t\t\t\t\t\t\t\t(outerCache[ node.uniqueID ] = {});\n\n\t\t\t\t\t\t\tcache = uniqueCache[ type ] || [];\n\t\t\t\t\t\t\tnodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];\n\t\t\t\t\t\t\tdiff = nodeIndex && cache[ 2 ];\n\t\t\t\t\t\t\tnode = nodeIndex && parent.childNodes[ nodeIndex ];\n\n\t\t\t\t\t\t\twhile ( (node = ++nodeIndex && node && node[ dir ] ||\n\n\t\t\t\t\t\t\t\t// Fallback to seeking `elem` from the start\n\t\t\t\t\t\t\t\t(diff = nodeIndex = 0) || start.pop()) ) {\n\n\t\t\t\t\t\t\t\t// When found, cache indexes on `parent` and break\n\t\t\t\t\t\t\t\tif ( node.nodeType === 1 && ++diff && node === elem ) {\n\t\t\t\t\t\t\t\t\tuniqueCache[ type ] = [ dirruns, nodeIndex, diff ];\n\t\t\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t// Use previously-cached element index if available\n\t\t\t\t\t\t\tif ( useCache ) {\n\t\t\t\t\t\t\t\t// ...in a gzip-friendly way\n\t\t\t\t\t\t\t\tnode = elem;\n\t\t\t\t\t\t\t\touterCache = node[ expando ] || (node[ expando ] = {});\n\n\t\t\t\t\t\t\t\t// Support: IE <9 only\n\t\t\t\t\t\t\t\t// Defend against cloned attroperties (jQuery gh-1709)\n\t\t\t\t\t\t\t\tuniqueCache = outerCache[ node.uniqueID ] ||\n\t\t\t\t\t\t\t\t\t(outerCache[ node.uniqueID ] = {});\n\n\t\t\t\t\t\t\t\tcache = uniqueCache[ type ] || [];\n\t\t\t\t\t\t\t\tnodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];\n\t\t\t\t\t\t\t\tdiff = nodeIndex;\n\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t// xml :nth-child(...)\n\t\t\t\t\t\t\t// or :nth-last-child(...) or :nth(-last)?-of-type(...)\n\t\t\t\t\t\t\tif ( diff === false ) {\n\t\t\t\t\t\t\t\t// Use the same loop as above to seek `elem` from the start\n\t\t\t\t\t\t\t\twhile ( (node = ++nodeIndex && node && node[ dir ] ||\n\t\t\t\t\t\t\t\t\t(diff = nodeIndex = 0) || start.pop()) ) {\n\n\t\t\t\t\t\t\t\t\tif ( ( ofType ?\n\t\t\t\t\t\t\t\t\t\tnode.nodeName.toLowerCase() === name :\n\t\t\t\t\t\t\t\t\t\tnode.nodeType === 1 ) &&\n\t\t\t\t\t\t\t\t\t\t++diff ) {\n\n\t\t\t\t\t\t\t\t\t\t// Cache the index of each encountered element\n\t\t\t\t\t\t\t\t\t\tif ( useCache ) {\n\t\t\t\t\t\t\t\t\t\t\touterCache = node[ expando ] || (node[ expando ] = {});\n\n\t\t\t\t\t\t\t\t\t\t\t// Support: IE <9 only\n\t\t\t\t\t\t\t\t\t\t\t// Defend against cloned attroperties (jQuery gh-1709)\n\t\t\t\t\t\t\t\t\t\t\tuniqueCache = outerCache[ node.uniqueID ] ||\n\t\t\t\t\t\t\t\t\t\t\t\t(outerCache[ node.uniqueID ] = {});\n\n\t\t\t\t\t\t\t\t\t\t\tuniqueCache[ type ] = [ dirruns, diff ];\n\t\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t\tif ( node === elem ) {\n\t\t\t\t\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\n\t\t\t\t\t\t// Incorporate the offset, then check against cycle size\n\t\t\t\t\t\tdiff -= last;\n\t\t\t\t\t\treturn diff === first || ( diff % first === 0 && diff / first >= 0 );\n\t\t\t\t\t}\n\t\t\t\t};\n\t\t},\n\n\t\t"PSEUDO": function( pseudo, argument ) {\n\t\t\t// pseudo-class names are case-insensitive\n\t\t\t// http://www.w3.org/TR/selectors/#pseudo-classes\n\t\t\t// Prioritize by case sensitivity in case custom pseudos are added with uppercase letters\n\t\t\t// Remember that setFilters inherits from pseudos\n\t\t\tvar args,\n\t\t\t\tfn = Expr.pseudos[ pseudo ] || Expr.setFilters[ pseudo.toLowerCase() ] ||\n\t\t\t\t\tSizzle.error( "unsupported pseudo: " + pseudo );\n\n\t\t\t// The user may use createPseudo to indicate that\n\t\t\t// arguments are needed to create the filter function\n\t\t\t// just as Sizzle does\n\t\t\tif ( fn[ expando ] ) {\n\t\t\t\treturn fn( argument );\n\t\t\t}\n\n\t\t\t// But maintain support for old signatures\n\t\t\tif ( fn.length > 1 ) {\n\t\t\t\targs = [ pseudo, pseudo, "", argument ];\n\t\t\t\treturn Expr.setFilters.hasOwnProperty( pseudo.toLowerCase() ) ?\n\t\t\t\t\tmarkFunction(function( seed, matches ) {\n\t\t\t\t\t\tvar idx,\n\t\t\t\t\t\t\tmatched = fn( seed, argument ),\n\t\t\t\t\t\t\ti = matched.length;\n\t\t\t\t\t\twhile ( i-- ) {\n\t\t\t\t\t\t\tidx = indexOf( seed, matched[i] );\n\t\t\t\t\t\t\tseed[ idx ] = !( matches[ idx ] = matched[i] );\n\t\t\t\t\t\t}\n\t\t\t\t\t}) :\n\t\t\t\t\tfunction( elem ) {\n\t\t\t\t\t\treturn fn( elem, 0, args );\n\t\t\t\t\t};\n\t\t\t}\n\n\t\t\treturn fn;\n\t\t}\n\t},\n\n\tpseudos: {\n\t\t// Potentially complex pseudos\n\t\t"not": markFunction(function( selector ) {\n\t\t\t// Trim the selector passed to compile\n\t\t\t// to avoid treating leading and trailing\n\t\t\t// spaces as combinators\n\t\t\tvar input = [],\n\t\t\t\tresults = [],\n\t\t\t\tmatcher = compile( selector.replace( rtrim, "$1" ) );\n\n\t\t\treturn matcher[ expando ] ?\n\t\t\t\tmarkFunction(function( seed, matches, context, xml ) {\n\t\t\t\t\tvar elem,\n\t\t\t\t\t\tunmatched = matcher( seed, null, xml, [] ),\n\t\t\t\t\t\ti = seed.length;\n\n\t\t\t\t\t// Match elements unmatched by `matcher`\n\t\t\t\t\twhile ( i-- ) {\n\t\t\t\t\t\tif ( (elem = unmatched[i]) ) {\n\t\t\t\t\t\t\tseed[i] = !(matches[i] = elem);\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}) :\n\t\t\t\tfunction( elem, context, xml ) {\n\t\t\t\t\tinput[0] = elem;\n\t\t\t\t\tmatcher( input, null, xml, results );\n\t\t\t\t\t// Don\'t keep the element (issue #299)\n\t\t\t\t\tinput[0] = null;\n\t\t\t\t\treturn !results.pop();\n\t\t\t\t};\n\t\t}),\n\n\t\t"has": markFunction(function( selector ) {\n\t\t\treturn function( elem ) {\n\t\t\t\treturn Sizzle( selector, elem ).length > 0;\n\t\t\t};\n\t\t}),\n\n\t\t"contains": markFunction(function( text ) {\n\t\t\ttext = text.replace( runescape, funescape );\n\t\t\treturn function( elem ) {\n\t\t\t\treturn ( elem.textContent || elem.innerText || getText( elem ) ).indexOf( text ) > -1;\n\t\t\t};\n\t\t}),\n\n\t\t// "Whether an element is represented by a :lang() selector\n\t\t// is based solely on the element\'s language value\n\t\t// being equal to the identifier C,\n\t\t// or beginning with the identifier C immediately followed by "-".\n\t\t// The matching of C against the element\'s language value is performed case-insensitively.\n\t\t// The identifier C does not have to be a valid language name."\n\t\t// http://www.w3.org/TR/selectors/#lang-pseudo\n\t\t"lang": markFunction( function( lang ) {\n\t\t\t// lang value must be a valid identifier\n\t\t\tif ( !ridentifier.test(lang || "") ) {\n\t\t\t\tSizzle.error( "unsupported lang: " + lang );\n\t\t\t}\n\t\t\tlang = lang.replace( runescape, funescape ).toLowerCase();\n\t\t\treturn function( elem ) {\n\t\t\t\tvar elemLang;\n\t\t\t\tdo {\n\t\t\t\t\tif ( (elemLang = documentIsHTML ?\n\t\t\t\t\t\telem.lang :\n\t\t\t\t\t\telem.getAttribute("xml:lang") || elem.getAttribute("lang")) ) {\n\n\t\t\t\t\t\telemLang = elemLang.toLowerCase();\n\t\t\t\t\t\treturn elemLang === lang || elemLang.indexOf( lang + "-" ) === 0;\n\t\t\t\t\t}\n\t\t\t\t} while ( (elem = elem.parentNode) && elem.nodeType === 1 );\n\t\t\t\treturn false;\n\t\t\t};\n\t\t}),\n\n\t\t// Miscellaneous\n\t\t"target": function( elem ) {\n\t\t\tvar hash = window.location && window.location.hash;\n\t\t\treturn hash && hash.slice( 1 ) === elem.id;\n\t\t},\n\n\t\t"root": function( elem ) {\n\t\t\treturn elem === docElem;\n\t\t},\n\n\t\t"focus": function( elem ) {\n\t\t\treturn elem === document.activeElement && (!document.hasFocus || document.hasFocus()) && !!(elem.type || elem.href || ~elem.tabIndex);\n\t\t},\n\n\t\t// Boolean properties\n\t\t"enabled": createDisabledPseudo( false ),\n\t\t"disabled": createDisabledPseudo( true ),\n\n\t\t"checked": function( elem ) {\n\t\t\t// In CSS3, :checked should return both checked and selected elements\n\t\t\t// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked\n\t\t\tvar nodeName = elem.nodeName.toLowerCase();\n\t\t\treturn (nodeName === "input" && !!elem.checked) || (nodeName === "option" && !!elem.selected);\n\t\t},\n\n\t\t"selected": function( elem ) {\n\t\t\t// Accessing this property makes selected-by-default\n\t\t\t// options in Safari work properly\n\t\t\tif ( elem.parentNode ) {\n\t\t\t\telem.parentNode.selectedIndex;\n\t\t\t}\n\n\t\t\treturn elem.selected === true;\n\t\t},\n\n\t\t// Contents\n\t\t"empty": function( elem ) {\n\t\t\t// http://www.w3.org/TR/selectors/#empty-pseudo\n\t\t\t// :empty is negated by element (1) or content nodes (text: 3; cdata: 4; entity ref: 5),\n\t\t\t//   but not by others (comment: 8; processing instruction: 7; etc.)\n\t\t\t// nodeType < 6 works because attributes (2) do not appear as children\n\t\t\tfor ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {\n\t\t\t\tif ( elem.nodeType < 6 ) {\n\t\t\t\t\treturn false;\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn true;\n\t\t},\n\n\t\t"parent": function( elem ) {\n\t\t\treturn !Expr.pseudos["empty"]( elem );\n\t\t},\n\n\t\t// Element/input types\n\t\t"header": function( elem ) {\n\t\t\treturn rheader.test( elem.nodeName );\n\t\t},\n\n\t\t"input": function( elem ) {\n\t\t\treturn rinputs.test( elem.nodeName );\n\t\t},\n\n\t\t"button": function( elem ) {\n\t\t\tvar name = elem.nodeName.toLowerCase();\n\t\t\treturn name === "input" && elem.type === "button" || name === "button";\n\t\t},\n\n\t\t"text": function( elem ) {\n\t\t\tvar attr;\n\t\t\treturn elem.nodeName.toLowerCase() === "input" &&\n\t\t\t\telem.type === "text" &&\n\n\t\t\t\t// Support: IE<8\n\t\t\t\t// New HTML5 attribute values (e.g., "search") appear with elem.type === "text"\n\t\t\t\t( (attr = elem.getAttribute("type")) == null || attr.toLowerCase() === "text" );\n\t\t},\n\n\t\t// Position-in-collection\n\t\t"first": createPositionalPseudo(function() {\n\t\t\treturn [ 0 ];\n\t\t}),\n\n\t\t"last": createPositionalPseudo(function( matchIndexes, length ) {\n\t\t\treturn [ length - 1 ];\n\t\t}),\n\n\t\t"eq": createPositionalPseudo(function( matchIndexes, length, argument ) {\n\t\t\treturn [ argument < 0 ? argument + length : argument ];\n\t\t}),\n\n\t\t"even": createPositionalPseudo(function( matchIndexes, length ) {\n\t\t\tvar i = 0;\n\t\t\tfor ( ; i < length; i += 2 ) {\n\t\t\t\tmatchIndexes.push( i );\n\t\t\t}\n\t\t\treturn matchIndexes;\n\t\t}),\n\n\t\t"odd": createPositionalPseudo(function( matchIndexes, length ) {\n\t\t\tvar i = 1;\n\t\t\tfor ( ; i < length; i += 2 ) {\n\t\t\t\tmatchIndexes.push( i );\n\t\t\t}\n\t\t\treturn matchIndexes;\n\t\t}),\n\n\t\t"lt": createPositionalPseudo(function( matchIndexes, length, argument ) {\n\t\t\tvar i = argument < 0 ? argument + length : argument;\n\t\t\tfor ( ; --i >= 0; ) {\n\t\t\t\tmatchIndexes.push( i );\n\t\t\t}\n\t\t\treturn matchIndexes;\n\t\t}),\n\n\t\t"gt": createPositionalPseudo(function( matchIndexes, length, argument ) {\n\t\t\tvar i = argument < 0 ? argument + length : argument;\n\t\t\tfor ( ; ++i < length; ) {\n\t\t\t\tmatchIndexes.push( i );\n\t\t\t}\n\t\t\treturn matchIndexes;\n\t\t})\n\t}\n};\n\nExpr.pseudos["nth"] = Expr.pseudos["eq"];\n\n// Add button/input type pseudos\nfor ( i in { radio: true, checkbox: true, file: true, password: true, image: true } ) {\n\tExpr.pseudos[ i ] = createInputPseudo( i );\n}\nfor ( i in { submit: true, reset: true } ) {\n\tExpr.pseudos[ i ] = createButtonPseudo( i );\n}\n\n// Easy API for creating new setFilters\nfunction setFilters() {}\nsetFilters.prototype = Expr.filters = Expr.pseudos;\nExpr.setFilters = new setFilters();\n\ntokenize = Sizzle.tokenize = function( selector, parseOnly ) {\n\tvar matched, match, tokens, type,\n\t\tsoFar, groups, preFilters,\n\t\tcached = tokenCache[ selector + " " ];\n\n\tif ( cached ) {\n\t\treturn parseOnly ? 0 : cached.slice( 0 );\n\t}\n\n\tsoFar = selector;\n\tgroups = [];\n\tpreFilters = Expr.preFilter;\n\n\twhile ( soFar ) {\n\n\t\t// Comma and first run\n\t\tif ( !matched || (match = rcomma.exec( soFar )) ) {\n\t\t\tif ( match ) {\n\t\t\t\t// Don\'t consume trailing commas as valid\n\t\t\t\tsoFar = soFar.slice( match[0].length ) || soFar;\n\t\t\t}\n\t\t\tgroups.push( (tokens = []) );\n\t\t}\n\n\t\tmatched = false;\n\n\t\t// Combinators\n\t\tif ( (match = rcombinators.exec( soFar )) ) {\n\t\t\tmatched = match.shift();\n\t\t\ttokens.push({\n\t\t\t\tvalue: matched,\n\t\t\t\t// Cast descendant combinators to space\n\t\t\t\ttype: match[0].replace( rtrim, " " )\n\t\t\t});\n\t\t\tsoFar = soFar.slice( matched.length );\n\t\t}\n\n\t\t// Filters\n\t\tfor ( type in Expr.filter ) {\n\t\t\tif ( (match = matchExpr[ type ].exec( soFar )) && (!preFilters[ type ] ||\n\t\t\t\t(match = preFilters[ type ]( match ))) ) {\n\t\t\t\tmatched = match.shift();\n\t\t\t\ttokens.push({\n\t\t\t\t\tvalue: matched,\n\t\t\t\t\ttype: type,\n\t\t\t\t\tmatches: match\n\t\t\t\t});\n\t\t\t\tsoFar = soFar.slice( matched.length );\n\t\t\t}\n\t\t}\n\n\t\tif ( !matched ) {\n\t\t\tbreak;\n\t\t}\n\t}\n\n\t// Return the length of the invalid excess\n\t// if we\'re just parsing\n\t// Otherwise, throw an error or return tokens\n\treturn parseOnly ?\n\t\tsoFar.length :\n\t\tsoFar ?\n\t\t\tSizzle.error( selector ) :\n\t\t\t// Cache the tokens\n\t\t\ttokenCache( selector, groups ).slice( 0 );\n};\n\nfunction toSelector( tokens ) {\n\tvar i = 0,\n\t\tlen = tokens.length,\n\t\tselector = "";\n\tfor ( ; i < len; i++ ) {\n\t\tselector += tokens[i].value;\n\t}\n\treturn selector;\n}\n\nfunction addCombinator( matcher, combinator, base ) {\n\tvar dir = combinator.dir,\n\t\tskip = combinator.next,\n\t\tkey = skip || dir,\n\t\tcheckNonElements = base && key === "parentNode",\n\t\tdoneName = done++;\n\n\treturn combinator.first ?\n\t\t// Check against closest ancestor/preceding element\n\t\tfunction( elem, context, xml ) {\n\t\t\twhile ( (elem = elem[ dir ]) ) {\n\t\t\t\tif ( elem.nodeType === 1 || checkNonElements ) {\n\t\t\t\t\treturn matcher( elem, context, xml );\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn false;\n\t\t} :\n\n\t\t// Check against all ancestor/preceding elements\n\t\tfunction( elem, context, xml ) {\n\t\t\tvar oldCache, uniqueCache, outerCache,\n\t\t\t\tnewCache = [ dirruns, doneName ];\n\n\t\t\t// We can\'t set arbitrary data on XML nodes, so they don\'t benefit from combinator caching\n\t\t\tif ( xml ) {\n\t\t\t\twhile ( (elem = elem[ dir ]) ) {\n\t\t\t\t\tif ( elem.nodeType === 1 || checkNonElements ) {\n\t\t\t\t\t\tif ( matcher( elem, context, xml ) ) {\n\t\t\t\t\t\t\treturn true;\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\twhile ( (elem = elem[ dir ]) ) {\n\t\t\t\t\tif ( elem.nodeType === 1 || checkNonElements ) {\n\t\t\t\t\t\touterCache = elem[ expando ] || (elem[ expando ] = {});\n\n\t\t\t\t\t\t// Support: IE <9 only\n\t\t\t\t\t\t// Defend against cloned attroperties (jQuery gh-1709)\n\t\t\t\t\t\tuniqueCache = outerCache[ elem.uniqueID ] || (outerCache[ elem.uniqueID ] = {});\n\n\t\t\t\t\t\tif ( skip && skip === elem.nodeName.toLowerCase() ) {\n\t\t\t\t\t\t\telem = elem[ dir ] || elem;\n\t\t\t\t\t\t} else if ( (oldCache = uniqueCache[ key ]) &&\n\t\t\t\t\t\t\toldCache[ 0 ] === dirruns && oldCache[ 1 ] === doneName ) {\n\n\t\t\t\t\t\t\t// Assign to newCache so results back-propagate to previous elements\n\t\t\t\t\t\t\treturn (newCache[ 2 ] = oldCache[ 2 ]);\n\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t// Reuse newcache so results back-propagate to previous elements\n\t\t\t\t\t\t\tuniqueCache[ key ] = newCache;\n\n\t\t\t\t\t\t\t// A match means we\'re done; a fail means we have to keep checking\n\t\t\t\t\t\t\tif ( (newCache[ 2 ] = matcher( elem, context, xml )) ) {\n\t\t\t\t\t\t\t\treturn true;\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn false;\n\t\t};\n}\n\nfunction elementMatcher( matchers ) {\n\treturn matchers.length > 1 ?\n\t\tfunction( elem, context, xml ) {\n\t\t\tvar i = matchers.length;\n\t\t\twhile ( i-- ) {\n\t\t\t\tif ( !matchers[i]( elem, context, xml ) ) {\n\t\t\t\t\treturn false;\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn true;\n\t\t} :\n\t\tmatchers[0];\n}\n\nfunction multipleContexts( selector, contexts, results ) {\n\tvar i = 0,\n\t\tlen = contexts.length;\n\tfor ( ; i < len; i++ ) {\n\t\tSizzle( selector, contexts[i], results );\n\t}\n\treturn results;\n}\n\nfunction condense( unmatched, map, filter, context, xml ) {\n\tvar elem,\n\t\tnewUnmatched = [],\n\t\ti = 0,\n\t\tlen = unmatched.length,\n\t\tmapped = map != null;\n\n\tfor ( ; i < len; i++ ) {\n\t\tif ( (elem = unmatched[i]) ) {\n\t\t\tif ( !filter || filter( elem, context, xml ) ) {\n\t\t\t\tnewUnmatched.push( elem );\n\t\t\t\tif ( mapped ) {\n\t\t\t\t\tmap.push( i );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\treturn newUnmatched;\n}\n\nfunction setMatcher( preFilter, selector, matcher, postFilter, postFinder, postSelector ) {\n\tif ( postFilter && !postFilter[ expando ] ) {\n\t\tpostFilter = setMatcher( postFilter );\n\t}\n\tif ( postFinder && !postFinder[ expando ] ) {\n\t\tpostFinder = setMatcher( postFinder, postSelector );\n\t}\n\treturn markFunction(function( seed, results, context, xml ) {\n\t\tvar temp, i, elem,\n\t\t\tpreMap = [],\n\t\t\tpostMap = [],\n\t\t\tpreexisting = results.length,\n\n\t\t\t// Get initial elements from seed or context\n\t\t\telems = seed || multipleContexts( selector || "*", context.nodeType ? [ context ] : context, [] ),\n\n\t\t\t// Prefilter to get matcher input, preserving a map for seed-results synchronization\n\t\t\tmatcherIn = preFilter && ( seed || !selector ) ?\n\t\t\t\tcondense( elems, preMap, preFilter, context, xml ) :\n\t\t\t\telems,\n\n\t\t\tmatcherOut = matcher ?\n\t\t\t\t// If we have a postFinder, or filtered seed, or non-seed postFilter or preexisting results,\n\t\t\t\tpostFinder || ( seed ? preFilter : preexisting || postFilter ) ?\n\n\t\t\t\t\t// ...intermediate processing is necessary\n\t\t\t\t\t[] :\n\n\t\t\t\t\t// ...otherwise use results directly\n\t\t\t\t\tresults :\n\t\t\t\tmatcherIn;\n\n\t\t// Find primary matches\n\t\tif ( matcher ) {\n\t\t\tmatcher( matcherIn, matcherOut, context, xml );\n\t\t}\n\n\t\t// Apply postFilter\n\t\tif ( postFilter ) {\n\t\t\ttemp = condense( matcherOut, postMap );\n\t\t\tpostFilter( temp, [], context, xml );\n\n\t\t\t// Un-match failing elements by moving them back to matcherIn\n\t\t\ti = temp.length;\n\t\t\twhile ( i-- ) {\n\t\t\t\tif ( (elem = temp[i]) ) {\n\t\t\t\t\tmatcherOut[ postMap[i] ] = !(matcherIn[ postMap[i] ] = elem);\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\tif ( seed ) {\n\t\t\tif ( postFinder || preFilter ) {\n\t\t\t\tif ( postFinder ) {\n\t\t\t\t\t// Get the final matcherOut by condensing this intermediate into postFinder contexts\n\t\t\t\t\ttemp = [];\n\t\t\t\t\ti = matcherOut.length;\n\t\t\t\t\twhile ( i-- ) {\n\t\t\t\t\t\tif ( (elem = matcherOut[i]) ) {\n\t\t\t\t\t\t\t// Restore matcherIn since elem is not yet a final match\n\t\t\t\t\t\t\ttemp.push( (matcherIn[i] = elem) );\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t\tpostFinder( null, (matcherOut = []), temp, xml );\n\t\t\t\t}\n\n\t\t\t\t// Move matched elements from seed to results to keep them synchronized\n\t\t\t\ti = matcherOut.length;\n\t\t\t\twhile ( i-- ) {\n\t\t\t\t\tif ( (elem = matcherOut[i]) &&\n\t\t\t\t\t\t(temp = postFinder ? indexOf( seed, elem ) : preMap[i]) > -1 ) {\n\n\t\t\t\t\t\tseed[temp] = !(results[temp] = elem);\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t// Add elements to results, through postFinder if defined\n\t\t} else {\n\t\t\tmatcherOut = condense(\n\t\t\t\tmatcherOut === results ?\n\t\t\t\t\tmatcherOut.splice( preexisting, matcherOut.length ) :\n\t\t\t\t\tmatcherOut\n\t\t\t);\n\t\t\tif ( postFinder ) {\n\t\t\t\tpostFinder( null, results, matcherOut, xml );\n\t\t\t} else {\n\t\t\t\tpush.apply( results, matcherOut );\n\t\t\t}\n\t\t}\n\t});\n}\n\nfunction matcherFromTokens( tokens ) {\n\tvar checkContext, matcher, j,\n\t\tlen = tokens.length,\n\t\tleadingRelative = Expr.relative[ tokens[0].type ],\n\t\timplicitRelative = leadingRelative || Expr.relative[" "],\n\t\ti = leadingRelative ? 1 : 0,\n\n\t\t// The foundational matcher ensures that elements are reachable from top-level context(s)\n\t\tmatchContext = addCombinator( function( elem ) {\n\t\t\treturn elem === checkContext;\n\t\t}, implicitRelative, true ),\n\t\tmatchAnyContext = addCombinator( function( elem ) {\n\t\t\treturn indexOf( checkContext, elem ) > -1;\n\t\t}, implicitRelative, true ),\n\t\tmatchers = [ function( elem, context, xml ) {\n\t\t\tvar ret = ( !leadingRelative && ( xml || context !== outermostContext ) ) || (\n\t\t\t\t(checkContext = context).nodeType ?\n\t\t\t\t\tmatchContext( elem, context, xml ) :\n\t\t\t\t\tmatchAnyContext( elem, context, xml ) );\n\t\t\t// Avoid hanging onto element (issue #299)\n\t\t\tcheckContext = null;\n\t\t\treturn ret;\n\t\t} ];\n\n\tfor ( ; i < len; i++ ) {\n\t\tif ( (matcher = Expr.relative[ tokens[i].type ]) ) {\n\t\t\tmatchers = [ addCombinator(elementMatcher( matchers ), matcher) ];\n\t\t} else {\n\t\t\tmatcher = Expr.filter[ tokens[i].type ].apply( null, tokens[i].matches );\n\n\t\t\t// Return special upon seeing a positional matcher\n\t\t\tif ( matcher[ expando ] ) {\n\t\t\t\t// Find the next relative operator (if any) for proper handling\n\t\t\t\tj = ++i;\n\t\t\t\tfor ( ; j < len; j++ ) {\n\t\t\t\t\tif ( Expr.relative[ tokens[j].type ] ) {\n\t\t\t\t\t\tbreak;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t\treturn setMatcher(\n\t\t\t\t\ti > 1 && elementMatcher( matchers ),\n\t\t\t\t\ti > 1 && toSelector(\n\t\t\t\t\t\t// If the preceding token was a descendant combinator, insert an implicit any-element `*`\n\t\t\t\t\t\ttokens.slice( 0, i - 1 ).concat({ value: tokens[ i - 2 ].type === " " ? "*" : "" })\n\t\t\t\t\t).replace( rtrim, "$1" ),\n\t\t\t\t\tmatcher,\n\t\t\t\t\ti < j && matcherFromTokens( tokens.slice( i, j ) ),\n\t\t\t\t\tj < len && matcherFromTokens( (tokens = tokens.slice( j )) ),\n\t\t\t\t\tj < len && toSelector( tokens )\n\t\t\t\t);\n\t\t\t}\n\t\t\tmatchers.push( matcher );\n\t\t}\n\t}\n\n\treturn elementMatcher( matchers );\n}\n\nfunction matcherFromGroupMatchers( elementMatchers, setMatchers ) {\n\tvar bySet = setMatchers.length > 0,\n\t\tbyElement = elementMatchers.length > 0,\n\t\tsuperMatcher = function( seed, context, xml, results, outermost ) {\n\t\t\tvar elem, j, matcher,\n\t\t\t\tmatchedCount = 0,\n\t\t\t\ti = "0",\n\t\t\t\tunmatched = seed && [],\n\t\t\t\tsetMatched = [],\n\t\t\t\tcontextBackup = outermostContext,\n\t\t\t\t// We must always have either seed elements or outermost context\n\t\t\t\telems = seed || byElement && Expr.find["TAG"]( "*", outermost ),\n\t\t\t\t// Use integer dirruns iff this is the outermost matcher\n\t\t\t\tdirrunsUnique = (dirruns += contextBackup == null ? 1 : Math.random() || 0.1),\n\t\t\t\tlen = elems.length;\n\n\t\t\tif ( outermost ) {\n\t\t\t\toutermostContext = context === document || context || outermost;\n\t\t\t}\n\n\t\t\t// Add elements passing elementMatchers directly to results\n\t\t\t// Support: IE<9, Safari\n\t\t\t// Tolerate NodeList properties (IE: "length"; Safari: <number>) matching elements by id\n\t\t\tfor ( ; i !== len && (elem = elems[i]) != null; i++ ) {\n\t\t\t\tif ( byElement && elem ) {\n\t\t\t\t\tj = 0;\n\t\t\t\t\tif ( !context && elem.ownerDocument !== document ) {\n\t\t\t\t\t\tsetDocument( elem );\n\t\t\t\t\t\txml = !documentIsHTML;\n\t\t\t\t\t}\n\t\t\t\t\twhile ( (matcher = elementMatchers[j++]) ) {\n\t\t\t\t\t\tif ( matcher( elem, context || document, xml) ) {\n\t\t\t\t\t\t\tresults.push( elem );\n\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t\tif ( outermost ) {\n\t\t\t\t\t\tdirruns = dirrunsUnique;\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\t// Track unmatched elements for set filters\n\t\t\t\tif ( bySet ) {\n\t\t\t\t\t// They will have gone through all possible matchers\n\t\t\t\t\tif ( (elem = !matcher && elem) ) {\n\t\t\t\t\t\tmatchedCount--;\n\t\t\t\t\t}\n\n\t\t\t\t\t// Lengthen the array for every element, matched or not\n\t\t\t\t\tif ( seed ) {\n\t\t\t\t\t\tunmatched.push( elem );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// `i` is now the count of elements visited above, and adding it to `matchedCount`\n\t\t\t// makes the latter nonnegative.\n\t\t\tmatchedCount += i;\n\n\t\t\t// Apply set filters to unmatched elements\n\t\t\t// NOTE: This can be skipped if there are no unmatched elements (i.e., `matchedCount`\n\t\t\t// equals `i`), unless we didn\'t visit _any_ elements in the above loop because we have\n\t\t\t// no element matchers and no seed.\n\t\t\t// Incrementing an initially-string "0" `i` allows `i` to remain a string only in that\n\t\t\t// case, which will result in a "00" `matchedCount` that differs from `i` but is also\n\t\t\t// numerically zero.\n\t\t\tif ( bySet && i !== matchedCount ) {\n\t\t\t\tj = 0;\n\t\t\t\twhile ( (matcher = setMatchers[j++]) ) {\n\t\t\t\t\tmatcher( unmatched, setMatched, context, xml );\n\t\t\t\t}\n\n\t\t\t\tif ( seed ) {\n\t\t\t\t\t// Reintegrate element matches to eliminate the need for sorting\n\t\t\t\t\tif ( matchedCount > 0 ) {\n\t\t\t\t\t\twhile ( i-- ) {\n\t\t\t\t\t\t\tif ( !(unmatched[i] || setMatched[i]) ) {\n\t\t\t\t\t\t\t\tsetMatched[i] = pop.call( results );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\t// Discard index placeholder values to get only actual matches\n\t\t\t\t\tsetMatched = condense( setMatched );\n\t\t\t\t}\n\n\t\t\t\t// Add matches to results\n\t\t\t\tpush.apply( results, setMatched );\n\n\t\t\t\t// Seedless set matches succeeding multiple successful matchers stipulate sorting\n\t\t\t\tif ( outermost && !seed && setMatched.length > 0 &&\n\t\t\t\t\t( matchedCount + setMatchers.length ) > 1 ) {\n\n\t\t\t\t\tSizzle.uniqueSort( results );\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Override manipulation of globals by nested matchers\n\t\t\tif ( outermost ) {\n\t\t\t\tdirruns = dirrunsUnique;\n\t\t\t\toutermostContext = contextBackup;\n\t\t\t}\n\n\t\t\treturn unmatched;\n\t\t};\n\n\treturn bySet ?\n\t\tmarkFunction( superMatcher ) :\n\t\tsuperMatcher;\n}\n\ncompile = Sizzle.compile = function( selector, match /* Internal Use Only */ ) {\n\tvar i,\n\t\tsetMatchers = [],\n\t\telementMatchers = [],\n\t\tcached = compilerCache[ selector + " " ];\n\n\tif ( !cached ) {\n\t\t// Generate a function of recursive functions that can be used to check each element\n\t\tif ( !match ) {\n\t\t\tmatch = tokenize( selector );\n\t\t}\n\t\ti = match.length;\n\t\twhile ( i-- ) {\n\t\t\tcached = matcherFromTokens( match[i] );\n\t\t\tif ( cached[ expando ] ) {\n\t\t\t\tsetMatchers.push( cached );\n\t\t\t} else {\n\t\t\t\telementMatchers.push( cached );\n\t\t\t}\n\t\t}\n\n\t\t// Cache the compiled function\n\t\tcached = compilerCache( selector, matcherFromGroupMatchers( elementMatchers, setMatchers ) );\n\n\t\t// Save selector and tokenization\n\t\tcached.selector = selector;\n\t}\n\treturn cached;\n};\n\n/**\n * A low-level selection function that works with Sizzle\'s compiled\n *  selector functions\n * @param {String|Function} selector A selector or a pre-compiled\n *  selector function built with Sizzle.compile\n * @param {Element} context\n * @param {Array} [results]\n * @param {Array} [seed] A set of elements to match against\n */\nselect = Sizzle.select = function( selector, context, results, seed ) {\n\tvar i, tokens, token, type, find,\n\t\tcompiled = typeof selector === "function" && selector,\n\t\tmatch = !seed && tokenize( (selector = compiled.selector || selector) );\n\n\tresults = results || [];\n\n\t// Try to minimize operations if there is only one selector in the list and no seed\n\t// (the latter of which guarantees us context)\n\tif ( match.length === 1 ) {\n\n\t\t// Reduce context if the leading compound selector is an ID\n\t\ttokens = match[0] = match[0].slice( 0 );\n\t\tif ( tokens.length > 2 && (token = tokens[0]).type === "ID" &&\n\t\t\t\tcontext.nodeType === 9 && documentIsHTML && Expr.relative[ tokens[1].type ] ) {\n\n\t\t\tcontext = ( Expr.find["ID"]( token.matches[0].replace(runescape, funescape), context ) || [] )[0];\n\t\t\tif ( !context ) {\n\t\t\t\treturn results;\n\n\t\t\t// Precompiled matchers will still verify ancestry, so step up a level\n\t\t\t} else if ( compiled ) {\n\t\t\t\tcontext = context.parentNode;\n\t\t\t}\n\n\t\t\tselector = selector.slice( tokens.shift().value.length );\n\t\t}\n\n\t\t// Fetch a seed set for right-to-left matching\n\t\ti = matchExpr["needsContext"].test( selector ) ? 0 : tokens.length;\n\t\twhile ( i-- ) {\n\t\t\ttoken = tokens[i];\n\n\t\t\t// Abort if we hit a combinator\n\t\t\tif ( Expr.relative[ (type = token.type) ] ) {\n\t\t\t\tbreak;\n\t\t\t}\n\t\t\tif ( (find = Expr.find[ type ]) ) {\n\t\t\t\t// Search, expanding context for leading sibling combinators\n\t\t\t\tif ( (seed = find(\n\t\t\t\t\ttoken.matches[0].replace( runescape, funescape ),\n\t\t\t\t\trsibling.test( tokens[0].type ) && testContext( context.parentNode ) || context\n\t\t\t\t)) ) {\n\n\t\t\t\t\t// If seed is empty or no tokens remain, we can return early\n\t\t\t\t\ttokens.splice( i, 1 );\n\t\t\t\t\tselector = seed.length && toSelector( tokens );\n\t\t\t\t\tif ( !selector ) {\n\t\t\t\t\t\tpush.apply( results, seed );\n\t\t\t\t\t\treturn results;\n\t\t\t\t\t}\n\n\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\t// Compile and execute a filtering function if one is not provided\n\t// Provide `match` to avoid retokenization if we modified the selector above\n\t( compiled || compile( selector, match ) )(\n\t\tseed,\n\t\tcontext,\n\t\t!documentIsHTML,\n\t\tresults,\n\t\t!context || rsibling.test( selector ) && testContext( context.parentNode ) || context\n\t);\n\treturn results;\n};\n\n// One-time assignments\n\n// Sort stability\nsupport.sortStable = expando.split("").sort( sortOrder ).join("") === expando;\n\n// Support: Chrome 14-35+\n// Always assume duplicates if they aren\'t passed to the comparison function\nsupport.detectDuplicates = !!hasDuplicate;\n\n// Initialize against the default document\nsetDocument();\n\n// Support: Webkit<537.32 - Safari 6.0.3/Chrome 25 (fixed in Chrome 27)\n// Detached nodes confoundingly follow *each other*\nsupport.sortDetached = assert(function( el ) {\n\t// Should return 1, but returns 4 (following)\n\treturn el.compareDocumentPosition( document.createElement("fieldset") ) & 1;\n});\n\n// Support: IE<8\n// Prevent attribute/property "interpolation"\n// https://msdn.microsoft.com/en-us/library/ms536429%28VS.85%29.aspx\nif ( !assert(function( el ) {\n\tel.innerHTML = "<a href=\'#\'></a>";\n\treturn el.firstChild.getAttribute("href") === "#" ;\n}) ) {\n\taddHandle( "type|href|height|width", function( elem, name, isXML ) {\n\t\tif ( !isXML ) {\n\t\t\treturn elem.getAttribute( name, name.toLowerCase() === "type" ? 1 : 2 );\n\t\t}\n\t});\n}\n\n// Support: IE<9\n// Use defaultValue in place of getAttribute("value")\nif ( !support.attributes || !assert(function( el ) {\n\tel.innerHTML = "<input/>";\n\tel.firstChild.setAttribute( "value", "" );\n\treturn el.firstChild.getAttribute( "value" ) === "";\n}) ) {\n\taddHandle( "value", function( elem, name, isXML ) {\n\t\tif ( !isXML && elem.nodeName.toLowerCase() === "input" ) {\n\t\t\treturn elem.defaultValue;\n\t\t}\n\t});\n}\n\n// Support: IE<9\n// Use getAttributeNode to fetch booleans when getAttribute lies\nif ( !assert(function( el ) {\n\treturn el.getAttribute("disabled") == null;\n}) ) {\n\taddHandle( booleans, function( elem, name, isXML ) {\n\t\tvar val;\n\t\tif ( !isXML ) {\n\t\t\treturn elem[ name ] === true ? name.toLowerCase() :\n\t\t\t\t\t(val = elem.getAttributeNode( name )) && val.specified ?\n\t\t\t\t\tval.value :\n\t\t\t\tnull;\n\t\t}\n\t});\n}\n\nreturn Sizzle;\n\n})( window );\n\n\n\njQuery.find = Sizzle;\njQuery.expr = Sizzle.selectors;\n\n// Deprecated\njQuery.expr[ ":" ] = jQuery.expr.pseudos;\njQuery.uniqueSort = jQuery.unique = Sizzle.uniqueSort;\njQuery.text = Sizzle.getText;\njQuery.isXMLDoc = Sizzle.isXML;\njQuery.contains = Sizzle.contains;\njQuery.escapeSelector = Sizzle.escape;\n\n\n\n\nvar dir = function( elem, dir, until ) {\n\tvar matched = [],\n\t\ttruncate = until !== undefined;\n\n\twhile ( ( elem = elem[ dir ] ) && elem.nodeType !== 9 ) {\n\t\tif ( elem.nodeType === 1 ) {\n\t\t\tif ( truncate && jQuery( elem ).is( until ) ) {\n\t\t\t\tbreak;\n\t\t\t}\n\t\t\tmatched.push( elem );\n\t\t}\n\t}\n\treturn matched;\n};\n\n\nvar siblings = function( n, elem ) {\n\tvar matched = [];\n\n\tfor ( ; n; n = n.nextSibling ) {\n\t\tif ( n.nodeType === 1 && n !== elem ) {\n\t\t\tmatched.push( n );\n\t\t}\n\t}\n\n\treturn matched;\n};\n\n\nvar rneedsContext = jQuery.expr.match.needsContext;\n\n\n\nfunction nodeName( elem, name ) {\n\n  return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();\n\n};\nvar rsingleTag = ( /^<([a-z][^\\/\\0>:\\x20\\t\\r\\n\\f]*)[\\x20\\t\\r\\n\\f]*\\/?>(?:<\\/\\1>|)$/i );\n\n\n\n// Implement the identical functionality for filter and not\nfunction winnow( elements, qualifier, not ) {\n\tif ( isFunction( qualifier ) ) {\n\t\treturn jQuery.grep( elements, function( elem, i ) {\n\t\t\treturn !!qualifier.call( elem, i, elem ) !== not;\n\t\t} );\n\t}\n\n\t// Single element\n\tif ( qualifier.nodeType ) {\n\t\treturn jQuery.grep( elements, function( elem ) {\n\t\t\treturn ( elem === qualifier ) !== not;\n\t\t} );\n\t}\n\n\t// Arraylike of elements (jQuery, arguments, Array)\n\tif ( typeof qualifier !== "string" ) {\n\t\treturn jQuery.grep( elements, function( elem ) {\n\t\t\treturn ( indexOf.call( qualifier, elem ) > -1 ) !== not;\n\t\t} );\n\t}\n\n\t// Filtered directly for both simple and complex selectors\n\treturn jQuery.filter( qualifier, elements, not );\n}\n\njQuery.filter = function( expr, elems, not ) {\n\tvar elem = elems[ 0 ];\n\n\tif ( not ) {\n\t\texpr = ":not(" + expr + ")";\n\t}\n\n\tif ( elems.length === 1 && elem.nodeType === 1 ) {\n\t\treturn jQuery.find.matchesSelector( elem, expr ) ? [ elem ] : [];\n\t}\n\n\treturn jQuery.find.matches( expr, jQuery.grep( elems, function( elem ) {\n\t\treturn elem.nodeType === 1;\n\t} ) );\n};\n\njQuery.fn.extend( {\n\tfind: function( selector ) {\n\t\tvar i, ret,\n\t\t\tlen = this.length,\n\t\t\tself = this;\n\n\t\tif ( typeof selector !== "string" ) {\n\t\t\treturn this.pushStack( jQuery( selector ).filter( function() {\n\t\t\t\tfor ( i = 0; i < len; i++ ) {\n\t\t\t\t\tif ( jQuery.contains( self[ i ], this ) ) {\n\t\t\t\t\t\treturn true;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t} ) );\n\t\t}\n\n\t\tret = this.pushStack( [] );\n\n\t\tfor ( i = 0; i < len; i++ ) {\n\t\t\tjQuery.find( selector, self[ i ], ret );\n\t\t}\n\n\t\treturn len > 1 ? jQuery.uniqueSort( ret ) : ret;\n\t},\n\tfilter: function( selector ) {\n\t\treturn this.pushStack( winnow( this, selector || [], false ) );\n\t},\n\tnot: function( selector ) {\n\t\treturn this.pushStack( winnow( this, selector || [], true ) );\n\t},\n\tis: function( selector ) {\n\t\treturn !!winnow(\n\t\t\tthis,\n\n\t\t\t// If this is a positional/relative selector, check membership in the returned set\n\t\t\t// so $("p:first").is("p:last") won\'t return true for a doc with two "p".\n\t\t\ttypeof selector === "string" && rneedsContext.test( selector ) ?\n\t\t\t\tjQuery( selector ) :\n\t\t\t\tselector || [],\n\t\t\tfalse\n\t\t).length;\n\t}\n} );\n\n\n// Initialize a jQuery object\n\n\n// A central reference to the root jQuery(document)\nvar rootjQuery,\n\n\t// A simple way to check for HTML strings\n\t// Prioritize #id over <tag> to avoid XSS via location.hash (#9521)\n\t// Strict HTML recognition (#11290: must start with <)\n\t// Shortcut simple #id case for speed\n\trquickExpr = /^(?:\\s*(<[\\w\\W]+>)[^>]*|#([\\w-]+))$/,\n\n\tinit = jQuery.fn.init = function( selector, context, root ) {\n\t\tvar match, elem;\n\n\t\t// HANDLE: $(""), $(null), $(undefined), $(false)\n\t\tif ( !selector ) {\n\t\t\treturn this;\n\t\t}\n\n\t\t// Method init() accepts an alternate rootjQuery\n\t\t// so migrate can support jQuery.sub (gh-2101)\n\t\troot = root || rootjQuery;\n\n\t\t// Handle HTML strings\n\t\tif ( typeof selector === "string" ) {\n\t\t\tif ( selector[ 0 ] === "<" &&\n\t\t\t\tselector[ selector.length - 1 ] === ">" &&\n\t\t\t\tselector.length >= 3 ) {\n\n\t\t\t\t// Assume that strings that start and end with <> are HTML and skip the regex check\n\t\t\t\tmatch = [ null, selector, null ];\n\n\t\t\t} else {\n\t\t\t\tmatch = rquickExpr.exec( selector );\n\t\t\t}\n\n\t\t\t// Match html or make sure no context is specified for #id\n\t\t\tif ( match && ( match[ 1 ] || !context ) ) {\n\n\t\t\t\t// HANDLE: $(html) -> $(array)\n\t\t\t\tif ( match[ 1 ] ) {\n\t\t\t\t\tcontext = context instanceof jQuery ? context[ 0 ] : context;\n\n\t\t\t\t\t// Option to run scripts is true for back-compat\n\t\t\t\t\t// Intentionally let the error be thrown if parseHTML is not present\n\t\t\t\t\tjQuery.merge( this, jQuery.parseHTML(\n\t\t\t\t\t\tmatch[ 1 ],\n\t\t\t\t\t\tcontext && context.nodeType ? context.ownerDocument || context : document,\n\t\t\t\t\t\ttrue\n\t\t\t\t\t) );\n\n\t\t\t\t\t// HANDLE: $(html, props)\n\t\t\t\t\tif ( rsingleTag.test( match[ 1 ] ) && jQuery.isPlainObject( context ) ) {\n\t\t\t\t\t\tfor ( match in context ) {\n\n\t\t\t\t\t\t\t// Properties of context are called as methods if possible\n\t\t\t\t\t\t\tif ( isFunction( this[ match ] ) ) {\n\t\t\t\t\t\t\t\tthis[ match ]( context[ match ] );\n\n\t\t\t\t\t\t\t// ...and otherwise set as attributes\n\t\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t\tthis.attr( match, context[ match ] );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\treturn this;\n\n\t\t\t\t// HANDLE: $(#id)\n\t\t\t\t} else {\n\t\t\t\t\telem = document.getElementById( match[ 2 ] );\n\n\t\t\t\t\tif ( elem ) {\n\n\t\t\t\t\t\t// Inject the element directly into the jQuery object\n\t\t\t\t\t\tthis[ 0 ] = elem;\n\t\t\t\t\t\tthis.length = 1;\n\t\t\t\t\t}\n\t\t\t\t\treturn this;\n\t\t\t\t}\n\n\t\t\t// HANDLE: $(expr, $(...))\n\t\t\t} else if ( !context || context.jquery ) {\n\t\t\t\treturn ( context || root ).find( selector );\n\n\t\t\t// HANDLE: $(expr, context)\n\t\t\t// (which is just equivalent to: $(context).find(expr)\n\t\t\t} else {\n\t\t\t\treturn this.constructor( context ).find( selector );\n\t\t\t}\n\n\t\t// HANDLE: $(DOMElement)\n\t\t} else if ( selector.nodeType ) {\n\t\t\tthis[ 0 ] = selector;\n\t\t\tthis.length = 1;\n\t\t\treturn this;\n\n\t\t// HANDLE: $(function)\n\t\t// Shortcut for document ready\n\t\t} else if ( isFunction( selector ) ) {\n\t\t\treturn root.ready !== undefined ?\n\t\t\t\troot.ready( selector ) :\n\n\t\t\t\t// Execute immediately if ready is not present\n\t\t\t\tselector( jQuery );\n\t\t}\n\n\t\treturn jQuery.makeArray( selector, this );\n\t};\n\n// Give the init function the jQuery prototype for later instantiation\ninit.prototype = jQuery.fn;\n\n// Initialize central reference\nrootjQuery = jQuery( document );\n\n\nvar rparentsprev = /^(?:parents|prev(?:Until|All))/,\n\n\t// Methods guaranteed to produce a unique set when starting from a unique set\n\tguaranteedUnique = {\n\t\tchildren: true,\n\t\tcontents: true,\n\t\tnext: true,\n\t\tprev: true\n\t};\n\njQuery.fn.extend( {\n\thas: function( target ) {\n\t\tvar targets = jQuery( target, this ),\n\t\t\tl = targets.length;\n\n\t\treturn this.filter( function() {\n\t\t\tvar i = 0;\n\t\t\tfor ( ; i < l; i++ ) {\n\t\t\t\tif ( jQuery.contains( this, targets[ i ] ) ) {\n\t\t\t\t\treturn true;\n\t\t\t\t}\n\t\t\t}\n\t\t} );\n\t},\n\n\tclosest: function( selectors, context ) {\n\t\tvar cur,\n\t\t\ti = 0,\n\t\t\tl = this.length,\n\t\t\tmatched = [],\n\t\t\ttargets = typeof selectors !== "string" && jQuery( selectors );\n\n\t\t// Positional selectors never match, since there\'s no _selection_ context\n\t\tif ( !rneedsContext.test( selectors ) ) {\n\t\t\tfor ( ; i < l; i++ ) {\n\t\t\t\tfor ( cur = this[ i ]; cur && cur !== context; cur = cur.parentNode ) {\n\n\t\t\t\t\t// Always skip document fragments\n\t\t\t\t\tif ( cur.nodeType < 11 && ( targets ?\n\t\t\t\t\t\ttargets.index( cur ) > -1 :\n\n\t\t\t\t\t\t// Don\'t pass non-elements to Sizzle\n\t\t\t\t\t\tcur.nodeType === 1 &&\n\t\t\t\t\t\t\tjQuery.find.matchesSelector( cur, selectors ) ) ) {\n\n\t\t\t\t\t\tmatched.push( cur );\n\t\t\t\t\t\tbreak;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn this.pushStack( matched.length > 1 ? jQuery.uniqueSort( matched ) : matched );\n\t},\n\n\t// Determine the position of an element within the set\n\tindex: function( elem ) {\n\n\t\t// No argument, return index in parent\n\t\tif ( !elem ) {\n\t\t\treturn ( this[ 0 ] && this[ 0 ].parentNode ) ? this.first().prevAll().length : -1;\n\t\t}\n\n\t\t// Index in selector\n\t\tif ( typeof elem === "string" ) {\n\t\t\treturn indexOf.call( jQuery( elem ), this[ 0 ] );\n\t\t}\n\n\t\t// Locate the position of the desired element\n\t\treturn indexOf.call( this,\n\n\t\t\t// If it receives a jQuery object, the first element is used\n\t\t\telem.jquery ? elem[ 0 ] : elem\n\t\t);\n\t},\n\n\tadd: function( selector, context ) {\n\t\treturn this.pushStack(\n\t\t\tjQuery.uniqueSort(\n\t\t\t\tjQuery.merge( this.get(), jQuery( selector, context ) )\n\t\t\t)\n\t\t);\n\t},\n\n\taddBack: function( selector ) {\n\t\treturn this.add( selector == null ?\n\t\t\tthis.prevObject : this.prevObject.filter( selector )\n\t\t);\n\t}\n} );\n\nfunction sibling( cur, dir ) {\n\twhile ( ( cur = cur[ dir ] ) && cur.nodeType !== 1 ) {}\n\treturn cur;\n}\n\njQuery.each( {\n\tparent: function( elem ) {\n\t\tvar parent = elem.parentNode;\n\t\treturn parent && parent.nodeType !== 11 ? parent : null;\n\t},\n\tparents: function( elem ) {\n\t\treturn dir( elem, "parentNode" );\n\t},\n\tparentsUntil: function( elem, i, until ) {\n\t\treturn dir( elem, "parentNode", until );\n\t},\n\tnext: function( elem ) {\n\t\treturn sibling( elem, "nextSibling" );\n\t},\n\tprev: function( elem ) {\n\t\treturn sibling( elem, "previousSibling" );\n\t},\n\tnextAll: function( elem ) {\n\t\treturn dir( elem, "nextSibling" );\n\t},\n\tprevAll: function( elem ) {\n\t\treturn dir( elem, "previousSibling" );\n\t},\n\tnextUntil: function( elem, i, until ) {\n\t\treturn dir( elem, "nextSibling", until );\n\t},\n\tprevUntil: function( elem, i, until ) {\n\t\treturn dir( elem, "previousSibling", until );\n\t},\n\tsiblings: function( elem ) {\n\t\treturn siblings( ( elem.parentNode || {} ).firstChild, elem );\n\t},\n\tchildren: function( elem ) {\n\t\treturn siblings( elem.firstChild );\n\t},\n\tcontents: function( elem ) {\n        if ( nodeName( elem, "iframe" ) ) {\n            return elem.contentDocument;\n        }\n\n        // Support: IE 9 - 11 only, iOS 7 only, Android Browser <=4.3 only\n        // Treat the template element as a regular one in browsers that\n        // don\'t support it.\n        if ( nodeName( elem, "template" ) ) {\n            elem = elem.content || elem;\n        }\n\n        return jQuery.merge( [], elem.childNodes );\n\t}\n}, function( name, fn ) {\n\tjQuery.fn[ name ] = function( until, selector ) {\n\t\tvar matched = jQuery.map( this, fn, until );\n\n\t\tif ( name.slice( -5 ) !== "Until" ) {\n\t\t\tselector = until;\n\t\t}\n\n\t\tif ( selector && typeof selector === "string" ) {\n\t\t\tmatched = jQuery.filter( selector, matched );\n\t\t}\n\n\t\tif ( this.length > 1 ) {\n\n\t\t\t// Remove duplicates\n\t\t\tif ( !guaranteedUnique[ name ] ) {\n\t\t\t\tjQuery.uniqueSort( matched );\n\t\t\t}\n\n\t\t\t// Reverse order for parents* and prev-derivatives\n\t\t\tif ( rparentsprev.test( name ) ) {\n\t\t\t\tmatched.reverse();\n\t\t\t}\n\t\t}\n\n\t\treturn this.pushStack( matched );\n\t};\n} );\nvar rnothtmlwhite = ( /[^\\x20\\t\\r\\n\\f]+/g );\n\n\n\n// Convert String-formatted options into Object-formatted ones\nfunction createOptions( options ) {\n\tvar object = {};\n\tjQuery.each( options.match( rnothtmlwhite ) || [], function( _, flag ) {\n\t\tobject[ flag ] = true;\n\t} );\n\treturn object;\n}\n\n/*\n * Create a callback list using the following parameters:\n *\n *\toptions: an optional list of space-separated options that will change how\n *\t\t\tthe callback list behaves or a more traditional option object\n *\n * By default a callback list will act like an event callback list and can be\n * "fired" multiple times.\n *\n * Possible options:\n *\n *\tonce:\t\t\twill ensure the callback list can only be fired once (like a Deferred)\n *\n *\tmemory:\t\t\twill keep track of previous values and will call any callback added\n *\t\t\t\t\tafter the list has been fired right away with the latest "memorized"\n *\t\t\t\t\tvalues (like a Deferred)\n *\n *\tunique:\t\t\twill ensure a callback can only be added once (no duplicate in the list)\n *\n *\tstopOnFalse:\tinterrupt callings when a callback returns false\n *\n */\njQuery.Callbacks = function( options ) {\n\n\t// Convert options from String-formatted to Object-formatted if needed\n\t// (we check in cache first)\n\toptions = typeof options === "string" ?\n\t\tcreateOptions( options ) :\n\t\tjQuery.extend( {}, options );\n\n\tvar // Flag to know if list is currently firing\n\t\tfiring,\n\n\t\t// Last fire value for non-forgettable lists\n\t\tmemory,\n\n\t\t// Flag to know if list was already fired\n\t\tfired,\n\n\t\t// Flag to prevent firing\n\t\tlocked,\n\n\t\t// Actual callback list\n\t\tlist = [],\n\n\t\t// Queue of execution data for repeatable lists\n\t\tqueue = [],\n\n\t\t// Index of currently firing callback (modified by add/remove as needed)\n\t\tfiringIndex = -1,\n\n\t\t// Fire callbacks\n\t\tfire = function() {\n\n\t\t\t// Enforce single-firing\n\t\t\tlocked = locked || options.once;\n\n\t\t\t// Execute callbacks for all pending executions,\n\t\t\t// respecting firingIndex overrides and runtime changes\n\t\t\tfired = firing = true;\n\t\t\tfor ( ; queue.length; firingIndex = -1 ) {\n\t\t\t\tmemory = queue.shift();\n\t\t\t\twhile ( ++firingIndex < list.length ) {\n\n\t\t\t\t\t// Run callback and check for early termination\n\t\t\t\t\tif ( list[ firingIndex ].apply( memory[ 0 ], memory[ 1 ] ) === false &&\n\t\t\t\t\t\toptions.stopOnFalse ) {\n\n\t\t\t\t\t\t// Jump to end and forget the data so .add doesn\'t re-fire\n\t\t\t\t\t\tfiringIndex = list.length;\n\t\t\t\t\t\tmemory = false;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Forget the data if we\'re done with it\n\t\t\tif ( !options.memory ) {\n\t\t\t\tmemory = false;\n\t\t\t}\n\n\t\t\tfiring = false;\n\n\t\t\t// Clean up if we\'re done firing for good\n\t\t\tif ( locked ) {\n\n\t\t\t\t// Keep an empty list if we have data for future add calls\n\t\t\t\tif ( memory ) {\n\t\t\t\t\tlist = [];\n\n\t\t\t\t// Otherwise, this object is spent\n\t\t\t\t} else {\n\t\t\t\t\tlist = "";\n\t\t\t\t}\n\t\t\t}\n\t\t},\n\n\t\t// Actual Callbacks object\n\t\tself = {\n\n\t\t\t// Add a callback or a collection of callbacks to the list\n\t\t\tadd: function() {\n\t\t\t\tif ( list ) {\n\n\t\t\t\t\t// If we have memory from a past run, we should fire after adding\n\t\t\t\t\tif ( memory && !firing ) {\n\t\t\t\t\t\tfiringIndex = list.length - 1;\n\t\t\t\t\t\tqueue.push( memory );\n\t\t\t\t\t}\n\n\t\t\t\t\t( function add( args ) {\n\t\t\t\t\t\tjQuery.each( args, function( _, arg ) {\n\t\t\t\t\t\t\tif ( isFunction( arg ) ) {\n\t\t\t\t\t\t\t\tif ( !options.unique || !self.has( arg ) ) {\n\t\t\t\t\t\t\t\t\tlist.push( arg );\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t} else if ( arg && arg.length && toType( arg ) !== "string" ) {\n\n\t\t\t\t\t\t\t\t// Inspect recursively\n\t\t\t\t\t\t\t\tadd( arg );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t} );\n\t\t\t\t\t} )( arguments );\n\n\t\t\t\t\tif ( memory && !firing ) {\n\t\t\t\t\t\tfire();\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t\treturn this;\n\t\t\t},\n\n\t\t\t// Remove a callback from the list\n\t\t\tremove: function() {\n\t\t\t\tjQuery.each( arguments, function( _, arg ) {\n\t\t\t\t\tvar index;\n\t\t\t\t\twhile ( ( index = jQuery.inArray( arg, list, index ) ) > -1 ) {\n\t\t\t\t\t\tlist.splice( index, 1 );\n\n\t\t\t\t\t\t// Handle firing indexes\n\t\t\t\t\t\tif ( index <= firingIndex ) {\n\t\t\t\t\t\t\tfiringIndex--;\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t} );\n\t\t\t\treturn this;\n\t\t\t},\n\n\t\t\t// Check if a given callback is in the list.\n\t\t\t// If no argument is given, return whether or not list has callbacks attached.\n\t\t\thas: function( fn ) {\n\t\t\t\treturn fn ?\n\t\t\t\t\tjQuery.inArray( fn, list ) > -1 :\n\t\t\t\t\tlist.length > 0;\n\t\t\t},\n\n\t\t\t// Remove all callbacks from the list\n\t\t\tempty: function() {\n\t\t\t\tif ( list ) {\n\t\t\t\t\tlist = [];\n\t\t\t\t}\n\t\t\t\treturn this;\n\t\t\t},\n\n\t\t\t// Disable .fire and .add\n\t\t\t// Abort any current/pending executions\n\t\t\t// Clear all callbacks and values\n\t\t\tdisable: function() {\n\t\t\t\tlocked = queue = [];\n\t\t\t\tlist = memory = "";\n\t\t\t\treturn this;\n\t\t\t},\n\t\t\tdisabled: function() {\n\t\t\t\treturn !list;\n\t\t\t},\n\n\t\t\t// Disable .fire\n\t\t\t// Also disable .add unless we have memory (since it would have no effect)\n\t\t\t// Abort any pending executions\n\t\t\tlock: function() {\n\t\t\t\tlocked = queue = [];\n\t\t\t\tif ( !memory && !firing ) {\n\t\t\t\t\tlist = memory = "";\n\t\t\t\t}\n\t\t\t\treturn this;\n\t\t\t},\n\t\t\tlocked: function() {\n\t\t\t\treturn !!locked;\n\t\t\t},\n\n\t\t\t// Call all callbacks with the given context and arguments\n\t\t\tfireWith: function( context, args ) {\n\t\t\t\tif ( !locked ) {\n\t\t\t\t\targs = args || [];\n\t\t\t\t\targs = [ context, args.slice ? args.slice() : args ];\n\t\t\t\t\tqueue.push( args );\n\t\t\t\t\tif ( !firing ) {\n\t\t\t\t\t\tfire();\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t\treturn this;\n\t\t\t},\n\n\t\t\t// Call all the callbacks with the given arguments\n\t\t\tfire: function() {\n\t\t\t\tself.fireWith( this, arguments );\n\t\t\t\treturn this;\n\t\t\t},\n\n\t\t\t// To know if the callbacks have already been called at least once\n\t\t\tfired: function() {\n\t\t\t\treturn !!fired;\n\t\t\t}\n\t\t};\n\n\treturn self;\n};\n\n\nfunction Identity( v ) {\n\treturn v;\n}\nfunction Thrower( ex ) {\n\tthrow ex;\n}\n\nfunction adoptValue( value, resolve, reject, noValue ) {\n\tvar method;\n\n\ttry {\n\n\t\t// Check for promise aspect first to privilege synchronous behavior\n\t\tif ( value && isFunction( ( method = value.promise ) ) ) {\n\t\t\tmethod.call( value ).done( resolve ).fail( reject );\n\n\t\t// Other thenables\n\t\t} else if ( value && isFunction( ( method = value.then ) ) ) {\n\t\t\tmethod.call( value, resolve, reject );\n\n\t\t// Other non-thenables\n\t\t} else {\n\n\t\t\t// Control `resolve` arguments by letting Array#slice cast boolean `noValue` to integer:\n\t\t\t// * false: [ value ].slice( 0 ) => resolve( value )\n\t\t\t// * true: [ value ].slice( 1 ) => resolve()\n\t\t\tresolve.apply( undefined, [ value ].slice( noValue ) );\n\t\t}\n\n\t// For Promises/A+, convert exceptions into rejections\n\t// Since jQuery.when doesn\'t unwrap thenables, we can skip the extra checks appearing in\n\t// Deferred#then to conditionally suppress rejection.\n\t} catch ( value ) {\n\n\t\t// Support: Android 4.0 only\n\t\t// Strict mode functions invoked without .call/.apply get global-object context\n\t\treject.apply( undefined, [ value ] );\n\t}\n}\n\njQuery.extend( {\n\n\tDeferred: function( func ) {\n\t\tvar tuples = [\n\n\t\t\t\t// action, add listener, callbacks,\n\t\t\t\t// ... .then handlers, argument index, [final state]\n\t\t\t\t[ "notify", "progress", jQuery.Callbacks( "memory" ),\n\t\t\t\t\tjQuery.Callbacks( "memory" ), 2 ],\n\t\t\t\t[ "resolve", "done", jQuery.Callbacks( "once memory" ),\n\t\t\t\t\tjQuery.Callbacks( "once memory" ), 0, "resolved" ],\n\t\t\t\t[ "reject", "fail", jQuery.Callbacks( "once memory" ),\n\t\t\t\t\tjQuery.Callbacks( "once memory" ), 1, "rejected" ]\n\t\t\t],\n\t\t\tstate = "pending",\n\t\t\tpromise = {\n\t\t\t\tstate: function() {\n\t\t\t\t\treturn state;\n\t\t\t\t},\n\t\t\t\talways: function() {\n\t\t\t\t\tdeferred.done( arguments ).fail( arguments );\n\t\t\t\t\treturn this;\n\t\t\t\t},\n\t\t\t\t"catch": function( fn ) {\n\t\t\t\t\treturn promise.then( null, fn );\n\t\t\t\t},\n\n\t\t\t\t// Keep pipe for back-compat\n\t\t\t\tpipe: function( /* fnDone, fnFail, fnProgress */ ) {\n\t\t\t\t\tvar fns = arguments;\n\n\t\t\t\t\treturn jQuery.Deferred( function( newDefer ) {\n\t\t\t\t\t\tjQuery.each( tuples, function( i, tuple ) {\n\n\t\t\t\t\t\t\t// Map tuples (progress, done, fail) to arguments (done, fail, progress)\n\t\t\t\t\t\t\tvar fn = isFunction( fns[ tuple[ 4 ] ] ) && fns[ tuple[ 4 ] ];\n\n\t\t\t\t\t\t\t// deferred.progress(function() { bind to newDefer or newDefer.notify })\n\t\t\t\t\t\t\t// deferred.done(function() { bind to newDefer or newDefer.resolve })\n\t\t\t\t\t\t\t// deferred.fail(function() { bind to newDefer or newDefer.reject })\n\t\t\t\t\t\t\tdeferred[ tuple[ 1 ] ]( function() {\n\t\t\t\t\t\t\t\tvar returned = fn && fn.apply( this, arguments );\n\t\t\t\t\t\t\t\tif ( returned && isFunction( returned.promise ) ) {\n\t\t\t\t\t\t\t\t\treturned.promise()\n\t\t\t\t\t\t\t\t\t\t.progress( newDefer.notify )\n\t\t\t\t\t\t\t\t\t\t.done( newDefer.resolve )\n\t\t\t\t\t\t\t\t\t\t.fail( newDefer.reject );\n\t\t\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t\t\tnewDefer[ tuple[ 0 ] + "With" ](\n\t\t\t\t\t\t\t\t\t\tthis,\n\t\t\t\t\t\t\t\t\t\tfn ? [ returned ] : arguments\n\t\t\t\t\t\t\t\t\t);\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t} );\n\t\t\t\t\t\t} );\n\t\t\t\t\t\tfns = null;\n\t\t\t\t\t} ).promise();\n\t\t\t\t},\n\t\t\t\tthen: function( onFulfilled, onRejected, onProgress ) {\n\t\t\t\t\tvar maxDepth = 0;\n\t\t\t\t\tfunction resolve( depth, deferred, handler, special ) {\n\t\t\t\t\t\treturn function() {\n\t\t\t\t\t\t\tvar that = this,\n\t\t\t\t\t\t\t\targs = arguments,\n\t\t\t\t\t\t\t\tmightThrow = function() {\n\t\t\t\t\t\t\t\t\tvar returned, then;\n\n\t\t\t\t\t\t\t\t\t// Support: Promises/A+ section 2.3.3.3.3\n\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-59\n\t\t\t\t\t\t\t\t\t// Ignore double-resolution attempts\n\t\t\t\t\t\t\t\t\tif ( depth < maxDepth ) {\n\t\t\t\t\t\t\t\t\t\treturn;\n\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\treturned = handler.apply( that, args );\n\n\t\t\t\t\t\t\t\t\t// Support: Promises/A+ section 2.3.1\n\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-48\n\t\t\t\t\t\t\t\t\tif ( returned === deferred.promise() ) {\n\t\t\t\t\t\t\t\t\t\tthrow new TypeError( "Thenable self-resolution" );\n\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t// Support: Promises/A+ sections 2.3.3.1, 3.5\n\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-54\n\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-75\n\t\t\t\t\t\t\t\t\t// Retrieve `then` only once\n\t\t\t\t\t\t\t\t\tthen = returned &&\n\n\t\t\t\t\t\t\t\t\t\t// Support: Promises/A+ section 2.3.4\n\t\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-64\n\t\t\t\t\t\t\t\t\t\t// Only check objects and functions for thenability\n\t\t\t\t\t\t\t\t\t\t( typeof returned === "object" ||\n\t\t\t\t\t\t\t\t\t\t\ttypeof returned === "function" ) &&\n\t\t\t\t\t\t\t\t\t\treturned.then;\n\n\t\t\t\t\t\t\t\t\t// Handle a returned thenable\n\t\t\t\t\t\t\t\t\tif ( isFunction( then ) ) {\n\n\t\t\t\t\t\t\t\t\t\t// Special processors (notify) just wait for resolution\n\t\t\t\t\t\t\t\t\t\tif ( special ) {\n\t\t\t\t\t\t\t\t\t\t\tthen.call(\n\t\t\t\t\t\t\t\t\t\t\t\treturned,\n\t\t\t\t\t\t\t\t\t\t\t\tresolve( maxDepth, deferred, Identity, special ),\n\t\t\t\t\t\t\t\t\t\t\t\tresolve( maxDepth, deferred, Thrower, special )\n\t\t\t\t\t\t\t\t\t\t\t);\n\n\t\t\t\t\t\t\t\t\t\t// Normal processors (resolve) also hook into progress\n\t\t\t\t\t\t\t\t\t\t} else {\n\n\t\t\t\t\t\t\t\t\t\t\t// ...and disregard older resolution values\n\t\t\t\t\t\t\t\t\t\t\tmaxDepth++;\n\n\t\t\t\t\t\t\t\t\t\t\tthen.call(\n\t\t\t\t\t\t\t\t\t\t\t\treturned,\n\t\t\t\t\t\t\t\t\t\t\t\tresolve( maxDepth, deferred, Identity, special ),\n\t\t\t\t\t\t\t\t\t\t\t\tresolve( maxDepth, deferred, Thrower, special ),\n\t\t\t\t\t\t\t\t\t\t\t\tresolve( maxDepth, deferred, Identity,\n\t\t\t\t\t\t\t\t\t\t\t\t\tdeferred.notifyWith )\n\t\t\t\t\t\t\t\t\t\t\t);\n\t\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t// Handle all other returned values\n\t\t\t\t\t\t\t\t\t} else {\n\n\t\t\t\t\t\t\t\t\t\t// Only substitute handlers pass on context\n\t\t\t\t\t\t\t\t\t\t// and multiple values (non-spec behavior)\n\t\t\t\t\t\t\t\t\t\tif ( handler !== Identity ) {\n\t\t\t\t\t\t\t\t\t\t\tthat = undefined;\n\t\t\t\t\t\t\t\t\t\t\targs = [ returned ];\n\t\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t\t// Process the value(s)\n\t\t\t\t\t\t\t\t\t\t// Default process is resolve\n\t\t\t\t\t\t\t\t\t\t( special || deferred.resolveWith )( that, args );\n\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t},\n\n\t\t\t\t\t\t\t\t// Only normal processors (resolve) catch and reject exceptions\n\t\t\t\t\t\t\t\tprocess = special ?\n\t\t\t\t\t\t\t\t\tmightThrow :\n\t\t\t\t\t\t\t\t\tfunction() {\n\t\t\t\t\t\t\t\t\t\ttry {\n\t\t\t\t\t\t\t\t\t\t\tmightThrow();\n\t\t\t\t\t\t\t\t\t\t} catch ( e ) {\n\n\t\t\t\t\t\t\t\t\t\t\tif ( jQuery.Deferred.exceptionHook ) {\n\t\t\t\t\t\t\t\t\t\t\t\tjQuery.Deferred.exceptionHook( e,\n\t\t\t\t\t\t\t\t\t\t\t\t\tprocess.stackTrace );\n\t\t\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t\t\t// Support: Promises/A+ section 2.3.3.3.4.1\n\t\t\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-61\n\t\t\t\t\t\t\t\t\t\t\t// Ignore post-resolution exceptions\n\t\t\t\t\t\t\t\t\t\t\tif ( depth + 1 >= maxDepth ) {\n\n\t\t\t\t\t\t\t\t\t\t\t\t// Only substitute handlers pass on context\n\t\t\t\t\t\t\t\t\t\t\t\t// and multiple values (non-spec behavior)\n\t\t\t\t\t\t\t\t\t\t\t\tif ( handler !== Thrower ) {\n\t\t\t\t\t\t\t\t\t\t\t\t\tthat = undefined;\n\t\t\t\t\t\t\t\t\t\t\t\t\targs = [ e ];\n\t\t\t\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t\t\t\tdeferred.rejectWith( that, args );\n\t\t\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t\t};\n\n\t\t\t\t\t\t\t// Support: Promises/A+ section 2.3.3.3.1\n\t\t\t\t\t\t\t// https://promisesaplus.com/#point-57\n\t\t\t\t\t\t\t// Re-resolve promises immediately to dodge false rejection from\n\t\t\t\t\t\t\t// subsequent errors\n\t\t\t\t\t\t\tif ( depth ) {\n\t\t\t\t\t\t\t\tprocess();\n\t\t\t\t\t\t\t} else {\n\n\t\t\t\t\t\t\t\t// Call an optional hook to record the stack, in case of exception\n\t\t\t\t\t\t\t\t// since it\'s otherwise lost when execution goes async\n\t\t\t\t\t\t\t\tif ( jQuery.Deferred.getStackHook ) {\n\t\t\t\t\t\t\t\t\tprocess.stackTrace = jQuery.Deferred.getStackHook();\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\twindow.setTimeout( process );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t};\n\t\t\t\t\t}\n\n\t\t\t\t\treturn jQuery.Deferred( function( newDefer ) {\n\n\t\t\t\t\t\t// progress_handlers.add( ... )\n\t\t\t\t\t\ttuples[ 0 ][ 3 ].add(\n\t\t\t\t\t\t\tresolve(\n\t\t\t\t\t\t\t\t0,\n\t\t\t\t\t\t\t\tnewDefer,\n\t\t\t\t\t\t\t\tisFunction( onProgress ) ?\n\t\t\t\t\t\t\t\t\tonProgress :\n\t\t\t\t\t\t\t\t\tIdentity,\n\t\t\t\t\t\t\t\tnewDefer.notifyWith\n\t\t\t\t\t\t\t)\n\t\t\t\t\t\t);\n\n\t\t\t\t\t\t// fulfilled_handlers.add( ... )\n\t\t\t\t\t\ttuples[ 1 ][ 3 ].add(\n\t\t\t\t\t\t\tresolve(\n\t\t\t\t\t\t\t\t0,\n\t\t\t\t\t\t\t\tnewDefer,\n\t\t\t\t\t\t\t\tisFunction( onFulfilled ) ?\n\t\t\t\t\t\t\t\t\tonFulfilled :\n\t\t\t\t\t\t\t\t\tIdentity\n\t\t\t\t\t\t\t)\n\t\t\t\t\t\t);\n\n\t\t\t\t\t\t// rejected_handlers.add( ... )\n\t\t\t\t\t\ttuples[ 2 ][ 3 ].add(\n\t\t\t\t\t\t\tresolve(\n\t\t\t\t\t\t\t\t0,\n\t\t\t\t\t\t\t\tnewDefer,\n\t\t\t\t\t\t\t\tisFunction( onRejected ) ?\n\t\t\t\t\t\t\t\t\tonRejected :\n\t\t\t\t\t\t\t\t\tThrower\n\t\t\t\t\t\t\t)\n\t\t\t\t\t\t);\n\t\t\t\t\t} ).promise();\n\t\t\t\t},\n\n\t\t\t\t// Get a promise for this deferred\n\t\t\t\t// If obj is provided, the promise aspect is added to the object\n\t\t\t\tpromise: function( obj ) {\n\t\t\t\t\treturn obj != null ? jQuery.extend( obj, promise ) : promise;\n\t\t\t\t}\n\t\t\t},\n\t\t\tdeferred = {};\n\n\t\t// Add list-specific methods\n\t\tjQuery.each( tuples, function( i, tuple ) {\n\t\t\tvar list = tuple[ 2 ],\n\t\t\t\tstateString = tuple[ 5 ];\n\n\t\t\t// promise.progress = list.add\n\t\t\t// promise.done = list.add\n\t\t\t// promise.fail = list.add\n\t\t\tpromise[ tuple[ 1 ] ] = list.add;\n\n\t\t\t// Handle state\n\t\t\tif ( stateString ) {\n\t\t\t\tlist.add(\n\t\t\t\t\tfunction() {\n\n\t\t\t\t\t\t// state = "resolved" (i.e., fulfilled)\n\t\t\t\t\t\t// state = "rejected"\n\t\t\t\t\t\tstate = stateString;\n\t\t\t\t\t},\n\n\t\t\t\t\t// rejected_callbacks.disable\n\t\t\t\t\t// fulfilled_callbacks.disable\n\t\t\t\t\ttuples[ 3 - i ][ 2 ].disable,\n\n\t\t\t\t\t// rejected_handlers.disable\n\t\t\t\t\t// fulfilled_handlers.disable\n\t\t\t\t\ttuples[ 3 - i ][ 3 ].disable,\n\n\t\t\t\t\t// progress_callbacks.lock\n\t\t\t\t\ttuples[ 0 ][ 2 ].lock,\n\n\t\t\t\t\t// progress_handlers.lock\n\t\t\t\t\ttuples[ 0 ][ 3 ].lock\n\t\t\t\t);\n\t\t\t}\n\n\t\t\t// progress_handlers.fire\n\t\t\t// fulfilled_handlers.fire\n\t\t\t// rejected_handlers.fire\n\t\t\tlist.add( tuple[ 3 ].fire );\n\n\t\t\t// deferred.notify = function() { deferred.notifyWith(...) }\n\t\t\t// deferred.resolve = function() { deferred.resolveWith(...) }\n\t\t\t// deferred.reject = function() { deferred.rejectWith(...) }\n\t\t\tdeferred[ tuple[ 0 ] ] = function() {\n\t\t\t\tdeferred[ tuple[ 0 ] + "With" ]( this === deferred ? undefined : this, arguments );\n\t\t\t\treturn this;\n\t\t\t};\n\n\t\t\t// deferred.notifyWith = list.fireWith\n\t\t\t// deferred.resolveWith = list.fireWith\n\t\t\t// deferred.rejectWith = list.fireWith\n\t\t\tdeferred[ tuple[ 0 ] + "With" ] = list.fireWith;\n\t\t} );\n\n\t\t// Make the deferred a promise\n\t\tpromise.promise( deferred );\n\n\t\t// Call given func if any\n\t\tif ( func ) {\n\t\t\tfunc.call( deferred, deferred );\n\t\t}\n\n\t\t// All done!\n\t\treturn deferred;\n\t},\n\n\t// Deferred helper\n\twhen: function( singleValue ) {\n\t\tvar\n\n\t\t\t// count of uncompleted subordinates\n\t\t\tremaining = arguments.length,\n\n\t\t\t// count of unprocessed arguments\n\t\t\ti = remaining,\n\n\t\t\t// subordinate fulfillment data\n\t\t\tresolveContexts = Array( i ),\n\t\t\tresolveValues = slice.call( arguments ),\n\n\t\t\t// the master Deferred\n\t\t\tmaster = jQuery.Deferred(),\n\n\t\t\t// subordinate callback factory\n\t\t\tupdateFunc = function( i ) {\n\t\t\t\treturn function( value ) {\n\t\t\t\t\tresolveContexts[ i ] = this;\n\t\t\t\t\tresolveValues[ i ] = arguments.length > 1 ? slice.call( arguments ) : value;\n\t\t\t\t\tif ( !( --remaining ) ) {\n\t\t\t\t\t\tmaster.resolveWith( resolveContexts, resolveValues );\n\t\t\t\t\t}\n\t\t\t\t};\n\t\t\t};\n\n\t\t// Single- and empty arguments are adopted like Promise.resolve\n\t\tif ( remaining <= 1 ) {\n\t\t\tadoptValue( singleValue, master.done( updateFunc( i ) ).resolve, master.reject,\n\t\t\t\t!remaining );\n\n\t\t\t// Use .then() to unwrap secondary thenables (cf. gh-3000)\n\t\t\tif ( master.state() === "pending" ||\n\t\t\t\tisFunction( resolveValues[ i ] && resolveValues[ i ].then ) ) {\n\n\t\t\t\treturn master.then();\n\t\t\t}\n\t\t}\n\n\t\t// Multiple arguments are aggregated like Promise.all array elements\n\t\twhile ( i-- ) {\n\t\t\tadoptValue( resolveValues[ i ], updateFunc( i ), master.reject );\n\t\t}\n\n\t\treturn master.promise();\n\t}\n} );\n\n\n// These usually indicate a programmer mistake during development,\n// warn about them ASAP rather than swallowing them by default.\nvar rerrorNames = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;\n\njQuery.Deferred.exceptionHook = function( error, stack ) {\n\n\t// Support: IE 8 - 9 only\n\t// Console exists when dev tools are open, which can happen at any time\n\tif ( window.console && window.console.warn && error && rerrorNames.test( error.name ) ) {\n\t\twindow.console.warn( "jQuery.Deferred exception: " + error.message, error.stack, stack );\n\t}\n};\n\n\n\n\njQuery.readyException = function( error ) {\n\twindow.setTimeout( function() {\n\t\tthrow error;\n\t} );\n};\n\n\n\n\n// The deferred used on DOM ready\nvar readyList = jQuery.Deferred();\n\njQuery.fn.ready = function( fn ) {\n\n\treadyList\n\t\t.then( fn )\n\n\t\t// Wrap jQuery.readyException in a function so that the lookup\n\t\t// happens at the time of error handling instead of callback\n\t\t// registration.\n\t\t.catch( function( error ) {\n\t\t\tjQuery.readyException( error );\n\t\t} );\n\n\treturn this;\n};\n\njQuery.extend( {\n\n\t// Is the DOM ready to be used? Set to true once it occurs.\n\tisReady: false,\n\n\t// A counter to track how many items to wait for before\n\t// the ready event fires. See #6781\n\treadyWait: 1,\n\n\t// Handle when the DOM is ready\n\tready: function( wait ) {\n\n\t\t// Abort if there are pending holds or we\'re already ready\n\t\tif ( wait === true ? --jQuery.readyWait : jQuery.isReady ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Remember that the DOM is ready\n\t\tjQuery.isReady = true;\n\n\t\t// If a normal DOM Ready event fired, decrement, and wait if need be\n\t\tif ( wait !== true && --jQuery.readyWait > 0 ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// If there are functions bound, to execute\n\t\treadyList.resolveWith( document, [ jQuery ] );\n\t}\n} );\n\njQuery.ready.then = readyList.then;\n\n// The ready event handler and self cleanup method\nfunction completed() {\n\tdocument.removeEventListener( "DOMContentLoaded", completed );\n\twindow.removeEventListener( "load", completed );\n\tjQuery.ready();\n}\n\n// Catch cases where $(document).ready() is called\n// after the browser event has already occurred.\n// Support: IE <=9 - 10 only\n// Older IE sometimes signals "interactive" too soon\nif ( document.readyState === "complete" ||\n\t( document.readyState !== "loading" && !document.documentElement.doScroll ) ) {\n\n\t// Handle it asynchronously to allow scripts the opportunity to delay ready\n\twindow.setTimeout( jQuery.ready );\n\n} else {\n\n\t// Use the handy event callback\n\tdocument.addEventListener( "DOMContentLoaded", completed );\n\n\t// A fallback to window.onload, that will always work\n\twindow.addEventListener( "load", completed );\n}\n\n\n\n\n// Multifunctional method to get and set values of a collection\n// The value/s can optionally be executed if it\'s a function\nvar access = function( elems, fn, key, value, chainable, emptyGet, raw ) {\n\tvar i = 0,\n\t\tlen = elems.length,\n\t\tbulk = key == null;\n\n\t// Sets many values\n\tif ( toType( key ) === "object" ) {\n\t\tchainable = true;\n\t\tfor ( i in key ) {\n\t\t\taccess( elems, fn, i, key[ i ], true, emptyGet, raw );\n\t\t}\n\n\t// Sets one value\n\t} else if ( value !== undefined ) {\n\t\tchainable = true;\n\n\t\tif ( !isFunction( value ) ) {\n\t\t\traw = true;\n\t\t}\n\n\t\tif ( bulk ) {\n\n\t\t\t// Bulk operations run against the entire set\n\t\t\tif ( raw ) {\n\t\t\t\tfn.call( elems, value );\n\t\t\t\tfn = null;\n\n\t\t\t// ...except when executing function values\n\t\t\t} else {\n\t\t\t\tbulk = fn;\n\t\t\t\tfn = function( elem, key, value ) {\n\t\t\t\t\treturn bulk.call( jQuery( elem ), value );\n\t\t\t\t};\n\t\t\t}\n\t\t}\n\n\t\tif ( fn ) {\n\t\t\tfor ( ; i < len; i++ ) {\n\t\t\t\tfn(\n\t\t\t\t\telems[ i ], key, raw ?\n\t\t\t\t\tvalue :\n\t\t\t\t\tvalue.call( elems[ i ], i, fn( elems[ i ], key ) )\n\t\t\t\t);\n\t\t\t}\n\t\t}\n\t}\n\n\tif ( chainable ) {\n\t\treturn elems;\n\t}\n\n\t// Gets\n\tif ( bulk ) {\n\t\treturn fn.call( elems );\n\t}\n\n\treturn len ? fn( elems[ 0 ], key ) : emptyGet;\n};\n\n\n// Matches dashed string for camelizing\nvar rmsPrefix = /^-ms-/,\n\trdashAlpha = /-([a-z])/g;\n\n// Used by camelCase as callback to replace()\nfunction fcamelCase( all, letter ) {\n\treturn letter.toUpperCase();\n}\n\n// Convert dashed to camelCase; used by the css and data modules\n// Support: IE <=9 - 11, Edge 12 - 15\n// Microsoft forgot to hump their vendor prefix (#9572)\nfunction camelCase( string ) {\n\treturn string.replace( rmsPrefix, "ms-" ).replace( rdashAlpha, fcamelCase );\n}\nvar acceptData = function( owner ) {\n\n\t// Accepts only:\n\t//  - Node\n\t//    - Node.ELEMENT_NODE\n\t//    - Node.DOCUMENT_NODE\n\t//  - Object\n\t//    - Any\n\treturn owner.nodeType === 1 || owner.nodeType === 9 || !( +owner.nodeType );\n};\n\n\n\n\nfunction Data() {\n\tthis.expando = jQuery.expando + Data.uid++;\n}\n\nData.uid = 1;\n\nData.prototype = {\n\n\tcache: function( owner ) {\n\n\t\t// Check if the owner object already has a cache\n\t\tvar value = owner[ this.expando ];\n\n\t\t// If not, create one\n\t\tif ( !value ) {\n\t\t\tvalue = {};\n\n\t\t\t// We can accept data for non-element nodes in modern browsers,\n\t\t\t// but we should not, see #8335.\n\t\t\t// Always return an empty object.\n\t\t\tif ( acceptData( owner ) ) {\n\n\t\t\t\t// If it is a node unlikely to be stringify-ed or looped over\n\t\t\t\t// use plain assignment\n\t\t\t\tif ( owner.nodeType ) {\n\t\t\t\t\towner[ this.expando ] = value;\n\n\t\t\t\t// Otherwise secure it in a non-enumerable property\n\t\t\t\t// configurable must be true to allow the property to be\n\t\t\t\t// deleted when data is removed\n\t\t\t\t} else {\n\t\t\t\t\tObject.defineProperty( owner, this.expando, {\n\t\t\t\t\t\tvalue: value,\n\t\t\t\t\t\tconfigurable: true\n\t\t\t\t\t} );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn value;\n\t},\n\tset: function( owner, data, value ) {\n\t\tvar prop,\n\t\t\tcache = this.cache( owner );\n\n\t\t// Handle: [ owner, key, value ] args\n\t\t// Always use camelCase key (gh-2257)\n\t\tif ( typeof data === "string" ) {\n\t\t\tcache[ camelCase( data ) ] = value;\n\n\t\t// Handle: [ owner, { properties } ] args\n\t\t} else {\n\n\t\t\t// Copy the properties one-by-one to the cache object\n\t\t\tfor ( prop in data ) {\n\t\t\t\tcache[ camelCase( prop ) ] = data[ prop ];\n\t\t\t}\n\t\t}\n\t\treturn cache;\n\t},\n\tget: function( owner, key ) {\n\t\treturn key === undefined ?\n\t\t\tthis.cache( owner ) :\n\n\t\t\t// Always use camelCase key (gh-2257)\n\t\t\towner[ this.expando ] && owner[ this.expando ][ camelCase( key ) ];\n\t},\n\taccess: function( owner, key, value ) {\n\n\t\t// In cases where either:\n\t\t//\n\t\t//   1. No key was specified\n\t\t//   2. A string key was specified, but no value provided\n\t\t//\n\t\t// Take the "read" path and allow the get method to determine\n\t\t// which value to return, respectively either:\n\t\t//\n\t\t//   1. The entire cache object\n\t\t//   2. The data stored at the key\n\t\t//\n\t\tif ( key === undefined ||\n\t\t\t\t( ( key && typeof key === "string" ) && value === undefined ) ) {\n\n\t\t\treturn this.get( owner, key );\n\t\t}\n\n\t\t// When the key is not a string, or both a key and value\n\t\t// are specified, set or extend (existing objects) with either:\n\t\t//\n\t\t//   1. An object of properties\n\t\t//   2. A key and value\n\t\t//\n\t\tthis.set( owner, key, value );\n\n\t\t// Since the "set" path can have two possible entry points\n\t\t// return the expected data based on which path was taken[*]\n\t\treturn value !== undefined ? value : key;\n\t},\n\tremove: function( owner, key ) {\n\t\tvar i,\n\t\t\tcache = owner[ this.expando ];\n\n\t\tif ( cache === undefined ) {\n\t\t\treturn;\n\t\t}\n\n\t\tif ( key !== undefined ) {\n\n\t\t\t// Support array or space separated string of keys\n\t\t\tif ( Array.isArray( key ) ) {\n\n\t\t\t\t// If key is an array of keys...\n\t\t\t\t// We always set camelCase keys, so remove that.\n\t\t\t\tkey = key.map( camelCase );\n\t\t\t} else {\n\t\t\t\tkey = camelCase( key );\n\n\t\t\t\t// If a key with the spaces exists, use it.\n\t\t\t\t// Otherwise, create an array by matching non-whitespace\n\t\t\t\tkey = key in cache ?\n\t\t\t\t\t[ key ] :\n\t\t\t\t\t( key.match( rnothtmlwhite ) || [] );\n\t\t\t}\n\n\t\t\ti = key.length;\n\n\t\t\twhile ( i-- ) {\n\t\t\t\tdelete cache[ key[ i ] ];\n\t\t\t}\n\t\t}\n\n\t\t// Remove the expando if there\'s no more data\n\t\tif ( key === undefined || jQuery.isEmptyObject( cache ) ) {\n\n\t\t\t// Support: Chrome <=35 - 45\n\t\t\t// Webkit & Blink performance suffers when deleting properties\n\t\t\t// from DOM nodes, so set to undefined instead\n\t\t\t// https://bugs.chromium.org/p/chromium/issues/detail?id=378607 (bug restricted)\n\t\t\tif ( owner.nodeType ) {\n\t\t\t\towner[ this.expando ] = undefined;\n\t\t\t} else {\n\t\t\t\tdelete owner[ this.expando ];\n\t\t\t}\n\t\t}\n\t},\n\thasData: function( owner ) {\n\t\tvar cache = owner[ this.expando ];\n\t\treturn cache !== undefined && !jQuery.isEmptyObject( cache );\n\t}\n};\nvar dataPriv = new Data();\n\nvar dataUser = new Data();\n\n\n\n//\tImplementation Summary\n//\n//\t1. Enforce API surface and semantic compatibility with 1.9.x branch\n//\t2. Improve the module\'s maintainability by reducing the storage\n//\t\tpaths to a single mechanism.\n//\t3. Use the same single mechanism to support "private" and "user" data.\n//\t4. _Never_ expose "private" data to user code (TODO: Drop _data, _removeData)\n//\t5. Avoid exposing implementation details on user objects (eg. expando properties)\n//\t6. Provide a clear path for implementation upgrade to WeakMap in 2014\n\nvar rbrace = /^(?:\\{[\\w\\W]*\\}|\\[[\\w\\W]*\\])$/,\n\trmultiDash = /[A-Z]/g;\n\nfunction getData( data ) {\n\tif ( data === "true" ) {\n\t\treturn true;\n\t}\n\n\tif ( data === "false" ) {\n\t\treturn false;\n\t}\n\n\tif ( data === "null" ) {\n\t\treturn null;\n\t}\n\n\t// Only convert to a number if it doesn\'t change the string\n\tif ( data === +data + "" ) {\n\t\treturn +data;\n\t}\n\n\tif ( rbrace.test( data ) ) {\n\t\treturn JSON.parse( data );\n\t}\n\n\treturn data;\n}\n\nfunction dataAttr( elem, key, data ) {\n\tvar name;\n\n\t// If nothing was found internally, try to fetch any\n\t// data from the HTML5 data-* attribute\n\tif ( data === undefined && elem.nodeType === 1 ) {\n\t\tname = "data-" + key.replace( rmultiDash, "-$&" ).toLowerCase();\n\t\tdata = elem.getAttribute( name );\n\n\t\tif ( typeof data === "string" ) {\n\t\t\ttry {\n\t\t\t\tdata = getData( data );\n\t\t\t} catch ( e ) {}\n\n\t\t\t// Make sure we set the data so it isn\'t changed later\n\t\t\tdataUser.set( elem, key, data );\n\t\t} else {\n\t\t\tdata = undefined;\n\t\t}\n\t}\n\treturn data;\n}\n\njQuery.extend( {\n\thasData: function( elem ) {\n\t\treturn dataUser.hasData( elem ) || dataPriv.hasData( elem );\n\t},\n\n\tdata: function( elem, name, data ) {\n\t\treturn dataUser.access( elem, name, data );\n\t},\n\n\tremoveData: function( elem, name ) {\n\t\tdataUser.remove( elem, name );\n\t},\n\n\t// TODO: Now that all calls to _data and _removeData have been replaced\n\t// with direct calls to dataPriv methods, these can be deprecated.\n\t_data: function( elem, name, data ) {\n\t\treturn dataPriv.access( elem, name, data );\n\t},\n\n\t_removeData: function( elem, name ) {\n\t\tdataPriv.remove( elem, name );\n\t}\n} );\n\njQuery.fn.extend( {\n\tdata: function( key, value ) {\n\t\tvar i, name, data,\n\t\t\telem = this[ 0 ],\n\t\t\tattrs = elem && elem.attributes;\n\n\t\t// Gets all values\n\t\tif ( key === undefined ) {\n\t\t\tif ( this.length ) {\n\t\t\t\tdata = dataUser.get( elem );\n\n\t\t\t\tif ( elem.nodeType === 1 && !dataPriv.get( elem, "hasDataAttrs" ) ) {\n\t\t\t\t\ti = attrs.length;\n\t\t\t\t\twhile ( i-- ) {\n\n\t\t\t\t\t\t// Support: IE 11 only\n\t\t\t\t\t\t// The attrs elements can be null (#14894)\n\t\t\t\t\t\tif ( attrs[ i ] ) {\n\t\t\t\t\t\t\tname = attrs[ i ].name;\n\t\t\t\t\t\t\tif ( name.indexOf( "data-" ) === 0 ) {\n\t\t\t\t\t\t\t\tname = camelCase( name.slice( 5 ) );\n\t\t\t\t\t\t\t\tdataAttr( elem, name, data[ name ] );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t\tdataPriv.set( elem, "hasDataAttrs", true );\n\t\t\t\t}\n\t\t\t}\n\n\t\t\treturn data;\n\t\t}\n\n\t\t// Sets multiple values\n\t\tif ( typeof key === "object" ) {\n\t\t\treturn this.each( function() {\n\t\t\t\tdataUser.set( this, key );\n\t\t\t} );\n\t\t}\n\n\t\treturn access( this, function( value ) {\n\t\t\tvar data;\n\n\t\t\t// The calling jQuery object (element matches) is not empty\n\t\t\t// (and therefore has an element appears at this[ 0 ]) and the\n\t\t\t// `value` parameter was not undefined. An empty jQuery object\n\t\t\t// will result in `undefined` for elem = this[ 0 ] which will\n\t\t\t// throw an exception if an attempt to read a data cache is made.\n\t\t\tif ( elem && value === undefined ) {\n\n\t\t\t\t// Attempt to get data from the cache\n\t\t\t\t// The key will always be camelCased in Data\n\t\t\t\tdata = dataUser.get( elem, key );\n\t\t\t\tif ( data !== undefined ) {\n\t\t\t\t\treturn data;\n\t\t\t\t}\n\n\t\t\t\t// Attempt to "discover" the data in\n\t\t\t\t// HTML5 custom data-* attrs\n\t\t\t\tdata = dataAttr( elem, key );\n\t\t\t\tif ( data !== undefined ) {\n\t\t\t\t\treturn data;\n\t\t\t\t}\n\n\t\t\t\t// We tried really hard, but the data doesn\'t exist.\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\t// Set the data...\n\t\t\tthis.each( function() {\n\n\t\t\t\t// We always store the camelCased key\n\t\t\t\tdataUser.set( this, key, value );\n\t\t\t} );\n\t\t}, null, value, arguments.length > 1, null, true );\n\t},\n\n\tremoveData: function( key ) {\n\t\treturn this.each( function() {\n\t\t\tdataUser.remove( this, key );\n\t\t} );\n\t}\n} );\n\n\njQuery.extend( {\n\tqueue: function( elem, type, data ) {\n\t\tvar queue;\n\n\t\tif ( elem ) {\n\t\t\ttype = ( type || "fx" ) + "queue";\n\t\t\tqueue = dataPriv.get( elem, type );\n\n\t\t\t// Speed up dequeue by getting out quickly if this is just a lookup\n\t\t\tif ( data ) {\n\t\t\t\tif ( !queue || Array.isArray( data ) ) {\n\t\t\t\t\tqueue = dataPriv.access( elem, type, jQuery.makeArray( data ) );\n\t\t\t\t} else {\n\t\t\t\t\tqueue.push( data );\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn queue || [];\n\t\t}\n\t},\n\n\tdequeue: function( elem, type ) {\n\t\ttype = type || "fx";\n\n\t\tvar queue = jQuery.queue( elem, type ),\n\t\t\tstartLength = queue.length,\n\t\t\tfn = queue.shift(),\n\t\t\thooks = jQuery._queueHooks( elem, type ),\n\t\t\tnext = function() {\n\t\t\t\tjQuery.dequeue( elem, type );\n\t\t\t};\n\n\t\t// If the fx queue is dequeued, always remove the progress sentinel\n\t\tif ( fn === "inprogress" ) {\n\t\t\tfn = queue.shift();\n\t\t\tstartLength--;\n\t\t}\n\n\t\tif ( fn ) {\n\n\t\t\t// Add a progress sentinel to prevent the fx queue from being\n\t\t\t// automatically dequeued\n\t\t\tif ( type === "fx" ) {\n\t\t\t\tqueue.unshift( "inprogress" );\n\t\t\t}\n\n\t\t\t// Clear up the last queue stop function\n\t\t\tdelete hooks.stop;\n\t\t\tfn.call( elem, next, hooks );\n\t\t}\n\n\t\tif ( !startLength && hooks ) {\n\t\t\thooks.empty.fire();\n\t\t}\n\t},\n\n\t// Not public - generate a queueHooks object, or return the current one\n\t_queueHooks: function( elem, type ) {\n\t\tvar key = type + "queueHooks";\n\t\treturn dataPriv.get( elem, key ) || dataPriv.access( elem, key, {\n\t\t\tempty: jQuery.Callbacks( "once memory" ).add( function() {\n\t\t\t\tdataPriv.remove( elem, [ type + "queue", key ] );\n\t\t\t} )\n\t\t} );\n\t}\n} );\n\njQuery.fn.extend( {\n\tqueue: function( type, data ) {\n\t\tvar setter = 2;\n\n\t\tif ( typeof type !== "string" ) {\n\t\t\tdata = type;\n\t\t\ttype = "fx";\n\t\t\tsetter--;\n\t\t}\n\n\t\tif ( arguments.length < setter ) {\n\t\t\treturn jQuery.queue( this[ 0 ], type );\n\t\t}\n\n\t\treturn data === undefined ?\n\t\t\tthis :\n\t\t\tthis.each( function() {\n\t\t\t\tvar queue = jQuery.queue( this, type, data );\n\n\t\t\t\t// Ensure a hooks for this queue\n\t\t\t\tjQuery._queueHooks( this, type );\n\n\t\t\t\tif ( type === "fx" && queue[ 0 ] !== "inprogress" ) {\n\t\t\t\t\tjQuery.dequeue( this, type );\n\t\t\t\t}\n\t\t\t} );\n\t},\n\tdequeue: function( type ) {\n\t\treturn this.each( function() {\n\t\t\tjQuery.dequeue( this, type );\n\t\t} );\n\t},\n\tclearQueue: function( type ) {\n\t\treturn this.queue( type || "fx", [] );\n\t},\n\n\t// Get a promise resolved when queues of a certain type\n\t// are emptied (fx is the type by default)\n\tpromise: function( type, obj ) {\n\t\tvar tmp,\n\t\t\tcount = 1,\n\t\t\tdefer = jQuery.Deferred(),\n\t\t\telements = this,\n\t\t\ti = this.length,\n\t\t\tresolve = function() {\n\t\t\t\tif ( !( --count ) ) {\n\t\t\t\t\tdefer.resolveWith( elements, [ elements ] );\n\t\t\t\t}\n\t\t\t};\n\n\t\tif ( typeof type !== "string" ) {\n\t\t\tobj = type;\n\t\t\ttype = undefined;\n\t\t}\n\t\ttype = type || "fx";\n\n\t\twhile ( i-- ) {\n\t\t\ttmp = dataPriv.get( elements[ i ], type + "queueHooks" );\n\t\t\tif ( tmp && tmp.empty ) {\n\t\t\t\tcount++;\n\t\t\t\ttmp.empty.add( resolve );\n\t\t\t}\n\t\t}\n\t\tresolve();\n\t\treturn defer.promise( obj );\n\t}\n} );\nvar pnum = ( /[+-]?(?:\\d*\\.|)\\d+(?:[eE][+-]?\\d+|)/ ).source;\n\nvar rcssNum = new RegExp( "^(?:([+-])=|)(" + pnum + ")([a-z%]*)$", "i" );\n\n\nvar cssExpand = [ "Top", "Right", "Bottom", "Left" ];\n\nvar isHiddenWithinTree = function( elem, el ) {\n\n\t\t// isHiddenWithinTree might be called from jQuery#filter function;\n\t\t// in that case, element will be second argument\n\t\telem = el || elem;\n\n\t\t// Inline style trumps all\n\t\treturn elem.style.display === "none" ||\n\t\t\telem.style.display === "" &&\n\n\t\t\t// Otherwise, check computed style\n\t\t\t// Support: Firefox <=43 - 45\n\t\t\t// Disconnected elements can have computed display: none, so first confirm that elem is\n\t\t\t// in the document.\n\t\t\tjQuery.contains( elem.ownerDocument, elem ) &&\n\n\t\t\tjQuery.css( elem, "display" ) === "none";\n\t};\n\nvar swap = function( elem, options, callback, args ) {\n\tvar ret, name,\n\t\told = {};\n\n\t// Remember the old values, and insert the new ones\n\tfor ( name in options ) {\n\t\told[ name ] = elem.style[ name ];\n\t\telem.style[ name ] = options[ name ];\n\t}\n\n\tret = callback.apply( elem, args || [] );\n\n\t// Revert the old values\n\tfor ( name in options ) {\n\t\telem.style[ name ] = old[ name ];\n\t}\n\n\treturn ret;\n};\n\n\n\n\nfunction adjustCSS( elem, prop, valueParts, tween ) {\n\tvar adjusted, scale,\n\t\tmaxIterations = 20,\n\t\tcurrentValue = tween ?\n\t\t\tfunction() {\n\t\t\t\treturn tween.cur();\n\t\t\t} :\n\t\t\tfunction() {\n\t\t\t\treturn jQuery.css( elem, prop, "" );\n\t\t\t},\n\t\tinitial = currentValue(),\n\t\tunit = valueParts && valueParts[ 3 ] || ( jQuery.cssNumber[ prop ] ? "" : "px" ),\n\n\t\t// Starting value computation is required for potential unit mismatches\n\t\tinitialInUnit = ( jQuery.cssNumber[ prop ] || unit !== "px" && +initial ) &&\n\t\t\trcssNum.exec( jQuery.css( elem, prop ) );\n\n\tif ( initialInUnit && initialInUnit[ 3 ] !== unit ) {\n\n\t\t// Support: Firefox <=54\n\t\t// Halve the iteration target value to prevent interference from CSS upper bounds (gh-2144)\n\t\tinitial = initial / 2;\n\n\t\t// Trust units reported by jQuery.css\n\t\tunit = unit || initialInUnit[ 3 ];\n\n\t\t// Iteratively approximate from a nonzero starting point\n\t\tinitialInUnit = +initial || 1;\n\n\t\twhile ( maxIterations-- ) {\n\n\t\t\t// Evaluate and update our best guess (doubling guesses that zero out).\n\t\t\t// Finish if the scale equals or crosses 1 (making the old*new product non-positive).\n\t\t\tjQuery.style( elem, prop, initialInUnit + unit );\n\t\t\tif ( ( 1 - scale ) * ( 1 - ( scale = currentValue() / initial || 0.5 ) ) <= 0 ) {\n\t\t\t\tmaxIterations = 0;\n\t\t\t}\n\t\t\tinitialInUnit = initialInUnit / scale;\n\n\t\t}\n\n\t\tinitialInUnit = initialInUnit * 2;\n\t\tjQuery.style( elem, prop, initialInUnit + unit );\n\n\t\t// Make sure we update the tween properties later on\n\t\tvalueParts = valueParts || [];\n\t}\n\n\tif ( valueParts ) {\n\t\tinitialInUnit = +initialInUnit || +initial || 0;\n\n\t\t// Apply relative offset (+=/-=) if specified\n\t\tadjusted = valueParts[ 1 ] ?\n\t\t\tinitialInUnit + ( valueParts[ 1 ] + 1 ) * valueParts[ 2 ] :\n\t\t\t+valueParts[ 2 ];\n\t\tif ( tween ) {\n\t\t\ttween.unit = unit;\n\t\t\ttween.start = initialInUnit;\n\t\t\ttween.end = adjusted;\n\t\t}\n\t}\n\treturn adjusted;\n}\n\n\nvar defaultDisplayMap = {};\n\nfunction getDefaultDisplay( elem ) {\n\tvar temp,\n\t\tdoc = elem.ownerDocument,\n\t\tnodeName = elem.nodeName,\n\t\tdisplay = defaultDisplayMap[ nodeName ];\n\n\tif ( display ) {\n\t\treturn display;\n\t}\n\n\ttemp = doc.body.appendChild( doc.createElement( nodeName ) );\n\tdisplay = jQuery.css( temp, "display" );\n\n\ttemp.parentNode.removeChild( temp );\n\n\tif ( display === "none" ) {\n\t\tdisplay = "block";\n\t}\n\tdefaultDisplayMap[ nodeName ] = display;\n\n\treturn display;\n}\n\nfunction showHide( elements, show ) {\n\tvar display, elem,\n\t\tvalues = [],\n\t\tindex = 0,\n\t\tlength = elements.length;\n\n\t// Determine new display value for elements that need to change\n\tfor ( ; index < length; index++ ) {\n\t\telem = elements[ index ];\n\t\tif ( !elem.style ) {\n\t\t\tcontinue;\n\t\t}\n\n\t\tdisplay = elem.style.display;\n\t\tif ( show ) {\n\n\t\t\t// Since we force visibility upon cascade-hidden elements, an immediate (and slow)\n\t\t\t// check is required in this first loop unless we have a nonempty display value (either\n\t\t\t// inline or about-to-be-restored)\n\t\t\tif ( display === "none" ) {\n\t\t\t\tvalues[ index ] = dataPriv.get( elem, "display" ) || null;\n\t\t\t\tif ( !values[ index ] ) {\n\t\t\t\t\telem.style.display = "";\n\t\t\t\t}\n\t\t\t}\n\t\t\tif ( elem.style.display === "" && isHiddenWithinTree( elem ) ) {\n\t\t\t\tvalues[ index ] = getDefaultDisplay( elem );\n\t\t\t}\n\t\t} else {\n\t\t\tif ( display !== "none" ) {\n\t\t\t\tvalues[ index ] = "none";\n\n\t\t\t\t// Remember what we\'re overwriting\n\t\t\t\tdataPriv.set( elem, "display", display );\n\t\t\t}\n\t\t}\n\t}\n\n\t// Set the display of the elements in a second loop to avoid constant reflow\n\tfor ( index = 0; index < length; index++ ) {\n\t\tif ( values[ index ] != null ) {\n\t\t\telements[ index ].style.display = values[ index ];\n\t\t}\n\t}\n\n\treturn elements;\n}\n\njQuery.fn.extend( {\n\tshow: function() {\n\t\treturn showHide( this, true );\n\t},\n\thide: function() {\n\t\treturn showHide( this );\n\t},\n\ttoggle: function( state ) {\n\t\tif ( typeof state === "boolean" ) {\n\t\t\treturn state ? this.show() : this.hide();\n\t\t}\n\n\t\treturn this.each( function() {\n\t\t\tif ( isHiddenWithinTree( this ) ) {\n\t\t\t\tjQuery( this ).show();\n\t\t\t} else {\n\t\t\t\tjQuery( this ).hide();\n\t\t\t}\n\t\t} );\n\t}\n} );\nvar rcheckableType = ( /^(?:checkbox|radio)$/i );\n\nvar rtagName = ( /<([a-z][^\\/\\0>\\x20\\t\\r\\n\\f]+)/i );\n\nvar rscriptType = ( /^$|^module$|\\/(?:java|ecma)script/i );\n\n\n\n// We have to close these tags to support XHTML (#13200)\nvar wrapMap = {\n\n\t// Support: IE <=9 only\n\toption: [ 1, "<select multiple=\'multiple\'>", "</select>" ],\n\n\t// XHTML parsers do not magically insert elements in the\n\t// same way that tag soup parsers do. So we cannot shorten\n\t// this by omitting <tbody> or other required elements.\n\tthead: [ 1, "<table>", "</table>" ],\n\tcol: [ 2, "<table><colgroup>", "</colgroup></table>" ],\n\ttr: [ 2, "<table><tbody>", "</tbody></table>" ],\n\ttd: [ 3, "<table><tbody><tr>", "</tr></tbody></table>" ],\n\n\t_default: [ 0, "", "" ]\n};\n\n// Support: IE <=9 only\nwrapMap.optgroup = wrapMap.option;\n\nwrapMap.tbody = wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;\nwrapMap.th = wrapMap.td;\n\n\nfunction getAll( context, tag ) {\n\n\t// Support: IE <=9 - 11 only\n\t// Use typeof to avoid zero-argument method invocation on host objects (#15151)\n\tvar ret;\n\n\tif ( typeof context.getElementsByTagName !== "undefined" ) {\n\t\tret = context.getElementsByTagName( tag || "*" );\n\n\t} else if ( typeof context.querySelectorAll !== "undefined" ) {\n\t\tret = context.querySelectorAll( tag || "*" );\n\n\t} else {\n\t\tret = [];\n\t}\n\n\tif ( tag === undefined || tag && nodeName( context, tag ) ) {\n\t\treturn jQuery.merge( [ context ], ret );\n\t}\n\n\treturn ret;\n}\n\n\n// Mark scripts as having already been evaluated\nfunction setGlobalEval( elems, refElements ) {\n\tvar i = 0,\n\t\tl = elems.length;\n\n\tfor ( ; i < l; i++ ) {\n\t\tdataPriv.set(\n\t\t\telems[ i ],\n\t\t\t"globalEval",\n\t\t\t!refElements || dataPriv.get( refElements[ i ], "globalEval" )\n\t\t);\n\t}\n}\n\n\nvar rhtml = /<|&#?\\w+;/;\n\nfunction buildFragment( elems, context, scripts, selection, ignored ) {\n\tvar elem, tmp, tag, wrap, contains, j,\n\t\tfragment = context.createDocumentFragment(),\n\t\tnodes = [],\n\t\ti = 0,\n\t\tl = elems.length;\n\n\tfor ( ; i < l; i++ ) {\n\t\telem = elems[ i ];\n\n\t\tif ( elem || elem === 0 ) {\n\n\t\t\t// Add nodes directly\n\t\t\tif ( toType( elem ) === "object" ) {\n\n\t\t\t\t// Support: Android <=4.0 only, PhantomJS 1 only\n\t\t\t\t// push.apply(_, arraylike) throws on ancient WebKit\n\t\t\t\tjQuery.merge( nodes, elem.nodeType ? [ elem ] : elem );\n\n\t\t\t// Convert non-html into a text node\n\t\t\t} else if ( !rhtml.test( elem ) ) {\n\t\t\t\tnodes.push( context.createTextNode( elem ) );\n\n\t\t\t// Convert html into DOM nodes\n\t\t\t} else {\n\t\t\t\ttmp = tmp || fragment.appendChild( context.createElement( "div" ) );\n\n\t\t\t\t// Deserialize a standard representation\n\t\t\t\ttag = ( rtagName.exec( elem ) || [ "", "" ] )[ 1 ].toLowerCase();\n\t\t\t\twrap = wrapMap[ tag ] || wrapMap._default;\n\t\t\t\ttmp.innerHTML = wrap[ 1 ] + jQuery.htmlPrefilter( elem ) + wrap[ 2 ];\n\n\t\t\t\t// Descend through wrappers to the right content\n\t\t\t\tj = wrap[ 0 ];\n\t\t\t\twhile ( j-- ) {\n\t\t\t\t\ttmp = tmp.lastChild;\n\t\t\t\t}\n\n\t\t\t\t// Support: Android <=4.0 only, PhantomJS 1 only\n\t\t\t\t// push.apply(_, arraylike) throws on ancient WebKit\n\t\t\t\tjQuery.merge( nodes, tmp.childNodes );\n\n\t\t\t\t// Remember the top-level container\n\t\t\t\ttmp = fragment.firstChild;\n\n\t\t\t\t// Ensure the created nodes are orphaned (#12392)\n\t\t\t\ttmp.textContent = "";\n\t\t\t}\n\t\t}\n\t}\n\n\t// Remove wrapper from fragment\n\tfragment.textContent = "";\n\n\ti = 0;\n\twhile ( ( elem = nodes[ i++ ] ) ) {\n\n\t\t// Skip elements already in the context collection (trac-4087)\n\t\tif ( selection && jQuery.inArray( elem, selection ) > -1 ) {\n\t\t\tif ( ignored ) {\n\t\t\t\tignored.push( elem );\n\t\t\t}\n\t\t\tcontinue;\n\t\t}\n\n\t\tcontains = jQuery.contains( elem.ownerDocument, elem );\n\n\t\t// Append to fragment\n\t\ttmp = getAll( fragment.appendChild( elem ), "script" );\n\n\t\t// Preserve script evaluation history\n\t\tif ( contains ) {\n\t\t\tsetGlobalEval( tmp );\n\t\t}\n\n\t\t// Capture executables\n\t\tif ( scripts ) {\n\t\t\tj = 0;\n\t\t\twhile ( ( elem = tmp[ j++ ] ) ) {\n\t\t\t\tif ( rscriptType.test( elem.type || "" ) ) {\n\t\t\t\t\tscripts.push( elem );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\treturn fragment;\n}\n\n\n( function() {\n\tvar fragment = document.createDocumentFragment(),\n\t\tdiv = fragment.appendChild( document.createElement( "div" ) ),\n\t\tinput = document.createElement( "input" );\n\n\t// Support: Android 4.0 - 4.3 only\n\t// Check state lost if the name is set (#11217)\n\t// Support: Windows Web Apps (WWA)\n\t// `name` and `type` must use .setAttribute for WWA (#14901)\n\tinput.setAttribute( "type", "radio" );\n\tinput.setAttribute( "checked", "checked" );\n\tinput.setAttribute( "name", "t" );\n\n\tdiv.appendChild( input );\n\n\t// Support: Android <=4.1 only\n\t// Older WebKit doesn\'t clone checked state correctly in fragments\n\tsupport.checkClone = div.cloneNode( true ).cloneNode( true ).lastChild.checked;\n\n\t// Support: IE <=11 only\n\t// Make sure textarea (and checkbox) defaultValue is properly cloned\n\tdiv.innerHTML = "<textarea>x</textarea>";\n\tsupport.noCloneChecked = !!div.cloneNode( true ).lastChild.defaultValue;\n} )();\nvar documentElement = document.documentElement;\n\n\n\nvar\n\trkeyEvent = /^key/,\n\trmouseEvent = /^(?:mouse|pointer|contextmenu|drag|drop)|click/,\n\trtypenamespace = /^([^.]*)(?:\\.(.+)|)/;\n\nfunction returnTrue() {\n\treturn true;\n}\n\nfunction returnFalse() {\n\treturn false;\n}\n\n// Support: IE <=9 only\n// See #13393 for more info\nfunction safeActiveElement() {\n\ttry {\n\t\treturn document.activeElement;\n\t} catch ( err ) { }\n}\n\nfunction on( elem, types, selector, data, fn, one ) {\n\tvar origFn, type;\n\n\t// Types can be a map of types/handlers\n\tif ( typeof types === "object" ) {\n\n\t\t// ( types-Object, selector, data )\n\t\tif ( typeof selector !== "string" ) {\n\n\t\t\t// ( types-Object, data )\n\t\t\tdata = data || selector;\n\t\t\tselector = undefined;\n\t\t}\n\t\tfor ( type in types ) {\n\t\t\ton( elem, type, selector, data, types[ type ], one );\n\t\t}\n\t\treturn elem;\n\t}\n\n\tif ( data == null && fn == null ) {\n\n\t\t// ( types, fn )\n\t\tfn = selector;\n\t\tdata = selector = undefined;\n\t} else if ( fn == null ) {\n\t\tif ( typeof selector === "string" ) {\n\n\t\t\t// ( types, selector, fn )\n\t\t\tfn = data;\n\t\t\tdata = undefined;\n\t\t} else {\n\n\t\t\t// ( types, data, fn )\n\t\t\tfn = data;\n\t\t\tdata = selector;\n\t\t\tselector = undefined;\n\t\t}\n\t}\n\tif ( fn === false ) {\n\t\tfn = returnFalse;\n\t} else if ( !fn ) {\n\t\treturn elem;\n\t}\n\n\tif ( one === 1 ) {\n\t\torigFn = fn;\n\t\tfn = function( event ) {\n\n\t\t\t// Can use an empty set, since event contains the info\n\t\t\tjQuery().off( event );\n\t\t\treturn origFn.apply( this, arguments );\n\t\t};\n\n\t\t// Use same guid so caller can remove using origFn\n\t\tfn.guid = origFn.guid || ( origFn.guid = jQuery.guid++ );\n\t}\n\treturn elem.each( function() {\n\t\tjQuery.event.add( this, types, fn, data, selector );\n\t} );\n}\n\n/*\n * Helper functions for managing events -- not part of the public interface.\n * Props to Dean Edwards\' addEvent library for many of the ideas.\n */\njQuery.event = {\n\n\tglobal: {},\n\n\tadd: function( elem, types, handler, data, selector ) {\n\n\t\tvar handleObjIn, eventHandle, tmp,\n\t\t\tevents, t, handleObj,\n\t\t\tspecial, handlers, type, namespaces, origType,\n\t\t\telemData = dataPriv.get( elem );\n\n\t\t// Don\'t attach events to noData or text/comment nodes (but allow plain objects)\n\t\tif ( !elemData ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Caller can pass in an object of custom data in lieu of the handler\n\t\tif ( handler.handler ) {\n\t\t\thandleObjIn = handler;\n\t\t\thandler = handleObjIn.handler;\n\t\t\tselector = handleObjIn.selector;\n\t\t}\n\n\t\t// Ensure that invalid selectors throw exceptions at attach time\n\t\t// Evaluate against documentElement in case elem is a non-element node (e.g., document)\n\t\tif ( selector ) {\n\t\t\tjQuery.find.matchesSelector( documentElement, selector );\n\t\t}\n\n\t\t// Make sure that the handler has a unique ID, used to find/remove it later\n\t\tif ( !handler.guid ) {\n\t\t\thandler.guid = jQuery.guid++;\n\t\t}\n\n\t\t// Init the element\'s event structure and main handler, if this is the first\n\t\tif ( !( events = elemData.events ) ) {\n\t\t\tevents = elemData.events = {};\n\t\t}\n\t\tif ( !( eventHandle = elemData.handle ) ) {\n\t\t\teventHandle = elemData.handle = function( e ) {\n\n\t\t\t\t// Discard the second event of a jQuery.event.trigger() and\n\t\t\t\t// when an event is called after a page has unloaded\n\t\t\t\treturn typeof jQuery !== "undefined" && jQuery.event.triggered !== e.type ?\n\t\t\t\t\tjQuery.event.dispatch.apply( elem, arguments ) : undefined;\n\t\t\t};\n\t\t}\n\n\t\t// Handle multiple events separated by a space\n\t\ttypes = ( types || "" ).match( rnothtmlwhite ) || [ "" ];\n\t\tt = types.length;\n\t\twhile ( t-- ) {\n\t\t\ttmp = rtypenamespace.exec( types[ t ] ) || [];\n\t\t\ttype = origType = tmp[ 1 ];\n\t\t\tnamespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();\n\n\t\t\t// There *must* be a type, no attaching namespace-only handlers\n\t\t\tif ( !type ) {\n\t\t\t\tcontinue;\n\t\t\t}\n\n\t\t\t// If event changes its type, use the special event handlers for the changed type\n\t\t\tspecial = jQuery.event.special[ type ] || {};\n\n\t\t\t// If selector defined, determine special event api type, otherwise given type\n\t\t\ttype = ( selector ? special.delegateType : special.bindType ) || type;\n\n\t\t\t// Update special based on newly reset type\n\t\t\tspecial = jQuery.event.special[ type ] || {};\n\n\t\t\t// handleObj is passed to all event handlers\n\t\t\thandleObj = jQuery.extend( {\n\t\t\t\ttype: type,\n\t\t\t\torigType: origType,\n\t\t\t\tdata: data,\n\t\t\t\thandler: handler,\n\t\t\t\tguid: handler.guid,\n\t\t\t\tselector: selector,\n\t\t\t\tneedsContext: selector && jQuery.expr.match.needsContext.test( selector ),\n\t\t\t\tnamespace: namespaces.join( "." )\n\t\t\t}, handleObjIn );\n\n\t\t\t// Init the event handler queue if we\'re the first\n\t\t\tif ( !( handlers = events[ type ] ) ) {\n\t\t\t\thandlers = events[ type ] = [];\n\t\t\t\thandlers.delegateCount = 0;\n\n\t\t\t\t// Only use addEventListener if the special events handler returns false\n\t\t\t\tif ( !special.setup ||\n\t\t\t\t\tspecial.setup.call( elem, data, namespaces, eventHandle ) === false ) {\n\n\t\t\t\t\tif ( elem.addEventListener ) {\n\t\t\t\t\t\telem.addEventListener( type, eventHandle );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\tif ( special.add ) {\n\t\t\t\tspecial.add.call( elem, handleObj );\n\n\t\t\t\tif ( !handleObj.handler.guid ) {\n\t\t\t\t\thandleObj.handler.guid = handler.guid;\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Add to the element\'s handler list, delegates in front\n\t\t\tif ( selector ) {\n\t\t\t\thandlers.splice( handlers.delegateCount++, 0, handleObj );\n\t\t\t} else {\n\t\t\t\thandlers.push( handleObj );\n\t\t\t}\n\n\t\t\t// Keep track of which events have ever been used, for event optimization\n\t\t\tjQuery.event.global[ type ] = true;\n\t\t}\n\n\t},\n\n\t// Detach an event or set of events from an element\n\tremove: function( elem, types, handler, selector, mappedTypes ) {\n\n\t\tvar j, origCount, tmp,\n\t\t\tevents, t, handleObj,\n\t\t\tspecial, handlers, type, namespaces, origType,\n\t\t\telemData = dataPriv.hasData( elem ) && dataPriv.get( elem );\n\n\t\tif ( !elemData || !( events = elemData.events ) ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Once for each type.namespace in types; type may be omitted\n\t\ttypes = ( types || "" ).match( rnothtmlwhite ) || [ "" ];\n\t\tt = types.length;\n\t\twhile ( t-- ) {\n\t\t\ttmp = rtypenamespace.exec( types[ t ] ) || [];\n\t\t\ttype = origType = tmp[ 1 ];\n\t\t\tnamespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();\n\n\t\t\t// Unbind all events (on this namespace, if provided) for the element\n\t\t\tif ( !type ) {\n\t\t\t\tfor ( type in events ) {\n\t\t\t\t\tjQuery.event.remove( elem, type + types[ t ], handler, selector, true );\n\t\t\t\t}\n\t\t\t\tcontinue;\n\t\t\t}\n\n\t\t\tspecial = jQuery.event.special[ type ] || {};\n\t\t\ttype = ( selector ? special.delegateType : special.bindType ) || type;\n\t\t\thandlers = events[ type ] || [];\n\t\t\ttmp = tmp[ 2 ] &&\n\t\t\t\tnew RegExp( "(^|\\\\.)" + namespaces.join( "\\\\.(?:.*\\\\.|)" ) + "(\\\\.|$)" );\n\n\t\t\t// Remove matching events\n\t\t\torigCount = j = handlers.length;\n\t\t\twhile ( j-- ) {\n\t\t\t\thandleObj = handlers[ j ];\n\n\t\t\t\tif ( ( mappedTypes || origType === handleObj.origType ) &&\n\t\t\t\t\t( !handler || handler.guid === handleObj.guid ) &&\n\t\t\t\t\t( !tmp || tmp.test( handleObj.namespace ) ) &&\n\t\t\t\t\t( !selector || selector === handleObj.selector ||\n\t\t\t\t\t\tselector === "**" && handleObj.selector ) ) {\n\t\t\t\t\thandlers.splice( j, 1 );\n\n\t\t\t\t\tif ( handleObj.selector ) {\n\t\t\t\t\t\thandlers.delegateCount--;\n\t\t\t\t\t}\n\t\t\t\t\tif ( special.remove ) {\n\t\t\t\t\t\tspecial.remove.call( elem, handleObj );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Remove generic event handler if we removed something and no more handlers exist\n\t\t\t// (avoids potential for endless recursion during removal of special event handlers)\n\t\t\tif ( origCount && !handlers.length ) {\n\t\t\t\tif ( !special.teardown ||\n\t\t\t\t\tspecial.teardown.call( elem, namespaces, elemData.handle ) === false ) {\n\n\t\t\t\t\tjQuery.removeEvent( elem, type, elemData.handle );\n\t\t\t\t}\n\n\t\t\t\tdelete events[ type ];\n\t\t\t}\n\t\t}\n\n\t\t// Remove data and the expando if it\'s no longer used\n\t\tif ( jQuery.isEmptyObject( events ) ) {\n\t\t\tdataPriv.remove( elem, "handle events" );\n\t\t}\n\t},\n\n\tdispatch: function( nativeEvent ) {\n\n\t\t// Make a writable jQuery.Event from the native event object\n\t\tvar event = jQuery.event.fix( nativeEvent );\n\n\t\tvar i, j, ret, matched, handleObj, handlerQueue,\n\t\t\targs = new Array( arguments.length ),\n\t\t\thandlers = ( dataPriv.get( this, "events" ) || {} )[ event.type ] || [],\n\t\t\tspecial = jQuery.event.special[ event.type ] || {};\n\n\t\t// Use the fix-ed jQuery.Event rather than the (read-only) native event\n\t\targs[ 0 ] = event;\n\n\t\tfor ( i = 1; i < arguments.length; i++ ) {\n\t\t\targs[ i ] = arguments[ i ];\n\t\t}\n\n\t\tevent.delegateTarget = this;\n\n\t\t// Call the preDispatch hook for the mapped type, and let it bail if desired\n\t\tif ( special.preDispatch && special.preDispatch.call( this, event ) === false ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Determine handlers\n\t\thandlerQueue = jQuery.event.handlers.call( this, event, handlers );\n\n\t\t// Run delegates first; they may want to stop propagation beneath us\n\t\ti = 0;\n\t\twhile ( ( matched = handlerQueue[ i++ ] ) && !event.isPropagationStopped() ) {\n\t\t\tevent.currentTarget = matched.elem;\n\n\t\t\tj = 0;\n\t\t\twhile ( ( handleObj = matched.handlers[ j++ ] ) &&\n\t\t\t\t!event.isImmediatePropagationStopped() ) {\n\n\t\t\t\t// Triggered event must either 1) have no namespace, or 2) have namespace(s)\n\t\t\t\t// a subset or equal to those in the bound event (both can have no namespace).\n\t\t\t\tif ( !event.rnamespace || event.rnamespace.test( handleObj.namespace ) ) {\n\n\t\t\t\t\tevent.handleObj = handleObj;\n\t\t\t\t\tevent.data = handleObj.data;\n\n\t\t\t\t\tret = ( ( jQuery.event.special[ handleObj.origType ] || {} ).handle ||\n\t\t\t\t\t\thandleObj.handler ).apply( matched.elem, args );\n\n\t\t\t\t\tif ( ret !== undefined ) {\n\t\t\t\t\t\tif ( ( event.result = ret ) === false ) {\n\t\t\t\t\t\t\tevent.preventDefault();\n\t\t\t\t\t\t\tevent.stopPropagation();\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\t// Call the postDispatch hook for the mapped type\n\t\tif ( special.postDispatch ) {\n\t\t\tspecial.postDispatch.call( this, event );\n\t\t}\n\n\t\treturn event.result;\n\t},\n\n\thandlers: function( event, handlers ) {\n\t\tvar i, handleObj, sel, matchedHandlers, matchedSelectors,\n\t\t\thandlerQueue = [],\n\t\t\tdelegateCount = handlers.delegateCount,\n\t\t\tcur = event.target;\n\n\t\t// Find delegate handlers\n\t\tif ( delegateCount &&\n\n\t\t\t// Support: IE <=9\n\t\t\t// Black-hole SVG <use> instance trees (trac-13180)\n\t\t\tcur.nodeType &&\n\n\t\t\t// Support: Firefox <=42\n\t\t\t// Suppress spec-violating clicks indicating a non-primary pointer button (trac-3861)\n\t\t\t// https://www.w3.org/TR/DOM-Level-3-Events/#event-type-click\n\t\t\t// Support: IE 11 only\n\t\t\t// ...but not arrow key "clicks" of radio inputs, which can have `button` -1 (gh-2343)\n\t\t\t!( event.type === "click" && event.button >= 1 ) ) {\n\n\t\t\tfor ( ; cur !== this; cur = cur.parentNode || this ) {\n\n\t\t\t\t// Don\'t check non-elements (#13208)\n\t\t\t\t// Don\'t process clicks on disabled elements (#6911, #8165, #11382, #11764)\n\t\t\t\tif ( cur.nodeType === 1 && !( event.type === "click" && cur.disabled === true ) ) {\n\t\t\t\t\tmatchedHandlers = [];\n\t\t\t\t\tmatchedSelectors = {};\n\t\t\t\t\tfor ( i = 0; i < delegateCount; i++ ) {\n\t\t\t\t\t\thandleObj = handlers[ i ];\n\n\t\t\t\t\t\t// Don\'t conflict with Object.prototype properties (#13203)\n\t\t\t\t\t\tsel = handleObj.selector + " ";\n\n\t\t\t\t\t\tif ( matchedSelectors[ sel ] === undefined ) {\n\t\t\t\t\t\t\tmatchedSelectors[ sel ] = handleObj.needsContext ?\n\t\t\t\t\t\t\t\tjQuery( sel, this ).index( cur ) > -1 :\n\t\t\t\t\t\t\t\tjQuery.find( sel, this, null, [ cur ] ).length;\n\t\t\t\t\t\t}\n\t\t\t\t\t\tif ( matchedSelectors[ sel ] ) {\n\t\t\t\t\t\t\tmatchedHandlers.push( handleObj );\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t\tif ( matchedHandlers.length ) {\n\t\t\t\t\t\thandlerQueue.push( { elem: cur, handlers: matchedHandlers } );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\t// Add the remaining (directly-bound) handlers\n\t\tcur = this;\n\t\tif ( delegateCount < handlers.length ) {\n\t\t\thandlerQueue.push( { elem: cur, handlers: handlers.slice( delegateCount ) } );\n\t\t}\n\n\t\treturn handlerQueue;\n\t},\n\n\taddProp: function( name, hook ) {\n\t\tObject.defineProperty( jQuery.Event.prototype, name, {\n\t\t\tenumerable: true,\n\t\t\tconfigurable: true,\n\n\t\t\tget: isFunction( hook ) ?\n\t\t\t\tfunction() {\n\t\t\t\t\tif ( this.originalEvent ) {\n\t\t\t\t\t\t\treturn hook( this.originalEvent );\n\t\t\t\t\t}\n\t\t\t\t} :\n\t\t\t\tfunction() {\n\t\t\t\t\tif ( this.originalEvent ) {\n\t\t\t\t\t\t\treturn this.originalEvent[ name ];\n\t\t\t\t\t}\n\t\t\t\t},\n\n\t\t\tset: function( value ) {\n\t\t\t\tObject.defineProperty( this, name, {\n\t\t\t\t\tenumerable: true,\n\t\t\t\t\tconfigurable: true,\n\t\t\t\t\twritable: true,\n\t\t\t\t\tvalue: value\n\t\t\t\t} );\n\t\t\t}\n\t\t} );\n\t},\n\n\tfix: function( originalEvent ) {\n\t\treturn originalEvent[ jQuery.expando ] ?\n\t\t\toriginalEvent :\n\t\t\tnew jQuery.Event( originalEvent );\n\t},\n\n\tspecial: {\n\t\tload: {\n\n\t\t\t// Prevent triggered image.load events from bubbling to window.load\n\t\t\tnoBubble: true\n\t\t},\n\t\tfocus: {\n\n\t\t\t// Fire native event if possible so blur/focus sequence is correct\n\t\t\ttrigger: function() {\n\t\t\t\tif ( this !== safeActiveElement() && this.focus ) {\n\t\t\t\t\tthis.focus();\n\t\t\t\t\treturn false;\n\t\t\t\t}\n\t\t\t},\n\t\t\tdelegateType: "focusin"\n\t\t},\n\t\tblur: {\n\t\t\ttrigger: function() {\n\t\t\t\tif ( this === safeActiveElement() && this.blur ) {\n\t\t\t\t\tthis.blur();\n\t\t\t\t\treturn false;\n\t\t\t\t}\n\t\t\t},\n\t\t\tdelegateType: "focusout"\n\t\t},\n\t\tclick: {\n\n\t\t\t// For checkbox, fire native event so checked state will be right\n\t\t\ttrigger: function() {\n\t\t\t\tif ( this.type === "checkbox" && this.click && nodeName( this, "input" ) ) {\n\t\t\t\t\tthis.click();\n\t\t\t\t\treturn false;\n\t\t\t\t}\n\t\t\t},\n\n\t\t\t// For cross-browser consistency, don\'t fire native .click() on links\n\t\t\t_default: function( event ) {\n\t\t\t\treturn nodeName( event.target, "a" );\n\t\t\t}\n\t\t},\n\n\t\tbeforeunload: {\n\t\t\tpostDispatch: function( event ) {\n\n\t\t\t\t// Support: Firefox 20+\n\t\t\t\t// Firefox doesn\'t alert if the returnValue field is not set.\n\t\t\t\tif ( event.result !== undefined && event.originalEvent ) {\n\t\t\t\t\tevent.originalEvent.returnValue = event.result;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n};\n\njQuery.removeEvent = function( elem, type, handle ) {\n\n\t// This "if" is needed for plain objects\n\tif ( elem.removeEventListener ) {\n\t\telem.removeEventListener( type, handle );\n\t}\n};\n\njQuery.Event = function( src, props ) {\n\n\t// Allow instantiation without the \'new\' keyword\n\tif ( !( this instanceof jQuery.Event ) ) {\n\t\treturn new jQuery.Event( src, props );\n\t}\n\n\t// Event object\n\tif ( src && src.type ) {\n\t\tthis.originalEvent = src;\n\t\tthis.type = src.type;\n\n\t\t// Events bubbling up the document may have been marked as prevented\n\t\t// by a handler lower down the tree; reflect the correct value.\n\t\tthis.isDefaultPrevented = src.defaultPrevented ||\n\t\t\t\tsrc.defaultPrevented === undefined &&\n\n\t\t\t\t// Support: Android <=2.3 only\n\t\t\t\tsrc.returnValue === false ?\n\t\t\treturnTrue :\n\t\t\treturnFalse;\n\n\t\t// Create target properties\n\t\t// Support: Safari <=6 - 7 only\n\t\t// Target should not be a text node (#504, #13143)\n\t\tthis.target = ( src.target && src.target.nodeType === 3 ) ?\n\t\t\tsrc.target.parentNode :\n\t\t\tsrc.target;\n\n\t\tthis.currentTarget = src.currentTarget;\n\t\tthis.relatedTarget = src.relatedTarget;\n\n\t// Event type\n\t} else {\n\t\tthis.type = src;\n\t}\n\n\t// Put explicitly provided properties onto the event object\n\tif ( props ) {\n\t\tjQuery.extend( this, props );\n\t}\n\n\t// Create a timestamp if incoming event doesn\'t have one\n\tthis.timeStamp = src && src.timeStamp || Date.now();\n\n\t// Mark it as fixed\n\tthis[ jQuery.expando ] = true;\n};\n\n// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding\n// https://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html\njQuery.Event.prototype = {\n\tconstructor: jQuery.Event,\n\tisDefaultPrevented: returnFalse,\n\tisPropagationStopped: returnFalse,\n\tisImmediatePropagationStopped: returnFalse,\n\tisSimulated: false,\n\n\tpreventDefault: function() {\n\t\tvar e = this.originalEvent;\n\n\t\tthis.isDefaultPrevented = returnTrue;\n\n\t\tif ( e && !this.isSimulated ) {\n\t\t\te.preventDefault();\n\t\t}\n\t},\n\tstopPropagation: function() {\n\t\tvar e = this.originalEvent;\n\n\t\tthis.isPropagationStopped = returnTrue;\n\n\t\tif ( e && !this.isSimulated ) {\n\t\t\te.stopPropagation();\n\t\t}\n\t},\n\tstopImmediatePropagation: function() {\n\t\tvar e = this.originalEvent;\n\n\t\tthis.isImmediatePropagationStopped = returnTrue;\n\n\t\tif ( e && !this.isSimulated ) {\n\t\t\te.stopImmediatePropagation();\n\t\t}\n\n\t\tthis.stopPropagation();\n\t}\n};\n\n// Includes all common event props including KeyEvent and MouseEvent specific props\njQuery.each( {\n\taltKey: true,\n\tbubbles: true,\n\tcancelable: true,\n\tchangedTouches: true,\n\tctrlKey: true,\n\tdetail: true,\n\teventPhase: true,\n\tmetaKey: true,\n\tpageX: true,\n\tpageY: true,\n\tshiftKey: true,\n\tview: true,\n\t"char": true,\n\tcharCode: true,\n\tkey: true,\n\tkeyCode: true,\n\tbutton: true,\n\tbuttons: true,\n\tclientX: true,\n\tclientY: true,\n\toffsetX: true,\n\toffsetY: true,\n\tpointerId: true,\n\tpointerType: true,\n\tscreenX: true,\n\tscreenY: true,\n\ttargetTouches: true,\n\ttoElement: true,\n\ttouches: true,\n\n\twhich: function( event ) {\n\t\tvar button = event.button;\n\n\t\t// Add which for key events\n\t\tif ( event.which == null && rkeyEvent.test( event.type ) ) {\n\t\t\treturn event.charCode != null ? event.charCode : event.keyCode;\n\t\t}\n\n\t\t// Add which for click: 1 === left; 2 === middle; 3 === right\n\t\tif ( !event.which && button !== undefined && rmouseEvent.test( event.type ) ) {\n\t\t\tif ( button & 1 ) {\n\t\t\t\treturn 1;\n\t\t\t}\n\n\t\t\tif ( button & 2 ) {\n\t\t\t\treturn 3;\n\t\t\t}\n\n\t\t\tif ( button & 4 ) {\n\t\t\t\treturn 2;\n\t\t\t}\n\n\t\t\treturn 0;\n\t\t}\n\n\t\treturn event.which;\n\t}\n}, jQuery.event.addProp );\n\n// Create mouseenter/leave events using mouseover/out and event-time checks\n// so that event delegation works in jQuery.\n// Do the same for pointerenter/pointerleave and pointerover/pointerout\n//\n// Support: Safari 7 only\n// Safari sends mouseenter too often; see:\n// https://bugs.chromium.org/p/chromium/issues/detail?id=470258\n// for the description of the bug (it existed in older Chrome versions as well).\njQuery.each( {\n\tmouseenter: "mouseover",\n\tmouseleave: "mouseout",\n\tpointerenter: "pointerover",\n\tpointerleave: "pointerout"\n}, function( orig, fix ) {\n\tjQuery.event.special[ orig ] = {\n\t\tdelegateType: fix,\n\t\tbindType: fix,\n\n\t\thandle: function( event ) {\n\t\t\tvar ret,\n\t\t\t\ttarget = this,\n\t\t\t\trelated = event.relatedTarget,\n\t\t\t\thandleObj = event.handleObj;\n\n\t\t\t// For mouseenter/leave call the handler if related is outside the target.\n\t\t\t// NB: No relatedTarget if the mouse left/entered the browser window\n\t\t\tif ( !related || ( related !== target && !jQuery.contains( target, related ) ) ) {\n\t\t\t\tevent.type = handleObj.origType;\n\t\t\t\tret = handleObj.handler.apply( this, arguments );\n\t\t\t\tevent.type = fix;\n\t\t\t}\n\t\t\treturn ret;\n\t\t}\n\t};\n} );\n\njQuery.fn.extend( {\n\n\ton: function( types, selector, data, fn ) {\n\t\treturn on( this, types, selector, data, fn );\n\t},\n\tone: function( types, selector, data, fn ) {\n\t\treturn on( this, types, selector, data, fn, 1 );\n\t},\n\toff: function( types, selector, fn ) {\n\t\tvar handleObj, type;\n\t\tif ( types && types.preventDefault && types.handleObj ) {\n\n\t\t\t// ( event )  dispatched jQuery.Event\n\t\t\thandleObj = types.handleObj;\n\t\t\tjQuery( types.delegateTarget ).off(\n\t\t\t\thandleObj.namespace ?\n\t\t\t\t\thandleObj.origType + "." + handleObj.namespace :\n\t\t\t\t\thandleObj.origType,\n\t\t\t\thandleObj.selector,\n\t\t\t\thandleObj.handler\n\t\t\t);\n\t\t\treturn this;\n\t\t}\n\t\tif ( typeof types === "object" ) {\n\n\t\t\t// ( types-object [, selector] )\n\t\t\tfor ( type in types ) {\n\t\t\t\tthis.off( type, selector, types[ type ] );\n\t\t\t}\n\t\t\treturn this;\n\t\t}\n\t\tif ( selector === false || typeof selector === "function" ) {\n\n\t\t\t// ( types [, fn] )\n\t\t\tfn = selector;\n\t\t\tselector = undefined;\n\t\t}\n\t\tif ( fn === false ) {\n\t\t\tfn = returnFalse;\n\t\t}\n\t\treturn this.each( function() {\n\t\t\tjQuery.event.remove( this, types, fn, selector );\n\t\t} );\n\t}\n} );\n\n\nvar\n\n\t/* eslint-disable max-len */\n\n\t// See https://github.com/eslint/eslint/issues/3229\n\trxhtmlTag = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([a-z][^\\/\\0>\\x20\\t\\r\\n\\f]*)[^>]*)\\/>/gi,\n\n\t/* eslint-enable */\n\n\t// Support: IE <=10 - 11, Edge 12 - 13 only\n\t// In IE/Edge using regex groups here causes severe slowdowns.\n\t// See https://connect.microsoft.com/IE/feedback/details/1736512/\n\trnoInnerhtml = /<script|<style|<link/i,\n\n\t// checked="checked" or checked\n\trchecked = /checked\\s*(?:[^=]|=\\s*.checked.)/i,\n\trcleanScript = /^\\s*<!(?:\\[CDATA\\[|--)|(?:\\]\\]|--)>\\s*$/g;\n\n// Prefer a tbody over its parent table for containing new rows\nfunction manipulationTarget( elem, content ) {\n\tif ( nodeName( elem, "table" ) &&\n\t\tnodeName( content.nodeType !== 11 ? content : content.firstChild, "tr" ) ) {\n\n\t\treturn jQuery( elem ).children( "tbody" )[ 0 ] || elem;\n\t}\n\n\treturn elem;\n}\n\n// Replace/restore the type attribute of script elements for safe DOM manipulation\nfunction disableScript( elem ) {\n\telem.type = ( elem.getAttribute( "type" ) !== null ) + "/" + elem.type;\n\treturn elem;\n}\nfunction restoreScript( elem ) {\n\tif ( ( elem.type || "" ).slice( 0, 5 ) === "true/" ) {\n\t\telem.type = elem.type.slice( 5 );\n\t} else {\n\t\telem.removeAttribute( "type" );\n\t}\n\n\treturn elem;\n}\n\nfunction cloneCopyEvent( src, dest ) {\n\tvar i, l, type, pdataOld, pdataCur, udataOld, udataCur, events;\n\n\tif ( dest.nodeType !== 1 ) {\n\t\treturn;\n\t}\n\n\t// 1. Copy private data: events, handlers, etc.\n\tif ( dataPriv.hasData( src ) ) {\n\t\tpdataOld = dataPriv.access( src );\n\t\tpdataCur = dataPriv.set( dest, pdataOld );\n\t\tevents = pdataOld.events;\n\n\t\tif ( events ) {\n\t\t\tdelete pdataCur.handle;\n\t\t\tpdataCur.events = {};\n\n\t\t\tfor ( type in events ) {\n\t\t\t\tfor ( i = 0, l = events[ type ].length; i < l; i++ ) {\n\t\t\t\t\tjQuery.event.add( dest, type, events[ type ][ i ] );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\t// 2. Copy user data\n\tif ( dataUser.hasData( src ) ) {\n\t\tudataOld = dataUser.access( src );\n\t\tudataCur = jQuery.extend( {}, udataOld );\n\n\t\tdataUser.set( dest, udataCur );\n\t}\n}\n\n// Fix IE bugs, see support tests\nfunction fixInput( src, dest ) {\n\tvar nodeName = dest.nodeName.toLowerCase();\n\n\t// Fails to persist the checked state of a cloned checkbox or radio button.\n\tif ( nodeName === "input" && rcheckableType.test( src.type ) ) {\n\t\tdest.checked = src.checked;\n\n\t// Fails to return the selected option to the default selected state when cloning options\n\t} else if ( nodeName === "input" || nodeName === "textarea" ) {\n\t\tdest.defaultValue = src.defaultValue;\n\t}\n}\n\nfunction domManip( collection, args, callback, ignored ) {\n\n\t// Flatten any nested arrays\n\targs = concat.apply( [], args );\n\n\tvar fragment, first, scripts, hasScripts, node, doc,\n\t\ti = 0,\n\t\tl = collection.length,\n\t\tiNoClone = l - 1,\n\t\tvalue = args[ 0 ],\n\t\tvalueIsFunction = isFunction( value );\n\n\t// We can\'t cloneNode fragments that contain checked, in WebKit\n\tif ( valueIsFunction ||\n\t\t\t( l > 1 && typeof value === "string" &&\n\t\t\t\t!support.checkClone && rchecked.test( value ) ) ) {\n\t\treturn collection.each( function( index ) {\n\t\t\tvar self = collection.eq( index );\n\t\t\tif ( valueIsFunction ) {\n\t\t\t\targs[ 0 ] = value.call( this, index, self.html() );\n\t\t\t}\n\t\t\tdomManip( self, args, callback, ignored );\n\t\t} );\n\t}\n\n\tif ( l ) {\n\t\tfragment = buildFragment( args, collection[ 0 ].ownerDocument, false, collection, ignored );\n\t\tfirst = fragment.firstChild;\n\n\t\tif ( fragment.childNodes.length === 1 ) {\n\t\t\tfragment = first;\n\t\t}\n\n\t\t// Require either new content or an interest in ignored elements to invoke the callback\n\t\tif ( first || ignored ) {\n\t\t\tscripts = jQuery.map( getAll( fragment, "script" ), disableScript );\n\t\t\thasScripts = scripts.length;\n\n\t\t\t// Use the original fragment for the last item\n\t\t\t// instead of the first because it can end up\n\t\t\t// being emptied incorrectly in certain situations (#8070).\n\t\t\tfor ( ; i < l; i++ ) {\n\t\t\t\tnode = fragment;\n\n\t\t\t\tif ( i !== iNoClone ) {\n\t\t\t\t\tnode = jQuery.clone( node, true, true );\n\n\t\t\t\t\t// Keep references to cloned scripts for later restoration\n\t\t\t\t\tif ( hasScripts ) {\n\n\t\t\t\t\t\t// Support: Android <=4.0 only, PhantomJS 1 only\n\t\t\t\t\t\t// push.apply(_, arraylike) throws on ancient WebKit\n\t\t\t\t\t\tjQuery.merge( scripts, getAll( node, "script" ) );\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\tcallback.call( collection[ i ], node, i );\n\t\t\t}\n\n\t\t\tif ( hasScripts ) {\n\t\t\t\tdoc = scripts[ scripts.length - 1 ].ownerDocument;\n\n\t\t\t\t// Reenable scripts\n\t\t\t\tjQuery.map( scripts, restoreScript );\n\n\t\t\t\t// Evaluate executable scripts on first document insertion\n\t\t\t\tfor ( i = 0; i < hasScripts; i++ ) {\n\t\t\t\t\tnode = scripts[ i ];\n\t\t\t\t\tif ( rscriptType.test( node.type || "" ) &&\n\t\t\t\t\t\t!dataPriv.access( node, "globalEval" ) &&\n\t\t\t\t\t\tjQuery.contains( doc, node ) ) {\n\n\t\t\t\t\t\tif ( node.src && ( node.type || "" ).toLowerCase()  !== "module" ) {\n\n\t\t\t\t\t\t\t// Optional AJAX dependency, but won\'t run scripts if not present\n\t\t\t\t\t\t\tif ( jQuery._evalUrl ) {\n\t\t\t\t\t\t\t\tjQuery._evalUrl( node.src );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\tDOMEval( node.textContent.replace( rcleanScript, "" ), doc, node );\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\treturn collection;\n}\n\nfunction remove( elem, selector, keepData ) {\n\tvar node,\n\t\tnodes = selector ? jQuery.filter( selector, elem ) : elem,\n\t\ti = 0;\n\n\tfor ( ; ( node = nodes[ i ] ) != null; i++ ) {\n\t\tif ( !keepData && node.nodeType === 1 ) {\n\t\t\tjQuery.cleanData( getAll( node ) );\n\t\t}\n\n\t\tif ( node.parentNode ) {\n\t\t\tif ( keepData && jQuery.contains( node.ownerDocument, node ) ) {\n\t\t\t\tsetGlobalEval( getAll( node, "script" ) );\n\t\t\t}\n\t\t\tnode.parentNode.removeChild( node );\n\t\t}\n\t}\n\n\treturn elem;\n}\n\njQuery.extend( {\n\thtmlPrefilter: function( html ) {\n\t\treturn html.replace( rxhtmlTag, "<$1></$2>" );\n\t},\n\n\tclone: function( elem, dataAndEvents, deepDataAndEvents ) {\n\t\tvar i, l, srcElements, destElements,\n\t\t\tclone = elem.cloneNode( true ),\n\t\t\tinPage = jQuery.contains( elem.ownerDocument, elem );\n\n\t\t// Fix IE cloning issues\n\t\tif ( !support.noCloneChecked && ( elem.nodeType === 1 || elem.nodeType === 11 ) &&\n\t\t\t\t!jQuery.isXMLDoc( elem ) ) {\n\n\t\t\t// We eschew Sizzle here for performance reasons: https://jsperf.com/getall-vs-sizzle/2\n\t\t\tdestElements = getAll( clone );\n\t\t\tsrcElements = getAll( elem );\n\n\t\t\tfor ( i = 0, l = srcElements.length; i < l; i++ ) {\n\t\t\t\tfixInput( srcElements[ i ], destElements[ i ] );\n\t\t\t}\n\t\t}\n\n\t\t// Copy the events from the original to the clone\n\t\tif ( dataAndEvents ) {\n\t\t\tif ( deepDataAndEvents ) {\n\t\t\t\tsrcElements = srcElements || getAll( elem );\n\t\t\t\tdestElements = destElements || getAll( clone );\n\n\t\t\t\tfor ( i = 0, l = srcElements.length; i < l; i++ ) {\n\t\t\t\t\tcloneCopyEvent( srcElements[ i ], destElements[ i ] );\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\tcloneCopyEvent( elem, clone );\n\t\t\t}\n\t\t}\n\n\t\t// Preserve script evaluation history\n\t\tdestElements = getAll( clone, "script" );\n\t\tif ( destElements.length > 0 ) {\n\t\t\tsetGlobalEval( destElements, !inPage && getAll( elem, "script" ) );\n\t\t}\n\n\t\t// Return the cloned set\n\t\treturn clone;\n\t},\n\n\tcleanData: function( elems ) {\n\t\tvar data, elem, type,\n\t\t\tspecial = jQuery.event.special,\n\t\t\ti = 0;\n\n\t\tfor ( ; ( elem = elems[ i ] ) !== undefined; i++ ) {\n\t\t\tif ( acceptData( elem ) ) {\n\t\t\t\tif ( ( data = elem[ dataPriv.expando ] ) ) {\n\t\t\t\t\tif ( data.events ) {\n\t\t\t\t\t\tfor ( type in data.events ) {\n\t\t\t\t\t\t\tif ( special[ type ] ) {\n\t\t\t\t\t\t\t\tjQuery.event.remove( elem, type );\n\n\t\t\t\t\t\t\t// This is a shortcut to avoid jQuery.event.remove\'s overhead\n\t\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t\tjQuery.removeEvent( elem, type, data.handle );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\t// Support: Chrome <=35 - 45+\n\t\t\t\t\t// Assign undefined instead of using delete, see Data#remove\n\t\t\t\t\telem[ dataPriv.expando ] = undefined;\n\t\t\t\t}\n\t\t\t\tif ( elem[ dataUser.expando ] ) {\n\n\t\t\t\t\t// Support: Chrome <=35 - 45+\n\t\t\t\t\t// Assign undefined instead of using delete, see Data#remove\n\t\t\t\t\telem[ dataUser.expando ] = undefined;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n} );\n\njQuery.fn.extend( {\n\tdetach: function( selector ) {\n\t\treturn remove( this, selector, true );\n\t},\n\n\tremove: function( selector ) {\n\t\treturn remove( this, selector );\n\t},\n\n\ttext: function( value ) {\n\t\treturn access( this, function( value ) {\n\t\t\treturn value === undefined ?\n\t\t\t\tjQuery.text( this ) :\n\t\t\t\tthis.empty().each( function() {\n\t\t\t\t\tif ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {\n\t\t\t\t\t\tthis.textContent = value;\n\t\t\t\t\t}\n\t\t\t\t} );\n\t\t}, null, value, arguments.length );\n\t},\n\n\tappend: function() {\n\t\treturn domManip( this, arguments, function( elem ) {\n\t\t\tif ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {\n\t\t\t\tvar target = manipulationTarget( this, elem );\n\t\t\t\ttarget.appendChild( elem );\n\t\t\t}\n\t\t} );\n\t},\n\n\tprepend: function() {\n\t\treturn domManip( this, arguments, function( elem ) {\n\t\t\tif ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {\n\t\t\t\tvar target = manipulationTarget( this, elem );\n\t\t\t\ttarget.insertBefore( elem, target.firstChild );\n\t\t\t}\n\t\t} );\n\t},\n\n\tbefore: function() {\n\t\treturn domManip( this, arguments, function( elem ) {\n\t\t\tif ( this.parentNode ) {\n\t\t\t\tthis.parentNode.insertBefore( elem, this );\n\t\t\t}\n\t\t} );\n\t},\n\n\tafter: function() {\n\t\treturn domManip( this, arguments, function( elem ) {\n\t\t\tif ( this.parentNode ) {\n\t\t\t\tthis.parentNode.insertBefore( elem, this.nextSibling );\n\t\t\t}\n\t\t} );\n\t},\n\n\tempty: function() {\n\t\tvar elem,\n\t\t\ti = 0;\n\n\t\tfor ( ; ( elem = this[ i ] ) != null; i++ ) {\n\t\t\tif ( elem.nodeType === 1 ) {\n\n\t\t\t\t// Prevent memory leaks\n\t\t\t\tjQuery.cleanData( getAll( elem, false ) );\n\n\t\t\t\t// Remove any remaining nodes\n\t\t\t\telem.textContent = "";\n\t\t\t}\n\t\t}\n\n\t\treturn this;\n\t},\n\n\tclone: function( dataAndEvents, deepDataAndEvents ) {\n\t\tdataAndEvents = dataAndEvents == null ? false : dataAndEvents;\n\t\tdeepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;\n\n\t\treturn this.map( function() {\n\t\t\treturn jQuery.clone( this, dataAndEvents, deepDataAndEvents );\n\t\t} );\n\t},\n\n\thtml: function( value ) {\n\t\treturn access( this, function( value ) {\n\t\t\tvar elem = this[ 0 ] || {},\n\t\t\t\ti = 0,\n\t\t\t\tl = this.length;\n\n\t\t\tif ( value === undefined && elem.nodeType === 1 ) {\n\t\t\t\treturn elem.innerHTML;\n\t\t\t}\n\n\t\t\t// See if we can take a shortcut and just use innerHTML\n\t\t\tif ( typeof value === "string" && !rnoInnerhtml.test( value ) &&\n\t\t\t\t!wrapMap[ ( rtagName.exec( value ) || [ "", "" ] )[ 1 ].toLowerCase() ] ) {\n\n\t\t\t\tvalue = jQuery.htmlPrefilter( value );\n\n\t\t\t\ttry {\n\t\t\t\t\tfor ( ; i < l; i++ ) {\n\t\t\t\t\t\telem = this[ i ] || {};\n\n\t\t\t\t\t\t// Remove element nodes and prevent memory leaks\n\t\t\t\t\t\tif ( elem.nodeType === 1 ) {\n\t\t\t\t\t\t\tjQuery.cleanData( getAll( elem, false ) );\n\t\t\t\t\t\t\telem.innerHTML = value;\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\telem = 0;\n\n\t\t\t\t// If using innerHTML throws an exception, use the fallback method\n\t\t\t\t} catch ( e ) {}\n\t\t\t}\n\n\t\t\tif ( elem ) {\n\t\t\t\tthis.empty().append( value );\n\t\t\t}\n\t\t}, null, value, arguments.length );\n\t},\n\n\treplaceWith: function() {\n\t\tvar ignored = [];\n\n\t\t// Make the changes, replacing each non-ignored context element with the new content\n\t\treturn domManip( this, arguments, function( elem ) {\n\t\t\tvar parent = this.parentNode;\n\n\t\t\tif ( jQuery.inArray( this, ignored ) < 0 ) {\n\t\t\t\tjQuery.cleanData( getAll( this ) );\n\t\t\t\tif ( parent ) {\n\t\t\t\t\tparent.replaceChild( elem, this );\n\t\t\t\t}\n\t\t\t}\n\n\t\t// Force callback invocation\n\t\t}, ignored );\n\t}\n} );\n\njQuery.each( {\n\tappendTo: "append",\n\tprependTo: "prepend",\n\tinsertBefore: "before",\n\tinsertAfter: "after",\n\treplaceAll: "replaceWith"\n}, function( name, original ) {\n\tjQuery.fn[ name ] = function( selector ) {\n\t\tvar elems,\n\t\t\tret = [],\n\t\t\tinsert = jQuery( selector ),\n\t\t\tlast = insert.length - 1,\n\t\t\ti = 0;\n\n\t\tfor ( ; i <= last; i++ ) {\n\t\t\telems = i === last ? this : this.clone( true );\n\t\t\tjQuery( insert[ i ] )[ original ]( elems );\n\n\t\t\t// Support: Android <=4.0 only, PhantomJS 1 only\n\t\t\t// .get() because push.apply(_, arraylike) throws on ancient WebKit\n\t\t\tpush.apply( ret, elems.get() );\n\t\t}\n\n\t\treturn this.pushStack( ret );\n\t};\n} );\nvar rnumnonpx = new RegExp( "^(" + pnum + ")(?!px)[a-z%]+$", "i" );\n\nvar getStyles = function( elem ) {\n\n\t\t// Support: IE <=11 only, Firefox <=30 (#15098, #14150)\n\t\t// IE throws on elements created in popups\n\t\t// FF meanwhile throws on frame elements through "defaultView.getComputedStyle"\n\t\tvar view = elem.ownerDocument.defaultView;\n\n\t\tif ( !view || !view.opener ) {\n\t\t\tview = window;\n\t\t}\n\n\t\treturn view.getComputedStyle( elem );\n\t};\n\nvar rboxStyle = new RegExp( cssExpand.join( "|" ), "i" );\n\n\n\n( function() {\n\n\t// Executing both pixelPosition & boxSizingReliable tests require only one layout\n\t// so they\'re executed at the same time to save the second computation.\n\tfunction computeStyleTests() {\n\n\t\t// This is a singleton, we need to execute it only once\n\t\tif ( !div ) {\n\t\t\treturn;\n\t\t}\n\n\t\tcontainer.style.cssText = "position:absolute;left:-11111px;width:60px;" +\n\t\t\t"margin-top:1px;padding:0;border:0";\n\t\tdiv.style.cssText =\n\t\t\t"position:relative;display:block;box-sizing:border-box;overflow:scroll;" +\n\t\t\t"margin:auto;border:1px;padding:1px;" +\n\t\t\t"width:60%;top:1%";\n\t\tdocumentElement.appendChild( container ).appendChild( div );\n\n\t\tvar divStyle = window.getComputedStyle( div );\n\t\tpixelPositionVal = divStyle.top !== "1%";\n\n\t\t// Support: Android 4.0 - 4.3 only, Firefox <=3 - 44\n\t\treliableMarginLeftVal = roundPixelMeasures( divStyle.marginLeft ) === 12;\n\n\t\t// Support: Android 4.0 - 4.3 only, Safari <=9.1 - 10.1, iOS <=7.0 - 9.3\n\t\t// Some styles come back with percentage values, even though they shouldn\'t\n\t\tdiv.style.right = "60%";\n\t\tpixelBoxStylesVal = roundPixelMeasures( divStyle.right ) === 36;\n\n\t\t// Support: IE 9 - 11 only\n\t\t// Detect misreporting of content dimensions for box-sizing:border-box elements\n\t\tboxSizingReliableVal = roundPixelMeasures( divStyle.width ) === 36;\n\n\t\t// Support: IE 9 only\n\t\t// Detect overflow:scroll screwiness (gh-3699)\n\t\tdiv.style.position = "absolute";\n\t\tscrollboxSizeVal = div.offsetWidth === 36 || "absolute";\n\n\t\tdocumentElement.removeChild( container );\n\n\t\t// Nullify the div so it wouldn\'t be stored in the memory and\n\t\t// it will also be a sign that checks already performed\n\t\tdiv = null;\n\t}\n\n\tfunction roundPixelMeasures( measure ) {\n\t\treturn Math.round( parseFloat( measure ) );\n\t}\n\n\tvar pixelPositionVal, boxSizingReliableVal, scrollboxSizeVal, pixelBoxStylesVal,\n\t\treliableMarginLeftVal,\n\t\tcontainer = document.createElement( "div" ),\n\t\tdiv = document.createElement( "div" );\n\n\t// Finish early in limited (non-browser) environments\n\tif ( !div.style ) {\n\t\treturn;\n\t}\n\n\t// Support: IE <=9 - 11 only\n\t// Style of cloned element affects source element cloned (#8908)\n\tdiv.style.backgroundClip = "content-box";\n\tdiv.cloneNode( true ).style.backgroundClip = "";\n\tsupport.clearCloneStyle = div.style.backgroundClip === "content-box";\n\n\tjQuery.extend( support, {\n\t\tboxSizingReliable: function() {\n\t\t\tcomputeStyleTests();\n\t\t\treturn boxSizingReliableVal;\n\t\t},\n\t\tpixelBoxStyles: function() {\n\t\t\tcomputeStyleTests();\n\t\t\treturn pixelBoxStylesVal;\n\t\t},\n\t\tpixelPosition: function() {\n\t\t\tcomputeStyleTests();\n\t\t\treturn pixelPositionVal;\n\t\t},\n\t\treliableMarginLeft: function() {\n\t\t\tcomputeStyleTests();\n\t\t\treturn reliableMarginLeftVal;\n\t\t},\n\t\tscrollboxSize: function() {\n\t\t\tcomputeStyleTests();\n\t\t\treturn scrollboxSizeVal;\n\t\t}\n\t} );\n} )();\n\n\nfunction curCSS( elem, name, computed ) {\n\tvar width, minWidth, maxWidth, ret,\n\n\t\t// Support: Firefox 51+\n\t\t// Retrieving style before computed somehow\n\t\t// fixes an issue with getting wrong values\n\t\t// on detached elements\n\t\tstyle = elem.style;\n\n\tcomputed = computed || getStyles( elem );\n\n\t// getPropertyValue is needed for:\n\t//   .css(\'filter\') (IE 9 only, #12537)\n\t//   .css(\'--customProperty) (#3144)\n\tif ( computed ) {\n\t\tret = computed.getPropertyValue( name ) || computed[ name ];\n\n\t\tif ( ret === "" && !jQuery.contains( elem.ownerDocument, elem ) ) {\n\t\t\tret = jQuery.style( elem, name );\n\t\t}\n\n\t\t// A tribute to the "awesome hack by Dean Edwards"\n\t\t// Android Browser returns percentage for some values,\n\t\t// but width seems to be reliably pixels.\n\t\t// This is against the CSSOM draft spec:\n\t\t// https://drafts.csswg.org/cssom/#resolved-values\n\t\tif ( !support.pixelBoxStyles() && rnumnonpx.test( ret ) && rboxStyle.test( name ) ) {\n\n\t\t\t// Remember the original values\n\t\t\twidth = style.width;\n\t\t\tminWidth = style.minWidth;\n\t\t\tmaxWidth = style.maxWidth;\n\n\t\t\t// Put in the new values to get a computed value out\n\t\t\tstyle.minWidth = style.maxWidth = style.width = ret;\n\t\t\tret = computed.width;\n\n\t\t\t// Revert the changed values\n\t\t\tstyle.width = width;\n\t\t\tstyle.minWidth = minWidth;\n\t\t\tstyle.maxWidth = maxWidth;\n\t\t}\n\t}\n\n\treturn ret !== undefined ?\n\n\t\t// Support: IE <=9 - 11 only\n\t\t// IE returns zIndex value as an integer.\n\t\tret + "" :\n\t\tret;\n}\n\n\nfunction addGetHookIf( conditionFn, hookFn ) {\n\n\t// Define the hook, we\'ll check on the first run if it\'s really needed.\n\treturn {\n\t\tget: function() {\n\t\t\tif ( conditionFn() ) {\n\n\t\t\t\t// Hook not needed (or it\'s not possible to use it due\n\t\t\t\t// to missing dependency), remove it.\n\t\t\t\tdelete this.get;\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\t// Hook needed; redefine it so that the support test is not executed again.\n\t\t\treturn ( this.get = hookFn ).apply( this, arguments );\n\t\t}\n\t};\n}\n\n\nvar\n\n\t// Swappable if display is none or starts with table\n\t// except "table", "table-cell", or "table-caption"\n\t// See here for display values: https://developer.mozilla.org/en-US/docs/CSS/display\n\trdisplayswap = /^(none|table(?!-c[ea]).+)/,\n\trcustomProp = /^--/,\n\tcssShow = { position: "absolute", visibility: "hidden", display: "block" },\n\tcssNormalTransform = {\n\t\tletterSpacing: "0",\n\t\tfontWeight: "400"\n\t},\n\n\tcssPrefixes = [ "Webkit", "Moz", "ms" ],\n\temptyStyle = document.createElement( "div" ).style;\n\n// Return a css property mapped to a potentially vendor prefixed property\nfunction vendorPropName( name ) {\n\n\t// Shortcut for names that are not vendor prefixed\n\tif ( name in emptyStyle ) {\n\t\treturn name;\n\t}\n\n\t// Check for vendor prefixed names\n\tvar capName = name[ 0 ].toUpperCase() + name.slice( 1 ),\n\t\ti = cssPrefixes.length;\n\n\twhile ( i-- ) {\n\t\tname = cssPrefixes[ i ] + capName;\n\t\tif ( name in emptyStyle ) {\n\t\t\treturn name;\n\t\t}\n\t}\n}\n\n// Return a property mapped along what jQuery.cssProps suggests or to\n// a vendor prefixed property.\nfunction finalPropName( name ) {\n\tvar ret = jQuery.cssProps[ name ];\n\tif ( !ret ) {\n\t\tret = jQuery.cssProps[ name ] = vendorPropName( name ) || name;\n\t}\n\treturn ret;\n}\n\nfunction setPositiveNumber( elem, value, subtract ) {\n\n\t// Any relative (+/-) values have already been\n\t// normalized at this point\n\tvar matches = rcssNum.exec( value );\n\treturn matches ?\n\n\t\t// Guard against undefined "subtract", e.g., when used as in cssHooks\n\t\tMath.max( 0, matches[ 2 ] - ( subtract || 0 ) ) + ( matches[ 3 ] || "px" ) :\n\t\tvalue;\n}\n\nfunction boxModelAdjustment( elem, dimension, box, isBorderBox, styles, computedVal ) {\n\tvar i = dimension === "width" ? 1 : 0,\n\t\textra = 0,\n\t\tdelta = 0;\n\n\t// Adjustment may not be necessary\n\tif ( box === ( isBorderBox ? "border" : "content" ) ) {\n\t\treturn 0;\n\t}\n\n\tfor ( ; i < 4; i += 2 ) {\n\n\t\t// Both box models exclude margin\n\t\tif ( box === "margin" ) {\n\t\t\tdelta += jQuery.css( elem, box + cssExpand[ i ], true, styles );\n\t\t}\n\n\t\t// If we get here with a content-box, we\'re seeking "padding" or "border" or "margin"\n\t\tif ( !isBorderBox ) {\n\n\t\t\t// Add padding\n\t\t\tdelta += jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );\n\n\t\t\t// For "border" or "margin", add border\n\t\t\tif ( box !== "padding" ) {\n\t\t\t\tdelta += jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );\n\n\t\t\t// But still keep track of it otherwise\n\t\t\t} else {\n\t\t\t\textra += jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );\n\t\t\t}\n\n\t\t// If we get here with a border-box (content + padding + border), we\'re seeking "content" or\n\t\t// "padding" or "margin"\n\t\t} else {\n\n\t\t\t// For "content", subtract padding\n\t\t\tif ( box === "content" ) {\n\t\t\t\tdelta -= jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );\n\t\t\t}\n\n\t\t\t// For "content" or "padding", subtract border\n\t\t\tif ( box !== "margin" ) {\n\t\t\t\tdelta -= jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );\n\t\t\t}\n\t\t}\n\t}\n\n\t// Account for positive content-box scroll gutter when requested by providing computedVal\n\tif ( !isBorderBox && computedVal >= 0 ) {\n\n\t\t// offsetWidth/offsetHeight is a rounded sum of content, padding, scroll gutter, and border\n\t\t// Assuming integer scroll gutter, subtract the rest and round down\n\t\tdelta += Math.max( 0, Math.ceil(\n\t\t\telem[ "offset" + dimension[ 0 ].toUpperCase() + dimension.slice( 1 ) ] -\n\t\t\tcomputedVal -\n\t\t\tdelta -\n\t\t\textra -\n\t\t\t0.5\n\t\t) );\n\t}\n\n\treturn delta;\n}\n\nfunction getWidthOrHeight( elem, dimension, extra ) {\n\n\t// Start with computed style\n\tvar styles = getStyles( elem ),\n\t\tval = curCSS( elem, dimension, styles ),\n\t\tisBorderBox = jQuery.css( elem, "boxSizing", false, styles ) === "border-box",\n\t\tvalueIsBorderBox = isBorderBox;\n\n\t// Support: Firefox <=54\n\t// Return a confounding non-pixel value or feign ignorance, as appropriate.\n\tif ( rnumnonpx.test( val ) ) {\n\t\tif ( !extra ) {\n\t\t\treturn val;\n\t\t}\n\t\tval = "auto";\n\t}\n\n\t// Check for style in case a browser which returns unreliable values\n\t// for getComputedStyle silently falls back to the reliable elem.style\n\tvalueIsBorderBox = valueIsBorderBox &&\n\t\t( support.boxSizingReliable() || val === elem.style[ dimension ] );\n\n\t// Fall back to offsetWidth/offsetHeight when value is "auto"\n\t// This happens for inline elements with no explicit setting (gh-3571)\n\t// Support: Android <=4.1 - 4.3 only\n\t// Also use offsetWidth/offsetHeight for misreported inline dimensions (gh-3602)\n\tif ( val === "auto" ||\n\t\t!parseFloat( val ) && jQuery.css( elem, "display", false, styles ) === "inline" ) {\n\n\t\tval = elem[ "offset" + dimension[ 0 ].toUpperCase() + dimension.slice( 1 ) ];\n\n\t\t// offsetWidth/offsetHeight provide border-box values\n\t\tvalueIsBorderBox = true;\n\t}\n\n\t// Normalize "" and auto\n\tval = parseFloat( val ) || 0;\n\n\t// Adjust for the element\'s box model\n\treturn ( val +\n\t\tboxModelAdjustment(\n\t\t\telem,\n\t\t\tdimension,\n\t\t\textra || ( isBorderBox ? "border" : "content" ),\n\t\t\tvalueIsBorderBox,\n\t\t\tstyles,\n\n\t\t\t// Provide the current computed size to request scroll gutter calculation (gh-3589)\n\t\t\tval\n\t\t)\n\t) + "px";\n}\n\njQuery.extend( {\n\n\t// Add in style property hooks for overriding the default\n\t// behavior of getting and setting a style property\n\tcssHooks: {\n\t\topacity: {\n\t\t\tget: function( elem, computed ) {\n\t\t\t\tif ( computed ) {\n\n\t\t\t\t\t// We should always get a number back from opacity\n\t\t\t\t\tvar ret = curCSS( elem, "opacity" );\n\t\t\t\t\treturn ret === "" ? "1" : ret;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t},\n\n\t// Don\'t automatically add "px" to these possibly-unitless properties\n\tcssNumber: {\n\t\t"animationIterationCount": true,\n\t\t"columnCount": true,\n\t\t"fillOpacity": true,\n\t\t"flexGrow": true,\n\t\t"flexShrink": true,\n\t\t"fontWeight": true,\n\t\t"lineHeight": true,\n\t\t"opacity": true,\n\t\t"order": true,\n\t\t"orphans": true,\n\t\t"widows": true,\n\t\t"zIndex": true,\n\t\t"zoom": true\n\t},\n\n\t// Add in properties whose names you wish to fix before\n\t// setting or getting the value\n\tcssProps: {},\n\n\t// Get and set the style property on a DOM Node\n\tstyle: function( elem, name, value, extra ) {\n\n\t\t// Don\'t set styles on text and comment nodes\n\t\tif ( !elem || elem.nodeType === 3 || elem.nodeType === 8 || !elem.style ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Make sure that we\'re working with the right name\n\t\tvar ret, type, hooks,\n\t\t\torigName = camelCase( name ),\n\t\t\tisCustomProp = rcustomProp.test( name ),\n\t\t\tstyle = elem.style;\n\n\t\t// Make sure that we\'re working with the right name. We don\'t\n\t\t// want to query the value if it is a CSS custom property\n\t\t// since they are user-defined.\n\t\tif ( !isCustomProp ) {\n\t\t\tname = finalPropName( origName );\n\t\t}\n\n\t\t// Gets hook for the prefixed version, then unprefixed version\n\t\thooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];\n\n\t\t// Check if we\'re setting a value\n\t\tif ( value !== undefined ) {\n\t\t\ttype = typeof value;\n\n\t\t\t// Convert "+=" or "-=" to relative numbers (#7345)\n\t\t\tif ( type === "string" && ( ret = rcssNum.exec( value ) ) && ret[ 1 ] ) {\n\t\t\t\tvalue = adjustCSS( elem, name, ret );\n\n\t\t\t\t// Fixes bug #9237\n\t\t\t\ttype = "number";\n\t\t\t}\n\n\t\t\t// Make sure that null and NaN values aren\'t set (#7116)\n\t\t\tif ( value == null || value !== value ) {\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\t// If a number was passed in, add the unit (except for certain CSS properties)\n\t\t\tif ( type === "number" ) {\n\t\t\t\tvalue += ret && ret[ 3 ] || ( jQuery.cssNumber[ origName ] ? "" : "px" );\n\t\t\t}\n\n\t\t\t// background-* props affect original clone\'s values\n\t\t\tif ( !support.clearCloneStyle && value === "" && name.indexOf( "background" ) === 0 ) {\n\t\t\t\tstyle[ name ] = "inherit";\n\t\t\t}\n\n\t\t\t// If a hook was provided, use that value, otherwise just set the specified value\n\t\t\tif ( !hooks || !( "set" in hooks ) ||\n\t\t\t\t( value = hooks.set( elem, value, extra ) ) !== undefined ) {\n\n\t\t\t\tif ( isCustomProp ) {\n\t\t\t\t\tstyle.setProperty( name, value );\n\t\t\t\t} else {\n\t\t\t\t\tstyle[ name ] = value;\n\t\t\t\t}\n\t\t\t}\n\n\t\t} else {\n\n\t\t\t// If a hook was provided get the non-computed value from there\n\t\t\tif ( hooks && "get" in hooks &&\n\t\t\t\t( ret = hooks.get( elem, false, extra ) ) !== undefined ) {\n\n\t\t\t\treturn ret;\n\t\t\t}\n\n\t\t\t// Otherwise just get the value from the style object\n\t\t\treturn style[ name ];\n\t\t}\n\t},\n\n\tcss: function( elem, name, extra, styles ) {\n\t\tvar val, num, hooks,\n\t\t\torigName = camelCase( name ),\n\t\t\tisCustomProp = rcustomProp.test( name );\n\n\t\t// Make sure that we\'re working with the right name. We don\'t\n\t\t// want to modify the value if it is a CSS custom property\n\t\t// since they are user-defined.\n\t\tif ( !isCustomProp ) {\n\t\t\tname = finalPropName( origName );\n\t\t}\n\n\t\t// Try prefixed name followed by the unprefixed name\n\t\thooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];\n\n\t\t// If a hook was provided get the computed value from there\n\t\tif ( hooks && "get" in hooks ) {\n\t\t\tval = hooks.get( elem, true, extra );\n\t\t}\n\n\t\t// Otherwise, if a way to get the computed value exists, use that\n\t\tif ( val === undefined ) {\n\t\t\tval = curCSS( elem, name, styles );\n\t\t}\n\n\t\t// Convert "normal" to computed value\n\t\tif ( val === "normal" && name in cssNormalTransform ) {\n\t\t\tval = cssNormalTransform[ name ];\n\t\t}\n\n\t\t// Make numeric if forced or a qualifier was provided and val looks numeric\n\t\tif ( extra === "" || extra ) {\n\t\t\tnum = parseFloat( val );\n\t\t\treturn extra === true || isFinite( num ) ? num || 0 : val;\n\t\t}\n\n\t\treturn val;\n\t}\n} );\n\njQuery.each( [ "height", "width" ], function( i, dimension ) {\n\tjQuery.cssHooks[ dimension ] = {\n\t\tget: function( elem, computed, extra ) {\n\t\t\tif ( computed ) {\n\n\t\t\t\t// Certain elements can have dimension info if we invisibly show them\n\t\t\t\t// but it must have a current display style that would benefit\n\t\t\t\treturn rdisplayswap.test( jQuery.css( elem, "display" ) ) &&\n\n\t\t\t\t\t// Support: Safari 8+\n\t\t\t\t\t// Table columns in Safari have non-zero offsetWidth & zero\n\t\t\t\t\t// getBoundingClientRect().width unless display is changed.\n\t\t\t\t\t// Support: IE <=11 only\n\t\t\t\t\t// Running getBoundingClientRect on a disconnected node\n\t\t\t\t\t// in IE throws an error.\n\t\t\t\t\t( !elem.getClientRects().length || !elem.getBoundingClientRect().width ) ?\n\t\t\t\t\t\tswap( elem, cssShow, function() {\n\t\t\t\t\t\t\treturn getWidthOrHeight( elem, dimension, extra );\n\t\t\t\t\t\t} ) :\n\t\t\t\t\t\tgetWidthOrHeight( elem, dimension, extra );\n\t\t\t}\n\t\t},\n\n\t\tset: function( elem, value, extra ) {\n\t\t\tvar matches,\n\t\t\t\tstyles = getStyles( elem ),\n\t\t\t\tisBorderBox = jQuery.css( elem, "boxSizing", false, styles ) === "border-box",\n\t\t\t\tsubtract = extra && boxModelAdjustment(\n\t\t\t\t\telem,\n\t\t\t\t\tdimension,\n\t\t\t\t\textra,\n\t\t\t\t\tisBorderBox,\n\t\t\t\t\tstyles\n\t\t\t\t);\n\n\t\t\t// Account for unreliable border-box dimensions by comparing offset* to computed and\n\t\t\t// faking a content-box to get border and padding (gh-3699)\n\t\t\tif ( isBorderBox && support.scrollboxSize() === styles.position ) {\n\t\t\t\tsubtract -= Math.ceil(\n\t\t\t\t\telem[ "offset" + dimension[ 0 ].toUpperCase() + dimension.slice( 1 ) ] -\n\t\t\t\t\tparseFloat( styles[ dimension ] ) -\n\t\t\t\t\tboxModelAdjustment( elem, dimension, "border", false, styles ) -\n\t\t\t\t\t0.5\n\t\t\t\t);\n\t\t\t}\n\n\t\t\t// Convert to pixels if value adjustment is needed\n\t\t\tif ( subtract && ( matches = rcssNum.exec( value ) ) &&\n\t\t\t\t( matches[ 3 ] || "px" ) !== "px" ) {\n\n\t\t\t\telem.style[ dimension ] = value;\n\t\t\t\tvalue = jQuery.css( elem, dimension );\n\t\t\t}\n\n\t\t\treturn setPositiveNumber( elem, value, subtract );\n\t\t}\n\t};\n} );\n\njQuery.cssHooks.marginLeft = addGetHookIf( support.reliableMarginLeft,\n\tfunction( elem, computed ) {\n\t\tif ( computed ) {\n\t\t\treturn ( parseFloat( curCSS( elem, "marginLeft" ) ) ||\n\t\t\t\telem.getBoundingClientRect().left -\n\t\t\t\t\tswap( elem, { marginLeft: 0 }, function() {\n\t\t\t\t\t\treturn elem.getBoundingClientRect().left;\n\t\t\t\t\t} )\n\t\t\t\t) + "px";\n\t\t}\n\t}\n);\n\n// These hooks are used by animate to expand properties\njQuery.each( {\n\tmargin: "",\n\tpadding: "",\n\tborder: "Width"\n}, function( prefix, suffix ) {\n\tjQuery.cssHooks[ prefix + suffix ] = {\n\t\texpand: function( value ) {\n\t\t\tvar i = 0,\n\t\t\t\texpanded = {},\n\n\t\t\t\t// Assumes a single number if not a string\n\t\t\t\tparts = typeof value === "string" ? value.split( " " ) : [ value ];\n\n\t\t\tfor ( ; i < 4; i++ ) {\n\t\t\t\texpanded[ prefix + cssExpand[ i ] + suffix ] =\n\t\t\t\t\tparts[ i ] || parts[ i - 2 ] || parts[ 0 ];\n\t\t\t}\n\n\t\t\treturn expanded;\n\t\t}\n\t};\n\n\tif ( prefix !== "margin" ) {\n\t\tjQuery.cssHooks[ prefix + suffix ].set = setPositiveNumber;\n\t}\n} );\n\njQuery.fn.extend( {\n\tcss: function( name, value ) {\n\t\treturn access( this, function( elem, name, value ) {\n\t\t\tvar styles, len,\n\t\t\t\tmap = {},\n\t\t\t\ti = 0;\n\n\t\t\tif ( Array.isArray( name ) ) {\n\t\t\t\tstyles = getStyles( elem );\n\t\t\t\tlen = name.length;\n\n\t\t\t\tfor ( ; i < len; i++ ) {\n\t\t\t\t\tmap[ name[ i ] ] = jQuery.css( elem, name[ i ], false, styles );\n\t\t\t\t}\n\n\t\t\t\treturn map;\n\t\t\t}\n\n\t\t\treturn value !== undefined ?\n\t\t\t\tjQuery.style( elem, name, value ) :\n\t\t\t\tjQuery.css( elem, name );\n\t\t}, name, value, arguments.length > 1 );\n\t}\n} );\n\n\nfunction Tween( elem, options, prop, end, easing ) {\n\treturn new Tween.prototype.init( elem, options, prop, end, easing );\n}\njQuery.Tween = Tween;\n\nTween.prototype = {\n\tconstructor: Tween,\n\tinit: function( elem, options, prop, end, easing, unit ) {\n\t\tthis.elem = elem;\n\t\tthis.prop = prop;\n\t\tthis.easing = easing || jQuery.easing._default;\n\t\tthis.options = options;\n\t\tthis.start = this.now = this.cur();\n\t\tthis.end = end;\n\t\tthis.unit = unit || ( jQuery.cssNumber[ prop ] ? "" : "px" );\n\t},\n\tcur: function() {\n\t\tvar hooks = Tween.propHooks[ this.prop ];\n\n\t\treturn hooks && hooks.get ?\n\t\t\thooks.get( this ) :\n\t\t\tTween.propHooks._default.get( this );\n\t},\n\trun: function( percent ) {\n\t\tvar eased,\n\t\t\thooks = Tween.propHooks[ this.prop ];\n\n\t\tif ( this.options.duration ) {\n\t\t\tthis.pos = eased = jQuery.easing[ this.easing ](\n\t\t\t\tpercent, this.options.duration * percent, 0, 1, this.options.duration\n\t\t\t);\n\t\t} else {\n\t\t\tthis.pos = eased = percent;\n\t\t}\n\t\tthis.now = ( this.end - this.start ) * eased + this.start;\n\n\t\tif ( this.options.step ) {\n\t\t\tthis.options.step.call( this.elem, this.now, this );\n\t\t}\n\n\t\tif ( hooks && hooks.set ) {\n\t\t\thooks.set( this );\n\t\t} else {\n\t\t\tTween.propHooks._default.set( this );\n\t\t}\n\t\treturn this;\n\t}\n};\n\nTween.prototype.init.prototype = Tween.prototype;\n\nTween.propHooks = {\n\t_default: {\n\t\tget: function( tween ) {\n\t\t\tvar result;\n\n\t\t\t// Use a property on the element directly when it is not a DOM element,\n\t\t\t// or when there is no matching style property that exists.\n\t\t\tif ( tween.elem.nodeType !== 1 ||\n\t\t\t\ttween.elem[ tween.prop ] != null && tween.elem.style[ tween.prop ] == null ) {\n\t\t\t\treturn tween.elem[ tween.prop ];\n\t\t\t}\n\n\t\t\t// Passing an empty string as a 3rd parameter to .css will automatically\n\t\t\t// attempt a parseFloat and fallback to a string if the parse fails.\n\t\t\t// Simple values such as "10px" are parsed to Float;\n\t\t\t// complex values such as "rotate(1rad)" are returned as-is.\n\t\t\tresult = jQuery.css( tween.elem, tween.prop, "" );\n\n\t\t\t// Empty strings, null, undefined and "auto" are converted to 0.\n\t\t\treturn !result || result === "auto" ? 0 : result;\n\t\t},\n\t\tset: function( tween ) {\n\n\t\t\t// Use step hook for back compat.\n\t\t\t// Use cssHook if its there.\n\t\t\t// Use .style if available and use plain properties where available.\n\t\t\tif ( jQuery.fx.step[ tween.prop ] ) {\n\t\t\t\tjQuery.fx.step[ tween.prop ]( tween );\n\t\t\t} else if ( tween.elem.nodeType === 1 &&\n\t\t\t\t( tween.elem.style[ jQuery.cssProps[ tween.prop ] ] != null ||\n\t\t\t\t\tjQuery.cssHooks[ tween.prop ] ) ) {\n\t\t\t\tjQuery.style( tween.elem, tween.prop, tween.now + tween.unit );\n\t\t\t} else {\n\t\t\t\ttween.elem[ tween.prop ] = tween.now;\n\t\t\t}\n\t\t}\n\t}\n};\n\n// Support: IE <=9 only\n// Panic based approach to setting things on disconnected nodes\nTween.propHooks.scrollTop = Tween.propHooks.scrollLeft = {\n\tset: function( tween ) {\n\t\tif ( tween.elem.nodeType && tween.elem.parentNode ) {\n\t\t\ttween.elem[ tween.prop ] = tween.now;\n\t\t}\n\t}\n};\n\njQuery.easing = {\n\tlinear: function( p ) {\n\t\treturn p;\n\t},\n\tswing: function( p ) {\n\t\treturn 0.5 - Math.cos( p * Math.PI ) / 2;\n\t},\n\t_default: "swing"\n};\n\njQuery.fx = Tween.prototype.init;\n\n// Back compat <1.8 extension point\njQuery.fx.step = {};\n\n\n\n\nvar\n\tfxNow, inProgress,\n\trfxtypes = /^(?:toggle|show|hide)$/,\n\trrun = /queueHooks$/;\n\nfunction schedule() {\n\tif ( inProgress ) {\n\t\tif ( document.hidden === false && window.requestAnimationFrame ) {\n\t\t\twindow.requestAnimationFrame( schedule );\n\t\t} else {\n\t\t\twindow.setTimeout( schedule, jQuery.fx.interval );\n\t\t}\n\n\t\tjQuery.fx.tick();\n\t}\n}\n\n// Animations created synchronously will run synchronously\nfunction createFxNow() {\n\twindow.setTimeout( function() {\n\t\tfxNow = undefined;\n\t} );\n\treturn ( fxNow = Date.now() );\n}\n\n// Generate parameters to create a standard animation\nfunction genFx( type, includeWidth ) {\n\tvar which,\n\t\ti = 0,\n\t\tattrs = { height: type };\n\n\t// If we include width, step value is 1 to do all cssExpand values,\n\t// otherwise step value is 2 to skip over Left and Right\n\tincludeWidth = includeWidth ? 1 : 0;\n\tfor ( ; i < 4; i += 2 - includeWidth ) {\n\t\twhich = cssExpand[ i ];\n\t\tattrs[ "margin" + which ] = attrs[ "padding" + which ] = type;\n\t}\n\n\tif ( includeWidth ) {\n\t\tattrs.opacity = attrs.width = type;\n\t}\n\n\treturn attrs;\n}\n\nfunction createTween( value, prop, animation ) {\n\tvar tween,\n\t\tcollection = ( Animation.tweeners[ prop ] || [] ).concat( Animation.tweeners[ "*" ] ),\n\t\tindex = 0,\n\t\tlength = collection.length;\n\tfor ( ; index < length; index++ ) {\n\t\tif ( ( tween = collection[ index ].call( animation, prop, value ) ) ) {\n\n\t\t\t// We\'re done with this property\n\t\t\treturn tween;\n\t\t}\n\t}\n}\n\nfunction defaultPrefilter( elem, props, opts ) {\n\tvar prop, value, toggle, hooks, oldfire, propTween, restoreDisplay, display,\n\t\tisBox = "width" in props || "height" in props,\n\t\tanim = this,\n\t\torig = {},\n\t\tstyle = elem.style,\n\t\thidden = elem.nodeType && isHiddenWithinTree( elem ),\n\t\tdataShow = dataPriv.get( elem, "fxshow" );\n\n\t// Queue-skipping animations hijack the fx hooks\n\tif ( !opts.queue ) {\n\t\thooks = jQuery._queueHooks( elem, "fx" );\n\t\tif ( hooks.unqueued == null ) {\n\t\t\thooks.unqueued = 0;\n\t\t\toldfire = hooks.empty.fire;\n\t\t\thooks.empty.fire = function() {\n\t\t\t\tif ( !hooks.unqueued ) {\n\t\t\t\t\toldfire();\n\t\t\t\t}\n\t\t\t};\n\t\t}\n\t\thooks.unqueued++;\n\n\t\tanim.always( function() {\n\n\t\t\t// Ensure the complete handler is called before this completes\n\t\t\tanim.always( function() {\n\t\t\t\thooks.unqueued--;\n\t\t\t\tif ( !jQuery.queue( elem, "fx" ).length ) {\n\t\t\t\t\thooks.empty.fire();\n\t\t\t\t}\n\t\t\t} );\n\t\t} );\n\t}\n\n\t// Detect show/hide animations\n\tfor ( prop in props ) {\n\t\tvalue = props[ prop ];\n\t\tif ( rfxtypes.test( value ) ) {\n\t\t\tdelete props[ prop ];\n\t\t\ttoggle = toggle || value === "toggle";\n\t\t\tif ( value === ( hidden ? "hide" : "show" ) ) {\n\n\t\t\t\t// Pretend to be hidden if this is a "show" and\n\t\t\t\t// there is still data from a stopped show/hide\n\t\t\t\tif ( value === "show" && dataShow && dataShow[ prop ] !== undefined ) {\n\t\t\t\t\thidden = true;\n\n\t\t\t\t// Ignore all other no-op show/hide data\n\t\t\t\t} else {\n\t\t\t\t\tcontinue;\n\t\t\t\t}\n\t\t\t}\n\t\t\torig[ prop ] = dataShow && dataShow[ prop ] || jQuery.style( elem, prop );\n\t\t}\n\t}\n\n\t// Bail out if this is a no-op like .hide().hide()\n\tpropTween = !jQuery.isEmptyObject( props );\n\tif ( !propTween && jQuery.isEmptyObject( orig ) ) {\n\t\treturn;\n\t}\n\n\t// Restrict "overflow" and "display" styles during box animations\n\tif ( isBox && elem.nodeType === 1 ) {\n\n\t\t// Support: IE <=9 - 11, Edge 12 - 15\n\t\t// Record all 3 overflow attributes because IE does not infer the shorthand\n\t\t// from identically-valued overflowX and overflowY and Edge just mirrors\n\t\t// the overflowX value there.\n\t\topts.overflow = [ style.overflow, style.overflowX, style.overflowY ];\n\n\t\t// Identify a display type, preferring old show/hide data over the CSS cascade\n\t\trestoreDisplay = dataShow && dataShow.display;\n\t\tif ( restoreDisplay == null ) {\n\t\t\trestoreDisplay = dataPriv.get( elem, "display" );\n\t\t}\n\t\tdisplay = jQuery.css( elem, "display" );\n\t\tif ( display === "none" ) {\n\t\t\tif ( restoreDisplay ) {\n\t\t\t\tdisplay = restoreDisplay;\n\t\t\t} else {\n\n\t\t\t\t// Get nonempty value(s) by temporarily forcing visibility\n\t\t\t\tshowHide( [ elem ], true );\n\t\t\t\trestoreDisplay = elem.style.display || restoreDisplay;\n\t\t\t\tdisplay = jQuery.css( elem, "display" );\n\t\t\t\tshowHide( [ elem ] );\n\t\t\t}\n\t\t}\n\n\t\t// Animate inline elements as inline-block\n\t\tif ( display === "inline" || display === "inline-block" && restoreDisplay != null ) {\n\t\t\tif ( jQuery.css( elem, "float" ) === "none" ) {\n\n\t\t\t\t// Restore the original display value at the end of pure show/hide animations\n\t\t\t\tif ( !propTween ) {\n\t\t\t\t\tanim.done( function() {\n\t\t\t\t\t\tstyle.display = restoreDisplay;\n\t\t\t\t\t} );\n\t\t\t\t\tif ( restoreDisplay == null ) {\n\t\t\t\t\t\tdisplay = style.display;\n\t\t\t\t\t\trestoreDisplay = display === "none" ? "" : display;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t\tstyle.display = "inline-block";\n\t\t\t}\n\t\t}\n\t}\n\n\tif ( opts.overflow ) {\n\t\tstyle.overflow = "hidden";\n\t\tanim.always( function() {\n\t\t\tstyle.overflow = opts.overflow[ 0 ];\n\t\t\tstyle.overflowX = opts.overflow[ 1 ];\n\t\t\tstyle.overflowY = opts.overflow[ 2 ];\n\t\t} );\n\t}\n\n\t// Implement show/hide animations\n\tpropTween = false;\n\tfor ( prop in orig ) {\n\n\t\t// General show/hide setup for this element animation\n\t\tif ( !propTween ) {\n\t\t\tif ( dataShow ) {\n\t\t\t\tif ( "hidden" in dataShow ) {\n\t\t\t\t\thidden = dataShow.hidden;\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\tdataShow = dataPriv.access( elem, "fxshow", { display: restoreDisplay } );\n\t\t\t}\n\n\t\t\t// Store hidden/visible for toggle so `.stop().toggle()` "reverses"\n\t\t\tif ( toggle ) {\n\t\t\t\tdataShow.hidden = !hidden;\n\t\t\t}\n\n\t\t\t// Show elements before animating them\n\t\t\tif ( hidden ) {\n\t\t\t\tshowHide( [ elem ], true );\n\t\t\t}\n\n\t\t\t/* eslint-disable no-loop-func */\n\n\t\t\tanim.done( function() {\n\n\t\t\t/* eslint-enable no-loop-func */\n\n\t\t\t\t// The final step of a "hide" animation is actually hiding the element\n\t\t\t\tif ( !hidden ) {\n\t\t\t\t\tshowHide( [ elem ] );\n\t\t\t\t}\n\t\t\t\tdataPriv.remove( elem, "fxshow" );\n\t\t\t\tfor ( prop in orig ) {\n\t\t\t\t\tjQuery.style( elem, prop, orig[ prop ] );\n\t\t\t\t}\n\t\t\t} );\n\t\t}\n\n\t\t// Per-property setup\n\t\tpropTween = createTween( hidden ? dataShow[ prop ] : 0, prop, anim );\n\t\tif ( !( prop in dataShow ) ) {\n\t\t\tdataShow[ prop ] = propTween.start;\n\t\t\tif ( hidden ) {\n\t\t\t\tpropTween.end = propTween.start;\n\t\t\t\tpropTween.start = 0;\n\t\t\t}\n\t\t}\n\t}\n}\n\nfunction propFilter( props, specialEasing ) {\n\tvar index, name, easing, value, hooks;\n\n\t// camelCase, specialEasing and expand cssHook pass\n\tfor ( index in props ) {\n\t\tname = camelCase( index );\n\t\teasing = specialEasing[ name ];\n\t\tvalue = props[ index ];\n\t\tif ( Array.isArray( value ) ) {\n\t\t\teasing = value[ 1 ];\n\t\t\tvalue = props[ index ] = value[ 0 ];\n\t\t}\n\n\t\tif ( index !== name ) {\n\t\t\tprops[ name ] = value;\n\t\t\tdelete props[ index ];\n\t\t}\n\n\t\thooks = jQuery.cssHooks[ name ];\n\t\tif ( hooks && "expand" in hooks ) {\n\t\t\tvalue = hooks.expand( value );\n\t\t\tdelete props[ name ];\n\n\t\t\t// Not quite $.extend, this won\'t overwrite existing keys.\n\t\t\t// Reusing \'index\' because we have the correct "name"\n\t\t\tfor ( index in value ) {\n\t\t\t\tif ( !( index in props ) ) {\n\t\t\t\t\tprops[ index ] = value[ index ];\n\t\t\t\t\tspecialEasing[ index ] = easing;\n\t\t\t\t}\n\t\t\t}\n\t\t} else {\n\t\t\tspecialEasing[ name ] = easing;\n\t\t}\n\t}\n}\n\nfunction Animation( elem, properties, options ) {\n\tvar result,\n\t\tstopped,\n\t\tindex = 0,\n\t\tlength = Animation.prefilters.length,\n\t\tdeferred = jQuery.Deferred().always( function() {\n\n\t\t\t// Don\'t match elem in the :animated selector\n\t\t\tdelete tick.elem;\n\t\t} ),\n\t\ttick = function() {\n\t\t\tif ( stopped ) {\n\t\t\t\treturn false;\n\t\t\t}\n\t\t\tvar currentTime = fxNow || createFxNow(),\n\t\t\t\tremaining = Math.max( 0, animation.startTime + animation.duration - currentTime ),\n\n\t\t\t\t// Support: Android 2.3 only\n\t\t\t\t// Archaic crash bug won\'t allow us to use `1 - ( 0.5 || 0 )` (#12497)\n\t\t\t\ttemp = remaining / animation.duration || 0,\n\t\t\t\tpercent = 1 - temp,\n\t\t\t\tindex = 0,\n\t\t\t\tlength = animation.tweens.length;\n\n\t\t\tfor ( ; index < length; index++ ) {\n\t\t\t\tanimation.tweens[ index ].run( percent );\n\t\t\t}\n\n\t\t\tdeferred.notifyWith( elem, [ animation, percent, remaining ] );\n\n\t\t\t// If there\'s more to do, yield\n\t\t\tif ( percent < 1 && length ) {\n\t\t\t\treturn remaining;\n\t\t\t}\n\n\t\t\t// If this was an empty animation, synthesize a final progress notification\n\t\t\tif ( !length ) {\n\t\t\t\tdeferred.notifyWith( elem, [ animation, 1, 0 ] );\n\t\t\t}\n\n\t\t\t// Resolve the animation and report its conclusion\n\t\t\tdeferred.resolveWith( elem, [ animation ] );\n\t\t\treturn false;\n\t\t},\n\t\tanimation = deferred.promise( {\n\t\t\telem: elem,\n\t\t\tprops: jQuery.extend( {}, properties ),\n\t\t\topts: jQuery.extend( true, {\n\t\t\t\tspecialEasing: {},\n\t\t\t\teasing: jQuery.easing._default\n\t\t\t}, options ),\n\t\t\toriginalProperties: properties,\n\t\t\toriginalOptions: options,\n\t\t\tstartTime: fxNow || createFxNow(),\n\t\t\tduration: options.duration,\n\t\t\ttweens: [],\n\t\t\tcreateTween: function( prop, end ) {\n\t\t\t\tvar tween = jQuery.Tween( elem, animation.opts, prop, end,\n\t\t\t\t\t\tanimation.opts.specialEasing[ prop ] || animation.opts.easing );\n\t\t\t\tanimation.tweens.push( tween );\n\t\t\t\treturn tween;\n\t\t\t},\n\t\t\tstop: function( gotoEnd ) {\n\t\t\t\tvar index = 0,\n\n\t\t\t\t\t// If we are going to the end, we want to run all the tweens\n\t\t\t\t\t// otherwise we skip this part\n\t\t\t\t\tlength = gotoEnd ? animation.tweens.length : 0;\n\t\t\t\tif ( stopped ) {\n\t\t\t\t\treturn this;\n\t\t\t\t}\n\t\t\t\tstopped = true;\n\t\t\t\tfor ( ; index < length; index++ ) {\n\t\t\t\t\tanimation.tweens[ index ].run( 1 );\n\t\t\t\t}\n\n\t\t\t\t// Resolve when we played the last frame; otherwise, reject\n\t\t\t\tif ( gotoEnd ) {\n\t\t\t\t\tdeferred.notifyWith( elem, [ animation, 1, 0 ] );\n\t\t\t\t\tdeferred.resolveWith( elem, [ animation, gotoEnd ] );\n\t\t\t\t} else {\n\t\t\t\t\tdeferred.rejectWith( elem, [ animation, gotoEnd ] );\n\t\t\t\t}\n\t\t\t\treturn this;\n\t\t\t}\n\t\t} ),\n\t\tprops = animation.props;\n\n\tpropFilter( props, animation.opts.specialEasing );\n\n\tfor ( ; index < length; index++ ) {\n\t\tresult = Animation.prefilters[ index ].call( animation, elem, props, animation.opts );\n\t\tif ( result ) {\n\t\t\tif ( isFunction( result.stop ) ) {\n\t\t\t\tjQuery._queueHooks( animation.elem, animation.opts.queue ).stop =\n\t\t\t\t\tresult.stop.bind( result );\n\t\t\t}\n\t\t\treturn result;\n\t\t}\n\t}\n\n\tjQuery.map( props, createTween, animation );\n\n\tif ( isFunction( animation.opts.start ) ) {\n\t\tanimation.opts.start.call( elem, animation );\n\t}\n\n\t// Attach callbacks from options\n\tanimation\n\t\t.progress( animation.opts.progress )\n\t\t.done( animation.opts.done, animation.opts.complete )\n\t\t.fail( animation.opts.fail )\n\t\t.always( animation.opts.always );\n\n\tjQuery.fx.timer(\n\t\tjQuery.extend( tick, {\n\t\t\telem: elem,\n\t\t\tanim: animation,\n\t\t\tqueue: animation.opts.queue\n\t\t} )\n\t);\n\n\treturn animation;\n}\n\njQuery.Animation = jQuery.extend( Animation, {\n\n\ttweeners: {\n\t\t"*": [ function( prop, value ) {\n\t\t\tvar tween = this.createTween( prop, value );\n\t\t\tadjustCSS( tween.elem, prop, rcssNum.exec( value ), tween );\n\t\t\treturn tween;\n\t\t} ]\n\t},\n\n\ttweener: function( props, callback ) {\n\t\tif ( isFunction( props ) ) {\n\t\t\tcallback = props;\n\t\t\tprops = [ "*" ];\n\t\t} else {\n\t\t\tprops = props.match( rnothtmlwhite );\n\t\t}\n\n\t\tvar prop,\n\t\t\tindex = 0,\n\t\t\tlength = props.length;\n\n\t\tfor ( ; index < length; index++ ) {\n\t\t\tprop = props[ index ];\n\t\t\tAnimation.tweeners[ prop ] = Animation.tweeners[ prop ] || [];\n\t\t\tAnimation.tweeners[ prop ].unshift( callback );\n\t\t}\n\t},\n\n\tprefilters: [ defaultPrefilter ],\n\n\tprefilter: function( callback, prepend ) {\n\t\tif ( prepend ) {\n\t\t\tAnimation.prefilters.unshift( callback );\n\t\t} else {\n\t\t\tAnimation.prefilters.push( callback );\n\t\t}\n\t}\n} );\n\njQuery.speed = function( speed, easing, fn ) {\n\tvar opt = speed && typeof speed === "object" ? jQuery.extend( {}, speed ) : {\n\t\tcomplete: fn || !fn && easing ||\n\t\t\tisFunction( speed ) && speed,\n\t\tduration: speed,\n\t\teasing: fn && easing || easing && !isFunction( easing ) && easing\n\t};\n\n\t// Go to the end state if fx are off\n\tif ( jQuery.fx.off ) {\n\t\topt.duration = 0;\n\n\t} else {\n\t\tif ( typeof opt.duration !== "number" ) {\n\t\t\tif ( opt.duration in jQuery.fx.speeds ) {\n\t\t\t\topt.duration = jQuery.fx.speeds[ opt.duration ];\n\n\t\t\t} else {\n\t\t\t\topt.duration = jQuery.fx.speeds._default;\n\t\t\t}\n\t\t}\n\t}\n\n\t// Normalize opt.queue - true/undefined/null -> "fx"\n\tif ( opt.queue == null || opt.queue === true ) {\n\t\topt.queue = "fx";\n\t}\n\n\t// Queueing\n\topt.old = opt.complete;\n\n\topt.complete = function() {\n\t\tif ( isFunction( opt.old ) ) {\n\t\t\topt.old.call( this );\n\t\t}\n\n\t\tif ( opt.queue ) {\n\t\t\tjQuery.dequeue( this, opt.queue );\n\t\t}\n\t};\n\n\treturn opt;\n};\n\njQuery.fn.extend( {\n\tfadeTo: function( speed, to, easing, callback ) {\n\n\t\t// Show any hidden elements after setting opacity to 0\n\t\treturn this.filter( isHiddenWithinTree ).css( "opacity", 0 ).show()\n\n\t\t\t// Animate to the value specified\n\t\t\t.end().animate( { opacity: to }, speed, easing, callback );\n\t},\n\tanimate: function( prop, speed, easing, callback ) {\n\t\tvar empty = jQuery.isEmptyObject( prop ),\n\t\t\toptall = jQuery.speed( speed, easing, callback ),\n\t\t\tdoAnimation = function() {\n\n\t\t\t\t// Operate on a copy of prop so per-property easing won\'t be lost\n\t\t\t\tvar anim = Animation( this, jQuery.extend( {}, prop ), optall );\n\n\t\t\t\t// Empty animations, or finishing resolves immediately\n\t\t\t\tif ( empty || dataPriv.get( this, "finish" ) ) {\n\t\t\t\t\tanim.stop( true );\n\t\t\t\t}\n\t\t\t};\n\t\t\tdoAnimation.finish = doAnimation;\n\n\t\treturn empty || optall.queue === false ?\n\t\t\tthis.each( doAnimation ) :\n\t\t\tthis.queue( optall.queue, doAnimation );\n\t},\n\tstop: function( type, clearQueue, gotoEnd ) {\n\t\tvar stopQueue = function( hooks ) {\n\t\t\tvar stop = hooks.stop;\n\t\t\tdelete hooks.stop;\n\t\t\tstop( gotoEnd );\n\t\t};\n\n\t\tif ( typeof type !== "string" ) {\n\t\t\tgotoEnd = clearQueue;\n\t\t\tclearQueue = type;\n\t\t\ttype = undefined;\n\t\t}\n\t\tif ( clearQueue && type !== false ) {\n\t\t\tthis.queue( type || "fx", [] );\n\t\t}\n\n\t\treturn this.each( function() {\n\t\t\tvar dequeue = true,\n\t\t\t\tindex = type != null && type + "queueHooks",\n\t\t\t\ttimers = jQuery.timers,\n\t\t\t\tdata = dataPriv.get( this );\n\n\t\t\tif ( index ) {\n\t\t\t\tif ( data[ index ] && data[ index ].stop ) {\n\t\t\t\t\tstopQueue( data[ index ] );\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\tfor ( index in data ) {\n\t\t\t\t\tif ( data[ index ] && data[ index ].stop && rrun.test( index ) ) {\n\t\t\t\t\t\tstopQueue( data[ index ] );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\tfor ( index = timers.length; index--; ) {\n\t\t\t\tif ( timers[ index ].elem === this &&\n\t\t\t\t\t( type == null || timers[ index ].queue === type ) ) {\n\n\t\t\t\t\ttimers[ index ].anim.stop( gotoEnd );\n\t\t\t\t\tdequeue = false;\n\t\t\t\t\ttimers.splice( index, 1 );\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Start the next in the queue if the last step wasn\'t forced.\n\t\t\t// Timers currently will call their complete callbacks, which\n\t\t\t// will dequeue but only if they were gotoEnd.\n\t\t\tif ( dequeue || !gotoEnd ) {\n\t\t\t\tjQuery.dequeue( this, type );\n\t\t\t}\n\t\t} );\n\t},\n\tfinish: function( type ) {\n\t\tif ( type !== false ) {\n\t\t\ttype = type || "fx";\n\t\t}\n\t\treturn this.each( function() {\n\t\t\tvar index,\n\t\t\t\tdata = dataPriv.get( this ),\n\t\t\t\tqueue = data[ type + "queue" ],\n\t\t\t\thooks = data[ type + "queueHooks" ],\n\t\t\t\ttimers = jQuery.timers,\n\t\t\t\tlength = queue ? queue.length : 0;\n\n\t\t\t// Enable finishing flag on private data\n\t\t\tdata.finish = true;\n\n\t\t\t// Empty the queue first\n\t\t\tjQuery.queue( this, type, [] );\n\n\t\t\tif ( hooks && hooks.stop ) {\n\t\t\t\thooks.stop.call( this, true );\n\t\t\t}\n\n\t\t\t// Look for any active animations, and finish them\n\t\t\tfor ( index = timers.length; index--; ) {\n\t\t\t\tif ( timers[ index ].elem === this && timers[ index ].queue === type ) {\n\t\t\t\t\ttimers[ index ].anim.stop( true );\n\t\t\t\t\ttimers.splice( index, 1 );\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Look for any animations in the old queue and finish them\n\t\t\tfor ( index = 0; index < length; index++ ) {\n\t\t\t\tif ( queue[ index ] && queue[ index ].finish ) {\n\t\t\t\t\tqueue[ index ].finish.call( this );\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Turn off finishing flag\n\t\t\tdelete data.finish;\n\t\t} );\n\t}\n} );\n\njQuery.each( [ "toggle", "show", "hide" ], function( i, name ) {\n\tvar cssFn = jQuery.fn[ name ];\n\tjQuery.fn[ name ] = function( speed, easing, callback ) {\n\t\treturn speed == null || typeof speed === "boolean" ?\n\t\t\tcssFn.apply( this, arguments ) :\n\t\t\tthis.animate( genFx( name, true ), speed, easing, callback );\n\t};\n} );\n\n// Generate shortcuts for custom animations\njQuery.each( {\n\tslideDown: genFx( "show" ),\n\tslideUp: genFx( "hide" ),\n\tslideToggle: genFx( "toggle" ),\n\tfadeIn: { opacity: "show" },\n\tfadeOut: { opacity: "hide" },\n\tfadeToggle: { opacity: "toggle" }\n}, function( name, props ) {\n\tjQuery.fn[ name ] = function( speed, easing, callback ) {\n\t\treturn this.animate( props, speed, easing, callback );\n\t};\n} );\n\njQuery.timers = [];\njQuery.fx.tick = function() {\n\tvar timer,\n\t\ti = 0,\n\t\ttimers = jQuery.timers;\n\n\tfxNow = Date.now();\n\n\tfor ( ; i < timers.length; i++ ) {\n\t\ttimer = timers[ i ];\n\n\t\t// Run the timer and safely remove it when done (allowing for external removal)\n\t\tif ( !timer() && timers[ i ] === timer ) {\n\t\t\ttimers.splice( i--, 1 );\n\t\t}\n\t}\n\n\tif ( !timers.length ) {\n\t\tjQuery.fx.stop();\n\t}\n\tfxNow = undefined;\n};\n\njQuery.fx.timer = function( timer ) {\n\tjQuery.timers.push( timer );\n\tjQuery.fx.start();\n};\n\njQuery.fx.interval = 13;\njQuery.fx.start = function() {\n\tif ( inProgress ) {\n\t\treturn;\n\t}\n\n\tinProgress = true;\n\tschedule();\n};\n\njQuery.fx.stop = function() {\n\tinProgress = null;\n};\n\njQuery.fx.speeds = {\n\tslow: 600,\n\tfast: 200,\n\n\t// Default speed\n\t_default: 400\n};\n\n\n// Based off of the plugin by Clint Helfers, with permission.\n// https://web.archive.org/web/20100324014747/http://blindsignals.com/index.php/2009/07/jquery-delay/\njQuery.fn.delay = function( time, type ) {\n\ttime = jQuery.fx ? jQuery.fx.speeds[ time ] || time : time;\n\ttype = type || "fx";\n\n\treturn this.queue( type, function( next, hooks ) {\n\t\tvar timeout = window.setTimeout( next, time );\n\t\thooks.stop = function() {\n\t\t\twindow.clearTimeout( timeout );\n\t\t};\n\t} );\n};\n\n\n( function() {\n\tvar input = document.createElement( "input" ),\n\t\tselect = document.createElement( "select" ),\n\t\topt = select.appendChild( document.createElement( "option" ) );\n\n\tinput.type = "checkbox";\n\n\t// Support: Android <=4.3 only\n\t// Default value for a checkbox should be "on"\n\tsupport.checkOn = input.value !== "";\n\n\t// Support: IE <=11 only\n\t// Must access selectedIndex to make default options select\n\tsupport.optSelected = opt.selected;\n\n\t// Support: IE <=11 only\n\t// An input loses its value after becoming a radio\n\tinput = document.createElement( "input" );\n\tinput.value = "t";\n\tinput.type = "radio";\n\tsupport.radioValue = input.value === "t";\n} )();\n\n\nvar boolHook,\n\tattrHandle = jQuery.expr.attrHandle;\n\njQuery.fn.extend( {\n\tattr: function( name, value ) {\n\t\treturn access( this, jQuery.attr, name, value, arguments.length > 1 );\n\t},\n\n\tremoveAttr: function( name ) {\n\t\treturn this.each( function() {\n\t\t\tjQuery.removeAttr( this, name );\n\t\t} );\n\t}\n} );\n\njQuery.extend( {\n\tattr: function( elem, name, value ) {\n\t\tvar ret, hooks,\n\t\t\tnType = elem.nodeType;\n\n\t\t// Don\'t get/set attributes on text, comment and attribute nodes\n\t\tif ( nType === 3 || nType === 8 || nType === 2 ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Fallback to prop when attributes are not supported\n\t\tif ( typeof elem.getAttribute === "undefined" ) {\n\t\t\treturn jQuery.prop( elem, name, value );\n\t\t}\n\n\t\t// Attribute hooks are determined by the lowercase version\n\t\t// Grab necessary hook if one is defined\n\t\tif ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {\n\t\t\thooks = jQuery.attrHooks[ name.toLowerCase() ] ||\n\t\t\t\t( jQuery.expr.match.bool.test( name ) ? boolHook : undefined );\n\t\t}\n\n\t\tif ( value !== undefined ) {\n\t\t\tif ( value === null ) {\n\t\t\t\tjQuery.removeAttr( elem, name );\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\tif ( hooks && "set" in hooks &&\n\t\t\t\t( ret = hooks.set( elem, value, name ) ) !== undefined ) {\n\t\t\t\treturn ret;\n\t\t\t}\n\n\t\t\telem.setAttribute( name, value + "" );\n\t\t\treturn value;\n\t\t}\n\n\t\tif ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {\n\t\t\treturn ret;\n\t\t}\n\n\t\tret = jQuery.find.attr( elem, name );\n\n\t\t// Non-existent attributes return null, we normalize to undefined\n\t\treturn ret == null ? undefined : ret;\n\t},\n\n\tattrHooks: {\n\t\ttype: {\n\t\t\tset: function( elem, value ) {\n\t\t\t\tif ( !support.radioValue && value === "radio" &&\n\t\t\t\t\tnodeName( elem, "input" ) ) {\n\t\t\t\t\tvar val = elem.value;\n\t\t\t\t\telem.setAttribute( "type", value );\n\t\t\t\t\tif ( val ) {\n\t\t\t\t\t\telem.value = val;\n\t\t\t\t\t}\n\t\t\t\t\treturn value;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t},\n\n\tremoveAttr: function( elem, value ) {\n\t\tvar name,\n\t\t\ti = 0,\n\n\t\t\t// Attribute names can contain non-HTML whitespace characters\n\t\t\t// https://html.spec.whatwg.org/multipage/syntax.html#attributes-2\n\t\t\tattrNames = value && value.match( rnothtmlwhite );\n\n\t\tif ( attrNames && elem.nodeType === 1 ) {\n\t\t\twhile ( ( name = attrNames[ i++ ] ) ) {\n\t\t\t\telem.removeAttribute( name );\n\t\t\t}\n\t\t}\n\t}\n} );\n\n// Hooks for boolean attributes\nboolHook = {\n\tset: function( elem, value, name ) {\n\t\tif ( value === false ) {\n\n\t\t\t// Remove boolean attributes when set to false\n\t\t\tjQuery.removeAttr( elem, name );\n\t\t} else {\n\t\t\telem.setAttribute( name, name );\n\t\t}\n\t\treturn name;\n\t}\n};\n\njQuery.each( jQuery.expr.match.bool.source.match( /\\w+/g ), function( i, name ) {\n\tvar getter = attrHandle[ name ] || jQuery.find.attr;\n\n\tattrHandle[ name ] = function( elem, name, isXML ) {\n\t\tvar ret, handle,\n\t\t\tlowercaseName = name.toLowerCase();\n\n\t\tif ( !isXML ) {\n\n\t\t\t// Avoid an infinite loop by temporarily removing this function from the getter\n\t\t\thandle = attrHandle[ lowercaseName ];\n\t\t\tattrHandle[ lowercaseName ] = ret;\n\t\t\tret = getter( elem, name, isXML ) != null ?\n\t\t\t\tlowercaseName :\n\t\t\t\tnull;\n\t\t\tattrHandle[ lowercaseName ] = handle;\n\t\t}\n\t\treturn ret;\n\t};\n} );\n\n\n\n\nvar rfocusable = /^(?:input|select|textarea|button)$/i,\n\trclickable = /^(?:a|area)$/i;\n\njQuery.fn.extend( {\n\tprop: function( name, value ) {\n\t\treturn access( this, jQuery.prop, name, value, arguments.length > 1 );\n\t},\n\n\tremoveProp: function( name ) {\n\t\treturn this.each( function() {\n\t\t\tdelete this[ jQuery.propFix[ name ] || name ];\n\t\t} );\n\t}\n} );\n\njQuery.extend( {\n\tprop: function( elem, name, value ) {\n\t\tvar ret, hooks,\n\t\t\tnType = elem.nodeType;\n\n\t\t// Don\'t get/set properties on text, comment and attribute nodes\n\t\tif ( nType === 3 || nType === 8 || nType === 2 ) {\n\t\t\treturn;\n\t\t}\n\n\t\tif ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {\n\n\t\t\t// Fix name and attach hooks\n\t\t\tname = jQuery.propFix[ name ] || name;\n\t\t\thooks = jQuery.propHooks[ name ];\n\t\t}\n\n\t\tif ( value !== undefined ) {\n\t\t\tif ( hooks && "set" in hooks &&\n\t\t\t\t( ret = hooks.set( elem, value, name ) ) !== undefined ) {\n\t\t\t\treturn ret;\n\t\t\t}\n\n\t\t\treturn ( elem[ name ] = value );\n\t\t}\n\n\t\tif ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {\n\t\t\treturn ret;\n\t\t}\n\n\t\treturn elem[ name ];\n\t},\n\n\tpropHooks: {\n\t\ttabIndex: {\n\t\t\tget: function( elem ) {\n\n\t\t\t\t// Support: IE <=9 - 11 only\n\t\t\t\t// elem.tabIndex doesn\'t always return the\n\t\t\t\t// correct value when it hasn\'t been explicitly set\n\t\t\t\t// https://web.archive.org/web/20141116233347/http://fluidproject.org/blog/2008/01/09/getting-setting-and-removing-tabindex-values-with-javascript/\n\t\t\t\t// Use proper attribute retrieval(#12072)\n\t\t\t\tvar tabindex = jQuery.find.attr( elem, "tabindex" );\n\n\t\t\t\tif ( tabindex ) {\n\t\t\t\t\treturn parseInt( tabindex, 10 );\n\t\t\t\t}\n\n\t\t\t\tif (\n\t\t\t\t\trfocusable.test( elem.nodeName ) ||\n\t\t\t\t\trclickable.test( elem.nodeName ) &&\n\t\t\t\t\telem.href\n\t\t\t\t) {\n\t\t\t\t\treturn 0;\n\t\t\t\t}\n\n\t\t\t\treturn -1;\n\t\t\t}\n\t\t}\n\t},\n\n\tpropFix: {\n\t\t"for": "htmlFor",\n\t\t"class": "className"\n\t}\n} );\n\n// Support: IE <=11 only\n// Accessing the selectedIndex property\n// forces the browser to respect setting selected\n// on the option\n// The getter ensures a default option is selected\n// when in an optgroup\n// eslint rule "no-unused-expressions" is disabled for this code\n// since it considers such accessions noop\nif ( !support.optSelected ) {\n\tjQuery.propHooks.selected = {\n\t\tget: function( elem ) {\n\n\t\t\t/* eslint no-unused-expressions: "off" */\n\n\t\t\tvar parent = elem.parentNode;\n\t\t\tif ( parent && parent.parentNode ) {\n\t\t\t\tparent.parentNode.selectedIndex;\n\t\t\t}\n\t\t\treturn null;\n\t\t},\n\t\tset: function( elem ) {\n\n\t\t\t/* eslint no-unused-expressions: "off" */\n\n\t\t\tvar parent = elem.parentNode;\n\t\t\tif ( parent ) {\n\t\t\t\tparent.selectedIndex;\n\n\t\t\t\tif ( parent.parentNode ) {\n\t\t\t\t\tparent.parentNode.selectedIndex;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t};\n}\n\njQuery.each( [\n\t"tabIndex",\n\t"readOnly",\n\t"maxLength",\n\t"cellSpacing",\n\t"cellPadding",\n\t"rowSpan",\n\t"colSpan",\n\t"useMap",\n\t"frameBorder",\n\t"contentEditable"\n], function() {\n\tjQuery.propFix[ this.toLowerCase() ] = this;\n} );\n\n\n\n\n\t// Strip and collapse whitespace according to HTML spec\n\t// https://infra.spec.whatwg.org/#strip-and-collapse-ascii-whitespace\n\tfunction stripAndCollapse( value ) {\n\t\tvar tokens = value.match( rnothtmlwhite ) || [];\n\t\treturn tokens.join( " " );\n\t}\n\n\nfunction getClass( elem ) {\n\treturn elem.getAttribute && elem.getAttribute( "class" ) || "";\n}\n\nfunction classesToArray( value ) {\n\tif ( Array.isArray( value ) ) {\n\t\treturn value;\n\t}\n\tif ( typeof value === "string" ) {\n\t\treturn value.match( rnothtmlwhite ) || [];\n\t}\n\treturn [];\n}\n\njQuery.fn.extend( {\n\taddClass: function( value ) {\n\t\tvar classes, elem, cur, curValue, clazz, j, finalValue,\n\t\t\ti = 0;\n\n\t\tif ( isFunction( value ) ) {\n\t\t\treturn this.each( function( j ) {\n\t\t\t\tjQuery( this ).addClass( value.call( this, j, getClass( this ) ) );\n\t\t\t} );\n\t\t}\n\n\t\tclasses = classesToArray( value );\n\n\t\tif ( classes.length ) {\n\t\t\twhile ( ( elem = this[ i++ ] ) ) {\n\t\t\t\tcurValue = getClass( elem );\n\t\t\t\tcur = elem.nodeType === 1 && ( " " + stripAndCollapse( curValue ) + " " );\n\n\t\t\t\tif ( cur ) {\n\t\t\t\t\tj = 0;\n\t\t\t\t\twhile ( ( clazz = classes[ j++ ] ) ) {\n\t\t\t\t\t\tif ( cur.indexOf( " " + clazz + " " ) < 0 ) {\n\t\t\t\t\t\t\tcur += clazz + " ";\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\t// Only assign if different to avoid unneeded rendering.\n\t\t\t\t\tfinalValue = stripAndCollapse( cur );\n\t\t\t\t\tif ( curValue !== finalValue ) {\n\t\t\t\t\t\telem.setAttribute( "class", finalValue );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn this;\n\t},\n\n\tremoveClass: function( value ) {\n\t\tvar classes, elem, cur, curValue, clazz, j, finalValue,\n\t\t\ti = 0;\n\n\t\tif ( isFunction( value ) ) {\n\t\t\treturn this.each( function( j ) {\n\t\t\t\tjQuery( this ).removeClass( value.call( this, j, getClass( this ) ) );\n\t\t\t} );\n\t\t}\n\n\t\tif ( !arguments.length ) {\n\t\t\treturn this.attr( "class", "" );\n\t\t}\n\n\t\tclasses = classesToArray( value );\n\n\t\tif ( classes.length ) {\n\t\t\twhile ( ( elem = this[ i++ ] ) ) {\n\t\t\t\tcurValue = getClass( elem );\n\n\t\t\t\t// This expression is here for better compressibility (see addClass)\n\t\t\t\tcur = elem.nodeType === 1 && ( " " + stripAndCollapse( curValue ) + " " );\n\n\t\t\t\tif ( cur ) {\n\t\t\t\t\tj = 0;\n\t\t\t\t\twhile ( ( clazz = classes[ j++ ] ) ) {\n\n\t\t\t\t\t\t// Remove *all* instances\n\t\t\t\t\t\twhile ( cur.indexOf( " " + clazz + " " ) > -1 ) {\n\t\t\t\t\t\t\tcur = cur.replace( " " + clazz + " ", " " );\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\t// Only assign if different to avoid unneeded rendering.\n\t\t\t\t\tfinalValue = stripAndCollapse( cur );\n\t\t\t\t\tif ( curValue !== finalValue ) {\n\t\t\t\t\t\telem.setAttribute( "class", finalValue );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn this;\n\t},\n\n\ttoggleClass: function( value, stateVal ) {\n\t\tvar type = typeof value,\n\t\t\tisValidValue = type === "string" || Array.isArray( value );\n\n\t\tif ( typeof stateVal === "boolean" && isValidValue ) {\n\t\t\treturn stateVal ? this.addClass( value ) : this.removeClass( value );\n\t\t}\n\n\t\tif ( isFunction( value ) ) {\n\t\t\treturn this.each( function( i ) {\n\t\t\t\tjQuery( this ).toggleClass(\n\t\t\t\t\tvalue.call( this, i, getClass( this ), stateVal ),\n\t\t\t\t\tstateVal\n\t\t\t\t);\n\t\t\t} );\n\t\t}\n\n\t\treturn this.each( function() {\n\t\t\tvar className, i, self, classNames;\n\n\t\t\tif ( isValidValue ) {\n\n\t\t\t\t// Toggle individual class names\n\t\t\t\ti = 0;\n\t\t\t\tself = jQuery( this );\n\t\t\t\tclassNames = classesToArray( value );\n\n\t\t\t\twhile ( ( className = classNames[ i++ ] ) ) {\n\n\t\t\t\t\t// Check each className given, space separated list\n\t\t\t\t\tif ( self.hasClass( className ) ) {\n\t\t\t\t\t\tself.removeClass( className );\n\t\t\t\t\t} else {\n\t\t\t\t\t\tself.addClass( className );\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t// Toggle whole class name\n\t\t\t} else if ( value === undefined || type === "boolean" ) {\n\t\t\t\tclassName = getClass( this );\n\t\t\t\tif ( className ) {\n\n\t\t\t\t\t// Store className if set\n\t\t\t\t\tdataPriv.set( this, "__className__", className );\n\t\t\t\t}\n\n\t\t\t\t// If the element has a class name or if we\'re passed `false`,\n\t\t\t\t// then remove the whole classname (if there was one, the above saved it).\n\t\t\t\t// Otherwise bring back whatever was previously saved (if anything),\n\t\t\t\t// falling back to the empty string if nothing was stored.\n\t\t\t\tif ( this.setAttribute ) {\n\t\t\t\t\tthis.setAttribute( "class",\n\t\t\t\t\t\tclassName || value === false ?\n\t\t\t\t\t\t"" :\n\t\t\t\t\t\tdataPriv.get( this, "__className__" ) || ""\n\t\t\t\t\t);\n\t\t\t\t}\n\t\t\t}\n\t\t} );\n\t},\n\n\thasClass: function( selector ) {\n\t\tvar className, elem,\n\t\t\ti = 0;\n\n\t\tclassName = " " + selector + " ";\n\t\twhile ( ( elem = this[ i++ ] ) ) {\n\t\t\tif ( elem.nodeType === 1 &&\n\t\t\t\t( " " + stripAndCollapse( getClass( elem ) ) + " " ).indexOf( className ) > -1 ) {\n\t\t\t\t\treturn true;\n\t\t\t}\n\t\t}\n\n\t\treturn false;\n\t}\n} );\n\n\n\n\nvar rreturn = /\\r/g;\n\njQuery.fn.extend( {\n\tval: function( value ) {\n\t\tvar hooks, ret, valueIsFunction,\n\t\t\telem = this[ 0 ];\n\n\t\tif ( !arguments.length ) {\n\t\t\tif ( elem ) {\n\t\t\t\thooks = jQuery.valHooks[ elem.type ] ||\n\t\t\t\t\tjQuery.valHooks[ elem.nodeName.toLowerCase() ];\n\n\t\t\t\tif ( hooks &&\n\t\t\t\t\t"get" in hooks &&\n\t\t\t\t\t( ret = hooks.get( elem, "value" ) ) !== undefined\n\t\t\t\t) {\n\t\t\t\t\treturn ret;\n\t\t\t\t}\n\n\t\t\t\tret = elem.value;\n\n\t\t\t\t// Handle most common string cases\n\t\t\t\tif ( typeof ret === "string" ) {\n\t\t\t\t\treturn ret.replace( rreturn, "" );\n\t\t\t\t}\n\n\t\t\t\t// Handle cases where value is null/undef or number\n\t\t\t\treturn ret == null ? "" : ret;\n\t\t\t}\n\n\t\t\treturn;\n\t\t}\n\n\t\tvalueIsFunction = isFunction( value );\n\n\t\treturn this.each( function( i ) {\n\t\t\tvar val;\n\n\t\t\tif ( this.nodeType !== 1 ) {\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\tif ( valueIsFunction ) {\n\t\t\t\tval = value.call( this, i, jQuery( this ).val() );\n\t\t\t} else {\n\t\t\t\tval = value;\n\t\t\t}\n\n\t\t\t// Treat null/undefined as ""; convert numbers to string\n\t\t\tif ( val == null ) {\n\t\t\t\tval = "";\n\n\t\t\t} else if ( typeof val === "number" ) {\n\t\t\t\tval += "";\n\n\t\t\t} else if ( Array.isArray( val ) ) {\n\t\t\t\tval = jQuery.map( val, function( value ) {\n\t\t\t\t\treturn value == null ? "" : value + "";\n\t\t\t\t} );\n\t\t\t}\n\n\t\t\thooks = jQuery.valHooks[ this.type ] || jQuery.valHooks[ this.nodeName.toLowerCase() ];\n\n\t\t\t// If set returns undefined, fall back to normal setting\n\t\t\tif ( !hooks || !( "set" in hooks ) || hooks.set( this, val, "value" ) === undefined ) {\n\t\t\t\tthis.value = val;\n\t\t\t}\n\t\t} );\n\t}\n} );\n\njQuery.extend( {\n\tvalHooks: {\n\t\toption: {\n\t\t\tget: function( elem ) {\n\n\t\t\t\tvar val = jQuery.find.attr( elem, "value" );\n\t\t\t\treturn val != null ?\n\t\t\t\t\tval :\n\n\t\t\t\t\t// Support: IE <=10 - 11 only\n\t\t\t\t\t// option.text throws exceptions (#14686, #14858)\n\t\t\t\t\t// Strip and collapse whitespace\n\t\t\t\t\t// https://html.spec.whatwg.org/#strip-and-collapse-whitespace\n\t\t\t\t\tstripAndCollapse( jQuery.text( elem ) );\n\t\t\t}\n\t\t},\n\t\tselect: {\n\t\t\tget: function( elem ) {\n\t\t\t\tvar value, option, i,\n\t\t\t\t\toptions = elem.options,\n\t\t\t\t\tindex = elem.selectedIndex,\n\t\t\t\t\tone = elem.type === "select-one",\n\t\t\t\t\tvalues = one ? null : [],\n\t\t\t\t\tmax = one ? index + 1 : options.length;\n\n\t\t\t\tif ( index < 0 ) {\n\t\t\t\t\ti = max;\n\n\t\t\t\t} else {\n\t\t\t\t\ti = one ? index : 0;\n\t\t\t\t}\n\n\t\t\t\t// Loop through all the selected options\n\t\t\t\tfor ( ; i < max; i++ ) {\n\t\t\t\t\toption = options[ i ];\n\n\t\t\t\t\t// Support: IE <=9 only\n\t\t\t\t\t// IE8-9 doesn\'t update selected after form reset (#2551)\n\t\t\t\t\tif ( ( option.selected || i === index ) &&\n\n\t\t\t\t\t\t\t// Don\'t return options that are disabled or in a disabled optgroup\n\t\t\t\t\t\t\t!option.disabled &&\n\t\t\t\t\t\t\t( !option.parentNode.disabled ||\n\t\t\t\t\t\t\t\t!nodeName( option.parentNode, "optgroup" ) ) ) {\n\n\t\t\t\t\t\t// Get the specific value for the option\n\t\t\t\t\t\tvalue = jQuery( option ).val();\n\n\t\t\t\t\t\t// We don\'t need an array for one selects\n\t\t\t\t\t\tif ( one ) {\n\t\t\t\t\t\t\treturn value;\n\t\t\t\t\t\t}\n\n\t\t\t\t\t\t// Multi-Selects return an array\n\t\t\t\t\t\tvalues.push( value );\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\treturn values;\n\t\t\t},\n\n\t\t\tset: function( elem, value ) {\n\t\t\t\tvar optionSet, option,\n\t\t\t\t\toptions = elem.options,\n\t\t\t\t\tvalues = jQuery.makeArray( value ),\n\t\t\t\t\ti = options.length;\n\n\t\t\t\twhile ( i-- ) {\n\t\t\t\t\toption = options[ i ];\n\n\t\t\t\t\t/* eslint-disable no-cond-assign */\n\n\t\t\t\t\tif ( option.selected =\n\t\t\t\t\t\tjQuery.inArray( jQuery.valHooks.option.get( option ), values ) > -1\n\t\t\t\t\t) {\n\t\t\t\t\t\toptionSet = true;\n\t\t\t\t\t}\n\n\t\t\t\t\t/* eslint-enable no-cond-assign */\n\t\t\t\t}\n\n\t\t\t\t// Force browsers to behave consistently when non-matching value is set\n\t\t\t\tif ( !optionSet ) {\n\t\t\t\t\telem.selectedIndex = -1;\n\t\t\t\t}\n\t\t\t\treturn values;\n\t\t\t}\n\t\t}\n\t}\n} );\n\n// Radios and checkboxes getter/setter\njQuery.each( [ "radio", "checkbox" ], function() {\n\tjQuery.valHooks[ this ] = {\n\t\tset: function( elem, value ) {\n\t\t\tif ( Array.isArray( value ) ) {\n\t\t\t\treturn ( elem.checked = jQuery.inArray( jQuery( elem ).val(), value ) > -1 );\n\t\t\t}\n\t\t}\n\t};\n\tif ( !support.checkOn ) {\n\t\tjQuery.valHooks[ this ].get = function( elem ) {\n\t\t\treturn elem.getAttribute( "value" ) === null ? "on" : elem.value;\n\t\t};\n\t}\n} );\n\n\n\n\n// Return jQuery for attributes-only inclusion\n\n\nsupport.focusin = "onfocusin" in window;\n\n\nvar rfocusMorph = /^(?:focusinfocus|focusoutblur)$/,\n\tstopPropagationCallback = function( e ) {\n\t\te.stopPropagation();\n\t};\n\njQuery.extend( jQuery.event, {\n\n\ttrigger: function( event, data, elem, onlyHandlers ) {\n\n\t\tvar i, cur, tmp, bubbleType, ontype, handle, special, lastElement,\n\t\t\teventPath = [ elem || document ],\n\t\t\ttype = hasOwn.call( event, "type" ) ? event.type : event,\n\t\t\tnamespaces = hasOwn.call( event, "namespace" ) ? event.namespace.split( "." ) : [];\n\n\t\tcur = lastElement = tmp = elem = elem || document;\n\n\t\t// Don\'t do events on text and comment nodes\n\t\tif ( elem.nodeType === 3 || elem.nodeType === 8 ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// focus/blur morphs to focusin/out; ensure we\'re not firing them right now\n\t\tif ( rfocusMorph.test( type + jQuery.event.triggered ) ) {\n\t\t\treturn;\n\t\t}\n\n\t\tif ( type.indexOf( "." ) > -1 ) {\n\n\t\t\t// Namespaced trigger; create a regexp to match event type in handle()\n\t\t\tnamespaces = type.split( "." );\n\t\t\ttype = namespaces.shift();\n\t\t\tnamespaces.sort();\n\t\t}\n\t\tontype = type.indexOf( ":" ) < 0 && "on" + type;\n\n\t\t// Caller can pass in a jQuery.Event object, Object, or just an event type string\n\t\tevent = event[ jQuery.expando ] ?\n\t\t\tevent :\n\t\t\tnew jQuery.Event( type, typeof event === "object" && event );\n\n\t\t// Trigger bitmask: & 1 for native handlers; & 2 for jQuery (always true)\n\t\tevent.isTrigger = onlyHandlers ? 2 : 3;\n\t\tevent.namespace = namespaces.join( "." );\n\t\tevent.rnamespace = event.namespace ?\n\t\t\tnew RegExp( "(^|\\\\.)" + namespaces.join( "\\\\.(?:.*\\\\.|)" ) + "(\\\\.|$)" ) :\n\t\t\tnull;\n\n\t\t// Clean up the event in case it is being reused\n\t\tevent.result = undefined;\n\t\tif ( !event.target ) {\n\t\t\tevent.target = elem;\n\t\t}\n\n\t\t// Clone any incoming data and prepend the event, creating the handler arg list\n\t\tdata = data == null ?\n\t\t\t[ event ] :\n\t\t\tjQuery.makeArray( data, [ event ] );\n\n\t\t// Allow special events to draw outside the lines\n\t\tspecial = jQuery.event.special[ type ] || {};\n\t\tif ( !onlyHandlers && special.trigger && special.trigger.apply( elem, data ) === false ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Determine event propagation path in advance, per W3C events spec (#9951)\n\t\t// Bubble up to document, then to window; watch for a global ownerDocument var (#9724)\n\t\tif ( !onlyHandlers && !special.noBubble && !isWindow( elem ) ) {\n\n\t\t\tbubbleType = special.delegateType || type;\n\t\t\tif ( !rfocusMorph.test( bubbleType + type ) ) {\n\t\t\t\tcur = cur.parentNode;\n\t\t\t}\n\t\t\tfor ( ; cur; cur = cur.parentNode ) {\n\t\t\t\teventPath.push( cur );\n\t\t\t\ttmp = cur;\n\t\t\t}\n\n\t\t\t// Only add window if we got to document (e.g., not plain obj or detached DOM)\n\t\t\tif ( tmp === ( elem.ownerDocument || document ) ) {\n\t\t\t\teventPath.push( tmp.defaultView || tmp.parentWindow || window );\n\t\t\t}\n\t\t}\n\n\t\t// Fire handlers on the event path\n\t\ti = 0;\n\t\twhile ( ( cur = eventPath[ i++ ] ) && !event.isPropagationStopped() ) {\n\t\t\tlastElement = cur;\n\t\t\tevent.type = i > 1 ?\n\t\t\t\tbubbleType :\n\t\t\t\tspecial.bindType || type;\n\n\t\t\t// jQuery handler\n\t\t\thandle = ( dataPriv.get( cur, "events" ) || {} )[ event.type ] &&\n\t\t\t\tdataPriv.get( cur, "handle" );\n\t\t\tif ( handle ) {\n\t\t\t\thandle.apply( cur, data );\n\t\t\t}\n\n\t\t\t// Native handler\n\t\t\thandle = ontype && cur[ ontype ];\n\t\t\tif ( handle && handle.apply && acceptData( cur ) ) {\n\t\t\t\tevent.result = handle.apply( cur, data );\n\t\t\t\tif ( event.result === false ) {\n\t\t\t\t\tevent.preventDefault();\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t\tevent.type = type;\n\n\t\t// If nobody prevented the default action, do it now\n\t\tif ( !onlyHandlers && !event.isDefaultPrevented() ) {\n\n\t\t\tif ( ( !special._default ||\n\t\t\t\tspecial._default.apply( eventPath.pop(), data ) === false ) &&\n\t\t\t\tacceptData( elem ) ) {\n\n\t\t\t\t// Call a native DOM method on the target with the same name as the event.\n\t\t\t\t// Don\'t do default actions on window, that\'s where global variables be (#6170)\n\t\t\t\tif ( ontype && isFunction( elem[ type ] ) && !isWindow( elem ) ) {\n\n\t\t\t\t\t// Don\'t re-trigger an onFOO event when we call its FOO() method\n\t\t\t\t\ttmp = elem[ ontype ];\n\n\t\t\t\t\tif ( tmp ) {\n\t\t\t\t\t\telem[ ontype ] = null;\n\t\t\t\t\t}\n\n\t\t\t\t\t// Prevent re-triggering of the same event, since we already bubbled it above\n\t\t\t\t\tjQuery.event.triggered = type;\n\n\t\t\t\t\tif ( event.isPropagationStopped() ) {\n\t\t\t\t\t\tlastElement.addEventListener( type, stopPropagationCallback );\n\t\t\t\t\t}\n\n\t\t\t\t\telem[ type ]();\n\n\t\t\t\t\tif ( event.isPropagationStopped() ) {\n\t\t\t\t\t\tlastElement.removeEventListener( type, stopPropagationCallback );\n\t\t\t\t\t}\n\n\t\t\t\t\tjQuery.event.triggered = undefined;\n\n\t\t\t\t\tif ( tmp ) {\n\t\t\t\t\t\telem[ ontype ] = tmp;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn event.result;\n\t},\n\n\t// Piggyback on a donor event to simulate a different one\n\t// Used only for `focus(in | out)` events\n\tsimulate: function( type, elem, event ) {\n\t\tvar e = jQuery.extend(\n\t\t\tnew jQuery.Event(),\n\t\t\tevent,\n\t\t\t{\n\t\t\t\ttype: type,\n\t\t\t\tisSimulated: true\n\t\t\t}\n\t\t);\n\n\t\tjQuery.event.trigger( e, null, elem );\n\t}\n\n} );\n\njQuery.fn.extend( {\n\n\ttrigger: function( type, data ) {\n\t\treturn this.each( function() {\n\t\t\tjQuery.event.trigger( type, data, this );\n\t\t} );\n\t},\n\ttriggerHandler: function( type, data ) {\n\t\tvar elem = this[ 0 ];\n\t\tif ( elem ) {\n\t\t\treturn jQuery.event.trigger( type, data, elem, true );\n\t\t}\n\t}\n} );\n\n\n// Support: Firefox <=44\n// Firefox doesn\'t have focus(in | out) events\n// Related ticket - https://bugzilla.mozilla.org/show_bug.cgi?id=687787\n//\n// Support: Chrome <=48 - 49, Safari <=9.0 - 9.1\n// focus(in | out) events fire after focus & blur events,\n// which is spec violation - http://www.w3.org/TR/DOM-Level-3-Events/#events-focusevent-event-order\n// Related ticket - https://bugs.chromium.org/p/chromium/issues/detail?id=449857\nif ( !support.focusin ) {\n\tjQuery.each( { focus: "focusin", blur: "focusout" }, function( orig, fix ) {\n\n\t\t// Attach a single capturing handler on the document while someone wants focusin/focusout\n\t\tvar handler = function( event ) {\n\t\t\tjQuery.event.simulate( fix, event.target, jQuery.event.fix( event ) );\n\t\t};\n\n\t\tjQuery.event.special[ fix ] = {\n\t\t\tsetup: function() {\n\t\t\t\tvar doc = this.ownerDocument || this,\n\t\t\t\t\tattaches = dataPriv.access( doc, fix );\n\n\t\t\t\tif ( !attaches ) {\n\t\t\t\t\tdoc.addEventListener( orig, handler, true );\n\t\t\t\t}\n\t\t\t\tdataPriv.access( doc, fix, ( attaches || 0 ) + 1 );\n\t\t\t},\n\t\t\tteardown: function() {\n\t\t\t\tvar doc = this.ownerDocument || this,\n\t\t\t\t\tattaches = dataPriv.access( doc, fix ) - 1;\n\n\t\t\t\tif ( !attaches ) {\n\t\t\t\t\tdoc.removeEventListener( orig, handler, true );\n\t\t\t\t\tdataPriv.remove( doc, fix );\n\n\t\t\t\t} else {\n\t\t\t\t\tdataPriv.access( doc, fix, attaches );\n\t\t\t\t}\n\t\t\t}\n\t\t};\n\t} );\n}\nvar location = window.location;\n\nvar nonce = Date.now();\n\nvar rquery = ( /\\?/ );\n\n\n\n// Cross-browser xml parsing\njQuery.parseXML = function( data ) {\n\tvar xml;\n\tif ( !data || typeof data !== "string" ) {\n\t\treturn null;\n\t}\n\n\t// Support: IE 9 - 11 only\n\t// IE throws on parseFromString with invalid input.\n\ttry {\n\t\txml = ( new window.DOMParser() ).parseFromString( data, "text/xml" );\n\t} catch ( e ) {\n\t\txml = undefined;\n\t}\n\n\tif ( !xml || xml.getElementsByTagName( "parsererror" ).length ) {\n\t\tjQuery.error( "Invalid XML: " + data );\n\t}\n\treturn xml;\n};\n\n\nvar\n\trbracket = /\\[\\]$/,\n\trCRLF = /\\r?\\n/g,\n\trsubmitterTypes = /^(?:submit|button|image|reset|file)$/i,\n\trsubmittable = /^(?:input|select|textarea|keygen)/i;\n\nfunction buildParams( prefix, obj, traditional, add ) {\n\tvar name;\n\n\tif ( Array.isArray( obj ) ) {\n\n\t\t// Serialize array item.\n\t\tjQuery.each( obj, function( i, v ) {\n\t\t\tif ( traditional || rbracket.test( prefix ) ) {\n\n\t\t\t\t// Treat each array item as a scalar.\n\t\t\t\tadd( prefix, v );\n\n\t\t\t} else {\n\n\t\t\t\t// Item is non-scalar (array or object), encode its numeric index.\n\t\t\t\tbuildParams(\n\t\t\t\t\tprefix + "[" + ( typeof v === "object" && v != null ? i : "" ) + "]",\n\t\t\t\t\tv,\n\t\t\t\t\ttraditional,\n\t\t\t\t\tadd\n\t\t\t\t);\n\t\t\t}\n\t\t} );\n\n\t} else if ( !traditional && toType( obj ) === "object" ) {\n\n\t\t// Serialize object item.\n\t\tfor ( name in obj ) {\n\t\t\tbuildParams( prefix + "[" + name + "]", obj[ name ], traditional, add );\n\t\t}\n\n\t} else {\n\n\t\t// Serialize scalar item.\n\t\tadd( prefix, obj );\n\t}\n}\n\n// Serialize an array of form elements or a set of\n// key/values into a query string\njQuery.param = function( a, traditional ) {\n\tvar prefix,\n\t\ts = [],\n\t\tadd = function( key, valueOrFunction ) {\n\n\t\t\t// If value is a function, invoke it and use its return value\n\t\t\tvar value = isFunction( valueOrFunction ) ?\n\t\t\t\tvalueOrFunction() :\n\t\t\t\tvalueOrFunction;\n\n\t\t\ts[ s.length ] = encodeURIComponent( key ) + "=" +\n\t\t\t\tencodeURIComponent( value == null ? "" : value );\n\t\t};\n\n\t// If an array was passed in, assume that it is an array of form elements.\n\tif ( Array.isArray( a ) || ( a.jquery && !jQuery.isPlainObject( a ) ) ) {\n\n\t\t// Serialize the form elements\n\t\tjQuery.each( a, function() {\n\t\t\tadd( this.name, this.value );\n\t\t} );\n\n\t} else {\n\n\t\t// If traditional, encode the "old" way (the way 1.3.2 or older\n\t\t// did it), otherwise encode params recursively.\n\t\tfor ( prefix in a ) {\n\t\t\tbuildParams( prefix, a[ prefix ], traditional, add );\n\t\t}\n\t}\n\n\t// Return the resulting serialization\n\treturn s.join( "&" );\n};\n\njQuery.fn.extend( {\n\tserialize: function() {\n\t\treturn jQuery.param( this.serializeArray() );\n\t},\n\tserializeArray: function() {\n\t\treturn this.map( function() {\n\n\t\t\t// Can add propHook for "elements" to filter or add form elements\n\t\t\tvar elements = jQuery.prop( this, "elements" );\n\t\t\treturn elements ? jQuery.makeArray( elements ) : this;\n\t\t} )\n\t\t.filter( function() {\n\t\t\tvar type = this.type;\n\n\t\t\t// Use .is( ":disabled" ) so that fieldset[disabled] works\n\t\t\treturn this.name && !jQuery( this ).is( ":disabled" ) &&\n\t\t\t\trsubmittable.test( this.nodeName ) && !rsubmitterTypes.test( type ) &&\n\t\t\t\t( this.checked || !rcheckableType.test( type ) );\n\t\t} )\n\t\t.map( function( i, elem ) {\n\t\t\tvar val = jQuery( this ).val();\n\n\t\t\tif ( val == null ) {\n\t\t\t\treturn null;\n\t\t\t}\n\n\t\t\tif ( Array.isArray( val ) ) {\n\t\t\t\treturn jQuery.map( val, function( val ) {\n\t\t\t\t\treturn { name: elem.name, value: val.replace( rCRLF, "\\r\\n" ) };\n\t\t\t\t} );\n\t\t\t}\n\n\t\t\treturn { name: elem.name, value: val.replace( rCRLF, "\\r\\n" ) };\n\t\t} ).get();\n\t}\n} );\n\n\nvar\n\tr20 = /%20/g,\n\trhash = /#.*$/,\n\trantiCache = /([?&])_=[^&]*/,\n\trheaders = /^(.*?):[ \\t]*([^\\r\\n]*)$/mg,\n\n\t// #7653, #8125, #8152: local protocol detection\n\trlocalProtocol = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/,\n\trnoContent = /^(?:GET|HEAD)$/,\n\trprotocol = /^\\/\\//,\n\n\t/* Prefilters\n\t * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)\n\t * 2) These are called:\n\t *    - BEFORE asking for a transport\n\t *    - AFTER param serialization (s.data is a string if s.processData is true)\n\t * 3) key is the dataType\n\t * 4) the catchall symbol "*" can be used\n\t * 5) execution will start with transport dataType and THEN continue down to "*" if needed\n\t */\n\tprefilters = {},\n\n\t/* Transports bindings\n\t * 1) key is the dataType\n\t * 2) the catchall symbol "*" can be used\n\t * 3) selection will start with transport dataType and THEN go to "*" if needed\n\t */\n\ttransports = {},\n\n\t// Avoid comment-prolog char sequence (#10098); must appease lint and evade compression\n\tallTypes = "*/".concat( "*" ),\n\n\t// Anchor tag for parsing the document origin\n\toriginAnchor = document.createElement( "a" );\n\toriginAnchor.href = location.href;\n\n// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport\nfunction addToPrefiltersOrTransports( structure ) {\n\n\t// dataTypeExpression is optional and defaults to "*"\n\treturn function( dataTypeExpression, func ) {\n\n\t\tif ( typeof dataTypeExpression !== "string" ) {\n\t\t\tfunc = dataTypeExpression;\n\t\t\tdataTypeExpression = "*";\n\t\t}\n\n\t\tvar dataType,\n\t\t\ti = 0,\n\t\t\tdataTypes = dataTypeExpression.toLowerCase().match( rnothtmlwhite ) || [];\n\n\t\tif ( isFunction( func ) ) {\n\n\t\t\t// For each dataType in the dataTypeExpression\n\t\t\twhile ( ( dataType = dataTypes[ i++ ] ) ) {\n\n\t\t\t\t// Prepend if requested\n\t\t\t\tif ( dataType[ 0 ] === "+" ) {\n\t\t\t\t\tdataType = dataType.slice( 1 ) || "*";\n\t\t\t\t\t( structure[ dataType ] = structure[ dataType ] || [] ).unshift( func );\n\n\t\t\t\t// Otherwise append\n\t\t\t\t} else {\n\t\t\t\t\t( structure[ dataType ] = structure[ dataType ] || [] ).push( func );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t};\n}\n\n// Base inspection function for prefilters and transports\nfunction inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR ) {\n\n\tvar inspected = {},\n\t\tseekingTransport = ( structure === transports );\n\n\tfunction inspect( dataType ) {\n\t\tvar selected;\n\t\tinspected[ dataType ] = true;\n\t\tjQuery.each( structure[ dataType ] || [], function( _, prefilterOrFactory ) {\n\t\t\tvar dataTypeOrTransport = prefilterOrFactory( options, originalOptions, jqXHR );\n\t\t\tif ( typeof dataTypeOrTransport === "string" &&\n\t\t\t\t!seekingTransport && !inspected[ dataTypeOrTransport ] ) {\n\n\t\t\t\toptions.dataTypes.unshift( dataTypeOrTransport );\n\t\t\t\tinspect( dataTypeOrTransport );\n\t\t\t\treturn false;\n\t\t\t} else if ( seekingTransport ) {\n\t\t\t\treturn !( selected = dataTypeOrTransport );\n\t\t\t}\n\t\t} );\n\t\treturn selected;\n\t}\n\n\treturn inspect( options.dataTypes[ 0 ] ) || !inspected[ "*" ] && inspect( "*" );\n}\n\n// A special extend for ajax options\n// that takes "flat" options (not to be deep extended)\n// Fixes #9887\nfunction ajaxExtend( target, src ) {\n\tvar key, deep,\n\t\tflatOptions = jQuery.ajaxSettings.flatOptions || {};\n\n\tfor ( key in src ) {\n\t\tif ( src[ key ] !== undefined ) {\n\t\t\t( flatOptions[ key ] ? target : ( deep || ( deep = {} ) ) )[ key ] = src[ key ];\n\t\t}\n\t}\n\tif ( deep ) {\n\t\tjQuery.extend( true, target, deep );\n\t}\n\n\treturn target;\n}\n\n/* Handles responses to an ajax request:\n * - finds the right dataType (mediates between content-type and expected dataType)\n * - returns the corresponding response\n */\nfunction ajaxHandleResponses( s, jqXHR, responses ) {\n\n\tvar ct, type, finalDataType, firstDataType,\n\t\tcontents = s.contents,\n\t\tdataTypes = s.dataTypes;\n\n\t// Remove auto dataType and get content-type in the process\n\twhile ( dataTypes[ 0 ] === "*" ) {\n\t\tdataTypes.shift();\n\t\tif ( ct === undefined ) {\n\t\t\tct = s.mimeType || jqXHR.getResponseHeader( "Content-Type" );\n\t\t}\n\t}\n\n\t// Check if we\'re dealing with a known content-type\n\tif ( ct ) {\n\t\tfor ( type in contents ) {\n\t\t\tif ( contents[ type ] && contents[ type ].test( ct ) ) {\n\t\t\t\tdataTypes.unshift( type );\n\t\t\t\tbreak;\n\t\t\t}\n\t\t}\n\t}\n\n\t// Check to see if we have a response for the expected dataType\n\tif ( dataTypes[ 0 ] in responses ) {\n\t\tfinalDataType = dataTypes[ 0 ];\n\t} else {\n\n\t\t// Try convertible dataTypes\n\t\tfor ( type in responses ) {\n\t\t\tif ( !dataTypes[ 0 ] || s.converters[ type + " " + dataTypes[ 0 ] ] ) {\n\t\t\t\tfinalDataType = type;\n\t\t\t\tbreak;\n\t\t\t}\n\t\t\tif ( !firstDataType ) {\n\t\t\t\tfirstDataType = type;\n\t\t\t}\n\t\t}\n\n\t\t// Or just use first one\n\t\tfinalDataType = finalDataType || firstDataType;\n\t}\n\n\t// If we found a dataType\n\t// We add the dataType to the list if needed\n\t// and return the corresponding response\n\tif ( finalDataType ) {\n\t\tif ( finalDataType !== dataTypes[ 0 ] ) {\n\t\t\tdataTypes.unshift( finalDataType );\n\t\t}\n\t\treturn responses[ finalDataType ];\n\t}\n}\n\n/* Chain conversions given the request and the original response\n * Also sets the responseXXX fields on the jqXHR instance\n */\nfunction ajaxConvert( s, response, jqXHR, isSuccess ) {\n\tvar conv2, current, conv, tmp, prev,\n\t\tconverters = {},\n\n\t\t// Work with a copy of dataTypes in case we need to modify it for conversion\n\t\tdataTypes = s.dataTypes.slice();\n\n\t// Create converters map with lowercased keys\n\tif ( dataTypes[ 1 ] ) {\n\t\tfor ( conv in s.converters ) {\n\t\t\tconverters[ conv.toLowerCase() ] = s.converters[ conv ];\n\t\t}\n\t}\n\n\tcurrent = dataTypes.shift();\n\n\t// Convert to each sequential dataType\n\twhile ( current ) {\n\n\t\tif ( s.responseFields[ current ] ) {\n\t\t\tjqXHR[ s.responseFields[ current ] ] = response;\n\t\t}\n\n\t\t// Apply the dataFilter if provided\n\t\tif ( !prev && isSuccess && s.dataFilter ) {\n\t\t\tresponse = s.dataFilter( response, s.dataType );\n\t\t}\n\n\t\tprev = current;\n\t\tcurrent = dataTypes.shift();\n\n\t\tif ( current ) {\n\n\t\t\t// There\'s only work to do if current dataType is non-auto\n\t\t\tif ( current === "*" ) {\n\n\t\t\t\tcurrent = prev;\n\n\t\t\t// Convert response if prev dataType is non-auto and differs from current\n\t\t\t} else if ( prev !== "*" && prev !== current ) {\n\n\t\t\t\t// Seek a direct converter\n\t\t\t\tconv = converters[ prev + " " + current ] || converters[ "* " + current ];\n\n\t\t\t\t// If none found, seek a pair\n\t\t\t\tif ( !conv ) {\n\t\t\t\t\tfor ( conv2 in converters ) {\n\n\t\t\t\t\t\t// If conv2 outputs current\n\t\t\t\t\t\ttmp = conv2.split( " " );\n\t\t\t\t\t\tif ( tmp[ 1 ] === current ) {\n\n\t\t\t\t\t\t\t// If prev can be converted to accepted input\n\t\t\t\t\t\t\tconv = converters[ prev + " " + tmp[ 0 ] ] ||\n\t\t\t\t\t\t\t\tconverters[ "* " + tmp[ 0 ] ];\n\t\t\t\t\t\t\tif ( conv ) {\n\n\t\t\t\t\t\t\t\t// Condense equivalence converters\n\t\t\t\t\t\t\t\tif ( conv === true ) {\n\t\t\t\t\t\t\t\t\tconv = converters[ conv2 ];\n\n\t\t\t\t\t\t\t\t// Otherwise, insert the intermediate dataType\n\t\t\t\t\t\t\t\t} else if ( converters[ conv2 ] !== true ) {\n\t\t\t\t\t\t\t\t\tcurrent = tmp[ 0 ];\n\t\t\t\t\t\t\t\t\tdataTypes.unshift( tmp[ 1 ] );\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\t// Apply converter (if not an equivalence)\n\t\t\t\tif ( conv !== true ) {\n\n\t\t\t\t\t// Unless errors are allowed to bubble, catch and return them\n\t\t\t\t\tif ( conv && s.throws ) {\n\t\t\t\t\t\tresponse = conv( response );\n\t\t\t\t\t} else {\n\t\t\t\t\t\ttry {\n\t\t\t\t\t\t\tresponse = conv( response );\n\t\t\t\t\t\t} catch ( e ) {\n\t\t\t\t\t\t\treturn {\n\t\t\t\t\t\t\t\tstate: "parsererror",\n\t\t\t\t\t\t\t\terror: conv ? e : "No conversion from " + prev + " to " + current\n\t\t\t\t\t\t\t};\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\treturn { state: "success", data: response };\n}\n\njQuery.extend( {\n\n\t// Counter for holding the number of active queries\n\tactive: 0,\n\n\t// Last-Modified header cache for next request\n\tlastModified: {},\n\tetag: {},\n\n\tajaxSettings: {\n\t\turl: location.href,\n\t\ttype: "GET",\n\t\tisLocal: rlocalProtocol.test( location.protocol ),\n\t\tglobal: true,\n\t\tprocessData: true,\n\t\tasync: true,\n\t\tcontentType: "application/x-www-form-urlencoded; charset=UTF-8",\n\n\t\t/*\n\t\ttimeout: 0,\n\t\tdata: null,\n\t\tdataType: null,\n\t\tusername: null,\n\t\tpassword: null,\n\t\tcache: null,\n\t\tthrows: false,\n\t\ttraditional: false,\n\t\theaders: {},\n\t\t*/\n\n\t\taccepts: {\n\t\t\t"*": allTypes,\n\t\t\ttext: "text/plain",\n\t\t\thtml: "text/html",\n\t\t\txml: "application/xml, text/xml",\n\t\t\tjson: "application/json, text/javascript"\n\t\t},\n\n\t\tcontents: {\n\t\t\txml: /\\bxml\\b/,\n\t\t\thtml: /\\bhtml/,\n\t\t\tjson: /\\bjson\\b/\n\t\t},\n\n\t\tresponseFields: {\n\t\t\txml: "responseXML",\n\t\t\ttext: "responseText",\n\t\t\tjson: "responseJSON"\n\t\t},\n\n\t\t// Data converters\n\t\t// Keys separate source (or catchall "*") and destination types with a single space\n\t\tconverters: {\n\n\t\t\t// Convert anything to text\n\t\t\t"* text": String,\n\n\t\t\t// Text to html (true = no transformation)\n\t\t\t"text html": true,\n\n\t\t\t// Evaluate text as a json expression\n\t\t\t"text json": JSON.parse,\n\n\t\t\t// Parse text as xml\n\t\t\t"text xml": jQuery.parseXML\n\t\t},\n\n\t\t// For options that shouldn\'t be deep extended:\n\t\t// you can add your own custom options here if\n\t\t// and when you create one that shouldn\'t be\n\t\t// deep extended (see ajaxExtend)\n\t\tflatOptions: {\n\t\t\turl: true,\n\t\t\tcontext: true\n\t\t}\n\t},\n\n\t// Creates a full fledged settings object into target\n\t// with both ajaxSettings and settings fields.\n\t// If target is omitted, writes into ajaxSettings.\n\tajaxSetup: function( target, settings ) {\n\t\treturn settings ?\n\n\t\t\t// Building a settings object\n\t\t\tajaxExtend( ajaxExtend( target, jQuery.ajaxSettings ), settings ) :\n\n\t\t\t// Extending ajaxSettings\n\t\t\tajaxExtend( jQuery.ajaxSettings, target );\n\t},\n\n\tajaxPrefilter: addToPrefiltersOrTransports( prefilters ),\n\tajaxTransport: addToPrefiltersOrTransports( transports ),\n\n\t// Main method\n\tajax: function( url, options ) {\n\n\t\t// If url is an object, simulate pre-1.5 signature\n\t\tif ( typeof url === "object" ) {\n\t\t\toptions = url;\n\t\t\turl = undefined;\n\t\t}\n\n\t\t// Force options to be an object\n\t\toptions = options || {};\n\n\t\tvar transport,\n\n\t\t\t// URL without anti-cache param\n\t\t\tcacheURL,\n\n\t\t\t// Response headers\n\t\t\tresponseHeadersString,\n\t\t\tresponseHeaders,\n\n\t\t\t// timeout handle\n\t\t\ttimeoutTimer,\n\n\t\t\t// Url cleanup var\n\t\t\turlAnchor,\n\n\t\t\t// Request state (becomes false upon send and true upon completion)\n\t\t\tcompleted,\n\n\t\t\t// To know if global events are to be dispatched\n\t\t\tfireGlobals,\n\n\t\t\t// Loop variable\n\t\t\ti,\n\n\t\t\t// uncached part of the url\n\t\t\tuncached,\n\n\t\t\t// Create the final options object\n\t\t\ts = jQuery.ajaxSetup( {}, options ),\n\n\t\t\t// Callbacks context\n\t\t\tcallbackContext = s.context || s,\n\n\t\t\t// Context for global events is callbackContext if it is a DOM node or jQuery collection\n\t\t\tglobalEventContext = s.context &&\n\t\t\t\t( callbackContext.nodeType || callbackContext.jquery ) ?\n\t\t\t\t\tjQuery( callbackContext ) :\n\t\t\t\t\tjQuery.event,\n\n\t\t\t// Deferreds\n\t\t\tdeferred = jQuery.Deferred(),\n\t\t\tcompleteDeferred = jQuery.Callbacks( "once memory" ),\n\n\t\t\t// Status-dependent callbacks\n\t\t\tstatusCode = s.statusCode || {},\n\n\t\t\t// Headers (they are sent all at once)\n\t\t\trequestHeaders = {},\n\t\t\trequestHeadersNames = {},\n\n\t\t\t// Default abort message\n\t\t\tstrAbort = "canceled",\n\n\t\t\t// Fake xhr\n\t\t\tjqXHR = {\n\t\t\t\treadyState: 0,\n\n\t\t\t\t// Builds headers hashtable if needed\n\t\t\t\tgetResponseHeader: function( key ) {\n\t\t\t\t\tvar match;\n\t\t\t\t\tif ( completed ) {\n\t\t\t\t\t\tif ( !responseHeaders ) {\n\t\t\t\t\t\t\tresponseHeaders = {};\n\t\t\t\t\t\t\twhile ( ( match = rheaders.exec( responseHeadersString ) ) ) {\n\t\t\t\t\t\t\t\tresponseHeaders[ match[ 1 ].toLowerCase() ] = match[ 2 ];\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t\tmatch = responseHeaders[ key.toLowerCase() ];\n\t\t\t\t\t}\n\t\t\t\t\treturn match == null ? null : match;\n\t\t\t\t},\n\n\t\t\t\t// Raw string\n\t\t\t\tgetAllResponseHeaders: function() {\n\t\t\t\t\treturn completed ? responseHeadersString : null;\n\t\t\t\t},\n\n\t\t\t\t// Caches the header\n\t\t\t\tsetRequestHeader: function( name, value ) {\n\t\t\t\t\tif ( completed == null ) {\n\t\t\t\t\t\tname = requestHeadersNames[ name.toLowerCase() ] =\n\t\t\t\t\t\t\trequestHeadersNames[ name.toLowerCase() ] || name;\n\t\t\t\t\t\trequestHeaders[ name ] = value;\n\t\t\t\t\t}\n\t\t\t\t\treturn this;\n\t\t\t\t},\n\n\t\t\t\t// Overrides response content-type header\n\t\t\t\toverrideMimeType: function( type ) {\n\t\t\t\t\tif ( completed == null ) {\n\t\t\t\t\t\ts.mimeType = type;\n\t\t\t\t\t}\n\t\t\t\t\treturn this;\n\t\t\t\t},\n\n\t\t\t\t// Status-dependent callbacks\n\t\t\t\tstatusCode: function( map ) {\n\t\t\t\t\tvar code;\n\t\t\t\t\tif ( map ) {\n\t\t\t\t\t\tif ( completed ) {\n\n\t\t\t\t\t\t\t// Execute the appropriate callbacks\n\t\t\t\t\t\t\tjqXHR.always( map[ jqXHR.status ] );\n\t\t\t\t\t\t} else {\n\n\t\t\t\t\t\t\t// Lazy-add the new callbacks in a way that preserves old ones\n\t\t\t\t\t\t\tfor ( code in map ) {\n\t\t\t\t\t\t\t\tstatusCode[ code ] = [ statusCode[ code ], map[ code ] ];\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t\treturn this;\n\t\t\t\t},\n\n\t\t\t\t// Cancel the request\n\t\t\t\tabort: function( statusText ) {\n\t\t\t\t\tvar finalText = statusText || strAbort;\n\t\t\t\t\tif ( transport ) {\n\t\t\t\t\t\ttransport.abort( finalText );\n\t\t\t\t\t}\n\t\t\t\t\tdone( 0, finalText );\n\t\t\t\t\treturn this;\n\t\t\t\t}\n\t\t\t};\n\n\t\t// Attach deferreds\n\t\tdeferred.promise( jqXHR );\n\n\t\t// Add protocol if not provided (prefilters might expect it)\n\t\t// Handle falsy url in the settings object (#10093: consistency with old signature)\n\t\t// We also use the url parameter if available\n\t\ts.url = ( ( url || s.url || location.href ) + "" )\n\t\t\t.replace( rprotocol, location.protocol + "//" );\n\n\t\t// Alias method option to type as per ticket #12004\n\t\ts.type = options.method || options.type || s.method || s.type;\n\n\t\t// Extract dataTypes list\n\t\ts.dataTypes = ( s.dataType || "*" ).toLowerCase().match( rnothtmlwhite ) || [ "" ];\n\n\t\t// A cross-domain request is in order when the origin doesn\'t match the current origin.\n\t\tif ( s.crossDomain == null ) {\n\t\t\turlAnchor = document.createElement( "a" );\n\n\t\t\t// Support: IE <=8 - 11, Edge 12 - 15\n\t\t\t// IE throws exception on accessing the href property if url is malformed,\n\t\t\t// e.g. http://example.com:80x/\n\t\t\ttry {\n\t\t\t\turlAnchor.href = s.url;\n\n\t\t\t\t// Support: IE <=8 - 11 only\n\t\t\t\t// Anchor\'s host property isn\'t correctly set when s.url is relative\n\t\t\t\turlAnchor.href = urlAnchor.href;\n\t\t\t\ts.crossDomain = originAnchor.protocol + "//" + originAnchor.host !==\n\t\t\t\t\turlAnchor.protocol + "//" + urlAnchor.host;\n\t\t\t} catch ( e ) {\n\n\t\t\t\t// If there is an error parsing the URL, assume it is crossDomain,\n\t\t\t\t// it can be rejected by the transport if it is invalid\n\t\t\t\ts.crossDomain = true;\n\t\t\t}\n\t\t}\n\n\t\t// Convert data if not already a string\n\t\tif ( s.data && s.processData && typeof s.data !== "string" ) {\n\t\t\ts.data = jQuery.param( s.data, s.traditional );\n\t\t}\n\n\t\t// Apply prefilters\n\t\tinspectPrefiltersOrTransports( prefilters, s, options, jqXHR );\n\n\t\t// If request was aborted inside a prefilter, stop there\n\t\tif ( completed ) {\n\t\t\treturn jqXHR;\n\t\t}\n\n\t\t// We can fire global events as of now if asked to\n\t\t// Don\'t fire events if jQuery.event is undefined in an AMD-usage scenario (#15118)\n\t\tfireGlobals = jQuery.event && s.global;\n\n\t\t// Watch for a new set of requests\n\t\tif ( fireGlobals && jQuery.active++ === 0 ) {\n\t\t\tjQuery.event.trigger( "ajaxStart" );\n\t\t}\n\n\t\t// Uppercase the type\n\t\ts.type = s.type.toUpperCase();\n\n\t\t// Determine if request has content\n\t\ts.hasContent = !rnoContent.test( s.type );\n\n\t\t// Save the URL in case we\'re toying with the If-Modified-Since\n\t\t// and/or If-None-Match header later on\n\t\t// Remove hash to simplify url manipulation\n\t\tcacheURL = s.url.replace( rhash, "" );\n\n\t\t// More options handling for requests with no content\n\t\tif ( !s.hasContent ) {\n\n\t\t\t// Remember the hash so we can put it back\n\t\t\tuncached = s.url.slice( cacheURL.length );\n\n\t\t\t// If data is available and should be processed, append data to url\n\t\t\tif ( s.data && ( s.processData || typeof s.data === "string" ) ) {\n\t\t\t\tcacheURL += ( rquery.test( cacheURL ) ? "&" : "?" ) + s.data;\n\n\t\t\t\t// #9682: remove data so that it\'s not used in an eventual retry\n\t\t\t\tdelete s.data;\n\t\t\t}\n\n\t\t\t// Add or update anti-cache param if needed\n\t\t\tif ( s.cache === false ) {\n\t\t\t\tcacheURL = cacheURL.replace( rantiCache, "$1" );\n\t\t\t\tuncached = ( rquery.test( cacheURL ) ? "&" : "?" ) + "_=" + ( nonce++ ) + uncached;\n\t\t\t}\n\n\t\t\t// Put hash and anti-cache on the URL that will be requested (gh-1732)\n\t\t\ts.url = cacheURL + uncached;\n\n\t\t// Change \'%20\' to \'+\' if this is encoded form body content (gh-2658)\n\t\t} else if ( s.data && s.processData &&\n\t\t\t( s.contentType || "" ).indexOf( "application/x-www-form-urlencoded" ) === 0 ) {\n\t\t\ts.data = s.data.replace( r20, "+" );\n\t\t}\n\n\t\t// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.\n\t\tif ( s.ifModified ) {\n\t\t\tif ( jQuery.lastModified[ cacheURL ] ) {\n\t\t\t\tjqXHR.setRequestHeader( "If-Modified-Since", jQuery.lastModified[ cacheURL ] );\n\t\t\t}\n\t\t\tif ( jQuery.etag[ cacheURL ] ) {\n\t\t\t\tjqXHR.setRequestHeader( "If-None-Match", jQuery.etag[ cacheURL ] );\n\t\t\t}\n\t\t}\n\n\t\t// Set the correct header, if data is being sent\n\t\tif ( s.data && s.hasContent && s.contentType !== false || options.contentType ) {\n\t\t\tjqXHR.setRequestHeader( "Content-Type", s.contentType );\n\t\t}\n\n\t\t// Set the Accepts header for the server, depending on the dataType\n\t\tjqXHR.setRequestHeader(\n\t\t\t"Accept",\n\t\t\ts.dataTypes[ 0 ] && s.accepts[ s.dataTypes[ 0 ] ] ?\n\t\t\t\ts.accepts[ s.dataTypes[ 0 ] ] +\n\t\t\t\t\t( s.dataTypes[ 0 ] !== "*" ? ", " + allTypes + "; q=0.01" : "" ) :\n\t\t\t\ts.accepts[ "*" ]\n\t\t);\n\n\t\t// Check for headers option\n\t\tfor ( i in s.headers ) {\n\t\t\tjqXHR.setRequestHeader( i, s.headers[ i ] );\n\t\t}\n\n\t\t// Allow custom headers/mimetypes and early abort\n\t\tif ( s.beforeSend &&\n\t\t\t( s.beforeSend.call( callbackContext, jqXHR, s ) === false || completed ) ) {\n\n\t\t\t// Abort if not done already and return\n\t\t\treturn jqXHR.abort();\n\t\t}\n\n\t\t// Aborting is no longer a cancellation\n\t\tstrAbort = "abort";\n\n\t\t// Install callbacks on deferreds\n\t\tcompleteDeferred.add( s.complete );\n\t\tjqXHR.done( s.success );\n\t\tjqXHR.fail( s.error );\n\n\t\t// Get transport\n\t\ttransport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );\n\n\t\t// If no transport, we auto-abort\n\t\tif ( !transport ) {\n\t\t\tdone( -1, "No Transport" );\n\t\t} else {\n\t\t\tjqXHR.readyState = 1;\n\n\t\t\t// Send global event\n\t\t\tif ( fireGlobals ) {\n\t\t\t\tglobalEventContext.trigger( "ajaxSend", [ jqXHR, s ] );\n\t\t\t}\n\n\t\t\t// If request was aborted inside ajaxSend, stop there\n\t\t\tif ( completed ) {\n\t\t\t\treturn jqXHR;\n\t\t\t}\n\n\t\t\t// Timeout\n\t\t\tif ( s.async && s.timeout > 0 ) {\n\t\t\t\ttimeoutTimer = window.setTimeout( function() {\n\t\t\t\t\tjqXHR.abort( "timeout" );\n\t\t\t\t}, s.timeout );\n\t\t\t}\n\n\t\t\ttry {\n\t\t\t\tcompleted = false;\n\t\t\t\ttransport.send( requestHeaders, done );\n\t\t\t} catch ( e ) {\n\n\t\t\t\t// Rethrow post-completion exceptions\n\t\t\t\tif ( completed ) {\n\t\t\t\t\tthrow e;\n\t\t\t\t}\n\n\t\t\t\t// Propagate others as results\n\t\t\t\tdone( -1, e );\n\t\t\t}\n\t\t}\n\n\t\t// Callback for when everything is done\n\t\tfunction done( status, nativeStatusText, responses, headers ) {\n\t\t\tvar isSuccess, success, error, response, modified,\n\t\t\t\tstatusText = nativeStatusText;\n\n\t\t\t// Ignore repeat invocations\n\t\t\tif ( completed ) {\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\tcompleted = true;\n\n\t\t\t// Clear timeout if it exists\n\t\t\tif ( timeoutTimer ) {\n\t\t\t\twindow.clearTimeout( timeoutTimer );\n\t\t\t}\n\n\t\t\t// Dereference transport for early garbage collection\n\t\t\t// (no matter how long the jqXHR object will be used)\n\t\t\ttransport = undefined;\n\n\t\t\t// Cache response headers\n\t\t\tresponseHeadersString = headers || "";\n\n\t\t\t// Set readyState\n\t\t\tjqXHR.readyState = status > 0 ? 4 : 0;\n\n\t\t\t// Determine if successful\n\t\t\tisSuccess = status >= 200 && status < 300 || status === 304;\n\n\t\t\t// Get response data\n\t\t\tif ( responses ) {\n\t\t\t\tresponse = ajaxHandleResponses( s, jqXHR, responses );\n\t\t\t}\n\n\t\t\t// Convert no matter what (that way responseXXX fields are always set)\n\t\t\tresponse = ajaxConvert( s, response, jqXHR, isSuccess );\n\n\t\t\t// If successful, handle type chaining\n\t\t\tif ( isSuccess ) {\n\n\t\t\t\t// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.\n\t\t\t\tif ( s.ifModified ) {\n\t\t\t\t\tmodified = jqXHR.getResponseHeader( "Last-Modified" );\n\t\t\t\t\tif ( modified ) {\n\t\t\t\t\t\tjQuery.lastModified[ cacheURL ] = modified;\n\t\t\t\t\t}\n\t\t\t\t\tmodified = jqXHR.getResponseHeader( "etag" );\n\t\t\t\t\tif ( modified ) {\n\t\t\t\t\t\tjQuery.etag[ cacheURL ] = modified;\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\t// if no content\n\t\t\t\tif ( status === 204 || s.type === "HEAD" ) {\n\t\t\t\t\tstatusText = "nocontent";\n\n\t\t\t\t// if not modified\n\t\t\t\t} else if ( status === 304 ) {\n\t\t\t\t\tstatusText = "notmodified";\n\n\t\t\t\t// If we have data, let\'s convert it\n\t\t\t\t} else {\n\t\t\t\t\tstatusText = response.state;\n\t\t\t\t\tsuccess = response.data;\n\t\t\t\t\terror = response.error;\n\t\t\t\t\tisSuccess = !error;\n\t\t\t\t}\n\t\t\t} else {\n\n\t\t\t\t// Extract error from statusText and normalize for non-aborts\n\t\t\t\terror = statusText;\n\t\t\t\tif ( status || !statusText ) {\n\t\t\t\t\tstatusText = "error";\n\t\t\t\t\tif ( status < 0 ) {\n\t\t\t\t\t\tstatus = 0;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Set data for the fake xhr object\n\t\t\tjqXHR.status = status;\n\t\t\tjqXHR.statusText = ( nativeStatusText || statusText ) + "";\n\n\t\t\t// Success/Error\n\t\t\tif ( isSuccess ) {\n\t\t\t\tdeferred.resolveWith( callbackContext, [ success, statusText, jqXHR ] );\n\t\t\t} else {\n\t\t\t\tdeferred.rejectWith( callbackContext, [ jqXHR, statusText, error ] );\n\t\t\t}\n\n\t\t\t// Status-dependent callbacks\n\t\t\tjqXHR.statusCode( statusCode );\n\t\t\tstatusCode = undefined;\n\n\t\t\tif ( fireGlobals ) {\n\t\t\t\tglobalEventContext.trigger( isSuccess ? "ajaxSuccess" : "ajaxError",\n\t\t\t\t\t[ jqXHR, s, isSuccess ? success : error ] );\n\t\t\t}\n\n\t\t\t// Complete\n\t\t\tcompleteDeferred.fireWith( callbackContext, [ jqXHR, statusText ] );\n\n\t\t\tif ( fireGlobals ) {\n\t\t\t\tglobalEventContext.trigger( "ajaxComplete", [ jqXHR, s ] );\n\n\t\t\t\t// Handle the global AJAX counter\n\t\t\t\tif ( !( --jQuery.active ) ) {\n\t\t\t\t\tjQuery.event.trigger( "ajaxStop" );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn jqXHR;\n\t},\n\n\tgetJSON: function( url, data, callback ) {\n\t\treturn jQuery.get( url, data, callback, "json" );\n\t},\n\n\tgetScript: function( url, callback ) {\n\t\treturn jQuery.get( url, undefined, callback, "script" );\n\t}\n} );\n\njQuery.each( [ "get", "post" ], function( i, method ) {\n\tjQuery[ method ] = function( url, data, callback, type ) {\n\n\t\t// Shift arguments if data argument was omitted\n\t\tif ( isFunction( data ) ) {\n\t\t\ttype = type || callback;\n\t\t\tcallback = data;\n\t\t\tdata = undefined;\n\t\t}\n\n\t\t// The url can be an options object (which then must have .url)\n\t\treturn jQuery.ajax( jQuery.extend( {\n\t\t\turl: url,\n\t\t\ttype: method,\n\t\t\tdataType: type,\n\t\t\tdata: data,\n\t\t\tsuccess: callback\n\t\t}, jQuery.isPlainObject( url ) && url ) );\n\t};\n} );\n\n\njQuery._evalUrl = function( url ) {\n\treturn jQuery.ajax( {\n\t\turl: url,\n\n\t\t// Make this explicit, since user can override this through ajaxSetup (#11264)\n\t\ttype: "GET",\n\t\tdataType: "script",\n\t\tcache: true,\n\t\tasync: false,\n\t\tglobal: false,\n\t\t"throws": true\n\t} );\n};\n\n\njQuery.fn.extend( {\n\twrapAll: function( html ) {\n\t\tvar wrap;\n\n\t\tif ( this[ 0 ] ) {\n\t\t\tif ( isFunction( html ) ) {\n\t\t\t\thtml = html.call( this[ 0 ] );\n\t\t\t}\n\n\t\t\t// The elements to wrap the target around\n\t\t\twrap = jQuery( html, this[ 0 ].ownerDocument ).eq( 0 ).clone( true );\n\n\t\t\tif ( this[ 0 ].parentNode ) {\n\t\t\t\twrap.insertBefore( this[ 0 ] );\n\t\t\t}\n\n\t\t\twrap.map( function() {\n\t\t\t\tvar elem = this;\n\n\t\t\t\twhile ( elem.firstElementChild ) {\n\t\t\t\t\telem = elem.firstElementChild;\n\t\t\t\t}\n\n\t\t\t\treturn elem;\n\t\t\t} ).append( this );\n\t\t}\n\n\t\treturn this;\n\t},\n\n\twrapInner: function( html ) {\n\t\tif ( isFunction( html ) ) {\n\t\t\treturn this.each( function( i ) {\n\t\t\t\tjQuery( this ).wrapInner( html.call( this, i ) );\n\t\t\t} );\n\t\t}\n\n\t\treturn this.each( function() {\n\t\t\tvar self = jQuery( this ),\n\t\t\t\tcontents = self.contents();\n\n\t\t\tif ( contents.length ) {\n\t\t\t\tcontents.wrapAll( html );\n\n\t\t\t} else {\n\t\t\t\tself.append( html );\n\t\t\t}\n\t\t} );\n\t},\n\n\twrap: function( html ) {\n\t\tvar htmlIsFunction = isFunction( html );\n\n\t\treturn this.each( function( i ) {\n\t\t\tjQuery( this ).wrapAll( htmlIsFunction ? html.call( this, i ) : html );\n\t\t} );\n\t},\n\n\tunwrap: function( selector ) {\n\t\tthis.parent( selector ).not( "body" ).each( function() {\n\t\t\tjQuery( this ).replaceWith( this.childNodes );\n\t\t} );\n\t\treturn this;\n\t}\n} );\n\n\njQuery.expr.pseudos.hidden = function( elem ) {\n\treturn !jQuery.expr.pseudos.visible( elem );\n};\njQuery.expr.pseudos.visible = function( elem ) {\n\treturn !!( elem.offsetWidth || elem.offsetHeight || elem.getClientRects().length );\n};\n\n\n\n\njQuery.ajaxSettings.xhr = function() {\n\ttry {\n\t\treturn new window.XMLHttpRequest();\n\t} catch ( e ) {}\n};\n\nvar xhrSuccessStatus = {\n\n\t\t// File protocol always yields status code 0, assume 200\n\t\t0: 200,\n\n\t\t// Support: IE <=9 only\n\t\t// #1450: sometimes IE returns 1223 when it should be 204\n\t\t1223: 204\n\t},\n\txhrSupported = jQuery.ajaxSettings.xhr();\n\nsupport.cors = !!xhrSupported && ( "withCredentials" in xhrSupported );\nsupport.ajax = xhrSupported = !!xhrSupported;\n\njQuery.ajaxTransport( function( options ) {\n\tvar callback, errorCallback;\n\n\t// Cross domain only allowed if supported through XMLHttpRequest\n\tif ( support.cors || xhrSupported && !options.crossDomain ) {\n\t\treturn {\n\t\t\tsend: function( headers, complete ) {\n\t\t\t\tvar i,\n\t\t\t\t\txhr = options.xhr();\n\n\t\t\t\txhr.open(\n\t\t\t\t\toptions.type,\n\t\t\t\t\toptions.url,\n\t\t\t\t\toptions.async,\n\t\t\t\t\toptions.username,\n\t\t\t\t\toptions.password\n\t\t\t\t);\n\n\t\t\t\t// Apply custom fields if provided\n\t\t\t\tif ( options.xhrFields ) {\n\t\t\t\t\tfor ( i in options.xhrFields ) {\n\t\t\t\t\t\txhr[ i ] = options.xhrFields[ i ];\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\t// Override mime type if needed\n\t\t\t\tif ( options.mimeType && xhr.overrideMimeType ) {\n\t\t\t\t\txhr.overrideMimeType( options.mimeType );\n\t\t\t\t}\n\n\t\t\t\t// X-Requested-With header\n\t\t\t\t// For cross-domain requests, seeing as conditions for a preflight are\n\t\t\t\t// akin to a jigsaw puzzle, we simply never set it to be sure.\n\t\t\t\t// (it can always be set on a per-request basis or even using ajaxSetup)\n\t\t\t\t// For same-domain requests, won\'t change header if already provided.\n\t\t\t\tif ( !options.crossDomain && !headers[ "X-Requested-With" ] ) {\n\t\t\t\t\theaders[ "X-Requested-With" ] = "XMLHttpRequest";\n\t\t\t\t}\n\n\t\t\t\t// Set headers\n\t\t\t\tfor ( i in headers ) {\n\t\t\t\t\txhr.setRequestHeader( i, headers[ i ] );\n\t\t\t\t}\n\n\t\t\t\t// Callback\n\t\t\t\tcallback = function( type ) {\n\t\t\t\t\treturn function() {\n\t\t\t\t\t\tif ( callback ) {\n\t\t\t\t\t\t\tcallback = errorCallback = xhr.onload =\n\t\t\t\t\t\t\t\txhr.onerror = xhr.onabort = xhr.ontimeout =\n\t\t\t\t\t\t\t\t\txhr.onreadystatechange = null;\n\n\t\t\t\t\t\t\tif ( type === "abort" ) {\n\t\t\t\t\t\t\t\txhr.abort();\n\t\t\t\t\t\t\t} else if ( type === "error" ) {\n\n\t\t\t\t\t\t\t\t// Support: IE <=9 only\n\t\t\t\t\t\t\t\t// On a manual native abort, IE9 throws\n\t\t\t\t\t\t\t\t// errors on any property access that is not readyState\n\t\t\t\t\t\t\t\tif ( typeof xhr.status !== "number" ) {\n\t\t\t\t\t\t\t\t\tcomplete( 0, "error" );\n\t\t\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t\t\tcomplete(\n\n\t\t\t\t\t\t\t\t\t\t// File: protocol always yields status 0; see #8605, #14207\n\t\t\t\t\t\t\t\t\t\txhr.status,\n\t\t\t\t\t\t\t\t\t\txhr.statusText\n\t\t\t\t\t\t\t\t\t);\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t\tcomplete(\n\t\t\t\t\t\t\t\t\txhrSuccessStatus[ xhr.status ] || xhr.status,\n\t\t\t\t\t\t\t\t\txhr.statusText,\n\n\t\t\t\t\t\t\t\t\t// Support: IE <=9 only\n\t\t\t\t\t\t\t\t\t// IE9 has no XHR2 but throws on binary (trac-11426)\n\t\t\t\t\t\t\t\t\t// For XHR2 non-text, let the caller handle it (gh-2498)\n\t\t\t\t\t\t\t\t\t( xhr.responseType || "text" ) !== "text"  ||\n\t\t\t\t\t\t\t\t\ttypeof xhr.responseText !== "string" ?\n\t\t\t\t\t\t\t\t\t\t{ binary: xhr.response } :\n\t\t\t\t\t\t\t\t\t\t{ text: xhr.responseText },\n\t\t\t\t\t\t\t\t\txhr.getAllResponseHeaders()\n\t\t\t\t\t\t\t\t);\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t};\n\t\t\t\t};\n\n\t\t\t\t// Listen to events\n\t\t\t\txhr.onload = callback();\n\t\t\t\terrorCallback = xhr.onerror = xhr.ontimeout = callback( "error" );\n\n\t\t\t\t// Support: IE 9 only\n\t\t\t\t// Use onreadystatechange to replace onabort\n\t\t\t\t// to handle uncaught aborts\n\t\t\t\tif ( xhr.onabort !== undefined ) {\n\t\t\t\t\txhr.onabort = errorCallback;\n\t\t\t\t} else {\n\t\t\t\t\txhr.onreadystatechange = function() {\n\n\t\t\t\t\t\t// Check readyState before timeout as it changes\n\t\t\t\t\t\tif ( xhr.readyState === 4 ) {\n\n\t\t\t\t\t\t\t// Allow onerror to be called first,\n\t\t\t\t\t\t\t// but that will not handle a native abort\n\t\t\t\t\t\t\t// Also, save errorCallback to a variable\n\t\t\t\t\t\t\t// as xhr.onerror cannot be accessed\n\t\t\t\t\t\t\twindow.setTimeout( function() {\n\t\t\t\t\t\t\t\tif ( callback ) {\n\t\t\t\t\t\t\t\t\terrorCallback();\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t} );\n\t\t\t\t\t\t}\n\t\t\t\t\t};\n\t\t\t\t}\n\n\t\t\t\t// Create the abort callback\n\t\t\t\tcallback = callback( "abort" );\n\n\t\t\t\ttry {\n\n\t\t\t\t\t// Do send the request (this may raise an exception)\n\t\t\t\t\txhr.send( options.hasContent && options.data || null );\n\t\t\t\t} catch ( e ) {\n\n\t\t\t\t\t// #14683: Only rethrow if this hasn\'t been notified as an error yet\n\t\t\t\t\tif ( callback ) {\n\t\t\t\t\t\tthrow e;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t},\n\n\t\t\tabort: function() {\n\t\t\t\tif ( callback ) {\n\t\t\t\t\tcallback();\n\t\t\t\t}\n\t\t\t}\n\t\t};\n\t}\n} );\n\n\n\n\n// Prevent auto-execution of scripts when no explicit dataType was provided (See gh-2432)\njQuery.ajaxPrefilter( function( s ) {\n\tif ( s.crossDomain ) {\n\t\ts.contents.script = false;\n\t}\n} );\n\n// Install script dataType\njQuery.ajaxSetup( {\n\taccepts: {\n\t\tscript: "text/javascript, application/javascript, " +\n\t\t\t"application/ecmascript, application/x-ecmascript"\n\t},\n\tcontents: {\n\t\tscript: /\\b(?:java|ecma)script\\b/\n\t},\n\tconverters: {\n\t\t"text script": function( text ) {\n\t\t\tjQuery.globalEval( text );\n\t\t\treturn text;\n\t\t}\n\t}\n} );\n\n// Handle cache\'s special case and crossDomain\njQuery.ajaxPrefilter( "script", function( s ) {\n\tif ( s.cache === undefined ) {\n\t\ts.cache = false;\n\t}\n\tif ( s.crossDomain ) {\n\t\ts.type = "GET";\n\t}\n} );\n\n// Bind script tag hack transport\njQuery.ajaxTransport( "script", function( s ) {\n\n\t// This transport only deals with cross domain requests\n\tif ( s.crossDomain ) {\n\t\tvar script, callback;\n\t\treturn {\n\t\t\tsend: function( _, complete ) {\n\t\t\t\tscript = jQuery( "<script>" ).prop( {\n\t\t\t\t\tcharset: s.scriptCharset,\n\t\t\t\t\tsrc: s.url\n\t\t\t\t} ).on(\n\t\t\t\t\t"load error",\n\t\t\t\t\tcallback = function( evt ) {\n\t\t\t\t\t\tscript.remove();\n\t\t\t\t\t\tcallback = null;\n\t\t\t\t\t\tif ( evt ) {\n\t\t\t\t\t\t\tcomplete( evt.type === "error" ? 404 : 200, evt.type );\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t);\n\n\t\t\t\t// Use native DOM manipulation to avoid our domManip AJAX trickery\n\t\t\t\tdocument.head.appendChild( script[ 0 ] );\n\t\t\t},\n\t\t\tabort: function() {\n\t\t\t\tif ( callback ) {\n\t\t\t\t\tcallback();\n\t\t\t\t}\n\t\t\t}\n\t\t};\n\t}\n} );\n\n\n\n\nvar oldCallbacks = [],\n\trjsonp = /(=)\\?(?=&|$)|\\?\\?/;\n\n// Default jsonp settings\njQuery.ajaxSetup( {\n\tjsonp: "callback",\n\tjsonpCallback: function() {\n\t\tvar callback = oldCallbacks.pop() || ( jQuery.expando + "_" + ( nonce++ ) );\n\t\tthis[ callback ] = true;\n\t\treturn callback;\n\t}\n} );\n\n// Detect, normalize options and install callbacks for jsonp requests\njQuery.ajaxPrefilter( "json jsonp", function( s, originalSettings, jqXHR ) {\n\n\tvar callbackName, overwritten, responseContainer,\n\t\tjsonProp = s.jsonp !== false && ( rjsonp.test( s.url ) ?\n\t\t\t"url" :\n\t\t\ttypeof s.data === "string" &&\n\t\t\t\t( s.contentType || "" )\n\t\t\t\t\t.indexOf( "application/x-www-form-urlencoded" ) === 0 &&\n\t\t\t\trjsonp.test( s.data ) && "data"\n\t\t);\n\n\t// Handle iff the expected data type is "jsonp" or we have a parameter to set\n\tif ( jsonProp || s.dataTypes[ 0 ] === "jsonp" ) {\n\n\t\t// Get callback name, remembering preexisting value associated with it\n\t\tcallbackName = s.jsonpCallback = isFunction( s.jsonpCallback ) ?\n\t\t\ts.jsonpCallback() :\n\t\t\ts.jsonpCallback;\n\n\t\t// Insert callback into url or form data\n\t\tif ( jsonProp ) {\n\t\t\ts[ jsonProp ] = s[ jsonProp ].replace( rjsonp, "$1" + callbackName );\n\t\t} else if ( s.jsonp !== false ) {\n\t\t\ts.url += ( rquery.test( s.url ) ? "&" : "?" ) + s.jsonp + "=" + callbackName;\n\t\t}\n\n\t\t// Use data converter to retrieve json after script execution\n\t\ts.converters[ "script json" ] = function() {\n\t\t\tif ( !responseContainer ) {\n\t\t\t\tjQuery.error( callbackName + " was not called" );\n\t\t\t}\n\t\t\treturn responseContainer[ 0 ];\n\t\t};\n\n\t\t// Force json dataType\n\t\ts.dataTypes[ 0 ] = "json";\n\n\t\t// Install callback\n\t\toverwritten = window[ callbackName ];\n\t\twindow[ callbackName ] = function() {\n\t\t\tresponseContainer = arguments;\n\t\t};\n\n\t\t// Clean-up function (fires after converters)\n\t\tjqXHR.always( function() {\n\n\t\t\t// If previous value didn\'t exist - remove it\n\t\t\tif ( overwritten === undefined ) {\n\t\t\t\tjQuery( window ).removeProp( callbackName );\n\n\t\t\t// Otherwise restore preexisting value\n\t\t\t} else {\n\t\t\t\twindow[ callbackName ] = overwritten;\n\t\t\t}\n\n\t\t\t// Save back as free\n\t\t\tif ( s[ callbackName ] ) {\n\n\t\t\t\t// Make sure that re-using the options doesn\'t screw things around\n\t\t\t\ts.jsonpCallback = originalSettings.jsonpCallback;\n\n\t\t\t\t// Save the callback name for future use\n\t\t\t\toldCallbacks.push( callbackName );\n\t\t\t}\n\n\t\t\t// Call if it was a function and we have a response\n\t\t\tif ( responseContainer && isFunction( overwritten ) ) {\n\t\t\t\toverwritten( responseContainer[ 0 ] );\n\t\t\t}\n\n\t\t\tresponseContainer = overwritten = undefined;\n\t\t} );\n\n\t\t// Delegate to script\n\t\treturn "script";\n\t}\n} );\n\n\n\n\n// Support: Safari 8 only\n// In Safari 8 documents created via document.implementation.createHTMLDocument\n// collapse sibling forms: the second one becomes a child of the first one.\n// Because of that, this security measure has to be disabled in Safari 8.\n// https://bugs.webkit.org/show_bug.cgi?id=137337\nsupport.createHTMLDocument = ( function() {\n\tvar body = document.implementation.createHTMLDocument( "" ).body;\n\tbody.innerHTML = "<form></form><form></form>";\n\treturn body.childNodes.length === 2;\n} )();\n\n\n// Argument "data" should be string of html\n// context (optional): If specified, the fragment will be created in this context,\n// defaults to document\n// keepScripts (optional): If true, will include scripts passed in the html string\njQuery.parseHTML = function( data, context, keepScripts ) {\n\tif ( typeof data !== "string" ) {\n\t\treturn [];\n\t}\n\tif ( typeof context === "boolean" ) {\n\t\tkeepScripts = context;\n\t\tcontext = false;\n\t}\n\n\tvar base, parsed, scripts;\n\n\tif ( !context ) {\n\n\t\t// Stop scripts or inline event handlers from being executed immediately\n\t\t// by using document.implementation\n\t\tif ( support.createHTMLDocument ) {\n\t\t\tcontext = document.implementation.createHTMLDocument( "" );\n\n\t\t\t// Set the base href for the created document\n\t\t\t// so any parsed elements with URLs\n\t\t\t// are based on the document\'s URL (gh-2965)\n\t\t\tbase = context.createElement( "base" );\n\t\t\tbase.href = document.location.href;\n\t\t\tcontext.head.appendChild( base );\n\t\t} else {\n\t\t\tcontext = document;\n\t\t}\n\t}\n\n\tparsed = rsingleTag.exec( data );\n\tscripts = !keepScripts && [];\n\n\t// Single tag\n\tif ( parsed ) {\n\t\treturn [ context.createElement( parsed[ 1 ] ) ];\n\t}\n\n\tparsed = buildFragment( [ data ], context, scripts );\n\n\tif ( scripts && scripts.length ) {\n\t\tjQuery( scripts ).remove();\n\t}\n\n\treturn jQuery.merge( [], parsed.childNodes );\n};\n\n\n/**\n * Load a url into a page\n */\njQuery.fn.load = function( url, params, callback ) {\n\tvar selector, type, response,\n\t\tself = this,\n\t\toff = url.indexOf( " " );\n\n\tif ( off > -1 ) {\n\t\tselector = stripAndCollapse( url.slice( off ) );\n\t\turl = url.slice( 0, off );\n\t}\n\n\t// If it\'s a function\n\tif ( isFunction( params ) ) {\n\n\t\t// We assume that it\'s the callback\n\t\tcallback = params;\n\t\tparams = undefined;\n\n\t// Otherwise, build a param string\n\t} else if ( params && typeof params === "object" ) {\n\t\ttype = "POST";\n\t}\n\n\t// If we have elements to modify, make the request\n\tif ( self.length > 0 ) {\n\t\tjQuery.ajax( {\n\t\t\turl: url,\n\n\t\t\t// If "type" variable is undefined, then "GET" method will be used.\n\t\t\t// Make value of this field explicit since\n\t\t\t// user can override it through ajaxSetup method\n\t\t\ttype: type || "GET",\n\t\t\tdataType: "html",\n\t\t\tdata: params\n\t\t} ).done( function( responseText ) {\n\n\t\t\t// Save response for use in complete callback\n\t\t\tresponse = arguments;\n\n\t\t\tself.html( selector ?\n\n\t\t\t\t// If a selector was specified, locate the right elements in a dummy div\n\t\t\t\t// Exclude scripts to avoid IE \'Permission Denied\' errors\n\t\t\t\tjQuery( "<div>" ).append( jQuery.parseHTML( responseText ) ).find( selector ) :\n\n\t\t\t\t// Otherwise use the full result\n\t\t\t\tresponseText );\n\n\t\t// If the request succeeds, this function gets "data", "status", "jqXHR"\n\t\t// but they are ignored because response was set above.\n\t\t// If it fails, this function gets "jqXHR", "status", "error"\n\t\t} ).always( callback && function( jqXHR, status ) {\n\t\t\tself.each( function() {\n\t\t\t\tcallback.apply( this, response || [ jqXHR.responseText, status, jqXHR ] );\n\t\t\t} );\n\t\t} );\n\t}\n\n\treturn this;\n};\n\n\n\n\n// Attach a bunch of functions for handling common AJAX events\njQuery.each( [\n\t"ajaxStart",\n\t"ajaxStop",\n\t"ajaxComplete",\n\t"ajaxError",\n\t"ajaxSuccess",\n\t"ajaxSend"\n], function( i, type ) {\n\tjQuery.fn[ type ] = function( fn ) {\n\t\treturn this.on( type, fn );\n\t};\n} );\n\n\n\n\njQuery.expr.pseudos.animated = function( elem ) {\n\treturn jQuery.grep( jQuery.timers, function( fn ) {\n\t\treturn elem === fn.elem;\n\t} ).length;\n};\n\n\n\n\njQuery.offset = {\n\tsetOffset: function( elem, options, i ) {\n\t\tvar curPosition, curLeft, curCSSTop, curTop, curOffset, curCSSLeft, calculatePosition,\n\t\t\tposition = jQuery.css( elem, "position" ),\n\t\t\tcurElem = jQuery( elem ),\n\t\t\tprops = {};\n\n\t\t// Set position first, in-case top/left are set even on static elem\n\t\tif ( position === "static" ) {\n\t\t\telem.style.position = "relative";\n\t\t}\n\n\t\tcurOffset = curElem.offset();\n\t\tcurCSSTop = jQuery.css( elem, "top" );\n\t\tcurCSSLeft = jQuery.css( elem, "left" );\n\t\tcalculatePosition = ( position === "absolute" || position === "fixed" ) &&\n\t\t\t( curCSSTop + curCSSLeft ).indexOf( "auto" ) > -1;\n\n\t\t// Need to be able to calculate position if either\n\t\t// top or left is auto and position is either absolute or fixed\n\t\tif ( calculatePosition ) {\n\t\t\tcurPosition = curElem.position();\n\t\t\tcurTop = curPosition.top;\n\t\t\tcurLeft = curPosition.left;\n\n\t\t} else {\n\t\t\tcurTop = parseFloat( curCSSTop ) || 0;\n\t\t\tcurLeft = parseFloat( curCSSLeft ) || 0;\n\t\t}\n\n\t\tif ( isFunction( options ) ) {\n\n\t\t\t// Use jQuery.extend here to allow modification of coordinates argument (gh-1848)\n\t\t\toptions = options.call( elem, i, jQuery.extend( {}, curOffset ) );\n\t\t}\n\n\t\tif ( options.top != null ) {\n\t\t\tprops.top = ( options.top - curOffset.top ) + curTop;\n\t\t}\n\t\tif ( options.left != null ) {\n\t\t\tprops.left = ( options.left - curOffset.left ) + curLeft;\n\t\t}\n\n\t\tif ( "using" in options ) {\n\t\t\toptions.using.call( elem, props );\n\n\t\t} else {\n\t\t\tcurElem.css( props );\n\t\t}\n\t}\n};\n\njQuery.fn.extend( {\n\n\t// offset() relates an element\'s border box to the document origin\n\toffset: function( options ) {\n\n\t\t// Preserve chaining for setter\n\t\tif ( arguments.length ) {\n\t\t\treturn options === undefined ?\n\t\t\t\tthis :\n\t\t\t\tthis.each( function( i ) {\n\t\t\t\t\tjQuery.offset.setOffset( this, options, i );\n\t\t\t\t} );\n\t\t}\n\n\t\tvar rect, win,\n\t\t\telem = this[ 0 ];\n\n\t\tif ( !elem ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Return zeros for disconnected and hidden (display: none) elements (gh-2310)\n\t\t// Support: IE <=11 only\n\t\t// Running getBoundingClientRect on a\n\t\t// disconnected node in IE throws an error\n\t\tif ( !elem.getClientRects().length ) {\n\t\t\treturn { top: 0, left: 0 };\n\t\t}\n\n\t\t// Get document-relative position by adding viewport scroll to viewport-relative gBCR\n\t\trect = elem.getBoundingClientRect();\n\t\twin = elem.ownerDocument.defaultView;\n\t\treturn {\n\t\t\ttop: rect.top + win.pageYOffset,\n\t\t\tleft: rect.left + win.pageXOffset\n\t\t};\n\t},\n\n\t// position() relates an element\'s margin box to its offset parent\'s padding box\n\t// This corresponds to the behavior of CSS absolute positioning\n\tposition: function() {\n\t\tif ( !this[ 0 ] ) {\n\t\t\treturn;\n\t\t}\n\n\t\tvar offsetParent, offset, doc,\n\t\t\telem = this[ 0 ],\n\t\t\tparentOffset = { top: 0, left: 0 };\n\n\t\t// position:fixed elements are offset from the viewport, which itself always has zero offset\n\t\tif ( jQuery.css( elem, "position" ) === "fixed" ) {\n\n\t\t\t// Assume position:fixed implies availability of getBoundingClientRect\n\t\t\toffset = elem.getBoundingClientRect();\n\n\t\t} else {\n\t\t\toffset = this.offset();\n\n\t\t\t// Account for the *real* offset parent, which can be the document or its root element\n\t\t\t// when a statically positioned element is identified\n\t\t\tdoc = elem.ownerDocument;\n\t\t\toffsetParent = elem.offsetParent || doc.documentElement;\n\t\t\twhile ( offsetParent &&\n\t\t\t\t( offsetParent === doc.body || offsetParent === doc.documentElement ) &&\n\t\t\t\tjQuery.css( offsetParent, "position" ) === "static" ) {\n\n\t\t\t\toffsetParent = offsetParent.parentNode;\n\t\t\t}\n\t\t\tif ( offsetParent && offsetParent !== elem && offsetParent.nodeType === 1 ) {\n\n\t\t\t\t// Incorporate borders into its offset, since they are outside its content origin\n\t\t\t\tparentOffset = jQuery( offsetParent ).offset();\n\t\t\t\tparentOffset.top += jQuery.css( offsetParent, "borderTopWidth", true );\n\t\t\t\tparentOffset.left += jQuery.css( offsetParent, "borderLeftWidth", true );\n\t\t\t}\n\t\t}\n\n\t\t// Subtract parent offsets and element margins\n\t\treturn {\n\t\t\ttop: offset.top - parentOffset.top - jQuery.css( elem, "marginTop", true ),\n\t\t\tleft: offset.left - parentOffset.left - jQuery.css( elem, "marginLeft", true )\n\t\t};\n\t},\n\n\t// This method will return documentElement in the following cases:\n\t// 1) For the element inside the iframe without offsetParent, this method will return\n\t//    documentElement of the parent window\n\t// 2) For the hidden or detached element\n\t// 3) For body or html element, i.e. in case of the html node - it will return itself\n\t//\n\t// but those exceptions were never presented as a real life use-cases\n\t// and might be considered as more preferable results.\n\t//\n\t// This logic, however, is not guaranteed and can change at any point in the future\n\toffsetParent: function() {\n\t\treturn this.map( function() {\n\t\t\tvar offsetParent = this.offsetParent;\n\n\t\t\twhile ( offsetParent && jQuery.css( offsetParent, "position" ) === "static" ) {\n\t\t\t\toffsetParent = offsetParent.offsetParent;\n\t\t\t}\n\n\t\t\treturn offsetParent || documentElement;\n\t\t} );\n\t}\n} );\n\n// Create scrollLeft and scrollTop methods\njQuery.each( { scrollLeft: "pageXOffset", scrollTop: "pageYOffset" }, function( method, prop ) {\n\tvar top = "pageYOffset" === prop;\n\n\tjQuery.fn[ method ] = function( val ) {\n\t\treturn access( this, function( elem, method, val ) {\n\n\t\t\t// Coalesce documents and windows\n\t\t\tvar win;\n\t\t\tif ( isWindow( elem ) ) {\n\t\t\t\twin = elem;\n\t\t\t} else if ( elem.nodeType === 9 ) {\n\t\t\t\twin = elem.defaultView;\n\t\t\t}\n\n\t\t\tif ( val === undefined ) {\n\t\t\t\treturn win ? win[ prop ] : elem[ method ];\n\t\t\t}\n\n\t\t\tif ( win ) {\n\t\t\t\twin.scrollTo(\n\t\t\t\t\t!top ? val : win.pageXOffset,\n\t\t\t\t\ttop ? val : win.pageYOffset\n\t\t\t\t);\n\n\t\t\t} else {\n\t\t\t\telem[ method ] = val;\n\t\t\t}\n\t\t}, method, val, arguments.length );\n\t};\n} );\n\n// Support: Safari <=7 - 9.1, Chrome <=37 - 49\n// Add the top/left cssHooks using jQuery.fn.position\n// Webkit bug: https://bugs.webkit.org/show_bug.cgi?id=29084\n// Blink bug: https://bugs.chromium.org/p/chromium/issues/detail?id=589347\n// getComputedStyle returns percent when specified for top/left/bottom/right;\n// rather than make the css module depend on the offset module, just check for it here\njQuery.each( [ "top", "left" ], function( i, prop ) {\n\tjQuery.cssHooks[ prop ] = addGetHookIf( support.pixelPosition,\n\t\tfunction( elem, computed ) {\n\t\t\tif ( computed ) {\n\t\t\t\tcomputed = curCSS( elem, prop );\n\n\t\t\t\t// If curCSS returns percentage, fallback to offset\n\t\t\t\treturn rnumnonpx.test( computed ) ?\n\t\t\t\t\tjQuery( elem ).position()[ prop ] + "px" :\n\t\t\t\t\tcomputed;\n\t\t\t}\n\t\t}\n\t);\n} );\n\n\n// Create innerHeight, innerWidth, height, width, outerHeight and outerWidth methods\njQuery.each( { Height: "height", Width: "width" }, function( name, type ) {\n\tjQuery.each( { padding: "inner" + name, content: type, "": "outer" + name },\n\t\tfunction( defaultExtra, funcName ) {\n\n\t\t// Margin is only for outerHeight, outerWidth\n\t\tjQuery.fn[ funcName ] = function( margin, value ) {\n\t\t\tvar chainable = arguments.length && ( defaultExtra || typeof margin !== "boolean" ),\n\t\t\t\textra = defaultExtra || ( margin === true || value === true ? "margin" : "border" );\n\n\t\t\treturn access( this, function( elem, type, value ) {\n\t\t\t\tvar doc;\n\n\t\t\t\tif ( isWindow( elem ) ) {\n\n\t\t\t\t\t// $( window ).outerWidth/Height return w/h including scrollbars (gh-1729)\n\t\t\t\t\treturn funcName.indexOf( "outer" ) === 0 ?\n\t\t\t\t\t\telem[ "inner" + name ] :\n\t\t\t\t\t\telem.document.documentElement[ "client" + name ];\n\t\t\t\t}\n\n\t\t\t\t// Get document width or height\n\t\t\t\tif ( elem.nodeType === 9 ) {\n\t\t\t\t\tdoc = elem.documentElement;\n\n\t\t\t\t\t// Either scroll[Width/Height] or offset[Width/Height] or client[Width/Height],\n\t\t\t\t\t// whichever is greatest\n\t\t\t\t\treturn Math.max(\n\t\t\t\t\t\telem.body[ "scroll" + name ], doc[ "scroll" + name ],\n\t\t\t\t\t\telem.body[ "offset" + name ], doc[ "offset" + name ],\n\t\t\t\t\t\tdoc[ "client" + name ]\n\t\t\t\t\t);\n\t\t\t\t}\n\n\t\t\t\treturn value === undefined ?\n\n\t\t\t\t\t// Get width or height on the element, requesting but not forcing parseFloat\n\t\t\t\t\tjQuery.css( elem, type, extra ) :\n\n\t\t\t\t\t// Set width or height on the element\n\t\t\t\t\tjQuery.style( elem, type, value, extra );\n\t\t\t}, type, chainable ? margin : undefined, chainable );\n\t\t};\n\t} );\n} );\n\n\njQuery.each( ( "blur focus focusin focusout resize scroll click dblclick " +\n\t"mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave " +\n\t"change select submit keydown keypress keyup contextmenu" ).split( " " ),\n\tfunction( i, name ) {\n\n\t// Handle event binding\n\tjQuery.fn[ name ] = function( data, fn ) {\n\t\treturn arguments.length > 0 ?\n\t\t\tthis.on( name, null, data, fn ) :\n\t\t\tthis.trigger( name );\n\t};\n} );\n\njQuery.fn.extend( {\n\thover: function( fnOver, fnOut ) {\n\t\treturn this.mouseenter( fnOver ).mouseleave( fnOut || fnOver );\n\t}\n} );\n\n\n\n\njQuery.fn.extend( {\n\n\tbind: function( types, data, fn ) {\n\t\treturn this.on( types, null, data, fn );\n\t},\n\tunbind: function( types, fn ) {\n\t\treturn this.off( types, null, fn );\n\t},\n\n\tdelegate: function( selector, types, data, fn ) {\n\t\treturn this.on( types, selector, data, fn );\n\t},\n\tundelegate: function( selector, types, fn ) {\n\n\t\t// ( namespace ) or ( selector, types [, fn] )\n\t\treturn arguments.length === 1 ?\n\t\t\tthis.off( selector, "**" ) :\n\t\t\tthis.off( types, selector || "**", fn );\n\t}\n} );\n\n// Bind a function to a context, optionally partially applying any\n// arguments.\n// jQuery.proxy is deprecated to promote standards (specifically Function#bind)\n// However, it is not slated for removal any time soon\njQuery.proxy = function( fn, context ) {\n\tvar tmp, args, proxy;\n\n\tif ( typeof context === "string" ) {\n\t\ttmp = fn[ context ];\n\t\tcontext = fn;\n\t\tfn = tmp;\n\t}\n\n\t// Quick check to determine if target is callable, in the spec\n\t// this throws a TypeError, but we will just return undefined.\n\tif ( !isFunction( fn ) ) {\n\t\treturn undefined;\n\t}\n\n\t// Simulated bind\n\targs = slice.call( arguments, 2 );\n\tproxy = function() {\n\t\treturn fn.apply( context || this, args.concat( slice.call( arguments ) ) );\n\t};\n\n\t// Set the guid of unique handler to the same of original handler, so it can be removed\n\tproxy.guid = fn.guid = fn.guid || jQuery.guid++;\n\n\treturn proxy;\n};\n\njQuery.holdReady = function( hold ) {\n\tif ( hold ) {\n\t\tjQuery.readyWait++;\n\t} else {\n\t\tjQuery.ready( true );\n\t}\n};\njQuery.isArray = Array.isArray;\njQuery.parseJSON = JSON.parse;\njQuery.nodeName = nodeName;\njQuery.isFunction = isFunction;\njQuery.isWindow = isWindow;\njQuery.camelCase = camelCase;\njQuery.type = toType;\n\njQuery.now = Date.now;\n\njQuery.isNumeric = function( obj ) {\n\n\t// As of jQuery 3.0, isNumeric is limited to\n\t// strings and numbers (primitives or objects)\n\t// that can be coerced to finite numbers (gh-2662)\n\tvar type = jQuery.type( obj );\n\treturn ( type === "number" || type === "string" ) &&\n\n\t\t// parseFloat NaNs numeric-cast false positives ("")\n\t\t// ...but misinterprets leading-number strings, particularly hex literals ("0x...")\n\t\t// subtraction forces infinities to NaN\n\t\t!isNaN( obj - parseFloat( obj ) );\n};\n\n\n\n\n// Register as a named AMD module, since jQuery can be concatenated with other\n// files that may use define, but not via a proper concatenation script that\n// understands anonymous AMD modules. A named AMD is safest and most robust\n// way to register. Lowercase jquery is used because AMD module names are\n// derived from file names, and jQuery is normally delivered in a lowercase\n// file name. Do this after creating the global so that if an AMD module wants\n// to call noConflict to hide this version of jQuery, it will work.\n\n// Note that for maximum portability, libraries that are not jQuery should\n// declare themselves as anonymous modules, and avoid setting a global if an\n// AMD loader is present. jQuery is a special case. For more information, see\n// https://github.com/jrburke/requirejs/wiki/Updating-existing-libraries#wiki-anon\n\nif ( true ) {\n\t!(__WEBPACK_AMD_DEFINE_ARRAY__ = [], __WEBPACK_AMD_DEFINE_RESULT__ = (function() {\n\t\treturn jQuery;\n\t}).apply(exports, __WEBPACK_AMD_DEFINE_ARRAY__),\n\t\t\t\t__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));\n}\n\n\n\n\nvar\n\n\t// Map over jQuery in case of overwrite\n\t_jQuery = window.jQuery,\n\n\t// Map over the $ in case of overwrite\n\t_$ = window.$;\n\njQuery.noConflict = function( deep ) {\n\tif ( window.$ === jQuery ) {\n\t\twindow.$ = _$;\n\t}\n\n\tif ( deep && window.jQuery === jQuery ) {\n\t\twindow.jQuery = _jQuery;\n\t}\n\n\treturn jQuery;\n};\n\n// Expose jQuery and $ identifiers, even in AMD\n// (#7102#comment:10, https://github.com/jquery/jquery/pull/557)\n// and CommonJS for browser emulators (#13566)\nif ( !noGlobal ) {\n\twindow.jQuery = window.$ = jQuery;\n}\n\n\n\n\nreturn jQuery;\n} );\n\n\n//# sourceURL=webpack:///./node_modules/jquery/dist/jquery.js?')},"./src/public/js/client/delete-items.js":[m
[32m+[m[32m/*! no static exports found */function node_modulesJqueryDistJqueryJs(module,exports,__webpack_require__){eval('var __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;/*!\n * jQuery JavaScript Library v3.4.0\n * https://jquery.com/\n *\n * Includes Sizzle.js\n * https://sizzlejs.com/\n *\n * Copyright JS Foundation and other contributors\n * Released under the MIT license\n * https://jquery.org/license\n *\n * Date: 2019-04-10T19:48Z\n */\n( function( global, factory ) {\n\n\t"use strict";\n\n\tif (  true && typeof module.exports === "object" ) {\n\n\t\t// For CommonJS and CommonJS-like environments where a proper `window`\n\t\t// is present, execute the factory and get jQuery.\n\t\t// For environments that do not have a `window` with a `document`\n\t\t// (such as Node.js), expose a factory as module.exports.\n\t\t// This accentuates the need for the creation of a real `window`.\n\t\t// e.g. var jQuery = require("jquery")(window);\n\t\t// See ticket #14549 for more info.\n\t\tmodule.exports = global.document ?\n\t\t\tfactory( global, true ) :\n\t\t\tfunction( w ) {\n\t\t\t\tif ( !w.document ) {\n\t\t\t\t\tthrow new Error( "jQuery requires a window with a document" );\n\t\t\t\t}\n\t\t\t\treturn factory( w );\n\t\t\t};\n\t} else {\n\t\tfactory( global );\n\t}\n\n// Pass this if window is not defined yet\n} )( typeof window !== "undefined" ? window : this, function( window, noGlobal ) {\n\n// Edge <= 12 - 13+, Firefox <=18 - 45+, IE 10 - 11, Safari 5.1 - 9+, iOS 6 - 9.1\n// throw exceptions when non-strict code (e.g., ASP.NET 4.5) accesses strict mode\n// arguments.callee.caller (trac-13335). But as of jQuery 3.0 (2016), strict mode should be common\n// enough that all such attempts are guarded in a try block.\n"use strict";\n\nvar arr = [];\n\nvar document = window.document;\n\nvar getProto = Object.getPrototypeOf;\n\nvar slice = arr.slice;\n\nvar concat = arr.concat;\n\nvar push = arr.push;\n\nvar indexOf = arr.indexOf;\n\nvar class2type = {};\n\nvar toString = class2type.toString;\n\nvar hasOwn = class2type.hasOwnProperty;\n\nvar fnToString = hasOwn.toString;\n\nvar ObjectFunctionString = fnToString.call( Object );\n\nvar support = {};\n\nvar isFunction = function isFunction( obj ) {\n\n      // Support: Chrome <=57, Firefox <=52\n      // In some browsers, typeof returns "function" for HTML <object> elements\n      // (i.e., `typeof document.createElement( "object" ) === "function"`).\n      // We don\'t want to classify *any* DOM node as a function.\n      return typeof obj === "function" && typeof obj.nodeType !== "number";\n  };\n\n\nvar isWindow = function isWindow( obj ) {\n\t\treturn obj != null && obj === obj.window;\n\t};\n\n\n\n\n\tvar preservedScriptAttributes = {\n\t\ttype: true,\n\t\tsrc: true,\n\t\tnonce: true,\n\t\tnoModule: true\n\t};\n\n\tfunction DOMEval( code, node, doc ) {\n\t\tdoc = doc || document;\n\n\t\tvar i, val,\n\t\t\tscript = doc.createElement( "script" );\n\n\t\tscript.text = code;\n\t\tif ( node ) {\n\t\t\tfor ( i in preservedScriptAttributes ) {\n\n\t\t\t\t// Support: Firefox 64+, Edge 18+\n\t\t\t\t// Some browsers don\'t support the "nonce" property on scripts.\n\t\t\t\t// On the other hand, just using `getAttribute` is not enough as\n\t\t\t\t// the `nonce` attribute is reset to an empty string whenever it\n\t\t\t\t// becomes browsing-context connected.\n\t\t\t\t// See https://github.com/whatwg/html/issues/2369\n\t\t\t\t// See https://html.spec.whatwg.org/#nonce-attributes\n\t\t\t\t// The `node.getAttribute` check was added for the sake of\n\t\t\t\t// `jQuery.globalEval` so that it can fake a nonce-containing node\n\t\t\t\t// via an object.\n\t\t\t\tval = node[ i ] || node.getAttribute && node.getAttribute( i );\n\t\t\t\tif ( val ) {\n\t\t\t\t\tscript.setAttribute( i, val );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t\tdoc.head.appendChild( script ).parentNode.removeChild( script );\n\t}\n\n\nfunction toType( obj ) {\n\tif ( obj == null ) {\n\t\treturn obj + "";\n\t}\n\n\t// Support: Android <=2.3 only (functionish RegExp)\n\treturn typeof obj === "object" || typeof obj === "function" ?\n\t\tclass2type[ toString.call( obj ) ] || "object" :\n\t\ttypeof obj;\n}\n/* global Symbol */\n// Defining this global in .eslintrc.json would create a danger of using the global\n// unguarded in another place, it seems safer to define global only for this module\n\n\n\nvar\n\tversion = "3.4.0",\n\n\t// Define a local copy of jQuery\n\tjQuery = function( selector, context ) {\n\n\t\t// The jQuery object is actually just the init constructor \'enhanced\'\n\t\t// Need init if jQuery is called (just allow error to be thrown if not included)\n\t\treturn new jQuery.fn.init( selector, context );\n\t},\n\n\t// Support: Android <=4.0 only\n\t// Make sure we trim BOM and NBSP\n\trtrim = /^[\\s\\uFEFF\\xA0]+|[\\s\\uFEFF\\xA0]+$/g;\n\njQuery.fn = jQuery.prototype = {\n\n\t// The current version of jQuery being used\n\tjquery: version,\n\n\tconstructor: jQuery,\n\n\t// The default length of a jQuery object is 0\n\tlength: 0,\n\n\ttoArray: function() {\n\t\treturn slice.call( this );\n\t},\n\n\t// Get the Nth element in the matched element set OR\n\t// Get the whole matched element set as a clean array\n\tget: function( num ) {\n\n\t\t// Return all the elements in a clean array\n\t\tif ( num == null ) {\n\t\t\treturn slice.call( this );\n\t\t}\n\n\t\t// Return just the one element from the set\n\t\treturn num < 0 ? this[ num + this.length ] : this[ num ];\n\t},\n\n\t// Take an array of elements and push it onto the stack\n\t// (returning the new matched element set)\n\tpushStack: function( elems ) {\n\n\t\t// Build a new jQuery matched element set\n\t\tvar ret = jQuery.merge( this.constructor(), elems );\n\n\t\t// Add the old object onto the stack (as a reference)\n\t\tret.prevObject = this;\n\n\t\t// Return the newly-formed element set\n\t\treturn ret;\n\t},\n\n\t// Execute a callback for every element in the matched set.\n\teach: function( callback ) {\n\t\treturn jQuery.each( this, callback );\n\t},\n\n\tmap: function( callback ) {\n\t\treturn this.pushStack( jQuery.map( this, function( elem, i ) {\n\t\t\treturn callback.call( elem, i, elem );\n\t\t} ) );\n\t},\n\n\tslice: function() {\n\t\treturn this.pushStack( slice.apply( this, arguments ) );\n\t},\n\n\tfirst: function() {\n\t\treturn this.eq( 0 );\n\t},\n\n\tlast: function() {\n\t\treturn this.eq( -1 );\n\t},\n\n\teq: function( i ) {\n\t\tvar len = this.length,\n\t\t\tj = +i + ( i < 0 ? len : 0 );\n\t\treturn this.pushStack( j >= 0 && j < len ? [ this[ j ] ] : [] );\n\t},\n\n\tend: function() {\n\t\treturn this.prevObject || this.constructor();\n\t},\n\n\t// For internal use only.\n\t// Behaves like an Array\'s method, not like a jQuery method.\n\tpush: push,\n\tsort: arr.sort,\n\tsplice: arr.splice\n};\n\njQuery.extend = jQuery.fn.extend = function() {\n\tvar options, name, src, copy, copyIsArray, clone,\n\t\ttarget = arguments[ 0 ] || {},\n\t\ti = 1,\n\t\tlength = arguments.length,\n\t\tdeep = false;\n\n\t// Handle a deep copy situation\n\tif ( typeof target === "boolean" ) {\n\t\tdeep = target;\n\n\t\t// Skip the boolean and the target\n\t\ttarget = arguments[ i ] || {};\n\t\ti++;\n\t}\n\n\t// Handle case when target is a string or something (possible in deep copy)\n\tif ( typeof target !== "object" && !isFunction( target ) ) {\n\t\ttarget = {};\n\t}\n\n\t// Extend jQuery itself if only one argument is passed\n\tif ( i === length ) {\n\t\ttarget = this;\n\t\ti--;\n\t}\n\n\tfor ( ; i < length; i++ ) {\n\n\t\t// Only deal with non-null/undefined values\n\t\tif ( ( options = arguments[ i ] ) != null ) {\n\n\t\t\t// Extend the base object\n\t\t\tfor ( name in options ) {\n\t\t\t\tcopy = options[ name ];\n\n\t\t\t\t// Prevent Object.prototype pollution\n\t\t\t\t// Prevent never-ending loop\n\t\t\t\tif ( name === "__proto__" || target === copy ) {\n\t\t\t\t\tcontinue;\n\t\t\t\t}\n\n\t\t\t\t// Recurse if we\'re merging plain objects or arrays\n\t\t\t\tif ( deep && copy && ( jQuery.isPlainObject( copy ) ||\n\t\t\t\t\t( copyIsArray = Array.isArray( copy ) ) ) ) {\n\t\t\t\t\tsrc = target[ name ];\n\n\t\t\t\t\t// Ensure proper type for the source value\n\t\t\t\t\tif ( copyIsArray && !Array.isArray( src ) ) {\n\t\t\t\t\t\tclone = [];\n\t\t\t\t\t} else if ( !copyIsArray && !jQuery.isPlainObject( src ) ) {\n\t\t\t\t\t\tclone = {};\n\t\t\t\t\t} else {\n\t\t\t\t\t\tclone = src;\n\t\t\t\t\t}\n\t\t\t\t\tcopyIsArray = false;\n\n\t\t\t\t\t// Never move original objects, clone them\n\t\t\t\t\ttarget[ name ] = jQuery.extend( deep, clone, copy );\n\n\t\t\t\t// Don\'t bring in undefined values\n\t\t\t\t} else if ( copy !== undefined ) {\n\t\t\t\t\ttarget[ name ] = copy;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\t// Return the modified object\n\treturn target;\n};\n\njQuery.extend( {\n\n\t// Unique for each copy of jQuery on the page\n\texpando: "jQuery" + ( version + Math.random() ).replace( /\\D/g, "" ),\n\n\t// Assume jQuery is ready without the ready module\n\tisReady: true,\n\n\terror: function( msg ) {\n\t\tthrow new Error( msg );\n\t},\n\n\tnoop: function() {},\n\n\tisPlainObject: function( obj ) {\n\t\tvar proto, Ctor;\n\n\t\t// Detect obvious negatives\n\t\t// Use toString instead of jQuery.type to catch host objects\n\t\tif ( !obj || toString.call( obj ) !== "[object Object]" ) {\n\t\t\treturn false;\n\t\t}\n\n\t\tproto = getProto( obj );\n\n\t\t// Objects with no prototype (e.g., `Object.create( null )`) are plain\n\t\tif ( !proto ) {\n\t\t\treturn true;\n\t\t}\n\n\t\t// Objects with prototype are plain iff they were constructed by a global Object function\n\t\tCtor = hasOwn.call( proto, "constructor" ) && proto.constructor;\n\t\treturn typeof Ctor === "function" && fnToString.call( Ctor ) === ObjectFunctionString;\n\t},\n\n\tisEmptyObject: function( obj ) {\n\t\tvar name;\n\n\t\tfor ( name in obj ) {\n\t\t\treturn false;\n\t\t}\n\t\treturn true;\n\t},\n\n\t// Evaluates a script in a global context\n\tglobalEval: function( code, options ) {\n\t\tDOMEval( code, { nonce: options && options.nonce } );\n\t},\n\n\teach: function( obj, callback ) {\n\t\tvar length, i = 0;\n\n\t\tif ( isArrayLike( obj ) ) {\n\t\t\tlength = obj.length;\n\t\t\tfor ( ; i < length; i++ ) {\n\t\t\t\tif ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {\n\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t}\n\t\t} else {\n\t\t\tfor ( i in obj ) {\n\t\t\t\tif ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {\n\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn obj;\n\t},\n\n\t// Support: Android <=4.0 only\n\ttrim: function( text ) {\n\t\treturn text == null ?\n\t\t\t"" :\n\t\t\t( text + "" ).replace( rtrim, "" );\n\t},\n\n\t// results is for internal usage only\n\tmakeArray: function( arr, results ) {\n\t\tvar ret = results || [];\n\n\t\tif ( arr != null ) {\n\t\t\tif ( isArrayLike( Object( arr ) ) ) {\n\t\t\t\tjQuery.merge( ret,\n\t\t\t\t\ttypeof arr === "string" ?\n\t\t\t\t\t[ arr ] : arr\n\t\t\t\t);\n\t\t\t} else {\n\t\t\t\tpush.call( ret, arr );\n\t\t\t}\n\t\t}\n\n\t\treturn ret;\n\t},\n\n\tinArray: function( elem, arr, i ) {\n\t\treturn arr == null ? -1 : indexOf.call( arr, elem, i );\n\t},\n\n\t// Support: Android <=4.0 only, PhantomJS 1 only\n\t// push.apply(_, arraylike) throws on ancient WebKit\n\tmerge: function( first, second ) {\n\t\tvar len = +second.length,\n\t\t\tj = 0,\n\t\t\ti = first.length;\n\n\t\tfor ( ; j < len; j++ ) {\n\t\t\tfirst[ i++ ] = second[ j ];\n\t\t}\n\n\t\tfirst.length = i;\n\n\t\treturn first;\n\t},\n\n\tgrep: function( elems, callback, invert ) {\n\t\tvar callbackInverse,\n\t\t\tmatches = [],\n\t\t\ti = 0,\n\t\t\tlength = elems.length,\n\t\t\tcallbackExpect = !invert;\n\n\t\t// Go through the array, only saving the items\n\t\t// that pass the validator function\n\t\tfor ( ; i < length; i++ ) {\n\t\t\tcallbackInverse = !callback( elems[ i ], i );\n\t\t\tif ( callbackInverse !== callbackExpect ) {\n\t\t\t\tmatches.push( elems[ i ] );\n\t\t\t}\n\t\t}\n\n\t\treturn matches;\n\t},\n\n\t// arg is for internal usage only\n\tmap: function( elems, callback, arg ) {\n\t\tvar length, value,\n\t\t\ti = 0,\n\t\t\tret = [];\n\n\t\t// Go through the array, translating each of the items to their new values\n\t\tif ( isArrayLike( elems ) ) {\n\t\t\tlength = elems.length;\n\t\t\tfor ( ; i < length; i++ ) {\n\t\t\t\tvalue = callback( elems[ i ], i, arg );\n\n\t\t\t\tif ( value != null ) {\n\t\t\t\t\tret.push( value );\n\t\t\t\t}\n\t\t\t}\n\n\t\t// Go through every key on the object,\n\t\t} else {\n\t\t\tfor ( i in elems ) {\n\t\t\t\tvalue = callback( elems[ i ], i, arg );\n\n\t\t\t\tif ( value != null ) {\n\t\t\t\t\tret.push( value );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\t// Flatten any nested arrays\n\t\treturn concat.apply( [], ret );\n\t},\n\n\t// A global GUID counter for objects\n\tguid: 1,\n\n\t// jQuery.support is not used in Core but other projects attach their\n\t// properties to it so it needs to exist.\n\tsupport: support\n} );\n\nif ( typeof Symbol === "function" ) {\n\tjQuery.fn[ Symbol.iterator ] = arr[ Symbol.iterator ];\n}\n\n// Populate the class2type map\njQuery.each( "Boolean Number String Function Array Date RegExp Object Error Symbol".split( " " ),\nfunction( i, name ) {\n\tclass2type[ "[object " + name + "]" ] = name.toLowerCase();\n} );\n\nfunction isArrayLike( obj ) {\n\n\t// Support: real iOS 8.2 only (not reproducible in simulator)\n\t// `in` check used to prevent JIT error (gh-2145)\n\t// hasOwn isn\'t used here due to false negatives\n\t// regarding Nodelist length in IE\n\tvar length = !!obj && "length" in obj && obj.length,\n\t\ttype = toType( obj );\n\n\tif ( isFunction( obj ) || isWindow( obj ) ) {\n\t\treturn false;\n\t}\n\n\treturn type === "array" || length === 0 ||\n\t\ttypeof length === "number" && length > 0 && ( length - 1 ) in obj;\n}\nvar Sizzle =\n/*!\n * Sizzle CSS Selector Engine v2.3.4\n * https://sizzlejs.com/\n *\n * Copyright JS Foundation and other contributors\n * Released under the MIT license\n * https://js.foundation/\n *\n * Date: 2019-04-08\n */\n(function( window ) {\n\nvar i,\n\tsupport,\n\tExpr,\n\tgetText,\n\tisXML,\n\ttokenize,\n\tcompile,\n\tselect,\n\toutermostContext,\n\tsortInput,\n\thasDuplicate,\n\n\t// Local document vars\n\tsetDocument,\n\tdocument,\n\tdocElem,\n\tdocumentIsHTML,\n\trbuggyQSA,\n\trbuggyMatches,\n\tmatches,\n\tcontains,\n\n\t// Instance-specific data\n\texpando = "sizzle" + 1 * new Date(),\n\tpreferredDoc = window.document,\n\tdirruns = 0,\n\tdone = 0,\n\tclassCache = createCache(),\n\ttokenCache = createCache(),\n\tcompilerCache = createCache(),\n\tnonnativeSelectorCache = createCache(),\n\tsortOrder = function( a, b ) {\n\t\tif ( a === b ) {\n\t\t\thasDuplicate = true;\n\t\t}\n\t\treturn 0;\n\t},\n\n\t// Instance methods\n\thasOwn = ({}).hasOwnProperty,\n\tarr = [],\n\tpop = arr.pop,\n\tpush_native = arr.push,\n\tpush = arr.push,\n\tslice = arr.slice,\n\t// Use a stripped-down indexOf as it\'s faster than native\n\t// https://jsperf.com/thor-indexof-vs-for/5\n\tindexOf = function( list, elem ) {\n\t\tvar i = 0,\n\t\t\tlen = list.length;\n\t\tfor ( ; i < len; i++ ) {\n\t\t\tif ( list[i] === elem ) {\n\t\t\t\treturn i;\n\t\t\t}\n\t\t}\n\t\treturn -1;\n\t},\n\n\tbooleans = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped",\n\n\t// Regular expressions\n\n\t// http://www.w3.org/TR/css3-selectors/#whitespace\n\twhitespace = "[\\\\x20\\\\t\\\\r\\\\n\\\\f]",\n\n\t// http://www.w3.org/TR/CSS21/syndata.html#value-def-identifier\n\tidentifier = "(?:\\\\\\\\.|[\\\\w-]|[^\\0-\\\\xa0])+",\n\n\t// Attribute selectors: http://www.w3.org/TR/selectors/#attribute-selectors\n\tattributes = "\\\\[" + whitespace + "*(" + identifier + ")(?:" + whitespace +\n\t\t// Operator (capture 2)\n\t\t"*([*^$|!~]?=)" + whitespace +\n\t\t// "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"\n\t\t"*(?:\'((?:\\\\\\\\.|[^\\\\\\\\\'])*)\'|\\"((?:\\\\\\\\.|[^\\\\\\\\\\"])*)\\"|(" + identifier + "))|)" + whitespace +\n\t\t"*\\\\]",\n\n\tpseudos = ":(" + identifier + ")(?:\\\\((" +\n\t\t// To reduce the number of selectors needing tokenize in the preFilter, prefer arguments:\n\t\t// 1. quoted (capture 3; capture 4 or capture 5)\n\t\t"(\'((?:\\\\\\\\.|[^\\\\\\\\\'])*)\'|\\"((?:\\\\\\\\.|[^\\\\\\\\\\"])*)\\")|" +\n\t\t// 2. simple (capture 6)\n\t\t"((?:\\\\\\\\.|[^\\\\\\\\()[\\\\]]|" + attributes + ")*)|" +\n\t\t// 3. anything else (capture 2)\n\t\t".*" +\n\t\t")\\\\)|)",\n\n\t// Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter\n\trwhitespace = new RegExp( whitespace + "+", "g" ),\n\trtrim = new RegExp( "^" + whitespace + "+|((?:^|[^\\\\\\\\])(?:\\\\\\\\.)*)" + whitespace + "+$", "g" ),\n\n\trcomma = new RegExp( "^" + whitespace + "*," + whitespace + "*" ),\n\trcombinators = new RegExp( "^" + whitespace + "*([>+~]|" + whitespace + ")" + whitespace + "*" ),\n\trdescend = new RegExp( whitespace + "|>" ),\n\n\trpseudo = new RegExp( pseudos ),\n\tridentifier = new RegExp( "^" + identifier + "$" ),\n\n\tmatchExpr = {\n\t\t"ID": new RegExp( "^#(" + identifier + ")" ),\n\t\t"CLASS": new RegExp( "^\\\\.(" + identifier + ")" ),\n\t\t"TAG": new RegExp( "^(" + identifier + "|[*])" ),\n\t\t"ATTR": new RegExp( "^" + attributes ),\n\t\t"PSEUDO": new RegExp( "^" + pseudos ),\n\t\t"CHILD": new RegExp( "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\\\(" + whitespace +\n\t\t\t"*(even|odd|(([+-]|)(\\\\d*)n|)" + whitespace + "*(?:([+-]|)" + whitespace +\n\t\t\t"*(\\\\d+)|))" + whitespace + "*\\\\)|)", "i" ),\n\t\t"bool": new RegExp( "^(?:" + booleans + ")$", "i" ),\n\t\t// For use in libraries implementing .is()\n\t\t// We use this for POS matching in `select`\n\t\t"needsContext": new RegExp( "^" + whitespace + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\\\(" +\n\t\t\twhitespace + "*((?:-\\\\d)?\\\\d*)" + whitespace + "*\\\\)|)(?=[^-]|$)", "i" )\n\t},\n\n\trhtml = /HTML$/i,\n\trinputs = /^(?:input|select|textarea|button)$/i,\n\trheader = /^h\\d$/i,\n\n\trnative = /^[^{]+\\{\\s*\\[native \\w/,\n\n\t// Easily-parseable/retrievable ID or TAG or CLASS selectors\n\trquickExpr = /^(?:#([\\w-]+)|(\\w+)|\\.([\\w-]+))$/,\n\n\trsibling = /[+~]/,\n\n\t// CSS escapes\n\t// http://www.w3.org/TR/CSS21/syndata.html#escaped-characters\n\trunescape = new RegExp( "\\\\\\\\([\\\\da-f]{1,6}" + whitespace + "?|(" + whitespace + ")|.)", "ig" ),\n\tfunescape = function( _, escaped, escapedWhitespace ) {\n\t\tvar high = "0x" + escaped - 0x10000;\n\t\t// NaN means non-codepoint\n\t\t// Support: Firefox<24\n\t\t// Workaround erroneous numeric interpretation of +"0x"\n\t\treturn high !== high || escapedWhitespace ?\n\t\t\tescaped :\n\t\t\thigh < 0 ?\n\t\t\t\t// BMP codepoint\n\t\t\t\tString.fromCharCode( high + 0x10000 ) :\n\t\t\t\t// Supplemental Plane codepoint (surrogate pair)\n\t\t\t\tString.fromCharCode( high >> 10 | 0xD800, high & 0x3FF | 0xDC00 );\n\t},\n\n\t// CSS string/identifier serialization\n\t// https://drafts.csswg.org/cssom/#common-serializing-idioms\n\trcssescape = /([\\0-\\x1f\\x7f]|^-?\\d)|^-$|[^\\0-\\x1f\\x7f-\\uFFFF\\w-]/g,\n\tfcssescape = function( ch, asCodePoint ) {\n\t\tif ( asCodePoint ) {\n\n\t\t\t// U+0000 NULL becomes U+FFFD REPLACEMENT CHARACTER\n\t\t\tif ( ch === "\\0" ) {\n\t\t\t\treturn "\\uFFFD";\n\t\t\t}\n\n\t\t\t// Control characters and (dependent upon position) numbers get escaped as code points\n\t\t\treturn ch.slice( 0, -1 ) + "\\\\" + ch.charCodeAt( ch.length - 1 ).toString( 16 ) + " ";\n\t\t}\n\n\t\t// Other potentially-special ASCII characters get backslash-escaped\n\t\treturn "\\\\" + ch;\n\t},\n\n\t// Used for iframes\n\t// See setDocument()\n\t// Removing the function wrapper causes a "Permission Denied"\n\t// error in IE\n\tunloadHandler = function() {\n\t\tsetDocument();\n\t},\n\n\tinDisabledFieldset = addCombinator(\n\t\tfunction( elem ) {\n\t\t\treturn elem.disabled === true && elem.nodeName.toLowerCase() === "fieldset";\n\t\t},\n\t\t{ dir: "parentNode", next: "legend" }\n\t);\n\n// Optimize for push.apply( _, NodeList )\ntry {\n\tpush.apply(\n\t\t(arr = slice.call( preferredDoc.childNodes )),\n\t\tpreferredDoc.childNodes\n\t);\n\t// Support: Android<4.0\n\t// Detect silently failing push.apply\n\tarr[ preferredDoc.childNodes.length ].nodeType;\n} catch ( e ) {\n\tpush = { apply: arr.length ?\n\n\t\t// Leverage slice if possible\n\t\tfunction( target, els ) {\n\t\t\tpush_native.apply( target, slice.call(els) );\n\t\t} :\n\n\t\t// Support: IE<9\n\t\t// Otherwise append directly\n\t\tfunction( target, els ) {\n\t\t\tvar j = target.length,\n\t\t\t\ti = 0;\n\t\t\t// Can\'t trust NodeList.length\n\t\t\twhile ( (target[j++] = els[i++]) ) {}\n\t\t\ttarget.length = j - 1;\n\t\t}\n\t};\n}\n\nfunction Sizzle( selector, context, results, seed ) {\n\tvar m, i, elem, nid, match, groups, newSelector,\n\t\tnewContext = context && context.ownerDocument,\n\n\t\t// nodeType defaults to 9, since context defaults to document\n\t\tnodeType = context ? context.nodeType : 9;\n\n\tresults = results || [];\n\n\t// Return early from calls with invalid selector or context\n\tif ( typeof selector !== "string" || !selector ||\n\t\tnodeType !== 1 && nodeType !== 9 && nodeType !== 11 ) {\n\n\t\treturn results;\n\t}\n\n\t// Try to shortcut find operations (as opposed to filters) in HTML documents\n\tif ( !seed ) {\n\n\t\tif ( ( context ? context.ownerDocument || context : preferredDoc ) !== document ) {\n\t\t\tsetDocument( context );\n\t\t}\n\t\tcontext = context || document;\n\n\t\tif ( documentIsHTML ) {\n\n\t\t\t// If the selector is sufficiently simple, try using a "get*By*" DOM method\n\t\t\t// (excepting DocumentFragment context, where the methods don\'t exist)\n\t\t\tif ( nodeType !== 11 && (match = rquickExpr.exec( selector )) ) {\n\n\t\t\t\t// ID selector\n\t\t\t\tif ( (m = match[1]) ) {\n\n\t\t\t\t\t// Document context\n\t\t\t\t\tif ( nodeType === 9 ) {\n\t\t\t\t\t\tif ( (elem = context.getElementById( m )) ) {\n\n\t\t\t\t\t\t\t// Support: IE, Opera, Webkit\n\t\t\t\t\t\t\t// TODO: identify versions\n\t\t\t\t\t\t\t// getElementById can match elements by name instead of ID\n\t\t\t\t\t\t\tif ( elem.id === m ) {\n\t\t\t\t\t\t\t\tresults.push( elem );\n\t\t\t\t\t\t\t\treturn results;\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\treturn results;\n\t\t\t\t\t\t}\n\n\t\t\t\t\t// Element context\n\t\t\t\t\t} else {\n\n\t\t\t\t\t\t// Support: IE, Opera, Webkit\n\t\t\t\t\t\t// TODO: identify versions\n\t\t\t\t\t\t// getElementById can match elements by name instead of ID\n\t\t\t\t\t\tif ( newContext && (elem = newContext.getElementById( m )) &&\n\t\t\t\t\t\t\tcontains( context, elem ) &&\n\t\t\t\t\t\t\telem.id === m ) {\n\n\t\t\t\t\t\t\tresults.push( elem );\n\t\t\t\t\t\t\treturn results;\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t// Type selector\n\t\t\t\t} else if ( match[2] ) {\n\t\t\t\t\tpush.apply( results, context.getElementsByTagName( selector ) );\n\t\t\t\t\treturn results;\n\n\t\t\t\t// Class selector\n\t\t\t\t} else if ( (m = match[3]) && support.getElementsByClassName &&\n\t\t\t\t\tcontext.getElementsByClassName ) {\n\n\t\t\t\t\tpush.apply( results, context.getElementsByClassName( m ) );\n\t\t\t\t\treturn results;\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Take advantage of querySelectorAll\n\t\t\tif ( support.qsa &&\n\t\t\t\t!nonnativeSelectorCache[ selector + " " ] &&\n\t\t\t\t(!rbuggyQSA || !rbuggyQSA.test( selector )) &&\n\n\t\t\t\t// Support: IE 8 only\n\t\t\t\t// Exclude object elements\n\t\t\t\t(nodeType !== 1 || context.nodeName.toLowerCase() !== "object") ) {\n\n\t\t\t\tnewSelector = selector;\n\t\t\t\tnewContext = context;\n\n\t\t\t\t// qSA considers elements outside a scoping root when evaluating child or\n\t\t\t\t// descendant combinators, which is not what we want.\n\t\t\t\t// In such cases, we work around the behavior by prefixing every selector in the\n\t\t\t\t// list with an ID selector referencing the scope context.\n\t\t\t\t// Thanks to Andrew Dupont for this technique.\n\t\t\t\tif ( nodeType === 1 && rdescend.test( selector ) ) {\n\n\t\t\t\t\t// Capture the context ID, setting it first if necessary\n\t\t\t\t\tif ( (nid = context.getAttribute( "id" )) ) {\n\t\t\t\t\t\tnid = nid.replace( rcssescape, fcssescape );\n\t\t\t\t\t} else {\n\t\t\t\t\t\tcontext.setAttribute( "id", (nid = expando) );\n\t\t\t\t\t}\n\n\t\t\t\t\t// Prefix every selector in the list\n\t\t\t\t\tgroups = tokenize( selector );\n\t\t\t\t\ti = groups.length;\n\t\t\t\t\twhile ( i-- ) {\n\t\t\t\t\t\tgroups[i] = "#" + nid + " " + toSelector( groups[i] );\n\t\t\t\t\t}\n\t\t\t\t\tnewSelector = groups.join( "," );\n\n\t\t\t\t\t// Expand context for sibling selectors\n\t\t\t\t\tnewContext = rsibling.test( selector ) && testContext( context.parentNode ) ||\n\t\t\t\t\t\tcontext;\n\t\t\t\t}\n\n\t\t\t\ttry {\n\t\t\t\t\tpush.apply( results,\n\t\t\t\t\t\tnewContext.querySelectorAll( newSelector )\n\t\t\t\t\t);\n\t\t\t\t\treturn results;\n\t\t\t\t} catch ( qsaError ) {\n\t\t\t\t\tnonnativeSelectorCache( selector, true );\n\t\t\t\t} finally {\n\t\t\t\t\tif ( nid === expando ) {\n\t\t\t\t\t\tcontext.removeAttribute( "id" );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\t// All others\n\treturn select( selector.replace( rtrim, "$1" ), context, results, seed );\n}\n\n/**\n * Create key-value caches of limited size\n * @returns {function(string, object)} Returns the Object data after storing it on itself with\n *\tproperty name the (space-suffixed) string and (if the cache is larger than Expr.cacheLength)\n *\tdeleting the oldest entry\n */\nfunction createCache() {\n\tvar keys = [];\n\n\tfunction cache( key, value ) {\n\t\t// Use (key + " ") to avoid collision with native prototype properties (see Issue #157)\n\t\tif ( keys.push( key + " " ) > Expr.cacheLength ) {\n\t\t\t// Only keep the most recent entries\n\t\t\tdelete cache[ keys.shift() ];\n\t\t}\n\t\treturn (cache[ key + " " ] = value);\n\t}\n\treturn cache;\n}\n\n/**\n * Mark a function for special use by Sizzle\n * @param {Function} fn The function to mark\n */\nfunction markFunction( fn ) {\n\tfn[ expando ] = true;\n\treturn fn;\n}\n\n/**\n * Support testing using an element\n * @param {Function} fn Passed the created element and returns a boolean result\n */\nfunction assert( fn ) {\n\tvar el = document.createElement("fieldset");\n\n\ttry {\n\t\treturn !!fn( el );\n\t} catch (e) {\n\t\treturn false;\n\t} finally {\n\t\t// Remove from its parent by default\n\t\tif ( el.parentNode ) {\n\t\t\tel.parentNode.removeChild( el );\n\t\t}\n\t\t// release memory in IE\n\t\tel = null;\n\t}\n}\n\n/**\n * Adds the same handler for all of the specified attrs\n * @param {String} attrs Pipe-separated list of attributes\n * @param {Function} handler The method that will be applied\n */\nfunction addHandle( attrs, handler ) {\n\tvar arr = attrs.split("|"),\n\t\ti = arr.length;\n\n\twhile ( i-- ) {\n\t\tExpr.attrHandle[ arr[i] ] = handler;\n\t}\n}\n\n/**\n * Checks document order of two siblings\n * @param {Element} a\n * @param {Element} b\n * @returns {Number} Returns less than 0 if a precedes b, greater than 0 if a follows b\n */\nfunction siblingCheck( a, b ) {\n\tvar cur = b && a,\n\t\tdiff = cur && a.nodeType === 1 && b.nodeType === 1 &&\n\t\t\ta.sourceIndex - b.sourceIndex;\n\n\t// Use IE sourceIndex if available on both nodes\n\tif ( diff ) {\n\t\treturn diff;\n\t}\n\n\t// Check if b follows a\n\tif ( cur ) {\n\t\twhile ( (cur = cur.nextSibling) ) {\n\t\t\tif ( cur === b ) {\n\t\t\t\treturn -1;\n\t\t\t}\n\t\t}\n\t}\n\n\treturn a ? 1 : -1;\n}\n\n/**\n * Returns a function to use in pseudos for input types\n * @param {String} type\n */\nfunction createInputPseudo( type ) {\n\treturn function( elem ) {\n\t\tvar name = elem.nodeName.toLowerCase();\n\t\treturn name === "input" && elem.type === type;\n\t};\n}\n\n/**\n * Returns a function to use in pseudos for buttons\n * @param {String} type\n */\nfunction createButtonPseudo( type ) {\n\treturn function( elem ) {\n\t\tvar name = elem.nodeName.toLowerCase();\n\t\treturn (name === "input" || name === "button") && elem.type === type;\n\t};\n}\n\n/**\n * Returns a function to use in pseudos for :enabled/:disabled\n * @param {Boolean} disabled true for :disabled; false for :enabled\n */\nfunction createDisabledPseudo( disabled ) {\n\n\t// Known :disabled false positives: fieldset[disabled] > legend:nth-of-type(n+2) :can-disable\n\treturn function( elem ) {\n\n\t\t// Only certain elements can match :enabled or :disabled\n\t\t// https://html.spec.whatwg.org/multipage/scripting.html#selector-enabled\n\t\t// https://html.spec.whatwg.org/multipage/scripting.html#selector-disabled\n\t\tif ( "form" in elem ) {\n\n\t\t\t// Check for inherited disabledness on relevant non-disabled elements:\n\t\t\t// * listed form-associated elements in a disabled fieldset\n\t\t\t//   https://html.spec.whatwg.org/multipage/forms.html#category-listed\n\t\t\t//   https://html.spec.whatwg.org/multipage/forms.html#concept-fe-disabled\n\t\t\t// * option elements in a disabled optgroup\n\t\t\t//   https://html.spec.whatwg.org/multipage/forms.html#concept-option-disabled\n\t\t\t// All such elements have a "form" property.\n\t\t\tif ( elem.parentNode && elem.disabled === false ) {\n\n\t\t\t\t// Option elements defer to a parent optgroup if present\n\t\t\t\tif ( "label" in elem ) {\n\t\t\t\t\tif ( "label" in elem.parentNode ) {\n\t\t\t\t\t\treturn elem.parentNode.disabled === disabled;\n\t\t\t\t\t} else {\n\t\t\t\t\t\treturn elem.disabled === disabled;\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\t// Support: IE 6 - 11\n\t\t\t\t// Use the isDisabled shortcut property to check for disabled fieldset ancestors\n\t\t\t\treturn elem.isDisabled === disabled ||\n\n\t\t\t\t\t// Where there is no isDisabled, check manually\n\t\t\t\t\t/* jshint -W018 */\n\t\t\t\t\telem.isDisabled !== !disabled &&\n\t\t\t\t\t\tinDisabledFieldset( elem ) === disabled;\n\t\t\t}\n\n\t\t\treturn elem.disabled === disabled;\n\n\t\t// Try to winnow out elements that can\'t be disabled before trusting the disabled property.\n\t\t// Some victims get caught in our net (label, legend, menu, track), but it shouldn\'t\n\t\t// even exist on them, let alone have a boolean value.\n\t\t} else if ( "label" in elem ) {\n\t\t\treturn elem.disabled === disabled;\n\t\t}\n\n\t\t// Remaining elements are neither :enabled nor :disabled\n\t\treturn false;\n\t};\n}\n\n/**\n * Returns a function to use in pseudos for positionals\n * @param {Function} fn\n */\nfunction createPositionalPseudo( fn ) {\n\treturn markFunction(function( argument ) {\n\t\targument = +argument;\n\t\treturn markFunction(function( seed, matches ) {\n\t\t\tvar j,\n\t\t\t\tmatchIndexes = fn( [], seed.length, argument ),\n\t\t\t\ti = matchIndexes.length;\n\n\t\t\t// Match elements found at the specified indexes\n\t\t\twhile ( i-- ) {\n\t\t\t\tif ( seed[ (j = matchIndexes[i]) ] ) {\n\t\t\t\t\tseed[j] = !(matches[j] = seed[j]);\n\t\t\t\t}\n\t\t\t}\n\t\t});\n\t});\n}\n\n/**\n * Checks a node for validity as a Sizzle context\n * @param {Element|Object=} context\n * @returns {Element|Object|Boolean} The input node if acceptable, otherwise a falsy value\n */\nfunction testContext( context ) {\n\treturn context && typeof context.getElementsByTagName !== "undefined" && context;\n}\n\n// Expose support vars for convenience\nsupport = Sizzle.support = {};\n\n/**\n * Detects XML nodes\n * @param {Element|Object} elem An element or a document\n * @returns {Boolean} True iff elem is a non-HTML XML node\n */\nisXML = Sizzle.isXML = function( elem ) {\n\tvar namespace = elem.namespaceURI,\n\t\tdocElem = (elem.ownerDocument || elem).documentElement;\n\n\t// Support: IE <=8\n\t// Assume HTML when documentElement doesn\'t yet exist, such as inside loading iframes\n\t// https://bugs.jquery.com/ticket/4833\n\treturn !rhtml.test( namespace || docElem && docElem.nodeName || "HTML" );\n};\n\n/**\n * Sets document-related variables once based on the current document\n * @param {Element|Object} [doc] An element or document object to use to set the document\n * @returns {Object} Returns the current document\n */\nsetDocument = Sizzle.setDocument = function( node ) {\n\tvar hasCompare, subWindow,\n\t\tdoc = node ? node.ownerDocument || node : preferredDoc;\n\n\t// Return early if doc is invalid or already selected\n\tif ( doc === document || doc.nodeType !== 9 || !doc.documentElement ) {\n\t\treturn document;\n\t}\n\n\t// Update global variables\n\tdocument = doc;\n\tdocElem = document.documentElement;\n\tdocumentIsHTML = !isXML( document );\n\n\t// Support: IE 9-11, Edge\n\t// Accessing iframe documents after unload throws "permission denied" errors (jQuery #13936)\n\tif ( preferredDoc !== document &&\n\t\t(subWindow = document.defaultView) && subWindow.top !== subWindow ) {\n\n\t\t// Support: IE 11, Edge\n\t\tif ( subWindow.addEventListener ) {\n\t\t\tsubWindow.addEventListener( "unload", unloadHandler, false );\n\n\t\t// Support: IE 9 - 10 only\n\t\t} else if ( subWindow.attachEvent ) {\n\t\t\tsubWindow.attachEvent( "onunload", unloadHandler );\n\t\t}\n\t}\n\n\t/* Attributes\n\t---------------------------------------------------------------------- */\n\n\t// Support: IE<8\n\t// Verify that getAttribute really returns attributes and not properties\n\t// (excepting IE8 booleans)\n\tsupport.attributes = assert(function( el ) {\n\t\tel.className = "i";\n\t\treturn !el.getAttribute("className");\n\t});\n\n\t/* getElement(s)By*\n\t---------------------------------------------------------------------- */\n\n\t// Check if getElementsByTagName("*") returns only elements\n\tsupport.getElementsByTagName = assert(function( el ) {\n\t\tel.appendChild( document.createComment("") );\n\t\treturn !el.getElementsByTagName("*").length;\n\t});\n\n\t// Support: IE<9\n\tsupport.getElementsByClassName = rnative.test( document.getElementsByClassName );\n\n\t// Support: IE<10\n\t// Check if getElementById returns elements by name\n\t// The broken getElementById methods don\'t pick up programmatically-set names,\n\t// so use a roundabout getElementsByName test\n\tsupport.getById = assert(function( el ) {\n\t\tdocElem.appendChild( el ).id = expando;\n\t\treturn !document.getElementsByName || !document.getElementsByName( expando ).length;\n\t});\n\n\t// ID filter and find\n\tif ( support.getById ) {\n\t\tExpr.filter["ID"] = function( id ) {\n\t\t\tvar attrId = id.replace( runescape, funescape );\n\t\t\treturn function( elem ) {\n\t\t\t\treturn elem.getAttribute("id") === attrId;\n\t\t\t};\n\t\t};\n\t\tExpr.find["ID"] = function( id, context ) {\n\t\t\tif ( typeof context.getElementById !== "undefined" && documentIsHTML ) {\n\t\t\t\tvar elem = context.getElementById( id );\n\t\t\t\treturn elem ? [ elem ] : [];\n\t\t\t}\n\t\t};\n\t} else {\n\t\tExpr.filter["ID"] =  function( id ) {\n\t\t\tvar attrId = id.replace( runescape, funescape );\n\t\t\treturn function( elem ) {\n\t\t\t\tvar node = typeof elem.getAttributeNode !== "undefined" &&\n\t\t\t\t\telem.getAttributeNode("id");\n\t\t\t\treturn node && node.value === attrId;\n\t\t\t};\n\t\t};\n\n\t\t// Support: IE 6 - 7 only\n\t\t// getElementById is not reliable as a find shortcut\n\t\tExpr.find["ID"] = function( id, context ) {\n\t\t\tif ( typeof context.getElementById !== "undefined" && documentIsHTML ) {\n\t\t\t\tvar node, i, elems,\n\t\t\t\t\telem = context.getElementById( id );\n\n\t\t\t\tif ( elem ) {\n\n\t\t\t\t\t// Verify the id attribute\n\t\t\t\t\tnode = elem.getAttributeNode("id");\n\t\t\t\t\tif ( node && node.value === id ) {\n\t\t\t\t\t\treturn [ elem ];\n\t\t\t\t\t}\n\n\t\t\t\t\t// Fall back on getElementsByName\n\t\t\t\t\telems = context.getElementsByName( id );\n\t\t\t\t\ti = 0;\n\t\t\t\t\twhile ( (elem = elems[i++]) ) {\n\t\t\t\t\t\tnode = elem.getAttributeNode("id");\n\t\t\t\t\t\tif ( node && node.value === id ) {\n\t\t\t\t\t\t\treturn [ elem ];\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\treturn [];\n\t\t\t}\n\t\t};\n\t}\n\n\t// Tag\n\tExpr.find["TAG"] = support.getElementsByTagName ?\n\t\tfunction( tag, context ) {\n\t\t\tif ( typeof context.getElementsByTagName !== "undefined" ) {\n\t\t\t\treturn context.getElementsByTagName( tag );\n\n\t\t\t// DocumentFragment nodes don\'t have gEBTN\n\t\t\t} else if ( support.qsa ) {\n\t\t\t\treturn context.querySelectorAll( tag );\n\t\t\t}\n\t\t} :\n\n\t\tfunction( tag, context ) {\n\t\t\tvar elem,\n\t\t\t\ttmp = [],\n\t\t\t\ti = 0,\n\t\t\t\t// By happy coincidence, a (broken) gEBTN appears on DocumentFragment nodes too\n\t\t\t\tresults = context.getElementsByTagName( tag );\n\n\t\t\t// Filter out possible comments\n\t\t\tif ( tag === "*" ) {\n\t\t\t\twhile ( (elem = results[i++]) ) {\n\t\t\t\t\tif ( elem.nodeType === 1 ) {\n\t\t\t\t\t\ttmp.push( elem );\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\treturn tmp;\n\t\t\t}\n\t\t\treturn results;\n\t\t};\n\n\t// Class\n\tExpr.find["CLASS"] = support.getElementsByClassName && function( className, context ) {\n\t\tif ( typeof context.getElementsByClassName !== "undefined" && documentIsHTML ) {\n\t\t\treturn context.getElementsByClassName( className );\n\t\t}\n\t};\n\n\t/* QSA/matchesSelector\n\t---------------------------------------------------------------------- */\n\n\t// QSA and matchesSelector support\n\n\t// matchesSelector(:active) reports false when true (IE9/Opera 11.5)\n\trbuggyMatches = [];\n\n\t// qSa(:focus) reports false when true (Chrome 21)\n\t// We allow this because of a bug in IE8/9 that throws an error\n\t// whenever `document.activeElement` is accessed on an iframe\n\t// So, we allow :focus to pass through QSA all the time to avoid the IE error\n\t// See https://bugs.jquery.com/ticket/13378\n\trbuggyQSA = [];\n\n\tif ( (support.qsa = rnative.test( document.querySelectorAll )) ) {\n\t\t// Build QSA regex\n\t\t// Regex strategy adopted from Diego Perini\n\t\tassert(function( el ) {\n\t\t\t// Select is set to empty string on purpose\n\t\t\t// This is to test IE\'s treatment of not explicitly\n\t\t\t// setting a boolean content attribute,\n\t\t\t// since its presence should be enough\n\t\t\t// https://bugs.jquery.com/ticket/12359\n\t\t\tdocElem.appendChild( el ).innerHTML = "<a id=\'" + expando + "\'></a>" +\n\t\t\t\t"<select id=\'" + expando + "-\\r\\\\\' msallowcapture=\'\'>" +\n\t\t\t\t"<option selected=\'\'></option></select>";\n\n\t\t\t// Support: IE8, Opera 11-12.16\n\t\t\t// Nothing should be selected when empty strings follow ^= or $= or *=\n\t\t\t// The test attribute must be unknown in Opera but "safe" for WinRT\n\t\t\t// https://msdn.microsoft.com/en-us/library/ie/hh465388.aspx#attribute_section\n\t\t\tif ( el.querySelectorAll("[msallowcapture^=\'\']").length ) {\n\t\t\t\trbuggyQSA.push( "[*^$]=" + whitespace + "*(?:\'\'|\\"\\")" );\n\t\t\t}\n\n\t\t\t// Support: IE8\n\t\t\t// Boolean attributes and "value" are not treated correctly\n\t\t\tif ( !el.querySelectorAll("[selected]").length ) {\n\t\t\t\trbuggyQSA.push( "\\\\[" + whitespace + "*(?:value|" + booleans + ")" );\n\t\t\t}\n\n\t\t\t// Support: Chrome<29, Android<4.4, Safari<7.0+, iOS<7.0+, PhantomJS<1.9.8+\n\t\t\tif ( !el.querySelectorAll( "[id~=" + expando + "-]" ).length ) {\n\t\t\t\trbuggyQSA.push("~=");\n\t\t\t}\n\n\t\t\t// Webkit/Opera - :checked should return selected option elements\n\t\t\t// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked\n\t\t\t// IE8 throws error here and will not see later tests\n\t\t\tif ( !el.querySelectorAll(":checked").length ) {\n\t\t\t\trbuggyQSA.push(":checked");\n\t\t\t}\n\n\t\t\t// Support: Safari 8+, iOS 8+\n\t\t\t// https://bugs.webkit.org/show_bug.cgi?id=136851\n\t\t\t// In-page `selector#id sibling-combinator selector` fails\n\t\t\tif ( !el.querySelectorAll( "a#" + expando + "+*" ).length ) {\n\t\t\t\trbuggyQSA.push(".#.+[+~]");\n\t\t\t}\n\t\t});\n\n\t\tassert(function( el ) {\n\t\t\tel.innerHTML = "<a href=\'\' disabled=\'disabled\'></a>" +\n\t\t\t\t"<select disabled=\'disabled\'><option/></select>";\n\n\t\t\t// Support: Windows 8 Native Apps\n\t\t\t// The type and name attributes are restricted during .innerHTML assignment\n\t\t\tvar input = document.createElement("input");\n\t\t\tinput.setAttribute( "type", "hidden" );\n\t\t\tel.appendChild( input ).setAttribute( "name", "D" );\n\n\t\t\t// Support: IE8\n\t\t\t// Enforce case-sensitivity of name attribute\n\t\t\tif ( el.querySelectorAll("[name=d]").length ) {\n\t\t\t\trbuggyQSA.push( "name" + whitespace + "*[*^$|!~]?=" );\n\t\t\t}\n\n\t\t\t// FF 3.5 - :enabled/:disabled and hidden elements (hidden elements are still enabled)\n\t\t\t// IE8 throws error here and will not see later tests\n\t\t\tif ( el.querySelectorAll(":enabled").length !== 2 ) {\n\t\t\t\trbuggyQSA.push( ":enabled", ":disabled" );\n\t\t\t}\n\n\t\t\t// Support: IE9-11+\n\t\t\t// IE\'s :disabled selector does not pick up the children of disabled fieldsets\n\t\t\tdocElem.appendChild( el ).disabled = true;\n\t\t\tif ( el.querySelectorAll(":disabled").length !== 2 ) {\n\t\t\t\trbuggyQSA.push( ":enabled", ":disabled" );\n\t\t\t}\n\n\t\t\t// Opera 10-11 does not throw on post-comma invalid pseudos\n\t\t\tel.querySelectorAll("*,:x");\n\t\t\trbuggyQSA.push(",.*:");\n\t\t});\n\t}\n\n\tif ( (support.matchesSelector = rnative.test( (matches = docElem.matches ||\n\t\tdocElem.webkitMatchesSelector ||\n\t\tdocElem.mozMatchesSelector ||\n\t\tdocElem.oMatchesSelector ||\n\t\tdocElem.msMatchesSelector) )) ) {\n\n\t\tassert(function( el ) {\n\t\t\t// Check to see if it\'s possible to do matchesSelector\n\t\t\t// on a disconnected node (IE 9)\n\t\t\tsupport.disconnectedMatch = matches.call( el, "*" );\n\n\t\t\t// This should fail with an exception\n\t\t\t// Gecko does not error, returns false instead\n\t\t\tmatches.call( el, "[s!=\'\']:x" );\n\t\t\trbuggyMatches.push( "!=", pseudos );\n\t\t});\n\t}\n\n\trbuggyQSA = rbuggyQSA.length && new RegExp( rbuggyQSA.join("|") );\n\trbuggyMatches = rbuggyMatches.length && new RegExp( rbuggyMatches.join("|") );\n\n\t/* Contains\n\t---------------------------------------------------------------------- */\n\thasCompare = rnative.test( docElem.compareDocumentPosition );\n\n\t// Element contains another\n\t// Purposefully self-exclusive\n\t// As in, an element does not contain itself\n\tcontains = hasCompare || rnative.test( docElem.contains ) ?\n\t\tfunction( a, b ) {\n\t\t\tvar adown = a.nodeType === 9 ? a.documentElement : a,\n\t\t\t\tbup = b && b.parentNode;\n\t\t\treturn a === bup || !!( bup && bup.nodeType === 1 && (\n\t\t\t\tadown.contains ?\n\t\t\t\t\tadown.contains( bup ) :\n\t\t\t\t\ta.compareDocumentPosition && a.compareDocumentPosition( bup ) & 16\n\t\t\t));\n\t\t} :\n\t\tfunction( a, b ) {\n\t\t\tif ( b ) {\n\t\t\t\twhile ( (b = b.parentNode) ) {\n\t\t\t\t\tif ( b === a ) {\n\t\t\t\t\t\treturn true;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn false;\n\t\t};\n\n\t/* Sorting\n\t---------------------------------------------------------------------- */\n\n\t// Document order sorting\n\tsortOrder = hasCompare ?\n\tfunction( a, b ) {\n\n\t\t// Flag for duplicate removal\n\t\tif ( a === b ) {\n\t\t\thasDuplicate = true;\n\t\t\treturn 0;\n\t\t}\n\n\t\t// Sort on method existence if only one input has compareDocumentPosition\n\t\tvar compare = !a.compareDocumentPosition - !b.compareDocumentPosition;\n\t\tif ( compare ) {\n\t\t\treturn compare;\n\t\t}\n\n\t\t// Calculate position if both inputs belong to the same document\n\t\tcompare = ( a.ownerDocument || a ) === ( b.ownerDocument || b ) ?\n\t\t\ta.compareDocumentPosition( b ) :\n\n\t\t\t// Otherwise we know they are disconnected\n\t\t\t1;\n\n\t\t// Disconnected nodes\n\t\tif ( compare & 1 ||\n\t\t\t(!support.sortDetached && b.compareDocumentPosition( a ) === compare) ) {\n\n\t\t\t// Choose the first element that is related to our preferred document\n\t\t\tif ( a === document || a.ownerDocument === preferredDoc && contains(preferredDoc, a) ) {\n\t\t\t\treturn -1;\n\t\t\t}\n\t\t\tif ( b === document || b.ownerDocument === preferredDoc && contains(preferredDoc, b) ) {\n\t\t\t\treturn 1;\n\t\t\t}\n\n\t\t\t// Maintain original order\n\t\t\treturn sortInput ?\n\t\t\t\t( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :\n\t\t\t\t0;\n\t\t}\n\n\t\treturn compare & 4 ? -1 : 1;\n\t} :\n\tfunction( a, b ) {\n\t\t// Exit early if the nodes are identical\n\t\tif ( a === b ) {\n\t\t\thasDuplicate = true;\n\t\t\treturn 0;\n\t\t}\n\n\t\tvar cur,\n\t\t\ti = 0,\n\t\t\taup = a.parentNode,\n\t\t\tbup = b.parentNode,\n\t\t\tap = [ a ],\n\t\t\tbp = [ b ];\n\n\t\t// Parentless nodes are either documents or disconnected\n\t\tif ( !aup || !bup ) {\n\t\t\treturn a === document ? -1 :\n\t\t\t\tb === document ? 1 :\n\t\t\t\taup ? -1 :\n\t\t\t\tbup ? 1 :\n\t\t\t\tsortInput ?\n\t\t\t\t( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :\n\t\t\t\t0;\n\n\t\t// If the nodes are siblings, we can do a quick check\n\t\t} else if ( aup === bup ) {\n\t\t\treturn siblingCheck( a, b );\n\t\t}\n\n\t\t// Otherwise we need full lists of their ancestors for comparison\n\t\tcur = a;\n\t\twhile ( (cur = cur.parentNode) ) {\n\t\t\tap.unshift( cur );\n\t\t}\n\t\tcur = b;\n\t\twhile ( (cur = cur.parentNode) ) {\n\t\t\tbp.unshift( cur );\n\t\t}\n\n\t\t// Walk down the tree looking for a discrepancy\n\t\twhile ( ap[i] === bp[i] ) {\n\t\t\ti++;\n\t\t}\n\n\t\treturn i ?\n\t\t\t// Do a sibling check if the nodes have a common ancestor\n\t\t\tsiblingCheck( ap[i], bp[i] ) :\n\n\t\t\t// Otherwise nodes in our document sort first\n\t\t\tap[i] === preferredDoc ? -1 :\n\t\t\tbp[i] === preferredDoc ? 1 :\n\t\t\t0;\n\t};\n\n\treturn document;\n};\n\nSizzle.matches = function( expr, elements ) {\n\treturn Sizzle( expr, null, null, elements );\n};\n\nSizzle.matchesSelector = function( elem, expr ) {\n\t// Set document vars if needed\n\tif ( ( elem.ownerDocument || elem ) !== document ) {\n\t\tsetDocument( elem );\n\t}\n\n\tif ( support.matchesSelector && documentIsHTML &&\n\t\t!nonnativeSelectorCache[ expr + " " ] &&\n\t\t( !rbuggyMatches || !rbuggyMatches.test( expr ) ) &&\n\t\t( !rbuggyQSA     || !rbuggyQSA.test( expr ) ) ) {\n\n\t\ttry {\n\t\t\tvar ret = matches.call( elem, expr );\n\n\t\t\t// IE 9\'s matchesSelector returns false on disconnected nodes\n\t\t\tif ( ret || support.disconnectedMatch ||\n\t\t\t\t\t// As well, disconnected nodes are said to be in a document\n\t\t\t\t\t// fragment in IE 9\n\t\t\t\t\telem.document && elem.document.nodeType !== 11 ) {\n\t\t\t\treturn ret;\n\t\t\t}\n\t\t} catch (e) {\n\t\t\tnonnativeSelectorCache( expr, true );\n\t\t}\n\t}\n\n\treturn Sizzle( expr, document, null, [ elem ] ).length > 0;\n};\n\nSizzle.contains = function( context, elem ) {\n\t// Set document vars if needed\n\tif ( ( context.ownerDocument || context ) !== document ) {\n\t\tsetDocument( context );\n\t}\n\treturn contains( context, elem );\n};\n\nSizzle.attr = function( elem, name ) {\n\t// Set document vars if needed\n\tif ( ( elem.ownerDocument || elem ) !== document ) {\n\t\tsetDocument( elem );\n\t}\n\n\tvar fn = Expr.attrHandle[ name.toLowerCase() ],\n\t\t// Don\'t get fooled by Object.prototype properties (jQuery #13807)\n\t\tval = fn && hasOwn.call( Expr.attrHandle, name.toLowerCase() ) ?\n\t\t\tfn( elem, name, !documentIsHTML ) :\n\t\t\tundefined;\n\n\treturn val !== undefined ?\n\t\tval :\n\t\tsupport.attributes || !documentIsHTML ?\n\t\t\telem.getAttribute( name ) :\n\t\t\t(val = elem.getAttributeNode(name)) && val.specified ?\n\t\t\t\tval.value :\n\t\t\t\tnull;\n};\n\nSizzle.escape = function( sel ) {\n\treturn (sel + "").replace( rcssescape, fcssescape );\n};\n\nSizzle.error = function( msg ) {\n\tthrow new Error( "Syntax error, unrecognized expression: " + msg );\n};\n\n/**\n * Document sorting and removing duplicates\n * @param {ArrayLike} results\n */\nSizzle.uniqueSort = function( results ) {\n\tvar elem,\n\t\tduplicates = [],\n\t\tj = 0,\n\t\ti = 0;\n\n\t// Unless we *know* we can detect duplicates, assume their presence\n\thasDuplicate = !support.detectDuplicates;\n\tsortInput = !support.sortStable && results.slice( 0 );\n\tresults.sort( sortOrder );\n\n\tif ( hasDuplicate ) {\n\t\twhile ( (elem = results[i++]) ) {\n\t\t\tif ( elem === results[ i ] ) {\n\t\t\t\tj = duplicates.push( i );\n\t\t\t}\n\t\t}\n\t\twhile ( j-- ) {\n\t\t\tresults.splice( duplicates[ j ], 1 );\n\t\t}\n\t}\n\n\t// Clear input after sorting to release objects\n\t// See https://github.com/jquery/sizzle/pull/225\n\tsortInput = null;\n\n\treturn results;\n};\n\n/**\n * Utility function for retrieving the text value of an array of DOM nodes\n * @param {Array|Element} elem\n */\ngetText = Sizzle.getText = function( elem ) {\n\tvar node,\n\t\tret = "",\n\t\ti = 0,\n\t\tnodeType = elem.nodeType;\n\n\tif ( !nodeType ) {\n\t\t// If no nodeType, this is expected to be an array\n\t\twhile ( (node = elem[i++]) ) {\n\t\t\t// Do not traverse comment nodes\n\t\t\tret += getText( node );\n\t\t}\n\t} else if ( nodeType === 1 || nodeType === 9 || nodeType === 11 ) {\n\t\t// Use textContent for elements\n\t\t// innerText usage removed for consistency of new lines (jQuery #11153)\n\t\tif ( typeof elem.textContent === "string" ) {\n\t\t\treturn elem.textContent;\n\t\t} else {\n\t\t\t// Traverse its children\n\t\t\tfor ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {\n\t\t\t\tret += getText( elem );\n\t\t\t}\n\t\t}\n\t} else if ( nodeType === 3 || nodeType === 4 ) {\n\t\treturn elem.nodeValue;\n\t}\n\t// Do not include comment or processing instruction nodes\n\n\treturn ret;\n};\n\nExpr = Sizzle.selectors = {\n\n\t// Can be adjusted by the user\n\tcacheLength: 50,\n\n\tcreatePseudo: markFunction,\n\n\tmatch: matchExpr,\n\n\tattrHandle: {},\n\n\tfind: {},\n\n\trelative: {\n\t\t">": { dir: "parentNode", first: true },\n\t\t" ": { dir: "parentNode" },\n\t\t"+": { dir: "previousSibling", first: true },\n\t\t"~": { dir: "previousSibling" }\n\t},\n\n\tpreFilter: {\n\t\t"ATTR": function( match ) {\n\t\t\tmatch[1] = match[1].replace( runescape, funescape );\n\n\t\t\t// Move the given value to match[3] whether quoted or unquoted\n\t\t\tmatch[3] = ( match[3] || match[4] || match[5] || "" ).replace( runescape, funescape );\n\n\t\t\tif ( match[2] === "~=" ) {\n\t\t\t\tmatch[3] = " " + match[3] + " ";\n\t\t\t}\n\n\t\t\treturn match.slice( 0, 4 );\n\t\t},\n\n\t\t"CHILD": function( match ) {\n\t\t\t/* matches from matchExpr["CHILD"]\n\t\t\t\t1 type (only|nth|...)\n\t\t\t\t2 what (child|of-type)\n\t\t\t\t3 argument (even|odd|\\d*|\\d*n([+-]\\d+)?|...)\n\t\t\t\t4 xn-component of xn+y argument ([+-]?\\d*n|)\n\t\t\t\t5 sign of xn-component\n\t\t\t\t6 x of xn-component\n\t\t\t\t7 sign of y-component\n\t\t\t\t8 y of y-component\n\t\t\t*/\n\t\t\tmatch[1] = match[1].toLowerCase();\n\n\t\t\tif ( match[1].slice( 0, 3 ) === "nth" ) {\n\t\t\t\t// nth-* requires argument\n\t\t\t\tif ( !match[3] ) {\n\t\t\t\t\tSizzle.error( match[0] );\n\t\t\t\t}\n\n\t\t\t\t// numeric x and y parameters for Expr.filter.CHILD\n\t\t\t\t// remember that false/true cast respectively to 0/1\n\t\t\t\tmatch[4] = +( match[4] ? match[5] + (match[6] || 1) : 2 * ( match[3] === "even" || match[3] === "odd" ) );\n\t\t\t\tmatch[5] = +( ( match[7] + match[8] ) || match[3] === "odd" );\n\n\t\t\t// other types prohibit arguments\n\t\t\t} else if ( match[3] ) {\n\t\t\t\tSizzle.error( match[0] );\n\t\t\t}\n\n\t\t\treturn match;\n\t\t},\n\n\t\t"PSEUDO": function( match ) {\n\t\t\tvar excess,\n\t\t\t\tunquoted = !match[6] && match[2];\n\n\t\t\tif ( matchExpr["CHILD"].test( match[0] ) ) {\n\t\t\t\treturn null;\n\t\t\t}\n\n\t\t\t// Accept quoted arguments as-is\n\t\t\tif ( match[3] ) {\n\t\t\t\tmatch[2] = match[4] || match[5] || "";\n\n\t\t\t// Strip excess characters from unquoted arguments\n\t\t\t} else if ( unquoted && rpseudo.test( unquoted ) &&\n\t\t\t\t// Get excess from tokenize (recursively)\n\t\t\t\t(excess = tokenize( unquoted, true )) &&\n\t\t\t\t// advance to the next closing parenthesis\n\t\t\t\t(excess = unquoted.indexOf( ")", unquoted.length - excess ) - unquoted.length) ) {\n\n\t\t\t\t// excess is a negative index\n\t\t\t\tmatch[0] = match[0].slice( 0, excess );\n\t\t\t\tmatch[2] = unquoted.slice( 0, excess );\n\t\t\t}\n\n\t\t\t// Return only captures needed by the pseudo filter method (type and argument)\n\t\t\treturn match.slice( 0, 3 );\n\t\t}\n\t},\n\n\tfilter: {\n\n\t\t"TAG": function( nodeNameSelector ) {\n\t\t\tvar nodeName = nodeNameSelector.replace( runescape, funescape ).toLowerCase();\n\t\t\treturn nodeNameSelector === "*" ?\n\t\t\t\tfunction() { return true; } :\n\t\t\t\tfunction( elem ) {\n\t\t\t\t\treturn elem.nodeName && elem.nodeName.toLowerCase() === nodeName;\n\t\t\t\t};\n\t\t},\n\n\t\t"CLASS": function( className ) {\n\t\t\tvar pattern = classCache[ className + " " ];\n\n\t\t\treturn pattern ||\n\t\t\t\t(pattern = new RegExp( "(^|" + whitespace + ")" + className + "(" + whitespace + "|$)" )) &&\n\t\t\t\tclassCache( className, function( elem ) {\n\t\t\t\t\treturn pattern.test( typeof elem.className === "string" && elem.className || typeof elem.getAttribute !== "undefined" && elem.getAttribute("class") || "" );\n\t\t\t\t});\n\t\t},\n\n\t\t"ATTR": function( name, operator, check ) {\n\t\t\treturn function( elem ) {\n\t\t\t\tvar result = Sizzle.attr( elem, name );\n\n\t\t\t\tif ( result == null ) {\n\t\t\t\t\treturn operator === "!=";\n\t\t\t\t}\n\t\t\t\tif ( !operator ) {\n\t\t\t\t\treturn true;\n\t\t\t\t}\n\n\t\t\t\tresult += "";\n\n\t\t\t\treturn operator === "=" ? result === check :\n\t\t\t\t\toperator === "!=" ? result !== check :\n\t\t\t\t\toperator === "^=" ? check && result.indexOf( check ) === 0 :\n\t\t\t\t\toperator === "*=" ? check && result.indexOf( check ) > -1 :\n\t\t\t\t\toperator === "$=" ? check && result.slice( -check.length ) === check :\n\t\t\t\t\toperator === "~=" ? ( " " + result.replace( rwhitespace, " " ) + " " ).indexOf( check ) > -1 :\n\t\t\t\t\toperator === "|=" ? result === check || result.slice( 0, check.length + 1 ) === check + "-" :\n\t\t\t\t\tfalse;\n\t\t\t};\n\t\t},\n\n\t\t"CHILD": function( type, what, argument, first, last ) {\n\t\t\tvar simple = type.slice( 0, 3 ) !== "nth",\n\t\t\t\tforward = type.slice( -4 ) !== "last",\n\t\t\t\tofType = what === "of-type";\n\n\t\t\treturn first === 1 && last === 0 ?\n\n\t\t\t\t// Shortcut for :nth-*(n)\n\t\t\t\tfunction( elem ) {\n\t\t\t\t\treturn !!elem.parentNode;\n\t\t\t\t} :\n\n\t\t\t\tfunction( elem, context, xml ) {\n\t\t\t\t\tvar cache, uniqueCache, outerCache, node, nodeIndex, start,\n\t\t\t\t\t\tdir = simple !== forward ? "nextSibling" : "previousSibling",\n\t\t\t\t\t\tparent = elem.parentNode,\n\t\t\t\t\t\tname = ofType && elem.nodeName.toLowerCase(),\n\t\t\t\t\t\tuseCache = !xml && !ofType,\n\t\t\t\t\t\tdiff = false;\n\n\t\t\t\t\tif ( parent ) {\n\n\t\t\t\t\t\t// :(first|last|only)-(child|of-type)\n\t\t\t\t\t\tif ( simple ) {\n\t\t\t\t\t\t\twhile ( dir ) {\n\t\t\t\t\t\t\t\tnode = elem;\n\t\t\t\t\t\t\t\twhile ( (node = node[ dir ]) ) {\n\t\t\t\t\t\t\t\t\tif ( ofType ?\n\t\t\t\t\t\t\t\t\t\tnode.nodeName.toLowerCase() === name :\n\t\t\t\t\t\t\t\t\t\tnode.nodeType === 1 ) {\n\n\t\t\t\t\t\t\t\t\t\treturn false;\n\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t// Reverse direction for :only-* (if we haven\'t yet done so)\n\t\t\t\t\t\t\t\tstart = dir = type === "only" && !start && "nextSibling";\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\treturn true;\n\t\t\t\t\t\t}\n\n\t\t\t\t\t\tstart = [ forward ? parent.firstChild : parent.lastChild ];\n\n\t\t\t\t\t\t// non-xml :nth-child(...) stores cache data on `parent`\n\t\t\t\t\t\tif ( forward && useCache ) {\n\n\t\t\t\t\t\t\t// Seek `elem` from a previously-cached index\n\n\t\t\t\t\t\t\t// ...in a gzip-friendly way\n\t\t\t\t\t\t\tnode = parent;\n\t\t\t\t\t\t\touterCache = node[ expando ] || (node[ expando ] = {});\n\n\t\t\t\t\t\t\t// Support: IE <9 only\n\t\t\t\t\t\t\t// Defend against cloned attroperties (jQuery gh-1709)\n\t\t\t\t\t\t\tuniqueCache = outerCache[ node.uniqueID ] ||\n\t\t\t\t\t\t\t\t(outerCache[ node.uniqueID ] = {});\n\n\t\t\t\t\t\t\tcache = uniqueCache[ type ] || [];\n\t\t\t\t\t\t\tnodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];\n\t\t\t\t\t\t\tdiff = nodeIndex && cache[ 2 ];\n\t\t\t\t\t\t\tnode = nodeIndex && parent.childNodes[ nodeIndex ];\n\n\t\t\t\t\t\t\twhile ( (node = ++nodeIndex && node && node[ dir ] ||\n\n\t\t\t\t\t\t\t\t// Fallback to seeking `elem` from the start\n\t\t\t\t\t\t\t\t(diff = nodeIndex = 0) || start.pop()) ) {\n\n\t\t\t\t\t\t\t\t// When found, cache indexes on `parent` and break\n\t\t\t\t\t\t\t\tif ( node.nodeType === 1 && ++diff && node === elem ) {\n\t\t\t\t\t\t\t\t\tuniqueCache[ type ] = [ dirruns, nodeIndex, diff ];\n\t\t\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t// Use previously-cached element index if available\n\t\t\t\t\t\t\tif ( useCache ) {\n\t\t\t\t\t\t\t\t// ...in a gzip-friendly way\n\t\t\t\t\t\t\t\tnode = elem;\n\t\t\t\t\t\t\t\touterCache = node[ expando ] || (node[ expando ] = {});\n\n\t\t\t\t\t\t\t\t// Support: IE <9 only\n\t\t\t\t\t\t\t\t// Defend against cloned attroperties (jQuery gh-1709)\n\t\t\t\t\t\t\t\tuniqueCache = outerCache[ node.uniqueID ] ||\n\t\t\t\t\t\t\t\t\t(outerCache[ node.uniqueID ] = {});\n\n\t\t\t\t\t\t\t\tcache = uniqueCache[ type ] || [];\n\t\t\t\t\t\t\t\tnodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];\n\t\t\t\t\t\t\t\tdiff = nodeIndex;\n\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t// xml :nth-child(...)\n\t\t\t\t\t\t\t// or :nth-last-child(...) or :nth(-last)?-of-type(...)\n\t\t\t\t\t\t\tif ( diff === false ) {\n\t\t\t\t\t\t\t\t// Use the same loop as above to seek `elem` from the start\n\t\t\t\t\t\t\t\twhile ( (node = ++nodeIndex && node && node[ dir ] ||\n\t\t\t\t\t\t\t\t\t(diff = nodeIndex = 0) || start.pop()) ) {\n\n\t\t\t\t\t\t\t\t\tif ( ( ofType ?\n\t\t\t\t\t\t\t\t\t\tnode.nodeName.toLowerCase() === name :\n\t\t\t\t\t\t\t\t\t\tnode.nodeType === 1 ) &&\n\t\t\t\t\t\t\t\t\t\t++diff ) {\n\n\t\t\t\t\t\t\t\t\t\t// Cache the index of each encountered element\n\t\t\t\t\t\t\t\t\t\tif ( useCache ) {\n\t\t\t\t\t\t\t\t\t\t\touterCache = node[ expando ] || (node[ expando ] = {});\n\n\t\t\t\t\t\t\t\t\t\t\t// Support: IE <9 only\n\t\t\t\t\t\t\t\t\t\t\t// Defend against cloned attroperties (jQuery gh-1709)\n\t\t\t\t\t\t\t\t\t\t\tuniqueCache = outerCache[ node.uniqueID ] ||\n\t\t\t\t\t\t\t\t\t\t\t\t(outerCache[ node.uniqueID ] = {});\n\n\t\t\t\t\t\t\t\t\t\t\tuniqueCache[ type ] = [ dirruns, diff ];\n\t\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t\tif ( node === elem ) {\n\t\t\t\t\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\n\t\t\t\t\t\t// Incorporate the offset, then check against cycle size\n\t\t\t\t\t\tdiff -= last;\n\t\t\t\t\t\treturn diff === first || ( diff % first === 0 && diff / first >= 0 );\n\t\t\t\t\t}\n\t\t\t\t};\n\t\t},\n\n\t\t"PSEUDO": function( pseudo, argument ) {\n\t\t\t// pseudo-class names are case-insensitive\n\t\t\t// http://www.w3.org/TR/selectors/#pseudo-classes\n\t\t\t// Prioritize by case sensitivity in case custom pseudos are added with uppercase letters\n\t\t\t// Remember that setFilters inherits from pseudos\n\t\t\tvar args,\n\t\t\t\tfn = Expr.pseudos[ pseudo ] || Expr.setFilters[ pseudo.toLowerCase() ] ||\n\t\t\t\t\tSizzle.error( "unsupported pseudo: " + pseudo );\n\n\t\t\t// The user may use createPseudo to indicate that\n\t\t\t// arguments are needed to create the filter function\n\t\t\t// just as Sizzle does\n\t\t\tif ( fn[ expando ] ) {\n\t\t\t\treturn fn( argument );\n\t\t\t}\n\n\t\t\t// But maintain support for old signatures\n\t\t\tif ( fn.length > 1 ) {\n\t\t\t\targs = [ pseudo, pseudo, "", argument ];\n\t\t\t\treturn Expr.setFilters.hasOwnProperty( pseudo.toLowerCase() ) ?\n\t\t\t\t\tmarkFunction(function( seed, matches ) {\n\t\t\t\t\t\tvar idx,\n\t\t\t\t\t\t\tmatched = fn( seed, argument ),\n\t\t\t\t\t\t\ti = matched.length;\n\t\t\t\t\t\twhile ( i-- ) {\n\t\t\t\t\t\t\tidx = indexOf( seed, matched[i] );\n\t\t\t\t\t\t\tseed[ idx ] = !( matches[ idx ] = matched[i] );\n\t\t\t\t\t\t}\n\t\t\t\t\t}) :\n\t\t\t\t\tfunction( elem ) {\n\t\t\t\t\t\treturn fn( elem, 0, args );\n\t\t\t\t\t};\n\t\t\t}\n\n\t\t\treturn fn;\n\t\t}\n\t},\n\n\tpseudos: {\n\t\t// Potentially complex pseudos\n\t\t"not": markFunction(function( selector ) {\n\t\t\t// Trim the selector passed to compile\n\t\t\t// to avoid treating leading and trailing\n\t\t\t// spaces as combinators\n\t\t\tvar input = [],\n\t\t\t\tresults = [],\n\t\t\t\tmatcher = compile( selector.replace( rtrim, "$1" ) );\n\n\t\t\treturn matcher[ expando ] ?\n\t\t\t\tmarkFunction(function( seed, matches, context, xml ) {\n\t\t\t\t\tvar elem,\n\t\t\t\t\t\tunmatched = matcher( seed, null, xml, [] ),\n\t\t\t\t\t\ti = seed.length;\n\n\t\t\t\t\t// Match elements unmatched by `matcher`\n\t\t\t\t\twhile ( i-- ) {\n\t\t\t\t\t\tif ( (elem = unmatched[i]) ) {\n\t\t\t\t\t\t\tseed[i] = !(matches[i] = elem);\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}) :\n\t\t\t\tfunction( elem, context, xml ) {\n\t\t\t\t\tinput[0] = elem;\n\t\t\t\t\tmatcher( input, null, xml, results );\n\t\t\t\t\t// Don\'t keep the element (issue #299)\n\t\t\t\t\tinput[0] = null;\n\t\t\t\t\treturn !results.pop();\n\t\t\t\t};\n\t\t}),\n\n\t\t"has": markFunction(function( selector ) {\n\t\t\treturn function( elem ) {\n\t\t\t\treturn Sizzle( selector, elem ).length > 0;\n\t\t\t};\n\t\t}),\n\n\t\t"contains": markFunction(function( text ) {\n\t\t\ttext = text.replace( runescape, funescape );\n\t\t\treturn function( elem ) {\n\t\t\t\treturn ( elem.textContent || getText( elem ) ).indexOf( text ) > -1;\n\t\t\t};\n\t\t}),\n\n\t\t// "Whether an element is represented by a :lang() selector\n\t\t// is based solely on the element\'s language value\n\t\t// being equal to the identifier C,\n\t\t// or beginning with the identifier C immediately followed by "-".\n\t\t// The matching of C against the element\'s language value is performed case-insensitively.\n\t\t// The identifier C does not have to be a valid language name."\n\t\t// http://www.w3.org/TR/selectors/#lang-pseudo\n\t\t"lang": markFunction( function( lang ) {\n\t\t\t// lang value must be a valid identifier\n\t\t\tif ( !ridentifier.test(lang || "") ) {\n\t\t\t\tSizzle.error( "unsupported lang: " + lang );\n\t\t\t}\n\t\t\tlang = lang.replace( runescape, funescape ).toLowerCase();\n\t\t\treturn function( elem ) {\n\t\t\t\tvar elemLang;\n\t\t\t\tdo {\n\t\t\t\t\tif ( (elemLang = documentIsHTML ?\n\t\t\t\t\t\telem.lang :\n\t\t\t\t\t\telem.getAttribute("xml:lang") || elem.getAttribute("lang")) ) {\n\n\t\t\t\t\t\telemLang = elemLang.toLowerCase();\n\t\t\t\t\t\treturn elemLang === lang || elemLang.indexOf( lang + "-" ) === 0;\n\t\t\t\t\t}\n\t\t\t\t} while ( (elem = elem.parentNode) && elem.nodeType === 1 );\n\t\t\t\treturn false;\n\t\t\t};\n\t\t}),\n\n\t\t// Miscellaneous\n\t\t"target": function( elem ) {\n\t\t\tvar hash = window.location && window.location.hash;\n\t\t\treturn hash && hash.slice( 1 ) === elem.id;\n\t\t},\n\n\t\t"root": function( elem ) {\n\t\t\treturn elem === docElem;\n\t\t},\n\n\t\t"focus": function( elem ) {\n\t\t\treturn elem === document.activeElement && (!document.hasFocus || document.hasFocus()) && !!(elem.type || elem.href || ~elem.tabIndex);\n\t\t},\n\n\t\t// Boolean properties\n\t\t"enabled": createDisabledPseudo( false ),\n\t\t"disabled": createDisabledPseudo( true ),\n\n\t\t"checked": function( elem ) {\n\t\t\t// In CSS3, :checked should return both checked and selected elements\n\t\t\t// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked\n\t\t\tvar nodeName = elem.nodeName.toLowerCase();\n\t\t\treturn (nodeName === "input" && !!elem.checked) || (nodeName === "option" && !!elem.selected);\n\t\t},\n\n\t\t"selected": function( elem ) {\n\t\t\t// Accessing this property makes selected-by-default\n\t\t\t// options in Safari work properly\n\t\t\tif ( elem.parentNode ) {\n\t\t\t\telem.parentNode.selectedIndex;\n\t\t\t}\n\n\t\t\treturn elem.selected === true;\n\t\t},\n\n\t\t// Contents\n\t\t"empty": function( elem ) {\n\t\t\t// http://www.w3.org/TR/selectors/#empty-pseudo\n\t\t\t// :empty is negated by element (1) or content nodes (text: 3; cdata: 4; entity ref: 5),\n\t\t\t//   but not by others (comment: 8; processing instruction: 7; etc.)\n\t\t\t// nodeType < 6 works because attributes (2) do not appear as children\n\t\t\tfor ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {\n\t\t\t\tif ( elem.nodeType < 6 ) {\n\t\t\t\t\treturn false;\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn true;\n\t\t},\n\n\t\t"parent": function( elem ) {\n\t\t\treturn !Expr.pseudos["empty"]( elem );\n\t\t},\n\n\t\t// Element/input types\n\t\t"header": function( elem ) {\n\t\t\treturn rheader.test( elem.nodeName );\n\t\t},\n\n\t\t"input": function( elem ) {\n\t\t\treturn rinputs.test( elem.nodeName );\n\t\t},\n\n\t\t"button": function( elem ) {\n\t\t\tvar name = elem.nodeName.toLowerCase();\n\t\t\treturn name === "input" && elem.type === "button" || name === "button";\n\t\t},\n\n\t\t"text": function( elem ) {\n\t\t\tvar attr;\n\t\t\treturn elem.nodeName.toLowerCase() === "input" &&\n\t\t\t\telem.type === "text" &&\n\n\t\t\t\t// Support: IE<8\n\t\t\t\t// New HTML5 attribute values (e.g., "search") appear with elem.type === "text"\n\t\t\t\t( (attr = elem.getAttribute("type")) == null || attr.toLowerCase() === "text" );\n\t\t},\n\n\t\t// Position-in-collection\n\t\t"first": createPositionalPseudo(function() {\n\t\t\treturn [ 0 ];\n\t\t}),\n\n\t\t"last": createPositionalPseudo(function( matchIndexes, length ) {\n\t\t\treturn [ length - 1 ];\n\t\t}),\n\n\t\t"eq": createPositionalPseudo(function( matchIndexes, length, argument ) {\n\t\t\treturn [ argument < 0 ? argument + length : argument ];\n\t\t}),\n\n\t\t"even": createPositionalPseudo(function( matchIndexes, length ) {\n\t\t\tvar i = 0;\n\t\t\tfor ( ; i < length; i += 2 ) {\n\t\t\t\tmatchIndexes.push( i );\n\t\t\t}\n\t\t\treturn matchIndexes;\n\t\t}),\n\n\t\t"odd": createPositionalPseudo(function( matchIndexes, length ) {\n\t\t\tvar i = 1;\n\t\t\tfor ( ; i < length; i += 2 ) {\n\t\t\t\tmatchIndexes.push( i );\n\t\t\t}\n\t\t\treturn matchIndexes;\n\t\t}),\n\n\t\t"lt": createPositionalPseudo(function( matchIndexes, length, argument ) {\n\t\t\tvar i = argument < 0 ?\n\t\t\t\targument + length :\n\t\t\t\targument > length ?\n\t\t\t\t\tlength :\n\t\t\t\t\targument;\n\t\t\tfor ( ; --i >= 0; ) {\n\t\t\t\tmatchIndexes.push( i );\n\t\t\t}\n\t\t\treturn matchIndexes;\n\t\t}),\n\n\t\t"gt": createPositionalPseudo(function( matchIndexes, length, argument ) {\n\t\t\tvar i = argument < 0 ? argument + length : argument;\n\t\t\tfor ( ; ++i < length; ) {\n\t\t\t\tmatchIndexes.push( i );\n\t\t\t}\n\t\t\treturn matchIndexes;\n\t\t})\n\t}\n};\n\nExpr.pseudos["nth"] = Expr.pseudos["eq"];\n\n// Add button/input type pseudos\nfor ( i in { radio: true, checkbox: true, file: true, password: true, image: true } ) {\n\tExpr.pseudos[ i ] = createInputPseudo( i );\n}\nfor ( i in { submit: true, reset: true } ) {\n\tExpr.pseudos[ i ] = createButtonPseudo( i );\n}\n\n// Easy API for creating new setFilters\nfunction setFilters() {}\nsetFilters.prototype = Expr.filters = Expr.pseudos;\nExpr.setFilters = new setFilters();\n\ntokenize = Sizzle.tokenize = function( selector, parseOnly ) {\n\tvar matched, match, tokens, type,\n\t\tsoFar, groups, preFilters,\n\t\tcached = tokenCache[ selector + " " ];\n\n\tif ( cached ) {\n\t\treturn parseOnly ? 0 : cached.slice( 0 );\n\t}\n\n\tsoFar = selector;\n\tgroups = [];\n\tpreFilters = Expr.preFilter;\n\n\twhile ( soFar ) {\n\n\t\t// Comma and first run\n\t\tif ( !matched || (match = rcomma.exec( soFar )) ) {\n\t\t\tif ( match ) {\n\t\t\t\t// Don\'t consume trailing commas as valid\n\t\t\t\tsoFar = soFar.slice( match[0].length ) || soFar;\n\t\t\t}\n\t\t\tgroups.push( (tokens = []) );\n\t\t}\n\n\t\tmatched = false;\n\n\t\t// Combinators\n\t\tif ( (match = rcombinators.exec( soFar )) ) {\n\t\t\tmatched = match.shift();\n\t\t\ttokens.push({\n\t\t\t\tvalue: matched,\n\t\t\t\t// Cast descendant combinators to space\n\t\t\t\ttype: match[0].replace( rtrim, " " )\n\t\t\t});\n\t\t\tsoFar = soFar.slice( matched.length );\n\t\t}\n\n\t\t// Filters\n\t\tfor ( type in Expr.filter ) {\n\t\t\tif ( (match = matchExpr[ type ].exec( soFar )) && (!preFilters[ type ] ||\n\t\t\t\t(match = preFilters[ type ]( match ))) ) {\n\t\t\t\tmatched = match.shift();\n\t\t\t\ttokens.push({\n\t\t\t\t\tvalue: matched,\n\t\t\t\t\ttype: type,\n\t\t\t\t\tmatches: match\n\t\t\t\t});\n\t\t\t\tsoFar = soFar.slice( matched.length );\n\t\t\t}\n\t\t}\n\n\t\tif ( !matched ) {\n\t\t\tbreak;\n\t\t}\n\t}\n\n\t// Return the length of the invalid excess\n\t// if we\'re just parsing\n\t// Otherwise, throw an error or return tokens\n\treturn parseOnly ?\n\t\tsoFar.length :\n\t\tsoFar ?\n\t\t\tSizzle.error( selector ) :\n\t\t\t// Cache the tokens\n\t\t\ttokenCache( selector, groups ).slice( 0 );\n};\n\nfunction toSelector( tokens ) {\n\tvar i = 0,\n\t\tlen = tokens.length,\n\t\tselector = "";\n\tfor ( ; i < len; i++ ) {\n\t\tselector += tokens[i].value;\n\t}\n\treturn selector;\n}\n\nfunction addCombinator( matcher, combinator, base ) {\n\tvar dir = combinator.dir,\n\t\tskip = combinator.next,\n\t\tkey = skip || dir,\n\t\tcheckNonElements = base && key === "parentNode",\n\t\tdoneName = done++;\n\n\treturn combinator.first ?\n\t\t// Check against closest ancestor/preceding element\n\t\tfunction( elem, context, xml ) {\n\t\t\twhile ( (elem = elem[ dir ]) ) {\n\t\t\t\tif ( elem.nodeType === 1 || checkNonElements ) {\n\t\t\t\t\treturn matcher( elem, context, xml );\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn false;\n\t\t} :\n\n\t\t// Check against all ancestor/preceding elements\n\t\tfunction( elem, context, xml ) {\n\t\t\tvar oldCache, uniqueCache, outerCache,\n\t\t\t\tnewCache = [ dirruns, doneName ];\n\n\t\t\t// We can\'t set arbitrary data on XML nodes, so they don\'t benefit from combinator caching\n\t\t\tif ( xml ) {\n\t\t\t\twhile ( (elem = elem[ dir ]) ) {\n\t\t\t\t\tif ( elem.nodeType === 1 || checkNonElements ) {\n\t\t\t\t\t\tif ( matcher( elem, context, xml ) ) {\n\t\t\t\t\t\t\treturn true;\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\twhile ( (elem = elem[ dir ]) ) {\n\t\t\t\t\tif ( elem.nodeType === 1 || checkNonElements ) {\n\t\t\t\t\t\touterCache = elem[ expando ] || (elem[ expando ] = {});\n\n\t\t\t\t\t\t// Support: IE <9 only\n\t\t\t\t\t\t// Defend against cloned attroperties (jQuery gh-1709)\n\t\t\t\t\t\tuniqueCache = outerCache[ elem.uniqueID ] || (outerCache[ elem.uniqueID ] = {});\n\n\t\t\t\t\t\tif ( skip && skip === elem.nodeName.toLowerCase() ) {\n\t\t\t\t\t\t\telem = elem[ dir ] || elem;\n\t\t\t\t\t\t} else if ( (oldCache = uniqueCache[ key ]) &&\n\t\t\t\t\t\t\toldCache[ 0 ] === dirruns && oldCache[ 1 ] === doneName ) {\n\n\t\t\t\t\t\t\t// Assign to newCache so results back-propagate to previous elements\n\t\t\t\t\t\t\treturn (newCache[ 2 ] = oldCache[ 2 ]);\n\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t// Reuse newcache so results back-propagate to previous elements\n\t\t\t\t\t\t\tuniqueCache[ key ] = newCache;\n\n\t\t\t\t\t\t\t// A match means we\'re done; a fail means we have to keep checking\n\t\t\t\t\t\t\tif ( (newCache[ 2 ] = matcher( elem, context, xml )) ) {\n\t\t\t\t\t\t\t\treturn true;\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn false;\n\t\t};\n}\n\nfunction elementMatcher( matchers ) {\n\treturn matchers.length > 1 ?\n\t\tfunction( elem, context, xml ) {\n\t\t\tvar i = matchers.length;\n\t\t\twhile ( i-- ) {\n\t\t\t\tif ( !matchers[i]( elem, context, xml ) ) {\n\t\t\t\t\treturn false;\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn true;\n\t\t} :\n\t\tmatchers[0];\n}\n\nfunction multipleContexts( selector, contexts, results ) {\n\tvar i = 0,\n\t\tlen = contexts.length;\n\tfor ( ; i < len; i++ ) {\n\t\tSizzle( selector, contexts[i], results );\n\t}\n\treturn results;\n}\n\nfunction condense( unmatched, map, filter, context, xml ) {\n\tvar elem,\n\t\tnewUnmatched = [],\n\t\ti = 0,\n\t\tlen = unmatched.length,\n\t\tmapped = map != null;\n\n\tfor ( ; i < len; i++ ) {\n\t\tif ( (elem = unmatched[i]) ) {\n\t\t\tif ( !filter || filter( elem, context, xml ) ) {\n\t\t\t\tnewUnmatched.push( elem );\n\t\t\t\tif ( mapped ) {\n\t\t\t\t\tmap.push( i );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\treturn newUnmatched;\n}\n\nfunction setMatcher( preFilter, selector, matcher, postFilter, postFinder, postSelector ) {\n\tif ( postFilter && !postFilter[ expando ] ) {\n\t\tpostFilter = setMatcher( postFilter );\n\t}\n\tif ( postFinder && !postFinder[ expando ] ) {\n\t\tpostFinder = setMatcher( postFinder, postSelector );\n\t}\n\treturn markFunction(function( seed, results, context, xml ) {\n\t\tvar temp, i, elem,\n\t\t\tpreMap = [],\n\t\t\tpostMap = [],\n\t\t\tpreexisting = results.length,\n\n\t\t\t// Get initial elements from seed or context\n\t\t\telems = seed || multipleContexts( selector || "*", context.nodeType ? [ context ] : context, [] ),\n\n\t\t\t// Prefilter to get matcher input, preserving a map for seed-results synchronization\n\t\t\tmatcherIn = preFilter && ( seed || !selector ) ?\n\t\t\t\tcondense( elems, preMap, preFilter, context, xml ) :\n\t\t\t\telems,\n\n\t\t\tmatcherOut = matcher ?\n\t\t\t\t// If we have a postFinder, or filtered seed, or non-seed postFilter or preexisting results,\n\t\t\t\tpostFinder || ( seed ? preFilter : preexisting || postFilter ) ?\n\n\t\t\t\t\t// ...intermediate processing is necessary\n\t\t\t\t\t[] :\n\n\t\t\t\t\t// ...otherwise use results directly\n\t\t\t\t\tresults :\n\t\t\t\tmatcherIn;\n\n\t\t// Find primary matches\n\t\tif ( matcher ) {\n\t\t\tmatcher( matcherIn, matcherOut, context, xml );\n\t\t}\n\n\t\t// Apply postFilter\n\t\tif ( postFilter ) {\n\t\t\ttemp = condense( matcherOut, postMap );\n\t\t\tpostFilter( temp, [], context, xml );\n\n\t\t\t// Un-match failing elements by moving them back to matcherIn\n\t\t\ti = temp.length;\n\t\t\twhile ( i-- ) {\n\t\t\t\tif ( (elem = temp[i]) ) {\n\t\t\t\t\tmatcherOut[ postMap[i] ] = !(matcherIn[ postMap[i] ] = elem);\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\tif ( seed ) {\n\t\t\tif ( postFinder || preFilter ) {\n\t\t\t\tif ( postFinder ) {\n\t\t\t\t\t// Get the final matcherOut by condensing this intermediate into postFinder contexts\n\t\t\t\t\ttemp = [];\n\t\t\t\t\ti = matcherOut.length;\n\t\t\t\t\twhile ( i-- ) {\n\t\t\t\t\t\tif ( (elem = matcherOut[i]) ) {\n\t\t\t\t\t\t\t// Restore matcherIn since elem is not yet a final match\n\t\t\t\t\t\t\ttemp.push( (matcherIn[i] = elem) );\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t\tpostFinder( null, (matcherOut = []), temp, xml );\n\t\t\t\t}\n\n\t\t\t\t// Move matched elements from seed to results to keep them synchronized\n\t\t\t\ti = matcherOut.length;\n\t\t\t\twhile ( i-- ) {\n\t\t\t\t\tif ( (elem = matcherOut[i]) &&\n\t\t\t\t\t\t(temp = postFinder ? indexOf( seed, elem ) : preMap[i]) > -1 ) {\n\n\t\t\t\t\t\tseed[temp] = !(results[temp] = elem);\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t// Add elements to results, through postFinder if defined\n\t\t} else {\n\t\t\tmatcherOut = condense(\n\t\t\t\tmatcherOut === results ?\n\t\t\t\t\tmatcherOut.splice( preexisting, matcherOut.length ) :\n\t\t\t\t\tmatcherOut\n\t\t\t);\n\t\t\tif ( postFinder ) {\n\t\t\t\tpostFinder( null, results, matcherOut, xml );\n\t\t\t} else {\n\t\t\t\tpush.apply( results, matcherOut );\n\t\t\t}\n\t\t}\n\t});\n}\n\nfunction matcherFromTokens( tokens ) {\n\tvar checkContext, matcher, j,\n\t\tlen = tokens.length,\n\t\tleadingRelative = Expr.relative[ tokens[0].type ],\n\t\timplicitRelative = leadingRelative || Expr.relative[" "],\n\t\ti = leadingRelative ? 1 : 0,\n\n\t\t// The foundational matcher ensures that elements are reachable from top-level context(s)\n\t\tmatchContext = addCombinator( function( elem ) {\n\t\t\treturn elem === checkContext;\n\t\t}, implicitRelative, true ),\n\t\tmatchAnyContext = addCombinator( function( elem ) {\n\t\t\treturn indexOf( checkContext, elem ) > -1;\n\t\t}, implicitRelative, true ),\n\t\tmatchers = [ function( elem, context, xml ) {\n\t\t\tvar ret = ( !leadingRelative && ( xml || context !== outermostContext ) ) || (\n\t\t\t\t(checkContext = context).nodeType ?\n\t\t\t\t\tmatchContext( elem, context, xml ) :\n\t\t\t\t\tmatchAnyContext( elem, context, xml ) );\n\t\t\t// Avoid hanging onto element (issue #299)\n\t\t\tcheckContext = null;\n\t\t\treturn ret;\n\t\t} ];\n\n\tfor ( ; i < len; i++ ) {\n\t\tif ( (matcher = Expr.relative[ tokens[i].type ]) ) {\n\t\t\tmatchers = [ addCombinator(elementMatcher( matchers ), matcher) ];\n\t\t} else {\n\t\t\tmatcher = Expr.filter[ tokens[i].type ].apply( null, tokens[i].matches );\n\n\t\t\t// Return special upon seeing a positional matcher\n\t\t\tif ( matcher[ expando ] ) {\n\t\t\t\t// Find the next relative operator (if any) for proper handling\n\t\t\t\tj = ++i;\n\t\t\t\tfor ( ; j < len; j++ ) {\n\t\t\t\t\tif ( Expr.relative[ tokens[j].type ] ) {\n\t\t\t\t\t\tbreak;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t\treturn setMatcher(\n\t\t\t\t\ti > 1 && elementMatcher( matchers ),\n\t\t\t\t\ti > 1 && toSelector(\n\t\t\t\t\t\t// If the preceding token was a descendant combinator, insert an implicit any-element `*`\n\t\t\t\t\t\ttokens.slice( 0, i - 1 ).concat({ value: tokens[ i - 2 ].type === " " ? "*" : "" })\n\t\t\t\t\t).replace( rtrim, "$1" ),\n\t\t\t\t\tmatcher,\n\t\t\t\t\ti < j && matcherFromTokens( tokens.slice( i, j ) ),\n\t\t\t\t\tj < len && matcherFromTokens( (tokens = tokens.slice( j )) ),\n\t\t\t\t\tj < len && toSelector( tokens )\n\t\t\t\t);\n\t\t\t}\n\t\t\tmatchers.push( matcher );\n\t\t}\n\t}\n\n\treturn elementMatcher( matchers );\n}\n\nfunction matcherFromGroupMatchers( elementMatchers, setMatchers ) {\n\tvar bySet = setMatchers.length > 0,\n\t\tbyElement = elementMatchers.length > 0,\n\t\tsuperMatcher = function( seed, context, xml, results, outermost ) {\n\t\t\tvar elem, j, matcher,\n\t\t\t\tmatchedCount = 0,\n\t\t\t\ti = "0",\n\t\t\t\tunmatched = seed && [],\n\t\t\t\tsetMatched = [],\n\t\t\t\tcontextBackup = outermostContext,\n\t\t\t\t// We must always have either seed elements or outermost context\n\t\t\t\telems = seed || byElement && Expr.find["TAG"]( "*", outermost ),\n\t\t\t\t// Use integer dirruns iff this is the outermost matcher\n\t\t\t\tdirrunsUnique = (dirruns += contextBackup == null ? 1 : Math.random() || 0.1),\n\t\t\t\tlen = elems.length;\n\n\t\t\tif ( outermost ) {\n\t\t\t\toutermostContext = context === document || context || outermost;\n\t\t\t}\n\n\t\t\t// Add elements passing elementMatchers directly to results\n\t\t\t// Support: IE<9, Safari\n\t\t\t// Tolerate NodeList properties (IE: "length"; Safari: <number>) matching elements by id\n\t\t\tfor ( ; i !== len && (elem = elems[i]) != null; i++ ) {\n\t\t\t\tif ( byElement && elem ) {\n\t\t\t\t\tj = 0;\n\t\t\t\t\tif ( !context && elem.ownerDocument !== document ) {\n\t\t\t\t\t\tsetDocument( elem );\n\t\t\t\t\t\txml = !documentIsHTML;\n\t\t\t\t\t}\n\t\t\t\t\twhile ( (matcher = elementMatchers[j++]) ) {\n\t\t\t\t\t\tif ( matcher( elem, context || document, xml) ) {\n\t\t\t\t\t\t\tresults.push( elem );\n\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t\tif ( outermost ) {\n\t\t\t\t\t\tdirruns = dirrunsUnique;\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\t// Track unmatched elements for set filters\n\t\t\t\tif ( bySet ) {\n\t\t\t\t\t// They will have gone through all possible matchers\n\t\t\t\t\tif ( (elem = !matcher && elem) ) {\n\t\t\t\t\t\tmatchedCount--;\n\t\t\t\t\t}\n\n\t\t\t\t\t// Lengthen the array for every element, matched or not\n\t\t\t\t\tif ( seed ) {\n\t\t\t\t\t\tunmatched.push( elem );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// `i` is now the count of elements visited above, and adding it to `matchedCount`\n\t\t\t// makes the latter nonnegative.\n\t\t\tmatchedCount += i;\n\n\t\t\t// Apply set filters to unmatched elements\n\t\t\t// NOTE: This can be skipped if there are no unmatched elements (i.e., `matchedCount`\n\t\t\t// equals `i`), unless we didn\'t visit _any_ elements in the above loop because we have\n\t\t\t// no element matchers and no seed.\n\t\t\t// Incrementing an initially-string "0" `i` allows `i` to remain a string only in that\n\t\t\t// case, which will result in a "00" `matchedCount` that differs from `i` but is also\n\t\t\t// numerically zero.\n\t\t\tif ( bySet && i !== matchedCount ) {\n\t\t\t\tj = 0;\n\t\t\t\twhile ( (matcher = setMatchers[j++]) ) {\n\t\t\t\t\tmatcher( unmatched, setMatched, context, xml );\n\t\t\t\t}\n\n\t\t\t\tif ( seed ) {\n\t\t\t\t\t// Reintegrate element matches to eliminate the need for sorting\n\t\t\t\t\tif ( matchedCount > 0 ) {\n\t\t\t\t\t\twhile ( i-- ) {\n\t\t\t\t\t\t\tif ( !(unmatched[i] || setMatched[i]) ) {\n\t\t\t\t\t\t\t\tsetMatched[i] = pop.call( results );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\t// Discard index placeholder values to get only actual matches\n\t\t\t\t\tsetMatched = condense( setMatched );\n\t\t\t\t}\n\n\t\t\t\t// Add matches to results\n\t\t\t\tpush.apply( results, setMatched );\n\n\t\t\t\t// Seedless set matches succeeding multiple successful matchers stipulate sorting\n\t\t\t\tif ( outermost && !seed && setMatched.length > 0 &&\n\t\t\t\t\t( matchedCount + setMatchers.length ) > 1 ) {\n\n\t\t\t\t\tSizzle.uniqueSort( results );\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Override manipulation of globals by nested matchers\n\t\t\tif ( outermost ) {\n\t\t\t\tdirruns = dirrunsUnique;\n\t\t\t\toutermostContext = contextBackup;\n\t\t\t}\n\n\t\t\treturn unmatched;\n\t\t};\n\n\treturn bySet ?\n\t\tmarkFunction( superMatcher ) :\n\t\tsuperMatcher;\n}\n\ncompile = Sizzle.compile = function( selector, match /* Internal Use Only */ ) {\n\tvar i,\n\t\tsetMatchers = [],\n\t\telementMatchers = [],\n\t\tcached = compilerCache[ selector + " " ];\n\n\tif ( !cached ) {\n\t\t// Generate a function of recursive functions that can be used to check each element\n\t\tif ( !match ) {\n\t\t\tmatch = tokenize( selector );\n\t\t}\n\t\ti = match.length;\n\t\twhile ( i-- ) {\n\t\t\tcached = matcherFromTokens( match[i] );\n\t\t\tif ( cached[ expando ] ) {\n\t\t\t\tsetMatchers.push( cached );\n\t\t\t} else {\n\t\t\t\telementMatchers.push( cached );\n\t\t\t}\n\t\t}\n\n\t\t// Cache the compiled function\n\t\tcached = compilerCache( selector, matcherFromGroupMatchers( elementMatchers, setMatchers ) );\n\n\t\t// Save selector and tokenization\n\t\tcached.selector = selector;\n\t}\n\treturn cached;\n};\n\n/**\n * A low-level selection function that works with Sizzle\'s compiled\n *  selector functions\n * @param {String|Function} selector A selector or a pre-compiled\n *  selector function built with Sizzle.compile\n * @param {Element} context\n * @param {Array} [results]\n * @param {Array} [seed] A set of elements to match against\n */\nselect = Sizzle.select = function( selector, context, results, seed ) {\n\tvar i, tokens, token, type, find,\n\t\tcompiled = typeof selector === "function" && selector,\n\t\tmatch = !seed && tokenize( (selector = compiled.selector || selector) );\n\n\tresults = results || [];\n\n\t// Try to minimize operations if there is only one selector in the list and no seed\n\t// (the latter of which guarantees us context)\n\tif ( match.length === 1 ) {\n\n\t\t// Reduce context if the leading compound selector is an ID\n\t\ttokens = match[0] = match[0].slice( 0 );\n\t\tif ( tokens.length > 2 && (token = tokens[0]).type === "ID" &&\n\t\t\t\tcontext.nodeType === 9 && documentIsHTML && Expr.relative[ tokens[1].type ] ) {\n\n\t\t\tcontext = ( Expr.find["ID"]( token.matches[0].replace(runescape, funescape), context ) || [] )[0];\n\t\t\tif ( !context ) {\n\t\t\t\treturn results;\n\n\t\t\t// Precompiled matchers will still verify ancestry, so step up a level\n\t\t\t} else if ( compiled ) {\n\t\t\t\tcontext = context.parentNode;\n\t\t\t}\n\n\t\t\tselector = selector.slice( tokens.shift().value.length );\n\t\t}\n\n\t\t// Fetch a seed set for right-to-left matching\n\t\ti = matchExpr["needsContext"].test( selector ) ? 0 : tokens.length;\n\t\twhile ( i-- ) {\n\t\t\ttoken = tokens[i];\n\n\t\t\t// Abort if we hit a combinator\n\t\t\tif ( Expr.relative[ (type = token.type) ] ) {\n\t\t\t\tbreak;\n\t\t\t}\n\t\t\tif ( (find = Expr.find[ type ]) ) {\n\t\t\t\t// Search, expanding context for leading sibling combinators\n\t\t\t\tif ( (seed = find(\n\t\t\t\t\ttoken.matches[0].replace( runescape, funescape ),\n\t\t\t\t\trsibling.test( tokens[0].type ) && testContext( context.parentNode ) || context\n\t\t\t\t)) ) {\n\n\t\t\t\t\t// If seed is empty or no tokens remain, we can return early\n\t\t\t\t\ttokens.splice( i, 1 );\n\t\t\t\t\tselector = seed.length && toSelector( tokens );\n\t\t\t\t\tif ( !selector ) {\n\t\t\t\t\t\tpush.apply( results, seed );\n\t\t\t\t\t\treturn results;\n\t\t\t\t\t}\n\n\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\t// Compile and execute a filtering function if one is not provided\n\t// Provide `match` to avoid retokenization if we modified the selector above\n\t( compiled || compile( selector, match ) )(\n\t\tseed,\n\t\tcontext,\n\t\t!documentIsHTML,\n\t\tresults,\n\t\t!context || rsibling.test( selector ) && testContext( context.parentNode ) || context\n\t);\n\treturn results;\n};\n\n// One-time assignments\n\n// Sort stability\nsupport.sortStable = expando.split("").sort( sortOrder ).join("") === expando;\n\n// Support: Chrome 14-35+\n// Always assume duplicates if they aren\'t passed to the comparison function\nsupport.detectDuplicates = !!hasDuplicate;\n\n// Initialize against the default document\nsetDocument();\n\n// Support: Webkit<537.32 - Safari 6.0.3/Chrome 25 (fixed in Chrome 27)\n// Detached nodes confoundingly follow *each other*\nsupport.sortDetached = assert(function( el ) {\n\t// Should return 1, but returns 4 (following)\n\treturn el.compareDocumentPosition( document.createElement("fieldset") ) & 1;\n});\n\n// Support: IE<8\n// Prevent attribute/property "interpolation"\n// https://msdn.microsoft.com/en-us/library/ms536429%28VS.85%29.aspx\nif ( !assert(function( el ) {\n\tel.innerHTML = "<a href=\'#\'></a>";\n\treturn el.firstChild.getAttribute("href") === "#" ;\n}) ) {\n\taddHandle( "type|href|height|width", function( elem, name, isXML ) {\n\t\tif ( !isXML ) {\n\t\t\treturn elem.getAttribute( name, name.toLowerCase() === "type" ? 1 : 2 );\n\t\t}\n\t});\n}\n\n// Support: IE<9\n// Use defaultValue in place of getAttribute("value")\nif ( !support.attributes || !assert(function( el ) {\n\tel.innerHTML = "<input/>";\n\tel.firstChild.setAttribute( "value", "" );\n\treturn el.firstChild.getAttribute( "value" ) === "";\n}) ) {\n\taddHandle( "value", function( elem, name, isXML ) {\n\t\tif ( !isXML && elem.nodeName.toLowerCase() === "input" ) {\n\t\t\treturn elem.defaultValue;\n\t\t}\n\t});\n}\n\n// Support: IE<9\n// Use getAttributeNode to fetch booleans when getAttribute lies\nif ( !assert(function( el ) {\n\treturn el.getAttribute("disabled") == null;\n}) ) {\n\taddHandle( booleans, function( elem, name, isXML ) {\n\t\tvar val;\n\t\tif ( !isXML ) {\n\t\t\treturn elem[ name ] === true ? name.toLowerCase() :\n\t\t\t\t\t(val = elem.getAttributeNode( name )) && val.specified ?\n\t\t\t\t\tval.value :\n\t\t\t\tnull;\n\t\t}\n\t});\n}\n\nreturn Sizzle;\n\n})( window );\n\n\n\njQuery.find = Sizzle;\njQuery.expr = Sizzle.selectors;\n\n// Deprecated\njQuery.expr[ ":" ] = jQuery.expr.pseudos;\njQuery.uniqueSort = jQuery.unique = Sizzle.uniqueSort;\njQuery.text = Sizzle.getText;\njQuery.isXMLDoc = Sizzle.isXML;\njQuery.contains = Sizzle.contains;\njQuery.escapeSelector = Sizzle.escape;\n\n\n\n\nvar dir = function( elem, dir, until ) {\n\tvar matched = [],\n\t\ttruncate = until !== undefined;\n\n\twhile ( ( elem = elem[ dir ] ) && elem.nodeType !== 9 ) {\n\t\tif ( elem.nodeType === 1 ) {\n\t\t\tif ( truncate && jQuery( elem ).is( until ) ) {\n\t\t\t\tbreak;\n\t\t\t}\n\t\t\tmatched.push( elem );\n\t\t}\n\t}\n\treturn matched;\n};\n\n\nvar siblings = function( n, elem ) {\n\tvar matched = [];\n\n\tfor ( ; n; n = n.nextSibling ) {\n\t\tif ( n.nodeType === 1 && n !== elem ) {\n\t\t\tmatched.push( n );\n\t\t}\n\t}\n\n\treturn matched;\n};\n\n\nvar rneedsContext = jQuery.expr.match.needsContext;\n\n\n\nfunction nodeName( elem, name ) {\n\n  return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();\n\n};\nvar rsingleTag = ( /^<([a-z][^\\/\\0>:\\x20\\t\\r\\n\\f]*)[\\x20\\t\\r\\n\\f]*\\/?>(?:<\\/\\1>|)$/i );\n\n\n\n// Implement the identical functionality for filter and not\nfunction winnow( elements, qualifier, not ) {\n\tif ( isFunction( qualifier ) ) {\n\t\treturn jQuery.grep( elements, function( elem, i ) {\n\t\t\treturn !!qualifier.call( elem, i, elem ) !== not;\n\t\t} );\n\t}\n\n\t// Single element\n\tif ( qualifier.nodeType ) {\n\t\treturn jQuery.grep( elements, function( elem ) {\n\t\t\treturn ( elem === qualifier ) !== not;\n\t\t} );\n\t}\n\n\t// Arraylike of elements (jQuery, arguments, Array)\n\tif ( typeof qualifier !== "string" ) {\n\t\treturn jQuery.grep( elements, function( elem ) {\n\t\t\treturn ( indexOf.call( qualifier, elem ) > -1 ) !== not;\n\t\t} );\n\t}\n\n\t// Filtered directly for both simple and complex selectors\n\treturn jQuery.filter( qualifier, elements, not );\n}\n\njQuery.filter = function( expr, elems, not ) {\n\tvar elem = elems[ 0 ];\n\n\tif ( not ) {\n\t\texpr = ":not(" + expr + ")";\n\t}\n\n\tif ( elems.length === 1 && elem.nodeType === 1 ) {\n\t\treturn jQuery.find.matchesSelector( elem, expr ) ? [ elem ] : [];\n\t}\n\n\treturn jQuery.find.matches( expr, jQuery.grep( elems, function( elem ) {\n\t\treturn elem.nodeType === 1;\n\t} ) );\n};\n\njQuery.fn.extend( {\n\tfind: function( selector ) {\n\t\tvar i, ret,\n\t\t\tlen = this.length,\n\t\t\tself = this;\n\n\t\tif ( typeof selector !== "string" ) {\n\t\t\treturn this.pushStack( jQuery( selector ).filter( function() {\n\t\t\t\tfor ( i = 0; i < len; i++ ) {\n\t\t\t\t\tif ( jQuery.contains( self[ i ], this ) ) {\n\t\t\t\t\t\treturn true;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t} ) );\n\t\t}\n\n\t\tret = this.pushStack( [] );\n\n\t\tfor ( i = 0; i < len; i++ ) {\n\t\t\tjQuery.find( selector, self[ i ], ret );\n\t\t}\n\n\t\treturn len > 1 ? jQuery.uniqueSort( ret ) : ret;\n\t},\n\tfilter: function( selector ) {\n\t\treturn this.pushStack( winnow( this, selector || [], false ) );\n\t},\n\tnot: function( selector ) {\n\t\treturn this.pushStack( winnow( this, selector || [], true ) );\n\t},\n\tis: function( selector ) {\n\t\treturn !!winnow(\n\t\t\tthis,\n\n\t\t\t// If this is a positional/relative selector, check membership in the returned set\n\t\t\t// so $("p:first").is("p:last") won\'t return true for a doc with two "p".\n\t\t\ttypeof selector === "string" && rneedsContext.test( selector ) ?\n\t\t\t\tjQuery( selector ) :\n\t\t\t\tselector || [],\n\t\t\tfalse\n\t\t).length;\n\t}\n} );\n\n\n// Initialize a jQuery object\n\n\n// A central reference to the root jQuery(document)\nvar rootjQuery,\n\n\t// A simple way to check for HTML strings\n\t// Prioritize #id over <tag> to avoid XSS via location.hash (#9521)\n\t// Strict HTML recognition (#11290: must start with <)\n\t// Shortcut simple #id case for speed\n\trquickExpr = /^(?:\\s*(<[\\w\\W]+>)[^>]*|#([\\w-]+))$/,\n\n\tinit = jQuery.fn.init = function( selector, context, root ) {\n\t\tvar match, elem;\n\n\t\t// HANDLE: $(""), $(null), $(undefined), $(false)\n\t\tif ( !selector ) {\n\t\t\treturn this;\n\t\t}\n\n\t\t// Method init() accepts an alternate rootjQuery\n\t\t// so migrate can support jQuery.sub (gh-2101)\n\t\troot = root || rootjQuery;\n\n\t\t// Handle HTML strings\n\t\tif ( typeof selector === "string" ) {\n\t\t\tif ( selector[ 0 ] === "<" &&\n\t\t\t\tselector[ selector.length - 1 ] === ">" &&\n\t\t\t\tselector.length >= 3 ) {\n\n\t\t\t\t// Assume that strings that start and end with <> are HTML and skip the regex check\n\t\t\t\tmatch = [ null, selector, null ];\n\n\t\t\t} else {\n\t\t\t\tmatch = rquickExpr.exec( selector );\n\t\t\t}\n\n\t\t\t// Match html or make sure no context is specified for #id\n\t\t\tif ( match && ( match[ 1 ] || !context ) ) {\n\n\t\t\t\t// HANDLE: $(html) -> $(array)\n\t\t\t\tif ( match[ 1 ] ) {\n\t\t\t\t\tcontext = context instanceof jQuery ? context[ 0 ] : context;\n\n\t\t\t\t\t// Option to run scripts is true for back-compat\n\t\t\t\t\t// Intentionally let the error be thrown if parseHTML is not present\n\t\t\t\t\tjQuery.merge( this, jQuery.parseHTML(\n\t\t\t\t\t\tmatch[ 1 ],\n\t\t\t\t\t\tcontext && context.nodeType ? context.ownerDocument || context : document,\n\t\t\t\t\t\ttrue\n\t\t\t\t\t) );\n\n\t\t\t\t\t// HANDLE: $(html, props)\n\t\t\t\t\tif ( rsingleTag.test( match[ 1 ] ) && jQuery.isPlainObject( context ) ) {\n\t\t\t\t\t\tfor ( match in context ) {\n\n\t\t\t\t\t\t\t// Properties of context are called as methods if possible\n\t\t\t\t\t\t\tif ( isFunction( this[ match ] ) ) {\n\t\t\t\t\t\t\t\tthis[ match ]( context[ match ] );\n\n\t\t\t\t\t\t\t// ...and otherwise set as attributes\n\t\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t\tthis.attr( match, context[ match ] );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\treturn this;\n\n\t\t\t\t// HANDLE: $(#id)\n\t\t\t\t} else {\n\t\t\t\t\telem = document.getElementById( match[ 2 ] );\n\n\t\t\t\t\tif ( elem ) {\n\n\t\t\t\t\t\t// Inject the element directly into the jQuery object\n\t\t\t\t\t\tthis[ 0 ] = elem;\n\t\t\t\t\t\tthis.length = 1;\n\t\t\t\t\t}\n\t\t\t\t\treturn this;\n\t\t\t\t}\n\n\t\t\t// HANDLE: $(expr, $(...))\n\t\t\t} else if ( !context || context.jquery ) {\n\t\t\t\treturn ( context || root ).find( selector );\n\n\t\t\t// HANDLE: $(expr, context)\n\t\t\t// (which is just equivalent to: $(context).find(expr)\n\t\t\t} else {\n\t\t\t\treturn this.constructor( context ).find( selector );\n\t\t\t}\n\n\t\t// HANDLE: $(DOMElement)\n\t\t} else if ( selector.nodeType ) {\n\t\t\tthis[ 0 ] = selector;\n\t\t\tthis.length = 1;\n\t\t\treturn this;\n\n\t\t// HANDLE: $(function)\n\t\t// Shortcut for document ready\n\t\t} else if ( isFunction( selector ) ) {\n\t\t\treturn root.ready !== undefined ?\n\t\t\t\troot.ready( selector ) :\n\n\t\t\t\t// Execute immediately if ready is not present\n\t\t\t\tselector( jQuery );\n\t\t}\n\n\t\treturn jQuery.makeArray( selector, this );\n\t};\n\n// Give the init function the jQuery prototype for later instantiation\ninit.prototype = jQuery.fn;\n\n// Initialize central reference\nrootjQuery = jQuery( document );\n\n\nvar rparentsprev = /^(?:parents|prev(?:Until|All))/,\n\n\t// Methods guaranteed to produce a unique set when starting from a unique set\n\tguaranteedUnique = {\n\t\tchildren: true,\n\t\tcontents: true,\n\t\tnext: true,\n\t\tprev: true\n\t};\n\njQuery.fn.extend( {\n\thas: function( target ) {\n\t\tvar targets = jQuery( target, this ),\n\t\t\tl = targets.length;\n\n\t\treturn this.filter( function() {\n\t\t\tvar i = 0;\n\t\t\tfor ( ; i < l; i++ ) {\n\t\t\t\tif ( jQuery.contains( this, targets[ i ] ) ) {\n\t\t\t\t\treturn true;\n\t\t\t\t}\n\t\t\t}\n\t\t} );\n\t},\n\n\tclosest: function( selectors, context ) {\n\t\tvar cur,\n\t\t\ti = 0,\n\t\t\tl = this.length,\n\t\t\tmatched = [],\n\t\t\ttargets = typeof selectors !== "string" && jQuery( selectors );\n\n\t\t// Positional selectors never match, since there\'s no _selection_ context\n\t\tif ( !rneedsContext.test( selectors ) ) {\n\t\t\tfor ( ; i < l; i++ ) {\n\t\t\t\tfor ( cur = this[ i ]; cur && cur !== context; cur = cur.parentNode ) {\n\n\t\t\t\t\t// Always skip document fragments\n\t\t\t\t\tif ( cur.nodeType < 11 && ( targets ?\n\t\t\t\t\t\ttargets.index( cur ) > -1 :\n\n\t\t\t\t\t\t// Don\'t pass non-elements to Sizzle\n\t\t\t\t\t\tcur.nodeType === 1 &&\n\t\t\t\t\t\t\tjQuery.find.matchesSelector( cur, selectors ) ) ) {\n\n\t\t\t\t\t\tmatched.push( cur );\n\t\t\t\t\t\tbreak;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn this.pushStack( matched.length > 1 ? jQuery.uniqueSort( matched ) : matched );\n\t},\n\n\t// Determine the position of an element within the set\n\tindex: function( elem ) {\n\n\t\t// No argument, return index in parent\n\t\tif ( !elem ) {\n\t\t\treturn ( this[ 0 ] && this[ 0 ].parentNode ) ? this.first().prevAll().length : -1;\n\t\t}\n\n\t\t// Index in selector\n\t\tif ( typeof elem === "string" ) {\n\t\t\treturn indexOf.call( jQuery( elem ), this[ 0 ] );\n\t\t}\n\n\t\t// Locate the position of the desired element\n\t\treturn indexOf.call( this,\n\n\t\t\t// If it receives a jQuery object, the first element is used\n\t\t\telem.jquery ? elem[ 0 ] : elem\n\t\t);\n\t},\n\n\tadd: function( selector, context ) {\n\t\treturn this.pushStack(\n\t\t\tjQuery.uniqueSort(\n\t\t\t\tjQuery.merge( this.get(), jQuery( selector, context ) )\n\t\t\t)\n\t\t);\n\t},\n\n\taddBack: function( selector ) {\n\t\treturn this.add( selector == null ?\n\t\t\tthis.prevObject : this.prevObject.filter( selector )\n\t\t);\n\t}\n} );\n\nfunction sibling( cur, dir ) {\n\twhile ( ( cur = cur[ dir ] ) && cur.nodeType !== 1 ) {}\n\treturn cur;\n}\n\njQuery.each( {\n\tparent: function( elem ) {\n\t\tvar parent = elem.parentNode;\n\t\treturn parent && parent.nodeType !== 11 ? parent : null;\n\t},\n\tparents: function( elem ) {\n\t\treturn dir( elem, "parentNode" );\n\t},\n\tparentsUntil: function( elem, i, until ) {\n\t\treturn dir( elem, "parentNode", until );\n\t},\n\tnext: function( elem ) {\n\t\treturn sibling( elem, "nextSibling" );\n\t},\n\tprev: function( elem ) {\n\t\treturn sibling( elem, "previousSibling" );\n\t},\n\tnextAll: function( elem ) {\n\t\treturn dir( elem, "nextSibling" );\n\t},\n\tprevAll: function( elem ) {\n\t\treturn dir( elem, "previousSibling" );\n\t},\n\tnextUntil: function( elem, i, until ) {\n\t\treturn dir( elem, "nextSibling", until );\n\t},\n\tprevUntil: function( elem, i, until ) {\n\t\treturn dir( elem, "previousSibling", until );\n\t},\n\tsiblings: function( elem ) {\n\t\treturn siblings( ( elem.parentNode || {} ).firstChild, elem );\n\t},\n\tchildren: function( elem ) {\n\t\treturn siblings( elem.firstChild );\n\t},\n\tcontents: function( elem ) {\n\t\tif ( typeof elem.contentDocument !== "undefined" ) {\n\t\t\treturn elem.contentDocument;\n\t\t}\n\n\t\t// Support: IE 9 - 11 only, iOS 7 only, Android Browser <=4.3 only\n\t\t// Treat the template element as a regular one in browsers that\n\t\t// don\'t support it.\n\t\tif ( nodeName( elem, "template" ) ) {\n\t\t\telem = elem.content || elem;\n\t\t}\n\n\t\treturn jQuery.merge( [], elem.childNodes );\n\t}\n}, function( name, fn ) {\n\tjQuery.fn[ name ] = function( until, selector ) {\n\t\tvar matched = jQuery.map( this, fn, until );\n\n\t\tif ( name.slice( -5 ) !== "Until" ) {\n\t\t\tselector = until;\n\t\t}\n\n\t\tif ( selector && typeof selector === "string" ) {\n\t\t\tmatched = jQuery.filter( selector, matched );\n\t\t}\n\n\t\tif ( this.length > 1 ) {\n\n\t\t\t// Remove duplicates\n\t\t\tif ( !guaranteedUnique[ name ] ) {\n\t\t\t\tjQuery.uniqueSort( matched );\n\t\t\t}\n\n\t\t\t// Reverse order for parents* and prev-derivatives\n\t\t\tif ( rparentsprev.test( name ) ) {\n\t\t\t\tmatched.reverse();\n\t\t\t}\n\t\t}\n\n\t\treturn this.pushStack( matched );\n\t};\n} );\nvar rnothtmlwhite = ( /[^\\x20\\t\\r\\n\\f]+/g );\n\n\n\n// Convert String-formatted options into Object-formatted ones\nfunction createOptions( options ) {\n\tvar object = {};\n\tjQuery.each( options.match( rnothtmlwhite ) || [], function( _, flag ) {\n\t\tobject[ flag ] = true;\n\t} );\n\treturn object;\n}\n\n/*\n * Create a callback list using the following parameters:\n *\n *\toptions: an optional list of space-separated options that will change how\n *\t\t\tthe callback list behaves or a more traditional option object\n *\n * By default a callback list will act like an event callback list and can be\n * "fired" multiple times.\n *\n * Possible options:\n *\n *\tonce:\t\t\twill ensure the callback list can only be fired once (like a Deferred)\n *\n *\tmemory:\t\t\twill keep track of previous values and will call any callback added\n *\t\t\t\t\tafter the list has been fired right away with the latest "memorized"\n *\t\t\t\t\tvalues (like a Deferred)\n *\n *\tunique:\t\t\twill ensure a callback can only be added once (no duplicate in the list)\n *\n *\tstopOnFalse:\tinterrupt callings when a callback returns false\n *\n */\njQuery.Callbacks = function( options ) {\n\n\t// Convert options from String-formatted to Object-formatted if needed\n\t// (we check in cache first)\n\toptions = typeof options === "string" ?\n\t\tcreateOptions( options ) :\n\t\tjQuery.extend( {}, options );\n\n\tvar // Flag to know if list is currently firing\n\t\tfiring,\n\n\t\t// Last fire value for non-forgettable lists\n\t\tmemory,\n\n\t\t// Flag to know if list was already fired\n\t\tfired,\n\n\t\t// Flag to prevent firing\n\t\tlocked,\n\n\t\t// Actual callback list\n\t\tlist = [],\n\n\t\t// Queue of execution data for repeatable lists\n\t\tqueue = [],\n\n\t\t// Index of currently firing callback (modified by add/remove as needed)\n\t\tfiringIndex = -1,\n\n\t\t// Fire callbacks\n\t\tfire = function() {\n\n\t\t\t// Enforce single-firing\n\t\t\tlocked = locked || options.once;\n\n\t\t\t// Execute callbacks for all pending executions,\n\t\t\t// respecting firingIndex overrides and runtime changes\n\t\t\tfired = firing = true;\n\t\t\tfor ( ; queue.length; firingIndex = -1 ) {\n\t\t\t\tmemory = queue.shift();\n\t\t\t\twhile ( ++firingIndex < list.length ) {\n\n\t\t\t\t\t// Run callback and check for early termination\n\t\t\t\t\tif ( list[ firingIndex ].apply( memory[ 0 ], memory[ 1 ] ) === false &&\n\t\t\t\t\t\toptions.stopOnFalse ) {\n\n\t\t\t\t\t\t// Jump to end and forget the data so .add doesn\'t re-fire\n\t\t\t\t\t\tfiringIndex = list.length;\n\t\t\t\t\t\tmemory = false;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Forget the data if we\'re done with it\n\t\t\tif ( !options.memory ) {\n\t\t\t\tmemory = false;\n\t\t\t}\n\n\t\t\tfiring = false;\n\n\t\t\t// Clean up if we\'re done firing for good\n\t\t\tif ( locked ) {\n\n\t\t\t\t// Keep an empty list if we have data for future add calls\n\t\t\t\tif ( memory ) {\n\t\t\t\t\tlist = [];\n\n\t\t\t\t// Otherwise, this object is spent\n\t\t\t\t} else {\n\t\t\t\t\tlist = "";\n\t\t\t\t}\n\t\t\t}\n\t\t},\n\n\t\t// Actual Callbacks object\n\t\tself = {\n\n\t\t\t// Add a callback or a collection of callbacks to the list\n\t\t\tadd: function() {\n\t\t\t\tif ( list ) {\n\n\t\t\t\t\t// If we have memory from a past run, we should fire after adding\n\t\t\t\t\tif ( memory && !firing ) {\n\t\t\t\t\t\tfiringIndex = list.length - 1;\n\t\t\t\t\t\tqueue.push( memory );\n\t\t\t\t\t}\n\n\t\t\t\t\t( function add( args ) {\n\t\t\t\t\t\tjQuery.each( args, function( _, arg ) {\n\t\t\t\t\t\t\tif ( isFunction( arg ) ) {\n\t\t\t\t\t\t\t\tif ( !options.unique || !self.has( arg ) ) {\n\t\t\t\t\t\t\t\t\tlist.push( arg );\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t} else if ( arg && arg.length && toType( arg ) !== "string" ) {\n\n\t\t\t\t\t\t\t\t// Inspect recursively\n\t\t\t\t\t\t\t\tadd( arg );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t} );\n\t\t\t\t\t} )( arguments );\n\n\t\t\t\t\tif ( memory && !firing ) {\n\t\t\t\t\t\tfire();\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t\treturn this;\n\t\t\t},\n\n\t\t\t// Remove a callback from the list\n\t\t\tremove: function() {\n\t\t\t\tjQuery.each( arguments, function( _, arg ) {\n\t\t\t\t\tvar index;\n\t\t\t\t\twhile ( ( index = jQuery.inArray( arg, list, index ) ) > -1 ) {\n\t\t\t\t\t\tlist.splice( index, 1 );\n\n\t\t\t\t\t\t// Handle firing indexes\n\t\t\t\t\t\tif ( index <= firingIndex ) {\n\t\t\t\t\t\t\tfiringIndex--;\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t} );\n\t\t\t\treturn this;\n\t\t\t},\n\n\t\t\t// Check if a given callback is in the list.\n\t\t\t// If no argument is given, return whether or not list has callbacks attached.\n\t\t\thas: function( fn ) {\n\t\t\t\treturn fn ?\n\t\t\t\t\tjQuery.inArray( fn, list ) > -1 :\n\t\t\t\t\tlist.length > 0;\n\t\t\t},\n\n\t\t\t// Remove all callbacks from the list\n\t\t\tempty: function() {\n\t\t\t\tif ( list ) {\n\t\t\t\t\tlist = [];\n\t\t\t\t}\n\t\t\t\treturn this;\n\t\t\t},\n\n\t\t\t// Disable .fire and .add\n\t\t\t// Abort any current/pending executions\n\t\t\t// Clear all callbacks and values\n\t\t\tdisable: function() {\n\t\t\t\tlocked = queue = [];\n\t\t\t\tlist = memory = "";\n\t\t\t\treturn this;\n\t\t\t},\n\t\t\tdisabled: function() {\n\t\t\t\treturn !list;\n\t\t\t},\n\n\t\t\t// Disable .fire\n\t\t\t// Also disable .add unless we have memory (since it would have no effect)\n\t\t\t// Abort any pending executions\n\t\t\tlock: function() {\n\t\t\t\tlocked = queue = [];\n\t\t\t\tif ( !memory && !firing ) {\n\t\t\t\t\tlist = memory = "";\n\t\t\t\t}\n\t\t\t\treturn this;\n\t\t\t},\n\t\t\tlocked: function() {\n\t\t\t\treturn !!locked;\n\t\t\t},\n\n\t\t\t// Call all callbacks with the given context and arguments\n\t\t\tfireWith: function( context, args ) {\n\t\t\t\tif ( !locked ) {\n\t\t\t\t\targs = args || [];\n\t\t\t\t\targs = [ context, args.slice ? args.slice() : args ];\n\t\t\t\t\tqueue.push( args );\n\t\t\t\t\tif ( !firing ) {\n\t\t\t\t\t\tfire();\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t\treturn this;\n\t\t\t},\n\n\t\t\t// Call all the callbacks with the given arguments\n\t\t\tfire: function() {\n\t\t\t\tself.fireWith( this, arguments );\n\t\t\t\treturn this;\n\t\t\t},\n\n\t\t\t// To know if the callbacks have already been called at least once\n\t\t\tfired: function() {\n\t\t\t\treturn !!fired;\n\t\t\t}\n\t\t};\n\n\treturn self;\n};\n\n\nfunction Identity( v ) {\n\treturn v;\n}\nfunction Thrower( ex ) {\n\tthrow ex;\n}\n\nfunction adoptValue( value, resolve, reject, noValue ) {\n\tvar method;\n\n\ttry {\n\n\t\t// Check for promise aspect first to privilege synchronous behavior\n\t\tif ( value && isFunction( ( method = value.promise ) ) ) {\n\t\t\tmethod.call( value ).done( resolve ).fail( reject );\n\n\t\t// Other thenables\n\t\t} else if ( value && isFunction( ( method = value.then ) ) ) {\n\t\t\tmethod.call( value, resolve, reject );\n\n\t\t// Other non-thenables\n\t\t} else {\n\n\t\t\t// Control `resolve` arguments by letting Array#slice cast boolean `noValue` to integer:\n\t\t\t// * false: [ value ].slice( 0 ) => resolve( value )\n\t\t\t// * true: [ value ].slice( 1 ) => resolve()\n\t\t\tresolve.apply( undefined, [ value ].slice( noValue ) );\n\t\t}\n\n\t// For Promises/A+, convert exceptions into rejections\n\t// Since jQuery.when doesn\'t unwrap thenables, we can skip the extra checks appearing in\n\t// Deferred#then to conditionally suppress rejection.\n\t} catch ( value ) {\n\n\t\t// Support: Android 4.0 only\n\t\t// Strict mode functions invoked without .call/.apply get global-object context\n\t\treject.apply( undefined, [ value ] );\n\t}\n}\n\njQuery.extend( {\n\n\tDeferred: function( func ) {\n\t\tvar tuples = [\n\n\t\t\t\t// action, add listener, callbacks,\n\t\t\t\t// ... .then handlers, argument index, [final state]\n\t\t\t\t[ "notify", "progress", jQuery.Callbacks( "memory" ),\n\t\t\t\t\tjQuery.Callbacks( "memory" ), 2 ],\n\t\t\t\t[ "resolve", "done", jQuery.Callbacks( "once memory" ),\n\t\t\t\t\tjQuery.Callbacks( "once memory" ), 0, "resolved" ],\n\t\t\t\t[ "reject", "fail", jQuery.Callbacks( "once memory" ),\n\t\t\t\t\tjQuery.Callbacks( "once memory" ), 1, "rejected" ]\n\t\t\t],\n\t\t\tstate = "pending",\n\t\t\tpromise = {\n\t\t\t\tstate: function() {\n\t\t\t\t\treturn state;\n\t\t\t\t},\n\t\t\t\talways: function() {\n\t\t\t\t\tdeferred.done( arguments ).fail( arguments );\n\t\t\t\t\treturn this;\n\t\t\t\t},\n\t\t\t\t"catch": function( fn ) {\n\t\t\t\t\treturn promise.then( null, fn );\n\t\t\t\t},\n\n\t\t\t\t// Keep pipe for back-compat\n\t\t\t\tpipe: function( /* fnDone, fnFail, fnProgress */ ) {\n\t\t\t\t\tvar fns = arguments;\n\n\t\t\t\t\treturn jQuery.Deferred( function( newDefer ) {\n\t\t\t\t\t\tjQuery.each( tuples, function( i, tuple ) {\n\n\t\t\t\t\t\t\t// Map tuples (progress, done, fail) to arguments (done, fail, progress)\n\t\t\t\t\t\t\tvar fn = isFunction( fns[ tuple[ 4 ] ] ) && fns[ tuple[ 4 ] ];\n\n\t\t\t\t\t\t\t// deferred.progress(function() { bind to newDefer or newDefer.notify })\n\t\t\t\t\t\t\t// deferred.done(function() { bind to newDefer or newDefer.resolve })\n\t\t\t\t\t\t\t// deferred.fail(function() { bind to newDefer or newDefer.reject })\n\t\t\t\t\t\t\tdeferred[ tuple[ 1 ] ]( function() {\n\t\t\t\t\t\t\t\tvar returned = fn && fn.apply( this, arguments );\n\t\t\t\t\t\t\t\tif ( returned && isFunction( returned.promise ) ) {\n\t\t\t\t\t\t\t\t\treturned.promise()\n\t\t\t\t\t\t\t\t\t\t.progress( newDefer.notify )\n\t\t\t\t\t\t\t\t\t\t.done( newDefer.resolve )\n\t\t\t\t\t\t\t\t\t\t.fail( newDefer.reject );\n\t\t\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t\t\tnewDefer[ tuple[ 0 ] + "With" ](\n\t\t\t\t\t\t\t\t\t\tthis,\n\t\t\t\t\t\t\t\t\t\tfn ? [ returned ] : arguments\n\t\t\t\t\t\t\t\t\t);\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t} );\n\t\t\t\t\t\t} );\n\t\t\t\t\t\tfns = null;\n\t\t\t\t\t} ).promise();\n\t\t\t\t},\n\t\t\t\tthen: function( onFulfilled, onRejected, onProgress ) {\n\t\t\t\t\tvar maxDepth = 0;\n\t\t\t\t\tfunction resolve( depth, deferred, handler, special ) {\n\t\t\t\t\t\treturn function() {\n\t\t\t\t\t\t\tvar that = this,\n\t\t\t\t\t\t\t\targs = arguments,\n\t\t\t\t\t\t\t\tmightThrow = function() {\n\t\t\t\t\t\t\t\t\tvar returned, then;\n\n\t\t\t\t\t\t\t\t\t// Support: Promises/A+ section 2.3.3.3.3\n\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-59\n\t\t\t\t\t\t\t\t\t// Ignore double-resolution attempts\n\t\t\t\t\t\t\t\t\tif ( depth < maxDepth ) {\n\t\t\t\t\t\t\t\t\t\treturn;\n\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\treturned = handler.apply( that, args );\n\n\t\t\t\t\t\t\t\t\t// Support: Promises/A+ section 2.3.1\n\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-48\n\t\t\t\t\t\t\t\t\tif ( returned === deferred.promise() ) {\n\t\t\t\t\t\t\t\t\t\tthrow new TypeError( "Thenable self-resolution" );\n\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t// Support: Promises/A+ sections 2.3.3.1, 3.5\n\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-54\n\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-75\n\t\t\t\t\t\t\t\t\t// Retrieve `then` only once\n\t\t\t\t\t\t\t\t\tthen = returned &&\n\n\t\t\t\t\t\t\t\t\t\t// Support: Promises/A+ section 2.3.4\n\t\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-64\n\t\t\t\t\t\t\t\t\t\t// Only check objects and functions for thenability\n\t\t\t\t\t\t\t\t\t\t( typeof returned === "object" ||\n\t\t\t\t\t\t\t\t\t\t\ttypeof returned === "function" ) &&\n\t\t\t\t\t\t\t\t\t\treturned.then;\n\n\t\t\t\t\t\t\t\t\t// Handle a returned thenable\n\t\t\t\t\t\t\t\t\tif ( isFunction( then ) ) {\n\n\t\t\t\t\t\t\t\t\t\t// Special processors (notify) just wait for resolution\n\t\t\t\t\t\t\t\t\t\tif ( special ) {\n\t\t\t\t\t\t\t\t\t\t\tthen.call(\n\t\t\t\t\t\t\t\t\t\t\t\treturned,\n\t\t\t\t\t\t\t\t\t\t\t\tresolve( maxDepth, deferred, Identity, special ),\n\t\t\t\t\t\t\t\t\t\t\t\tresolve( maxDepth, deferred, Thrower, special )\n\t\t\t\t\t\t\t\t\t\t\t);\n\n\t\t\t\t\t\t\t\t\t\t// Normal processors (resolve) also hook into progress\n\t\t\t\t\t\t\t\t\t\t} else {\n\n\t\t\t\t\t\t\t\t\t\t\t// ...and disregard older resolution values\n\t\t\t\t\t\t\t\t\t\t\tmaxDepth++;\n\n\t\t\t\t\t\t\t\t\t\t\tthen.call(\n\t\t\t\t\t\t\t\t\t\t\t\treturned,\n\t\t\t\t\t\t\t\t\t\t\t\tresolve( maxDepth, deferred, Identity, special ),\n\t\t\t\t\t\t\t\t\t\t\t\tresolve( maxDepth, deferred, Thrower, special ),\n\t\t\t\t\t\t\t\t\t\t\t\tresolve( maxDepth, deferred, Identity,\n\t\t\t\t\t\t\t\t\t\t\t\t\tdeferred.notifyWith )\n\t\t\t\t\t\t\t\t\t\t\t);\n\t\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t// Handle all other returned values\n\t\t\t\t\t\t\t\t\t} else {\n\n\t\t\t\t\t\t\t\t\t\t// Only substitute handlers pass on context\n\t\t\t\t\t\t\t\t\t\t// and multiple values (non-spec behavior)\n\t\t\t\t\t\t\t\t\t\tif ( handler !== Identity ) {\n\t\t\t\t\t\t\t\t\t\t\tthat = undefined;\n\t\t\t\t\t\t\t\t\t\t\targs = [ returned ];\n\t\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t\t// Process the value(s)\n\t\t\t\t\t\t\t\t\t\t// Default process is resolve\n\t\t\t\t\t\t\t\t\t\t( special || deferred.resolveWith )( that, args );\n\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t},\n\n\t\t\t\t\t\t\t\t// Only normal processors (resolve) catch and reject exceptions\n\t\t\t\t\t\t\t\tprocess = special ?\n\t\t\t\t\t\t\t\t\tmightThrow :\n\t\t\t\t\t\t\t\t\tfunction() {\n\t\t\t\t\t\t\t\t\t\ttry {\n\t\t\t\t\t\t\t\t\t\t\tmightThrow();\n\t\t\t\t\t\t\t\t\t\t} catch ( e ) {\n\n\t\t\t\t\t\t\t\t\t\t\tif ( jQuery.Deferred.exceptionHook ) {\n\t\t\t\t\t\t\t\t\t\t\t\tjQuery.Deferred.exceptionHook( e,\n\t\t\t\t\t\t\t\t\t\t\t\t\tprocess.stackTrace );\n\t\t\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t\t\t// Support: Promises/A+ section 2.3.3.3.4.1\n\t\t\t\t\t\t\t\t\t\t\t// https://promisesaplus.com/#point-61\n\t\t\t\t\t\t\t\t\t\t\t// Ignore post-resolution exceptions\n\t\t\t\t\t\t\t\t\t\t\tif ( depth + 1 >= maxDepth ) {\n\n\t\t\t\t\t\t\t\t\t\t\t\t// Only substitute handlers pass on context\n\t\t\t\t\t\t\t\t\t\t\t\t// and multiple values (non-spec behavior)\n\t\t\t\t\t\t\t\t\t\t\t\tif ( handler !== Thrower ) {\n\t\t\t\t\t\t\t\t\t\t\t\t\tthat = undefined;\n\t\t\t\t\t\t\t\t\t\t\t\t\targs = [ e ];\n\t\t\t\t\t\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t\t\t\t\t\tdeferred.rejectWith( that, args );\n\t\t\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t\t};\n\n\t\t\t\t\t\t\t// Support: Promises/A+ section 2.3.3.3.1\n\t\t\t\t\t\t\t// https://promisesaplus.com/#point-57\n\t\t\t\t\t\t\t// Re-resolve promises immediately to dodge false rejection from\n\t\t\t\t\t\t\t// subsequent errors\n\t\t\t\t\t\t\tif ( depth ) {\n\t\t\t\t\t\t\t\tprocess();\n\t\t\t\t\t\t\t} else {\n\n\t\t\t\t\t\t\t\t// Call an optional hook to record the stack, in case of exception\n\t\t\t\t\t\t\t\t// since it\'s otherwise lost when execution goes async\n\t\t\t\t\t\t\t\tif ( jQuery.Deferred.getStackHook ) {\n\t\t\t\t\t\t\t\t\tprocess.stackTrace = jQuery.Deferred.getStackHook();\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\twindow.setTimeout( process );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t};\n\t\t\t\t\t}\n\n\t\t\t\t\treturn jQuery.Deferred( function( newDefer ) {\n\n\t\t\t\t\t\t// progress_handlers.add( ... )\n\t\t\t\t\t\ttuples[ 0 ][ 3 ].add(\n\t\t\t\t\t\t\tresolve(\n\t\t\t\t\t\t\t\t0,\n\t\t\t\t\t\t\t\tnewDefer,\n\t\t\t\t\t\t\t\tisFunction( onProgress ) ?\n\t\t\t\t\t\t\t\t\tonProgress :\n\t\t\t\t\t\t\t\t\tIdentity,\n\t\t\t\t\t\t\t\tnewDefer.notifyWith\n\t\t\t\t\t\t\t)\n\t\t\t\t\t\t);\n\n\t\t\t\t\t\t// fulfilled_handlers.add( ... )\n\t\t\t\t\t\ttuples[ 1 ][ 3 ].add(\n\t\t\t\t\t\t\tresolve(\n\t\t\t\t\t\t\t\t0,\n\t\t\t\t\t\t\t\tnewDefer,\n\t\t\t\t\t\t\t\tisFunction( onFulfilled ) ?\n\t\t\t\t\t\t\t\t\tonFulfilled :\n\t\t\t\t\t\t\t\t\tIdentity\n\t\t\t\t\t\t\t)\n\t\t\t\t\t\t);\n\n\t\t\t\t\t\t// rejected_handlers.add( ... )\n\t\t\t\t\t\ttuples[ 2 ][ 3 ].add(\n\t\t\t\t\t\t\tresolve(\n\t\t\t\t\t\t\t\t0,\n\t\t\t\t\t\t\t\tnewDefer,\n\t\t\t\t\t\t\t\tisFunction( onRejected ) ?\n\t\t\t\t\t\t\t\t\tonRejected :\n\t\t\t\t\t\t\t\t\tThrower\n\t\t\t\t\t\t\t)\n\t\t\t\t\t\t);\n\t\t\t\t\t} ).promise();\n\t\t\t\t},\n\n\t\t\t\t// Get a promise for this deferred\n\t\t\t\t// If obj is provided, the promise aspect is added to the object\n\t\t\t\tpromise: function( obj ) {\n\t\t\t\t\treturn obj != null ? jQuery.extend( obj, promise ) : promise;\n\t\t\t\t}\n\t\t\t},\n\t\t\tdeferred = {};\n\n\t\t// Add list-specific methods\n\t\tjQuery.each( tuples, function( i, tuple ) {\n\t\t\tvar list = tuple[ 2 ],\n\t\t\t\tstateString = tuple[ 5 ];\n\n\t\t\t// promise.progress = list.add\n\t\t\t// promise.done = list.add\n\t\t\t// promise.fail = list.add\n\t\t\tpromise[ tuple[ 1 ] ] = list.add;\n\n\t\t\t// Handle state\n\t\t\tif ( stateString ) {\n\t\t\t\tlist.add(\n\t\t\t\t\tfunction() {\n\n\t\t\t\t\t\t// state = "resolved" (i.e., fulfilled)\n\t\t\t\t\t\t// state = "rejected"\n\t\t\t\t\t\tstate = stateString;\n\t\t\t\t\t},\n\n\t\t\t\t\t// rejected_callbacks.disable\n\t\t\t\t\t// fulfilled_callbacks.disable\n\t\t\t\t\ttuples[ 3 - i ][ 2 ].disable,\n\n\t\t\t\t\t// rejected_handlers.disable\n\t\t\t\t\t// fulfilled_handlers.disable\n\t\t\t\t\ttuples[ 3 - i ][ 3 ].disable,\n\n\t\t\t\t\t// progress_callbacks.lock\n\t\t\t\t\ttuples[ 0 ][ 2 ].lock,\n\n\t\t\t\t\t// progress_handlers.lock\n\t\t\t\t\ttuples[ 0 ][ 3 ].lock\n\t\t\t\t);\n\t\t\t}\n\n\t\t\t// progress_handlers.fire\n\t\t\t// fulfilled_handlers.fire\n\t\t\t// rejected_handlers.fire\n\t\t\tlist.add( tuple[ 3 ].fire );\n\n\t\t\t// deferred.notify = function() { deferred.notifyWith(...) }\n\t\t\t// deferred.resolve = function() { deferred.resolveWith(...) }\n\t\t\t// deferred.reject = function() { deferred.rejectWith(...) }\n\t\t\tdeferred[ tuple[ 0 ] ] = function() {\n\t\t\t\tdeferred[ tuple[ 0 ] + "With" ]( this === deferred ? undefined : this, arguments );\n\t\t\t\treturn this;\n\t\t\t};\n\n\t\t\t// deferred.notifyWith = list.fireWith\n\t\t\t// deferred.resolveWith = list.fireWith\n\t\t\t// deferred.rejectWith = list.fireWith\n\t\t\tdeferred[ tuple[ 0 ] + "With" ] = list.fireWith;\n\t\t} );\n\n\t\t// Make the deferred a promise\n\t\tpromise.promise( deferred );\n\n\t\t// Call given func if any\n\t\tif ( func ) {\n\t\t\tfunc.call( deferred, deferred );\n\t\t}\n\n\t\t// All done!\n\t\treturn deferred;\n\t},\n\n\t// Deferred helper\n\twhen: function( singleValue ) {\n\t\tvar\n\n\t\t\t// count of uncompleted subordinates\n\t\t\tremaining = arguments.length,\n\n\t\t\t// count of unprocessed arguments\n\t\t\ti = remaining,\n\n\t\t\t// subordinate fulfillment data\n\t\t\tresolveContexts = Array( i ),\n\t\t\tresolveValues = slice.call( arguments ),\n\n\t\t\t// the master Deferred\n\t\t\tmaster = jQuery.Deferred(),\n\n\t\t\t// subordinate callback factory\n\t\t\tupdateFunc = function( i ) {\n\t\t\t\treturn function( value ) {\n\t\t\t\t\tresolveContexts[ i ] = this;\n\t\t\t\t\tresolveValues[ i ] = arguments.length > 1 ? slice.call( arguments ) : value;\n\t\t\t\t\tif ( !( --remaining ) ) {\n\t\t\t\t\t\tmaster.resolveWith( resolveContexts, resolveValues );\n\t\t\t\t\t}\n\t\t\t\t};\n\t\t\t};\n\n\t\t// Single- and empty arguments are adopted like Promise.resolve\n\t\tif ( remaining <= 1 ) {\n\t\t\tadoptValue( singleValue, master.done( updateFunc( i ) ).resolve, master.reject,\n\t\t\t\t!remaining );\n\n\t\t\t// Use .then() to unwrap secondary thenables (cf. gh-3000)\n\t\t\tif ( master.state() === "pending" ||\n\t\t\t\tisFunction( resolveValues[ i ] && resolveValues[ i ].then ) ) {\n\n\t\t\t\treturn master.then();\n\t\t\t}\n\t\t}\n\n\t\t// Multiple arguments are aggregated like Promise.all array elements\n\t\twhile ( i-- ) {\n\t\t\tadoptValue( resolveValues[ i ], updateFunc( i ), master.reject );\n\t\t}\n\n\t\treturn master.promise();\n\t}\n} );\n\n\n// These usually indicate a programmer mistake during development,\n// warn about them ASAP rather than swallowing them by default.\nvar rerrorNames = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;\n\njQuery.Deferred.exceptionHook = function( error, stack ) {\n\n\t// Support: IE 8 - 9 only\n\t// Console exists when dev tools are open, which can happen at any time\n\tif ( window.console && window.console.warn && error && rerrorNames.test( error.name ) ) {\n\t\twindow.console.warn( "jQuery.Deferred exception: " + error.message, error.stack, stack );\n\t}\n};\n\n\n\n\njQuery.readyException = function( error ) {\n\twindow.setTimeout( function() {\n\t\tthrow error;\n\t} );\n};\n\n\n\n\n// The deferred used on DOM ready\nvar readyList = jQuery.Deferred();\n\njQuery.fn.ready = function( fn ) {\n\n\treadyList\n\t\t.then( fn )\n\n\t\t// Wrap jQuery.readyException in a function so that the lookup\n\t\t// happens at the time of error handling instead of callback\n\t\t// registration.\n\t\t.catch( function( error ) {\n\t\t\tjQuery.readyException( error );\n\t\t} );\n\n\treturn this;\n};\n\njQuery.extend( {\n\n\t// Is the DOM ready to be used? Set to true once it occurs.\n\tisReady: false,\n\n\t// A counter to track how many items to wait for before\n\t// the ready event fires. See #6781\n\treadyWait: 1,\n\n\t// Handle when the DOM is ready\n\tready: function( wait ) {\n\n\t\t// Abort if there are pending holds or we\'re already ready\n\t\tif ( wait === true ? --jQuery.readyWait : jQuery.isReady ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Remember that the DOM is ready\n\t\tjQuery.isReady = true;\n\n\t\t// If a normal DOM Ready event fired, decrement, and wait if need be\n\t\tif ( wait !== true && --jQuery.readyWait > 0 ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// If there are functions bound, to execute\n\t\treadyList.resolveWith( document, [ jQuery ] );\n\t}\n} );\n\njQuery.ready.then = readyList.then;\n\n// The ready event handler and self cleanup method\nfunction completed() {\n\tdocument.removeEventListener( "DOMContentLoaded", completed );\n\twindow.removeEventListener( "load", completed );\n\tjQuery.ready();\n}\n\n// Catch cases where $(document).ready() is called\n// after the browser event has already occurred.\n// Support: IE <=9 - 10 only\n// Older IE sometimes signals "interactive" too soon\nif ( document.readyState === "complete" ||\n\t( document.readyState !== "loading" && !document.documentElement.doScroll ) ) {\n\n\t// Handle it asynchronously to allow scripts the opportunity to delay ready\n\twindow.setTimeout( jQuery.ready );\n\n} else {\n\n\t// Use the handy event callback\n\tdocument.addEventListener( "DOMContentLoaded", completed );\n\n\t// A fallback to window.onload, that will always work\n\twindow.addEventListener( "load", completed );\n}\n\n\n\n\n// Multifunctional method to get and set values of a collection\n// The value/s can optionally be executed if it\'s a function\nvar access = function( elems, fn, key, value, chainable, emptyGet, raw ) {\n\tvar i = 0,\n\t\tlen = elems.length,\n\t\tbulk = key == null;\n\n\t// Sets many values\n\tif ( toType( key ) === "object" ) {\n\t\tchainable = true;\n\t\tfor ( i in key ) {\n\t\t\taccess( elems, fn, i, key[ i ], true, emptyGet, raw );\n\t\t}\n\n\t// Sets one value\n\t} else if ( value !== undefined ) {\n\t\tchainable = true;\n\n\t\tif ( !isFunction( value ) ) {\n\t\t\traw = true;\n\t\t}\n\n\t\tif ( bulk ) {\n\n\t\t\t// Bulk operations run against the entire set\n\t\t\tif ( raw ) {\n\t\t\t\tfn.call( elems, value );\n\t\t\t\tfn = null;\n\n\t\t\t// ...except when executing function values\n\t\t\t} else {\n\t\t\t\tbulk = fn;\n\t\t\t\tfn = function( elem, key, value ) {\n\t\t\t\t\treturn bulk.call( jQuery( elem ), value );\n\t\t\t\t};\n\t\t\t}\n\t\t}\n\n\t\tif ( fn ) {\n\t\t\tfor ( ; i < len; i++ ) {\n\t\t\t\tfn(\n\t\t\t\t\telems[ i ], key, raw ?\n\t\t\t\t\tvalue :\n\t\t\t\t\tvalue.call( elems[ i ], i, fn( elems[ i ], key ) )\n\t\t\t\t);\n\t\t\t}\n\t\t}\n\t}\n\n\tif ( chainable ) {\n\t\treturn elems;\n\t}\n\n\t// Gets\n\tif ( bulk ) {\n\t\treturn fn.call( elems );\n\t}\n\n\treturn len ? fn( elems[ 0 ], key ) : emptyGet;\n};\n\n\n// Matches dashed string for camelizing\nvar rmsPrefix = /^-ms-/,\n\trdashAlpha = /-([a-z])/g;\n\n// Used by camelCase as callback to replace()\nfunction fcamelCase( all, letter ) {\n\treturn letter.toUpperCase();\n}\n\n// Convert dashed to camelCase; used by the css and data modules\n// Support: IE <=9 - 11, Edge 12 - 15\n// Microsoft forgot to hump their vendor prefix (#9572)\nfunction camelCase( string ) {\n\treturn string.replace( rmsPrefix, "ms-" ).replace( rdashAlpha, fcamelCase );\n}\nvar acceptData = function( owner ) {\n\n\t// Accepts only:\n\t//  - Node\n\t//    - Node.ELEMENT_NODE\n\t//    - Node.DOCUMENT_NODE\n\t//  - Object\n\t//    - Any\n\treturn owner.nodeType === 1 || owner.nodeType === 9 || !( +owner.nodeType );\n};\n\n\n\n\nfunction Data() {\n\tthis.expando = jQuery.expando + Data.uid++;\n}\n\nData.uid = 1;\n\nData.prototype = {\n\n\tcache: function( owner ) {\n\n\t\t// Check if the owner object already has a cache\n\t\tvar value = owner[ this.expando ];\n\n\t\t// If not, create one\n\t\tif ( !value ) {\n\t\t\tvalue = {};\n\n\t\t\t// We can accept data for non-element nodes in modern browsers,\n\t\t\t// but we should not, see #8335.\n\t\t\t// Always return an empty object.\n\t\t\tif ( acceptData( owner ) ) {\n\n\t\t\t\t// If it is a node unlikely to be stringify-ed or looped over\n\t\t\t\t// use plain assignment\n\t\t\t\tif ( owner.nodeType ) {\n\t\t\t\t\towner[ this.expando ] = value;\n\n\t\t\t\t// Otherwise secure it in a non-enumerable property\n\t\t\t\t// configurable must be true to allow the property to be\n\t\t\t\t// deleted when data is removed\n\t\t\t\t} else {\n\t\t\t\t\tObject.defineProperty( owner, this.expando, {\n\t\t\t\t\t\tvalue: value,\n\t\t\t\t\t\tconfigurable: true\n\t\t\t\t\t} );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn value;\n\t},\n\tset: function( owner, data, value ) {\n\t\tvar prop,\n\t\t\tcache = this.cache( owner );\n\n\t\t// Handle: [ owner, key, value ] args\n\t\t// Always use camelCase key (gh-2257)\n\t\tif ( typeof data === "string" ) {\n\t\t\tcache[ camelCase( data ) ] = value;\n\n\t\t// Handle: [ owner, { properties } ] args\n\t\t} else {\n\n\t\t\t// Copy the properties one-by-one to the cache object\n\t\t\tfor ( prop in data ) {\n\t\t\t\tcache[ camelCase( prop ) ] = data[ prop ];\n\t\t\t}\n\t\t}\n\t\treturn cache;\n\t},\n\tget: function( owner, key ) {\n\t\treturn key === undefined ?\n\t\t\tthis.cache( owner ) :\n\n\t\t\t// Always use camelCase key (gh-2257)\n\t\t\towner[ this.expando ] && owner[ this.expando ][ camelCase( key ) ];\n\t},\n\taccess: function( owner, key, value ) {\n\n\t\t// In cases where either:\n\t\t//\n\t\t//   1. No key was specified\n\t\t//   2. A string key was specified, but no value provided\n\t\t//\n\t\t// Take the "read" path and allow the get method to determine\n\t\t// which value to return, respectively either:\n\t\t//\n\t\t//   1. The entire cache object\n\t\t//   2. The data stored at the key\n\t\t//\n\t\tif ( key === undefined ||\n\t\t\t\t( ( key && typeof key === "string" ) && value === undefined ) ) {\n\n\t\t\treturn this.get( owner, key );\n\t\t}\n\n\t\t// When the key is not a string, or both a key and value\n\t\t// are specified, set or extend (existing objects) with either:\n\t\t//\n\t\t//   1. An object of properties\n\t\t//   2. A key and value\n\t\t//\n\t\tthis.set( owner, key, value );\n\n\t\t// Since the "set" path can have two possible entry points\n\t\t// return the expected data based on which path was taken[*]\n\t\treturn value !== undefined ? value : key;\n\t},\n\tremove: function( owner, key ) {\n\t\tvar i,\n\t\t\tcache = owner[ this.expando ];\n\n\t\tif ( cache === undefined ) {\n\t\t\treturn;\n\t\t}\n\n\t\tif ( key !== undefined ) {\n\n\t\t\t// Support array or space separated string of keys\n\t\t\tif ( Array.isArray( key ) ) {\n\n\t\t\t\t// If key is an array of keys...\n\t\t\t\t// We always set camelCase keys, so remove that.\n\t\t\t\tkey = key.map( camelCase );\n\t\t\t} else {\n\t\t\t\tkey = camelCase( key );\n\n\t\t\t\t// If a key with the spaces exists, use it.\n\t\t\t\t// Otherwise, create an array by matching non-whitespace\n\t\t\t\tkey = key in cache ?\n\t\t\t\t\t[ key ] :\n\t\t\t\t\t( key.match( rnothtmlwhite ) || [] );\n\t\t\t}\n\n\t\t\ti = key.length;\n\n\t\t\twhile ( i-- ) {\n\t\t\t\tdelete cache[ key[ i ] ];\n\t\t\t}\n\t\t}\n\n\t\t// Remove the expando if there\'s no more data\n\t\tif ( key === undefined || jQuery.isEmptyObject( cache ) ) {\n\n\t\t\t// Support: Chrome <=35 - 45\n\t\t\t// Webkit & Blink performance suffers when deleting properties\n\t\t\t// from DOM nodes, so set to undefined instead\n\t\t\t// https://bugs.chromium.org/p/chromium/issues/detail?id=378607 (bug restricted)\n\t\t\tif ( owner.nodeType ) {\n\t\t\t\towner[ this.expando ] = undefined;\n\t\t\t} else {\n\t\t\t\tdelete owner[ this.expando ];\n\t\t\t}\n\t\t}\n\t},\n\thasData: function( owner ) {\n\t\tvar cache = owner[ this.expando ];\n\t\treturn cache !== undefined && !jQuery.isEmptyObject( cache );\n\t}\n};\nvar dataPriv = new Data();\n\nvar dataUser = new Data();\n\n\n\n//\tImplementation Summary\n//\n//\t1. Enforce API surface and semantic compatibility with 1.9.x branch\n//\t2. Improve the module\'s maintainability by reducing the storage\n//\t\tpaths to a single mechanism.\n//\t3. Use the same single mechanism to support "private" and "user" data.\n//\t4. _Never_ expose "private" data to user code (TODO: Drop _data, _removeData)\n//\t5. Avoid exposing implementation details on user objects (eg. expando properties)\n//\t6. Provide a clear path for implementation upgrade to WeakMap in 2014\n\nvar rbrace = /^(?:\\{[\\w\\W]*\\}|\\[[\\w\\W]*\\])$/,\n\trmultiDash = /[A-Z]/g;\n\nfunction getData( data ) {\n\tif ( data === "true" ) {\n\t\treturn true;\n\t}\n\n\tif ( data === "false" ) {\n\t\treturn false;\n\t}\n\n\tif ( data === "null" ) {\n\t\treturn null;\n\t}\n\n\t// Only convert to a number if it doesn\'t change the string\n\tif ( data === +data + "" ) {\n\t\treturn +data;\n\t}\n\n\tif ( rbrace.test( data ) ) {\n\t\treturn JSON.parse( data );\n\t}\n\n\treturn data;\n}\n\nfunction dataAttr( elem, key, data ) {\n\tvar name;\n\n\t// If nothing was found internally, try to fetch any\n\t// data from the HTML5 data-* attribute\n\tif ( data === undefined && elem.nodeType === 1 ) {\n\t\tname = "data-" + key.replace( rmultiDash, "-$&" ).toLowerCase();\n\t\tdata = elem.getAttribute( name );\n\n\t\tif ( typeof data === "string" ) {\n\t\t\ttry {\n\t\t\t\tdata = getData( data );\n\t\t\t} catch ( e ) {}\n\n\t\t\t// Make sure we set the data so it isn\'t changed later\n\t\t\tdataUser.set( elem, key, data );\n\t\t} else {\n\t\t\tdata = undefined;\n\t\t}\n\t}\n\treturn data;\n}\n\njQuery.extend( {\n\thasData: function( elem ) {\n\t\treturn dataUser.hasData( elem ) || dataPriv.hasData( elem );\n\t},\n\n\tdata: function( elem, name, data ) {\n\t\treturn dataUser.access( elem, name, data );\n\t},\n\n\tremoveData: function( elem, name ) {\n\t\tdataUser.remove( elem, name );\n\t},\n\n\t// TODO: Now that all calls to _data and _removeData have been replaced\n\t// with direct calls to dataPriv methods, these can be deprecated.\n\t_data: function( elem, name, data ) {\n\t\treturn dataPriv.access( elem, name, data );\n\t},\n\n\t_removeData: function( elem, name ) {\n\t\tdataPriv.remove( elem, name );\n\t}\n} );\n\njQuery.fn.extend( {\n\tdata: function( key, value ) {\n\t\tvar i, name, data,\n\t\t\telem = this[ 0 ],\n\t\t\tattrs = elem && elem.attributes;\n\n\t\t// Gets all values\n\t\tif ( key === undefined ) {\n\t\t\tif ( this.length ) {\n\t\t\t\tdata = dataUser.get( elem );\n\n\t\t\t\tif ( elem.nodeType === 1 && !dataPriv.get( elem, "hasDataAttrs" ) ) {\n\t\t\t\t\ti = attrs.length;\n\t\t\t\t\twhile ( i-- ) {\n\n\t\t\t\t\t\t// Support: IE 11 only\n\t\t\t\t\t\t// The attrs elements can be null (#14894)\n\t\t\t\t\t\tif ( attrs[ i ] ) {\n\t\t\t\t\t\t\tname = attrs[ i ].name;\n\t\t\t\t\t\t\tif ( name.indexOf( "data-" ) === 0 ) {\n\t\t\t\t\t\t\t\tname = camelCase( name.slice( 5 ) );\n\t\t\t\t\t\t\t\tdataAttr( elem, name, data[ name ] );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t\tdataPriv.set( elem, "hasDataAttrs", true );\n\t\t\t\t}\n\t\t\t}\n\n\t\t\treturn data;\n\t\t}\n\n\t\t// Sets multiple values\n\t\tif ( typeof key === "object" ) {\n\t\t\treturn this.each( function() {\n\t\t\t\tdataUser.set( this, key );\n\t\t\t} );\n\t\t}\n\n\t\treturn access( this, function( value ) {\n\t\t\tvar data;\n\n\t\t\t// The calling jQuery object (element matches) is not empty\n\t\t\t// (and therefore has an element appears at this[ 0 ]) and the\n\t\t\t// `value` parameter was not undefined. An empty jQuery object\n\t\t\t// will result in `undefined` for elem = this[ 0 ] which will\n\t\t\t// throw an exception if an attempt to read a data cache is made.\n\t\t\tif ( elem && value === undefined ) {\n\n\t\t\t\t// Attempt to get data from the cache\n\t\t\t\t// The key will always be camelCased in Data\n\t\t\t\tdata = dataUser.get( elem, key );\n\t\t\t\tif ( data !== undefined ) {\n\t\t\t\t\treturn data;\n\t\t\t\t}\n\n\t\t\t\t// Attempt to "discover" the data in\n\t\t\t\t// HTML5 custom data-* attrs\n\t\t\t\tdata = dataAttr( elem, key );\n\t\t\t\tif ( data !== undefined ) {\n\t\t\t\t\treturn data;\n\t\t\t\t}\n\n\t\t\t\t// We tried really hard, but the data doesn\'t exist.\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\t// Set the data...\n\t\t\tthis.each( function() {\n\n\t\t\t\t// We always store the camelCased key\n\t\t\t\tdataUser.set( this, key, value );\n\t\t\t} );\n\t\t}, null, value, arguments.length > 1, null, true );\n\t},\n\n\tremoveData: function( key ) {\n\t\treturn this.each( function() {\n\t\t\tdataUser.remove( this, key );\n\t\t} );\n\t}\n} );\n\n\njQuery.extend( {\n\tqueue: function( elem, type, data ) {\n\t\tvar queue;\n\n\t\tif ( elem ) {\n\t\t\ttype = ( type || "fx" ) + "queue";\n\t\t\tqueue = dataPriv.get( elem, type );\n\n\t\t\t// Speed up dequeue by getting out quickly if this is just a lookup\n\t\t\tif ( data ) {\n\t\t\t\tif ( !queue || Array.isArray( data ) ) {\n\t\t\t\t\tqueue = dataPriv.access( elem, type, jQuery.makeArray( data ) );\n\t\t\t\t} else {\n\t\t\t\t\tqueue.push( data );\n\t\t\t\t}\n\t\t\t}\n\t\t\treturn queue || [];\n\t\t}\n\t},\n\n\tdequeue: function( elem, type ) {\n\t\ttype = type || "fx";\n\n\t\tvar queue = jQuery.queue( elem, type ),\n\t\t\tstartLength = queue.length,\n\t\t\tfn = queue.shift(),\n\t\t\thooks = jQuery._queueHooks( elem, type ),\n\t\t\tnext = function() {\n\t\t\t\tjQuery.dequeue( elem, type );\n\t\t\t};\n\n\t\t// If the fx queue is dequeued, always remove the progress sentinel\n\t\tif ( fn === "inprogress" ) {\n\t\t\tfn = queue.shift();\n\t\t\tstartLength--;\n\t\t}\n\n\t\tif ( fn ) {\n\n\t\t\t// Add a progress sentinel to prevent the fx queue from being\n\t\t\t// automatically dequeued\n\t\t\tif ( type === "fx" ) {\n\t\t\t\tqueue.unshift( "inprogress" );\n\t\t\t}\n\n\t\t\t// Clear up the last queue stop function\n\t\t\tdelete hooks.stop;\n\t\t\tfn.call( elem, next, hooks );\n\t\t}\n\n\t\tif ( !startLength && hooks ) {\n\t\t\thooks.empty.fire();\n\t\t}\n\t},\n\n\t// Not public - generate a queueHooks object, or return the current one\n\t_queueHooks: function( elem, type ) {\n\t\tvar key = type + "queueHooks";\n\t\treturn dataPriv.get( elem, key ) || dataPriv.access( elem, key, {\n\t\t\tempty: jQuery.Callbacks( "once memory" ).add( function() {\n\t\t\t\tdataPriv.remove( elem, [ type + "queue", key ] );\n\t\t\t} )\n\t\t} );\n\t}\n} );\n\njQuery.fn.extend( {\n\tqueue: function( type, data ) {\n\t\tvar setter = 2;\n\n\t\tif ( typeof type !== "string" ) {\n\t\t\tdata = type;\n\t\t\ttype = "fx";\n\t\t\tsetter--;\n\t\t}\n\n\t\tif ( arguments.length < setter ) {\n\t\t\treturn jQuery.queue( this[ 0 ], type );\n\t\t}\n\n\t\treturn data === undefined ?\n\t\t\tthis :\n\t\t\tthis.each( function() {\n\t\t\t\tvar queue = jQuery.queue( this, type, data );\n\n\t\t\t\t// Ensure a hooks for this queue\n\t\t\t\tjQuery._queueHooks( this, type );\n\n\t\t\t\tif ( type === "fx" && queue[ 0 ] !== "inprogress" ) {\n\t\t\t\t\tjQuery.dequeue( this, type );\n\t\t\t\t}\n\t\t\t} );\n\t},\n\tdequeue: function( type ) {\n\t\treturn this.each( function() {\n\t\t\tjQuery.dequeue( this, type );\n\t\t} );\n\t},\n\tclearQueue: function( type ) {\n\t\treturn this.queue( type || "fx", [] );\n\t},\n\n\t// Get a promise resolved when queues of a certain type\n\t// are emptied (fx is the type by default)\n\tpromise: function( type, obj ) {\n\t\tvar tmp,\n\t\t\tcount = 1,\n\t\t\tdefer = jQuery.Deferred(),\n\t\t\telements = this,\n\t\t\ti = this.length,\n\t\t\tresolve = function() {\n\t\t\t\tif ( !( --count ) ) {\n\t\t\t\t\tdefer.resolveWith( elements, [ elements ] );\n\t\t\t\t}\n\t\t\t};\n\n\t\tif ( typeof type !== "string" ) {\n\t\t\tobj = type;\n\t\t\ttype = undefined;\n\t\t}\n\t\ttype = type || "fx";\n\n\t\twhile ( i-- ) {\n\t\t\ttmp = dataPriv.get( elements[ i ], type + "queueHooks" );\n\t\t\tif ( tmp && tmp.empty ) {\n\t\t\t\tcount++;\n\t\t\t\ttmp.empty.add( resolve );\n\t\t\t}\n\t\t}\n\t\tresolve();\n\t\treturn defer.promise( obj );\n\t}\n} );\nvar pnum = ( /[+-]?(?:\\d*\\.|)\\d+(?:[eE][+-]?\\d+|)/ ).source;\n\nvar rcssNum = new RegExp( "^(?:([+-])=|)(" + pnum + ")([a-z%]*)$", "i" );\n\n\nvar cssExpand = [ "Top", "Right", "Bottom", "Left" ];\n\nvar documentElement = document.documentElement;\n\n\n\n\tvar isAttached = function( elem ) {\n\t\t\treturn jQuery.contains( elem.ownerDocument, elem );\n\t\t},\n\t\tcomposed = { composed: true };\n\n\t// Check attachment across shadow DOM boundaries when possible (gh-3504)\n\tif ( documentElement.attachShadow ) {\n\t\tisAttached = function( elem ) {\n\t\t\treturn jQuery.contains( elem.ownerDocument, elem ) ||\n\t\t\t\telem.getRootNode( composed ) === elem.ownerDocument;\n\t\t};\n\t}\nvar isHiddenWithinTree = function( elem, el ) {\n\n\t\t// isHiddenWithinTree might be called from jQuery#filter function;\n\t\t// in that case, element will be second argument\n\t\telem = el || elem;\n\n\t\t// Inline style trumps all\n\t\treturn elem.style.display === "none" ||\n\t\t\telem.style.display === "" &&\n\n\t\t\t// Otherwise, check computed style\n\t\t\t// Support: Firefox <=43 - 45\n\t\t\t// Disconnected elements can have computed display: none, so first confirm that elem is\n\t\t\t// in the document.\n\t\t\tisAttached( elem ) &&\n\n\t\t\tjQuery.css( elem, "display" ) === "none";\n\t};\n\nvar swap = function( elem, options, callback, args ) {\n\tvar ret, name,\n\t\told = {};\n\n\t// Remember the old values, and insert the new ones\n\tfor ( name in options ) {\n\t\told[ name ] = elem.style[ name ];\n\t\telem.style[ name ] = options[ name ];\n\t}\n\n\tret = callback.apply( elem, args || [] );\n\n\t// Revert the old values\n\tfor ( name in options ) {\n\t\telem.style[ name ] = old[ name ];\n\t}\n\n\treturn ret;\n};\n\n\n\n\nfunction adjustCSS( elem, prop, valueParts, tween ) {\n\tvar adjusted, scale,\n\t\tmaxIterations = 20,\n\t\tcurrentValue = tween ?\n\t\t\tfunction() {\n\t\t\t\treturn tween.cur();\n\t\t\t} :\n\t\t\tfunction() {\n\t\t\t\treturn jQuery.css( elem, prop, "" );\n\t\t\t},\n\t\tinitial = currentValue(),\n\t\tunit = valueParts && valueParts[ 3 ] || ( jQuery.cssNumber[ prop ] ? "" : "px" ),\n\n\t\t// Starting value computation is required for potential unit mismatches\n\t\tinitialInUnit = elem.nodeType &&\n\t\t\t( jQuery.cssNumber[ prop ] || unit !== "px" && +initial ) &&\n\t\t\trcssNum.exec( jQuery.css( elem, prop ) );\n\n\tif ( initialInUnit && initialInUnit[ 3 ] !== unit ) {\n\n\t\t// Support: Firefox <=54\n\t\t// Halve the iteration target value to prevent interference from CSS upper bounds (gh-2144)\n\t\tinitial = initial / 2;\n\n\t\t// Trust units reported by jQuery.css\n\t\tunit = unit || initialInUnit[ 3 ];\n\n\t\t// Iteratively approximate from a nonzero starting point\n\t\tinitialInUnit = +initial || 1;\n\n\t\twhile ( maxIterations-- ) {\n\n\t\t\t// Evaluate and update our best guess (doubling guesses that zero out).\n\t\t\t// Finish if the scale equals or crosses 1 (making the old*new product non-positive).\n\t\t\tjQuery.style( elem, prop, initialInUnit + unit );\n\t\t\tif ( ( 1 - scale ) * ( 1 - ( scale = currentValue() / initial || 0.5 ) ) <= 0 ) {\n\t\t\t\tmaxIterations = 0;\n\t\t\t}\n\t\t\tinitialInUnit = initialInUnit / scale;\n\n\t\t}\n\n\t\tinitialInUnit = initialInUnit * 2;\n\t\tjQuery.style( elem, prop, initialInUnit + unit );\n\n\t\t// Make sure we update the tween properties later on\n\t\tvalueParts = valueParts || [];\n\t}\n\n\tif ( valueParts ) {\n\t\tinitialInUnit = +initialInUnit || +initial || 0;\n\n\t\t// Apply relative offset (+=/-=) if specified\n\t\tadjusted = valueParts[ 1 ] ?\n\t\t\tinitialInUnit + ( valueParts[ 1 ] + 1 ) * valueParts[ 2 ] :\n\t\t\t+valueParts[ 2 ];\n\t\tif ( tween ) {\n\t\t\ttween.unit = unit;\n\t\t\ttween.start = initialInUnit;\n\t\t\ttween.end = adjusted;\n\t\t}\n\t}\n\treturn adjusted;\n}\n\n\nvar defaultDisplayMap = {};\n\nfunction getDefaultDisplay( elem ) {\n\tvar temp,\n\t\tdoc = elem.ownerDocument,\n\t\tnodeName = elem.nodeName,\n\t\tdisplay = defaultDisplayMap[ nodeName ];\n\n\tif ( display ) {\n\t\treturn display;\n\t}\n\n\ttemp = doc.body.appendChild( doc.createElement( nodeName ) );\n\tdisplay = jQuery.css( temp, "display" );\n\n\ttemp.parentNode.removeChild( temp );\n\n\tif ( display === "none" ) {\n\t\tdisplay = "block";\n\t}\n\tdefaultDisplayMap[ nodeName ] = display;\n\n\treturn display;\n}\n\nfunction showHide( elements, show ) {\n\tvar display, elem,\n\t\tvalues = [],\n\t\tindex = 0,\n\t\tlength = elements.length;\n\n\t// Determine new display value for elements that need to change\n\tfor ( ; index < length; index++ ) {\n\t\telem = elements[ index ];\n\t\tif ( !elem.style ) {\n\t\t\tcontinue;\n\t\t}\n\n\t\tdisplay = elem.style.display;\n\t\tif ( show ) {\n\n\t\t\t// Since we force visibility upon cascade-hidden elements, an immediate (and slow)\n\t\t\t// check is required in this first loop unless we have a nonempty display value (either\n\t\t\t// inline or about-to-be-restored)\n\t\t\tif ( display === "none" ) {\n\t\t\t\tvalues[ index ] = dataPriv.get( elem, "display" ) || null;\n\t\t\t\tif ( !values[ index ] ) {\n\t\t\t\t\telem.style.display = "";\n\t\t\t\t}\n\t\t\t}\n\t\t\tif ( elem.style.display === "" && isHiddenWithinTree( elem ) ) {\n\t\t\t\tvalues[ index ] = getDefaultDisplay( elem );\n\t\t\t}\n\t\t} else {\n\t\t\tif ( display !== "none" ) {\n\t\t\t\tvalues[ index ] = "none";\n\n\t\t\t\t// Remember what we\'re overwriting\n\t\t\t\tdataPriv.set( elem, "display", display );\n\t\t\t}\n\t\t}\n\t}\n\n\t// Set the display of the elements in a second loop to avoid constant reflow\n\tfor ( index = 0; index < length; index++ ) {\n\t\tif ( values[ index ] != null ) {\n\t\t\telements[ index ].style.display = values[ index ];\n\t\t}\n\t}\n\n\treturn elements;\n}\n\njQuery.fn.extend( {\n\tshow: function() {\n\t\treturn showHide( this, true );\n\t},\n\thide: function() {\n\t\treturn showHide( this );\n\t},\n\ttoggle: function( state ) {\n\t\tif ( typeof state === "boolean" ) {\n\t\t\treturn state ? this.show() : this.hide();\n\t\t}\n\n\t\treturn this.each( function() {\n\t\t\tif ( isHiddenWithinTree( this ) ) {\n\t\t\t\tjQuery( this ).show();\n\t\t\t} else {\n\t\t\t\tjQuery( this ).hide();\n\t\t\t}\n\t\t} );\n\t}\n} );\nvar rcheckableType = ( /^(?:checkbox|radio)$/i );\n\nvar rtagName = ( /<([a-z][^\\/\\0>\\x20\\t\\r\\n\\f]*)/i );\n\nvar rscriptType = ( /^$|^module$|\\/(?:java|ecma)script/i );\n\n\n\n// We have to close these tags to support XHTML (#13200)\nvar wrapMap = {\n\n\t// Support: IE <=9 only\n\toption: [ 1, "<select multiple=\'multiple\'>", "</select>" ],\n\n\t// XHTML parsers do not magically insert elements in the\n\t// same way that tag soup parsers do. So we cannot shorten\n\t// this by omitting <tbody> or other required elements.\n\tthead: [ 1, "<table>", "</table>" ],\n\tcol: [ 2, "<table><colgroup>", "</colgroup></table>" ],\n\ttr: [ 2, "<table><tbody>", "</tbody></table>" ],\n\ttd: [ 3, "<table><tbody><tr>", "</tr></tbody></table>" ],\n\n\t_default: [ 0, "", "" ]\n};\n\n// Support: IE <=9 only\nwrapMap.optgroup = wrapMap.option;\n\nwrapMap.tbody = wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;\nwrapMap.th = wrapMap.td;\n\n\nfunction getAll( context, tag ) {\n\n\t// Support: IE <=9 - 11 only\n\t// Use typeof to avoid zero-argument method invocation on host objects (#15151)\n\tvar ret;\n\n\tif ( typeof context.getElementsByTagName !== "undefined" ) {\n\t\tret = context.getElementsByTagName( tag || "*" );\n\n\t} else if ( typeof context.querySelectorAll !== "undefined" ) {\n\t\tret = context.querySelectorAll( tag || "*" );\n\n\t} else {\n\t\tret = [];\n\t}\n\n\tif ( tag === undefined || tag && nodeName( context, tag ) ) {\n\t\treturn jQuery.merge( [ context ], ret );\n\t}\n\n\treturn ret;\n}\n\n\n// Mark scripts as having already been evaluated\nfunction setGlobalEval( elems, refElements ) {\n\tvar i = 0,\n\t\tl = elems.length;\n\n\tfor ( ; i < l; i++ ) {\n\t\tdataPriv.set(\n\t\t\telems[ i ],\n\t\t\t"globalEval",\n\t\t\t!refElements || dataPriv.get( refElements[ i ], "globalEval" )\n\t\t);\n\t}\n}\n\n\nvar rhtml = /<|&#?\\w+;/;\n\nfunction buildFragment( elems, context, scripts, selection, ignored ) {\n\tvar elem, tmp, tag, wrap, attached, j,\n\t\tfragment = context.createDocumentFragment(),\n\t\tnodes = [],\n\t\ti = 0,\n\t\tl = elems.length;\n\n\tfor ( ; i < l; i++ ) {\n\t\telem = elems[ i ];\n\n\t\tif ( elem || elem === 0 ) {\n\n\t\t\t// Add nodes directly\n\t\t\tif ( toType( elem ) === "object" ) {\n\n\t\t\t\t// Support: Android <=4.0 only, PhantomJS 1 only\n\t\t\t\t// push.apply(_, arraylike) throws on ancient WebKit\n\t\t\t\tjQuery.merge( nodes, elem.nodeType ? [ elem ] : elem );\n\n\t\t\t// Convert non-html into a text node\n\t\t\t} else if ( !rhtml.test( elem ) ) {\n\t\t\t\tnodes.push( context.createTextNode( elem ) );\n\n\t\t\t// Convert html into DOM nodes\n\t\t\t} else {\n\t\t\t\ttmp = tmp || fragment.appendChild( context.createElement( "div" ) );\n\n\t\t\t\t// Deserialize a standard representation\n\t\t\t\ttag = ( rtagName.exec( elem ) || [ "", "" ] )[ 1 ].toLowerCase();\n\t\t\t\twrap = wrapMap[ tag ] || wrapMap._default;\n\t\t\t\ttmp.innerHTML = wrap[ 1 ] + jQuery.htmlPrefilter( elem ) + wrap[ 2 ];\n\n\t\t\t\t// Descend through wrappers to the right content\n\t\t\t\tj = wrap[ 0 ];\n\t\t\t\twhile ( j-- ) {\n\t\t\t\t\ttmp = tmp.lastChild;\n\t\t\t\t}\n\n\t\t\t\t// Support: Android <=4.0 only, PhantomJS 1 only\n\t\t\t\t// push.apply(_, arraylike) throws on ancient WebKit\n\t\t\t\tjQuery.merge( nodes, tmp.childNodes );\n\n\t\t\t\t// Remember the top-level container\n\t\t\t\ttmp = fragment.firstChild;\n\n\t\t\t\t// Ensure the created nodes are orphaned (#12392)\n\t\t\t\ttmp.textContent = "";\n\t\t\t}\n\t\t}\n\t}\n\n\t// Remove wrapper from fragment\n\tfragment.textContent = "";\n\n\ti = 0;\n\twhile ( ( elem = nodes[ i++ ] ) ) {\n\n\t\t// Skip elements already in the context collection (trac-4087)\n\t\tif ( selection && jQuery.inArray( elem, selection ) > -1 ) {\n\t\t\tif ( ignored ) {\n\t\t\t\tignored.push( elem );\n\t\t\t}\n\t\t\tcontinue;\n\t\t}\n\n\t\tattached = isAttached( elem );\n\n\t\t// Append to fragment\n\t\ttmp = getAll( fragment.appendChild( elem ), "script" );\n\n\t\t// Preserve script evaluation history\n\t\tif ( attached ) {\n\t\t\tsetGlobalEval( tmp );\n\t\t}\n\n\t\t// Capture executables\n\t\tif ( scripts ) {\n\t\t\tj = 0;\n\t\t\twhile ( ( elem = tmp[ j++ ] ) ) {\n\t\t\t\tif ( rscriptType.test( elem.type || "" ) ) {\n\t\t\t\t\tscripts.push( elem );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\treturn fragment;\n}\n\n\n( function() {\n\tvar fragment = document.createDocumentFragment(),\n\t\tdiv = fragment.appendChild( document.createElement( "div" ) ),\n\t\tinput = document.createElement( "input" );\n\n\t// Support: Android 4.0 - 4.3 only\n\t// Check state lost if the name is set (#11217)\n\t// Support: Windows Web Apps (WWA)\n\t// `name` and `type` must use .setAttribute for WWA (#14901)\n\tinput.setAttribute( "type", "radio" );\n\tinput.setAttribute( "checked", "checked" );\n\tinput.setAttribute( "name", "t" );\n\n\tdiv.appendChild( input );\n\n\t// Support: Android <=4.1 only\n\t// Older WebKit doesn\'t clone checked state correctly in fragments\n\tsupport.checkClone = div.cloneNode( true ).cloneNode( true ).lastChild.checked;\n\n\t// Support: IE <=11 only\n\t// Make sure textarea (and checkbox) defaultValue is properly cloned\n\tdiv.innerHTML = "<textarea>x</textarea>";\n\tsupport.noCloneChecked = !!div.cloneNode( true ).lastChild.defaultValue;\n} )();\n\n\nvar\n\trkeyEvent = /^key/,\n\trmouseEvent = /^(?:mouse|pointer|contextmenu|drag|drop)|click/,\n\trtypenamespace = /^([^.]*)(?:\\.(.+)|)/;\n\nfunction returnTrue() {\n\treturn true;\n}\n\nfunction returnFalse() {\n\treturn false;\n}\n\n// Support: IE <=9 - 11+\n// focus() and blur() are asynchronous, except when they are no-op.\n// So expect focus to be synchronous when the element is already active,\n// and blur to be synchronous when the element is not already active.\n// (focus and blur are always synchronous in other supported browsers,\n// this just defines when we can count on it).\nfunction expectSync( elem, type ) {\n\treturn ( elem === safeActiveElement() ) === ( type === "focus" );\n}\n\n// Support: IE <=9 only\n// Accessing document.activeElement can throw unexpectedly\n// https://bugs.jquery.com/ticket/13393\nfunction safeActiveElement() {\n\ttry {\n\t\treturn document.activeElement;\n\t} catch ( err ) { }\n}\n\nfunction on( elem, types, selector, data, fn, one ) {\n\tvar origFn, type;\n\n\t// Types can be a map of types/handlers\n\tif ( typeof types === "object" ) {\n\n\t\t// ( types-Object, selector, data )\n\t\tif ( typeof selector !== "string" ) {\n\n\t\t\t// ( types-Object, data )\n\t\t\tdata = data || selector;\n\t\t\tselector = undefined;\n\t\t}\n\t\tfor ( type in types ) {\n\t\t\ton( elem, type, selector, data, types[ type ], one );\n\t\t}\n\t\treturn elem;\n\t}\n\n\tif ( data == null && fn == null ) {\n\n\t\t// ( types, fn )\n\t\tfn = selector;\n\t\tdata = selector = undefined;\n\t} else if ( fn == null ) {\n\t\tif ( typeof selector === "string" ) {\n\n\t\t\t// ( types, selector, fn )\n\t\t\tfn = data;\n\t\t\tdata = undefined;\n\t\t} else {\n\n\t\t\t// ( types, data, fn )\n\t\t\tfn = data;\n\t\t\tdata = selector;\n\t\t\tselector = undefined;\n\t\t}\n\t}\n\tif ( fn === false ) {\n\t\tfn = returnFalse;\n\t} else if ( !fn ) {\n\t\treturn elem;\n\t}\n\n\tif ( one === 1 ) {\n\t\torigFn = fn;\n\t\tfn = function( event ) {\n\n\t\t\t// Can use an empty set, since event contains the info\n\t\t\tjQuery().off( event );\n\t\t\treturn origFn.apply( this, arguments );\n\t\t};\n\n\t\t// Use same guid so caller can remove using origFn\n\t\tfn.guid = origFn.guid || ( origFn.guid = jQuery.guid++ );\n\t}\n\treturn elem.each( function() {\n\t\tjQuery.event.add( this, types, fn, data, selector );\n\t} );\n}\n\n/*\n * Helper functions for managing events -- not part of the public interface.\n * Props to Dean Edwards\' addEvent library for many of the ideas.\n */\njQuery.event = {\n\n\tglobal: {},\n\n\tadd: function( elem, types, handler, data, selector ) {\n\n\t\tvar handleObjIn, eventHandle, tmp,\n\t\t\tevents, t, handleObj,\n\t\t\tspecial, handlers, type, namespaces, origType,\n\t\t\telemData = dataPriv.get( elem );\n\n\t\t// Don\'t attach events to noData or text/comment nodes (but allow plain objects)\n\t\tif ( !elemData ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Caller can pass in an object of custom data in lieu of the handler\n\t\tif ( handler.handler ) {\n\t\t\thandleObjIn = handler;\n\t\t\thandler = handleObjIn.handler;\n\t\t\tselector = handleObjIn.selector;\n\t\t}\n\n\t\t// Ensure that invalid selectors throw exceptions at attach time\n\t\t// Evaluate against documentElement in case elem is a non-element node (e.g., document)\n\t\tif ( selector ) {\n\t\t\tjQuery.find.matchesSelector( documentElement, selector );\n\t\t}\n\n\t\t// Make sure that the handler has a unique ID, used to find/remove it later\n\t\tif ( !handler.guid ) {\n\t\t\thandler.guid = jQuery.guid++;\n\t\t}\n\n\t\t// Init the element\'s event structure and main handler, if this is the first\n\t\tif ( !( events = elemData.events ) ) {\n\t\t\tevents = elemData.events = {};\n\t\t}\n\t\tif ( !( eventHandle = elemData.handle ) ) {\n\t\t\teventHandle = elemData.handle = function( e ) {\n\n\t\t\t\t// Discard the second event of a jQuery.event.trigger() and\n\t\t\t\t// when an event is called after a page has unloaded\n\t\t\t\treturn typeof jQuery !== "undefined" && jQuery.event.triggered !== e.type ?\n\t\t\t\t\tjQuery.event.dispatch.apply( elem, arguments ) : undefined;\n\t\t\t};\n\t\t}\n\n\t\t// Handle multiple events separated by a space\n\t\ttypes = ( types || "" ).match( rnothtmlwhite ) || [ "" ];\n\t\tt = types.length;\n\t\twhile ( t-- ) {\n\t\t\ttmp = rtypenamespace.exec( types[ t ] ) || [];\n\t\t\ttype = origType = tmp[ 1 ];\n\t\t\tnamespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();\n\n\t\t\t// There *must* be a type, no attaching namespace-only handlers\n\t\t\tif ( !type ) {\n\t\t\t\tcontinue;\n\t\t\t}\n\n\t\t\t// If event changes its type, use the special event handlers for the changed type\n\t\t\tspecial = jQuery.event.special[ type ] || {};\n\n\t\t\t// If selector defined, determine special event api type, otherwise given type\n\t\t\ttype = ( selector ? special.delegateType : special.bindType ) || type;\n\n\t\t\t// Update special based on newly reset type\n\t\t\tspecial = jQuery.event.special[ type ] || {};\n\n\t\t\t// handleObj is passed to all event handlers\n\t\t\thandleObj = jQuery.extend( {\n\t\t\t\ttype: type,\n\t\t\t\torigType: origType,\n\t\t\t\tdata: data,\n\t\t\t\thandler: handler,\n\t\t\t\tguid: handler.guid,\n\t\t\t\tselector: selector,\n\t\t\t\tneedsContext: selector && jQuery.expr.match.needsContext.test( selector ),\n\t\t\t\tnamespace: namespaces.join( "." )\n\t\t\t}, handleObjIn );\n\n\t\t\t// Init the event handler queue if we\'re the first\n\t\t\tif ( !( handlers = events[ type ] ) ) {\n\t\t\t\thandlers = events[ type ] = [];\n\t\t\t\thandlers.delegateCount = 0;\n\n\t\t\t\t// Only use addEventListener if the special events handler returns false\n\t\t\t\tif ( !special.setup ||\n\t\t\t\t\tspecial.setup.call( elem, data, namespaces, eventHandle ) === false ) {\n\n\t\t\t\t\tif ( elem.addEventListener ) {\n\t\t\t\t\t\telem.addEventListener( type, eventHandle );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\tif ( special.add ) {\n\t\t\t\tspecial.add.call( elem, handleObj );\n\n\t\t\t\tif ( !handleObj.handler.guid ) {\n\t\t\t\t\thandleObj.handler.guid = handler.guid;\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Add to the element\'s handler list, delegates in front\n\t\t\tif ( selector ) {\n\t\t\t\thandlers.splice( handlers.delegateCount++, 0, handleObj );\n\t\t\t} else {\n\t\t\t\thandlers.push( handleObj );\n\t\t\t}\n\n\t\t\t// Keep track of which events have ever been used, for event optimization\n\t\t\tjQuery.event.global[ type ] = true;\n\t\t}\n\n\t},\n\n\t// Detach an event or set of events from an element\n\tremove: function( elem, types, handler, selector, mappedTypes ) {\n\n\t\tvar j, origCount, tmp,\n\t\t\tevents, t, handleObj,\n\t\t\tspecial, handlers, type, namespaces, origType,\n\t\t\telemData = dataPriv.hasData( elem ) && dataPriv.get( elem );\n\n\t\tif ( !elemData || !( events = elemData.events ) ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Once for each type.namespace in types; type may be omitted\n\t\ttypes = ( types || "" ).match( rnothtmlwhite ) || [ "" ];\n\t\tt = types.length;\n\t\twhile ( t-- ) {\n\t\t\ttmp = rtypenamespace.exec( types[ t ] ) || [];\n\t\t\ttype = origType = tmp[ 1 ];\n\t\t\tnamespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();\n\n\t\t\t// Unbind all events (on this namespace, if provided) for the element\n\t\t\tif ( !type ) {\n\t\t\t\tfor ( type in events ) {\n\t\t\t\t\tjQuery.event.remove( elem, type + types[ t ], handler, selector, true );\n\t\t\t\t}\n\t\t\t\tcontinue;\n\t\t\t}\n\n\t\t\tspecial = jQuery.event.special[ type ] || {};\n\t\t\ttype = ( selector ? special.delegateType : special.bindType ) || type;\n\t\t\thandlers = events[ type ] || [];\n\t\t\ttmp = tmp[ 2 ] &&\n\t\t\t\tnew RegExp( "(^|\\\\.)" + namespaces.join( "\\\\.(?:.*\\\\.|)" ) + "(\\\\.|$)" );\n\n\t\t\t// Remove matching events\n\t\t\torigCount = j = handlers.length;\n\t\t\twhile ( j-- ) {\n\t\t\t\thandleObj = handlers[ j ];\n\n\t\t\t\tif ( ( mappedTypes || origType === handleObj.origType ) &&\n\t\t\t\t\t( !handler || handler.guid === handleObj.guid ) &&\n\t\t\t\t\t( !tmp || tmp.test( handleObj.namespace ) ) &&\n\t\t\t\t\t( !selector || selector === handleObj.selector ||\n\t\t\t\t\t\tselector === "**" && handleObj.selector ) ) {\n\t\t\t\t\thandlers.splice( j, 1 );\n\n\t\t\t\t\tif ( handleObj.selector ) {\n\t\t\t\t\t\thandlers.delegateCount--;\n\t\t\t\t\t}\n\t\t\t\t\tif ( special.remove ) {\n\t\t\t\t\t\tspecial.remove.call( elem, handleObj );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Remove generic event handler if we removed something and no more handlers exist\n\t\t\t// (avoids potential for endless recursion during removal of special event handlers)\n\t\t\tif ( origCount && !handlers.length ) {\n\t\t\t\tif ( !special.teardown ||\n\t\t\t\t\tspecial.teardown.call( elem, namespaces, elemData.handle ) === false ) {\n\n\t\t\t\t\tjQuery.removeEvent( elem, type, elemData.handle );\n\t\t\t\t}\n\n\t\t\t\tdelete events[ type ];\n\t\t\t}\n\t\t}\n\n\t\t// Remove data and the expando if it\'s no longer used\n\t\tif ( jQuery.isEmptyObject( events ) ) {\n\t\t\tdataPriv.remove( elem, "handle events" );\n\t\t}\n\t},\n\n\tdispatch: function( nativeEvent ) {\n\n\t\t// Make a writable jQuery.Event from the native event object\n\t\tvar event = jQuery.event.fix( nativeEvent );\n\n\t\tvar i, j, ret, matched, handleObj, handlerQueue,\n\t\t\targs = new Array( arguments.length ),\n\t\t\thandlers = ( dataPriv.get( this, "events" ) || {} )[ event.type ] || [],\n\t\t\tspecial = jQuery.event.special[ event.type ] || {};\n\n\t\t// Use the fix-ed jQuery.Event rather than the (read-only) native event\n\t\targs[ 0 ] = event;\n\n\t\tfor ( i = 1; i < arguments.length; i++ ) {\n\t\t\targs[ i ] = arguments[ i ];\n\t\t}\n\n\t\tevent.delegateTarget = this;\n\n\t\t// Call the preDispatch hook for the mapped type, and let it bail if desired\n\t\tif ( special.preDispatch && special.preDispatch.call( this, event ) === false ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Determine handlers\n\t\thandlerQueue = jQuery.event.handlers.call( this, event, handlers );\n\n\t\t// Run delegates first; they may want to stop propagation beneath us\n\t\ti = 0;\n\t\twhile ( ( matched = handlerQueue[ i++ ] ) && !event.isPropagationStopped() ) {\n\t\t\tevent.currentTarget = matched.elem;\n\n\t\t\tj = 0;\n\t\t\twhile ( ( handleObj = matched.handlers[ j++ ] ) &&\n\t\t\t\t!event.isImmediatePropagationStopped() ) {\n\n\t\t\t\t// If the event is namespaced, then each handler is only invoked if it is\n\t\t\t\t// specially universal or its namespaces are a superset of the event\'s.\n\t\t\t\tif ( !event.rnamespace || handleObj.namespace === false ||\n\t\t\t\t\tevent.rnamespace.test( handleObj.namespace ) ) {\n\n\t\t\t\t\tevent.handleObj = handleObj;\n\t\t\t\t\tevent.data = handleObj.data;\n\n\t\t\t\t\tret = ( ( jQuery.event.special[ handleObj.origType ] || {} ).handle ||\n\t\t\t\t\t\thandleObj.handler ).apply( matched.elem, args );\n\n\t\t\t\t\tif ( ret !== undefined ) {\n\t\t\t\t\t\tif ( ( event.result = ret ) === false ) {\n\t\t\t\t\t\t\tevent.preventDefault();\n\t\t\t\t\t\t\tevent.stopPropagation();\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\t// Call the postDispatch hook for the mapped type\n\t\tif ( special.postDispatch ) {\n\t\t\tspecial.postDispatch.call( this, event );\n\t\t}\n\n\t\treturn event.result;\n\t},\n\n\thandlers: function( event, handlers ) {\n\t\tvar i, handleObj, sel, matchedHandlers, matchedSelectors,\n\t\t\thandlerQueue = [],\n\t\t\tdelegateCount = handlers.delegateCount,\n\t\t\tcur = event.target;\n\n\t\t// Find delegate handlers\n\t\tif ( delegateCount &&\n\n\t\t\t// Support: IE <=9\n\t\t\t// Black-hole SVG <use> instance trees (trac-13180)\n\t\t\tcur.nodeType &&\n\n\t\t\t// Support: Firefox <=42\n\t\t\t// Suppress spec-violating clicks indicating a non-primary pointer button (trac-3861)\n\t\t\t// https://www.w3.org/TR/DOM-Level-3-Events/#event-type-click\n\t\t\t// Support: IE 11 only\n\t\t\t// ...but not arrow key "clicks" of radio inputs, which can have `button` -1 (gh-2343)\n\t\t\t!( event.type === "click" && event.button >= 1 ) ) {\n\n\t\t\tfor ( ; cur !== this; cur = cur.parentNode || this ) {\n\n\t\t\t\t// Don\'t check non-elements (#13208)\n\t\t\t\t// Don\'t process clicks on disabled elements (#6911, #8165, #11382, #11764)\n\t\t\t\tif ( cur.nodeType === 1 && !( event.type === "click" && cur.disabled === true ) ) {\n\t\t\t\t\tmatchedHandlers = [];\n\t\t\t\t\tmatchedSelectors = {};\n\t\t\t\t\tfor ( i = 0; i < delegateCount; i++ ) {\n\t\t\t\t\t\thandleObj = handlers[ i ];\n\n\t\t\t\t\t\t// Don\'t conflict with Object.prototype properties (#13203)\n\t\t\t\t\t\tsel = handleObj.selector + " ";\n\n\t\t\t\t\t\tif ( matchedSelectors[ sel ] === undefined ) {\n\t\t\t\t\t\t\tmatchedSelectors[ sel ] = handleObj.needsContext ?\n\t\t\t\t\t\t\t\tjQuery( sel, this ).index( cur ) > -1 :\n\t\t\t\t\t\t\t\tjQuery.find( sel, this, null, [ cur ] ).length;\n\t\t\t\t\t\t}\n\t\t\t\t\t\tif ( matchedSelectors[ sel ] ) {\n\t\t\t\t\t\t\tmatchedHandlers.push( handleObj );\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t\tif ( matchedHandlers.length ) {\n\t\t\t\t\t\thandlerQueue.push( { elem: cur, handlers: matchedHandlers } );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\t// Add the remaining (directly-bound) handlers\n\t\tcur = this;\n\t\tif ( delegateCount < handlers.length ) {\n\t\t\thandlerQueue.push( { elem: cur, handlers: handlers.slice( delegateCount ) } );\n\t\t}\n\n\t\treturn handlerQueue;\n\t},\n\n\taddProp: function( name, hook ) {\n\t\tObject.defineProperty( jQuery.Event.prototype, name, {\n\t\t\tenumerable: true,\n\t\t\tconfigurable: true,\n\n\t\t\tget: isFunction( hook ) ?\n\t\t\t\tfunction() {\n\t\t\t\t\tif ( this.originalEvent ) {\n\t\t\t\t\t\t\treturn hook( this.originalEvent );\n\t\t\t\t\t}\n\t\t\t\t} :\n\t\t\t\tfunction() {\n\t\t\t\t\tif ( this.originalEvent ) {\n\t\t\t\t\t\t\treturn this.originalEvent[ name ];\n\t\t\t\t\t}\n\t\t\t\t},\n\n\t\t\tset: function( value ) {\n\t\t\t\tObject.defineProperty( this, name, {\n\t\t\t\t\tenumerable: true,\n\t\t\t\t\tconfigurable: true,\n\t\t\t\t\twritable: true,\n\t\t\t\t\tvalue: value\n\t\t\t\t} );\n\t\t\t}\n\t\t} );\n\t},\n\n\tfix: function( originalEvent ) {\n\t\treturn originalEvent[ jQuery.expando ] ?\n\t\t\toriginalEvent :\n\t\t\tnew jQuery.Event( originalEvent );\n\t},\n\n\tspecial: {\n\t\tload: {\n\n\t\t\t// Prevent triggered image.load events from bubbling to window.load\n\t\t\tnoBubble: true\n\t\t},\n\t\tclick: {\n\n\t\t\t// Utilize native event to ensure correct state for checkable inputs\n\t\t\tsetup: function( data ) {\n\n\t\t\t\t// For mutual compressibility with _default, replace `this` access with a local var.\n\t\t\t\t// `|| data` is dead code meant only to preserve the variable through minification.\n\t\t\t\tvar el = this || data;\n\n\t\t\t\t// Claim the first handler\n\t\t\t\tif ( rcheckableType.test( el.type ) &&\n\t\t\t\t\tel.click && nodeName( el, "input" ) &&\n\t\t\t\t\tdataPriv.get( el, "click" ) === undefined ) {\n\n\t\t\t\t\t// dataPriv.set( el, "click", ... )\n\t\t\t\t\tleverageNative( el, "click", returnTrue );\n\t\t\t\t}\n\n\t\t\t\t// Return false to allow normal processing in the caller\n\t\t\t\treturn false;\n\t\t\t},\n\t\t\ttrigger: function( data ) {\n\n\t\t\t\t// For mutual compressibility with _default, replace `this` access with a local var.\n\t\t\t\t// `|| data` is dead code meant only to preserve the variable through minification.\n\t\t\t\tvar el = this || data;\n\n\t\t\t\t// Force setup before triggering a click\n\t\t\t\tif ( rcheckableType.test( el.type ) &&\n\t\t\t\t\tel.click && nodeName( el, "input" ) &&\n\t\t\t\t\tdataPriv.get( el, "click" ) === undefined ) {\n\n\t\t\t\t\tleverageNative( el, "click" );\n\t\t\t\t}\n\n\t\t\t\t// Return non-false to allow normal event-path propagation\n\t\t\t\treturn true;\n\t\t\t},\n\n\t\t\t// For cross-browser consistency, suppress native .click() on links\n\t\t\t// Also prevent it if we\'re currently inside a leveraged native-event stack\n\t\t\t_default: function( event ) {\n\t\t\t\tvar target = event.target;\n\t\t\t\treturn rcheckableType.test( target.type ) &&\n\t\t\t\t\ttarget.click && nodeName( target, "input" ) &&\n\t\t\t\t\tdataPriv.get( target, "click" ) ||\n\t\t\t\t\tnodeName( target, "a" );\n\t\t\t}\n\t\t},\n\n\t\tbeforeunload: {\n\t\t\tpostDispatch: function( event ) {\n\n\t\t\t\t// Support: Firefox 20+\n\t\t\t\t// Firefox doesn\'t alert if the returnValue field is not set.\n\t\t\t\tif ( event.result !== undefined && event.originalEvent ) {\n\t\t\t\t\tevent.originalEvent.returnValue = event.result;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n};\n\n// Ensure the presence of an event listener that handles manually-triggered\n// synthetic events by interrupting progress until reinvoked in response to\n// *native* events that it fires directly, ensuring that state changes have\n// already occurred before other listeners are invoked.\nfunction leverageNative( el, type, expectSync ) {\n\n\t// Missing expectSync indicates a trigger call, which must force setup through jQuery.event.add\n\tif ( !expectSync ) {\n\t\tjQuery.event.add( el, type, returnTrue );\n\t\treturn;\n\t}\n\n\t// Register the controller as a special universal handler for all event namespaces\n\tdataPriv.set( el, type, false );\n\tjQuery.event.add( el, type, {\n\t\tnamespace: false,\n\t\thandler: function( event ) {\n\t\t\tvar notAsync, result,\n\t\t\t\tsaved = dataPriv.get( this, type );\n\n\t\t\tif ( ( event.isTrigger & 1 ) && this[ type ] ) {\n\n\t\t\t\t// Interrupt processing of the outer synthetic .trigger()ed event\n\t\t\t\tif ( !saved ) {\n\n\t\t\t\t\t// Store arguments for use when handling the inner native event\n\t\t\t\t\tsaved = slice.call( arguments );\n\t\t\t\t\tdataPriv.set( this, type, saved );\n\n\t\t\t\t\t// Trigger the native event and capture its result\n\t\t\t\t\t// Support: IE <=9 - 11+\n\t\t\t\t\t// focus() and blur() are asynchronous\n\t\t\t\t\tnotAsync = expectSync( this, type );\n\t\t\t\t\tthis[ type ]();\n\t\t\t\t\tresult = dataPriv.get( this, type );\n\t\t\t\t\tif ( saved !== result || notAsync ) {\n\t\t\t\t\t\tdataPriv.set( this, type, false );\n\t\t\t\t\t} else {\n\t\t\t\t\t\tresult = undefined;\n\t\t\t\t\t}\n\t\t\t\t\tif ( saved !== result ) {\n\n\t\t\t\t\t\t// Cancel the outer synthetic event\n\t\t\t\t\t\tevent.stopImmediatePropagation();\n\t\t\t\t\t\tevent.preventDefault();\n\t\t\t\t\t\treturn result;\n\t\t\t\t\t}\n\n\t\t\t\t// If this is an inner synthetic event for an event with a bubbling surrogate\n\t\t\t\t// (focus or blur), assume that the surrogate already propagated from triggering the\n\t\t\t\t// native event and prevent that from happening again here.\n\t\t\t\t// This technically gets the ordering wrong w.r.t. to `.trigger()` (in which the\n\t\t\t\t// bubbling surrogate propagates *after* the non-bubbling base), but that seems\n\t\t\t\t// less bad than duplication.\n\t\t\t\t} else if ( ( jQuery.event.special[ type ] || {} ).delegateType ) {\n\t\t\t\t\tevent.stopPropagation();\n\t\t\t\t}\n\n\t\t\t// If this is a native event triggered above, everything is now in order\n\t\t\t// Fire an inner synthetic event with the original arguments\n\t\t\t} else if ( saved ) {\n\n\t\t\t\t// ...and capture the result\n\t\t\t\tdataPriv.set( this, type, jQuery.event.trigger(\n\n\t\t\t\t\t// Support: IE <=9 - 11+\n\t\t\t\t\t// Extend with the prototype to reset the above stopImmediatePropagation()\n\t\t\t\t\tjQuery.extend( saved.shift(), jQuery.Event.prototype ),\n\t\t\t\t\tsaved,\n\t\t\t\t\tthis\n\t\t\t\t) );\n\n\t\t\t\t// Abort handling of the native event\n\t\t\t\tevent.stopImmediatePropagation();\n\t\t\t}\n\t\t}\n\t} );\n}\n\njQuery.removeEvent = function( elem, type, handle ) {\n\n\t// This "if" is needed for plain objects\n\tif ( elem.removeEventListener ) {\n\t\telem.removeEventListener( type, handle );\n\t}\n};\n\njQuery.Event = function( src, props ) {\n\n\t// Allow instantiation without the \'new\' keyword\n\tif ( !( this instanceof jQuery.Event ) ) {\n\t\treturn new jQuery.Event( src, props );\n\t}\n\n\t// Event object\n\tif ( src && src.type ) {\n\t\tthis.originalEvent = src;\n\t\tthis.type = src.type;\n\n\t\t// Events bubbling up the document may have been marked as prevented\n\t\t// by a handler lower down the tree; reflect the correct value.\n\t\tthis.isDefaultPrevented = src.defaultPrevented ||\n\t\t\t\tsrc.defaultPrevented === undefined &&\n\n\t\t\t\t// Support: Android <=2.3 only\n\t\t\t\tsrc.returnValue === false ?\n\t\t\treturnTrue :\n\t\t\treturnFalse;\n\n\t\t// Create target properties\n\t\t// Support: Safari <=6 - 7 only\n\t\t// Target should not be a text node (#504, #13143)\n\t\tthis.target = ( src.target && src.target.nodeType === 3 ) ?\n\t\t\tsrc.target.parentNode :\n\t\t\tsrc.target;\n\n\t\tthis.currentTarget = src.currentTarget;\n\t\tthis.relatedTarget = src.relatedTarget;\n\n\t// Event type\n\t} else {\n\t\tthis.type = src;\n\t}\n\n\t// Put explicitly provided properties onto the event object\n\tif ( props ) {\n\t\tjQuery.extend( this, props );\n\t}\n\n\t// Create a timestamp if incoming event doesn\'t have one\n\tthis.timeStamp = src && src.timeStamp || Date.now();\n\n\t// Mark it as fixed\n\tthis[ jQuery.expando ] = true;\n};\n\n// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding\n// https://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html\njQuery.Event.prototype = {\n\tconstructor: jQuery.Event,\n\tisDefaultPrevented: returnFalse,\n\tisPropagationStopped: returnFalse,\n\tisImmediatePropagationStopped: returnFalse,\n\tisSimulated: false,\n\n\tpreventDefault: function() {\n\t\tvar e = this.originalEvent;\n\n\t\tthis.isDefaultPrevented = returnTrue;\n\n\t\tif ( e && !this.isSimulated ) {\n\t\t\te.preventDefault();\n\t\t}\n\t},\n\tstopPropagation: function() {\n\t\tvar e = this.originalEvent;\n\n\t\tthis.isPropagationStopped = returnTrue;\n\n\t\tif ( e && !this.isSimulated ) {\n\t\t\te.stopPropagation();\n\t\t}\n\t},\n\tstopImmediatePropagation: function() {\n\t\tvar e = this.originalEvent;\n\n\t\tthis.isImmediatePropagationStopped = returnTrue;\n\n\t\tif ( e && !this.isSimulated ) {\n\t\t\te.stopImmediatePropagation();\n\t\t}\n\n\t\tthis.stopPropagation();\n\t}\n};\n\n// Includes all common event props including KeyEvent and MouseEvent specific props\njQuery.each( {\n\taltKey: true,\n\tbubbles: true,\n\tcancelable: true,\n\tchangedTouches: true,\n\tctrlKey: true,\n\tdetail: true,\n\teventPhase: true,\n\tmetaKey: true,\n\tpageX: true,\n\tpageY: true,\n\tshiftKey: true,\n\tview: true,\n\t"char": true,\n\tcode: true,\n\tcharCode: true,\n\tkey: true,\n\tkeyCode: true,\n\tbutton: true,\n\tbuttons: true,\n\tclientX: true,\n\tclientY: true,\n\toffsetX: true,\n\toffsetY: true,\n\tpointerId: true,\n\tpointerType: true,\n\tscreenX: true,\n\tscreenY: true,\n\ttargetTouches: true,\n\ttoElement: true,\n\ttouches: true,\n\n\twhich: function( event ) {\n\t\tvar button = event.button;\n\n\t\t// Add which for key events\n\t\tif ( event.which == null && rkeyEvent.test( event.type ) ) {\n\t\t\treturn event.charCode != null ? event.charCode : event.keyCode;\n\t\t}\n\n\t\t// Add which for click: 1 === left; 2 === middle; 3 === right\n\t\tif ( !event.which && button !== undefined && rmouseEvent.test( event.type ) ) {\n\t\t\tif ( button & 1 ) {\n\t\t\t\treturn 1;\n\t\t\t}\n\n\t\t\tif ( button & 2 ) {\n\t\t\t\treturn 3;\n\t\t\t}\n\n\t\t\tif ( button & 4 ) {\n\t\t\t\treturn 2;\n\t\t\t}\n\n\t\t\treturn 0;\n\t\t}\n\n\t\treturn event.which;\n\t}\n}, jQuery.event.addProp );\n\njQuery.each( { focus: "focusin", blur: "focusout" }, function( type, delegateType ) {\n\tjQuery.event.special[ type ] = {\n\n\t\t// Utilize native event if possible so blur/focus sequence is correct\n\t\tsetup: function() {\n\n\t\t\t// Claim the first handler\n\t\t\t// dataPriv.set( this, "focus", ... )\n\t\t\t// dataPriv.set( this, "blur", ... )\n\t\t\tleverageNative( this, type, expectSync );\n\n\t\t\t// Return false to allow normal processing in the caller\n\t\t\treturn false;\n\t\t},\n\t\ttrigger: function() {\n\n\t\t\t// Force setup before trigger\n\t\t\tleverageNative( this, type );\n\n\t\t\t// Return non-false to allow normal event-path propagation\n\t\t\treturn true;\n\t\t},\n\n\t\tdelegateType: delegateType\n\t};\n} );\n\n// Create mouseenter/leave events using mouseover/out and event-time checks\n// so that event delegation works in jQuery.\n// Do the same for pointerenter/pointerleave and pointerover/pointerout\n//\n// Support: Safari 7 only\n// Safari sends mouseenter too often; see:\n// https://bugs.chromium.org/p/chromium/issues/detail?id=470258\n// for the description of the bug (it existed in older Chrome versions as well).\njQuery.each( {\n\tmouseenter: "mouseover",\n\tmouseleave: "mouseout",\n\tpointerenter: "pointerover",\n\tpointerleave: "pointerout"\n}, function( orig, fix ) {\n\tjQuery.event.special[ orig ] = {\n\t\tdelegateType: fix,\n\t\tbindType: fix,\n\n\t\thandle: function( event ) {\n\t\t\tvar ret,\n\t\t\t\ttarget = this,\n\t\t\t\trelated = event.relatedTarget,\n\t\t\t\thandleObj = event.handleObj;\n\n\t\t\t// For mouseenter/leave call the handler if related is outside the target.\n\t\t\t// NB: No relatedTarget if the mouse left/entered the browser window\n\t\t\tif ( !related || ( related !== target && !jQuery.contains( target, related ) ) ) {\n\t\t\t\tevent.type = handleObj.origType;\n\t\t\t\tret = handleObj.handler.apply( this, arguments );\n\t\t\t\tevent.type = fix;\n\t\t\t}\n\t\t\treturn ret;\n\t\t}\n\t};\n} );\n\njQuery.fn.extend( {\n\n\ton: function( types, selector, data, fn ) {\n\t\treturn on( this, types, selector, data, fn );\n\t},\n\tone: function( types, selector, data, fn ) {\n\t\treturn on( this, types, selector, data, fn, 1 );\n\t},\n\toff: function( types, selector, fn ) {\n\t\tvar handleObj, type;\n\t\tif ( types && types.preventDefault && types.handleObj ) {\n\n\t\t\t// ( event )  dispatched jQuery.Event\n\t\t\thandleObj = types.handleObj;\n\t\t\tjQuery( types.delegateTarget ).off(\n\t\t\t\thandleObj.namespace ?\n\t\t\t\t\thandleObj.origType + "." + handleObj.namespace :\n\t\t\t\t\thandleObj.origType,\n\t\t\t\thandleObj.selector,\n\t\t\t\thandleObj.handler\n\t\t\t);\n\t\t\treturn this;\n\t\t}\n\t\tif ( typeof types === "object" ) {\n\n\t\t\t// ( types-object [, selector] )\n\t\t\tfor ( type in types ) {\n\t\t\t\tthis.off( type, selector, types[ type ] );\n\t\t\t}\n\t\t\treturn this;\n\t\t}\n\t\tif ( selector === false || typeof selector === "function" ) {\n\n\t\t\t// ( types [, fn] )\n\t\t\tfn = selector;\n\t\t\tselector = undefined;\n\t\t}\n\t\tif ( fn === false ) {\n\t\t\tfn = returnFalse;\n\t\t}\n\t\treturn this.each( function() {\n\t\t\tjQuery.event.remove( this, types, fn, selector );\n\t\t} );\n\t}\n} );\n\n\nvar\n\n\t/* eslint-disable max-len */\n\n\t// See https://github.com/eslint/eslint/issues/3229\n\trxhtmlTag = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([a-z][^\\/\\0>\\x20\\t\\r\\n\\f]*)[^>]*)\\/>/gi,\n\n\t/* eslint-enable */\n\n\t// Support: IE <=10 - 11, Edge 12 - 13 only\n\t// In IE/Edge using regex groups here causes severe slowdowns.\n\t// See https://connect.microsoft.com/IE/feedback/details/1736512/\n\trnoInnerhtml = /<script|<style|<link/i,\n\n\t// checked="checked" or checked\n\trchecked = /checked\\s*(?:[^=]|=\\s*.checked.)/i,\n\trcleanScript = /^\\s*<!(?:\\[CDATA\\[|--)|(?:\\]\\]|--)>\\s*$/g;\n\n// Prefer a tbody over its parent table for containing new rows\nfunction manipulationTarget( elem, content ) {\n\tif ( nodeName( elem, "table" ) &&\n\t\tnodeName( content.nodeType !== 11 ? content : content.firstChild, "tr" ) ) {\n\n\t\treturn jQuery( elem ).children( "tbody" )[ 0 ] || elem;\n\t}\n\n\treturn elem;\n}\n\n// Replace/restore the type attribute of script elements for safe DOM manipulation\nfunction disableScript( elem ) {\n\telem.type = ( elem.getAttribute( "type" ) !== null ) + "/" + elem.type;\n\treturn elem;\n}\nfunction restoreScript( elem ) {\n\tif ( ( elem.type || "" ).slice( 0, 5 ) === "true/" ) {\n\t\telem.type = elem.type.slice( 5 );\n\t} else {\n\t\telem.removeAttribute( "type" );\n\t}\n\n\treturn elem;\n}\n\nfunction cloneCopyEvent( src, dest ) {\n\tvar i, l, type, pdataOld, pdataCur, udataOld, udataCur, events;\n\n\tif ( dest.nodeType !== 1 ) {\n\t\treturn;\n\t}\n\n\t// 1. Copy private data: events, handlers, etc.\n\tif ( dataPriv.hasData( src ) ) {\n\t\tpdataOld = dataPriv.access( src );\n\t\tpdataCur = dataPriv.set( dest, pdataOld );\n\t\tevents = pdataOld.events;\n\n\t\tif ( events ) {\n\t\t\tdelete pdataCur.handle;\n\t\t\tpdataCur.events = {};\n\n\t\t\tfor ( type in events ) {\n\t\t\t\tfor ( i = 0, l = events[ type ].length; i < l; i++ ) {\n\t\t\t\t\tjQuery.event.add( dest, type, events[ type ][ i ] );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\t// 2. Copy user data\n\tif ( dataUser.hasData( src ) ) {\n\t\tudataOld = dataUser.access( src );\n\t\tudataCur = jQuery.extend( {}, udataOld );\n\n\t\tdataUser.set( dest, udataCur );\n\t}\n}\n\n// Fix IE bugs, see support tests\nfunction fixInput( src, dest ) {\n\tvar nodeName = dest.nodeName.toLowerCase();\n\n\t// Fails to persist the checked state of a cloned checkbox or radio button.\n\tif ( nodeName === "input" && rcheckableType.test( src.type ) ) {\n\t\tdest.checked = src.checked;\n\n\t// Fails to return the selected option to the default selected state when cloning options\n\t} else if ( nodeName === "input" || nodeName === "textarea" ) {\n\t\tdest.defaultValue = src.defaultValue;\n\t}\n}\n\nfunction domManip( collection, args, callback, ignored ) {\n\n\t// Flatten any nested arrays\n\targs = concat.apply( [], args );\n\n\tvar fragment, first, scripts, hasScripts, node, doc,\n\t\ti = 0,\n\t\tl = collection.length,\n\t\tiNoClone = l - 1,\n\t\tvalue = args[ 0 ],\n\t\tvalueIsFunction = isFunction( value );\n\n\t// We can\'t cloneNode fragments that contain checked, in WebKit\n\tif ( valueIsFunction ||\n\t\t\t( l > 1 && typeof value === "string" &&\n\t\t\t\t!support.checkClone && rchecked.test( value ) ) ) {\n\t\treturn collection.each( function( index ) {\n\t\t\tvar self = collection.eq( index );\n\t\t\tif ( valueIsFunction ) {\n\t\t\t\targs[ 0 ] = value.call( this, index, self.html() );\n\t\t\t}\n\t\t\tdomManip( self, args, callback, ignored );\n\t\t} );\n\t}\n\n\tif ( l ) {\n\t\tfragment = buildFragment( args, collection[ 0 ].ownerDocument, false, collection, ignored );\n\t\tfirst = fragment.firstChild;\n\n\t\tif ( fragment.childNodes.length === 1 ) {\n\t\t\tfragment = first;\n\t\t}\n\n\t\t// Require either new content or an interest in ignored elements to invoke the callback\n\t\tif ( first || ignored ) {\n\t\t\tscripts = jQuery.map( getAll( fragment, "script" ), disableScript );\n\t\t\thasScripts = scripts.length;\n\n\t\t\t// Use the original fragment for the last item\n\t\t\t// instead of the first because it can end up\n\t\t\t// being emptied incorrectly in certain situations (#8070).\n\t\t\tfor ( ; i < l; i++ ) {\n\t\t\t\tnode = fragment;\n\n\t\t\t\tif ( i !== iNoClone ) {\n\t\t\t\t\tnode = jQuery.clone( node, true, true );\n\n\t\t\t\t\t// Keep references to cloned scripts for later restoration\n\t\t\t\t\tif ( hasScripts ) {\n\n\t\t\t\t\t\t// Support: Android <=4.0 only, PhantomJS 1 only\n\t\t\t\t\t\t// push.apply(_, arraylike) throws on ancient WebKit\n\t\t\t\t\t\tjQuery.merge( scripts, getAll( node, "script" ) );\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\tcallback.call( collection[ i ], node, i );\n\t\t\t}\n\n\t\t\tif ( hasScripts ) {\n\t\t\t\tdoc = scripts[ scripts.length - 1 ].ownerDocument;\n\n\t\t\t\t// Reenable scripts\n\t\t\t\tjQuery.map( scripts, restoreScript );\n\n\t\t\t\t// Evaluate executable scripts on first document insertion\n\t\t\t\tfor ( i = 0; i < hasScripts; i++ ) {\n\t\t\t\t\tnode = scripts[ i ];\n\t\t\t\t\tif ( rscriptType.test( node.type || "" ) &&\n\t\t\t\t\t\t!dataPriv.access( node, "globalEval" ) &&\n\t\t\t\t\t\tjQuery.contains( doc, node ) ) {\n\n\t\t\t\t\t\tif ( node.src && ( node.type || "" ).toLowerCase()  !== "module" ) {\n\n\t\t\t\t\t\t\t// Optional AJAX dependency, but won\'t run scripts if not present\n\t\t\t\t\t\t\tif ( jQuery._evalUrl && !node.noModule ) {\n\t\t\t\t\t\t\t\tjQuery._evalUrl( node.src, {\n\t\t\t\t\t\t\t\t\tnonce: node.nonce || node.getAttribute( "nonce" )\n\t\t\t\t\t\t\t\t} );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\tDOMEval( node.textContent.replace( rcleanScript, "" ), node, doc );\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\treturn collection;\n}\n\nfunction remove( elem, selector, keepData ) {\n\tvar node,\n\t\tnodes = selector ? jQuery.filter( selector, elem ) : elem,\n\t\ti = 0;\n\n\tfor ( ; ( node = nodes[ i ] ) != null; i++ ) {\n\t\tif ( !keepData && node.nodeType === 1 ) {\n\t\t\tjQuery.cleanData( getAll( node ) );\n\t\t}\n\n\t\tif ( node.parentNode ) {\n\t\t\tif ( keepData && isAttached( node ) ) {\n\t\t\t\tsetGlobalEval( getAll( node, "script" ) );\n\t\t\t}\n\t\t\tnode.parentNode.removeChild( node );\n\t\t}\n\t}\n\n\treturn elem;\n}\n\njQuery.extend( {\n\thtmlPrefilter: function( html ) {\n\t\treturn html.replace( rxhtmlTag, "<$1></$2>" );\n\t},\n\n\tclone: function( elem, dataAndEvents, deepDataAndEvents ) {\n\t\tvar i, l, srcElements, destElements,\n\t\t\tclone = elem.cloneNode( true ),\n\t\t\tinPage = isAttached( elem );\n\n\t\t// Fix IE cloning issues\n\t\tif ( !support.noCloneChecked && ( elem.nodeType === 1 || elem.nodeType === 11 ) &&\n\t\t\t\t!jQuery.isXMLDoc( elem ) ) {\n\n\t\t\t// We eschew Sizzle here for performance reasons: https://jsperf.com/getall-vs-sizzle/2\n\t\t\tdestElements = getAll( clone );\n\t\t\tsrcElements = getAll( elem );\n\n\t\t\tfor ( i = 0, l = srcElements.length; i < l; i++ ) {\n\t\t\t\tfixInput( srcElements[ i ], destElements[ i ] );\n\t\t\t}\n\t\t}\n\n\t\t// Copy the events from the original to the clone\n\t\tif ( dataAndEvents ) {\n\t\t\tif ( deepDataAndEvents ) {\n\t\t\t\tsrcElements = srcElements || getAll( elem );\n\t\t\t\tdestElements = destElements || getAll( clone );\n\n\t\t\t\tfor ( i = 0, l = srcElements.length; i < l; i++ ) {\n\t\t\t\t\tcloneCopyEvent( srcElements[ i ], destElements[ i ] );\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\tcloneCopyEvent( elem, clone );\n\t\t\t}\n\t\t}\n\n\t\t// Preserve script evaluation history\n\t\tdestElements = getAll( clone, "script" );\n\t\tif ( destElements.length > 0 ) {\n\t\t\tsetGlobalEval( destElements, !inPage && getAll( elem, "script" ) );\n\t\t}\n\n\t\t// Return the cloned set\n\t\treturn clone;\n\t},\n\n\tcleanData: function( elems ) {\n\t\tvar data, elem, type,\n\t\t\tspecial = jQuery.event.special,\n\t\t\ti = 0;\n\n\t\tfor ( ; ( elem = elems[ i ] ) !== undefined; i++ ) {\n\t\t\tif ( acceptData( elem ) ) {\n\t\t\t\tif ( ( data = elem[ dataPriv.expando ] ) ) {\n\t\t\t\t\tif ( data.events ) {\n\t\t\t\t\t\tfor ( type in data.events ) {\n\t\t\t\t\t\t\tif ( special[ type ] ) {\n\t\t\t\t\t\t\t\tjQuery.event.remove( elem, type );\n\n\t\t\t\t\t\t\t// This is a shortcut to avoid jQuery.event.remove\'s overhead\n\t\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t\tjQuery.removeEvent( elem, type, data.handle );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\t// Support: Chrome <=35 - 45+\n\t\t\t\t\t// Assign undefined instead of using delete, see Data#remove\n\t\t\t\t\telem[ dataPriv.expando ] = undefined;\n\t\t\t\t}\n\t\t\t\tif ( elem[ dataUser.expando ] ) {\n\n\t\t\t\t\t// Support: Chrome <=35 - 45+\n\t\t\t\t\t// Assign undefined instead of using delete, see Data#remove\n\t\t\t\t\telem[ dataUser.expando ] = undefined;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n} );\n\njQuery.fn.extend( {\n\tdetach: function( selector ) {\n\t\treturn remove( this, selector, true );\n\t},\n\n\tremove: function( selector ) {\n\t\treturn remove( this, selector );\n\t},\n\n\ttext: function( value ) {\n\t\treturn access( this, function( value ) {\n\t\t\treturn value === undefined ?\n\t\t\t\tjQuery.text( this ) :\n\t\t\t\tthis.empty().each( function() {\n\t\t\t\t\tif ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {\n\t\t\t\t\t\tthis.textContent = value;\n\t\t\t\t\t}\n\t\t\t\t} );\n\t\t}, null, value, arguments.length );\n\t},\n\n\tappend: function() {\n\t\treturn domManip( this, arguments, function( elem ) {\n\t\t\tif ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {\n\t\t\t\tvar target = manipulationTarget( this, elem );\n\t\t\t\ttarget.appendChild( elem );\n\t\t\t}\n\t\t} );\n\t},\n\n\tprepend: function() {\n\t\treturn domManip( this, arguments, function( elem ) {\n\t\t\tif ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {\n\t\t\t\tvar target = manipulationTarget( this, elem );\n\t\t\t\ttarget.insertBefore( elem, target.firstChild );\n\t\t\t}\n\t\t} );\n\t},\n\n\tbefore: function() {\n\t\treturn domManip( this, arguments, function( elem ) {\n\t\t\tif ( this.parentNode ) {\n\t\t\t\tthis.parentNode.insertBefore( elem, this );\n\t\t\t}\n\t\t} );\n\t},\n\n\tafter: function() {\n\t\treturn domManip( this, arguments, function( elem ) {\n\t\t\tif ( this.parentNode ) {\n\t\t\t\tthis.parentNode.insertBefore( elem, this.nextSibling );\n\t\t\t}\n\t\t} );\n\t},\n\n\tempty: function() {\n\t\tvar elem,\n\t\t\ti = 0;\n\n\t\tfor ( ; ( elem = this[ i ] ) != null; i++ ) {\n\t\t\tif ( elem.nodeType === 1 ) {\n\n\t\t\t\t// Prevent memory leaks\n\t\t\t\tjQuery.cleanData( getAll( elem, false ) );\n\n\t\t\t\t// Remove any remaining nodes\n\t\t\t\telem.textContent = "";\n\t\t\t}\n\t\t}\n\n\t\treturn this;\n\t},\n\n\tclone: function( dataAndEvents, deepDataAndEvents ) {\n\t\tdataAndEvents = dataAndEvents == null ? false : dataAndEvents;\n\t\tdeepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;\n\n\t\treturn this.map( function() {\n\t\t\treturn jQuery.clone( this, dataAndEvents, deepDataAndEvents );\n\t\t} );\n\t},\n\n\thtml: function( value ) {\n\t\treturn access( this, function( value ) {\n\t\t\tvar elem = this[ 0 ] || {},\n\t\t\t\ti = 0,\n\t\t\t\tl = this.length;\n\n\t\t\tif ( value === undefined && elem.nodeType === 1 ) {\n\t\t\t\treturn elem.innerHTML;\n\t\t\t}\n\n\t\t\t// See if we can take a shortcut and just use innerHTML\n\t\t\tif ( typeof value === "string" && !rnoInnerhtml.test( value ) &&\n\t\t\t\t!wrapMap[ ( rtagName.exec( value ) || [ "", "" ] )[ 1 ].toLowerCase() ] ) {\n\n\t\t\t\tvalue = jQuery.htmlPrefilter( value );\n\n\t\t\t\ttry {\n\t\t\t\t\tfor ( ; i < l; i++ ) {\n\t\t\t\t\t\telem = this[ i ] || {};\n\n\t\t\t\t\t\t// Remove element nodes and prevent memory leaks\n\t\t\t\t\t\tif ( elem.nodeType === 1 ) {\n\t\t\t\t\t\t\tjQuery.cleanData( getAll( elem, false ) );\n\t\t\t\t\t\t\telem.innerHTML = value;\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\telem = 0;\n\n\t\t\t\t// If using innerHTML throws an exception, use the fallback method\n\t\t\t\t} catch ( e ) {}\n\t\t\t}\n\n\t\t\tif ( elem ) {\n\t\t\t\tthis.empty().append( value );\n\t\t\t}\n\t\t}, null, value, arguments.length );\n\t},\n\n\treplaceWith: function() {\n\t\tvar ignored = [];\n\n\t\t// Make the changes, replacing each non-ignored context element with the new content\n\t\treturn domManip( this, arguments, function( elem ) {\n\t\t\tvar parent = this.parentNode;\n\n\t\t\tif ( jQuery.inArray( this, ignored ) < 0 ) {\n\t\t\t\tjQuery.cleanData( getAll( this ) );\n\t\t\t\tif ( parent ) {\n\t\t\t\t\tparent.replaceChild( elem, this );\n\t\t\t\t}\n\t\t\t}\n\n\t\t// Force callback invocation\n\t\t}, ignored );\n\t}\n} );\n\njQuery.each( {\n\tappendTo: "append",\n\tprependTo: "prepend",\n\tinsertBefore: "before",\n\tinsertAfter: "after",\n\treplaceAll: "replaceWith"\n}, function( name, original ) {\n\tjQuery.fn[ name ] = function( selector ) {\n\t\tvar elems,\n\t\t\tret = [],\n\t\t\tinsert = jQuery( selector ),\n\t\t\tlast = insert.length - 1,\n\t\t\ti = 0;\n\n\t\tfor ( ; i <= last; i++ ) {\n\t\t\telems = i === last ? this : this.clone( true );\n\t\t\tjQuery( insert[ i ] )[ original ]( elems );\n\n\t\t\t// Support: Android <=4.0 only, PhantomJS 1 only\n\t\t\t// .get() because push.apply(_, arraylike) throws on ancient WebKit\n\t\t\tpush.apply( ret, elems.get() );\n\t\t}\n\n\t\treturn this.pushStack( ret );\n\t};\n} );\nvar rnumnonpx = new RegExp( "^(" + pnum + ")(?!px)[a-z%]+$", "i" );\n\nvar getStyles = function( elem ) {\n\n\t\t// Support: IE <=11 only, Firefox <=30 (#15098, #14150)\n\t\t// IE throws on elements created in popups\n\t\t// FF meanwhile throws on frame elements through "defaultView.getComputedStyle"\n\t\tvar view = elem.ownerDocument.defaultView;\n\n\t\tif ( !view || !view.opener ) {\n\t\t\tview = window;\n\t\t}\n\n\t\treturn view.getComputedStyle( elem );\n\t};\n\nvar rboxStyle = new RegExp( cssExpand.join( "|" ), "i" );\n\n\n\n( function() {\n\n\t// Executing both pixelPosition & boxSizingReliable tests require only one layout\n\t// so they\'re executed at the same time to save the second computation.\n\tfunction computeStyleTests() {\n\n\t\t// This is a singleton, we need to execute it only once\n\t\tif ( !div ) {\n\t\t\treturn;\n\t\t}\n\n\t\tcontainer.style.cssText = "position:absolute;left:-11111px;width:60px;" +\n\t\t\t"margin-top:1px;padding:0;border:0";\n\t\tdiv.style.cssText =\n\t\t\t"position:relative;display:block;box-sizing:border-box;overflow:scroll;" +\n\t\t\t"margin:auto;border:1px;padding:1px;" +\n\t\t\t"width:60%;top:1%";\n\t\tdocumentElement.appendChild( container ).appendChild( div );\n\n\t\tvar divStyle = window.getComputedStyle( div );\n\t\tpixelPositionVal = divStyle.top !== "1%";\n\n\t\t// Support: Android 4.0 - 4.3 only, Firefox <=3 - 44\n\t\treliableMarginLeftVal = roundPixelMeasures( divStyle.marginLeft ) === 12;\n\n\t\t// Support: Android 4.0 - 4.3 only, Safari <=9.1 - 10.1, iOS <=7.0 - 9.3\n\t\t// Some styles come back with percentage values, even though they shouldn\'t\n\t\tdiv.style.right = "60%";\n\t\tpixelBoxStylesVal = roundPixelMeasures( divStyle.right ) === 36;\n\n\t\t// Support: IE 9 - 11 only\n\t\t// Detect misreporting of content dimensions for box-sizing:border-box elements\n\t\tboxSizingReliableVal = roundPixelMeasures( divStyle.width ) === 36;\n\n\t\t// Support: IE 9 only\n\t\t// Detect overflow:scroll screwiness (gh-3699)\n\t\t// Support: Chrome <=64\n\t\t// Don\'t get tricked when zoom affects offsetWidth (gh-4029)\n\t\tdiv.style.position = "absolute";\n\t\tscrollboxSizeVal = roundPixelMeasures( div.offsetWidth / 3 ) === 12;\n\n\t\tdocumentElement.removeChild( container );\n\n\t\t// Nullify the div so it wouldn\'t be stored in the memory and\n\t\t// it will also be a sign that checks already performed\n\t\tdiv = null;\n\t}\n\n\tfunction roundPixelMeasures( measure ) {\n\t\treturn Math.round( parseFloat( measure ) );\n\t}\n\n\tvar pixelPositionVal, boxSizingReliableVal, scrollboxSizeVal, pixelBoxStylesVal,\n\t\treliableMarginLeftVal,\n\t\tcontainer = document.createElement( "div" ),\n\t\tdiv = document.createElement( "div" );\n\n\t// Finish early in limited (non-browser) environments\n\tif ( !div.style ) {\n\t\treturn;\n\t}\n\n\t// Support: IE <=9 - 11 only\n\t// Style of cloned element affects source element cloned (#8908)\n\tdiv.style.backgroundClip = "content-box";\n\tdiv.cloneNode( true ).style.backgroundClip = "";\n\tsupport.clearCloneStyle = div.style.backgroundClip === "content-box";\n\n\tjQuery.extend( support, {\n\t\tboxSizingReliable: function() {\n\t\t\tcomputeStyleTests();\n\t\t\treturn boxSizingReliableVal;\n\t\t},\n\t\tpixelBoxStyles: function() {\n\t\t\tcomputeStyleTests();\n\t\t\treturn pixelBoxStylesVal;\n\t\t},\n\t\tpixelPosition: function() {\n\t\t\tcomputeStyleTests();\n\t\t\treturn pixelPositionVal;\n\t\t},\n\t\treliableMarginLeft: function() {\n\t\t\tcomputeStyleTests();\n\t\t\treturn reliableMarginLeftVal;\n\t\t},\n\t\tscrollboxSize: function() {\n\t\t\tcomputeStyleTests();\n\t\t\treturn scrollboxSizeVal;\n\t\t}\n\t} );\n} )();\n\n\nfunction curCSS( elem, name, computed ) {\n\tvar width, minWidth, maxWidth, ret,\n\n\t\t// Support: Firefox 51+\n\t\t// Retrieving style before computed somehow\n\t\t// fixes an issue with getting wrong values\n\t\t// on detached elements\n\t\tstyle = elem.style;\n\n\tcomputed = computed || getStyles( elem );\n\n\t// getPropertyValue is needed for:\n\t//   .css(\'filter\') (IE 9 only, #12537)\n\t//   .css(\'--customProperty) (#3144)\n\tif ( computed ) {\n\t\tret = computed.getPropertyValue( name ) || computed[ name ];\n\n\t\tif ( ret === "" && !isAttached( elem ) ) {\n\t\t\tret = jQuery.style( elem, name );\n\t\t}\n\n\t\t// A tribute to the "awesome hack by Dean Edwards"\n\t\t// Android Browser returns percentage for some values,\n\t\t// but width seems to be reliably pixels.\n\t\t// This is against the CSSOM draft spec:\n\t\t// https://drafts.csswg.org/cssom/#resolved-values\n\t\tif ( !support.pixelBoxStyles() && rnumnonpx.test( ret ) && rboxStyle.test( name ) ) {\n\n\t\t\t// Remember the original values\n\t\t\twidth = style.width;\n\t\t\tminWidth = style.minWidth;\n\t\t\tmaxWidth = style.maxWidth;\n\n\t\t\t// Put in the new values to get a computed value out\n\t\t\tstyle.minWidth = style.maxWidth = style.width = ret;\n\t\t\tret = computed.width;\n\n\t\t\t// Revert the changed values\n\t\t\tstyle.width = width;\n\t\t\tstyle.minWidth = minWidth;\n\t\t\tstyle.maxWidth = maxWidth;\n\t\t}\n\t}\n\n\treturn ret !== undefined ?\n\n\t\t// Support: IE <=9 - 11 only\n\t\t// IE returns zIndex value as an integer.\n\t\tret + "" :\n\t\tret;\n}\n\n\nfunction addGetHookIf( conditionFn, hookFn ) {\n\n\t// Define the hook, we\'ll check on the first run if it\'s really needed.\n\treturn {\n\t\tget: function() {\n\t\t\tif ( conditionFn() ) {\n\n\t\t\t\t// Hook not needed (or it\'s not possible to use it due\n\t\t\t\t// to missing dependency), remove it.\n\t\t\t\tdelete this.get;\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\t// Hook needed; redefine it so that the support test is not executed again.\n\t\t\treturn ( this.get = hookFn ).apply( this, arguments );\n\t\t}\n\t};\n}\n\n\nvar cssPrefixes = [ "Webkit", "Moz", "ms" ],\n\temptyStyle = document.createElement( "div" ).style,\n\tvendorProps = {};\n\n// Return a vendor-prefixed property or undefined\nfunction vendorPropName( name ) {\n\n\t// Check for vendor prefixed names\n\tvar capName = name[ 0 ].toUpperCase() + name.slice( 1 ),\n\t\ti = cssPrefixes.length;\n\n\twhile ( i-- ) {\n\t\tname = cssPrefixes[ i ] + capName;\n\t\tif ( name in emptyStyle ) {\n\t\t\treturn name;\n\t\t}\n\t}\n}\n\n// Return a potentially-mapped jQuery.cssProps or vendor prefixed property\nfunction finalPropName( name ) {\n\tvar final = jQuery.cssProps[ name ] || vendorProps[ name ];\n\n\tif ( final ) {\n\t\treturn final;\n\t}\n\tif ( name in emptyStyle ) {\n\t\treturn name;\n\t}\n\treturn vendorProps[ name ] = vendorPropName( name ) || name;\n}\n\n\nvar\n\n\t// Swappable if display is none or starts with table\n\t// except "table", "table-cell", or "table-caption"\n\t// See here for display values: https://developer.mozilla.org/en-US/docs/CSS/display\n\trdisplayswap = /^(none|table(?!-c[ea]).+)/,\n\trcustomProp = /^--/,\n\tcssShow = { position: "absolute", visibility: "hidden", display: "block" },\n\tcssNormalTransform = {\n\t\tletterSpacing: "0",\n\t\tfontWeight: "400"\n\t};\n\nfunction setPositiveNumber( elem, value, subtract ) {\n\n\t// Any relative (+/-) values have already been\n\t// normalized at this point\n\tvar matches = rcssNum.exec( value );\n\treturn matches ?\n\n\t\t// Guard against undefined "subtract", e.g., when used as in cssHooks\n\t\tMath.max( 0, matches[ 2 ] - ( subtract || 0 ) ) + ( matches[ 3 ] || "px" ) :\n\t\tvalue;\n}\n\nfunction boxModelAdjustment( elem, dimension, box, isBorderBox, styles, computedVal ) {\n\tvar i = dimension === "width" ? 1 : 0,\n\t\textra = 0,\n\t\tdelta = 0;\n\n\t// Adjustment may not be necessary\n\tif ( box === ( isBorderBox ? "border" : "content" ) ) {\n\t\treturn 0;\n\t}\n\n\tfor ( ; i < 4; i += 2 ) {\n\n\t\t// Both box models exclude margin\n\t\tif ( box === "margin" ) {\n\t\t\tdelta += jQuery.css( elem, box + cssExpand[ i ], true, styles );\n\t\t}\n\n\t\t// If we get here with a content-box, we\'re seeking "padding" or "border" or "margin"\n\t\tif ( !isBorderBox ) {\n\n\t\t\t// Add padding\n\t\t\tdelta += jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );\n\n\t\t\t// For "border" or "margin", add border\n\t\t\tif ( box !== "padding" ) {\n\t\t\t\tdelta += jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );\n\n\t\t\t// But still keep track of it otherwise\n\t\t\t} else {\n\t\t\t\textra += jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );\n\t\t\t}\n\n\t\t// If we get here with a border-box (content + padding + border), we\'re seeking "content" or\n\t\t// "padding" or "margin"\n\t\t} else {\n\n\t\t\t// For "content", subtract padding\n\t\t\tif ( box === "content" ) {\n\t\t\t\tdelta -= jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );\n\t\t\t}\n\n\t\t\t// For "content" or "padding", subtract border\n\t\t\tif ( box !== "margin" ) {\n\t\t\t\tdelta -= jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );\n\t\t\t}\n\t\t}\n\t}\n\n\t// Account for positive content-box scroll gutter when requested by providing computedVal\n\tif ( !isBorderBox && computedVal >= 0 ) {\n\n\t\t// offsetWidth/offsetHeight is a rounded sum of content, padding, scroll gutter, and border\n\t\t// Assuming integer scroll gutter, subtract the rest and round down\n\t\tdelta += Math.max( 0, Math.ceil(\n\t\t\telem[ "offset" + dimension[ 0 ].toUpperCase() + dimension.slice( 1 ) ] -\n\t\t\tcomputedVal -\n\t\t\tdelta -\n\t\t\textra -\n\t\t\t0.5\n\n\t\t// If offsetWidth/offsetHeight is unknown, then we can\'t determine content-box scroll gutter\n\t\t// Use an explicit zero to avoid NaN (gh-3964)\n\t\t) ) || 0;\n\t}\n\n\treturn delta;\n}\n\nfunction getWidthOrHeight( elem, dimension, extra ) {\n\n\t// Start with computed style\n\tvar styles = getStyles( elem ),\n\n\t\t// To avoid forcing a reflow, only fetch boxSizing if we need it (gh-4322).\n\t\t// Fake content-box until we know it\'s needed to know the true value.\n\t\tboxSizingNeeded = !support.boxSizingReliable() || extra,\n\t\tisBorderBox = boxSizingNeeded &&\n\t\t\tjQuery.css( elem, "boxSizing", false, styles ) === "border-box",\n\t\tvalueIsBorderBox = isBorderBox,\n\n\t\tval = curCSS( elem, dimension, styles ),\n\t\toffsetProp = "offset" + dimension[ 0 ].toUpperCase() + dimension.slice( 1 );\n\n\t// Support: Firefox <=54\n\t// Return a confounding non-pixel value or feign ignorance, as appropriate.\n\tif ( rnumnonpx.test( val ) ) {\n\t\tif ( !extra ) {\n\t\t\treturn val;\n\t\t}\n\t\tval = "auto";\n\t}\n\n\n\t// Fall back to offsetWidth/offsetHeight when value is "auto"\n\t// This happens for inline elements with no explicit setting (gh-3571)\n\t// Support: Android <=4.1 - 4.3 only\n\t// Also use offsetWidth/offsetHeight for misreported inline dimensions (gh-3602)\n\t// Support: IE 9-11 only\n\t// Also use offsetWidth/offsetHeight for when box sizing is unreliable\n\t// We use getClientRects() to check for hidden/disconnected.\n\t// In those cases, the computed value can be trusted to be border-box\n\tif ( ( !support.boxSizingReliable() && isBorderBox ||\n\t\tval === "auto" ||\n\t\t!parseFloat( val ) && jQuery.css( elem, "display", false, styles ) === "inline" ) &&\n\t\telem.getClientRects().length ) {\n\n\t\tisBorderBox = jQuery.css( elem, "boxSizing", false, styles ) === "border-box";\n\n\t\t// Where available, offsetWidth/offsetHeight approximate border box dimensions.\n\t\t// Where not available (e.g., SVG), assume unreliable box-sizing and interpret the\n\t\t// retrieved value as a content box dimension.\n\t\tvalueIsBorderBox = offsetProp in elem;\n\t\tif ( valueIsBorderBox ) {\n\t\t\tval = elem[ offsetProp ];\n\t\t}\n\t}\n\n\t// Normalize "" and auto\n\tval = parseFloat( val ) || 0;\n\n\t// Adjust for the element\'s box model\n\treturn ( val +\n\t\tboxModelAdjustment(\n\t\t\telem,\n\t\t\tdimension,\n\t\t\textra || ( isBorderBox ? "border" : "content" ),\n\t\t\tvalueIsBorderBox,\n\t\t\tstyles,\n\n\t\t\t// Provide the current computed size to request scroll gutter calculation (gh-3589)\n\t\t\tval\n\t\t)\n\t) + "px";\n}\n\njQuery.extend( {\n\n\t// Add in style property hooks for overriding the default\n\t// behavior of getting and setting a style property\n\tcssHooks: {\n\t\topacity: {\n\t\t\tget: function( elem, computed ) {\n\t\t\t\tif ( computed ) {\n\n\t\t\t\t\t// We should always get a number back from opacity\n\t\t\t\t\tvar ret = curCSS( elem, "opacity" );\n\t\t\t\t\treturn ret === "" ? "1" : ret;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t},\n\n\t// Don\'t automatically add "px" to these possibly-unitless properties\n\tcssNumber: {\n\t\t"animationIterationCount": true,\n\t\t"columnCount": true,\n\t\t"fillOpacity": true,\n\t\t"flexGrow": true,\n\t\t"flexShrink": true,\n\t\t"fontWeight": true,\n\t\t"gridArea": true,\n\t\t"gridColumn": true,\n\t\t"gridColumnEnd": true,\n\t\t"gridColumnStart": true,\n\t\t"gridRow": true,\n\t\t"gridRowEnd": true,\n\t\t"gridRowStart": true,\n\t\t"lineHeight": true,\n\t\t"opacity": true,\n\t\t"order": true,\n\t\t"orphans": true,\n\t\t"widows": true,\n\t\t"zIndex": true,\n\t\t"zoom": true\n\t},\n\n\t// Add in properties whose names you wish to fix before\n\t// setting or getting the value\n\tcssProps: {},\n\n\t// Get and set the style property on a DOM Node\n\tstyle: function( elem, name, value, extra ) {\n\n\t\t// Don\'t set styles on text and comment nodes\n\t\tif ( !elem || elem.nodeType === 3 || elem.nodeType === 8 || !elem.style ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Make sure that we\'re working with the right name\n\t\tvar ret, type, hooks,\n\t\t\torigName = camelCase( name ),\n\t\t\tisCustomProp = rcustomProp.test( name ),\n\t\t\tstyle = elem.style;\n\n\t\t// Make sure that we\'re working with the right name. We don\'t\n\t\t// want to query the value if it is a CSS custom property\n\t\t// since they are user-defined.\n\t\tif ( !isCustomProp ) {\n\t\t\tname = finalPropName( origName );\n\t\t}\n\n\t\t// Gets hook for the prefixed version, then unprefixed version\n\t\thooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];\n\n\t\t// Check if we\'re setting a value\n\t\tif ( value !== undefined ) {\n\t\t\ttype = typeof value;\n\n\t\t\t// Convert "+=" or "-=" to relative numbers (#7345)\n\t\t\tif ( type === "string" && ( ret = rcssNum.exec( value ) ) && ret[ 1 ] ) {\n\t\t\t\tvalue = adjustCSS( elem, name, ret );\n\n\t\t\t\t// Fixes bug #9237\n\t\t\t\ttype = "number";\n\t\t\t}\n\n\t\t\t// Make sure that null and NaN values aren\'t set (#7116)\n\t\t\tif ( value == null || value !== value ) {\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\t// If a number was passed in, add the unit (except for certain CSS properties)\n\t\t\t// The isCustomProp check can be removed in jQuery 4.0 when we only auto-append\n\t\t\t// "px" to a few hardcoded values.\n\t\t\tif ( type === "number" && !isCustomProp ) {\n\t\t\t\tvalue += ret && ret[ 3 ] || ( jQuery.cssNumber[ origName ] ? "" : "px" );\n\t\t\t}\n\n\t\t\t// background-* props affect original clone\'s values\n\t\t\tif ( !support.clearCloneStyle && value === "" && name.indexOf( "background" ) === 0 ) {\n\t\t\t\tstyle[ name ] = "inherit";\n\t\t\t}\n\n\t\t\t// If a hook was provided, use that value, otherwise just set the specified value\n\t\t\tif ( !hooks || !( "set" in hooks ) ||\n\t\t\t\t( value = hooks.set( elem, value, extra ) ) !== undefined ) {\n\n\t\t\t\tif ( isCustomProp ) {\n\t\t\t\t\tstyle.setProperty( name, value );\n\t\t\t\t} else {\n\t\t\t\t\tstyle[ name ] = value;\n\t\t\t\t}\n\t\t\t}\n\n\t\t} else {\n\n\t\t\t// If a hook was provided get the non-computed value from there\n\t\t\tif ( hooks && "get" in hooks &&\n\t\t\t\t( ret = hooks.get( elem, false, extra ) ) !== undefined ) {\n\n\t\t\t\treturn ret;\n\t\t\t}\n\n\t\t\t// Otherwise just get the value from the style object\n\t\t\treturn style[ name ];\n\t\t}\n\t},\n\n\tcss: function( elem, name, extra, styles ) {\n\t\tvar val, num, hooks,\n\t\t\torigName = camelCase( name ),\n\t\t\tisCustomProp = rcustomProp.test( name );\n\n\t\t// Make sure that we\'re working with the right name. We don\'t\n\t\t// want to modify the value if it is a CSS custom property\n\t\t// since they are user-defined.\n\t\tif ( !isCustomProp ) {\n\t\t\tname = finalPropName( origName );\n\t\t}\n\n\t\t// Try prefixed name followed by the unprefixed name\n\t\thooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];\n\n\t\t// If a hook was provided get the computed value from there\n\t\tif ( hooks && "get" in hooks ) {\n\t\t\tval = hooks.get( elem, true, extra );\n\t\t}\n\n\t\t// Otherwise, if a way to get the computed value exists, use that\n\t\tif ( val === undefined ) {\n\t\t\tval = curCSS( elem, name, styles );\n\t\t}\n\n\t\t// Convert "normal" to computed value\n\t\tif ( val === "normal" && name in cssNormalTransform ) {\n\t\t\tval = cssNormalTransform[ name ];\n\t\t}\n\n\t\t// Make numeric if forced or a qualifier was provided and val looks numeric\n\t\tif ( extra === "" || extra ) {\n\t\t\tnum = parseFloat( val );\n\t\t\treturn extra === true || isFinite( num ) ? num || 0 : val;\n\t\t}\n\n\t\treturn val;\n\t}\n} );\n\njQuery.each( [ "height", "width" ], function( i, dimension ) {\n\tjQuery.cssHooks[ dimension ] = {\n\t\tget: function( elem, computed, extra ) {\n\t\t\tif ( computed ) {\n\n\t\t\t\t// Certain elements can have dimension info if we invisibly show them\n\t\t\t\t// but it must have a current display style that would benefit\n\t\t\t\treturn rdisplayswap.test( jQuery.css( elem, "display" ) ) &&\n\n\t\t\t\t\t// Support: Safari 8+\n\t\t\t\t\t// Table columns in Safari have non-zero offsetWidth & zero\n\t\t\t\t\t// getBoundingClientRect().width unless display is changed.\n\t\t\t\t\t// Support: IE <=11 only\n\t\t\t\t\t// Running getBoundingClientRect on a disconnected node\n\t\t\t\t\t// in IE throws an error.\n\t\t\t\t\t( !elem.getClientRects().length || !elem.getBoundingClientRect().width ) ?\n\t\t\t\t\t\tswap( elem, cssShow, function() {\n\t\t\t\t\t\t\treturn getWidthOrHeight( elem, dimension, extra );\n\t\t\t\t\t\t} ) :\n\t\t\t\t\t\tgetWidthOrHeight( elem, dimension, extra );\n\t\t\t}\n\t\t},\n\n\t\tset: function( elem, value, extra ) {\n\t\t\tvar matches,\n\t\t\t\tstyles = getStyles( elem ),\n\n\t\t\t\t// Only read styles.position if the test has a chance to fail\n\t\t\t\t// to avoid forcing a reflow.\n\t\t\t\tscrollboxSizeBuggy = !support.scrollboxSize() &&\n\t\t\t\t\tstyles.position === "absolute",\n\n\t\t\t\t// To avoid forcing a reflow, only fetch boxSizing if we need it (gh-3991)\n\t\t\t\tboxSizingNeeded = scrollboxSizeBuggy || extra,\n\t\t\t\tisBorderBox = boxSizingNeeded &&\n\t\t\t\t\tjQuery.css( elem, "boxSizing", false, styles ) === "border-box",\n\t\t\t\tsubtract = extra ?\n\t\t\t\t\tboxModelAdjustment(\n\t\t\t\t\t\telem,\n\t\t\t\t\t\tdimension,\n\t\t\t\t\t\textra,\n\t\t\t\t\t\tisBorderBox,\n\t\t\t\t\t\tstyles\n\t\t\t\t\t) :\n\t\t\t\t\t0;\n\n\t\t\t// Account for unreliable border-box dimensions by comparing offset* to computed and\n\t\t\t// faking a content-box to get border and padding (gh-3699)\n\t\t\tif ( isBorderBox && scrollboxSizeBuggy ) {\n\t\t\t\tsubtract -= Math.ceil(\n\t\t\t\t\telem[ "offset" + dimension[ 0 ].toUpperCase() + dimension.slice( 1 ) ] -\n\t\t\t\t\tparseFloat( styles[ dimension ] ) -\n\t\t\t\t\tboxModelAdjustment( elem, dimension, "border", false, styles ) -\n\t\t\t\t\t0.5\n\t\t\t\t);\n\t\t\t}\n\n\t\t\t// Convert to pixels if value adjustment is needed\n\t\t\tif ( subtract && ( matches = rcssNum.exec( value ) ) &&\n\t\t\t\t( matches[ 3 ] || "px" ) !== "px" ) {\n\n\t\t\t\telem.style[ dimension ] = value;\n\t\t\t\tvalue = jQuery.css( elem, dimension );\n\t\t\t}\n\n\t\t\treturn setPositiveNumber( elem, value, subtract );\n\t\t}\n\t};\n} );\n\njQuery.cssHooks.marginLeft = addGetHookIf( support.reliableMarginLeft,\n\tfunction( elem, computed ) {\n\t\tif ( computed ) {\n\t\t\treturn ( parseFloat( curCSS( elem, "marginLeft" ) ) ||\n\t\t\t\telem.getBoundingClientRect().left -\n\t\t\t\t\tswap( elem, { marginLeft: 0 }, function() {\n\t\t\t\t\t\treturn elem.getBoundingClientRect().left;\n\t\t\t\t\t} )\n\t\t\t\t) + "px";\n\t\t}\n\t}\n);\n\n// These hooks are used by animate to expand properties\njQuery.each( {\n\tmargin: "",\n\tpadding: "",\n\tborder: "Width"\n}, function( prefix, suffix ) {\n\tjQuery.cssHooks[ prefix + suffix ] = {\n\t\texpand: function( value ) {\n\t\t\tvar i = 0,\n\t\t\t\texpanded = {},\n\n\t\t\t\t// Assumes a single number if not a string\n\t\t\t\tparts = typeof value === "string" ? value.split( " " ) : [ value ];\n\n\t\t\tfor ( ; i < 4; i++ ) {\n\t\t\t\texpanded[ prefix + cssExpand[ i ] + suffix ] =\n\t\t\t\t\tparts[ i ] || parts[ i - 2 ] || parts[ 0 ];\n\t\t\t}\n\n\t\t\treturn expanded;\n\t\t}\n\t};\n\n\tif ( prefix !== "margin" ) {\n\t\tjQuery.cssHooks[ prefix + suffix ].set = setPositiveNumber;\n\t}\n} );\n\njQuery.fn.extend( {\n\tcss: function( name, value ) {\n\t\treturn access( this, function( elem, name, value ) {\n\t\t\tvar styles, len,\n\t\t\t\tmap = {},\n\t\t\t\ti = 0;\n\n\t\t\tif ( Array.isArray( name ) ) {\n\t\t\t\tstyles = getStyles( elem );\n\t\t\t\tlen = name.length;\n\n\t\t\t\tfor ( ; i < len; i++ ) {\n\t\t\t\t\tmap[ name[ i ] ] = jQuery.css( elem, name[ i ], false, styles );\n\t\t\t\t}\n\n\t\t\t\treturn map;\n\t\t\t}\n\n\t\t\treturn value !== undefined ?\n\t\t\t\tjQuery.style( elem, name, value ) :\n\t\t\t\tjQuery.css( elem, name );\n\t\t}, name, value, arguments.length > 1 );\n\t}\n} );\n\n\nfunction Tween( elem, options, prop, end, easing ) {\n\treturn new Tween.prototype.init( elem, options, prop, end, easing );\n}\njQuery.Tween = Tween;\n\nTween.prototype = {\n\tconstructor: Tween,\n\tinit: function( elem, options, prop, end, easing, unit ) {\n\t\tthis.elem = elem;\n\t\tthis.prop = prop;\n\t\tthis.easing = easing || jQuery.easing._default;\n\t\tthis.options = options;\n\t\tthis.start = this.now = this.cur();\n\t\tthis.end = end;\n\t\tthis.unit = unit || ( jQuery.cssNumber[ prop ] ? "" : "px" );\n\t},\n\tcur: function() {\n\t\tvar hooks = Tween.propHooks[ this.prop ];\n\n\t\treturn hooks && hooks.get ?\n\t\t\thooks.get( this ) :\n\t\t\tTween.propHooks._default.get( this );\n\t},\n\trun: function( percent ) {\n\t\tvar eased,\n\t\t\thooks = Tween.propHooks[ this.prop ];\n\n\t\tif ( this.options.duration ) {\n\t\t\tthis.pos = eased = jQuery.easing[ this.easing ](\n\t\t\t\tpercent, this.options.duration * percent, 0, 1, this.options.duration\n\t\t\t);\n\t\t} else {\n\t\t\tthis.pos = eased = percent;\n\t\t}\n\t\tthis.now = ( this.end - this.start ) * eased + this.start;\n\n\t\tif ( this.options.step ) {\n\t\t\tthis.options.step.call( this.elem, this.now, this );\n\t\t}\n\n\t\tif ( hooks && hooks.set ) {\n\t\t\thooks.set( this );\n\t\t} else {\n\t\t\tTween.propHooks._default.set( this );\n\t\t}\n\t\treturn this;\n\t}\n};\n\nTween.prototype.init.prototype = Tween.prototype;\n\nTween.propHooks = {\n\t_default: {\n\t\tget: function( tween ) {\n\t\t\tvar result;\n\n\t\t\t// Use a property on the element directly when it is not a DOM element,\n\t\t\t// or when there is no matching style property that exists.\n\t\t\tif ( tween.elem.nodeType !== 1 ||\n\t\t\t\ttween.elem[ tween.prop ] != null && tween.elem.style[ tween.prop ] == null ) {\n\t\t\t\treturn tween.elem[ tween.prop ];\n\t\t\t}\n\n\t\t\t// Passing an empty string as a 3rd parameter to .css will automatically\n\t\t\t// attempt a parseFloat and fallback to a string if the parse fails.\n\t\t\t// Simple values such as "10px" are parsed to Float;\n\t\t\t// complex values such as "rotate(1rad)" are returned as-is.\n\t\t\tresult = jQuery.css( tween.elem, tween.prop, "" );\n\n\t\t\t// Empty strings, null, undefined and "auto" are converted to 0.\n\t\t\treturn !result || result === "auto" ? 0 : result;\n\t\t},\n\t\tset: function( tween ) {\n\n\t\t\t// Use step hook for back compat.\n\t\t\t// Use cssHook if its there.\n\t\t\t// Use .style if available and use plain properties where available.\n\t\t\tif ( jQuery.fx.step[ tween.prop ] ) {\n\t\t\t\tjQuery.fx.step[ tween.prop ]( tween );\n\t\t\t} else if ( tween.elem.nodeType === 1 && (\n\t\t\t\t\tjQuery.cssHooks[ tween.prop ] ||\n\t\t\t\t\ttween.elem.style[ finalPropName( tween.prop ) ] != null ) ) {\n\t\t\t\tjQuery.style( tween.elem, tween.prop, tween.now + tween.unit );\n\t\t\t} else {\n\t\t\t\ttween.elem[ tween.prop ] = tween.now;\n\t\t\t}\n\t\t}\n\t}\n};\n\n// Support: IE <=9 only\n// Panic based approach to setting things on disconnected nodes\nTween.propHooks.scrollTop = Tween.propHooks.scrollLeft = {\n\tset: function( tween ) {\n\t\tif ( tween.elem.nodeType && tween.elem.parentNode ) {\n\t\t\ttween.elem[ tween.prop ] = tween.now;\n\t\t}\n\t}\n};\n\njQuery.easing = {\n\tlinear: function( p ) {\n\t\treturn p;\n\t},\n\tswing: function( p ) {\n\t\treturn 0.5 - Math.cos( p * Math.PI ) / 2;\n\t},\n\t_default: "swing"\n};\n\njQuery.fx = Tween.prototype.init;\n\n// Back compat <1.8 extension point\njQuery.fx.step = {};\n\n\n\n\nvar\n\tfxNow, inProgress,\n\trfxtypes = /^(?:toggle|show|hide)$/,\n\trrun = /queueHooks$/;\n\nfunction schedule() {\n\tif ( inProgress ) {\n\t\tif ( document.hidden === false && window.requestAnimationFrame ) {\n\t\t\twindow.requestAnimationFrame( schedule );\n\t\t} else {\n\t\t\twindow.setTimeout( schedule, jQuery.fx.interval );\n\t\t}\n\n\t\tjQuery.fx.tick();\n\t}\n}\n\n// Animations created synchronously will run synchronously\nfunction createFxNow() {\n\twindow.setTimeout( function() {\n\t\tfxNow = undefined;\n\t} );\n\treturn ( fxNow = Date.now() );\n}\n\n// Generate parameters to create a standard animation\nfunction genFx( type, includeWidth ) {\n\tvar which,\n\t\ti = 0,\n\t\tattrs = { height: type };\n\n\t// If we include width, step value is 1 to do all cssExpand values,\n\t// otherwise step value is 2 to skip over Left and Right\n\tincludeWidth = includeWidth ? 1 : 0;\n\tfor ( ; i < 4; i += 2 - includeWidth ) {\n\t\twhich = cssExpand[ i ];\n\t\tattrs[ "margin" + which ] = attrs[ "padding" + which ] = type;\n\t}\n\n\tif ( includeWidth ) {\n\t\tattrs.opacity = attrs.width = type;\n\t}\n\n\treturn attrs;\n}\n\nfunction createTween( value, prop, animation ) {\n\tvar tween,\n\t\tcollection = ( Animation.tweeners[ prop ] || [] ).concat( Animation.tweeners[ "*" ] ),\n\t\tindex = 0,\n\t\tlength = collection.length;\n\tfor ( ; index < length; index++ ) {\n\t\tif ( ( tween = collection[ index ].call( animation, prop, value ) ) ) {\n\n\t\t\t// We\'re done with this property\n\t\t\treturn tween;\n\t\t}\n\t}\n}\n\nfunction defaultPrefilter( elem, props, opts ) {\n\tvar prop, value, toggle, hooks, oldfire, propTween, restoreDisplay, display,\n\t\tisBox = "width" in props || "height" in props,\n\t\tanim = this,\n\t\torig = {},\n\t\tstyle = elem.style,\n\t\thidden = elem.nodeType && isHiddenWithinTree( elem ),\n\t\tdataShow = dataPriv.get( elem, "fxshow" );\n\n\t// Queue-skipping animations hijack the fx hooks\n\tif ( !opts.queue ) {\n\t\thooks = jQuery._queueHooks( elem, "fx" );\n\t\tif ( hooks.unqueued == null ) {\n\t\t\thooks.unqueued = 0;\n\t\t\toldfire = hooks.empty.fire;\n\t\t\thooks.empty.fire = function() {\n\t\t\t\tif ( !hooks.unqueued ) {\n\t\t\t\t\toldfire();\n\t\t\t\t}\n\t\t\t};\n\t\t}\n\t\thooks.unqueued++;\n\n\t\tanim.always( function() {\n\n\t\t\t// Ensure the complete handler is called before this completes\n\t\t\tanim.always( function() {\n\t\t\t\thooks.unqueued--;\n\t\t\t\tif ( !jQuery.queue( elem, "fx" ).length ) {\n\t\t\t\t\thooks.empty.fire();\n\t\t\t\t}\n\t\t\t} );\n\t\t} );\n\t}\n\n\t// Detect show/hide animations\n\tfor ( prop in props ) {\n\t\tvalue = props[ prop ];\n\t\tif ( rfxtypes.test( value ) ) {\n\t\t\tdelete props[ prop ];\n\t\t\ttoggle = toggle || value === "toggle";\n\t\t\tif ( value === ( hidden ? "hide" : "show" ) ) {\n\n\t\t\t\t// Pretend to be hidden if this is a "show" and\n\t\t\t\t// there is still data from a stopped show/hide\n\t\t\t\tif ( value === "show" && dataShow && dataShow[ prop ] !== undefined ) {\n\t\t\t\t\thidden = true;\n\n\t\t\t\t// Ignore all other no-op show/hide data\n\t\t\t\t} else {\n\t\t\t\t\tcontinue;\n\t\t\t\t}\n\t\t\t}\n\t\t\torig[ prop ] = dataShow && dataShow[ prop ] || jQuery.style( elem, prop );\n\t\t}\n\t}\n\n\t// Bail out if this is a no-op like .hide().hide()\n\tpropTween = !jQuery.isEmptyObject( props );\n\tif ( !propTween && jQuery.isEmptyObject( orig ) ) {\n\t\treturn;\n\t}\n\n\t// Restrict "overflow" and "display" styles during box animations\n\tif ( isBox && elem.nodeType === 1 ) {\n\n\t\t// Support: IE <=9 - 11, Edge 12 - 15\n\t\t// Record all 3 overflow attributes because IE does not infer the shorthand\n\t\t// from identically-valued overflowX and overflowY and Edge just mirrors\n\t\t// the overflowX value there.\n\t\topts.overflow = [ style.overflow, style.overflowX, style.overflowY ];\n\n\t\t// Identify a display type, preferring old show/hide data over the CSS cascade\n\t\trestoreDisplay = dataShow && dataShow.display;\n\t\tif ( restoreDisplay == null ) {\n\t\t\trestoreDisplay = dataPriv.get( elem, "display" );\n\t\t}\n\t\tdisplay = jQuery.css( elem, "display" );\n\t\tif ( display === "none" ) {\n\t\t\tif ( restoreDisplay ) {\n\t\t\t\tdisplay = restoreDisplay;\n\t\t\t} else {\n\n\t\t\t\t// Get nonempty value(s) by temporarily forcing visibility\n\t\t\t\tshowHide( [ elem ], true );\n\t\t\t\trestoreDisplay = elem.style.display || restoreDisplay;\n\t\t\t\tdisplay = jQuery.css( elem, "display" );\n\t\t\t\tshowHide( [ elem ] );\n\t\t\t}\n\t\t}\n\n\t\t// Animate inline elements as inline-block\n\t\tif ( display === "inline" || display === "inline-block" && restoreDisplay != null ) {\n\t\t\tif ( jQuery.css( elem, "float" ) === "none" ) {\n\n\t\t\t\t// Restore the original display value at the end of pure show/hide animations\n\t\t\t\tif ( !propTween ) {\n\t\t\t\t\tanim.done( function() {\n\t\t\t\t\t\tstyle.display = restoreDisplay;\n\t\t\t\t\t} );\n\t\t\t\t\tif ( restoreDisplay == null ) {\n\t\t\t\t\t\tdisplay = style.display;\n\t\t\t\t\t\trestoreDisplay = display === "none" ? "" : display;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t\tstyle.display = "inline-block";\n\t\t\t}\n\t\t}\n\t}\n\n\tif ( opts.overflow ) {\n\t\tstyle.overflow = "hidden";\n\t\tanim.always( function() {\n\t\t\tstyle.overflow = opts.overflow[ 0 ];\n\t\t\tstyle.overflowX = opts.overflow[ 1 ];\n\t\t\tstyle.overflowY = opts.overflow[ 2 ];\n\t\t} );\n\t}\n\n\t// Implement show/hide animations\n\tpropTween = false;\n\tfor ( prop in orig ) {\n\n\t\t// General show/hide setup for this element animation\n\t\tif ( !propTween ) {\n\t\t\tif ( dataShow ) {\n\t\t\t\tif ( "hidden" in dataShow ) {\n\t\t\t\t\thidden = dataShow.hidden;\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\tdataShow = dataPriv.access( elem, "fxshow", { display: restoreDisplay } );\n\t\t\t}\n\n\t\t\t// Store hidden/visible for toggle so `.stop().toggle()` "reverses"\n\t\t\tif ( toggle ) {\n\t\t\t\tdataShow.hidden = !hidden;\n\t\t\t}\n\n\t\t\t// Show elements before animating them\n\t\t\tif ( hidden ) {\n\t\t\t\tshowHide( [ elem ], true );\n\t\t\t}\n\n\t\t\t/* eslint-disable no-loop-func */\n\n\t\t\tanim.done( function() {\n\n\t\t\t/* eslint-enable no-loop-func */\n\n\t\t\t\t// The final step of a "hide" animation is actually hiding the element\n\t\t\t\tif ( !hidden ) {\n\t\t\t\t\tshowHide( [ elem ] );\n\t\t\t\t}\n\t\t\t\tdataPriv.remove( elem, "fxshow" );\n\t\t\t\tfor ( prop in orig ) {\n\t\t\t\t\tjQuery.style( elem, prop, orig[ prop ] );\n\t\t\t\t}\n\t\t\t} );\n\t\t}\n\n\t\t// Per-property setup\n\t\tpropTween = createTween( hidden ? dataShow[ prop ] : 0, prop, anim );\n\t\tif ( !( prop in dataShow ) ) {\n\t\t\tdataShow[ prop ] = propTween.start;\n\t\t\tif ( hidden ) {\n\t\t\t\tpropTween.end = propTween.start;\n\t\t\t\tpropTween.start = 0;\n\t\t\t}\n\t\t}\n\t}\n}\n\nfunction propFilter( props, specialEasing ) {\n\tvar index, name, easing, value, hooks;\n\n\t// camelCase, specialEasing and expand cssHook pass\n\tfor ( index in props ) {\n\t\tname = camelCase( index );\n\t\teasing = specialEasing[ name ];\n\t\tvalue = props[ index ];\n\t\tif ( Array.isArray( value ) ) {\n\t\t\teasing = value[ 1 ];\n\t\t\tvalue = props[ index ] = value[ 0 ];\n\t\t}\n\n\t\tif ( index !== name ) {\n\t\t\tprops[ name ] = value;\n\t\t\tdelete props[ index ];\n\t\t}\n\n\t\thooks = jQuery.cssHooks[ name ];\n\t\tif ( hooks && "expand" in hooks ) {\n\t\t\tvalue = hooks.expand( value );\n\t\t\tdelete props[ name ];\n\n\t\t\t// Not quite $.extend, this won\'t overwrite existing keys.\n\t\t\t// Reusing \'index\' because we have the correct "name"\n\t\t\tfor ( index in value ) {\n\t\t\t\tif ( !( index in props ) ) {\n\t\t\t\t\tprops[ index ] = value[ index ];\n\t\t\t\t\tspecialEasing[ index ] = easing;\n\t\t\t\t}\n\t\t\t}\n\t\t} else {\n\t\t\tspecialEasing[ name ] = easing;\n\t\t}\n\t}\n}\n\nfunction Animation( elem, properties, options ) {\n\tvar result,\n\t\tstopped,\n\t\tindex = 0,\n\t\tlength = Animation.prefilters.length,\n\t\tdeferred = jQuery.Deferred().always( function() {\n\n\t\t\t// Don\'t match elem in the :animated selector\n\t\t\tdelete tick.elem;\n\t\t} ),\n\t\ttick = function() {\n\t\t\tif ( stopped ) {\n\t\t\t\treturn false;\n\t\t\t}\n\t\t\tvar currentTime = fxNow || createFxNow(),\n\t\t\t\tremaining = Math.max( 0, animation.startTime + animation.duration - currentTime ),\n\n\t\t\t\t// Support: Android 2.3 only\n\t\t\t\t// Archaic crash bug won\'t allow us to use `1 - ( 0.5 || 0 )` (#12497)\n\t\t\t\ttemp = remaining / animation.duration || 0,\n\t\t\t\tpercent = 1 - temp,\n\t\t\t\tindex = 0,\n\t\t\t\tlength = animation.tweens.length;\n\n\t\t\tfor ( ; index < length; index++ ) {\n\t\t\t\tanimation.tweens[ index ].run( percent );\n\t\t\t}\n\n\t\t\tdeferred.notifyWith( elem, [ animation, percent, remaining ] );\n\n\t\t\t// If there\'s more to do, yield\n\t\t\tif ( percent < 1 && length ) {\n\t\t\t\treturn remaining;\n\t\t\t}\n\n\t\t\t// If this was an empty animation, synthesize a final progress notification\n\t\t\tif ( !length ) {\n\t\t\t\tdeferred.notifyWith( elem, [ animation, 1, 0 ] );\n\t\t\t}\n\n\t\t\t// Resolve the animation and report its conclusion\n\t\t\tdeferred.resolveWith( elem, [ animation ] );\n\t\t\treturn false;\n\t\t},\n\t\tanimation = deferred.promise( {\n\t\t\telem: elem,\n\t\t\tprops: jQuery.extend( {}, properties ),\n\t\t\topts: jQuery.extend( true, {\n\t\t\t\tspecialEasing: {},\n\t\t\t\teasing: jQuery.easing._default\n\t\t\t}, options ),\n\t\t\toriginalProperties: properties,\n\t\t\toriginalOptions: options,\n\t\t\tstartTime: fxNow || createFxNow(),\n\t\t\tduration: options.duration,\n\t\t\ttweens: [],\n\t\t\tcreateTween: function( prop, end ) {\n\t\t\t\tvar tween = jQuery.Tween( elem, animation.opts, prop, end,\n\t\t\t\t\t\tanimation.opts.specialEasing[ prop ] || animation.opts.easing );\n\t\t\t\tanimation.tweens.push( tween );\n\t\t\t\treturn tween;\n\t\t\t},\n\t\t\tstop: function( gotoEnd ) {\n\t\t\t\tvar index = 0,\n\n\t\t\t\t\t// If we are going to the end, we want to run all the tweens\n\t\t\t\t\t// otherwise we skip this part\n\t\t\t\t\tlength = gotoEnd ? animation.tweens.length : 0;\n\t\t\t\tif ( stopped ) {\n\t\t\t\t\treturn this;\n\t\t\t\t}\n\t\t\t\tstopped = true;\n\t\t\t\tfor ( ; index < length; index++ ) {\n\t\t\t\t\tanimation.tweens[ index ].run( 1 );\n\t\t\t\t}\n\n\t\t\t\t// Resolve when we played the last frame; otherwise, reject\n\t\t\t\tif ( gotoEnd ) {\n\t\t\t\t\tdeferred.notifyWith( elem, [ animation, 1, 0 ] );\n\t\t\t\t\tdeferred.resolveWith( elem, [ animation, gotoEnd ] );\n\t\t\t\t} else {\n\t\t\t\t\tdeferred.rejectWith( elem, [ animation, gotoEnd ] );\n\t\t\t\t}\n\t\t\t\treturn this;\n\t\t\t}\n\t\t} ),\n\t\tprops = animation.props;\n\n\tpropFilter( props, animation.opts.specialEasing );\n\n\tfor ( ; index < length; index++ ) {\n\t\tresult = Animation.prefilters[ index ].call( animation, elem, props, animation.opts );\n\t\tif ( result ) {\n\t\t\tif ( isFunction( result.stop ) ) {\n\t\t\t\tjQuery._queueHooks( animation.elem, animation.opts.queue ).stop =\n\t\t\t\t\tresult.stop.bind( result );\n\t\t\t}\n\t\t\treturn result;\n\t\t}\n\t}\n\n\tjQuery.map( props, createTween, animation );\n\n\tif ( isFunction( animation.opts.start ) ) {\n\t\tanimation.opts.start.call( elem, animation );\n\t}\n\n\t// Attach callbacks from options\n\tanimation\n\t\t.progress( animation.opts.progress )\n\t\t.done( animation.opts.done, animation.opts.complete )\n\t\t.fail( animation.opts.fail )\n\t\t.always( animation.opts.always );\n\n\tjQuery.fx.timer(\n\t\tjQuery.extend( tick, {\n\t\t\telem: elem,\n\t\t\tanim: animation,\n\t\t\tqueue: animation.opts.queue\n\t\t} )\n\t);\n\n\treturn animation;\n}\n\njQuery.Animation = jQuery.extend( Animation, {\n\n\ttweeners: {\n\t\t"*": [ function( prop, value ) {\n\t\t\tvar tween = this.createTween( prop, value );\n\t\t\tadjustCSS( tween.elem, prop, rcssNum.exec( value ), tween );\n\t\t\treturn tween;\n\t\t} ]\n\t},\n\n\ttweener: function( props, callback ) {\n\t\tif ( isFunction( props ) ) {\n\t\t\tcallback = props;\n\t\t\tprops = [ "*" ];\n\t\t} else {\n\t\t\tprops = props.match( rnothtmlwhite );\n\t\t}\n\n\t\tvar prop,\n\t\t\tindex = 0,\n\t\t\tlength = props.length;\n\n\t\tfor ( ; index < length; index++ ) {\n\t\t\tprop = props[ index ];\n\t\t\tAnimation.tweeners[ prop ] = Animation.tweeners[ prop ] || [];\n\t\t\tAnimation.tweeners[ prop ].unshift( callback );\n\t\t}\n\t},\n\n\tprefilters: [ defaultPrefilter ],\n\n\tprefilter: function( callback, prepend ) {\n\t\tif ( prepend ) {\n\t\t\tAnimation.prefilters.unshift( callback );\n\t\t} else {\n\t\t\tAnimation.prefilters.push( callback );\n\t\t}\n\t}\n} );\n\njQuery.speed = function( speed, easing, fn ) {\n\tvar opt = speed && typeof speed === "object" ? jQuery.extend( {}, speed ) : {\n\t\tcomplete: fn || !fn && easing ||\n\t\t\tisFunction( speed ) && speed,\n\t\tduration: speed,\n\t\teasing: fn && easing || easing && !isFunction( easing ) && easing\n\t};\n\n\t// Go to the end state if fx are off\n\tif ( jQuery.fx.off ) {\n\t\topt.duration = 0;\n\n\t} else {\n\t\tif ( typeof opt.duration !== "number" ) {\n\t\t\tif ( opt.duration in jQuery.fx.speeds ) {\n\t\t\t\topt.duration = jQuery.fx.speeds[ opt.duration ];\n\n\t\t\t} else {\n\t\t\t\topt.duration = jQuery.fx.speeds._default;\n\t\t\t}\n\t\t}\n\t}\n\n\t// Normalize opt.queue - true/undefined/null -> "fx"\n\tif ( opt.queue == null || opt.queue === true ) {\n\t\topt.queue = "fx";\n\t}\n\n\t// Queueing\n\topt.old = opt.complete;\n\n\topt.complete = function() {\n\t\tif ( isFunction( opt.old ) ) {\n\t\t\topt.old.call( this );\n\t\t}\n\n\t\tif ( opt.queue ) {\n\t\t\tjQuery.dequeue( this, opt.queue );\n\t\t}\n\t};\n\n\treturn opt;\n};\n\njQuery.fn.extend( {\n\tfadeTo: function( speed, to, easing, callback ) {\n\n\t\t// Show any hidden elements after setting opacity to 0\n\t\treturn this.filter( isHiddenWithinTree ).css( "opacity", 0 ).show()\n\n\t\t\t// Animate to the value specified\n\t\t\t.end().animate( { opacity: to }, speed, easing, callback );\n\t},\n\tanimate: function( prop, speed, easing, callback ) {\n\t\tvar empty = jQuery.isEmptyObject( prop ),\n\t\t\toptall = jQuery.speed( speed, easing, callback ),\n\t\t\tdoAnimation = function() {\n\n\t\t\t\t// Operate on a copy of prop so per-property easing won\'t be lost\n\t\t\t\tvar anim = Animation( this, jQuery.extend( {}, prop ), optall );\n\n\t\t\t\t// Empty animations, or finishing resolves immediately\n\t\t\t\tif ( empty || dataPriv.get( this, "finish" ) ) {\n\t\t\t\t\tanim.stop( true );\n\t\t\t\t}\n\t\t\t};\n\t\t\tdoAnimation.finish = doAnimation;\n\n\t\treturn empty || optall.queue === false ?\n\t\t\tthis.each( doAnimation ) :\n\t\t\tthis.queue( optall.queue, doAnimation );\n\t},\n\tstop: function( type, clearQueue, gotoEnd ) {\n\t\tvar stopQueue = function( hooks ) {\n\t\t\tvar stop = hooks.stop;\n\t\t\tdelete hooks.stop;\n\t\t\tstop( gotoEnd );\n\t\t};\n\n\t\tif ( typeof type !== "string" ) {\n\t\t\tgotoEnd = clearQueue;\n\t\t\tclearQueue = type;\n\t\t\ttype = undefined;\n\t\t}\n\t\tif ( clearQueue && type !== false ) {\n\t\t\tthis.queue( type || "fx", [] );\n\t\t}\n\n\t\treturn this.each( function() {\n\t\t\tvar dequeue = true,\n\t\t\t\tindex = type != null && type + "queueHooks",\n\t\t\t\ttimers = jQuery.timers,\n\t\t\t\tdata = dataPriv.get( this );\n\n\t\t\tif ( index ) {\n\t\t\t\tif ( data[ index ] && data[ index ].stop ) {\n\t\t\t\t\tstopQueue( data[ index ] );\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\tfor ( index in data ) {\n\t\t\t\t\tif ( data[ index ] && data[ index ].stop && rrun.test( index ) ) {\n\t\t\t\t\t\tstopQueue( data[ index ] );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\tfor ( index = timers.length; index--; ) {\n\t\t\t\tif ( timers[ index ].elem === this &&\n\t\t\t\t\t( type == null || timers[ index ].queue === type ) ) {\n\n\t\t\t\t\ttimers[ index ].anim.stop( gotoEnd );\n\t\t\t\t\tdequeue = false;\n\t\t\t\t\ttimers.splice( index, 1 );\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Start the next in the queue if the last step wasn\'t forced.\n\t\t\t// Timers currently will call their complete callbacks, which\n\t\t\t// will dequeue but only if they were gotoEnd.\n\t\t\tif ( dequeue || !gotoEnd ) {\n\t\t\t\tjQuery.dequeue( this, type );\n\t\t\t}\n\t\t} );\n\t},\n\tfinish: function( type ) {\n\t\tif ( type !== false ) {\n\t\t\ttype = type || "fx";\n\t\t}\n\t\treturn this.each( function() {\n\t\t\tvar index,\n\t\t\t\tdata = dataPriv.get( this ),\n\t\t\t\tqueue = data[ type + "queue" ],\n\t\t\t\thooks = data[ type + "queueHooks" ],\n\t\t\t\ttimers = jQuery.timers,\n\t\t\t\tlength = queue ? queue.length : 0;\n\n\t\t\t// Enable finishing flag on private data\n\t\t\tdata.finish = true;\n\n\t\t\t// Empty the queue first\n\t\t\tjQuery.queue( this, type, [] );\n\n\t\t\tif ( hooks && hooks.stop ) {\n\t\t\t\thooks.stop.call( this, true );\n\t\t\t}\n\n\t\t\t// Look for any active animations, and finish them\n\t\t\tfor ( index = timers.length; index--; ) {\n\t\t\t\tif ( timers[ index ].elem === this && timers[ index ].queue === type ) {\n\t\t\t\t\ttimers[ index ].anim.stop( true );\n\t\t\t\t\ttimers.splice( index, 1 );\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Look for any animations in the old queue and finish them\n\t\t\tfor ( index = 0; index < length; index++ ) {\n\t\t\t\tif ( queue[ index ] && queue[ index ].finish ) {\n\t\t\t\t\tqueue[ index ].finish.call( this );\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Turn off finishing flag\n\t\t\tdelete data.finish;\n\t\t} );\n\t}\n} );\n\njQuery.each( [ "toggle", "show", "hide" ], function( i, name ) {\n\tvar cssFn = jQuery.fn[ name ];\n\tjQuery.fn[ name ] = function( speed, easing, callback ) {\n\t\treturn speed == null || typeof speed === "boolean" ?\n\t\t\tcssFn.apply( this, arguments ) :\n\t\t\tthis.animate( genFx( name, true ), speed, easing, callback );\n\t};\n} );\n\n// Generate shortcuts for custom animations\njQuery.each( {\n\tslideDown: genFx( "show" ),\n\tslideUp: genFx( "hide" ),\n\tslideToggle: genFx( "toggle" ),\n\tfadeIn: { opacity: "show" },\n\tfadeOut: { opacity: "hide" },\n\tfadeToggle: { opacity: "toggle" }\n}, function( name, props ) {\n\tjQuery.fn[ name ] = function( speed, easing, callback ) {\n\t\treturn this.animate( props, speed, easing, callback );\n\t};\n} );\n\njQuery.timers = [];\njQuery.fx.tick = function() {\n\tvar timer,\n\t\ti = 0,\n\t\ttimers = jQuery.timers;\n\n\tfxNow = Date.now();\n\n\tfor ( ; i < timers.length; i++ ) {\n\t\ttimer = timers[ i ];\n\n\t\t// Run the timer and safely remove it when done (allowing for external removal)\n\t\tif ( !timer() && timers[ i ] === timer ) {\n\t\t\ttimers.splice( i--, 1 );\n\t\t}\n\t}\n\n\tif ( !timers.length ) {\n\t\tjQuery.fx.stop();\n\t}\n\tfxNow = undefined;\n};\n\njQuery.fx.timer = function( timer ) {\n\tjQuery.timers.push( timer );\n\tjQuery.fx.start();\n};\n\njQuery.fx.interval = 13;\njQuery.fx.start = function() {\n\tif ( inProgress ) {\n\t\treturn;\n\t}\n\n\tinProgress = true;\n\tschedule();\n};\n\njQuery.fx.stop = function() {\n\tinProgress = null;\n};\n\njQuery.fx.speeds = {\n\tslow: 600,\n\tfast: 200,\n\n\t// Default speed\n\t_default: 400\n};\n\n\n// Based off of the plugin by Clint Helfers, with permission.\n// https://web.archive.org/web/20100324014747/http://blindsignals.com/index.php/2009/07/jquery-delay/\njQuery.fn.delay = function( time, type ) {\n\ttime = jQuery.fx ? jQuery.fx.speeds[ time ] || time : time;\n\ttype = type || "fx";\n\n\treturn this.queue( type, function( next, hooks ) {\n\t\tvar timeout = window.setTimeout( next, time );\n\t\thooks.stop = function() {\n\t\t\twindow.clearTimeout( timeout );\n\t\t};\n\t} );\n};\n\n\n( function() {\n\tvar input = document.createElement( "input" ),\n\t\tselect = document.createElement( "select" ),\n\t\topt = select.appendChild( document.createElement( "option" ) );\n\n\tinput.type = "checkbox";\n\n\t// Support: Android <=4.3 only\n\t// Default value for a checkbox should be "on"\n\tsupport.checkOn = input.value !== "";\n\n\t// Support: IE <=11 only\n\t// Must access selectedIndex to make default options select\n\tsupport.optSelected = opt.selected;\n\n\t// Support: IE <=11 only\n\t// An input loses its value after becoming a radio\n\tinput = document.createElement( "input" );\n\tinput.value = "t";\n\tinput.type = "radio";\n\tsupport.radioValue = input.value === "t";\n} )();\n\n\nvar boolHook,\n\tattrHandle = jQuery.expr.attrHandle;\n\njQuery.fn.extend( {\n\tattr: function( name, value ) {\n\t\treturn access( this, jQuery.attr, name, value, arguments.length > 1 );\n\t},\n\n\tremoveAttr: function( name ) {\n\t\treturn this.each( function() {\n\t\t\tjQuery.removeAttr( this, name );\n\t\t} );\n\t}\n} );\n\njQuery.extend( {\n\tattr: function( elem, name, value ) {\n\t\tvar ret, hooks,\n\t\t\tnType = elem.nodeType;\n\n\t\t// Don\'t get/set attributes on text, comment and attribute nodes\n\t\tif ( nType === 3 || nType === 8 || nType === 2 ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Fallback to prop when attributes are not supported\n\t\tif ( typeof elem.getAttribute === "undefined" ) {\n\t\t\treturn jQuery.prop( elem, name, value );\n\t\t}\n\n\t\t// Attribute hooks are determined by the lowercase version\n\t\t// Grab necessary hook if one is defined\n\t\tif ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {\n\t\t\thooks = jQuery.attrHooks[ name.toLowerCase() ] ||\n\t\t\t\t( jQuery.expr.match.bool.test( name ) ? boolHook : undefined );\n\t\t}\n\n\t\tif ( value !== undefined ) {\n\t\t\tif ( value === null ) {\n\t\t\t\tjQuery.removeAttr( elem, name );\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\tif ( hooks && "set" in hooks &&\n\t\t\t\t( ret = hooks.set( elem, value, name ) ) !== undefined ) {\n\t\t\t\treturn ret;\n\t\t\t}\n\n\t\t\telem.setAttribute( name, value + "" );\n\t\t\treturn value;\n\t\t}\n\n\t\tif ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {\n\t\t\treturn ret;\n\t\t}\n\n\t\tret = jQuery.find.attr( elem, name );\n\n\t\t// Non-existent attributes return null, we normalize to undefined\n\t\treturn ret == null ? undefined : ret;\n\t},\n\n\tattrHooks: {\n\t\ttype: {\n\t\t\tset: function( elem, value ) {\n\t\t\t\tif ( !support.radioValue && value === "radio" &&\n\t\t\t\t\tnodeName( elem, "input" ) ) {\n\t\t\t\t\tvar val = elem.value;\n\t\t\t\t\telem.setAttribute( "type", value );\n\t\t\t\t\tif ( val ) {\n\t\t\t\t\t\telem.value = val;\n\t\t\t\t\t}\n\t\t\t\t\treturn value;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t},\n\n\tremoveAttr: function( elem, value ) {\n\t\tvar name,\n\t\t\ti = 0,\n\n\t\t\t// Attribute names can contain non-HTML whitespace characters\n\t\t\t// https://html.spec.whatwg.org/multipage/syntax.html#attributes-2\n\t\t\tattrNames = value && value.match( rnothtmlwhite );\n\n\t\tif ( attrNames && elem.nodeType === 1 ) {\n\t\t\twhile ( ( name = attrNames[ i++ ] ) ) {\n\t\t\t\telem.removeAttribute( name );\n\t\t\t}\n\t\t}\n\t}\n} );\n\n// Hooks for boolean attributes\nboolHook = {\n\tset: function( elem, value, name ) {\n\t\tif ( value === false ) {\n\n\t\t\t// Remove boolean attributes when set to false\n\t\t\tjQuery.removeAttr( elem, name );\n\t\t} else {\n\t\t\telem.setAttribute( name, name );\n\t\t}\n\t\treturn name;\n\t}\n};\n\njQuery.each( jQuery.expr.match.bool.source.match( /\\w+/g ), function( i, name ) {\n\tvar getter = attrHandle[ name ] || jQuery.find.attr;\n\n\tattrHandle[ name ] = function( elem, name, isXML ) {\n\t\tvar ret, handle,\n\t\t\tlowercaseName = name.toLowerCase();\n\n\t\tif ( !isXML ) {\n\n\t\t\t// Avoid an infinite loop by temporarily removing this function from the getter\n\t\t\thandle = attrHandle[ lowercaseName ];\n\t\t\tattrHandle[ lowercaseName ] = ret;\n\t\t\tret = getter( elem, name, isXML ) != null ?\n\t\t\t\tlowercaseName :\n\t\t\t\tnull;\n\t\t\tattrHandle[ lowercaseName ] = handle;\n\t\t}\n\t\treturn ret;\n\t};\n} );\n\n\n\n\nvar rfocusable = /^(?:input|select|textarea|button)$/i,\n\trclickable = /^(?:a|area)$/i;\n\njQuery.fn.extend( {\n\tprop: function( name, value ) {\n\t\treturn access( this, jQuery.prop, name, value, arguments.length > 1 );\n\t},\n\n\tremoveProp: function( name ) {\n\t\treturn this.each( function() {\n\t\t\tdelete this[ jQuery.propFix[ name ] || name ];\n\t\t} );\n\t}\n} );\n\njQuery.extend( {\n\tprop: function( elem, name, value ) {\n\t\tvar ret, hooks,\n\t\t\tnType = elem.nodeType;\n\n\t\t// Don\'t get/set properties on text, comment and attribute nodes\n\t\tif ( nType === 3 || nType === 8 || nType === 2 ) {\n\t\t\treturn;\n\t\t}\n\n\t\tif ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {\n\n\t\t\t// Fix name and attach hooks\n\t\t\tname = jQuery.propFix[ name ] || name;\n\t\t\thooks = jQuery.propHooks[ name ];\n\t\t}\n\n\t\tif ( value !== undefined ) {\n\t\t\tif ( hooks && "set" in hooks &&\n\t\t\t\t( ret = hooks.set( elem, value, name ) ) !== undefined ) {\n\t\t\t\treturn ret;\n\t\t\t}\n\n\t\t\treturn ( elem[ name ] = value );\n\t\t}\n\n\t\tif ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {\n\t\t\treturn ret;\n\t\t}\n\n\t\treturn elem[ name ];\n\t},\n\n\tpropHooks: {\n\t\ttabIndex: {\n\t\t\tget: function( elem ) {\n\n\t\t\t\t// Support: IE <=9 - 11 only\n\t\t\t\t// elem.tabIndex doesn\'t always return the\n\t\t\t\t// correct value when it hasn\'t been explicitly set\n\t\t\t\t// https://web.archive.org/web/20141116233347/http://fluidproject.org/blog/2008/01/09/getting-setting-and-removing-tabindex-values-with-javascript/\n\t\t\t\t// Use proper attribute retrieval(#12072)\n\t\t\t\tvar tabindex = jQuery.find.attr( elem, "tabindex" );\n\n\t\t\t\tif ( tabindex ) {\n\t\t\t\t\treturn parseInt( tabindex, 10 );\n\t\t\t\t}\n\n\t\t\t\tif (\n\t\t\t\t\trfocusable.test( elem.nodeName ) ||\n\t\t\t\t\trclickable.test( elem.nodeName ) &&\n\t\t\t\t\telem.href\n\t\t\t\t) {\n\t\t\t\t\treturn 0;\n\t\t\t\t}\n\n\t\t\t\treturn -1;\n\t\t\t}\n\t\t}\n\t},\n\n\tpropFix: {\n\t\t"for": "htmlFor",\n\t\t"class": "className"\n\t}\n} );\n\n// Support: IE <=11 only\n// Accessing the selectedIndex property\n// forces the browser to respect setting selected\n// on the option\n// The getter ensures a default option is selected\n// when in an optgroup\n// eslint rule "no-unused-expressions" is disabled for this code\n// since it considers such accessions noop\nif ( !support.optSelected ) {\n\tjQuery.propHooks.selected = {\n\t\tget: function( elem ) {\n\n\t\t\t/* eslint no-unused-expressions: "off" */\n\n\t\t\tvar parent = elem.parentNode;\n\t\t\tif ( parent && parent.parentNode ) {\n\t\t\t\tparent.parentNode.selectedIndex;\n\t\t\t}\n\t\t\treturn null;\n\t\t},\n\t\tset: function( elem ) {\n\n\t\t\t/* eslint no-unused-expressions: "off" */\n\n\t\t\tvar parent = elem.parentNode;\n\t\t\tif ( parent ) {\n\t\t\t\tparent.selectedIndex;\n\n\t\t\t\tif ( parent.parentNode ) {\n\t\t\t\t\tparent.parentNode.selectedIndex;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t};\n}\n\njQuery.each( [\n\t"tabIndex",\n\t"readOnly",\n\t"maxLength",\n\t"cellSpacing",\n\t"cellPadding",\n\t"rowSpan",\n\t"colSpan",\n\t"useMap",\n\t"frameBorder",\n\t"contentEditable"\n], function() {\n\tjQuery.propFix[ this.toLowerCase() ] = this;\n} );\n\n\n\n\n\t// Strip and collapse whitespace according to HTML spec\n\t// https://infra.spec.whatwg.org/#strip-and-collapse-ascii-whitespace\n\tfunction stripAndCollapse( value ) {\n\t\tvar tokens = value.match( rnothtmlwhite ) || [];\n\t\treturn tokens.join( " " );\n\t}\n\n\nfunction getClass( elem ) {\n\treturn elem.getAttribute && elem.getAttribute( "class" ) || "";\n}\n\nfunction classesToArray( value ) {\n\tif ( Array.isArray( value ) ) {\n\t\treturn value;\n\t}\n\tif ( typeof value === "string" ) {\n\t\treturn value.match( rnothtmlwhite ) || [];\n\t}\n\treturn [];\n}\n\njQuery.fn.extend( {\n\taddClass: function( value ) {\n\t\tvar classes, elem, cur, curValue, clazz, j, finalValue,\n\t\t\ti = 0;\n\n\t\tif ( isFunction( value ) ) {\n\t\t\treturn this.each( function( j ) {\n\t\t\t\tjQuery( this ).addClass( value.call( this, j, getClass( this ) ) );\n\t\t\t} );\n\t\t}\n\n\t\tclasses = classesToArray( value );\n\n\t\tif ( classes.length ) {\n\t\t\twhile ( ( elem = this[ i++ ] ) ) {\n\t\t\t\tcurValue = getClass( elem );\n\t\t\t\tcur = elem.nodeType === 1 && ( " " + stripAndCollapse( curValue ) + " " );\n\n\t\t\t\tif ( cur ) {\n\t\t\t\t\tj = 0;\n\t\t\t\t\twhile ( ( clazz = classes[ j++ ] ) ) {\n\t\t\t\t\t\tif ( cur.indexOf( " " + clazz + " " ) < 0 ) {\n\t\t\t\t\t\t\tcur += clazz + " ";\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\t// Only assign if different to avoid unneeded rendering.\n\t\t\t\t\tfinalValue = stripAndCollapse( cur );\n\t\t\t\t\tif ( curValue !== finalValue ) {\n\t\t\t\t\t\telem.setAttribute( "class", finalValue );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn this;\n\t},\n\n\tremoveClass: function( value ) {\n\t\tvar classes, elem, cur, curValue, clazz, j, finalValue,\n\t\t\ti = 0;\n\n\t\tif ( isFunction( value ) ) {\n\t\t\treturn this.each( function( j ) {\n\t\t\t\tjQuery( this ).removeClass( value.call( this, j, getClass( this ) ) );\n\t\t\t} );\n\t\t}\n\n\t\tif ( !arguments.length ) {\n\t\t\treturn this.attr( "class", "" );\n\t\t}\n\n\t\tclasses = classesToArray( value );\n\n\t\tif ( classes.length ) {\n\t\t\twhile ( ( elem = this[ i++ ] ) ) {\n\t\t\t\tcurValue = getClass( elem );\n\n\t\t\t\t// This expression is here for better compressibility (see addClass)\n\t\t\t\tcur = elem.nodeType === 1 && ( " " + stripAndCollapse( curValue ) + " " );\n\n\t\t\t\tif ( cur ) {\n\t\t\t\t\tj = 0;\n\t\t\t\t\twhile ( ( clazz = classes[ j++ ] ) ) {\n\n\t\t\t\t\t\t// Remove *all* instances\n\t\t\t\t\t\twhile ( cur.indexOf( " " + clazz + " " ) > -1 ) {\n\t\t\t\t\t\t\tcur = cur.replace( " " + clazz + " ", " " );\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\n\t\t\t\t\t// Only assign if different to avoid unneeded rendering.\n\t\t\t\t\tfinalValue = stripAndCollapse( cur );\n\t\t\t\t\tif ( curValue !== finalValue ) {\n\t\t\t\t\t\telem.setAttribute( "class", finalValue );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn this;\n\t},\n\n\ttoggleClass: function( value, stateVal ) {\n\t\tvar type = typeof value,\n\t\t\tisValidValue = type === "string" || Array.isArray( value );\n\n\t\tif ( typeof stateVal === "boolean" && isValidValue ) {\n\t\t\treturn stateVal ? this.addClass( value ) : this.removeClass( value );\n\t\t}\n\n\t\tif ( isFunction( value ) ) {\n\t\t\treturn this.each( function( i ) {\n\t\t\t\tjQuery( this ).toggleClass(\n\t\t\t\t\tvalue.call( this, i, getClass( this ), stateVal ),\n\t\t\t\t\tstateVal\n\t\t\t\t);\n\t\t\t} );\n\t\t}\n\n\t\treturn this.each( function() {\n\t\t\tvar className, i, self, classNames;\n\n\t\t\tif ( isValidValue ) {\n\n\t\t\t\t// Toggle individual class names\n\t\t\t\ti = 0;\n\t\t\t\tself = jQuery( this );\n\t\t\t\tclassNames = classesToArray( value );\n\n\t\t\t\twhile ( ( className = classNames[ i++ ] ) ) {\n\n\t\t\t\t\t// Check each className given, space separated list\n\t\t\t\t\tif ( self.hasClass( className ) ) {\n\t\t\t\t\t\tself.removeClass( className );\n\t\t\t\t\t} else {\n\t\t\t\t\t\tself.addClass( className );\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t// Toggle whole class name\n\t\t\t} else if ( value === undefined || type === "boolean" ) {\n\t\t\t\tclassName = getClass( this );\n\t\t\t\tif ( className ) {\n\n\t\t\t\t\t// Store className if set\n\t\t\t\t\tdataPriv.set( this, "__className__", className );\n\t\t\t\t}\n\n\t\t\t\t// If the element has a class name or if we\'re passed `false`,\n\t\t\t\t// then remove the whole classname (if there was one, the above saved it).\n\t\t\t\t// Otherwise bring back whatever was previously saved (if anything),\n\t\t\t\t// falling back to the empty string if nothing was stored.\n\t\t\t\tif ( this.setAttribute ) {\n\t\t\t\t\tthis.setAttribute( "class",\n\t\t\t\t\t\tclassName || value === false ?\n\t\t\t\t\t\t"" :\n\t\t\t\t\t\tdataPriv.get( this, "__className__" ) || ""\n\t\t\t\t\t);\n\t\t\t\t}\n\t\t\t}\n\t\t} );\n\t},\n\n\thasClass: function( selector ) {\n\t\tvar className, elem,\n\t\t\ti = 0;\n\n\t\tclassName = " " + selector + " ";\n\t\twhile ( ( elem = this[ i++ ] ) ) {\n\t\t\tif ( elem.nodeType === 1 &&\n\t\t\t\t( " " + stripAndCollapse( getClass( elem ) ) + " " ).indexOf( className ) > -1 ) {\n\t\t\t\t\treturn true;\n\t\t\t}\n\t\t}\n\n\t\treturn false;\n\t}\n} );\n\n\n\n\nvar rreturn = /\\r/g;\n\njQuery.fn.extend( {\n\tval: function( value ) {\n\t\tvar hooks, ret, valueIsFunction,\n\t\t\telem = this[ 0 ];\n\n\t\tif ( !arguments.length ) {\n\t\t\tif ( elem ) {\n\t\t\t\thooks = jQuery.valHooks[ elem.type ] ||\n\t\t\t\t\tjQuery.valHooks[ elem.nodeName.toLowerCase() ];\n\n\t\t\t\tif ( hooks &&\n\t\t\t\t\t"get" in hooks &&\n\t\t\t\t\t( ret = hooks.get( elem, "value" ) ) !== undefined\n\t\t\t\t) {\n\t\t\t\t\treturn ret;\n\t\t\t\t}\n\n\t\t\t\tret = elem.value;\n\n\t\t\t\t// Handle most common string cases\n\t\t\t\tif ( typeof ret === "string" ) {\n\t\t\t\t\treturn ret.replace( rreturn, "" );\n\t\t\t\t}\n\n\t\t\t\t// Handle cases where value is null/undef or number\n\t\t\t\treturn ret == null ? "" : ret;\n\t\t\t}\n\n\t\t\treturn;\n\t\t}\n\n\t\tvalueIsFunction = isFunction( value );\n\n\t\treturn this.each( function( i ) {\n\t\t\tvar val;\n\n\t\t\tif ( this.nodeType !== 1 ) {\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\tif ( valueIsFunction ) {\n\t\t\t\tval = value.call( this, i, jQuery( this ).val() );\n\t\t\t} else {\n\t\t\t\tval = value;\n\t\t\t}\n\n\t\t\t// Treat null/undefined as ""; convert numbers to string\n\t\t\tif ( val == null ) {\n\t\t\t\tval = "";\n\n\t\t\t} else if ( typeof val === "number" ) {\n\t\t\t\tval += "";\n\n\t\t\t} else if ( Array.isArray( val ) ) {\n\t\t\t\tval = jQuery.map( val, function( value ) {\n\t\t\t\t\treturn value == null ? "" : value + "";\n\t\t\t\t} );\n\t\t\t}\n\n\t\t\thooks = jQuery.valHooks[ this.type ] || jQuery.valHooks[ this.nodeName.toLowerCase() ];\n\n\t\t\t// If set returns undefined, fall back to normal setting\n\t\t\tif ( !hooks || !( "set" in hooks ) || hooks.set( this, val, "value" ) === undefined ) {\n\t\t\t\tthis.value = val;\n\t\t\t}\n\t\t} );\n\t}\n} );\n\njQuery.extend( {\n\tvalHooks: {\n\t\toption: {\n\t\t\tget: function( elem ) {\n\n\t\t\t\tvar val = jQuery.find.attr( elem, "value" );\n\t\t\t\treturn val != null ?\n\t\t\t\t\tval :\n\n\t\t\t\t\t// Support: IE <=10 - 11 only\n\t\t\t\t\t// option.text throws exceptions (#14686, #14858)\n\t\t\t\t\t// Strip and collapse whitespace\n\t\t\t\t\t// https://html.spec.whatwg.org/#strip-and-collapse-whitespace\n\t\t\t\t\tstripAndCollapse( jQuery.text( elem ) );\n\t\t\t}\n\t\t},\n\t\tselect: {\n\t\t\tget: function( elem ) {\n\t\t\t\tvar value, option, i,\n\t\t\t\t\toptions = elem.options,\n\t\t\t\t\tindex = elem.selectedIndex,\n\t\t\t\t\tone = elem.type === "select-one",\n\t\t\t\t\tvalues = one ? null : [],\n\t\t\t\t\tmax = one ? index + 1 : options.length;\n\n\t\t\t\tif ( index < 0 ) {\n\t\t\t\t\ti = max;\n\n\t\t\t\t} else {\n\t\t\t\t\ti = one ? index : 0;\n\t\t\t\t}\n\n\t\t\t\t// Loop through all the selected options\n\t\t\t\tfor ( ; i < max; i++ ) {\n\t\t\t\t\toption = options[ i ];\n\n\t\t\t\t\t// Support: IE <=9 only\n\t\t\t\t\t// IE8-9 doesn\'t update selected after form reset (#2551)\n\t\t\t\t\tif ( ( option.selected || i === index ) &&\n\n\t\t\t\t\t\t\t// Don\'t return options that are disabled or in a disabled optgroup\n\t\t\t\t\t\t\t!option.disabled &&\n\t\t\t\t\t\t\t( !option.parentNode.disabled ||\n\t\t\t\t\t\t\t\t!nodeName( option.parentNode, "optgroup" ) ) ) {\n\n\t\t\t\t\t\t// Get the specific value for the option\n\t\t\t\t\t\tvalue = jQuery( option ).val();\n\n\t\t\t\t\t\t// We don\'t need an array for one selects\n\t\t\t\t\t\tif ( one ) {\n\t\t\t\t\t\t\treturn value;\n\t\t\t\t\t\t}\n\n\t\t\t\t\t\t// Multi-Selects return an array\n\t\t\t\t\t\tvalues.push( value );\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\treturn values;\n\t\t\t},\n\n\t\t\tset: function( elem, value ) {\n\t\t\t\tvar optionSet, option,\n\t\t\t\t\toptions = elem.options,\n\t\t\t\t\tvalues = jQuery.makeArray( value ),\n\t\t\t\t\ti = options.length;\n\n\t\t\t\twhile ( i-- ) {\n\t\t\t\t\toption = options[ i ];\n\n\t\t\t\t\t/* eslint-disable no-cond-assign */\n\n\t\t\t\t\tif ( option.selected =\n\t\t\t\t\t\tjQuery.inArray( jQuery.valHooks.option.get( option ), values ) > -1\n\t\t\t\t\t) {\n\t\t\t\t\t\toptionSet = true;\n\t\t\t\t\t}\n\n\t\t\t\t\t/* eslint-enable no-cond-assign */\n\t\t\t\t}\n\n\t\t\t\t// Force browsers to behave consistently when non-matching value is set\n\t\t\t\tif ( !optionSet ) {\n\t\t\t\t\telem.selectedIndex = -1;\n\t\t\t\t}\n\t\t\t\treturn values;\n\t\t\t}\n\t\t}\n\t}\n} );\n\n// Radios and checkboxes getter/setter\njQuery.each( [ "radio", "checkbox" ], function() {\n\tjQuery.valHooks[ this ] = {\n\t\tset: function( elem, value ) {\n\t\t\tif ( Array.isArray( value ) ) {\n\t\t\t\treturn ( elem.checked = jQuery.inArray( jQuery( elem ).val(), value ) > -1 );\n\t\t\t}\n\t\t}\n\t};\n\tif ( !support.checkOn ) {\n\t\tjQuery.valHooks[ this ].get = function( elem ) {\n\t\t\treturn elem.getAttribute( "value" ) === null ? "on" : elem.value;\n\t\t};\n\t}\n} );\n\n\n\n\n// Return jQuery for attributes-only inclusion\n\n\nsupport.focusin = "onfocusin" in window;\n\n\nvar rfocusMorph = /^(?:focusinfocus|focusoutblur)$/,\n\tstopPropagationCallback = function( e ) {\n\t\te.stopPropagation();\n\t};\n\njQuery.extend( jQuery.event, {\n\n\ttrigger: function( event, data, elem, onlyHandlers ) {\n\n\t\tvar i, cur, tmp, bubbleType, ontype, handle, special, lastElement,\n\t\t\teventPath = [ elem || document ],\n\t\t\ttype = hasOwn.call( event, "type" ) ? event.type : event,\n\t\t\tnamespaces = hasOwn.call( event, "namespace" ) ? event.namespace.split( "." ) : [];\n\n\t\tcur = lastElement = tmp = elem = elem || document;\n\n\t\t// Don\'t do events on text and comment nodes\n\t\tif ( elem.nodeType === 3 || elem.nodeType === 8 ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// focus/blur morphs to focusin/out; ensure we\'re not firing them right now\n\t\tif ( rfocusMorph.test( type + jQuery.event.triggered ) ) {\n\t\t\treturn;\n\t\t}\n\n\t\tif ( type.indexOf( "." ) > -1 ) {\n\n\t\t\t// Namespaced trigger; create a regexp to match event type in handle()\n\t\t\tnamespaces = type.split( "." );\n\t\t\ttype = namespaces.shift();\n\t\t\tnamespaces.sort();\n\t\t}\n\t\tontype = type.indexOf( ":" ) < 0 && "on" + type;\n\n\t\t// Caller can pass in a jQuery.Event object, Object, or just an event type string\n\t\tevent = event[ jQuery.expando ] ?\n\t\t\tevent :\n\t\t\tnew jQuery.Event( type, typeof event === "object" && event );\n\n\t\t// Trigger bitmask: & 1 for native handlers; & 2 for jQuery (always true)\n\t\tevent.isTrigger = onlyHandlers ? 2 : 3;\n\t\tevent.namespace = namespaces.join( "." );\n\t\tevent.rnamespace = event.namespace ?\n\t\t\tnew RegExp( "(^|\\\\.)" + namespaces.join( "\\\\.(?:.*\\\\.|)" ) + "(\\\\.|$)" ) :\n\t\t\tnull;\n\n\t\t// Clean up the event in case it is being reused\n\t\tevent.result = undefined;\n\t\tif ( !event.target ) {\n\t\t\tevent.target = elem;\n\t\t}\n\n\t\t// Clone any incoming data and prepend the event, creating the handler arg list\n\t\tdata = data == null ?\n\t\t\t[ event ] :\n\t\t\tjQuery.makeArray( data, [ event ] );\n\n\t\t// Allow special events to draw outside the lines\n\t\tspecial = jQuery.event.special[ type ] || {};\n\t\tif ( !onlyHandlers && special.trigger && special.trigger.apply( elem, data ) === false ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Determine event propagation path in advance, per W3C events spec (#9951)\n\t\t// Bubble up to document, then to window; watch for a global ownerDocument var (#9724)\n\t\tif ( !onlyHandlers && !special.noBubble && !isWindow( elem ) ) {\n\n\t\t\tbubbleType = special.delegateType || type;\n\t\t\tif ( !rfocusMorph.test( bubbleType + type ) ) {\n\t\t\t\tcur = cur.parentNode;\n\t\t\t}\n\t\t\tfor ( ; cur; cur = cur.parentNode ) {\n\t\t\t\teventPath.push( cur );\n\t\t\t\ttmp = cur;\n\t\t\t}\n\n\t\t\t// Only add window if we got to document (e.g., not plain obj or detached DOM)\n\t\t\tif ( tmp === ( elem.ownerDocument || document ) ) {\n\t\t\t\teventPath.push( tmp.defaultView || tmp.parentWindow || window );\n\t\t\t}\n\t\t}\n\n\t\t// Fire handlers on the event path\n\t\ti = 0;\n\t\twhile ( ( cur = eventPath[ i++ ] ) && !event.isPropagationStopped() ) {\n\t\t\tlastElement = cur;\n\t\t\tevent.type = i > 1 ?\n\t\t\t\tbubbleType :\n\t\t\t\tspecial.bindType || type;\n\n\t\t\t// jQuery handler\n\t\t\thandle = ( dataPriv.get( cur, "events" ) || {} )[ event.type ] &&\n\t\t\t\tdataPriv.get( cur, "handle" );\n\t\t\tif ( handle ) {\n\t\t\t\thandle.apply( cur, data );\n\t\t\t}\n\n\t\t\t// Native handler\n\t\t\thandle = ontype && cur[ ontype ];\n\t\t\tif ( handle && handle.apply && acceptData( cur ) ) {\n\t\t\t\tevent.result = handle.apply( cur, data );\n\t\t\t\tif ( event.result === false ) {\n\t\t\t\t\tevent.preventDefault();\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t\tevent.type = type;\n\n\t\t// If nobody prevented the default action, do it now\n\t\tif ( !onlyHandlers && !event.isDefaultPrevented() ) {\n\n\t\t\tif ( ( !special._default ||\n\t\t\t\tspecial._default.apply( eventPath.pop(), data ) === false ) &&\n\t\t\t\tacceptData( elem ) ) {\n\n\t\t\t\t// Call a native DOM method on the target with the same name as the event.\n\t\t\t\t// Don\'t do default actions on window, that\'s where global variables be (#6170)\n\t\t\t\tif ( ontype && isFunction( elem[ type ] ) && !isWindow( elem ) ) {\n\n\t\t\t\t\t// Don\'t re-trigger an onFOO event when we call its FOO() method\n\t\t\t\t\ttmp = elem[ ontype ];\n\n\t\t\t\t\tif ( tmp ) {\n\t\t\t\t\t\telem[ ontype ] = null;\n\t\t\t\t\t}\n\n\t\t\t\t\t// Prevent re-triggering of the same event, since we already bubbled it above\n\t\t\t\t\tjQuery.event.triggered = type;\n\n\t\t\t\t\tif ( event.isPropagationStopped() ) {\n\t\t\t\t\t\tlastElement.addEventListener( type, stopPropagationCallback );\n\t\t\t\t\t}\n\n\t\t\t\t\telem[ type ]();\n\n\t\t\t\t\tif ( event.isPropagationStopped() ) {\n\t\t\t\t\t\tlastElement.removeEventListener( type, stopPropagationCallback );\n\t\t\t\t\t}\n\n\t\t\t\t\tjQuery.event.triggered = undefined;\n\n\t\t\t\t\tif ( tmp ) {\n\t\t\t\t\t\telem[ ontype ] = tmp;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn event.result;\n\t},\n\n\t// Piggyback on a donor event to simulate a different one\n\t// Used only for `focus(in | out)` events\n\tsimulate: function( type, elem, event ) {\n\t\tvar e = jQuery.extend(\n\t\t\tnew jQuery.Event(),\n\t\t\tevent,\n\t\t\t{\n\t\t\t\ttype: type,\n\t\t\t\tisSimulated: true\n\t\t\t}\n\t\t);\n\n\t\tjQuery.event.trigger( e, null, elem );\n\t}\n\n} );\n\njQuery.fn.extend( {\n\n\ttrigger: function( type, data ) {\n\t\treturn this.each( function() {\n\t\t\tjQuery.event.trigger( type, data, this );\n\t\t} );\n\t},\n\ttriggerHandler: function( type, data ) {\n\t\tvar elem = this[ 0 ];\n\t\tif ( elem ) {\n\t\t\treturn jQuery.event.trigger( type, data, elem, true );\n\t\t}\n\t}\n} );\n\n\n// Support: Firefox <=44\n// Firefox doesn\'t have focus(in | out) events\n// Related ticket - https://bugzilla.mozilla.org/show_bug.cgi?id=687787\n//\n// Support: Chrome <=48 - 49, Safari <=9.0 - 9.1\n// focus(in | out) events fire after focus & blur events,\n// which is spec violation - http://www.w3.org/TR/DOM-Level-3-Events/#events-focusevent-event-order\n// Related ticket - https://bugs.chromium.org/p/chromium/issues/detail?id=449857\nif ( !support.focusin ) {\n\tjQuery.each( { focus: "focusin", blur: "focusout" }, function( orig, fix ) {\n\n\t\t// Attach a single capturing handler on the document while someone wants focusin/focusout\n\t\tvar handler = function( event ) {\n\t\t\tjQuery.event.simulate( fix, event.target, jQuery.event.fix( event ) );\n\t\t};\n\n\t\tjQuery.event.special[ fix ] = {\n\t\t\tsetup: function() {\n\t\t\t\tvar doc = this.ownerDocument || this,\n\t\t\t\t\tattaches = dataPriv.access( doc, fix );\n\n\t\t\t\tif ( !attaches ) {\n\t\t\t\t\tdoc.addEventListener( orig, handler, true );\n\t\t\t\t}\n\t\t\t\tdataPriv.access( doc, fix, ( attaches || 0 ) + 1 );\n\t\t\t},\n\t\t\tteardown: function() {\n\t\t\t\tvar doc = this.ownerDocument || this,\n\t\t\t\t\tattaches = dataPriv.access( doc, fix ) - 1;\n\n\t\t\t\tif ( !attaches ) {\n\t\t\t\t\tdoc.removeEventListener( orig, handler, true );\n\t\t\t\t\tdataPriv.remove( doc, fix );\n\n\t\t\t\t} else {\n\t\t\t\t\tdataPriv.access( doc, fix, attaches );\n\t\t\t\t}\n\t\t\t}\n\t\t};\n\t} );\n}\nvar location = window.location;\n\nvar nonce = Date.now();\n\nvar rquery = ( /\\?/ );\n\n\n\n// Cross-browser xml parsing\njQuery.parseXML = function( data ) {\n\tvar xml;\n\tif ( !data || typeof data !== "string" ) {\n\t\treturn null;\n\t}\n\n\t// Support: IE 9 - 11 only\n\t// IE throws on parseFromString with invalid input.\n\ttry {\n\t\txml = ( new window.DOMParser() ).parseFromString( data, "text/xml" );\n\t} catch ( e ) {\n\t\txml = undefined;\n\t}\n\n\tif ( !xml || xml.getElementsByTagName( "parsererror" ).length ) {\n\t\tjQuery.error( "Invalid XML: " + data );\n\t}\n\treturn xml;\n};\n\n\nvar\n\trbracket = /\\[\\]$/,\n\trCRLF = /\\r?\\n/g,\n\trsubmitterTypes = /^(?:submit|button|image|reset|file)$/i,\n\trsubmittable = /^(?:input|select|textarea|keygen)/i;\n\nfunction buildParams( prefix, obj, traditional, add ) {\n\tvar name;\n\n\tif ( Array.isArray( obj ) ) {\n\n\t\t// Serialize array item.\n\t\tjQuery.each( obj, function( i, v ) {\n\t\t\tif ( traditional || rbracket.test( prefix ) ) {\n\n\t\t\t\t// Treat each array item as a scalar.\n\t\t\t\tadd( prefix, v );\n\n\t\t\t} else {\n\n\t\t\t\t// Item is non-scalar (array or object), encode its numeric index.\n\t\t\t\tbuildParams(\n\t\t\t\t\tprefix + "[" + ( typeof v === "object" && v != null ? i : "" ) + "]",\n\t\t\t\t\tv,\n\t\t\t\t\ttraditional,\n\t\t\t\t\tadd\n\t\t\t\t);\n\t\t\t}\n\t\t} );\n\n\t} else if ( !traditional && toType( obj ) === "object" ) {\n\n\t\t// Serialize object item.\n\t\tfor ( name in obj ) {\n\t\t\tbuildParams( prefix + "[" + name + "]", obj[ name ], traditional, add );\n\t\t}\n\n\t} else {\n\n\t\t// Serialize scalar item.\n\t\tadd( prefix, obj );\n\t}\n}\n\n// Serialize an array of form elements or a set of\n// key/values into a query string\njQuery.param = function( a, traditional ) {\n\tvar prefix,\n\t\ts = [],\n\t\tadd = function( key, valueOrFunction ) {\n\n\t\t\t// If value is a function, invoke it and use its return value\n\t\t\tvar value = isFunction( valueOrFunction ) ?\n\t\t\t\tvalueOrFunction() :\n\t\t\t\tvalueOrFunction;\n\n\t\t\ts[ s.length ] = encodeURIComponent( key ) + "=" +\n\t\t\t\tencodeURIComponent( value == null ? "" : value );\n\t\t};\n\n\tif ( a == null ) {\n\t\treturn "";\n\t}\n\n\t// If an array was passed in, assume that it is an array of form elements.\n\tif ( Array.isArray( a ) || ( a.jquery && !jQuery.isPlainObject( a ) ) ) {\n\n\t\t// Serialize the form elements\n\t\tjQuery.each( a, function() {\n\t\t\tadd( this.name, this.value );\n\t\t} );\n\n\t} else {\n\n\t\t// If traditional, encode the "old" way (the way 1.3.2 or older\n\t\t// did it), otherwise encode params recursively.\n\t\tfor ( prefix in a ) {\n\t\t\tbuildParams( prefix, a[ prefix ], traditional, add );\n\t\t}\n\t}\n\n\t// Return the resulting serialization\n\treturn s.join( "&" );\n};\n\njQuery.fn.extend( {\n\tserialize: function() {\n\t\treturn jQuery.param( this.serializeArray() );\n\t},\n\tserializeArray: function() {\n\t\treturn this.map( function() {\n\n\t\t\t// Can add propHook for "elements" to filter or add form elements\n\t\t\tvar elements = jQuery.prop( this, "elements" );\n\t\t\treturn elements ? jQuery.makeArray( elements ) : this;\n\t\t} )\n\t\t.filter( function() {\n\t\t\tvar type = this.type;\n\n\t\t\t// Use .is( ":disabled" ) so that fieldset[disabled] works\n\t\t\treturn this.name && !jQuery( this ).is( ":disabled" ) &&\n\t\t\t\trsubmittable.test( this.nodeName ) && !rsubmitterTypes.test( type ) &&\n\t\t\t\t( this.checked || !rcheckableType.test( type ) );\n\t\t} )\n\t\t.map( function( i, elem ) {\n\t\t\tvar val = jQuery( this ).val();\n\n\t\t\tif ( val == null ) {\n\t\t\t\treturn null;\n\t\t\t}\n\n\t\t\tif ( Array.isArray( val ) ) {\n\t\t\t\treturn jQuery.map( val, function( val ) {\n\t\t\t\t\treturn { name: elem.name, value: val.replace( rCRLF, "\\r\\n" ) };\n\t\t\t\t} );\n\t\t\t}\n\n\t\t\treturn { name: elem.name, value: val.replace( rCRLF, "\\r\\n" ) };\n\t\t} ).get();\n\t}\n} );\n\n\nvar\n\tr20 = /%20/g,\n\trhash = /#.*$/,\n\trantiCache = /([?&])_=[^&]*/,\n\trheaders = /^(.*?):[ \\t]*([^\\r\\n]*)$/mg,\n\n\t// #7653, #8125, #8152: local protocol detection\n\trlocalProtocol = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/,\n\trnoContent = /^(?:GET|HEAD)$/,\n\trprotocol = /^\\/\\//,\n\n\t/* Prefilters\n\t * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)\n\t * 2) These are called:\n\t *    - BEFORE asking for a transport\n\t *    - AFTER param serialization (s.data is a string if s.processData is true)\n\t * 3) key is the dataType\n\t * 4) the catchall symbol "*" can be used\n\t * 5) execution will start with transport dataType and THEN continue down to "*" if needed\n\t */\n\tprefilters = {},\n\n\t/* Transports bindings\n\t * 1) key is the dataType\n\t * 2) the catchall symbol "*" can be used\n\t * 3) selection will start with transport dataType and THEN go to "*" if needed\n\t */\n\ttransports = {},\n\n\t// Avoid comment-prolog char sequence (#10098); must appease lint and evade compression\n\tallTypes = "*/".concat( "*" ),\n\n\t// Anchor tag for parsing the document origin\n\toriginAnchor = document.createElement( "a" );\n\toriginAnchor.href = location.href;\n\n// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport\nfunction addToPrefiltersOrTransports( structure ) {\n\n\t// dataTypeExpression is optional and defaults to "*"\n\treturn function( dataTypeExpression, func ) {\n\n\t\tif ( typeof dataTypeExpression !== "string" ) {\n\t\t\tfunc = dataTypeExpression;\n\t\t\tdataTypeExpression = "*";\n\t\t}\n\n\t\tvar dataType,\n\t\t\ti = 0,\n\t\t\tdataTypes = dataTypeExpression.toLowerCase().match( rnothtmlwhite ) || [];\n\n\t\tif ( isFunction( func ) ) {\n\n\t\t\t// For each dataType in the dataTypeExpression\n\t\t\twhile ( ( dataType = dataTypes[ i++ ] ) ) {\n\n\t\t\t\t// Prepend if requested\n\t\t\t\tif ( dataType[ 0 ] === "+" ) {\n\t\t\t\t\tdataType = dataType.slice( 1 ) || "*";\n\t\t\t\t\t( structure[ dataType ] = structure[ dataType ] || [] ).unshift( func );\n\n\t\t\t\t// Otherwise append\n\t\t\t\t} else {\n\t\t\t\t\t( structure[ dataType ] = structure[ dataType ] || [] ).push( func );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t};\n}\n\n// Base inspection function for prefilters and transports\nfunction inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR ) {\n\n\tvar inspected = {},\n\t\tseekingTransport = ( structure === transports );\n\n\tfunction inspect( dataType ) {\n\t\tvar selected;\n\t\tinspected[ dataType ] = true;\n\t\tjQuery.each( structure[ dataType ] || [], function( _, prefilterOrFactory ) {\n\t\t\tvar dataTypeOrTransport = prefilterOrFactory( options, originalOptions, jqXHR );\n\t\t\tif ( typeof dataTypeOrTransport === "string" &&\n\t\t\t\t!seekingTransport && !inspected[ dataTypeOrTransport ] ) {\n\n\t\t\t\toptions.dataTypes.unshift( dataTypeOrTransport );\n\t\t\t\tinspect( dataTypeOrTransport );\n\t\t\t\treturn false;\n\t\t\t} else if ( seekingTransport ) {\n\t\t\t\treturn !( selected = dataTypeOrTransport );\n\t\t\t}\n\t\t} );\n\t\treturn selected;\n\t}\n\n\treturn inspect( options.dataTypes[ 0 ] ) || !inspected[ "*" ] && inspect( "*" );\n}\n\n// A special extend for ajax options\n// that takes "flat" options (not to be deep extended)\n// Fixes #9887\nfunction ajaxExtend( target, src ) {\n\tvar key, deep,\n\t\tflatOptions = jQuery.ajaxSettings.flatOptions || {};\n\n\tfor ( key in src ) {\n\t\tif ( src[ key ] !== undefined ) {\n\t\t\t( flatOptions[ key ] ? target : ( deep || ( deep = {} ) ) )[ key ] = src[ key ];\n\t\t}\n\t}\n\tif ( deep ) {\n\t\tjQuery.extend( true, target, deep );\n\t}\n\n\treturn target;\n}\n\n/* Handles responses to an ajax request:\n * - finds the right dataType (mediates between content-type and expected dataType)\n * - returns the corresponding response\n */\nfunction ajaxHandleResponses( s, jqXHR, responses ) {\n\n\tvar ct, type, finalDataType, firstDataType,\n\t\tcontents = s.contents,\n\t\tdataTypes = s.dataTypes;\n\n\t// Remove auto dataType and get content-type in the process\n\twhile ( dataTypes[ 0 ] === "*" ) {\n\t\tdataTypes.shift();\n\t\tif ( ct === undefined ) {\n\t\t\tct = s.mimeType || jqXHR.getResponseHeader( "Content-Type" );\n\t\t}\n\t}\n\n\t// Check if we\'re dealing with a known content-type\n\tif ( ct ) {\n\t\tfor ( type in contents ) {\n\t\t\tif ( contents[ type ] && contents[ type ].test( ct ) ) {\n\t\t\t\tdataTypes.unshift( type );\n\t\t\t\tbreak;\n\t\t\t}\n\t\t}\n\t}\n\n\t// Check to see if we have a response for the expected dataType\n\tif ( dataTypes[ 0 ] in responses ) {\n\t\tfinalDataType = dataTypes[ 0 ];\n\t} else {\n\n\t\t// Try convertible dataTypes\n\t\tfor ( type in responses ) {\n\t\t\tif ( !dataTypes[ 0 ] || s.converters[ type + " " + dataTypes[ 0 ] ] ) {\n\t\t\t\tfinalDataType = type;\n\t\t\t\tbreak;\n\t\t\t}\n\t\t\tif ( !firstDataType ) {\n\t\t\t\tfirstDataType = type;\n\t\t\t}\n\t\t}\n\n\t\t// Or just use first one\n\t\tfinalDataType = finalDataType || firstDataType;\n\t}\n\n\t// If we found a dataType\n\t// We add the dataType to the list if needed\n\t// and return the corresponding response\n\tif ( finalDataType ) {\n\t\tif ( finalDataType !== dataTypes[ 0 ] ) {\n\t\t\tdataTypes.unshift( finalDataType );\n\t\t}\n\t\treturn responses[ finalDataType ];\n\t}\n}\n\n/* Chain conversions given the request and the original response\n * Also sets the responseXXX fields on the jqXHR instance\n */\nfunction ajaxConvert( s, response, jqXHR, isSuccess ) {\n\tvar conv2, current, conv, tmp, prev,\n\t\tconverters = {},\n\n\t\t// Work with a copy of dataTypes in case we need to modify it for conversion\n\t\tdataTypes = s.dataTypes.slice();\n\n\t// Create converters map with lowercased keys\n\tif ( dataTypes[ 1 ] ) {\n\t\tfor ( conv in s.converters ) {\n\t\t\tconverters[ conv.toLowerCase() ] = s.converters[ conv ];\n\t\t}\n\t}\n\n\tcurrent = dataTypes.shift();\n\n\t// Convert to each sequential dataType\n\twhile ( current ) {\n\n\t\tif ( s.responseFields[ current ] ) {\n\t\t\tjqXHR[ s.responseFields[ current ] ] = response;\n\t\t}\n\n\t\t// Apply the dataFilter if provided\n\t\tif ( !prev && isSuccess && s.dataFilter ) {\n\t\t\tresponse = s.dataFilter( response, s.dataType );\n\t\t}\n\n\t\tprev = current;\n\t\tcurrent = dataTypes.shift();\n\n\t\tif ( current ) {\n\n\t\t\t// There\'s only work to do if current dataType is non-auto\n\t\t\tif ( current === "*" ) {\n\n\t\t\t\tcurrent = prev;\n\n\t\t\t// Convert response if prev dataType is non-auto and differs from current\n\t\t\t} else if ( prev !== "*" && prev !== current ) {\n\n\t\t\t\t// Seek a direct converter\n\t\t\t\tconv = converters[ prev + " " + current ] || converters[ "* " + current ];\n\n\t\t\t\t// If none found, seek a pair\n\t\t\t\tif ( !conv ) {\n\t\t\t\t\tfor ( conv2 in converters ) {\n\n\t\t\t\t\t\t// If conv2 outputs current\n\t\t\t\t\t\ttmp = conv2.split( " " );\n\t\t\t\t\t\tif ( tmp[ 1 ] === current ) {\n\n\t\t\t\t\t\t\t// If prev can be converted to accepted input\n\t\t\t\t\t\t\tconv = converters[ prev + " " + tmp[ 0 ] ] ||\n\t\t\t\t\t\t\t\tconverters[ "* " + tmp[ 0 ] ];\n\t\t\t\t\t\t\tif ( conv ) {\n\n\t\t\t\t\t\t\t\t// Condense equivalence converters\n\t\t\t\t\t\t\t\tif ( conv === true ) {\n\t\t\t\t\t\t\t\t\tconv = converters[ conv2 ];\n\n\t\t\t\t\t\t\t\t// Otherwise, insert the intermediate dataType\n\t\t\t\t\t\t\t\t} else if ( converters[ conv2 ] !== true ) {\n\t\t\t\t\t\t\t\t\tcurrent = tmp[ 0 ];\n\t\t\t\t\t\t\t\t\tdataTypes.unshift( tmp[ 1 ] );\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\t// Apply converter (if not an equivalence)\n\t\t\t\tif ( conv !== true ) {\n\n\t\t\t\t\t// Unless errors are allowed to bubble, catch and return them\n\t\t\t\t\tif ( conv && s.throws ) {\n\t\t\t\t\t\tresponse = conv( response );\n\t\t\t\t\t} else {\n\t\t\t\t\t\ttry {\n\t\t\t\t\t\t\tresponse = conv( response );\n\t\t\t\t\t\t} catch ( e ) {\n\t\t\t\t\t\t\treturn {\n\t\t\t\t\t\t\t\tstate: "parsererror",\n\t\t\t\t\t\t\t\terror: conv ? e : "No conversion from " + prev + " to " + current\n\t\t\t\t\t\t\t};\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\treturn { state: "success", data: response };\n}\n\njQuery.extend( {\n\n\t// Counter for holding the number of active queries\n\tactive: 0,\n\n\t// Last-Modified header cache for next request\n\tlastModified: {},\n\tetag: {},\n\n\tajaxSettings: {\n\t\turl: location.href,\n\t\ttype: "GET",\n\t\tisLocal: rlocalProtocol.test( location.protocol ),\n\t\tglobal: true,\n\t\tprocessData: true,\n\t\tasync: true,\n\t\tcontentType: "application/x-www-form-urlencoded; charset=UTF-8",\n\n\t\t/*\n\t\ttimeout: 0,\n\t\tdata: null,\n\t\tdataType: null,\n\t\tusername: null,\n\t\tpassword: null,\n\t\tcache: null,\n\t\tthrows: false,\n\t\ttraditional: false,\n\t\theaders: {},\n\t\t*/\n\n\t\taccepts: {\n\t\t\t"*": allTypes,\n\t\t\ttext: "text/plain",\n\t\t\thtml: "text/html",\n\t\t\txml: "application/xml, text/xml",\n\t\t\tjson: "application/json, text/javascript"\n\t\t},\n\n\t\tcontents: {\n\t\t\txml: /\\bxml\\b/,\n\t\t\thtml: /\\bhtml/,\n\t\t\tjson: /\\bjson\\b/\n\t\t},\n\n\t\tresponseFields: {\n\t\t\txml: "responseXML",\n\t\t\ttext: "responseText",\n\t\t\tjson: "responseJSON"\n\t\t},\n\n\t\t// Data converters\n\t\t// Keys separate source (or catchall "*") and destination types with a single space\n\t\tconverters: {\n\n\t\t\t// Convert anything to text\n\t\t\t"* text": String,\n\n\t\t\t// Text to html (true = no transformation)\n\t\t\t"text html": true,\n\n\t\t\t// Evaluate text as a json expression\n\t\t\t"text json": JSON.parse,\n\n\t\t\t// Parse text as xml\n\t\t\t"text xml": jQuery.parseXML\n\t\t},\n\n\t\t// For options that shouldn\'t be deep extended:\n\t\t// you can add your own custom options here if\n\t\t// and when you create one that shouldn\'t be\n\t\t// deep extended (see ajaxExtend)\n\t\tflatOptions: {\n\t\t\turl: true,\n\t\t\tcontext: true\n\t\t}\n\t},\n\n\t// Creates a full fledged settings object into target\n\t// with both ajaxSettings and settings fields.\n\t// If target is omitted, writes into ajaxSettings.\n\tajaxSetup: function( target, settings ) {\n\t\treturn settings ?\n\n\t\t\t// Building a settings object\n\t\t\tajaxExtend( ajaxExtend( target, jQuery.ajaxSettings ), settings ) :\n\n\t\t\t// Extending ajaxSettings\n\t\t\tajaxExtend( jQuery.ajaxSettings, target );\n\t},\n\n\tajaxPrefilter: addToPrefiltersOrTransports( prefilters ),\n\tajaxTransport: addToPrefiltersOrTransports( transports ),\n\n\t// Main method\n\tajax: function( url, options ) {\n\n\t\t// If url is an object, simulate pre-1.5 signature\n\t\tif ( typeof url === "object" ) {\n\t\t\toptions = url;\n\t\t\turl = undefined;\n\t\t}\n\n\t\t// Force options to be an object\n\t\toptions = options || {};\n\n\t\tvar transport,\n\n\t\t\t// URL without anti-cache param\n\t\t\tcacheURL,\n\n\t\t\t// Response headers\n\t\t\tresponseHeadersString,\n\t\t\tresponseHeaders,\n\n\t\t\t// timeout handle\n\t\t\ttimeoutTimer,\n\n\t\t\t// Url cleanup var\n\t\t\turlAnchor,\n\n\t\t\t// Request state (becomes false upon send and true upon completion)\n\t\t\tcompleted,\n\n\t\t\t// To know if global events are to be dispatched\n\t\t\tfireGlobals,\n\n\t\t\t// Loop variable\n\t\t\ti,\n\n\t\t\t// uncached part of the url\n\t\t\tuncached,\n\n\t\t\t// Create the final options object\n\t\t\ts = jQuery.ajaxSetup( {}, options ),\n\n\t\t\t// Callbacks context\n\t\t\tcallbackContext = s.context || s,\n\n\t\t\t// Context for global events is callbackContext if it is a DOM node or jQuery collection\n\t\t\tglobalEventContext = s.context &&\n\t\t\t\t( callbackContext.nodeType || callbackContext.jquery ) ?\n\t\t\t\t\tjQuery( callbackContext ) :\n\t\t\t\t\tjQuery.event,\n\n\t\t\t// Deferreds\n\t\t\tdeferred = jQuery.Deferred(),\n\t\t\tcompleteDeferred = jQuery.Callbacks( "once memory" ),\n\n\t\t\t// Status-dependent callbacks\n\t\t\tstatusCode = s.statusCode || {},\n\n\t\t\t// Headers (they are sent all at once)\n\t\t\trequestHeaders = {},\n\t\t\trequestHeadersNames = {},\n\n\t\t\t// Default abort message\n\t\t\tstrAbort = "canceled",\n\n\t\t\t// Fake xhr\n\t\t\tjqXHR = {\n\t\t\t\treadyState: 0,\n\n\t\t\t\t// Builds headers hashtable if needed\n\t\t\t\tgetResponseHeader: function( key ) {\n\t\t\t\t\tvar match;\n\t\t\t\t\tif ( completed ) {\n\t\t\t\t\t\tif ( !responseHeaders ) {\n\t\t\t\t\t\t\tresponseHeaders = {};\n\t\t\t\t\t\t\twhile ( ( match = rheaders.exec( responseHeadersString ) ) ) {\n\t\t\t\t\t\t\t\tresponseHeaders[ match[ 1 ].toLowerCase() + " " ] =\n\t\t\t\t\t\t\t\t\t( responseHeaders[ match[ 1 ].toLowerCase() + " " ] || [] )\n\t\t\t\t\t\t\t\t\t\t.concat( match[ 2 ] );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t\tmatch = responseHeaders[ key.toLowerCase() + " " ];\n\t\t\t\t\t}\n\t\t\t\t\treturn match == null ? null : match.join( ", " );\n\t\t\t\t},\n\n\t\t\t\t// Raw string\n\t\t\t\tgetAllResponseHeaders: function() {\n\t\t\t\t\treturn completed ? responseHeadersString : null;\n\t\t\t\t},\n\n\t\t\t\t// Caches the header\n\t\t\t\tsetRequestHeader: function( name, value ) {\n\t\t\t\t\tif ( completed == null ) {\n\t\t\t\t\t\tname = requestHeadersNames[ name.toLowerCase() ] =\n\t\t\t\t\t\t\trequestHeadersNames[ name.toLowerCase() ] || name;\n\t\t\t\t\t\trequestHeaders[ name ] = value;\n\t\t\t\t\t}\n\t\t\t\t\treturn this;\n\t\t\t\t},\n\n\t\t\t\t// Overrides response content-type header\n\t\t\t\toverrideMimeType: function( type ) {\n\t\t\t\t\tif ( completed == null ) {\n\t\t\t\t\t\ts.mimeType = type;\n\t\t\t\t\t}\n\t\t\t\t\treturn this;\n\t\t\t\t},\n\n\t\t\t\t// Status-dependent callbacks\n\t\t\t\tstatusCode: function( map ) {\n\t\t\t\t\tvar code;\n\t\t\t\t\tif ( map ) {\n\t\t\t\t\t\tif ( completed ) {\n\n\t\t\t\t\t\t\t// Execute the appropriate callbacks\n\t\t\t\t\t\t\tjqXHR.always( map[ jqXHR.status ] );\n\t\t\t\t\t\t} else {\n\n\t\t\t\t\t\t\t// Lazy-add the new callbacks in a way that preserves old ones\n\t\t\t\t\t\t\tfor ( code in map ) {\n\t\t\t\t\t\t\t\tstatusCode[ code ] = [ statusCode[ code ], map[ code ] ];\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t\treturn this;\n\t\t\t\t},\n\n\t\t\t\t// Cancel the request\n\t\t\t\tabort: function( statusText ) {\n\t\t\t\t\tvar finalText = statusText || strAbort;\n\t\t\t\t\tif ( transport ) {\n\t\t\t\t\t\ttransport.abort( finalText );\n\t\t\t\t\t}\n\t\t\t\t\tdone( 0, finalText );\n\t\t\t\t\treturn this;\n\t\t\t\t}\n\t\t\t};\n\n\t\t// Attach deferreds\n\t\tdeferred.promise( jqXHR );\n\n\t\t// Add protocol if not provided (prefilters might expect it)\n\t\t// Handle falsy url in the settings object (#10093: consistency with old signature)\n\t\t// We also use the url parameter if available\n\t\ts.url = ( ( url || s.url || location.href ) + "" )\n\t\t\t.replace( rprotocol, location.protocol + "//" );\n\n\t\t// Alias method option to type as per ticket #12004\n\t\ts.type = options.method || options.type || s.method || s.type;\n\n\t\t// Extract dataTypes list\n\t\ts.dataTypes = ( s.dataType || "*" ).toLowerCase().match( rnothtmlwhite ) || [ "" ];\n\n\t\t// A cross-domain request is in order when the origin doesn\'t match the current origin.\n\t\tif ( s.crossDomain == null ) {\n\t\t\turlAnchor = document.createElement( "a" );\n\n\t\t\t// Support: IE <=8 - 11, Edge 12 - 15\n\t\t\t// IE throws exception on accessing the href property if url is malformed,\n\t\t\t// e.g. http://example.com:80x/\n\t\t\ttry {\n\t\t\t\turlAnchor.href = s.url;\n\n\t\t\t\t// Support: IE <=8 - 11 only\n\t\t\t\t// Anchor\'s host property isn\'t correctly set when s.url is relative\n\t\t\t\turlAnchor.href = urlAnchor.href;\n\t\t\t\ts.crossDomain = originAnchor.protocol + "//" + originAnchor.host !==\n\t\t\t\t\turlAnchor.protocol + "//" + urlAnchor.host;\n\t\t\t} catch ( e ) {\n\n\t\t\t\t// If there is an error parsing the URL, assume it is crossDomain,\n\t\t\t\t// it can be rejected by the transport if it is invalid\n\t\t\t\ts.crossDomain = true;\n\t\t\t}\n\t\t}\n\n\t\t// Convert data if not already a string\n\t\tif ( s.data && s.processData && typeof s.data !== "string" ) {\n\t\t\ts.data = jQuery.param( s.data, s.traditional );\n\t\t}\n\n\t\t// Apply prefilters\n\t\tinspectPrefiltersOrTransports( prefilters, s, options, jqXHR );\n\n\t\t// If request was aborted inside a prefilter, stop there\n\t\tif ( completed ) {\n\t\t\treturn jqXHR;\n\t\t}\n\n\t\t// We can fire global events as of now if asked to\n\t\t// Don\'t fire events if jQuery.event is undefined in an AMD-usage scenario (#15118)\n\t\tfireGlobals = jQuery.event && s.global;\n\n\t\t// Watch for a new set of requests\n\t\tif ( fireGlobals && jQuery.active++ === 0 ) {\n\t\t\tjQuery.event.trigger( "ajaxStart" );\n\t\t}\n\n\t\t// Uppercase the type\n\t\ts.type = s.type.toUpperCase();\n\n\t\t// Determine if request has content\n\t\ts.hasContent = !rnoContent.test( s.type );\n\n\t\t// Save the URL in case we\'re toying with the If-Modified-Since\n\t\t// and/or If-None-Match header later on\n\t\t// Remove hash to simplify url manipulation\n\t\tcacheURL = s.url.replace( rhash, "" );\n\n\t\t// More options handling for requests with no content\n\t\tif ( !s.hasContent ) {\n\n\t\t\t// Remember the hash so we can put it back\n\t\t\tuncached = s.url.slice( cacheURL.length );\n\n\t\t\t// If data is available and should be processed, append data to url\n\t\t\tif ( s.data && ( s.processData || typeof s.data === "string" ) ) {\n\t\t\t\tcacheURL += ( rquery.test( cacheURL ) ? "&" : "?" ) + s.data;\n\n\t\t\t\t// #9682: remove data so that it\'s not used in an eventual retry\n\t\t\t\tdelete s.data;\n\t\t\t}\n\n\t\t\t// Add or update anti-cache param if needed\n\t\t\tif ( s.cache === false ) {\n\t\t\t\tcacheURL = cacheURL.replace( rantiCache, "$1" );\n\t\t\t\tuncached = ( rquery.test( cacheURL ) ? "&" : "?" ) + "_=" + ( nonce++ ) + uncached;\n\t\t\t}\n\n\t\t\t// Put hash and anti-cache on the URL that will be requested (gh-1732)\n\t\t\ts.url = cacheURL + uncached;\n\n\t\t// Change \'%20\' to \'+\' if this is encoded form body content (gh-2658)\n\t\t} else if ( s.data && s.processData &&\n\t\t\t( s.contentType || "" ).indexOf( "application/x-www-form-urlencoded" ) === 0 ) {\n\t\t\ts.data = s.data.replace( r20, "+" );\n\t\t}\n\n\t\t// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.\n\t\tif ( s.ifModified ) {\n\t\t\tif ( jQuery.lastModified[ cacheURL ] ) {\n\t\t\t\tjqXHR.setRequestHeader( "If-Modified-Since", jQuery.lastModified[ cacheURL ] );\n\t\t\t}\n\t\t\tif ( jQuery.etag[ cacheURL ] ) {\n\t\t\t\tjqXHR.setRequestHeader( "If-None-Match", jQuery.etag[ cacheURL ] );\n\t\t\t}\n\t\t}\n\n\t\t// Set the correct header, if data is being sent\n\t\tif ( s.data && s.hasContent && s.contentType !== false || options.contentType ) {\n\t\t\tjqXHR.setRequestHeader( "Content-Type", s.contentType );\n\t\t}\n\n\t\t// Set the Accepts header for the server, depending on the dataType\n\t\tjqXHR.setRequestHeader(\n\t\t\t"Accept",\n\t\t\ts.dataTypes[ 0 ] && s.accepts[ s.dataTypes[ 0 ] ] ?\n\t\t\t\ts.accepts[ s.dataTypes[ 0 ] ] +\n\t\t\t\t\t( s.dataTypes[ 0 ] !== "*" ? ", " + allTypes + "; q=0.01" : "" ) :\n\t\t\t\ts.accepts[ "*" ]\n\t\t);\n\n\t\t// Check for headers option\n\t\tfor ( i in s.headers ) {\n\t\t\tjqXHR.setRequestHeader( i, s.headers[ i ] );\n\t\t}\n\n\t\t// Allow custom headers/mimetypes and early abort\n\t\tif ( s.beforeSend &&\n\t\t\t( s.beforeSend.call( callbackContext, jqXHR, s ) === false || completed ) ) {\n\n\t\t\t// Abort if not done already and return\n\t\t\treturn jqXHR.abort();\n\t\t}\n\n\t\t// Aborting is no longer a cancellation\n\t\tstrAbort = "abort";\n\n\t\t// Install callbacks on deferreds\n\t\tcompleteDeferred.add( s.complete );\n\t\tjqXHR.done( s.success );\n\t\tjqXHR.fail( s.error );\n\n\t\t// Get transport\n\t\ttransport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );\n\n\t\t// If no transport, we auto-abort\n\t\tif ( !transport ) {\n\t\t\tdone( -1, "No Transport" );\n\t\t} else {\n\t\t\tjqXHR.readyState = 1;\n\n\t\t\t// Send global event\n\t\t\tif ( fireGlobals ) {\n\t\t\t\tglobalEventContext.trigger( "ajaxSend", [ jqXHR, s ] );\n\t\t\t}\n\n\t\t\t// If request was aborted inside ajaxSend, stop there\n\t\t\tif ( completed ) {\n\t\t\t\treturn jqXHR;\n\t\t\t}\n\n\t\t\t// Timeout\n\t\t\tif ( s.async && s.timeout > 0 ) {\n\t\t\t\ttimeoutTimer = window.setTimeout( function() {\n\t\t\t\t\tjqXHR.abort( "timeout" );\n\t\t\t\t}, s.timeout );\n\t\t\t}\n\n\t\t\ttry {\n\t\t\t\tcompleted = false;\n\t\t\t\ttransport.send( requestHeaders, done );\n\t\t\t} catch ( e ) {\n\n\t\t\t\t// Rethrow post-completion exceptions\n\t\t\t\tif ( completed ) {\n\t\t\t\t\tthrow e;\n\t\t\t\t}\n\n\t\t\t\t// Propagate others as results\n\t\t\t\tdone( -1, e );\n\t\t\t}\n\t\t}\n\n\t\t// Callback for when everything is done\n\t\tfunction done( status, nativeStatusText, responses, headers ) {\n\t\t\tvar isSuccess, success, error, response, modified,\n\t\t\t\tstatusText = nativeStatusText;\n\n\t\t\t// Ignore repeat invocations\n\t\t\tif ( completed ) {\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\tcompleted = true;\n\n\t\t\t// Clear timeout if it exists\n\t\t\tif ( timeoutTimer ) {\n\t\t\t\twindow.clearTimeout( timeoutTimer );\n\t\t\t}\n\n\t\t\t// Dereference transport for early garbage collection\n\t\t\t// (no matter how long the jqXHR object will be used)\n\t\t\ttransport = undefined;\n\n\t\t\t// Cache response headers\n\t\t\tresponseHeadersString = headers || "";\n\n\t\t\t// Set readyState\n\t\t\tjqXHR.readyState = status > 0 ? 4 : 0;\n\n\t\t\t// Determine if successful\n\t\t\tisSuccess = status >= 200 && status < 300 || status === 304;\n\n\t\t\t// Get response data\n\t\t\tif ( responses ) {\n\t\t\t\tresponse = ajaxHandleResponses( s, jqXHR, responses );\n\t\t\t}\n\n\t\t\t// Convert no matter what (that way responseXXX fields are always set)\n\t\t\tresponse = ajaxConvert( s, response, jqXHR, isSuccess );\n\n\t\t\t// If successful, handle type chaining\n\t\t\tif ( isSuccess ) {\n\n\t\t\t\t// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.\n\t\t\t\tif ( s.ifModified ) {\n\t\t\t\t\tmodified = jqXHR.getResponseHeader( "Last-Modified" );\n\t\t\t\t\tif ( modified ) {\n\t\t\t\t\t\tjQuery.lastModified[ cacheURL ] = modified;\n\t\t\t\t\t}\n\t\t\t\t\tmodified = jqXHR.getResponseHeader( "etag" );\n\t\t\t\t\tif ( modified ) {\n\t\t\t\t\t\tjQuery.etag[ cacheURL ] = modified;\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\t// if no content\n\t\t\t\tif ( status === 204 || s.type === "HEAD" ) {\n\t\t\t\t\tstatusText = "nocontent";\n\n\t\t\t\t// if not modified\n\t\t\t\t} else if ( status === 304 ) {\n\t\t\t\t\tstatusText = "notmodified";\n\n\t\t\t\t// If we have data, let\'s convert it\n\t\t\t\t} else {\n\t\t\t\t\tstatusText = response.state;\n\t\t\t\t\tsuccess = response.data;\n\t\t\t\t\terror = response.error;\n\t\t\t\t\tisSuccess = !error;\n\t\t\t\t}\n\t\t\t} else {\n\n\t\t\t\t// Extract error from statusText and normalize for non-aborts\n\t\t\t\terror = statusText;\n\t\t\t\tif ( status || !statusText ) {\n\t\t\t\t\tstatusText = "error";\n\t\t\t\t\tif ( status < 0 ) {\n\t\t\t\t\t\tstatus = 0;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// Set data for the fake xhr object\n\t\t\tjqXHR.status = status;\n\t\t\tjqXHR.statusText = ( nativeStatusText || statusText ) + "";\n\n\t\t\t// Success/Error\n\t\t\tif ( isSuccess ) {\n\t\t\t\tdeferred.resolveWith( callbackContext, [ success, statusText, jqXHR ] );\n\t\t\t} else {\n\t\t\t\tdeferred.rejectWith( callbackContext, [ jqXHR, statusText, error ] );\n\t\t\t}\n\n\t\t\t// Status-dependent callbacks\n\t\t\tjqXHR.statusCode( statusCode );\n\t\t\tstatusCode = undefined;\n\n\t\t\tif ( fireGlobals ) {\n\t\t\t\tglobalEventContext.trigger( isSuccess ? "ajaxSuccess" : "ajaxError",\n\t\t\t\t\t[ jqXHR, s, isSuccess ? success : error ] );\n\t\t\t}\n\n\t\t\t// Complete\n\t\t\tcompleteDeferred.fireWith( callbackContext, [ jqXHR, statusText ] );\n\n\t\t\tif ( fireGlobals ) {\n\t\t\t\tglobalEventContext.trigger( "ajaxComplete", [ jqXHR, s ] );\n\n\t\t\t\t// Handle the global AJAX counter\n\t\t\t\tif ( !( --jQuery.active ) ) {\n\t\t\t\t\tjQuery.event.trigger( "ajaxStop" );\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\treturn jqXHR;\n\t},\n\n\tgetJSON: function( url, data, callback ) {\n\t\treturn jQuery.get( url, data, callback, "json" );\n\t},\n\n\tgetScript: function( url, callback ) {\n\t\treturn jQuery.get( url, undefined, callback, "script" );\n\t}\n} );\n\njQuery.each( [ "get", "post" ], function( i, method ) {\n\tjQuery[ method ] = function( url, data, callback, type ) {\n\n\t\t// Shift arguments if data argument was omitted\n\t\tif ( isFunction( data ) ) {\n\t\t\ttype = type || callback;\n\t\t\tcallback = data;\n\t\t\tdata = undefined;\n\t\t}\n\n\t\t// The url can be an options object (which then must have .url)\n\t\treturn jQuery.ajax( jQuery.extend( {\n\t\t\turl: url,\n\t\t\ttype: method,\n\t\t\tdataType: type,\n\t\t\tdata: data,\n\t\t\tsuccess: callback\n\t\t}, jQuery.isPlainObject( url ) && url ) );\n\t};\n} );\n\n\njQuery._evalUrl = function( url, options ) {\n\treturn jQuery.ajax( {\n\t\turl: url,\n\n\t\t// Make this explicit, since user can override this through ajaxSetup (#11264)\n\t\ttype: "GET",\n\t\tdataType: "script",\n\t\tcache: true,\n\t\tasync: false,\n\t\tglobal: false,\n\n\t\t// Only evaluate the response if it is successful (gh-4126)\n\t\t// dataFilter is not invoked for failure responses, so using it instead\n\t\t// of the default converter is kludgy but it works.\n\t\tconverters: {\n\t\t\t"text script": function() {}\n\t\t},\n\t\tdataFilter: function( response ) {\n\t\t\tjQuery.globalEval( response, options );\n\t\t}\n\t} );\n};\n\n\njQuery.fn.extend( {\n\twrapAll: function( html ) {\n\t\tvar wrap;\n\n\t\tif ( this[ 0 ] ) {\n\t\t\tif ( isFunction( html ) ) {\n\t\t\t\thtml = html.call( this[ 0 ] );\n\t\t\t}\n\n\t\t\t// The elements to wrap the target around\n\t\t\twrap = jQuery( html, this[ 0 ].ownerDocument ).eq( 0 ).clone( true );\n\n\t\t\tif ( this[ 0 ].parentNode ) {\n\t\t\t\twrap.insertBefore( this[ 0 ] );\n\t\t\t}\n\n\t\t\twrap.map( function() {\n\t\t\t\tvar elem = this;\n\n\t\t\t\twhile ( elem.firstElementChild ) {\n\t\t\t\t\telem = elem.firstElementChild;\n\t\t\t\t}\n\n\t\t\t\treturn elem;\n\t\t\t} ).append( this );\n\t\t}\n\n\t\treturn this;\n\t},\n\n\twrapInner: function( html ) {\n\t\tif ( isFunction( html ) ) {\n\t\t\treturn this.each( function( i ) {\n\t\t\t\tjQuery( this ).wrapInner( html.call( this, i ) );\n\t\t\t} );\n\t\t}\n\n\t\treturn this.each( function() {\n\t\t\tvar self = jQuery( this ),\n\t\t\t\tcontents = self.contents();\n\n\t\t\tif ( contents.length ) {\n\t\t\t\tcontents.wrapAll( html );\n\n\t\t\t} else {\n\t\t\t\tself.append( html );\n\t\t\t}\n\t\t} );\n\t},\n\n\twrap: function( html ) {\n\t\tvar htmlIsFunction = isFunction( html );\n\n\t\treturn this.each( function( i ) {\n\t\t\tjQuery( this ).wrapAll( htmlIsFunction ? html.call( this, i ) : html );\n\t\t} );\n\t},\n\n\tunwrap: function( selector ) {\n\t\tthis.parent( selector ).not( "body" ).each( function() {\n\t\t\tjQuery( this ).replaceWith( this.childNodes );\n\t\t} );\n\t\treturn this;\n\t}\n} );\n\n\njQuery.expr.pseudos.hidden = function( elem ) {\n\treturn !jQuery.expr.pseudos.visible( elem );\n};\njQuery.expr.pseudos.visible = function( elem ) {\n\treturn !!( elem.offsetWidth || elem.offsetHeight || elem.getClientRects().length );\n};\n\n\n\n\njQuery.ajaxSettings.xhr = function() {\n\ttry {\n\t\treturn new window.XMLHttpRequest();\n\t} catch ( e ) {}\n};\n\nvar xhrSuccessStatus = {\n\n\t\t// File protocol always yields status code 0, assume 200\n\t\t0: 200,\n\n\t\t// Support: IE <=9 only\n\t\t// #1450: sometimes IE returns 1223 when it should be 204\n\t\t1223: 204\n\t},\n\txhrSupported = jQuery.ajaxSettings.xhr();\n\nsupport.cors = !!xhrSupported && ( "withCredentials" in xhrSupported );\nsupport.ajax = xhrSupported = !!xhrSupported;\n\njQuery.ajaxTransport( function( options ) {\n\tvar callback, errorCallback;\n\n\t// Cross domain only allowed if supported through XMLHttpRequest\n\tif ( support.cors || xhrSupported && !options.crossDomain ) {\n\t\treturn {\n\t\t\tsend: function( headers, complete ) {\n\t\t\t\tvar i,\n\t\t\t\t\txhr = options.xhr();\n\n\t\t\t\txhr.open(\n\t\t\t\t\toptions.type,\n\t\t\t\t\toptions.url,\n\t\t\t\t\toptions.async,\n\t\t\t\t\toptions.username,\n\t\t\t\t\toptions.password\n\t\t\t\t);\n\n\t\t\t\t// Apply custom fields if provided\n\t\t\t\tif ( options.xhrFields ) {\n\t\t\t\t\tfor ( i in options.xhrFields ) {\n\t\t\t\t\t\txhr[ i ] = options.xhrFields[ i ];\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t\t// Override mime type if needed\n\t\t\t\tif ( options.mimeType && xhr.overrideMimeType ) {\n\t\t\t\t\txhr.overrideMimeType( options.mimeType );\n\t\t\t\t}\n\n\t\t\t\t// X-Requested-With header\n\t\t\t\t// For cross-domain requests, seeing as conditions for a preflight are\n\t\t\t\t// akin to a jigsaw puzzle, we simply never set it to be sure.\n\t\t\t\t// (it can always be set on a per-request basis or even using ajaxSetup)\n\t\t\t\t// For same-domain requests, won\'t change header if already provided.\n\t\t\t\tif ( !options.crossDomain && !headers[ "X-Requested-With" ] ) {\n\t\t\t\t\theaders[ "X-Requested-With" ] = "XMLHttpRequest";\n\t\t\t\t}\n\n\t\t\t\t// Set headers\n\t\t\t\tfor ( i in headers ) {\n\t\t\t\t\txhr.setRequestHeader( i, headers[ i ] );\n\t\t\t\t}\n\n\t\t\t\t// Callback\n\t\t\t\tcallback = function( type ) {\n\t\t\t\t\treturn function() {\n\t\t\t\t\t\tif ( callback ) {\n\t\t\t\t\t\t\tcallback = errorCallback = xhr.onload =\n\t\t\t\t\t\t\t\txhr.onerror = xhr.onabort = xhr.ontimeout =\n\t\t\t\t\t\t\t\t\txhr.onreadystatechange = null;\n\n\t\t\t\t\t\t\tif ( type === "abort" ) {\n\t\t\t\t\t\t\t\txhr.abort();\n\t\t\t\t\t\t\t} else if ( type === "error" ) {\n\n\t\t\t\t\t\t\t\t// Support: IE <=9 only\n\t\t\t\t\t\t\t\t// On a manual native abort, IE9 throws\n\t\t\t\t\t\t\t\t// errors on any property access that is not readyState\n\t\t\t\t\t\t\t\tif ( typeof xhr.status !== "number" ) {\n\t\t\t\t\t\t\t\t\tcomplete( 0, "error" );\n\t\t\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t\t\tcomplete(\n\n\t\t\t\t\t\t\t\t\t\t// File: protocol always yields status 0; see #8605, #14207\n\t\t\t\t\t\t\t\t\t\txhr.status,\n\t\t\t\t\t\t\t\t\t\txhr.statusText\n\t\t\t\t\t\t\t\t\t);\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t} else {\n\t\t\t\t\t\t\t\tcomplete(\n\t\t\t\t\t\t\t\t\txhrSuccessStatus[ xhr.status ] || xhr.status,\n\t\t\t\t\t\t\t\t\txhr.statusText,\n\n\t\t\t\t\t\t\t\t\t// Support: IE <=9 only\n\t\t\t\t\t\t\t\t\t// IE9 has no XHR2 but throws on binary (trac-11426)\n\t\t\t\t\t\t\t\t\t// For XHR2 non-text, let the caller handle it (gh-2498)\n\t\t\t\t\t\t\t\t\t( xhr.responseType || "text" ) !== "text"  ||\n\t\t\t\t\t\t\t\t\ttypeof xhr.responseText !== "string" ?\n\t\t\t\t\t\t\t\t\t\t{ binary: xhr.response } :\n\t\t\t\t\t\t\t\t\t\t{ text: xhr.responseText },\n\t\t\t\t\t\t\t\t\txhr.getAllResponseHeaders()\n\t\t\t\t\t\t\t\t);\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t};\n\t\t\t\t};\n\n\t\t\t\t// Listen to events\n\t\t\t\txhr.onload = callback();\n\t\t\t\terrorCallback = xhr.onerror = xhr.ontimeout = callback( "error" );\n\n\t\t\t\t// Support: IE 9 only\n\t\t\t\t// Use onreadystatechange to replace onabort\n\t\t\t\t// to handle uncaught aborts\n\t\t\t\tif ( xhr.onabort !== undefined ) {\n\t\t\t\t\txhr.onabort = errorCallback;\n\t\t\t\t} else {\n\t\t\t\t\txhr.onreadystatechange = function() {\n\n\t\t\t\t\t\t// Check readyState before timeout as it changes\n\t\t\t\t\t\tif ( xhr.readyState === 4 ) {\n\n\t\t\t\t\t\t\t// Allow onerror to be called first,\n\t\t\t\t\t\t\t// but that will not handle a native abort\n\t\t\t\t\t\t\t// Also, save errorCallback to a variable\n\t\t\t\t\t\t\t// as xhr.onerror cannot be accessed\n\t\t\t\t\t\t\twindow.setTimeout( function() {\n\t\t\t\t\t\t\t\tif ( callback ) {\n\t\t\t\t\t\t\t\t\terrorCallback();\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t} );\n\t\t\t\t\t\t}\n\t\t\t\t\t};\n\t\t\t\t}\n\n\t\t\t\t// Create the abort callback\n\t\t\t\tcallback = callback( "abort" );\n\n\t\t\t\ttry {\n\n\t\t\t\t\t// Do send the request (this may raise an exception)\n\t\t\t\t\txhr.send( options.hasContent && options.data || null );\n\t\t\t\t} catch ( e ) {\n\n\t\t\t\t\t// #14683: Only rethrow if this hasn\'t been notified as an error yet\n\t\t\t\t\tif ( callback ) {\n\t\t\t\t\t\tthrow e;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t},\n\n\t\t\tabort: function() {\n\t\t\t\tif ( callback ) {\n\t\t\t\t\tcallback();\n\t\t\t\t}\n\t\t\t}\n\t\t};\n\t}\n} );\n\n\n\n\n// Prevent auto-execution of scripts when no explicit dataType was provided (See gh-2432)\njQuery.ajaxPrefilter( function( s ) {\n\tif ( s.crossDomain ) {\n\t\ts.contents.script = false;\n\t}\n} );\n\n// Install script dataType\njQuery.ajaxSetup( {\n\taccepts: {\n\t\tscript: "text/javascript, application/javascript, " +\n\t\t\t"application/ecmascript, application/x-ecmascript"\n\t},\n\tcontents: {\n\t\tscript: /\\b(?:java|ecma)script\\b/\n\t},\n\tconverters: {\n\t\t"text script": function( text ) {\n\t\t\tjQuery.globalEval( text );\n\t\t\treturn text;\n\t\t}\n\t}\n} );\n\n// Handle cache\'s special case and crossDomain\njQuery.ajaxPrefilter( "script", function( s ) {\n\tif ( s.cache === undefined ) {\n\t\ts.cache = false;\n\t}\n\tif ( s.crossDomain ) {\n\t\ts.type = "GET";\n\t}\n} );\n\n// Bind script tag hack transport\njQuery.ajaxTransport( "script", function( s ) {\n\n\t// This transport only deals with cross domain or forced-by-attrs requests\n\tif ( s.crossDomain || s.scriptAttrs ) {\n\t\tvar script, callback;\n\t\treturn {\n\t\t\tsend: function( _, complete ) {\n\t\t\t\tscript = jQuery( "<script>" )\n\t\t\t\t\t.attr( s.scriptAttrs || {} )\n\t\t\t\t\t.prop( { charset: s.scriptCharset, src: s.url } )\n\t\t\t\t\t.on( "load error", callback = function( evt ) {\n\t\t\t\t\t\tscript.remove();\n\t\t\t\t\t\tcallback = null;\n\t\t\t\t\t\tif ( evt ) {\n\t\t\t\t\t\t\tcomplete( evt.type === "error" ? 404 : 200, evt.type );\n\t\t\t\t\t\t}\n\t\t\t\t\t} );\n\n\t\t\t\t// Use native DOM manipulation to avoid our domManip AJAX trickery\n\t\t\t\tdocument.head.appendChild( script[ 0 ] );\n\t\t\t},\n\t\t\tabort: function() {\n\t\t\t\tif ( callback ) {\n\t\t\t\t\tcallback();\n\t\t\t\t}\n\t\t\t}\n\t\t};\n\t}\n} );\n\n\n\n\nvar oldCallbacks = [],\n\trjsonp = /(=)\\?(?=&|$)|\\?\\?/;\n\n// Default jsonp settings\njQuery.ajaxSetup( {\n\tjsonp: "callback",\n\tjsonpCallback: function() {\n\t\tvar callback = oldCallbacks.pop() || ( jQuery.expando + "_" + ( nonce++ ) );\n\t\tthis[ callback ] = true;\n\t\treturn callback;\n\t}\n} );\n\n// Detect, normalize options and install callbacks for jsonp requests\njQuery.ajaxPrefilter( "json jsonp", function( s, originalSettings, jqXHR ) {\n\n\tvar callbackName, overwritten, responseContainer,\n\t\tjsonProp = s.jsonp !== false && ( rjsonp.test( s.url ) ?\n\t\t\t"url" :\n\t\t\ttypeof s.data === "string" &&\n\t\t\t\t( s.contentType || "" )\n\t\t\t\t\t.indexOf( "application/x-www-form-urlencoded" ) === 0 &&\n\t\t\t\trjsonp.test( s.data ) && "data"\n\t\t);\n\n\t// Handle iff the expected data type is "jsonp" or we have a parameter to set\n\tif ( jsonProp || s.dataTypes[ 0 ] === "jsonp" ) {\n\n\t\t// Get callback name, remembering preexisting value associated with it\n\t\tcallbackName = s.jsonpCallback = isFunction( s.jsonpCallback ) ?\n\t\t\ts.jsonpCallback() :\n\t\t\ts.jsonpCallback;\n\n\t\t// Insert callback into url or form data\n\t\tif ( jsonProp ) {\n\t\t\ts[ jsonProp ] = s[ jsonProp ].replace( rjsonp, "$1" + callbackName );\n\t\t} else if ( s.jsonp !== false ) {\n\t\t\ts.url += ( rquery.test( s.url ) ? "&" : "?" ) + s.jsonp + "=" + callbackName;\n\t\t}\n\n\t\t// Use data converter to retrieve json after script execution\n\t\ts.converters[ "script json" ] = function() {\n\t\t\tif ( !responseContainer ) {\n\t\t\t\tjQuery.error( callbackName + " was not called" );\n\t\t\t}\n\t\t\treturn responseContainer[ 0 ];\n\t\t};\n\n\t\t// Force json dataType\n\t\ts.dataTypes[ 0 ] = "json";\n\n\t\t// Install callback\n\t\toverwritten = window[ callbackName ];\n\t\twindow[ callbackName ] = function() {\n\t\t\tresponseContainer = arguments;\n\t\t};\n\n\t\t// Clean-up function (fires after converters)\n\t\tjqXHR.always( function() {\n\n\t\t\t// If previous value didn\'t exist - remove it\n\t\t\tif ( overwritten === undefined ) {\n\t\t\t\tjQuery( window ).removeProp( callbackName );\n\n\t\t\t// Otherwise restore preexisting value\n\t\t\t} else {\n\t\t\t\twindow[ callbackName ] = overwritten;\n\t\t\t}\n\n\t\t\t// Save back as free\n\t\t\tif ( s[ callbackName ] ) {\n\n\t\t\t\t// Make sure that re-using the options doesn\'t screw things around\n\t\t\t\ts.jsonpCallback = originalSettings.jsonpCallback;\n\n\t\t\t\t// Save the callback name for future use\n\t\t\t\toldCallbacks.push( callbackName );\n\t\t\t}\n\n\t\t\t// Call if it was a function and we have a response\n\t\t\tif ( responseContainer && isFunction( overwritten ) ) {\n\t\t\t\toverwritten( responseContainer[ 0 ] );\n\t\t\t}\n\n\t\t\tresponseContainer = overwritten = undefined;\n\t\t} );\n\n\t\t// Delegate to script\n\t\treturn "script";\n\t}\n} );\n\n\n\n\n// Support: Safari 8 only\n// In Safari 8 documents created via document.implementation.createHTMLDocument\n// collapse sibling forms: the second one becomes a child of the first one.\n// Because of that, this security measure has to be disabled in Safari 8.\n// https://bugs.webkit.org/show_bug.cgi?id=137337\nsupport.createHTMLDocument = ( function() {\n\tvar body = document.implementation.createHTMLDocument( "" ).body;\n\tbody.innerHTML = "<form></form><form></form>";\n\treturn body.childNodes.length === 2;\n} )();\n\n\n// Argument "data" should be string of html\n// context (optional): If specified, the fragment will be created in this context,\n// defaults to document\n// keepScripts (optional): If true, will include scripts passed in the html string\njQuery.parseHTML = function( data, context, keepScripts ) {\n\tif ( typeof data !== "string" ) {\n\t\treturn [];\n\t}\n\tif ( typeof context === "boolean" ) {\n\t\tkeepScripts = context;\n\t\tcontext = false;\n\t}\n\n\tvar base, parsed, scripts;\n\n\tif ( !context ) {\n\n\t\t// Stop scripts or inline event handlers from being executed immediately\n\t\t// by using document.implementation\n\t\tif ( support.createHTMLDocument ) {\n\t\t\tcontext = document.implementation.createHTMLDocument( "" );\n\n\t\t\t// Set the base href for the created document\n\t\t\t// so any parsed elements with URLs\n\t\t\t// are based on the document\'s URL (gh-2965)\n\t\t\tbase = context.createElement( "base" );\n\t\t\tbase.href = document.location.href;\n\t\t\tcontext.head.appendChild( base );\n\t\t} else {\n\t\t\tcontext = document;\n\t\t}\n\t}\n\n\tparsed = rsingleTag.exec( data );\n\tscripts = !keepScripts && [];\n\n\t// Single tag\n\tif ( parsed ) {\n\t\treturn [ context.createElement( parsed[ 1 ] ) ];\n\t}\n\n\tparsed = buildFragment( [ data ], context, scripts );\n\n\tif ( scripts && scripts.length ) {\n\t\tjQuery( scripts ).remove();\n\t}\n\n\treturn jQuery.merge( [], parsed.childNodes );\n};\n\n\n/**\n * Load a url into a page\n */\njQuery.fn.load = function( url, params, callback ) {\n\tvar selector, type, response,\n\t\tself = this,\n\t\toff = url.indexOf( " " );\n\n\tif ( off > -1 ) {\n\t\tselector = stripAndCollapse( url.slice( off ) );\n\t\turl = url.slice( 0, off );\n\t}\n\n\t// If it\'s a function\n\tif ( isFunction( params ) ) {\n\n\t\t// We assume that it\'s the callback\n\t\tcallback = params;\n\t\tparams = undefined;\n\n\t// Otherwise, build a param string\n\t} else if ( params && typeof params === "object" ) {\n\t\ttype = "POST";\n\t}\n\n\t// If we have elements to modify, make the request\n\tif ( self.length > 0 ) {\n\t\tjQuery.ajax( {\n\t\t\turl: url,\n\n\t\t\t// If "type" variable is undefined, then "GET" method will be used.\n\t\t\t// Make value of this field explicit since\n\t\t\t// user can override it through ajaxSetup method\n\t\t\ttype: type || "GET",\n\t\t\tdataType: "html",\n\t\t\tdata: params\n\t\t} ).done( function( responseText ) {\n\n\t\t\t// Save response for use in complete callback\n\t\t\tresponse = arguments;\n\n\t\t\tself.html( selector ?\n\n\t\t\t\t// If a selector was specified, locate the right elements in a dummy div\n\t\t\t\t// Exclude scripts to avoid IE \'Permission Denied\' errors\n\t\t\t\tjQuery( "<div>" ).append( jQuery.parseHTML( responseText ) ).find( selector ) :\n\n\t\t\t\t// Otherwise use the full result\n\t\t\t\tresponseText );\n\n\t\t// If the request succeeds, this function gets "data", "status", "jqXHR"\n\t\t// but they are ignored because response was set above.\n\t\t// If it fails, this function gets "jqXHR", "status", "error"\n\t\t} ).always( callback && function( jqXHR, status ) {\n\t\t\tself.each( function() {\n\t\t\t\tcallback.apply( this, response || [ jqXHR.responseText, status, jqXHR ] );\n\t\t\t} );\n\t\t} );\n\t}\n\n\treturn this;\n};\n\n\n\n\n// Attach a bunch of functions for handling common AJAX events\njQuery.each( [\n\t"ajaxStart",\n\t"ajaxStop",\n\t"ajaxComplete",\n\t"ajaxError",\n\t"ajaxSuccess",\n\t"ajaxSend"\n], function( i, type ) {\n\tjQuery.fn[ type ] = function( fn ) {\n\t\treturn this.on( type, fn );\n\t};\n} );\n\n\n\n\njQuery.expr.pseudos.animated = function( elem ) {\n\treturn jQuery.grep( jQuery.timers, function( fn ) {\n\t\treturn elem === fn.elem;\n\t} ).length;\n};\n\n\n\n\njQuery.offset = {\n\tsetOffset: function( elem, options, i ) {\n\t\tvar curPosition, curLeft, curCSSTop, curTop, curOffset, curCSSLeft, calculatePosition,\n\t\t\tposition = jQuery.css( elem, "position" ),\n\t\t\tcurElem = jQuery( elem ),\n\t\t\tprops = {};\n\n\t\t// Set position first, in-case top/left are set even on static elem\n\t\tif ( position === "static" ) {\n\t\t\telem.style.position = "relative";\n\t\t}\n\n\t\tcurOffset = curElem.offset();\n\t\tcurCSSTop = jQuery.css( elem, "top" );\n\t\tcurCSSLeft = jQuery.css( elem, "left" );\n\t\tcalculatePosition = ( position === "absolute" || position === "fixed" ) &&\n\t\t\t( curCSSTop + curCSSLeft ).indexOf( "auto" ) > -1;\n\n\t\t// Need to be able to calculate position if either\n\t\t// top or left is auto and position is either absolute or fixed\n\t\tif ( calculatePosition ) {\n\t\t\tcurPosition = curElem.position();\n\t\t\tcurTop = curPosition.top;\n\t\t\tcurLeft = curPosition.left;\n\n\t\t} else {\n\t\t\tcurTop = parseFloat( curCSSTop ) || 0;\n\t\t\tcurLeft = parseFloat( curCSSLeft ) || 0;\n\t\t}\n\n\t\tif ( isFunction( options ) ) {\n\n\t\t\t// Use jQuery.extend here to allow modification of coordinates argument (gh-1848)\n\t\t\toptions = options.call( elem, i, jQuery.extend( {}, curOffset ) );\n\t\t}\n\n\t\tif ( options.top != null ) {\n\t\t\tprops.top = ( options.top - curOffset.top ) + curTop;\n\t\t}\n\t\tif ( options.left != null ) {\n\t\t\tprops.left = ( options.left - curOffset.left ) + curLeft;\n\t\t}\n\n\t\tif ( "using" in options ) {\n\t\t\toptions.using.call( elem, props );\n\n\t\t} else {\n\t\t\tcurElem.css( props );\n\t\t}\n\t}\n};\n\njQuery.fn.extend( {\n\n\t// offset() relates an element\'s border box to the document origin\n\toffset: function( options ) {\n\n\t\t// Preserve chaining for setter\n\t\tif ( arguments.length ) {\n\t\t\treturn options === undefined ?\n\t\t\t\tthis :\n\t\t\t\tthis.each( function( i ) {\n\t\t\t\t\tjQuery.offset.setOffset( this, options, i );\n\t\t\t\t} );\n\t\t}\n\n\t\tvar rect, win,\n\t\t\telem = this[ 0 ];\n\n\t\tif ( !elem ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Return zeros for disconnected and hidden (display: none) elements (gh-2310)\n\t\t// Support: IE <=11 only\n\t\t// Running getBoundingClientRect on a\n\t\t// disconnected node in IE throws an error\n\t\tif ( !elem.getClientRects().length ) {\n\t\t\treturn { top: 0, left: 0 };\n\t\t}\n\n\t\t// Get document-relative position by adding viewport scroll to viewport-relative gBCR\n\t\trect = elem.getBoundingClientRect();\n\t\twin = elem.ownerDocument.defaultView;\n\t\treturn {\n\t\t\ttop: rect.top + win.pageYOffset,\n\t\t\tleft: rect.left + win.pageXOffset\n\t\t};\n\t},\n\n\t// position() relates an element\'s margin box to its offset parent\'s padding box\n\t// This corresponds to the behavior of CSS absolute positioning\n\tposition: function() {\n\t\tif ( !this[ 0 ] ) {\n\t\t\treturn;\n\t\t}\n\n\t\tvar offsetParent, offset, doc,\n\t\t\telem = this[ 0 ],\n\t\t\tparentOffset = { top: 0, left: 0 };\n\n\t\t// position:fixed elements are offset from the viewport, which itself always has zero offset\n\t\tif ( jQuery.css( elem, "position" ) === "fixed" ) {\n\n\t\t\t// Assume position:fixed implies availability of getBoundingClientRect\n\t\t\toffset = elem.getBoundingClientRect();\n\n\t\t} else {\n\t\t\toffset = this.offset();\n\n\t\t\t// Account for the *real* offset parent, which can be the document or its root element\n\t\t\t// when a statically positioned element is identified\n\t\t\tdoc = elem.ownerDocument;\n\t\t\toffsetParent = elem.offsetParent || doc.documentElement;\n\t\t\twhile ( offsetParent &&\n\t\t\t\t( offsetParent === doc.body || offsetParent === doc.documentElement ) &&\n\t\t\t\tjQuery.css( offsetParent, "position" ) === "static" ) {\n\n\t\t\t\toffsetParent = offsetParent.parentNode;\n\t\t\t}\n\t\t\tif ( offsetParent && offsetParent !== elem && offsetParent.nodeType === 1 ) {\n\n\t\t\t\t// Incorporate borders into its offset, since they are outside its content origin\n\t\t\t\tparentOffset = jQuery( offsetParent ).offset();\n\t\t\t\tparentOffset.top += jQuery.css( offsetParent, "borderTopWidth", true );\n\t\t\t\tparentOffset.left += jQuery.css( offsetParent, "borderLeftWidth", true );\n\t\t\t}\n\t\t}\n\n\t\t// Subtract parent offsets and element margins\n\t\treturn {\n\t\t\ttop: offset.top - parentOffset.top - jQuery.css( elem, "marginTop", true ),\n\t\t\tleft: offset.left - parentOffset.left - jQuery.css( elem, "marginLeft", true )\n\t\t};\n\t},\n\n\t// This method will return documentElement in the following cases:\n\t// 1) For the element inside the iframe without offsetParent, this method will return\n\t//    documentElement of the parent window\n\t// 2) For the hidden or detached element\n\t// 3) For body or html element, i.e. in case of the html node - it will return itself\n\t//\n\t// but those exceptions were never presented as a real life use-cases\n\t// and might be considered as more preferable results.\n\t//\n\t// This logic, however, is not guaranteed and can change at any point in the future\n\toffsetParent: function() {\n\t\treturn this.map( function() {\n\t\t\tvar offsetParent = this.offsetParent;\n\n\t\t\twhile ( offsetParent && jQuery.css( offsetParent, "position" ) === "static" ) {\n\t\t\t\toffsetParent = offsetParent.offsetParent;\n\t\t\t}\n\n\t\t\treturn offsetParent || documentElement;\n\t\t} );\n\t}\n} );\n\n// Create scrollLeft and scrollTop methods\njQuery.each( { scrollLeft: "pageXOffset", scrollTop: "pageYOffset" }, function( method, prop ) {\n\tvar top = "pageYOffset" === prop;\n\n\tjQuery.fn[ method ] = function( val ) {\n\t\treturn access( this, function( elem, method, val ) {\n\n\t\t\t// Coalesce documents and windows\n\t\t\tvar win;\n\t\t\tif ( isWindow( elem ) ) {\n\t\t\t\twin = elem;\n\t\t\t} else if ( elem.nodeType === 9 ) {\n\t\t\t\twin = elem.defaultView;\n\t\t\t}\n\n\t\t\tif ( val === undefined ) {\n\t\t\t\treturn win ? win[ prop ] : elem[ method ];\n\t\t\t}\n\n\t\t\tif ( win ) {\n\t\t\t\twin.scrollTo(\n\t\t\t\t\t!top ? val : win.pageXOffset,\n\t\t\t\t\ttop ? val : win.pageYOffset\n\t\t\t\t);\n\n\t\t\t} else {\n\t\t\t\telem[ method ] = val;\n\t\t\t}\n\t\t}, method, val, arguments.length );\n\t};\n} );\n\n// Support: Safari <=7 - 9.1, Chrome <=37 - 49\n// Add the top/left cssHooks using jQuery.fn.position\n// Webkit bug: https://bugs.webkit.org/show_bug.cgi?id=29084\n// Blink bug: https://bugs.chromium.org/p/chromium/issues/detail?id=589347\n// getComputedStyle returns percent when specified for top/left/bottom/right;\n// rather than make the css module depend on the offset module, just check for it here\njQuery.each( [ "top", "left" ], function( i, prop ) {\n\tjQuery.cssHooks[ prop ] = addGetHookIf( support.pixelPosition,\n\t\tfunction( elem, computed ) {\n\t\t\tif ( computed ) {\n\t\t\t\tcomputed = curCSS( elem, prop );\n\n\t\t\t\t// If curCSS returns percentage, fallback to offset\n\t\t\t\treturn rnumnonpx.test( computed ) ?\n\t\t\t\t\tjQuery( elem ).position()[ prop ] + "px" :\n\t\t\t\t\tcomputed;\n\t\t\t}\n\t\t}\n\t);\n} );\n\n\n// Create innerHeight, innerWidth, height, width, outerHeight and outerWidth methods\njQuery.each( { Height: "height", Width: "width" }, function( name, type ) {\n\tjQuery.each( { padding: "inner" + name, content: type, "": "outer" + name },\n\t\tfunction( defaultExtra, funcName ) {\n\n\t\t// Margin is only for outerHeight, outerWidth\n\t\tjQuery.fn[ funcName ] = function( margin, value ) {\n\t\t\tvar chainable = arguments.length && ( defaultExtra || typeof margin !== "boolean" ),\n\t\t\t\textra = defaultExtra || ( margin === true || value === true ? "margin" : "border" );\n\n\t\t\treturn access( this, function( elem, type, value ) {\n\t\t\t\tvar doc;\n\n\t\t\t\tif ( isWindow( elem ) ) {\n\n\t\t\t\t\t// $( window ).outerWidth/Height return w/h including scrollbars (gh-1729)\n\t\t\t\t\treturn funcName.indexOf( "outer" ) === 0 ?\n\t\t\t\t\t\telem[ "inner" + name ] :\n\t\t\t\t\t\telem.document.documentElement[ "client" + name ];\n\t\t\t\t}\n\n\t\t\t\t// Get document width or height\n\t\t\t\tif ( elem.nodeType === 9 ) {\n\t\t\t\t\tdoc = elem.documentElement;\n\n\t\t\t\t\t// Either scroll[Width/Height] or offset[Width/Height] or client[Width/Height],\n\t\t\t\t\t// whichever is greatest\n\t\t\t\t\treturn Math.max(\n\t\t\t\t\t\telem.body[ "scroll" + name ], doc[ "scroll" + name ],\n\t\t\t\t\t\telem.body[ "offset" + name ], doc[ "offset" + name ],\n\t\t\t\t\t\tdoc[ "client" + name ]\n\t\t\t\t\t);\n\t\t\t\t}\n\n\t\t\t\treturn value === undefined ?\n\n\t\t\t\t\t// Get width or height on the element, requesting but not forcing parseFloat\n\t\t\t\t\tjQuery.css( elem, type, extra ) :\n\n\t\t\t\t\t// Set width or height on the element\n\t\t\t\t\tjQuery.style( elem, type, value, extra );\n\t\t\t}, type, chainable ? margin : undefined, chainable );\n\t\t};\n\t} );\n} );\n\n\njQuery.each( ( "blur focus focusin focusout resize scroll click dblclick " +\n\t"mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave " +\n\t"change select submit keydown keypress keyup contextmenu" ).split( " " ),\n\tfunction( i, name ) {\n\n\t// Handle event binding\n\tjQuery.fn[ name ] = function( data, fn ) {\n\t\treturn arguments.length > 0 ?\n\t\t\tthis.on( name, null, data, fn ) :\n\t\t\tthis.trigger( name );\n\t};\n} );\n\njQuery.fn.extend( {\n\thover: function( fnOver, fnOut ) {\n\t\treturn this.mouseenter( fnOver ).mouseleave( fnOut || fnOver );\n\t}\n} );\n\n\n\n\njQuery.fn.extend( {\n\n\tbind: function( types, data, fn ) {\n\t\treturn this.on( types, null, data, fn );\n\t},\n\tunbind: function( types, fn ) {\n\t\treturn this.off( types, null, fn );\n\t},\n\n\tdelegate: function( selector, types, data, fn ) {\n\t\treturn this.on( types, selector, data, fn );\n\t},\n\tundelegate: function( selector, types, fn ) {\n\n\t\t// ( namespace ) or ( selector, types [, fn] )\n\t\treturn arguments.length === 1 ?\n\t\t\tthis.off( selector, "**" ) :\n\t\t\tthis.off( types, selector || "**", fn );\n\t}\n} );\n\n// Bind a function to a context, optionally partially applying any\n// arguments.\n// jQuery.proxy is deprecated to promote standards (specifically Function#bind)\n// However, it is not slated for removal any time soon\njQuery.proxy = function( fn, context ) {\n\tvar tmp, args, proxy;\n\n\tif ( typeof context === "string" ) {\n\t\ttmp = fn[ context ];\n\t\tcontext = fn;\n\t\tfn = tmp;\n\t}\n\n\t// Quick check to determine if target is callable, in the spec\n\t// this throws a TypeError, but we will just return undefined.\n\tif ( !isFunction( fn ) ) {\n\t\treturn undefined;\n\t}\n\n\t// Simulated bind\n\targs = slice.call( arguments, 2 );\n\tproxy = function() {\n\t\treturn fn.apply( context || this, args.concat( slice.call( arguments ) ) );\n\t};\n\n\t// Set the guid of unique handler to the same of original handler, so it can be removed\n\tproxy.guid = fn.guid = fn.guid || jQuery.guid++;\n\n\treturn proxy;\n};\n\njQuery.holdReady = function( hold ) {\n\tif ( hold ) {\n\t\tjQuery.readyWait++;\n\t} else {\n\t\tjQuery.ready( true );\n\t}\n};\njQuery.isArray = Array.isArray;\njQuery.parseJSON = JSON.parse;\njQuery.nodeName = nodeName;\njQuery.isFunction = isFunction;\njQuery.isWindow = isWindow;\njQuery.camelCase = camelCase;\njQuery.type = toType;\n\njQuery.now = Date.now;\n\njQuery.isNumeric = function( obj ) {\n\n\t// As of jQuery 3.0, isNumeric is limited to\n\t// strings and numbers (primitives or objects)\n\t// that can be coerced to finite numbers (gh-2662)\n\tvar type = jQuery.type( obj );\n\treturn ( type === "number" || type === "string" ) &&\n\n\t\t// parseFloat NaNs numeric-cast false positives ("")\n\t\t// ...but misinterprets leading-number strings, particularly hex literals ("0x...")\n\t\t// subtraction forces infinities to NaN\n\t\t!isNaN( obj - parseFloat( obj ) );\n};\n\n\n\n\n// Register as a named AMD module, since jQuery can be concatenated with other\n// files that may use define, but not via a proper concatenation script that\n// understands anonymous AMD modules. A named AMD is safest and most robust\n// way to register. Lowercase jquery is used because AMD module names are\n// derived from file names, and jQuery is normally delivered in a lowercase\n// file name. Do this after creating the global so that if an AMD module wants\n// to call noConflict to hide this version of jQuery, it will work.\n\n// Note that for maximum portability, libraries that are not jQuery should\n// declare themselves as anonymous modules, and avoid setting a global if an\n// AMD loader is present. jQuery is a special case. For more information, see\n// https://github.com/jrburke/requirejs/wiki/Updating-existing-libraries#wiki-anon\n\nif ( true ) {\n\t!(__WEBPACK_AMD_DEFINE_ARRAY__ = [], __WEBPACK_AMD_DEFINE_RESULT__ = (function() {\n\t\treturn jQuery;\n\t}).apply(exports, __WEBPACK_AMD_DEFINE_ARRAY__),\n\t\t\t\t__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));\n}\n\n\n\n\nvar\n\n\t// Map over jQuery in case of overwrite\n\t_jQuery = window.jQuery,\n\n\t// Map over the $ in case of overwrite\n\t_$ = window.$;\n\njQuery.noConflict = function( deep ) {\n\tif ( window.$ === jQuery ) {\n\t\twindow.$ = _$;\n\t}\n\n\tif ( deep && window.jQuery === jQuery ) {\n\t\twindow.jQuery = _jQuery;\n\t}\n\n\treturn jQuery;\n};\n\n// Expose jQuery and $ identifiers, even in AMD\n// (#7102#comment:10, https://github.com/jquery/jquery/pull/557)\n// and CommonJS for browser emulators (#13566)\nif ( !noGlobal ) {\n\twindow.jQuery = window.$ = jQuery;\n}\n\n\n\n\nreturn jQuery;\n} );\n\n\n//# sourceURL=webpack:///./node_modules/jquery/dist/jquery.js?')},"./src/public/js/client/delete-items.js":[m
 /*!**********************************************!*\[m
     !*** ./src/public/js/client/delete-items.js ***![m
     \**********************************************/[m
[36m@@ -14,7 +14,7 @@[m
 /*!**********************************************!*\[m
     !*** ./src/public/js/client/image-upload.js ***![m
     \**********************************************/[m
[31m-/*! no static exports found */function srcPublicJsClientImageUploadJs(module,exports,__webpack_require__){eval("\n\nvar $ = __webpack_require__(/*! jquery */ \"./node_modules/jquery/dist/jquery.js\");\n\n// Elements\nvar dropArea = void 0;\nvar inputLabel = void 0;\nvar imageInput = void 0;\nvar birdForm = void 0;\nvar uploadPlaceholder = void 0;\nvar submitBirdButton = void 0;\n\n// Variables and Data\nvar imagesToSubmit = [];\n\n// Check if admin-panel was loaded\n$(document).ready(function () {\n  if (window.location.href.indexOf(\"admin-panel\") > -1) initImageUpload();\n});\n\n// Initialise elements and event listeners\nfunction initImageUpload() {\n  dropArea = document.querySelector('#dropArea');\n  inputLabel = document.querySelector('#image-input-label');\n  imageInput = document.querySelector('#image-input');\n  birdForm = document.querySelector('#bird-form');\n  uploadPlaceholder = document.querySelector('#image-upload');\n  submitBirdButton = document.querySelector('#submit-bird');\n\n  ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(function (eventName) {\n    dropArea.addEventListener(eventName, preventDefaults, false);\n  });\n\n  ['dragenter', 'dragover'].forEach(function (eventName) {\n    dropArea.addEventListener(eventName, highlight, false);\n  });\n\n  ['dragleave', 'drop'].forEach(function (eventName) {\n    dropArea.addEventListener(eventName, unhighlight, false);\n  });\n\n  dropArea.addEventListener('drop', handleDrop, false);\n  birdForm.addEventListener('submit', handleSubmit, false);\n  imageInput.addEventListener('change', handleChange, false);\n}\n\nfunction preventDefaults(e) {\n  e.preventDefault();\n  e.stopPropagation();\n}\n\n//  Highlight upload svg\nfunction highlight(e) {\n  uploadPlaceholder.classList.remove('unhighlight-upload');\n  uploadPlaceholder.classList.add('highlight-upload');\n  dropArea.style.backgroundColor = 'grey';\n}\n\n//  Unhighlight upload svg\nfunction unhighlight(e) {\n  uploadPlaceholder.classList.remove('highlight-upload');\n  uploadPlaceholder.classList.add('unhighlight-upload');\n  dropArea.style.backgroundColor = 'white';\n}\n\n// Get files when user selects from input\nfunction handleChange(e) {\n  preventDefaults(e);\n\n  var files = imageInput.files;\n  var filesArr = Array.from(files);\n\n  for (var i = 0; i < filesArr.length; ++i) {\n    // Check if image limit was reached\n    if (limitIsReached()) {\n      createErrorMessage('Limit of 4 Files!');\n      break;\n    }\n\n    // Check file type\n    if (!fileIsImage(filesArr[i])) {\n      createErrorMessage('File Must be an Image!');\n      break;\n    }\n\n    // Add images for submission\n    imagesToSubmit.push(filesArr[i]);\n\n    // Load images into DOM\n    readAndPreview(filesArr[i]);\n  }\n}\n\n// Get files when user selects drags and drops files\nfunction handleDrop(e) {\n  preventDefaults(e);\n\n  var files = e.dataTransfer.files;\n  var filesArr = Array.from(files);\n\n  for (var i = 0; i < filesArr.length; ++i) {\n    // Check if image limit was reached\n    if (limitIsReached()) {\n      createErrorMessage('Limit of 4 Files!');\n      break;\n    }\n\n    // Check file type\n    if (!fileIsImage(filesArr[i])) {\n      createErrorMessage('File Must be an Image!');\n      break;\n    }\n\n    // Add images for submission\n    imagesToSubmit.push(filesArr[i]);\n\n    // Load images into DOM\n    readAndPreview(filesArr[i]);\n  }\n}\n\n// Create formData and send for submission to server\nfunction handleSubmit(e) {\n  preventDefaults(e);\n\n  // Create form data and remove inital images\n  var formData = new FormData(birdForm);\n  if (formData.get('images')) formData.delete('images');\n\n  // Re-populate with images from the drop area\n  for (var i = 0; i < imagesToSubmit.length; ++i) {\n    formData.append('images', imagesToSubmit[i]);\n  }\n\n  sendData(formData);\n}\n\n// Remove any loaded files from DOM and array\nfunction removeImage(e) {\n  preventDefaults(e);\n\n  // Get the div containing the image and the image's id\n  var parent = e.target.parentElement;\n  var selectedImageID = Number(e.target.parentElement.children[0].id);\n\n  // Search for image that matches the image selected for removal\n  imagesToSubmit.forEach(function (image, i) {\n    if (image.id === selectedImageID) imagesToSubmit.splice(i, 1);\n  });\n\n  // Remove the div that contains the image and update input label\n  parent.remove();\n  changeInputLabel();\n}\n\nfunction changeInputLabel() {\n  if (imagesToSubmit.length === 0) {\n    inputLabel.innerHTML = 'Choose Image';\n    uploadPlaceholder.style.display = 'block';\n  } else if (imagesToSubmit.length === 1) {\n    inputLabel.innerHTML = imagesToSubmit.length + ' Image Added';\n  } else {\n    inputLabel.innerHTML = imagesToSubmit.length + ' Images Added';\n  }\n}\n\nfunction limitIsReached() {\n  return imagesToSubmit.length === 4 ? true : false;\n}\n\nfunction fileIsImage(file) {\n  return file.type === 'image/jpeg' || file.type === 'image/png' || file.type === 'image/gif' ? true : false;\n}\n\nfunction createErrorMessage(message) {\n  var div = document.createElement('div');\n  div.className = 'alert alert-danger';\n  div.innerHTML = message;\n  birdForm.insertBefore(div, birdForm.childNodes[0]);\n\n  setTimeout(function () {\n    +div.remove();\n  }, 2000);\n}\n\n// Create image div and load into the DOM\nfunction readAndPreview(file) {\n  var img = void 0;\n  var reader = new FileReader();\n  var div = document.createElement('div');\n  var br = document.createElement(\"br\");\n  var removeButton = document.createElement('button');\n\n  changeInputLabel();\n  uploadPlaceholder.style.display = 'none';\n  removeButton.innerHTML = 'X';\n  removeButton.addEventListener('click', removeImage);\n\n  // Set id of each image with the current timestamp\n  // Will check this id when the remove button is clicked\n  Object.defineProperty(file, 'id', {\n    value: Date.now(),\n    writable: true\n  });\n\n  // Set properties and create a div for each image that was loaded\n  reader.addEventListener(\"load\", function () {\n    img = new Image(100, 100);\n    img.className = 'output-image';\n    img.src = reader.result;\n    img.id = file.id;\n    div.appendChild(img);\n    div.appendChild(br);\n    div.appendChild(removeButton);\n    dropArea.appendChild(div);\n  }, false);\n\n  reader.readAsDataURL(file);\n}\n\n// POST data to server\nfunction sendData(formData) {\n  var XHR = new XMLHttpRequest();\n\n  // Define what happens on successful data submission\n  XHR.addEventListener('load', function (e) {\n    document.location.href = '/admin-panel';\n  });\n\n  // Define what happens in case of error\n  XHR.addEventListener('error', function (e) {\n    console.log(e);\n  });\n\n  // Set up our request\n  XHR.open('POST', '/api/birds');\n\n  // Send our FormData object; HTTP headers are set automatically\n  XHR.send(formData);\n}\n\n//# sourceURL=webpack:///./src/public/js/client/image-upload.js?")},"./src/public/js/client/messages.js":[m
[32m+[m[32m/*! no static exports found */function srcPublicJsClientImageUploadJs(module,exports,__webpack_require__){eval("\n\n// Variables and Data\nvar imagesToSubmit = [];\nvar $ = __webpack_require__(/*! jquery */ \"./node_modules/jquery/dist/jquery.js\");\n\n// Elements\nvar dropArea = void 0;\nvar inputLabel = void 0;\nvar imageInput = void 0;\nvar birdForm = void 0;\nvar uploadPlaceholder = void 0;\nvar submitBirdButton = void 0;\n\n// Check if admin-panel was loaded\n$(document).ready(function () {\n  if (window.location.href.indexOf(\"admin-panel\") > -1) initImageUpload();\n});\n\n// Initialise elements and event listeners\nfunction initImageUpload() {\n  dropArea = document.querySelector('#dropArea');\n  inputLabel = document.querySelector('#image-input-label');\n  imageInput = document.querySelector('#image-input');\n  birdForm = document.querySelector('#bird-form');\n  uploadPlaceholder = document.querySelector('#image-upload');\n  submitBirdButton = document.querySelector('#submit-bird');\n\n  ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(function (eventName) {\n    dropArea.addEventListener(eventName, preventDefaults, false);\n  });\n\n  ['dragenter', 'dragover'].forEach(function (eventName) {\n    dropArea.addEventListener(eventName, highlight, false);\n  });\n\n  ['dragleave', 'drop'].forEach(function (eventName) {\n    dropArea.addEventListener(eventName, unhighlight, false);\n  });\n\n  dropArea.addEventListener('drop', handleDrop, false);\n  birdForm.addEventListener('submit', handleSubmit, false);\n  imageInput.addEventListener('change', handleInput, false);\n}\n\nfunction preventDefaults(e) {\n  e.preventDefault();\n  e.stopPropagation();\n}\n\n//  Highlight upload svg\nfunction highlight(e) {\n  uploadPlaceholder.classList.remove('unhighlight-upload');\n  uploadPlaceholder.classList.add('highlight-upload');\n  dropArea.style.backgroundColor = 'grey';\n}\n\n//  Unhighlight upload svg\nfunction unhighlight(e) {\n  uploadPlaceholder.classList.remove('highlight-upload');\n  uploadPlaceholder.classList.add('unhighlight-upload');\n  dropArea.style.backgroundColor = 'white';\n}\n\n// Get files when user selects from input\nfunction handleInput(e) {\n  preventDefaults(e);\n\n  var files = imageInput.files;\n  var filesArr = Array.from(files);\n\n  for (var i = 0; i < filesArr.length; ++i) {\n    // Check if image limit was reached\n    if (limitIsReached()) {\n      createErrorMessage('Limit of 4 Files!');\n      break;\n    }\n\n    // Check file type\n    if (!fileIsImage(filesArr[i])) {\n      createErrorMessage('File Must be an Image!');\n      break;\n    }\n\n    // Add images for submission\n    imagesToSubmit.push(filesArr[i]);\n\n    // Load images into DOM\n    readAndPreview(filesArr[i]);\n  }\n}\n\n// Get files when user selects drags and drops files\nfunction handleDrop(e) {\n  preventDefaults(e);\n\n  var files = e.dataTransfer.files;\n  var filesArr = Array.from(files);\n\n  for (var i = 0; i < filesArr.length; ++i) {\n    // Check if image limit was reached\n    if (limitIsReached()) {\n      createErrorMessage('Limit of 4 Files!');\n      break;\n    }\n\n    // Check file type\n    if (!fileIsImage(filesArr[i])) {\n      createErrorMessage('File Must be an Image!');\n      break;\n    }\n\n    // Add images for submission\n    imagesToSubmit.push(filesArr[i]);\n\n    // Load images into DOM\n    readAndPreview(filesArr[i]);\n  }\n}\n\n// Create formData and send for submission to server\nfunction handleSubmit(e) {\n  preventDefaults(e);\n\n  // Create form data and remove inital images\n  var formData = new FormData(birdForm);\n  if (formData.get('images')) formData.delete('images');\n\n  // Re-populate with images from the drop area\n  for (var i = 0; i < imagesToSubmit.length; ++i) {\n    formData.append('images', imagesToSubmit[i]);\n  }\n\n  sendData(formData);\n}\n\n// Remove any loaded files from DOM and array\nfunction removeImage(e) {\n  preventDefaults(e);\n\n  // Get the div containing the image and the image's id\n  var parent = e.target.parentElement;\n  var selectedImageID = Number(e.target.parentElement.children[0].id);\n\n  // Search for image that matches the image selected for removal\n  imagesToSubmit.forEach(function (image, i) {\n    if (image.id === selectedImageID) imagesToSubmit.splice(i, 1);\n  });\n\n  // Remove the div that contains the image and update input label\n  parent.remove();\n  changeInputLabel();\n}\n\nfunction changeInputLabel() {\n  if (imagesToSubmit.length === 0) {\n    inputLabel.innerHTML = 'Choose Image';\n    uploadPlaceholder.style.display = 'block';\n  } else if (imagesToSubmit.length === 1) {\n    inputLabel.innerHTML = imagesToSubmit.length + ' Image Added';\n  } else {\n    inputLabel.innerHTML = imagesToSubmit.length + ' Images Added';\n  }\n}\n\nfunction limitIsReached() {\n  return imagesToSubmit.length === 4 ? true : false;\n}\n\nfunction fileIsImage(file) {\n  return file.type === 'image/jpeg' || file.type === 'image/png' || file.type === 'image/gif' ? true : false;\n}\n\nfunction createErrorMessage(message) {\n  var div = document.createElement('div');\n  div.className = 'alert alert-danger';\n  div.innerHTML = message;\n  birdForm.insertBefore(div, birdForm.childNodes[0]);\n\n  setTimeout(function () {\n    +div.remove();\n  }, 2000);\n}\n\n// Create image div and load into the DOM\nfunction readAndPreview(file) {\n  var reader = new FileReader();\n  var img = new Image(100, 100);\n  var div = document.createElement('div');\n  var br = document.createElement(\"br\");\n  var removeButton = document.createElement('button');\n\n  changeInputLabel();\n  uploadPlaceholder.style.display = 'none';\n  removeButton.innerHTML = 'X';\n  removeButton.addEventListener('click', removeImage);\n\n  // Set id of each image with the current timestamp\n  // Will check this id when the remove button is clicked\n  Object.defineProperty(file, 'id', {\n    value: Date.now(),\n    writable: true\n  });\n\n  // Set properties and create a div for each image that was loaded\n  reader.addEventListener(\"load\", function () {\n    img.className = 'output-image';\n    img.src = reader.result;\n    img.id = file.id;\n    div.appendChild(img);\n    div.appendChild(br);\n    div.appendChild(removeButton);\n    dropArea.appendChild(div);\n  }, false);\n\n  reader.readAsDataURL(file);\n}\n\n// POST data to server\nfunction sendData(formData) {\n  var XHR = new XMLHttpRequest();\n\n  // Define what happens on successful data submission\n  XHR.addEventListener('load', function (e) {\n    document.location.href = '/admin-panel';\n  });\n\n  // Define what happens in case of error\n  XHR.addEventListener('error', function (e) {\n    console.log(e);\n  });\n\n  // Set up our request\n  XHR.open('POST', '/api/birds');\n\n  // Send our FormData object; HTTP headers are set automatically\n  XHR.send(formData);\n}\n\n//# sourceURL=webpack:///./src/public/js/client/image-upload.js?")},"./src/public/js/client/messages.js":[m
 /*!******************************************!*\[m
     !*** ./src/public/js/client/messages.js ***![m
     \******************************************/[m
[36m@@ -26,7 +26,7 @@[m
 /*!******************************************!*\[m
     !*** ./src/public/js/client/redirect.js ***![m
     \******************************************/[m
[31m-/*! no static exports found */function srcPublicJsClientRedirectJs(module,exports,__webpack_require__){eval("\n\nvar $ = __webpack_require__(/*! jquery */ \"./node_modules/jquery/dist/jquery.js\");\n\nvar inquiryButton = void 0;\n\n$(document).ready(function () {\n  var URL = window.location.href;\n  var SPLIT_URL = URL.split('/');\n\n  // If on home page \n  // Initialise buttons that will redirect to other pages\n  if (SPLIT_URL[3] === '') {\n    initButtons();\n  }\n});\n\nfunction initButtons() {\n  inquiryButton = document.querySelector('#inquiry-button');\n  inquiryButton.addEventListener('click', function () {\n    return redirect('contact');\n  });\n}\n\nfunction redirect(route) {\n  window.location.href = route;\n}\n\n//# sourceURL=webpack:///./src/public/js/client/redirect.js?")},"./src/public/js/client/sort-dropdown.js":[m
[32m+[m[32m/*! no static exports found */function srcPublicJsClientRedirectJs(module,exports,__webpack_require__){eval("\n\nvar $ = __webpack_require__(/*! jquery */ \"./node_modules/jquery/dist/jquery.js\");\n\nvar inquiryButton = void 0;\nvar viewLargeBtn = void 0;\n\n$(document).ready(function () {\n  var URL = window.location.href;\n  var SPLIT_URL = URL.split('/');\n\n  // If on home page \n  // Initialise buttons that will redirect to other pages\n  if (SPLIT_URL[3] === '') {\n    initButtons();\n  }\n});\n\nfunction initButtons() {\n  inquiryButton = document.querySelector('#inquiry-button');\n  viewLargeBtn = document.querySelector('#view-large-parrots');\n\n  inquiryButton.addEventListener('click', function () {\n    return redirect('contact');\n  });\n  viewLargeBtn.addEventListener('click', function () {\n    return redirect('birds');\n  });\n}\n\nfunction redirect(route) {\n  window.location.href = route;\n}\n\n//# sourceURL=webpack:///./src/public/js/client/redirect.js?")},"./src/public/js/client/sort-dropdown.js":[m
 /*!***********************************************!*\[m
     !*** ./src/public/js/client/sort-dropdown.js ***![m
     \***********************************************/[m
[36m@@ -34,4 +34,4 @@[m
 /*!***********************************************************************************************************************************************************************************************************************************************************************************!*\[m
     !*** multi ./src/public/js/client/delete-items.js ./src/public/js/client/image-upload.js ./src/public/js/client/messages.js ./src/public/js/client/filters-ls.js ./src/public/js/client/sort-dropdown.js ./src/public/js/client/pagination.js ./src/public/js/client/redirect.js ***![m
     \***********************************************************************************************************************************************************************************************************************************************************************************/[m
[31m-/*! no static exports found */function _(module,exports,__webpack_require__){eval('__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\delete-items.js */"./src/public/js/client/delete-items.js");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\image-upload.js */"./src/public/js/client/image-upload.js");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\messages.js */"./src/public/js/client/messages.js");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\filters-ls.js */"./src/public/js/client/filters-ls.js");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\sort-dropdown.js */"./src/public/js/client/sort-dropdown.js");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\pagination.js */"./src/public/js/client/pagination.js");\nmodule.exports = __webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\redirect.js */"./src/public/js/client/redirect.js");\n\n\n//# sourceURL=webpack:///multi_./src/public/js/client/delete-items.js_./src/public/js/client/image-upload.js_./src/public/js/client/messages.js_./src/public/js/client/filters-ls.js_./src/public/js/client/sort-dropdown.js_./src/public/js/client/pagination.js_./src/public/js/client/redirect.js?')}})}]);[m
\ No newline at end of file[m
[32m+[m[32m/*! no static exports found */function _(module,exports,__webpack_require__){eval('__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\delete-items.js */"./src/public/js/client/delete-items.js");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\image-upload.js */"./src/public/js/client/image-upload.js");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\messages.js */"./src/public/js/client/messages.js");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\filters-ls.js */"./src/public/js/client/filters-ls.js");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\sort-dropdown.js */"./src/public/js/client/sort-dropdown.js");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\pagination.js */"./src/public/js/client/pagination.js");\nmodule.exports = __webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\redirect.js */"./src/public/js/client/redirect.js");\n\n\n//# sourceURL=webpack:///multi_./src/public/js/client/delete-items.js_./src/public/js/client/image-upload.js_./src/public/js/client/messages.js_./src/public/js/client/filters-ls.js_./src/public/js/client/sort-dropdown.js_./src/public/js/client/pagination.js_./src/public/js/client/redirect.js?')}},x={},y.m=w,y.c=x,y.d=function(t,e,n){y.o(t,e)||Object.defineProperty(t,e,{enumerable:!0,get:n})},y.r=function(t){"undefined"!=typeof Symbol&&Symbol.toStringTag&&Object.defineProperty(t,Symbol.toStringTag,{value:"Module"}),Object.defineProperty(t,"__esModule",{value:!0})},y.t=function(e,t){if(1&t&&(e=y(e)),8&t)return e;if(4&t&&"object"===(void 0===e?"undefined":_typeof(e))&&e&&e.__esModule)return e;var n=Object.create(null);if(y.r(n),Object.defineProperty(n,"default",{enumerable:!0,value:e}),2&t&&"string"!=typeof e)for(var r in e)y.d(n,r,function(t){return e[t]}.bind(null,r));return n},y.n=function(t){var e=t&&t.__esModule?function(){return t.default}:function(){return t};return y.d(e,"a",e),e},y.o=function(t,e){return Object.prototype.hasOwnProperty.call(t,e)},y.p="/",y(y.s=0)}]);[m
\ No newline at end of file[m
[1mdiff --git a/package-lock.json b/package-lock.json[m
[1mindex 408c602..af9a09b 100644[m
[1m--- a/package-lock.json[m
[1m+++ b/package-lock.json[m
[36m@@ -194,7 +194,7 @@[m
 		},[m
 		"@xtuc/ieee754": {[m
 			"version": "1.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/@xtuc/ieee754/-/ieee754-1.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/@xtuc/ieee754/-/ieee754-1.2.0.tgz",[m
 			"integrity": "sha512-DX8nKgqcGwsc0eJSqYt5lwP4DH5FlHnmuWWBRy7X0NcaGR0ZtuyeESgMwTYVEtxmsNGY+qit4QYT/MIYTOTPeA==",[m
 			"dev": true[m
 		},[m
[36m@@ -206,7 +206,7 @@[m
 		},[m
 		"abbrev": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/abbrev/-/abbrev-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/abbrev/-/abbrev-1.1.1.tgz",[m
 			"integrity": "sha512-nne9/IiQ/hzIhY6pdDnbBtz7DjPTKrY00P/zvPSm5pOFkl6xuGrGnXn/VtTNNfNtAfZ9/1RtehkszU9qcTii0Q==",[m
 			"dev": true[m
 		},[m
[36m@@ -257,31 +257,31 @@[m
 		},[m
 		"alphanum-sort": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/alphanum-sort/-/alphanum-sort-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/alphanum-sort/-/alphanum-sort-1.0.2.tgz",[m
 			"integrity": "sha1-l6ERlkmyEa0zaR2fn0hqjsn74KM=",[m
 			"dev": true[m
 		},[m
 		"amdefine": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/amdefine/-/amdefine-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/amdefine/-/amdefine-1.0.1.tgz",[m
 			"integrity": "sha1-SlKCrBZHKek2Gbz9OtFR+BfOkfU=",[m
 			"dev": true[m
 		},[m
 		"ansi-regex": {[m
 			"version": "2.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-regex/-/ansi-regex-2.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/ansi-regex/-/ansi-regex-2.1.1.tgz",[m
 			"integrity": "sha1-w7M6te42DYbg5ijwRorn7yfWVN8=",[m
 			"dev": true[m
 		},[m
 		"ansi-styles": {[m
 			"version": "2.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-2.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-2.2.1.tgz",[m
 			"integrity": "sha1-tDLdM1i2NM914eRmQ2gkBTPB3b4=",[m
 			"dev": true[m
 		},[m
 		"anymatch": {[m
 			"version": "1.3.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/anymatch/-/anymatch-1.3.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/anymatch/-/anymatch-1.3.2.tgz",[m
 			"integrity": "sha512-0XNayC8lTHQ2OI8aljNCN3sSx6hsr/1+rlcDAotXJR7C1oZZHCNsfpbKwMjRA3Uqb5tF1Rae2oloTr4xpq+WjA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -291,19 +291,19 @@[m
 		},[m
 		"append-field": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/append-field/-/append-field-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/append-field/-/append-field-1.0.0.tgz",[m
 			"integrity": "sha1-HjRA6RXwsSA9I3SOeO3XubW0PlY=",[m
 			"dev": true[m
 		},[m
 		"aproba": {[m
 			"version": "1.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/aproba/-/aproba-1.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/aproba/-/aproba-1.2.0.tgz",[m
 			"integrity": "sha512-Y9J6ZjXtoYh8RnXVCMOU/ttDmk1aBjunq9vO0ta5x85WDQiQfUF9sIPBITdbiiIVcBo03Hi3jMxigBtsddlXRw==",[m
 			"dev": true[m
 		},[m
 		"are-we-there-yet": {[m
 			"version": "1.1.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/are-we-there-yet/-/are-we-there-yet-1.1.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/are-we-there-yet/-/are-we-there-yet-1.1.5.tgz",[m
 			"integrity": "sha512-5hYdAkZlcG8tOLujVDTgCT+uPX0VnpAH28gWsLfzpXYm7wP6mp5Q/gYyR7YQ0cKVJcXJnl3j2kpBan13PtQf6w==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -313,7 +313,7 @@[m
 		},[m
 		"argparse": {[m
 			"version": "1.0.10",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/argparse/-/argparse-1.0.10.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/argparse/-/argparse-1.0.10.tgz",[m
 			"integrity": "sha512-o5Roy6tNG4SL/FOkCAN6RzjiakZS25RLYFrcMttJqbdd8BWrnA+fGz57iN5Pb06pvBGvl5gQ0B48dJlslXvoTg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -322,7 +322,7 @@[m
 		},[m
 		"arr-diff": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/arr-diff/-/arr-diff-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/arr-diff/-/arr-diff-2.0.0.tgz",[m
 			"integrity": "sha1-jzuCf5Vai9ZpaX5KQlasPOrjVs8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -331,48 +331,48 @@[m
 		},[m
 		"arr-flatten": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/arr-flatten/-/arr-flatten-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/arr-flatten/-/arr-flatten-1.1.0.tgz",[m
 			"integrity": "sha512-L3hKV5R/p5o81R7O02IGnwpDmkp6E982XhtbuwSe3O4qOtMMMtodicASA1Cny2U+aCXcNpml+m4dPsvsJ3jatg==",[m
 			"dev": true[m
 		},[m
 		"arr-union": {[m
 			"version": "3.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/arr-union/-/arr-union-3.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/arr-union/-/arr-union-3.1.0.tgz",[m
 			"integrity": "sha1-45sJrqne+Gao8gbiiK9jkZuuOcQ=",[m
 			"dev": true[m
 		},[m
 		"array-filter": {[m
 			"version": "0.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-filter/-/array-filter-0.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/array-filter/-/array-filter-0.0.1.tgz",[m
 			"integrity": "sha1-fajPLiZijtcygDWB/SH2fKzS7uw=",[m
 			"dev": true[m
 		},[m
 		"array-find-index": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-find-index/-/array-find-index-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/array-find-index/-/array-find-index-1.0.2.tgz",[m
 			"integrity": "sha1-3wEKoSh+Fku9pvlyOwqWoexBh6E=",[m
 			"dev": true[m
 		},[m
 		"array-flatten": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-flatten/-/array-flatten-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/array-flatten/-/array-flatten-1.1.1.tgz",[m
 			"integrity": "sha1-ml9pkFGx5wczKPKgCJaLZOopVdI="[m
 		},[m
 		"array-map": {[m
 			"version": "0.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-map/-/array-map-0.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/array-map/-/array-map-0.0.0.tgz",[m
 			"integrity": "sha1-iKK6tz0c97zVwbEYoAP2b2ZfpmI=",[m
 			"dev": true[m
 		},[m
 		"array-reduce": {[m
 			"version": "0.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-reduce/-/array-reduce-0.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/array-reduce/-/array-reduce-0.0.0.tgz",[m
 			"integrity": "sha1-FziZ0//Rx9k4PkR5Ul2+J4yrXys=",[m
 			"dev": true[m
 		},[m
 		"array-union": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-union/-/array-union-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/array-union/-/array-union-1.0.2.tgz",[m
 			"integrity": "sha1-mjRBDk9OPaI96jdb5b5w8kd47Dk=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -381,25 +381,25 @@[m
 		},[m
 		"array-uniq": {[m
 			"version": "1.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-uniq/-/array-uniq-1.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/array-uniq/-/array-uniq-1.0.3.tgz",[m
 			"integrity": "sha1-r2rId6Jcx/dOBYiUdThY39sk/bY=",[m
 			"dev": true[m
 		},[m
 		"array-unique": {[m
 			"version": "0.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-unique/-/array-unique-0.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/array-unique/-/array-unique-0.2.1.tgz",[m
 			"integrity": "sha1-odl8yvy8JiXMcPrc6zalDFiwGlM=",[m
 			"dev": true[m
 		},[m
 		"arrify": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/arrify/-/arrify-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/arrify/-/arrify-1.0.1.tgz",[m
 			"integrity": "sha1-iYUI2iIm84DfkEcoRWhJwVAaSw0=",[m
 			"dev": true[m
 		},[m
 		"asn1": {[m
 			"version": "0.2.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/asn1/-/asn1-0.2.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/asn1/-/asn1-0.2.4.tgz",[m
 			"integrity": "sha512-jxwzQpLQjSmWXgwaCZE9Nz+glAG01yF1QnWgbhGwHI5A6FRIEY6IVqtHhIepHqI7/kyEyQEagBC5mBEFlIYvdg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -408,7 +408,7 @@[m
 		},[m
 		"asn1.js": {[m
 			"version": "4.10.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/asn1.js/-/asn1.js-4.10.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/asn1.js/-/asn1.js-4.10.1.tgz",[m
 			"integrity": "sha512-p32cOF5q0Zqs9uBiONKYLm6BClCoBCM5O9JfeUSlnQLBTxYdTK+pW+nXflm8UkKd2UYlEbYz5qEi0JuZR9ckSw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -419,7 +419,7 @@[m
 		},[m
 		"assert": {[m
 			"version": "1.4.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/assert/-/assert-1.4.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/assert/-/assert-1.4.1.tgz",[m
 			"integrity": "sha1-mZEtWRg2tab1s0XA8H7vwI/GXZE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -428,13 +428,13 @@[m
 			"dependencies": {[m
 				"inherits": {[m
 					"version": "2.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/inherits/-/inherits-2.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/inherits/-/inherits-2.0.1.tgz",[m
 					"integrity": "sha1-sX0I0ya0Qj5Wjv9xn5GwscvfafE=",[m
 					"dev": true[m
 				},[m
 				"util": {[m
 					"version": "0.10.3",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/util/-/util-0.10.3.tgz",[m
[32m+[m					[32m"resolved": "http://registry.npmjs.org/util/-/util-0.10.3.tgz",[m
 					"integrity": "sha1-evsa/lCAUkZInj23/g7TeTNqwPk=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -445,19 +445,19 @@[m
 		},[m
 		"assert-plus": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/assert-plus/-/assert-plus-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/assert-plus/-/assert-plus-1.0.0.tgz",[m
 			"integrity": "sha1-8S4PPF13sLHN2RRpQuTpbB5N1SU=",[m
 			"dev": true[m
 		},[m
 		"assign-symbols": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/assign-symbols/-/assign-symbols-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/assign-symbols/-/assign-symbols-1.0.0.tgz",[m
 			"integrity": "sha1-WWZ/QfrdTyDMvCu5a41Pf3jsA2c=",[m
 			"dev": true[m
 		},[m
 		"async": {[m
 			"version": "2.6.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/async/-/async-2.6.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/async/-/async-2.6.1.tgz",[m
 			"integrity": "sha512-fNEiL2+AZt6AlAw/29Cr0UDe4sRAHCpEHh54WMz+Bb7QfNcFw4h3loofyJpLeQs4Yx7yuqu/2dLgM5hKOs6HlQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -472,43 +472,43 @@[m
 		},[m
 		"async-foreach": {[m
 			"version": "0.1.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/async-foreach/-/async-foreach-0.1.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/async-foreach/-/async-foreach-0.1.3.tgz",[m
 			"integrity": "sha1-NhIfhFwFeBct5Bmpfb6x0W7DRUI=",[m
 			"dev": true[m
 		},[m
 		"asynckit": {[m
 			"version": "0.4.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/asynckit/-/asynckit-0.4.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/asynckit/-/asynckit-0.4.0.tgz",[m
 			"integrity": "sha1-x57Zf380y48robyXkLzDZkdLS3k=",[m
 			"dev": true[m
 		},[m
 		"atob": {[m
 			"version": "2.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/atob/-/atob-2.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/atob/-/atob-2.1.2.tgz",[m
 			"integrity": "sha512-Wm6ukoaOGJi/73p/cl2GvLjTI5JM1k/O14isD73YML8StrH/7/lRFgmg8nICZgD3bZZvjwCGxtMOD3wWNAu8cg==",[m
 			"dev": true[m
 		},[m
 		"aws-sign2": {[m
 			"version": "0.7.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/aws-sign2/-/aws-sign2-0.7.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/aws-sign2/-/aws-sign2-0.7.0.tgz",[m
 			"integrity": "sha1-tG6JCTSpWR8tL2+G1+ap8bP+dqg=",[m
 			"dev": true[m
 		},[m
 		"aws4": {[m
 			"version": "1.8.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/aws4/-/aws4-1.8.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/aws4/-/aws4-1.8.0.tgz",[m
 			"integrity": "sha512-ReZxvNHIOv88FlT7rxcXIIC0fPt4KZqZbOlivyWtXLt8ESx84zd3kMC6iK5jVeS2qt+g7ftS7ye4fi06X5rtRQ==",[m
 			"dev": true[m
 		},[m
 		"babel": {[m
 			"version": "6.23.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel/-/babel-6.23.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel/-/babel-6.23.0.tgz",[m
 			"integrity": "sha1-0NHn2APpdHZb7qMjLU4VPA77kPQ=",[m
 			"dev": true[m
 		},[m
 		"babel-cli": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-cli/-/babel-cli-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-cli/-/babel-cli-6.26.0.tgz",[m
 			"integrity": "sha1-UCq1SHTX24itALiHoGODzgPQAvE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -531,7 +531,7 @@[m
 		},[m
 		"babel-code-frame": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-code-frame/-/babel-code-frame-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-code-frame/-/babel-code-frame-6.26.0.tgz",[m
 			"integrity": "sha1-Y/1D99weO7fONZR9uP42mj9Yx0s=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -542,7 +542,7 @@[m
 		},[m
 		"babel-core": {[m
 			"version": "6.26.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-core/-/babel-core-6.26.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-core/-/babel-core-6.26.3.tgz",[m
 			"integrity": "sha512-6jyFLuDmeidKmUEb3NM+/yawG0M2bDZ9Z1qbZP59cyHLz8kYGKYwpJP0UwUKKUiTRNvxfLesJnTedqczP7cTDA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -569,7 +569,7 @@[m
 		},[m
 		"babel-generator": {[m
 			"version": "6.26.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-generator/-/babel-generator-6.26.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-generator/-/babel-generator-6.26.1.tgz",[m
 			"integrity": "sha512-HyfwY6ApZj7BYTcJURpM5tznulaBvyio7/0d4zFOeMPUmfxkCjHocCuoLa2SAGzBI8AREcH3eP3758F672DppA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -585,7 +585,7 @@[m
 		},[m
 		"babel-helper-builder-binary-assignment-operator-visitor": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helper-builder-binary-assignment-operator-visitor/-/babel-helper-builder-binary-assignment-operator-visitor-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helper-builder-binary-assignment-operator-visitor/-/babel-helper-builder-binary-assignment-operator-visitor-6.24.1.tgz",[m
 			"integrity": "sha1-zORReto1b0IgvK6KAsKzRvmlZmQ=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -596,7 +596,7 @@[m
 		},[m
 		"babel-helper-call-delegate": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helper-call-delegate/-/babel-helper-call-delegate-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helper-call-delegate/-/babel-helper-call-delegate-6.24.1.tgz",[m
 			"integrity": "sha1-7Oaqzdx25Bw0YfiL/Fdb0Nqi340=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -608,7 +608,7 @@[m
 		},[m
 		"babel-helper-define-map": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helper-define-map/-/babel-helper-define-map-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helper-define-map/-/babel-helper-define-map-6.26.0.tgz",[m
 			"integrity": "sha1-pfVtq0GiX5fstJjH66ypgZ+Vvl8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -620,7 +620,7 @@[m
 		},[m
 		"babel-helper-explode-assignable-expression": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helper-explode-assignable-expression/-/babel-helper-explode-assignable-expression-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helper-explode-assignable-expression/-/babel-helper-explode-assignable-expression-6.24.1.tgz",[m
 			"integrity": "sha1-8luCz33BBDPFX3BZLVdGQArCLKo=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -631,7 +631,7 @@[m
 		},[m
 		"babel-helper-function-name": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helper-function-name/-/babel-helper-function-name-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helper-function-name/-/babel-helper-function-name-6.24.1.tgz",[m
 			"integrity": "sha1-00dbjAPtmCQqJbSDUasYOZ01gKk=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -644,7 +644,7 @@[m
 		},[m
 		"babel-helper-get-function-arity": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helper-get-function-arity/-/babel-helper-get-function-arity-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helper-get-function-arity/-/babel-helper-get-function-arity-6.24.1.tgz",[m
 			"integrity": "sha1-j3eCqpNAfEHTqlCQj4mwMbG2hT0=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -654,7 +654,7 @@[m
 		},[m
 		"babel-helper-hoist-variables": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helper-hoist-variables/-/babel-helper-hoist-variables-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helper-hoist-variables/-/babel-helper-hoist-variables-6.24.1.tgz",[m
 			"integrity": "sha1-HssnaJydJVE+rbyZFKc/VAi+enY=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -664,7 +664,7 @@[m
 		},[m
 		"babel-helper-optimise-call-expression": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helper-optimise-call-expression/-/babel-helper-optimise-call-expression-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helper-optimise-call-expression/-/babel-helper-optimise-call-expression-6.24.1.tgz",[m
 			"integrity": "sha1-96E0J7qfc/j0+pk8VKl4gtEkQlc=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -674,7 +674,7 @@[m
 		},[m
 		"babel-helper-regex": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helper-regex/-/babel-helper-regex-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helper-regex/-/babel-helper-regex-6.26.0.tgz",[m
 			"integrity": "sha1-MlxZ+QL4LyS3T6zu0DY5VPZJXnI=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -685,7 +685,7 @@[m
 		},[m
 		"babel-helper-remap-async-to-generator": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helper-remap-async-to-generator/-/babel-helper-remap-async-to-generator-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helper-remap-async-to-generator/-/babel-helper-remap-async-to-generator-6.24.1.tgz",[m
 			"integrity": "sha1-XsWBgnrXI/7N04HxySg5BnbkVRs=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -698,7 +698,7 @@[m
 		},[m
 		"babel-helper-replace-supers": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helper-replace-supers/-/babel-helper-replace-supers-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helper-replace-supers/-/babel-helper-replace-supers-6.24.1.tgz",[m
 			"integrity": "sha1-v22/5Dk40XNpohPKiov3S2qQqxo=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -712,7 +712,7 @@[m
 		},[m
 		"babel-helpers": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-helpers/-/babel-helpers-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-helpers/-/babel-helpers-6.24.1.tgz",[m
 			"integrity": "sha1-NHHenK7DiOXIUOWX5Yom3fN2ArI=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -722,7 +722,7 @@[m
 		},[m
 		"babel-loader": {[m
 			"version": "7.1.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-loader/-/babel-loader-7.1.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-loader/-/babel-loader-7.1.5.tgz",[m
 			"integrity": "sha512-iCHfbieL5d1LfOQeeVJEUyD9rTwBcP/fcEbRCfempxTDuqrKpu0AZjLAQHEQa3Yqyj9ORKe2iHfoj4rHLf7xpw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -733,7 +733,7 @@[m
 		},[m
 		"babel-messages": {[m
 			"version": "6.23.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-messages/-/babel-messages-6.23.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-messages/-/babel-messages-6.23.0.tgz",[m
 			"integrity": "sha1-8830cDhYA1sqKVHG7F7fbGLyYw4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -742,7 +742,7 @@[m
 		},[m
 		"babel-plugin-check-es2015-constants": {[m
 			"version": "6.22.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-check-es2015-constants/-/babel-plugin-check-es2015-constants-6.22.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-check-es2015-constants/-/babel-plugin-check-es2015-constants-6.22.0.tgz",[m
 			"integrity": "sha1-NRV7EBQm/S/9PaP3XH0ekYNbv4o=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -751,25 +751,25 @@[m
 		},[m
 		"babel-plugin-syntax-async-functions": {[m
 			"version": "6.13.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-syntax-async-functions/-/babel-plugin-syntax-async-functions-6.13.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/babel-plugin-syntax-async-functions/-/babel-plugin-syntax-async-functions-6.13.0.tgz",[m
 			"integrity": "sha1-ytnK0RkbWtY0vzCuCHI5HgZHvpU=",[m
 			"dev": true[m
 		},[m
 		"babel-plugin-syntax-exponentiation-operator": {[m
 			"version": "6.13.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-syntax-exponentiation-operator/-/babel-plugin-syntax-exponentiation-operator-6.13.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/babel-plugin-syntax-exponentiation-operator/-/babel-plugin-syntax-exponentiation-operator-6.13.0.tgz",[m
 			"integrity": "sha1-nufoM3KQ2pUoggGmpX9BcDF4MN4=",[m
 			"dev": true[m
 		},[m
 		"babel-plugin-syntax-trailing-function-commas": {[m
 			"version": "6.22.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-syntax-trailing-function-commas/-/babel-plugin-syntax-trailing-function-commas-6.22.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-syntax-trailing-function-commas/-/babel-plugin-syntax-trailing-function-commas-6.22.0.tgz",[m
 			"integrity": "sha1-ugNgk3+NBuQBgKQ/4NVhb/9TLPM=",[m
 			"dev": true[m
 		},[m
 		"babel-plugin-transform-async-to-generator": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-async-to-generator/-/babel-plugin-transform-async-to-generator-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-async-to-generator/-/babel-plugin-transform-async-to-generator-6.24.1.tgz",[m
 			"integrity": "sha1-ZTbjeK/2yx1VF6wOQOs+n8jQh2E=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -780,7 +780,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-arrow-functions": {[m
 			"version": "6.22.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-arrow-functions/-/babel-plugin-transform-es2015-arrow-functions-6.22.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-arrow-functions/-/babel-plugin-transform-es2015-arrow-functions-6.22.0.tgz",[m
 			"integrity": "sha1-RSaSy3EdX3ncf4XkQM5BufJE0iE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -789,7 +789,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-block-scoped-functions": {[m
 			"version": "6.22.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-block-scoped-functions/-/babel-plugin-transform-es2015-block-scoped-functions-6.22.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-block-scoped-functions/-/babel-plugin-transform-es2015-block-scoped-functions-6.22.0.tgz",[m
 			"integrity": "sha1-u8UbSflk1wy42OC5ToICRs46YUE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -798,7 +798,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-block-scoping": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-block-scoping/-/babel-plugin-transform-es2015-block-scoping-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-block-scoping/-/babel-plugin-transform-es2015-block-scoping-6.26.0.tgz",[m
 			"integrity": "sha1-1w9SmcEwjQXBL0Y4E7CgnnOxiV8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -811,7 +811,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-classes": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-classes/-/babel-plugin-transform-es2015-classes-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-classes/-/babel-plugin-transform-es2015-classes-6.24.1.tgz",[m
 			"integrity": "sha1-WkxYpQyclGHlZLSyo7+ryXolhNs=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -828,7 +828,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-computed-properties": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-computed-properties/-/babel-plugin-transform-es2015-computed-properties-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-computed-properties/-/babel-plugin-transform-es2015-computed-properties-6.24.1.tgz",[m
 			"integrity": "sha1-b+Ko0WiV1WNPTNmZttNICjCBWbM=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -838,7 +838,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-destructuring": {[m
 			"version": "6.23.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-destructuring/-/babel-plugin-transform-es2015-destructuring-6.23.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-destructuring/-/babel-plugin-transform-es2015-destructuring-6.23.0.tgz",[m
 			"integrity": "sha1-mXux8auWf2gtKwh2/jWNYOdlxW0=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -847,7 +847,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-duplicate-keys": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-duplicate-keys/-/babel-plugin-transform-es2015-duplicate-keys-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-duplicate-keys/-/babel-plugin-transform-es2015-duplicate-keys-6.24.1.tgz",[m
 			"integrity": "sha1-c+s9MQypaePvnskcU3QabxV2Qj4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -857,7 +857,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-for-of": {[m
 			"version": "6.23.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-for-of/-/babel-plugin-transform-es2015-for-of-6.23.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-for-of/-/babel-plugin-transform-es2015-for-of-6.23.0.tgz",[m
 			"integrity": "sha1-9HyVsrYT3x0+zC/bdXNiPHUkhpE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -866,7 +866,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-function-name": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-function-name/-/babel-plugin-transform-es2015-function-name-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-function-name/-/babel-plugin-transform-es2015-function-name-6.24.1.tgz",[m
 			"integrity": "sha1-g0yJhTvDaxrw86TF26qU/Y6sqos=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -877,7 +877,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-literals": {[m
 			"version": "6.22.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-literals/-/babel-plugin-transform-es2015-literals-6.22.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-literals/-/babel-plugin-transform-es2015-literals-6.22.0.tgz",[m
 			"integrity": "sha1-T1SgLWzWbPkVKAAZox0xklN3yi4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -886,7 +886,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-modules-amd": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-modules-amd/-/babel-plugin-transform-es2015-modules-amd-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-modules-amd/-/babel-plugin-transform-es2015-modules-amd-6.24.1.tgz",[m
 			"integrity": "sha1-Oz5UAXI5hC1tGcMBHEvS8AoA0VQ=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -897,7 +897,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-modules-commonjs": {[m
 			"version": "6.26.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-modules-commonjs/-/babel-plugin-transform-es2015-modules-commonjs-6.26.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-modules-commonjs/-/babel-plugin-transform-es2015-modules-commonjs-6.26.2.tgz",[m
 			"integrity": "sha512-CV9ROOHEdrjcwhIaJNBGMBCodN+1cfkwtM1SbUHmvyy35KGT7fohbpOxkE2uLz1o6odKK2Ck/tz47z+VqQfi9Q==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -909,7 +909,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-modules-systemjs": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-modules-systemjs/-/babel-plugin-transform-es2015-modules-systemjs-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-modules-systemjs/-/babel-plugin-transform-es2015-modules-systemjs-6.24.1.tgz",[m
 			"integrity": "sha1-/4mhQrkRmpBhlfXxBuzzBdlAfSM=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -920,7 +920,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-modules-umd": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-modules-umd/-/babel-plugin-transform-es2015-modules-umd-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-modules-umd/-/babel-plugin-transform-es2015-modules-umd-6.24.1.tgz",[m
 			"integrity": "sha1-rJl+YoXNGO1hdq22B9YCNErThGg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -931,7 +931,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-object-super": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-object-super/-/babel-plugin-transform-es2015-object-super-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-object-super/-/babel-plugin-transform-es2015-object-super-6.24.1.tgz",[m
 			"integrity": "sha1-JM72muIcuDp/hgPa0CH1cusnj40=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -941,7 +941,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-parameters": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-parameters/-/babel-plugin-transform-es2015-parameters-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-parameters/-/babel-plugin-transform-es2015-parameters-6.24.1.tgz",[m
 			"integrity": "sha1-V6w1GrScrxSpfNE7CfZv3wpiXys=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -955,7 +955,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-shorthand-properties": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-shorthand-properties/-/babel-plugin-transform-es2015-shorthand-properties-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-shorthand-properties/-/babel-plugin-transform-es2015-shorthand-properties-6.24.1.tgz",[m
 			"integrity": "sha1-JPh11nIch2YbvZmkYi5R8U3jiqA=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -965,7 +965,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-spread": {[m
 			"version": "6.22.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-spread/-/babel-plugin-transform-es2015-spread-6.22.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-spread/-/babel-plugin-transform-es2015-spread-6.22.0.tgz",[m
 			"integrity": "sha1-1taKmfia7cRTbIGlQujdnxdG+NE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -974,7 +974,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-sticky-regex": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-sticky-regex/-/babel-plugin-transform-es2015-sticky-regex-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-sticky-regex/-/babel-plugin-transform-es2015-sticky-regex-6.24.1.tgz",[m
 			"integrity": "sha1-AMHNsaynERLN8M9hJsLta0V8zbw=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -985,7 +985,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-template-literals": {[m
 			"version": "6.22.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-template-literals/-/babel-plugin-transform-es2015-template-literals-6.22.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-template-literals/-/babel-plugin-transform-es2015-template-literals-6.22.0.tgz",[m
 			"integrity": "sha1-qEs0UPfp+PH2g51taH2oS7EjbY0=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -994,7 +994,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-typeof-symbol": {[m
 			"version": "6.23.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-typeof-symbol/-/babel-plugin-transform-es2015-typeof-symbol-6.23.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-typeof-symbol/-/babel-plugin-transform-es2015-typeof-symbol-6.23.0.tgz",[m
 			"integrity": "sha1-3sCfHN3/lLUqxz1QXITfWdzOs3I=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1003,7 +1003,7 @@[m
 		},[m
 		"babel-plugin-transform-es2015-unicode-regex": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-es2015-unicode-regex/-/babel-plugin-transform-es2015-unicode-regex-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-es2015-unicode-regex/-/babel-plugin-transform-es2015-unicode-regex-6.24.1.tgz",[m
 			"integrity": "sha1-04sS9C6nMj9yk4fxinxa4frrNek=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1014,7 +1014,7 @@[m
 		},[m
 		"babel-plugin-transform-exponentiation-operator": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-exponentiation-operator/-/babel-plugin-transform-exponentiation-operator-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-exponentiation-operator/-/babel-plugin-transform-exponentiation-operator-6.24.1.tgz",[m
 			"integrity": "sha1-KrDJx/MJj6SJB3cruBP+QejeOg4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1025,7 +1025,7 @@[m
 		},[m
 		"babel-plugin-transform-regenerator": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-regenerator/-/babel-plugin-transform-regenerator-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-regenerator/-/babel-plugin-transform-regenerator-6.26.0.tgz",[m
 			"integrity": "sha1-4HA2lvveJ/Cj78rPi03KL3s6jy8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1034,7 +1034,7 @@[m
 		},[m
 		"babel-plugin-transform-strict-mode": {[m
 			"version": "6.24.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-plugin-transform-strict-mode/-/babel-plugin-transform-strict-mode-6.24.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-plugin-transform-strict-mode/-/babel-plugin-transform-strict-mode-6.24.1.tgz",[m
 			"integrity": "sha1-1fr3qleKZbvlkc9e2uBKDGcCB1g=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1044,7 +1044,7 @@[m
 		},[m
 		"babel-polyfill": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-polyfill/-/babel-polyfill-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-polyfill/-/babel-polyfill-6.26.0.tgz",[m
 			"integrity": "sha1-N5k3q8Z9eJWXCtxiHyhM2WbPIVM=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1055,7 +1055,7 @@[m
 			"dependencies": {[m
 				"regenerator-runtime": {[m
 					"version": "0.10.5",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/regenerator-runtime/-/regenerator-runtime-0.10.5.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/regenerator-runtime/-/regenerator-runtime-0.10.5.tgz",[m
 					"integrity": "sha1-M2w+/BIgrc7dosn6tntaeVWjNlg=",[m
 					"dev": true[m
 				}[m
[36m@@ -1063,7 +1063,7 @@[m
 		},[m
 		"babel-preset-env": {[m
 			"version": "1.7.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-preset-env/-/babel-preset-env-1.7.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-preset-env/-/babel-preset-env-1.7.0.tgz",[m
 			"integrity": "sha512-9OR2afuKDneX2/q2EurSftUYM0xGu4O2D9adAhVfADDhrYDaxXV0rBbevVYoY9n6nyX1PmQW/0jtpJvUNr9CHg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1101,7 +1101,7 @@[m
 		},[m
 		"babel-register": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-register/-/babel-register-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-register/-/babel-register-6.26.0.tgz",[m
 			"integrity": "sha1-btAhFz4vy0htestFxgCahW9kcHE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1116,7 +1116,7 @@[m
 		},[m
 		"babel-runtime": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-runtime/-/babel-runtime-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-runtime/-/babel-runtime-6.26.0.tgz",[m
 			"integrity": "sha1-llxwWGaOgrVde/4E/yM3vItWR/4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1126,7 +1126,7 @@[m
 		},[m
 		"babel-template": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-template/-/babel-template-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-template/-/babel-template-6.26.0.tgz",[m
 			"integrity": "sha1-3gPi0WOWsGn0bdn/+FIfsaDjXgI=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1139,7 +1139,7 @@[m
 		},[m
 		"babel-traverse": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-traverse/-/babel-traverse-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-traverse/-/babel-traverse-6.26.0.tgz",[m
 			"integrity": "sha1-RqnL1+3MYsjlwGTi0tjQ9ANXZu4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1156,7 +1156,7 @@[m
 		},[m
 		"babel-types": {[m
 			"version": "6.26.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babel-types/-/babel-types-6.26.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babel-types/-/babel-types-6.26.0.tgz",[m
 			"integrity": "sha1-o7Bz+Uq0nrb6Vc1lInozQ4BjJJc=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1180,19 +1180,19 @@[m
 		},[m
 		"babylon": {[m
 			"version": "6.18.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/babylon/-/babylon-6.18.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/babylon/-/babylon-6.18.0.tgz",[m
 			"integrity": "sha512-q/UEjfGJ2Cm3oKV71DJz9d25TPnq5rhBVL2Q4fA5wcC3jcrdn7+SssEybFIxwAvvP+YCsCYNKughoF33GxgycQ==",[m
 			"dev": true[m
 		},[m
 		"balanced-match": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/balanced-match/-/balanced-match-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/balanced-match/-/balanced-match-1.0.0.tgz",[m
 			"integrity": "sha1-ibTRmasr7kneFk6gK4nORi1xt2c=",[m
 			"dev": true[m
 		},[m
 		"base": {[m
 			"version": "0.11.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/base/-/base-0.11.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/base/-/base-0.11.2.tgz",[m
 			"integrity": "sha512-5T6P4xPgpp0YDFvSWwEZ4NoE3aM4QBQXDzmVbraCkFj8zHM+mba8SyqB5DbZWyR7mYHo6Y7BdQo3MoA4m0TeQg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1207,7 +1207,7 @@[m
 			"dependencies": {[m
 				"define-property": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-1.0.0.tgz",[m
 					"integrity": "sha1-dp66rz9KY6rTr56NMEybvnm/sOY=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -1216,7 +1216,7 @@[m
 				},[m
 				"is-accessor-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-m5hnHTkcVsPfqx3AKlyttIPb7J+XykHvJP2B9bZDjlhLIoEq4XoK64Vg7boZlVWYK6LUY94dYPEE7Lh0ZkZKcQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -1225,7 +1225,7 @@[m
 				},[m
 				"is-data-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-jbRXy1FmtAoCjQkVmIVYwuuqDFUbaOeDjmed1tOGPrsMhtJA4rD9tkgA0F1qJ3gRFRXcHYVkdeaP50Q5rE/jLQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -1234,7 +1234,7 @@[m
 				},[m
 				"is-descriptor": {[m
 					"version": "1.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
 					"integrity": "sha512-2eis5WqQGV7peooDyLmNEPUrps9+SXX5c9pL3xEB+4e9HnGuDa7mB7kHxHw4CbqS9k1T2hOH3miL8n8WtiYVtg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -1245,13 +1245,13 @@[m
 				},[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				},[m
 				"kind-of": {[m
 					"version": "6.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-6.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-6.0.2.tgz",[m
 					"integrity": "sha512-s5kLOcnH0XqDO+FvuaLX8DDjZ18CGFk7VygH40QoKPUQhW4e2rvM0rwUq0t8IQDOwYSeLK01U90OjzBTme2QqA==",[m
 					"dev": true[m
 				}[m
[36m@@ -1259,13 +1259,13 @@[m
 		},[m
 		"base64-js": {[m
 			"version": "1.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/base64-js/-/base64-js-1.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/base64-js/-/base64-js-1.3.0.tgz",[m
 			"integrity": "sha512-ccav/yGvoa80BQDljCxsmmQ3Xvx60/UpBIij5QN21W3wBi/hhIC9OoO+KLpu9IJTS9j4DRVJ3aDDF9cMSoa2lw==",[m
 			"dev": true[m
 		},[m
 		"bcrypt-pbkdf": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/bcrypt-pbkdf/-/bcrypt-pbkdf-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/bcrypt-pbkdf/-/bcrypt-pbkdf-1.0.2.tgz",[m
 			"integrity": "sha1-pDAdOJtqQ/m2f/PKEaP2Y342Dp4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1274,7 +1274,7 @@[m
 		},[m
 		"bcryptjs": {[m
 			"version": "2.4.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/bcryptjs/-/bcryptjs-2.4.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/bcryptjs/-/bcryptjs-2.4.3.tgz",[m
 			"integrity": "sha1-mrVie5PmBiH/fNrF2pczAn3x0Ms="[m
 		},[m
 		"big.js": {[m
[36m@@ -1289,15 +1289,6 @@[m
 			"integrity": "sha512-Un7MIEDdUC5gNpcGDV97op1Ywk748MpHcFTHoYs6qnj1Z3j7I53VG3nwZhKzoBZmbdRNnb6WRdFlwl7tSDuZGw==",[m
 			"dev": true[m
 		},[m
[31m-		"block-stream": {[m
[31m-			"version": "0.0.9",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/block-stream/-/block-stream-0.0.9.tgz",[m
[31m-			"integrity": "sha1-E+v+d4oDIFz+A3UUgeu0szAMEmo=",[m
[31m-			"dev": true,[m
[31m-			"requires": {[m
[31m-				"inherits": "~2.0.0"[m
[31m-			}[m
[31m-		},[m
 		"bluebird": {[m
 			"version": "3.5.4",[m
 			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/bluebird/-/bluebird-3.5.4.tgz",[m
[36m@@ -1306,7 +1297,7 @@[m
 		},[m
 		"bn.js": {[m
 			"version": "4.11.8",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/bn.js/-/bn.js-4.11.8.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/bn.js/-/bn.js-4.11.8.tgz",[m
 			"integrity": "sha512-ItfYfPLkWHUjckQCk8xC+LwxgK8NYcXywGigJgSwOP8Y2iyWT4f2vsZnoOXTTbo+o5yXmIUJ4gn5538SO5S3gA==",[m
 			"dev": true[m
 		},[m
[36m@@ -1329,13 +1320,13 @@[m
 		},[m
 		"boolbase": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/boolbase/-/boolbase-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/boolbase/-/boolbase-1.0.0.tgz",[m
 			"integrity": "sha1-aN/1++YMUes3cl6p4+0xDcwed24=",[m
 			"dev": true[m
 		},[m
 		"brace-expansion": {[m
 			"version": "1.1.11",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/brace-expansion/-/brace-expansion-1.1.11.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/brace-expansion/-/brace-expansion-1.1.11.tgz",[m
 			"integrity": "sha512-iCuPHDFgrHX7H2vEI/5xpz07zSHB00TpugqhmYtVmMO6518mCuRMoOYFldEBl0g187ufozdaHgWKcYFb61qGiA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1345,7 +1336,7 @@[m
 		},[m
 		"braces": {[m
 			"version": "1.8.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/braces/-/braces-1.8.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/braces/-/braces-1.8.5.tgz",[m
 			"integrity": "sha1-uneWLhLf+WnWt2cR6RS3N4V79qc=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1356,13 +1347,13 @@[m
 		},[m
 		"brorand": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/brorand/-/brorand-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/brorand/-/brorand-1.1.0.tgz",[m
 			"integrity": "sha1-EsJe/kCkXjwyPrhnWgoM5XsiNx8=",[m
 			"dev": true[m
 		},[m
 		"browserify-aes": {[m
 			"version": "1.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/browserify-aes/-/browserify-aes-1.2.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/browserify-aes/-/browserify-aes-1.2.0.tgz",[m
 			"integrity": "sha512-+7CHXqGuspUn/Sl5aO7Ea0xWGAtETPXNSAjHo48JfLdPWcMng33Xe4znFvQweqc/uzk5zSOI3H52CYnjCfb5hA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1376,7 +1367,7 @@[m
 		},[m
 		"browserify-cipher": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/browserify-cipher/-/browserify-cipher-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/browserify-cipher/-/browserify-cipher-1.0.1.tgz",[m
 			"integrity": "sha512-sPhkz0ARKbf4rRQt2hTpAHqn47X3llLkUGn+xEJzLjwY8LRs2p0v7ljvI5EyoRO/mexrNunNECisZs+gw2zz1w==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1387,7 +1378,7 @@[m
 		},[m
 		"browserify-des": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/browserify-des/-/browserify-des-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/browserify-des/-/browserify-des-1.0.2.tgz",[m
 			"integrity": "sha512-BioO1xf3hFwz4kc6iBhI3ieDFompMhrMlnDFC4/0/vd5MokpuAc3R+LYbwTA9A5Yc9pq9UYPqffKpW2ObuwX5A==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1399,7 +1390,7 @@[m
 		},[m
 		"browserify-rsa": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/browserify-rsa/-/browserify-rsa-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/browserify-rsa/-/browserify-rsa-4.0.1.tgz",[m
 			"integrity": "sha1-IeCr+vbyApzy+vsTNWenAdQTVSQ=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1409,7 +1400,7 @@[m
 		},[m
 		"browserify-sign": {[m
 			"version": "4.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/browserify-sign/-/browserify-sign-4.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/browserify-sign/-/browserify-sign-4.0.4.tgz",[m
 			"integrity": "sha1-qk62jl17ZYuqa/alfmMMvXqT0pg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1424,7 +1415,7 @@[m
 		},[m
 		"browserify-zlib": {[m
 			"version": "0.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/browserify-zlib/-/browserify-zlib-0.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/browserify-zlib/-/browserify-zlib-0.2.0.tgz",[m
 			"integrity": "sha512-Z942RysHXmJrhqk88FmKBVq/v5tqmSkDz7p54G/MGyjMnCFFnC79XWNbg+Vta8W6Wb2qtSZTSxIGkJrRpCFEiA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1433,7 +1424,7 @@[m
 		},[m
 		"browserslist": {[m
 			"version": "3.2.8",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/browserslist/-/browserslist-3.2.8.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/browserslist/-/browserslist-3.2.8.tgz",[m
 			"integrity": "sha512-WHVocJYavUwVgVViC0ORikPHQquXwVh939TaelZ4WDqpWgTX/FsGhl/+P4qBUAGcRvtOgDgC+xftNWWp2RUTAQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1460,30 +1451,30 @@[m
 		},[m
 		"buffer-equal-constant-time": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/buffer-equal-constant-time/-/buffer-equal-constant-time-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/buffer-equal-constant-time/-/buffer-equal-constant-time-1.0.1.tgz",[m
 			"integrity": "sha1-+OcRMvf/5uAaXJaXpMbz5I1cyBk="[m
 		},[m
 		"buffer-from": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/buffer-from/-/buffer-from-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/buffer-from/-/buffer-from-1.1.1.tgz",[m
 			"integrity": "sha512-MQcXEUbCKtEo7bhqEs6560Hyd4XaovZlO/k9V3hjVUF/zwW7KBVdSK4gIt/bzwS9MbR5qob+F5jusZsb0YQK2A==",[m
 			"dev": true[m
 		},[m
 		"buffer-xor": {[m
 			"version": "1.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/buffer-xor/-/buffer-xor-1.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/buffer-xor/-/buffer-xor-1.0.3.tgz",[m
 			"integrity": "sha1-JuYe0UIvtw3ULm42cp7VHYVf6Nk=",[m
 			"dev": true[m
 		},[m
 		"builtin-status-codes": {[m
 			"version": "3.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/builtin-status-codes/-/builtin-status-codes-3.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/builtin-status-codes/-/builtin-status-codes-3.0.0.tgz",[m
 			"integrity": "sha1-hZgoeOIbmOHGZCXgPQF0eI9Wnug=",[m
 			"dev": true[m
 		},[m
 		"busboy": {[m
 			"version": "0.2.14",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/busboy/-/busboy-0.2.14.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/busboy/-/busboy-0.2.14.tgz",[m
 			"integrity": "sha1-bCpiLvz0fFe7vh4qnDetNseSVFM=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1493,13 +1484,13 @@[m
 			"dependencies": {[m
 				"isarray": {[m
 					"version": "0.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isarray/-/isarray-0.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isarray/-/isarray-0.0.1.tgz",[m
 					"integrity": "sha1-ihis/Kmo9Bd+Cav8YDiTmwXR7t8=",[m
 					"dev": true[m
 				},[m
 				"readable-stream": {[m
 					"version": "1.1.14",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/readable-stream/-/readable-stream-1.1.14.tgz",[m
[32m+[m					[32m"resolved": "http://registry.npmjs.org/readable-stream/-/readable-stream-1.1.14.tgz",[m
 					"integrity": "sha1-fPTFTvZI44EwhMY23SB54WbAgdk=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -1511,7 +1502,7 @@[m
 				},[m
 				"string_decoder": {[m
 					"version": "0.10.31",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/string_decoder/-/string_decoder-0.10.31.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/string_decoder/-/string_decoder-0.10.31.tgz",[m
 					"integrity": "sha1-YuIDvEF2bGwoyfyEMB2rHFMQ+pQ=",[m
 					"dev": true[m
 				}[m
[36m@@ -1524,7 +1515,7 @@[m
 		},[m
 		"cacache": {[m
 			"version": "10.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cacache/-/cacache-10.0.4.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/cacache/-/cacache-10.0.4.tgz",[m
 			"integrity": "sha512-Dph0MzuH+rTQzGPNT9fAnrPmMmjKfST6trxJeK7NQuHRaVw24VzPRWTmg9MpcwOVQZO0E1FBICUlFeNaKPIfHA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1545,7 +1536,7 @@[m
 		},[m
 		"cache-base": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cache-base/-/cache-base-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cache-base/-/cache-base-1.0.1.tgz",[m
 			"integrity": "sha512-AKcdTnFSWATd5/GCPRxr2ChwIJ85CeyrEyjRHlKxQ56d4XJMGym0uAiKn0xbLOGOl3+yRpOTi484dVCEc5AUzQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1562,7 +1553,7 @@[m
 			"dependencies": {[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				}[m
[36m@@ -1570,7 +1561,7 @@[m
 		},[m
 		"caller-callsite": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/caller-callsite/-/caller-callsite-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/caller-callsite/-/caller-callsite-2.0.0.tgz",[m
 			"integrity": "sha1-hH4PzgoiN1CpoCfFSzNzGtMVQTQ=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1579,7 +1570,7 @@[m
 		},[m
 		"caller-path": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/caller-path/-/caller-path-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/caller-path/-/caller-path-2.0.0.tgz",[m
 			"integrity": "sha1-Ro+DBE42mrIBD6xfBs7uFbsssfQ=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1588,19 +1579,19 @@[m
 		},[m
 		"callsites": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/callsites/-/callsites-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/callsites/-/callsites-2.0.0.tgz",[m
 			"integrity": "sha1-BuuE8A7qQT2oav/vrL/7Ngk7PFA=",[m
 			"dev": true[m
 		},[m
 		"camelcase": {[m
 			"version": "2.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/camelcase/-/camelcase-2.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/camelcase/-/camelcase-2.1.1.tgz",[m
 			"integrity": "sha1-fB0W1nmhu+WcoCys7PsBHiAfWh8=",[m
 			"dev": true[m
 		},[m
 		"camelcase-keys": {[m
 			"version": "2.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/camelcase-keys/-/camelcase-keys-2.1.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/camelcase-keys/-/camelcase-keys-2.1.0.tgz",[m
 			"integrity": "sha1-MIvur/3ygRkFHvodkyITyRuPkuc=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1610,7 +1601,7 @@[m
 		},[m
 		"caniuse-api": {[m
 			"version": "3.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/caniuse-api/-/caniuse-api-3.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/caniuse-api/-/caniuse-api-3.0.0.tgz",[m
 			"integrity": "sha512-bsTwuIg/BZZK/vreVTYYbSWoe2F+71P7K5QGEX+pT250DZbfU1MQ5prOKpPR+LL6uWKK3KMwMCAS74QB3Um1uw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1641,13 +1632,13 @@[m
 		},[m
 		"caseless": {[m
 			"version": "0.12.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/caseless/-/caseless-0.12.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/caseless/-/caseless-0.12.0.tgz",[m
 			"integrity": "sha1-G2gcIf+EAzyCZUMJBolCDRhxUdw=",[m
 			"dev": true[m
 		},[m
 		"chalk": {[m
 			"version": "1.1.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/chalk/-/chalk-1.1.3.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/chalk/-/chalk-1.1.3.tgz",[m
 			"integrity": "sha1-qBFcVeSnAv5NFQq9OHKCKn4J/Jg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1660,7 +1651,7 @@[m
 		},[m
 		"chokidar": {[m
 			"version": "1.7.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/chokidar/-/chokidar-1.7.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/chokidar/-/chokidar-1.7.0.tgz",[m
 			"integrity": "sha1-eY5ol3gVHIB2tLNg5e3SjNortGg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1677,13 +1668,13 @@[m
 		},[m
 		"chownr": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/chownr/-/chownr-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/chownr/-/chownr-1.1.1.tgz",[m
 			"integrity": "sha512-j38EvO5+LHX84jlo6h4UzmOwi0UgW61WRyPtJz4qaadK5eY3BTS5TY/S1Stc3Uk2lIM6TPevAlULiEJwie860g==",[m
 			"dev": true[m
 		},[m
 		"chrome-trace-event": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/chrome-trace-event/-/chrome-trace-event-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/chrome-trace-event/-/chrome-trace-event-1.0.0.tgz",[m
 			"integrity": "sha512-xDbVgyfDTT2piup/h8dK/y4QZfJRSa73bw1WZ8b4XM1o7fsFubUVGYcE+1ANtOzJJELGpYoG2961z0Z6OAld9A==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1692,7 +1683,7 @@[m
 		},[m
 		"cipher-base": {[m
 			"version": "1.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cipher-base/-/cipher-base-1.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cipher-base/-/cipher-base-1.0.4.tgz",[m
 			"integrity": "sha512-Kkht5ye6ZGmwv40uUDZztayT2ThLQGfnj/T71N/XzeZeo3nf8foyW7zGTsPYkEya3m5f3cAypH+qe7YOrM1U2Q==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1702,7 +1693,7 @@[m
 		},[m
 		"class-utils": {[m
 			"version": "0.3.6",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/class-utils/-/class-utils-0.3.6.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/class-utils/-/class-utils-0.3.6.tgz",[m
 			"integrity": "sha512-qOhPa/Fj7s6TY8H8esGu5QNpMMQxz79h+urzrNYN6mn+9BnxlDGf5QZ+XeCDsxSjPqsSR56XOZOJmpeurnLMeg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1714,7 +1705,7 @@[m
 			"dependencies": {[m
 				"define-property": {[m
 					"version": "0.2.5",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-0.2.5.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-0.2.5.tgz",[m
 					"integrity": "sha1-w1se+RjsPJkPmlvFe+BKrOxcgRY=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -1723,7 +1714,7 @@[m
 				},[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				}[m
[36m@@ -1731,7 +1722,7 @@[m
 		},[m
 		"cliui": {[m
 			"version": "3.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cliui/-/cliui-3.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cliui/-/cliui-3.2.0.tgz",[m
 			"integrity": "sha1-EgYBU3qRbSmUD5NNo7SNWFo5IT0=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1742,7 +1733,7 @@[m
 		},[m
 		"clone-deep": {[m
 			"version": "2.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/clone-deep/-/clone-deep-2.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/clone-deep/-/clone-deep-2.0.2.tgz",[m
 			"integrity": "sha512-SZegPTKjCgpQH63E+eN6mVEEPdQBOUzjyJm5Pora4lrwWRFS8I0QAxV/KD6vV/i0WuijHZWQC1fMsPEdxfdVCQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1754,7 +1745,7 @@[m
 			"dependencies": {[m
 				"for-own": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/for-own/-/for-own-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/for-own/-/for-own-1.0.0.tgz",[m
 					"integrity": "sha1-xjMy9BXO3EsE2/5wz4NklMU8tEs=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -1763,7 +1754,7 @@[m
 				},[m
 				"kind-of": {[m
 					"version": "6.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-6.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-6.0.2.tgz",[m
 					"integrity": "sha512-s5kLOcnH0XqDO+FvuaLX8DDjZ18CGFk7VygH40QoKPUQhW4e2rvM0rwUq0t8IQDOwYSeLK01U90OjzBTme2QqA==",[m
 					"dev": true[m
 				}[m
[36m@@ -1813,13 +1804,13 @@[m
 		},[m
 		"code-point-at": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/code-point-at/-/code-point-at-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/code-point-at/-/code-point-at-1.1.0.tgz",[m
 			"integrity": "sha1-DQcLTQQ6W+ozovGkDi7bPZpMz3c=",[m
 			"dev": true[m
 		},[m
 		"collection-visit": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/collection-visit/-/collection-visit-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/collection-visit/-/collection-visit-1.0.0.tgz",[m
 			"integrity": "sha1-S8A3PBZLwykbTTaMgpzxqApZ3KA=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1839,7 +1830,7 @@[m
 		},[m
 		"color-convert": {[m
 			"version": "1.9.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/color-convert/-/color-convert-1.9.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/color-convert/-/color-convert-1.9.3.tgz",[m
 			"integrity": "sha512-QfAUtd+vFdAtFQcC8CCyYt1fYWxSqAiK2cSD6zDB8N3cpsEBAvRxp9zOGg6G/SHHJYAT88/az/IuDGALsNVbGg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1848,13 +1839,13 @@[m
 		},[m
 		"color-name": {[m
 			"version": "1.1.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/color-name/-/color-name-1.1.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/color-name/-/color-name-1.1.3.tgz",[m
 			"integrity": "sha1-p9BVi9icQveV3UIyj3QIMcpTvCU=",[m
 			"dev": true[m
 		},[m
 		"color-string": {[m
 			"version": "1.5.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/color-string/-/color-string-1.5.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/color-string/-/color-string-1.5.3.tgz",[m
 			"integrity": "sha512-dC2C5qeWoYkxki5UAXapdjqO672AM4vZuPGRQfO8b5HKuKGBbKWpITyDYN7TOFKvRW7kOgAn3746clDBMDJyQw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1864,7 +1855,7 @@[m
 		},[m
 		"combined-stream": {[m
 			"version": "1.0.7",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/combined-stream/-/combined-stream-1.0.7.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/combined-stream/-/combined-stream-1.0.7.tgz",[m
 			"integrity": "sha512-brWl9y6vOB1xYPZcpZde3N9zDByXTosAeMDo4p1wzo6UMOX4vumB+TP1RZ76sfE6Md68Q0NJSrE/gbezd4Ul+w==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1879,7 +1870,7 @@[m
 		},[m
 		"commondir": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/commondir/-/commondir-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/commondir/-/commondir-1.0.1.tgz",[m
 			"integrity": "sha1-3dgA2gxmEnOTzKWVDqloo6rxJTs=",[m
 			"dev": true[m
 		},[m
[36m@@ -1891,13 +1882,13 @@[m
 		},[m
 		"concat-map": {[m
 			"version": "0.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/concat-map/-/concat-map-0.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/concat-map/-/concat-map-0.0.1.tgz",[m
 			"integrity": "sha1-2Klr13/Wjfd5OnMDajug1UBdR3s=",[m
 			"dev": true[m
 		},[m
 		"concat-stream": {[m
 			"version": "1.6.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/concat-stream/-/concat-stream-1.6.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/concat-stream/-/concat-stream-1.6.2.tgz",[m
 			"integrity": "sha512-27HBghJxjiZtIk3Ycvn/4kbJk/1uZuJFfuPEns6LaEvpvG1f0hTea8lilrouyo9mVc2GWdcEZ8OLoGmSADlrCw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1909,12 +1900,12 @@[m
 		},[m
 		"connect-flash": {[m
 			"version": "0.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/connect-flash/-/connect-flash-0.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/connect-flash/-/connect-flash-0.1.1.tgz",[m
 			"integrity": "sha1-2GMPJtlaf4UfmVax6MxnMvO2qjA="[m
 		},[m
 		"console-browserify": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/console-browserify/-/console-browserify-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/console-browserify/-/console-browserify-1.1.0.tgz",[m
 			"integrity": "sha1-8CQcRXMKn8YyOyBtvzjtx0HQuxA=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1923,29 +1914,29 @@[m
 		},[m
 		"console-control-strings": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/console-control-strings/-/console-control-strings-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/console-control-strings/-/console-control-strings-1.1.0.tgz",[m
 			"integrity": "sha1-PXz0Rk22RG6mRL9LOVB/mFEAjo4=",[m
 			"dev": true[m
 		},[m
 		"constants-browserify": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/constants-browserify/-/constants-browserify-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/constants-browserify/-/constants-browserify-1.0.0.tgz",[m
 			"integrity": "sha1-wguW2MYXdIqvHBYCF2DNJ/y4y3U=",[m
 			"dev": true[m
 		},[m
 		"content-disposition": {[m
 			"version": "0.5.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/content-disposition/-/content-disposition-0.5.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/content-disposition/-/content-disposition-0.5.2.tgz",[m
 			"integrity": "sha1-DPaLud318r55YcOoUXjLhdunjLQ="[m
 		},[m
 		"content-type": {[m
 			"version": "1.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/content-type/-/content-type-1.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/content-type/-/content-type-1.0.4.tgz",[m
 			"integrity": "sha512-hIP3EEPs8tB9AT1L+NUqtwOAps4mk2Zob89MWXMHjHWg9milF/j4osnnQLXBCBFBk/tvIG/tUc9mOUJiPBhPXA=="[m
 		},[m
 		"convert-source-map": {[m
 			"version": "1.6.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/convert-source-map/-/convert-source-map-1.6.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/convert-source-map/-/convert-source-map-1.6.0.tgz",[m
 			"integrity": "sha512-eFu7XigvxdZ1ETfbgPBohgyQ/Z++C0eEhTor0qRwBw9unw+L0/6V8wkSuGgzdThkiS5lSpdptOQPD8Ak40a+7A==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1954,7 +1945,7 @@[m
 		},[m
 		"cookie": {[m
 			"version": "0.3.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cookie/-/cookie-0.3.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cookie/-/cookie-0.3.1.tgz",[m
 			"integrity": "sha1-5+Ch+e9DtMi6klxcWpboBtFoc7s="[m
 		},[m
 		"cookie-parser": {[m
[36m@@ -1968,12 +1959,12 @@[m
 		},[m
 		"cookie-signature": {[m
 			"version": "1.0.6",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cookie-signature/-/cookie-signature-1.0.6.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cookie-signature/-/cookie-signature-1.0.6.tgz",[m
 			"integrity": "sha1-4wOogrNCzD7oylE6eZmXNNqzriw="[m
 		},[m
 		"copy-concurrently": {[m
 			"version": "1.0.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/copy-concurrently/-/copy-concurrently-1.0.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/copy-concurrently/-/copy-concurrently-1.0.5.tgz",[m
 			"integrity": "sha512-f2domd9fsVDFtaFcbaRZuYXwtdmnzqbADSwhSWYxYB/Q8zsdUUFMXVRwXGDMWmbEzAn1kdRrtI1T/KTFOL4X2A==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -1987,13 +1978,13 @@[m
 		},[m
 		"copy-descriptor": {[m
 			"version": "0.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/copy-descriptor/-/copy-descriptor-0.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/copy-descriptor/-/copy-descriptor-0.1.1.tgz",[m
 			"integrity": "sha1-Z29us8OZl8LuGsOpJP1hJHSPV40=",[m
 			"dev": true[m
 		},[m
 		"copy-webpack-plugin": {[m
 			"version": "4.6.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/copy-webpack-plugin/-/copy-webpack-plugin-4.6.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/copy-webpack-plugin/-/copy-webpack-plugin-4.6.0.tgz",[m
 			"integrity": "sha512-Y+SQCF+0NoWQryez2zXn5J5knmr9z/9qSQt7fbL78u83rxmigOy8X5+BFn8CFSuX+nKT8gpYwJX68ekqtQt6ZA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2009,7 +2000,7 @@[m
 			"dependencies": {[m
 				"is-extglob": {[m
 					"version": "2.1.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-extglob/-/is-extglob-2.1.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-extglob/-/is-extglob-2.1.1.tgz",[m
 					"integrity": "sha1-qIwCU1eR8C7TfHahueqXc8gz+MI=",[m
 					"dev": true[m
 				},[m
[36m@@ -2032,7 +2023,7 @@[m
 		},[m
 		"core-util-is": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/core-util-is/-/core-util-is-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/core-util-is/-/core-util-is-1.0.2.tgz",[m
 			"integrity": "sha1-tf1UIgqivFq1eqtxQMlAdUUDwac=",[m
 			"dev": true[m
 		},[m
[36m@@ -2050,7 +2041,7 @@[m
 			"dependencies": {[m
 				"parse-json": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/parse-json/-/parse-json-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/parse-json/-/parse-json-4.0.0.tgz",[m
 					"integrity": "sha1-vjX1Qlvh9/bHRxhPmKeIy5lHfuA=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -2062,7 +2053,7 @@[m
 		},[m
 		"create-ecdh": {[m
 			"version": "4.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/create-ecdh/-/create-ecdh-4.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/create-ecdh/-/create-ecdh-4.0.3.tgz",[m
 			"integrity": "sha512-GbEHQPMOswGpKXM9kCWVrremUcBmjteUaQ01T9rkKCPDXfUHX0IoP9LpHYo2NPFampa4e+/pFDc3jQdxrxQLaw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2072,7 +2063,7 @@[m
 		},[m
 		"create-hash": {[m
 			"version": "1.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/create-hash/-/create-hash-1.2.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/create-hash/-/create-hash-1.2.0.tgz",[m
 			"integrity": "sha512-z00bCGNHDG8mHAkP7CtT1qVu+bFQUPjYq/4Iv3C3kWjTFV10zIjfSoeqXo9Asws8gwSHDGj/hl2u4OGIjapeCg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2085,7 +2076,7 @@[m
 		},[m
 		"create-hmac": {[m
 			"version": "1.1.7",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/create-hmac/-/create-hmac-1.1.7.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/create-hmac/-/create-hmac-1.1.7.tgz",[m
 			"integrity": "sha512-MJG9liiZ+ogc4TzUwuvbER1JRdgvUFSB5+VR/g5h82fGaIRWMWddtKBHi7/sVhfjQZ6SehlyhvQYrcYkaUIpLg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2099,7 +2090,7 @@[m
 		},[m
 		"cross-spawn": {[m
 			"version": "3.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cross-spawn/-/cross-spawn-3.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cross-spawn/-/cross-spawn-3.0.1.tgz",[m
 			"integrity": "sha1-ElYDfsufDF9549bvE14wdwGEuYI=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2109,7 +2100,7 @@[m
 		},[m
 		"crypto-browserify": {[m
 			"version": "3.12.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/crypto-browserify/-/crypto-browserify-3.12.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/crypto-browserify/-/crypto-browserify-3.12.0.tgz",[m
 			"integrity": "sha512-fz4spIh+znjO2VjL+IdhEpRJ3YN6sMzITSBijk6FK2UvTqruSQW+/cCZTSNsMiZNvUeq0CqurF+dAbyiGOY6Wg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2128,13 +2119,13 @@[m
 		},[m
 		"css-color-names": {[m
 			"version": "0.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/css-color-names/-/css-color-names-0.0.4.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/css-color-names/-/css-color-names-0.0.4.tgz",[m
 			"integrity": "sha1-gIrcLnnPhHOAabZGyyDsJ762KeA=",[m
 			"dev": true[m
 		},[m
 		"css-declaration-sorter": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/css-declaration-sorter/-/css-declaration-sorter-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/css-declaration-sorter/-/css-declaration-sorter-4.0.1.tgz",[m
 			"integrity": "sha512-BcxQSKTSEEQUftYpBVnsH4SF05NTuBokb19/sBt6asXGKZ/6VP7PLG1CBCkFDYOnhXhPh0jMhO6xZ71oYHXHBA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2144,7 +2135,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -2186,7 +2177,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -2203,7 +2194,7 @@[m
 		},[m
 		"css-loader": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/css-loader/-/css-loader-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/css-loader/-/css-loader-1.0.1.tgz",[m
 			"integrity": "sha512-+ZHAZm/yqvJ2kDtPne3uX0C+Vr3Zn5jFn2N4HywtS5ujwvsVkyg0VArEXpl3BgczDA8anieki1FIzhchX4yrDw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2223,7 +2214,7 @@[m
 		},[m
 		"css-select": {[m
 			"version": "2.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/css-select/-/css-select-2.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/css-select/-/css-select-2.0.2.tgz",[m
 			"integrity": "sha512-dSpYaDVoWaELjvZ3mS6IKZM/y2PMPa/XYoEfYNZePL4U/XgyxZNroHEHReDx/d+VgXh9VbCTtFqLkFbmeqeaRQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2235,13 +2226,13 @@[m
 		},[m
 		"css-select-base-adapter": {[m
 			"version": "0.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/css-select-base-adapter/-/css-select-base-adapter-0.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/css-select-base-adapter/-/css-select-base-adapter-0.1.1.tgz",[m
 			"integrity": "sha512-jQVeeRG70QI08vSTwf1jHxp74JoZsr2XSgETae8/xC8ovSnL2WF87GTLO86Sbwdt2lK4Umg4HnnwMO4YF3Ce7w==",[m
 			"dev": true[m
 		},[m
 		"css-selector-tokenizer": {[m
 			"version": "0.7.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/css-selector-tokenizer/-/css-selector-tokenizer-0.7.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/css-selector-tokenizer/-/css-selector-tokenizer-0.7.1.tgz",[m
 			"integrity": "sha512-xYL0AMZJ4gFzJQsHUKa5jiWWi2vH77WVNg7JYRyewwj6oPh4yb/y6Y9ZCw9dsj/9UauMhtuxR+ogQd//EdEVNA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2252,7 +2243,7 @@[m
 			"dependencies": {[m
 				"regexpu-core": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/regexpu-core/-/regexpu-core-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/regexpu-core/-/regexpu-core-1.0.0.tgz",[m
 					"integrity": "sha1-hqdj9Y7k18L2sQLkdkBQ3n7ZDGs=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -2265,7 +2256,7 @@[m
 		},[m
 		"css-tree": {[m
 			"version": "1.0.0-alpha.28",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/css-tree/-/css-tree-1.0.0-alpha.28.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/css-tree/-/css-tree-1.0.0-alpha.28.tgz",[m
 			"integrity": "sha512-joNNW1gCp3qFFzj4St6zk+Wh/NBv0vM5YbEreZk0SD4S23S+1xBKb6cLDg2uj4P4k/GUMlIm6cKIDqIG+vdt0w==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2275,13 +2266,13 @@[m
 		},[m
 		"css-unit-converter": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/css-unit-converter/-/css-unit-converter-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/css-unit-converter/-/css-unit-converter-1.1.1.tgz",[m
 			"integrity": "sha1-2bkoGtz9jO2TW9urqDeGiX9k6ZY=",[m
 			"dev": true[m
 		},[m
 		"css-url-regex": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/css-url-regex/-/css-url-regex-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/css-url-regex/-/css-url-regex-1.1.0.tgz",[m
 			"integrity": "sha1-g4NCMMyfdMRX3lnuvRVD/uuDt+w=",[m
 			"dev": true[m
 		},[m
[36m@@ -2293,7 +2284,7 @@[m
 		},[m
 		"cssesc": {[m
 			"version": "0.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cssesc/-/cssesc-0.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cssesc/-/cssesc-0.1.0.tgz",[m
 			"integrity": "sha1-yBSQPkViM3GgR3tAEJqq++6t27Q=",[m
 			"dev": true[m
 		},[m
[36m@@ -2311,7 +2302,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -2353,7 +2344,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -2408,7 +2399,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -2450,7 +2441,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -2467,19 +2458,19 @@[m
 		},[m
 		"cssnano-util-get-arguments": {[m
 			"version": "4.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cssnano-util-get-arguments/-/cssnano-util-get-arguments-4.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cssnano-util-get-arguments/-/cssnano-util-get-arguments-4.0.0.tgz",[m
 			"integrity": "sha1-7ToIKZ8h11dBsg87gfGU7UnMFQ8=",[m
 			"dev": true[m
 		},[m
 		"cssnano-util-get-match": {[m
 			"version": "4.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cssnano-util-get-match/-/cssnano-util-get-match-4.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cssnano-util-get-match/-/cssnano-util-get-match-4.0.0.tgz",[m
 			"integrity": "sha1-wOTKB/U4a7F+xeUiULT1lhNlFW0=",[m
 			"dev": true[m
 		},[m
 		"cssnano-util-raw-cache": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cssnano-util-raw-cache/-/cssnano-util-raw-cache-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cssnano-util-raw-cache/-/cssnano-util-raw-cache-4.0.1.tgz",[m
 			"integrity": "sha512-qLuYtWK2b2Dy55I8ZX3ky1Z16WYsx544Q0UWViebptpwn/xDBmog2TLg4f+DBMg1rJ6JDWtn96WHbOKDWt1WQA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2488,7 +2479,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -2530,7 +2521,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -2547,13 +2538,13 @@[m
 		},[m
 		"cssnano-util-same-parent": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cssnano-util-same-parent/-/cssnano-util-same-parent-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cssnano-util-same-parent/-/cssnano-util-same-parent-4.0.1.tgz",[m
 			"integrity": "sha512-WcKx5OY+KoSIAxBW6UBBRay1U6vkYheCdjyVNDm85zt5K9mHoGOfsOsqIszfAqrQQFIIKgjh2+FDgIj/zsl21Q==",[m
 			"dev": true[m
 		},[m
 		"csso": {[m
 			"version": "3.5.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/csso/-/csso-3.5.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/csso/-/csso-3.5.1.tgz",[m
 			"integrity": "sha512-vrqULLffYU1Q2tLdJvaCYbONStnfkfimRxXNaGjxMldI0C7JPBC4rB1RyjhfdZ4m1frm8pM9uRPKH3d2knZ8gg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2562,7 +2553,7 @@[m
 			"dependencies": {[m
 				"css-tree": {[m
 					"version": "1.0.0-alpha.29",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/css-tree/-/css-tree-1.0.0-alpha.29.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/css-tree/-/css-tree-1.0.0-alpha.29.tgz",[m
 					"integrity": "sha512-sRNb1XydwkW9IOci6iB2xmy8IGCj6r/fr+JWitvJ2JxQRPzN3T4AGGVWCMlVmVwM1gtgALJRmGIlWv5ppnGGkg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -2574,7 +2565,7 @@[m
 		},[m
 		"currently-unhandled": {[m
 			"version": "0.4.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/currently-unhandled/-/currently-unhandled-0.4.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/currently-unhandled/-/currently-unhandled-0.4.1.tgz",[m
 			"integrity": "sha1-mI3zP+qxke95mmE2nddsF635V+o=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2583,13 +2574,13 @@[m
 		},[m
 		"cyclist": {[m
 			"version": "0.2.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cyclist/-/cyclist-0.2.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/cyclist/-/cyclist-0.2.2.tgz",[m
 			"integrity": "sha1-GzN5LhHpFKL9bW7WRHRkRE5fpkA=",[m
 			"dev": true[m
 		},[m
 		"dashdash": {[m
 			"version": "1.14.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/dashdash/-/dashdash-1.14.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/dashdash/-/dashdash-1.14.1.tgz",[m
 			"integrity": "sha1-hTz6D3y+L+1d4gMmuN1YEDX24vA=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2598,13 +2589,13 @@[m
 		},[m
 		"date-now": {[m
 			"version": "0.1.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/date-now/-/date-now-0.1.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/date-now/-/date-now-0.1.4.tgz",[m
 			"integrity": "sha1-6vQ5/U1ISK105cx9vvIAZyueNFs=",[m
 			"dev": true[m
 		},[m
 		"debug": {[m
 			"version": "2.6.9",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/debug/-/debug-2.6.9.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/debug/-/debug-2.6.9.tgz",[m
 			"integrity": "sha512-bC7ElrdJaJnPbAP+1EotYvqZsb3ecl5wi6Bfi6BJTUcNowp6cvspg0jXznRTKDjm/E7AdgFBVeAPVMNcKGsHMA==",[m
 			"requires": {[m
 				"ms": "2.0.0"[m
[36m@@ -2612,19 +2603,19 @@[m
 		},[m
 		"decamelize": {[m
 			"version": "1.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/decamelize/-/decamelize-1.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/decamelize/-/decamelize-1.2.0.tgz",[m
 			"integrity": "sha1-9lNNFRSCabIDUue+4m9QH5oZEpA=",[m
 			"dev": true[m
 		},[m
 		"decode-uri-component": {[m
 			"version": "0.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/decode-uri-component/-/decode-uri-component-0.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/decode-uri-component/-/decode-uri-component-0.2.0.tgz",[m
 			"integrity": "sha1-6zkTMzRYd1y4TNGh+uBiEGu4dUU=",[m
 			"dev": true[m
 		},[m
 		"define-properties": {[m
 			"version": "1.1.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-properties/-/define-properties-1.1.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/define-properties/-/define-properties-1.1.3.tgz",[m
 			"integrity": "sha512-3MqfYKj2lLzdMSf8ZIZE/V+Zuy+BgD6f164e8K2w7dgnpKArBDerGYpM46IYYcjnkdPNMjPk9A6VFB8+3SKlXQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2633,7 +2624,7 @@[m
 		},[m
 		"define-property": {[m
 			"version": "2.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-2.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-2.0.2.tgz",[m
 			"integrity": "sha512-jwK2UV4cnPpbcG7+VRARKTZPUWowwXA8bzH5NP6ud0oeAxyYPuGZUAC7hMugpCdz4BeSZl2Dl9k66CHJ/46ZYQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2643,7 +2634,7 @@[m
 			"dependencies": {[m
 				"is-accessor-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-m5hnHTkcVsPfqx3AKlyttIPb7J+XykHvJP2B9bZDjlhLIoEq4XoK64Vg7boZlVWYK6LUY94dYPEE7Lh0ZkZKcQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -2652,7 +2643,7 @@[m
 				},[m
 				"is-data-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-jbRXy1FmtAoCjQkVmIVYwuuqDFUbaOeDjmed1tOGPrsMhtJA4rD9tkgA0F1qJ3gRFRXcHYVkdeaP50Q5rE/jLQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -2661,7 +2652,7 @@[m
 				},[m
 				"is-descriptor": {[m
 					"version": "1.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
 					"integrity": "sha512-2eis5WqQGV7peooDyLmNEPUrps9+SXX5c9pL3xEB+4e9HnGuDa7mB7kHxHw4CbqS9k1T2hOH3miL8n8WtiYVtg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -2672,13 +2663,13 @@[m
 				},[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				},[m
 				"kind-of": {[m
 					"version": "6.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-6.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-6.0.2.tgz",[m
 					"integrity": "sha512-s5kLOcnH0XqDO+FvuaLX8DDjZ18CGFk7VygH40QoKPUQhW4e2rvM0rwUq0t8IQDOwYSeLK01U90OjzBTme2QqA==",[m
 					"dev": true[m
 				}[m
[36m@@ -2686,24 +2677,24 @@[m
 		},[m
 		"delayed-stream": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/delayed-stream/-/delayed-stream-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/delayed-stream/-/delayed-stream-1.0.0.tgz",[m
 			"integrity": "sha1-3zrhmayt+31ECqrgsp4icrJOxhk=",[m
 			"dev": true[m
 		},[m
 		"delegates": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/delegates/-/delegates-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/delegates/-/delegates-1.0.0.tgz",[m
 			"integrity": "sha1-hMbhWbgZBP3KWaDvRM2HDTElD5o=",[m
 			"dev": true[m
 		},[m
 		"depd": {[m
 			"version": "1.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/depd/-/depd-1.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/depd/-/depd-1.1.2.tgz",[m
 			"integrity": "sha1-m81S4UwJd2PnSbJ0xDRu0uVgtak="[m
 		},[m
 		"des.js": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/des.js/-/des.js-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/des.js/-/des.js-1.0.0.tgz",[m
 			"integrity": "sha1-wHTS4qpqipoH29YfmhXCzYPsjsw=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2713,7 +2704,7 @@[m
 		},[m
 		"destroy": {[m
 			"version": "1.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/destroy/-/destroy-1.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/destroy/-/destroy-1.0.4.tgz",[m
 			"integrity": "sha1-l4hXRCxEdJ5CBmE+N5RiBYJqvYA="[m
 		},[m
 		"detect-file": {[m
[36m@@ -2724,7 +2715,7 @@[m
 		},[m
 		"detect-indent": {[m
 			"version": "4.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/detect-indent/-/detect-indent-4.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/detect-indent/-/detect-indent-4.0.0.tgz",[m
 			"integrity": "sha1-920GQ1LN9Docts5hnE7jqUdd4gg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2733,7 +2724,7 @@[m
 		},[m
 		"dicer": {[m
 			"version": "0.2.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/dicer/-/dicer-0.2.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/dicer/-/dicer-0.2.5.tgz",[m
 			"integrity": "sha1-WZbAhrszIYyBLAkL3cCc0S+stw8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2743,13 +2734,13 @@[m
 			"dependencies": {[m
 				"isarray": {[m
 					"version": "0.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isarray/-/isarray-0.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isarray/-/isarray-0.0.1.tgz",[m
 					"integrity": "sha1-ihis/Kmo9Bd+Cav8YDiTmwXR7t8=",[m
 					"dev": true[m
 				},[m
 				"readable-stream": {[m
 					"version": "1.1.14",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/readable-stream/-/readable-stream-1.1.14.tgz",[m
[32m+[m					[32m"resolved": "http://registry.npmjs.org/readable-stream/-/readable-stream-1.1.14.tgz",[m
 					"integrity": "sha1-fPTFTvZI44EwhMY23SB54WbAgdk=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -2761,7 +2752,7 @@[m
 				},[m
 				"string_decoder": {[m
 					"version": "0.10.31",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/string_decoder/-/string_decoder-0.10.31.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/string_decoder/-/string_decoder-0.10.31.tgz",[m
 					"integrity": "sha1-YuIDvEF2bGwoyfyEMB2rHFMQ+pQ=",[m
 					"dev": true[m
 				}[m
[36m@@ -2769,7 +2760,7 @@[m
 		},[m
 		"diffie-hellman": {[m
 			"version": "5.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/diffie-hellman/-/diffie-hellman-5.0.3.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/diffie-hellman/-/diffie-hellman-5.0.3.tgz",[m
 			"integrity": "sha512-kqag/Nl+f3GwyK25fhUMYj81BUOrZ9IuJsjIcDE5icNM9FJHAVm3VcUDxdLPoQtTuUylWm6ZIknYJwwaPxsUzg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2799,7 +2790,7 @@[m
 		},[m
 		"domain-browser": {[m
 			"version": "1.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/domain-browser/-/domain-browser-1.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/domain-browser/-/domain-browser-1.2.0.tgz",[m
 			"integrity": "sha512-jnjyiM6eRyZl2H+W8Q/zLMA481hzi0eszAaBUzIVnmYVDBbnLxVNnfu1HgEBvCbL+71FrxMl3E6lpKH7Ge3OXA==",[m
 			"dev": true[m
 		},[m
[36m@@ -2811,7 +2802,7 @@[m
 		},[m
 		"domutils": {[m
 			"version": "1.7.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/domutils/-/domutils-1.7.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/domutils/-/domutils-1.7.0.tgz",[m
 			"integrity": "sha512-Lgd2XcJ/NjEw+7tFvfKxOzCYKZsdct5lczQ2ZaQY8Djz7pfAD3Gbp8ySJWtreII/vDlMVmxwa6pHmdxIYgttDg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2821,7 +2812,7 @@[m
 		},[m
 		"dot-prop": {[m
 			"version": "4.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/dot-prop/-/dot-prop-4.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/dot-prop/-/dot-prop-4.2.0.tgz",[m
 			"integrity": "sha512-tUMXrxlExSW6U2EXiiKGSBVdYgtV8qlHL+C10TsW4PURY/ic+eaysnSkwB4kA/mBlCyy/IKDJ+Lc3wbWeaXtuQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2842,7 +2833,7 @@[m
 		},[m
 		"ecc-jsbn": {[m
 			"version": "0.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ecc-jsbn/-/ecc-jsbn-0.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/ecc-jsbn/-/ecc-jsbn-0.1.2.tgz",[m
 			"integrity": "sha1-OoOpBOVDUyh4dMVkt1SThoSamMk=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2860,12 +2851,12 @@[m
 		},[m
 		"ee-first": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ee-first/-/ee-first-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/ee-first/-/ee-first-1.1.1.tgz",[m
 			"integrity": "sha1-WQxhFWsK4vTwJVcyoViyZrxWsh0="[m
 		},[m
 		"ejs": {[m
 			"version": "2.6.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ejs/-/ejs-2.6.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/ejs/-/ejs-2.6.1.tgz",[m
 			"integrity": "sha512-0xy4A/twfrRCnkhfk8ErDi5DqdAsAqeGxht4xkCUrsvhhbQNs7E+4jV0CN7+NKIY0aHE72+XvqtBIXzD31ZbXQ=="[m
 		},[m
 		"electron-to-chromium": {[m
[36m@@ -2876,7 +2867,7 @@[m
 		},[m
 		"elliptic": {[m
 			"version": "6.4.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/elliptic/-/elliptic-6.4.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/elliptic/-/elliptic-6.4.1.tgz",[m
 			"integrity": "sha512-BsXLz5sqX8OHcsh7CqBMztyXARmGQ3LWPtGjJi6DiJHq5C/qvi9P3OqgswKSDftbu8+IoI/QDTAm2fFnQ9SZSQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2891,18 +2882,18 @@[m
 		},[m
 		"emojis-list": {[m
 			"version": "2.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/emojis-list/-/emojis-list-2.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/emojis-list/-/emojis-list-2.1.0.tgz",[m
 			"integrity": "sha1-TapNnbAPmBmIDHn6RXrlsJof04k=",[m
 			"dev": true[m
 		},[m
 		"encodeurl": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/encodeurl/-/encodeurl-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/encodeurl/-/encodeurl-1.0.2.tgz",[m
 			"integrity": "sha1-rT/0yG7C0CkyL1oCw6mmBslbP1k="[m
 		},[m
 		"end-of-stream": {[m
 			"version": "1.4.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/end-of-stream/-/end-of-stream-1.4.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/end-of-stream/-/end-of-stream-1.4.1.tgz",[m
 			"integrity": "sha512-1MkrZNvWTKCaigbn+W15elq2BB/L22nqrSY5DKlo3X6+vclJm8Bb5djXJBmEX6fS3+zCh/F4VBK5Z2KxJt4s2Q==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2911,7 +2902,7 @@[m
 		},[m
 		"enhanced-resolve": {[m
 			"version": "4.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/enhanced-resolve/-/enhanced-resolve-4.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/enhanced-resolve/-/enhanced-resolve-4.1.0.tgz",[m
 			"integrity": "sha512-F/7vkyTtyc/llOIn8oWclcB25KdRaiPBpZYDgJHgh/UHtpgT2p2eldQgtQnLtUvfMKPKxbRaQM/hHkvLHt1Vng==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2922,13 +2913,13 @@[m
 		},[m
 		"entities": {[m
 			"version": "1.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/entities/-/entities-1.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/entities/-/entities-1.1.2.tgz",[m
 			"integrity": "sha512-f2LZMYl1Fzu7YSBKg+RoROelpOaNrcGmE9AZubeDfrCEia483oW4MI4VyFd5VNHIgQ/7qm1I0wUHK1eJnn2y2w==",[m
 			"dev": true[m
 		},[m
 		"errno": {[m
 			"version": "0.1.7",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/errno/-/errno-0.1.7.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/errno/-/errno-0.1.7.tgz",[m
 			"integrity": "sha512-MfrRBDWzIWifgq6tJj60gkAwtLNb6sQPlcFrSOflcP1aFmmruKQ2wRnze/8V6kgyz7H3FF8Npzv78mZ7XLLflg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2937,7 +2928,7 @@[m
 		},[m
 		"error-ex": {[m
 			"version": "1.3.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/error-ex/-/error-ex-1.3.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/error-ex/-/error-ex-1.3.2.tgz",[m
 			"integrity": "sha512-7dFHNmqeFSEt2ZBsCriorKnn3Z2pj+fd9kmI6QoWw4//DL+icEBfc0U7qJCisqrTsKTjw4fNFy2pW9OqStD84g==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2960,7 +2951,7 @@[m
 		},[m
 		"es-to-primitive": {[m
 			"version": "1.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/es-to-primitive/-/es-to-primitive-1.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/es-to-primitive/-/es-to-primitive-1.2.0.tgz",[m
 			"integrity": "sha512-qZryBOJjV//LaxLTV6UC//WewneB3LcXOL9NP++ozKVXsIIIpm/2c13UDiD9Jp2eThsecw9m3jPqDwTyobcdbg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -2971,12 +2962,12 @@[m
 		},[m
 		"escape-html": {[m
 			"version": "1.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/escape-html/-/escape-html-1.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/escape-html/-/escape-html-1.0.3.tgz",[m
 			"integrity": "sha1-Aljq5NPQwJdN4cFpGI7wBR0dGYg="[m
 		},[m
 		"escape-string-regexp": {[m
 			"version": "1.0.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/escape-string-regexp/-/escape-string-regexp-1.0.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/escape-string-regexp/-/escape-string-regexp-1.0.5.tgz",[m
 			"integrity": "sha1-G2HAViGQqN/2rjuyzwIAyhMLhtQ=",[m
 			"dev": true[m
 		},[m
[36m@@ -2992,13 +2983,13 @@[m
 		},[m
 		"esprima": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/esprima/-/esprima-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/esprima/-/esprima-4.0.1.tgz",[m
 			"integrity": "sha512-eGuFFw7Upda+g4p+QHvnW0RyTX/SVeJBDM/gCtMARO0cLuT2HcEKnTPvhjV6aGeqrCB/sbNop0Kszm0jsaWU4A==",[m
 			"dev": true[m
 		},[m
 		"esrecurse": {[m
 			"version": "4.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/esrecurse/-/esrecurse-4.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/esrecurse/-/esrecurse-4.2.1.tgz",[m
 			"integrity": "sha512-64RBB++fIOAXPw3P9cy89qfMlvZEXZkqqJkjqqXIvzP5ezRZjW+lPWjw35UX/3EhUPFYbg5ER4JYgDw4007/DQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3007,19 +2998,19 @@[m
 		},[m
 		"estraverse": {[m
 			"version": "4.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/estraverse/-/estraverse-4.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/estraverse/-/estraverse-4.2.0.tgz",[m
 			"integrity": "sha1-De4/7TH81GlhjOc0IJn8GvoL2xM=",[m
 			"dev": true[m
 		},[m
 		"esutils": {[m
 			"version": "2.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/esutils/-/esutils-2.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/esutils/-/esutils-2.0.2.tgz",[m
 			"integrity": "sha1-Cr9PHKpbyx96nYrMbepPqqBLrJs=",[m
 			"dev": true[m
 		},[m
 		"etag": {[m
 			"version": "1.8.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/etag/-/etag-1.8.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/etag/-/etag-1.8.1.tgz",[m
 			"integrity": "sha1-Qa4u62XvpiJorr/qg6x9eSmbCIc="[m
 		},[m
 		"events": {[m
[36m@@ -3030,7 +3021,7 @@[m
 		},[m
 		"evp_bytestokey": {[m
 			"version": "1.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/evp_bytestokey/-/evp_bytestokey-1.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/evp_bytestokey/-/evp_bytestokey-1.0.3.tgz",[m
 			"integrity": "sha512-/f2Go4TognH/KvCISP7OUsHn85hT9nUkxxA9BEWxFn+Oj9o8ZNLm/40hdlgSLyuOimsrTKLUMEorQexp/aPQeA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3070,7 +3061,7 @@[m
 		},[m
 		"expand-brackets": {[m
 			"version": "0.1.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/expand-brackets/-/expand-brackets-0.1.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/expand-brackets/-/expand-brackets-0.1.5.tgz",[m
 			"integrity": "sha1-3wcoTjQqgHzXM6xa9yQR5YHRF3s=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3079,7 +3070,7 @@[m
 		},[m
 		"expand-range": {[m
 			"version": "1.8.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/expand-range/-/expand-range-1.8.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/expand-range/-/expand-range-1.8.2.tgz",[m
 			"integrity": "sha1-opnv/TNf4nIeuujiV+x5ZE/IUzc=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3097,7 +3088,7 @@[m
 		},[m
 		"express": {[m
 			"version": "4.16.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/express/-/express-4.16.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/express/-/express-4.16.4.tgz",[m
 			"integrity": "sha512-j12Uuyb4FMrd/qQAm6uCHAkPtO8FDTRJZBDd5D2KOL2eLaz1yUNdUB/NOIyq0iU4q4cFarsUCrnFDPBcnksuOg==",[m
 			"requires": {[m
 				"accepts": "~1.3.5",[m
[36m@@ -3196,14 +3187,14 @@[m
 				},[m
 				"statuses": {[m
 					"version": "1.4.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/statuses/-/statuses-1.4.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/statuses/-/statuses-1.4.0.tgz",[m
 					"integrity": "sha512-zhSCtt8v2NDrRlPQpCNtw/heZLtfUDqxBM1udqikb/Hbk52LK4nQSwr10u77iopCW5LsyHpuXS0GnEc48mLeew=="[m
 				}[m
 			}[m
 		},[m
 		"express-messages": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/express-messages/-/express-messages-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/express-messages/-/express-messages-1.0.1.tgz",[m
 			"integrity": "sha1-mYGoWl0rEYx5/DP1K0GDT/7paFo="[m
 		},[m
 		"express-session": {[m
[36m@@ -3239,13 +3230,13 @@[m
 		},[m
 		"extend": {[m
 			"version": "3.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend/-/extend-3.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/extend/-/extend-3.0.2.tgz",[m
 			"integrity": "sha512-fjquC59cD7CyW6urNXK0FBufkZcoiGG80wTuPujX590cB5Ttln20E2UB4S/WARVqhXffZl2LNgS+gQdPIIim/g==",[m
 			"dev": true[m
 		},[m
 		"extend-shallow": {[m
 			"version": "3.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-3.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-3.0.2.tgz",[m
 			"integrity": "sha1-Jqcarwc7OfshJxcnRhMcJwQCjbg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3255,7 +3246,7 @@[m
 			"dependencies": {[m
 				"is-extendable": {[m
 					"version": "1.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-extendable/-/is-extendable-1.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-extendable/-/is-extendable-1.0.1.tgz",[m
 					"integrity": "sha512-arnXMxT1hhoKo9k1LZdmlNyJdDDfy2v0fXjFlmok4+i8ul/6WlbVge9bhM74OpNPQPMGUToDtz+KXa1PneJxOA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -3266,7 +3257,7 @@[m
 		},[m
 		"extglob": {[m
 			"version": "0.3.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extglob/-/extglob-0.3.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/extglob/-/extglob-0.3.2.tgz",[m
 			"integrity": "sha1-Lhj/PS9JqydlzskCPwEdqo2DSaE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3275,37 +3266,37 @@[m
 		},[m
 		"extsprintf": {[m
 			"version": "1.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extsprintf/-/extsprintf-1.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/extsprintf/-/extsprintf-1.3.0.tgz",[m
 			"integrity": "sha1-lpGEQOMEGnpBT4xS48V06zw+HgU=",[m
 			"dev": true[m
 		},[m
 		"fast-deep-equal": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fast-deep-equal/-/fast-deep-equal-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/fast-deep-equal/-/fast-deep-equal-2.0.1.tgz",[m
 			"integrity": "sha1-ewUhjd+WZ79/Nwv3/bLLFf3Qqkk=",[m
 			"dev": true[m
 		},[m
 		"fast-json-stable-stringify": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fast-json-stable-stringify/-/fast-json-stable-stringify-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/fast-json-stable-stringify/-/fast-json-stable-stringify-2.0.0.tgz",[m
 			"integrity": "sha1-1RQsDK7msRifh9OnYREGT4bIu/I=",[m
 			"dev": true[m
 		},[m
 		"fastparse": {[m
 			"version": "1.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fastparse/-/fastparse-1.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/fastparse/-/fastparse-1.1.2.tgz",[m
 			"integrity": "sha512-483XLLxTVIwWK3QTrMGRqUfUpoOs/0hbQrl2oz4J0pAcm3A3bu84wxTFqGqkJzewCLdME38xJLJAxBABfQT8sQ==",[m
 			"dev": true[m
 		},[m
 		"figgy-pudding": {[m
 			"version": "3.5.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/figgy-pudding/-/figgy-pudding-3.5.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/figgy-pudding/-/figgy-pudding-3.5.1.tgz",[m
 			"integrity": "sha512-vNKxJHTEKNThjfrdJwHc7brvM6eVevuO5nTj6ez8ZQ1qbXTvGthucRF7S4vf2cr71QVnT70V34v0S1DyQsti0w==",[m
 			"dev": true[m
 		},[m
 		"file-loader": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/file-loader/-/file-loader-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/file-loader/-/file-loader-2.0.0.tgz",[m
 			"integrity": "sha512-YCsBfd1ZGCyonOKLxPiKPdu+8ld9HAaMEvJewzz+b2eTF7uL5Zm/HdBF6FjCrpCMRq25Mi0U1gl4pwn2TlH7hQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3315,13 +3306,13 @@[m
 		},[m
 		"filename-regex": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/filename-regex/-/filename-regex-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/filename-regex/-/filename-regex-2.0.1.tgz",[m
 			"integrity": "sha1-wcS5vuPglyXdsQa3XB4wH+LxiyY=",[m
 			"dev": true[m
 		},[m
 		"fill-range": {[m
 			"version": "2.2.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fill-range/-/fill-range-2.2.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/fill-range/-/fill-range-2.2.4.tgz",[m
 			"integrity": "sha512-cnrcCbj01+j2gTG921VZPnHbjmdAf8oQV/iGeV2kZxGSyfYjjTyY79ErsK1WJWMpw6DaApEX72binqJE+/d+5Q==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3334,7 +3325,7 @@[m
 		},[m
 		"finalhandler": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/finalhandler/-/finalhandler-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/finalhandler/-/finalhandler-1.1.1.tgz",[m
 			"integrity": "sha512-Y1GUDo39ez4aHAw7MysnUD5JzYX+WaIj8I57kO3aEPT1fFRL4sr7mjei97FgnwhAyyzRYmQZaTHb2+9uZ1dPtg==",[m
 			"requires": {[m
 				"debug": "2.6.9",[m
[36m@@ -3348,14 +3339,14 @@[m
 			"dependencies": {[m
 				"statuses": {[m
 					"version": "1.4.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/statuses/-/statuses-1.4.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/statuses/-/statuses-1.4.0.tgz",[m
 					"integrity": "sha512-zhSCtt8v2NDrRlPQpCNtw/heZLtfUDqxBM1udqikb/Hbk52LK4nQSwr10u77iopCW5LsyHpuXS0GnEc48mLeew=="[m
 				}[m
 			}[m
 		},[m
 		"find-cache-dir": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/find-cache-dir/-/find-cache-dir-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/find-cache-dir/-/find-cache-dir-1.0.0.tgz",[m
 			"integrity": "sha1-kojj6ePMN0hxfTnq3hfPcfww7m8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3366,7 +3357,7 @@[m
 		},[m
 		"find-up": {[m
 			"version": "2.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/find-up/-/find-up-2.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/find-up/-/find-up-2.1.0.tgz",[m
 			"integrity": "sha1-RdG35QbHF93UgndaK3eSCjwMV6c=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3688,13 +3679,13 @@[m
 		},[m
 		"for-in": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/for-in/-/for-in-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/for-in/-/for-in-1.0.2.tgz",[m
 			"integrity": "sha1-gQaNKVqBQuwKxybG4iAMMPttXoA=",[m
 			"dev": true[m
 		},[m
 		"for-own": {[m
 			"version": "0.1.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/for-own/-/for-own-0.1.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/for-own/-/for-own-0.1.5.tgz",[m
 			"integrity": "sha1-UmXGgaTylNq78XyVCbZ2OqhFEM4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3703,13 +3694,13 @@[m
 		},[m
 		"forever-agent": {[m
 			"version": "0.6.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/forever-agent/-/forever-agent-0.6.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/forever-agent/-/forever-agent-0.6.1.tgz",[m
 			"integrity": "sha1-+8cfDEGt6zf5bFd60e1C2P2sypE=",[m
 			"dev": true[m
 		},[m
 		"form-data": {[m
 			"version": "2.3.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/form-data/-/form-data-2.3.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/form-data/-/form-data-2.3.3.tgz",[m
 			"integrity": "sha512-1lLKB2Mu3aGP1Q/2eCOx0fNbRMe7XdwktwOruhfqqd0rIJWwN4Dh+E3hrPSlDCXnSR7UtZ1N38rVXm+6+MEhJQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3720,12 +3711,12 @@[m
 		},[m
 		"forwarded": {[m
 			"version": "0.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/forwarded/-/forwarded-0.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/forwarded/-/forwarded-0.1.2.tgz",[m
 			"integrity": "sha1-mMI9qxF1ZXuMBXPozszZGw/xjIQ="[m
 		},[m
 		"fragment-cache": {[m
 			"version": "0.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fragment-cache/-/fragment-cache-0.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/fragment-cache/-/fragment-cache-0.2.1.tgz",[m
 			"integrity": "sha1-QpD60n8T6Jvn8zeZxrxaCr//DRk=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3734,12 +3725,12 @@[m
 		},[m
 		"fresh": {[m
 			"version": "0.5.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fresh/-/fresh-0.5.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/fresh/-/fresh-0.5.2.tgz",[m
 			"integrity": "sha1-PYyt2Q2XZWn6g1qx+OSyOhBWBac="[m
 		},[m
 		"from2": {[m
 			"version": "2.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/from2/-/from2-2.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/from2/-/from2-2.3.0.tgz",[m
 			"integrity": "sha1-i/tVAr3kpNNs/e6gB/zKIdfjgq8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3758,13 +3749,13 @@[m
 		},[m
 		"fs-readdir-recursive": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fs-readdir-recursive/-/fs-readdir-recursive-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/fs-readdir-recursive/-/fs-readdir-recursive-1.1.0.tgz",[m
 			"integrity": "sha512-GNanXlVr2pf02+sPN40XN8HG+ePaNcvM0q5mZBd668Obwb0yD5GiUbZOFgwn8kGMY6I3mdyDJzieUy3PTYyTRA==",[m
 			"dev": true[m
 		},[m
 		"fs-write-stream-atomic": {[m
 			"version": "1.0.10",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fs-write-stream-atomic/-/fs-write-stream-atomic-1.0.10.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/fs-write-stream-atomic/-/fs-write-stream-atomic-1.0.10.tgz",[m
 			"integrity": "sha1-tH31NJPvkR33VzHnCp3tAYnbQMk=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -3776,7 +3767,7 @@[m
 		},[m
 		"fs.realpath": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fs.realpath/-/fs.realpath-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/fs.realpath/-/fs.realpath-1.0.0.tgz",[m
 			"integrity": "sha1-FQStJSMVjKpA20onh8sBQRmU6k8=",[m
 			"dev": true[m
 		},[m
[36m@@ -4309,27 +4300,15 @@[m
 				}[m
 			}[m
 		},[m
[31m-		"fstream": {[m
[31m-			"version": "1.0.11",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fstream/-/fstream-1.0.11.tgz",[m
[31m-			"integrity": "sha1-XB+x8RdHcRTwYyoOtLcbPLD9MXE=",[m
[31m-			"dev": true,[m
[31m-			"requires": {[m
[31m-				"graceful-fs": "^4.1.2",[m
[31m-				"inherits": "~2.0.0",[m
[31m-				"mkdirp": ">=0.5 0",[m
[31m-				"rimraf": "2"[m
[31m-			}[m
[31m-		},[m
 		"function-bind": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/function-bind/-/function-bind-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/function-bind/-/function-bind-1.1.1.tgz",[m
 			"integrity": "sha512-yIovAzMX49sF8Yl58fSCWJ5svSLuaibPxXQJFLmBObTuCr0Mf1KiPopGM9NiFjiYBCbfaa2Fh6breQ6ANVTI0A==",[m
 			"dev": true[m
 		},[m
 		"gauge": {[m
 			"version": "2.7.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/gauge/-/gauge-2.7.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/gauge/-/gauge-2.7.4.tgz",[m
 			"integrity": "sha1-LANAXHU4w51+s3sxcCLjJfsBi/c=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4345,7 +4324,7 @@[m
 		},[m
 		"gaze": {[m
 			"version": "1.1.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/gaze/-/gaze-1.1.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/gaze/-/gaze-1.1.3.tgz",[m
 			"integrity": "sha512-BRdNm8hbWzFzWHERTrejLqwHDfS4GibPoq5wjTPIoJHoBtKGPg3xAFfxmM+9ztbXelxcf2hwQcaz1PtmFeue8g==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4354,13 +4333,13 @@[m
 		},[m
 		"get-caller-file": {[m
 			"version": "1.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/get-caller-file/-/get-caller-file-1.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/get-caller-file/-/get-caller-file-1.0.3.tgz",[m
 			"integrity": "sha512-3t6rVToeoZfYSGd8YoLFR2DJkiQrIiUrGcjvFX2mDw3bn6k2OtwHN0TNCLbBO+w8qTvimhDkv+LSscbJY1vE6w==",[m
 			"dev": true[m
 		},[m
 		"get-stdin": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/get-stdin/-/get-stdin-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/get-stdin/-/get-stdin-4.0.1.tgz",[m
 			"integrity": "sha1-uWjGsKBDhDJJAui/Gl3zJXmkUP4=",[m
 			"dev": true[m
 		},[m
[36m@@ -4387,13 +4366,13 @@[m
 		},[m
 		"get-value": {[m
 			"version": "2.0.6",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/get-value/-/get-value-2.0.6.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/get-value/-/get-value-2.0.6.tgz",[m
 			"integrity": "sha1-3BXKHGcjh8p2vTesCjlbogQqLCg=",[m
 			"dev": true[m
 		},[m
 		"getpass": {[m
 			"version": "0.1.7",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/getpass/-/getpass-0.1.7.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/getpass/-/getpass-0.1.7.tgz",[m
 			"integrity": "sha1-Xv+OPmhNVprkyysSgmBOi6YhSfo=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4402,7 +4381,7 @@[m
 		},[m
 		"glob": {[m
 			"version": "7.1.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/glob/-/glob-7.1.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/glob/-/glob-7.1.3.tgz",[m
 			"integrity": "sha512-vcfuiIxogLV4DlGBHIUOwI0IbrJ8HWPc4MU7HzviGeNho/UJDfi6B5p3sHeWIQ0KGIU0Jpxi5ZHxemQfLkkAwQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4416,7 +4395,7 @@[m
 		},[m
 		"glob-base": {[m
 			"version": "0.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/glob-base/-/glob-base-0.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/glob-base/-/glob-base-0.3.0.tgz",[m
 			"integrity": "sha1-27Fk9iIbHAscz4Kuoyi0l98Oo8Q=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4426,7 +4405,7 @@[m
 		},[m
 		"glob-parent": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/glob-parent/-/glob-parent-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/glob-parent/-/glob-parent-2.0.0.tgz",[m
 			"integrity": "sha1-gTg9ctsFT8zPUzbaqQLxgvbtuyg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4459,13 +4438,13 @@[m
 		},[m
 		"globals": {[m
 			"version": "9.18.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/globals/-/globals-9.18.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/globals/-/globals-9.18.0.tgz",[m
 			"integrity": "sha512-S0nG3CLEQiY/ILxqtztTWH/3iRRdyBLw6KMDxnKMchrtbj2OFmehVh0WUCfW3DUrIgx/qFrJPICrq4Z4sTR9UQ==",[m
 			"dev": true[m
 		},[m
 		"globby": {[m
 			"version": "7.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/globby/-/globby-7.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/globby/-/globby-7.1.1.tgz",[m
 			"integrity": "sha1-+yzP+UAfhgCUXfral0QMypcrhoA=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4479,7 +4458,7 @@[m
 		},[m
 		"globule": {[m
 			"version": "1.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/globule/-/globule-1.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/globule/-/globule-1.2.1.tgz",[m
 			"integrity": "sha512-g7QtgWF4uYSL5/dn71WxubOrS7JVGCnFPEnoeChJmBnyR9Mw8nGoEwOgJL/RC2Te0WhbsEUCejfH8SZNJ+adYQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4490,19 +4469,19 @@[m
 		},[m
 		"graceful-fs": {[m
 			"version": "4.1.15",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/graceful-fs/-/graceful-fs-4.1.15.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/graceful-fs/-/graceful-fs-4.1.15.tgz",[m
 			"integrity": "sha512-6uHUhOPEBgQ24HM+r6b/QwWfZq+yiFcipKFrOFiBEnWdy5sdzYoi+pJeQaPI5qOLRFqWmAXUPQNsielzdLoecA==",[m
 			"dev": true[m
 		},[m
 		"har-schema": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/har-schema/-/har-schema-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/har-schema/-/har-schema-2.0.0.tgz",[m
 			"integrity": "sha1-qUwiJOvKwEeCoNkDVSHyRzW37JI=",[m
 			"dev": true[m
 		},[m
 		"har-validator": {[m
 			"version": "5.1.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/har-validator/-/har-validator-5.1.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/har-validator/-/har-validator-5.1.3.tgz",[m
 			"integrity": "sha512-sNvOCzEQNr/qrvJgc3UG/kD4QtlHycrzwS+6mfTrrSq97BvaYcPZZI1ZSqGSPR73Cxn4LKTD4PttRwfU7jWq5g==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4512,7 +4491,7 @@[m
 		},[m
 		"has": {[m
 			"version": "1.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/has/-/has-1.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/has/-/has-1.0.3.tgz",[m
 			"integrity": "sha512-f2dvO0VU6Oej7RkWJGrehjbzMAjFp5/VKPp5tTpWIV4JHHZK1/BxbFRtf/siA2SWTe09caDmVtYYzWEIbBS4zw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4521,7 +4500,7 @@[m
 		},[m
 		"has-ansi": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/has-ansi/-/has-ansi-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/has-ansi/-/has-ansi-2.0.0.tgz",[m
 			"integrity": "sha1-NPUEnOHs3ysGSa8+8k5F7TVBbZE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4530,25 +4509,25 @@[m
 		},[m
 		"has-flag": {[m
 			"version": "3.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/has-flag/-/has-flag-3.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/has-flag/-/has-flag-3.0.0.tgz",[m
 			"integrity": "sha1-tdRU3CGZriJWmfNGfloH87lVuv0=",[m
 			"dev": true[m
 		},[m
 		"has-symbols": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/has-symbols/-/has-symbols-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/has-symbols/-/has-symbols-1.0.0.tgz",[m
 			"integrity": "sha1-uhqPGvKg/DllD1yFA2dwQSIGO0Q=",[m
 			"dev": true[m
 		},[m
 		"has-unicode": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/has-unicode/-/has-unicode-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/has-unicode/-/has-unicode-2.0.1.tgz",[m
 			"integrity": "sha1-4Ob+aijPUROIVeCG0Wkedx3iqLk=",[m
 			"dev": true[m
 		},[m
 		"has-value": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/has-value/-/has-value-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/has-value/-/has-value-1.0.0.tgz",[m
 			"integrity": "sha1-GLKB2lhbHFxR3vJMkw7SmgvmsXc=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4559,7 +4538,7 @@[m
 			"dependencies": {[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				}[m
[36m@@ -4567,7 +4546,7 @@[m
 		},[m
 		"has-values": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/has-values/-/has-values-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/has-values/-/has-values-1.0.0.tgz",[m
 			"integrity": "sha1-lbC2P+whRmGab+V/51Yo1aOe/k8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4577,7 +4556,7 @@[m
 			"dependencies": {[m
 				"is-number": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-number/-/is-number-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-number/-/is-number-3.0.0.tgz",[m
 					"integrity": "sha1-JP1iAaR4LPUFYcgQJ2r8fRLXEZU=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -4586,7 +4565,7 @@[m
 					"dependencies": {[m
 						"kind-of": {[m
 							"version": "3.2.2",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 							"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -4597,7 +4576,7 @@[m
 				},[m
 				"kind-of": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-4.0.0.tgz",[m
 					"integrity": "sha1-IIE989cSkosgc3hpGkUGb65y3Vc=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -4608,7 +4587,7 @@[m
 		},[m
 		"hash-base": {[m
 			"version": "3.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/hash-base/-/hash-base-3.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/hash-base/-/hash-base-3.0.4.tgz",[m
 			"integrity": "sha1-X8hoaEfs1zSZQDMZprCj8/auSRg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4628,13 +4607,13 @@[m
 		},[m
 		"hex-color-regex": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/hex-color-regex/-/hex-color-regex-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/hex-color-regex/-/hex-color-regex-1.1.0.tgz",[m
 			"integrity": "sha512-l9sfDFsuqtOqKDsQdqrMRk0U85RZc0RtOR9yPI7mRVOa4FsR/BVnZ0shmQRM96Ji99kYZP/7hn1cedc1+ApsTQ==",[m
 			"dev": true[m
 		},[m
 		"hmac-drbg": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/hmac-drbg/-/hmac-drbg-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/hmac-drbg/-/hmac-drbg-1.0.1.tgz",[m
 			"integrity": "sha1-0nRXAQJabHdabFRXk+1QL8DGSaE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4645,7 +4624,7 @@[m
 		},[m
 		"home-or-tmp": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/home-or-tmp/-/home-or-tmp-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/home-or-tmp/-/home-or-tmp-2.0.0.tgz",[m
 			"integrity": "sha1-42w/LSyufXRqhX440Y1fMqeILbg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4664,25 +4643,25 @@[m
 		},[m
 		"hosted-git-info": {[m
 			"version": "2.7.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/hosted-git-info/-/hosted-git-info-2.7.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/hosted-git-info/-/hosted-git-info-2.7.1.tgz",[m
 			"integrity": "sha512-7T/BxH19zbcCTa8XkMlbK5lTo1WtgkFi3GvdWEyNuc4Vex7/9Dqbnpsf4JMydcfj9HCg4zUWFTL3Za6lapg5/w==",[m
 			"dev": true[m
 		},[m
 		"hsl-regex": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/hsl-regex/-/hsl-regex-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/hsl-regex/-/hsl-regex-1.0.0.tgz",[m
 			"integrity": "sha1-1JMwx4ntgZ4nakwNJy3/owsY/m4=",[m
 			"dev": true[m
 		},[m
 		"hsla-regex": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/hsla-regex/-/hsla-regex-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/hsla-regex/-/hsla-regex-1.0.0.tgz",[m
 			"integrity": "sha1-wc56MWjIxmFAM6S194d/OyJfnDg=",[m
 			"dev": true[m
 		},[m
 		"html-comment-regex": {[m
 			"version": "1.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/html-comment-regex/-/html-comment-regex-1.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/html-comment-regex/-/html-comment-regex-1.1.2.tgz",[m
 			"integrity": "sha512-P+M65QY2JQ5Y0G9KKdlDpo0zK+/OHptU5AaBwUfAIDJZk1MYf32Frm84EcOytfJE0t5JvkAnKlmjsXDnWzCJmQ==",[m
 			"dev": true[m
 		},[m
[36m@@ -4700,7 +4679,7 @@[m
 		},[m
 		"http-signature": {[m
 			"version": "1.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/http-signature/-/http-signature-1.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/http-signature/-/http-signature-1.2.0.tgz",[m
 			"integrity": "sha1-muzZJRFHcvPZW2WmCruPfBj7rOE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4711,7 +4690,7 @@[m
 		},[m
 		"https-browserify": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/https-browserify/-/https-browserify-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/https-browserify/-/https-browserify-1.0.0.tgz",[m
 			"integrity": "sha1-7AbBDgo0wPL68Zn3/X/Hj//QPHM=",[m
 			"dev": true[m
 		},[m
[36m@@ -4725,13 +4704,13 @@[m
 		},[m
 		"icss-replace-symbols": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/icss-replace-symbols/-/icss-replace-symbols-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/icss-replace-symbols/-/icss-replace-symbols-1.1.0.tgz",[m
 			"integrity": "sha1-Bupvg2ead0njhs/h/oEq5dsiPe0=",[m
 			"dev": true[m
 		},[m
 		"icss-utils": {[m
 			"version": "2.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/icss-utils/-/icss-utils-2.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/icss-utils/-/icss-utils-2.1.0.tgz",[m
 			"integrity": "sha1-g/Cg7DeL8yRheLbCrZE28TWxyWI=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4746,19 +4725,19 @@[m
 		},[m
 		"iferr": {[m
 			"version": "0.1.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/iferr/-/iferr-0.1.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/iferr/-/iferr-0.1.5.tgz",[m
 			"integrity": "sha1-xg7taebY/bazEEofy8ocGS3FtQE=",[m
 			"dev": true[m
 		},[m
 		"ignore": {[m
 			"version": "3.3.10",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ignore/-/ignore-3.3.10.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/ignore/-/ignore-3.3.10.tgz",[m
 			"integrity": "sha512-Pgs951kaMm5GXP7MOvxERINe3gsaVjUWFm+UZPSq9xYriQAksyhg0csnS0KXSNRD5NmNdapXEpjxG49+AKh/ug==",[m
 			"dev": true[m
 		},[m
 		"import-fresh": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/import-fresh/-/import-fresh-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/import-fresh/-/import-fresh-2.0.0.tgz",[m
 			"integrity": "sha1-2BNVwVYS04bGH53dOSLUMEgipUY=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4768,7 +4747,7 @@[m
 			"dependencies": {[m
 				"resolve-from": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/resolve-from/-/resolve-from-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/resolve-from/-/resolve-from-3.0.0.tgz",[m
 					"integrity": "sha1-six699nWiBvItuZTM17rywoYh0g=",[m
 					"dev": true[m
 				}[m
[36m@@ -4776,7 +4755,7 @@[m
 		},[m
 		"import-local": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/import-local/-/import-local-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/import-local/-/import-local-2.0.0.tgz",[m
 			"integrity": "sha512-b6s04m3O+s3CGSbqDIyP4R6aAwAeYlVq9+WUWep6iHa8ETRf9yei1U48C5MmfJmV9AiLYYBKPMq/W+/WRpQmCQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4786,7 +4765,7 @@[m
 			"dependencies": {[m
 				"find-up": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/find-up/-/find-up-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/find-up/-/find-up-3.0.0.tgz",[m
 					"integrity": "sha512-1yD6RmLI1XBfxugvORwlck6f75tYL+iR0jqwsOrOxMZyGYqUuDhJ0l4AXdO1iX/FTs9cBAMEk1gWSEx1kSbylg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -4795,7 +4774,7 @@[m
 				},[m
 				"locate-path": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/locate-path/-/locate-path-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/locate-path/-/locate-path-3.0.0.tgz",[m
 					"integrity": "sha512-7AO748wWnIhNqAuaty2ZWHkQHRSNfPVIsPIfwEOWO22AmaoVrWavlOcMR5nzTLNYvp36X220/maaRsrec1G65A==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -4814,7 +4793,7 @@[m
 				},[m
 				"p-locate": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/p-locate/-/p-locate-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/p-locate/-/p-locate-3.0.0.tgz",[m
 					"integrity": "sha512-x+12w/To+4GFfgJhBEpiDcLozRJGegY+Ei7/z0tSLkMmxGZNybVMSfWj9aJn8Z5Fc7dBUNJOOVgPv2H7IwulSQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -4829,7 +4808,7 @@[m
 				},[m
 				"pkg-dir": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pkg-dir/-/pkg-dir-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/pkg-dir/-/pkg-dir-3.0.0.tgz",[m
 					"integrity": "sha512-/E57AYkoeQ25qkxMj5PBOVgF8Kiu/h7cYS30Z5+R7WaiCCBfLq58ZI/dSeaEKb9WVJV5n/03QwrN3IeWIFllvw==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -4840,19 +4819,19 @@[m
 		},[m
 		"imurmurhash": {[m
 			"version": "0.1.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/imurmurhash/-/imurmurhash-0.1.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/imurmurhash/-/imurmurhash-0.1.4.tgz",[m
 			"integrity": "sha1-khi5srkoojixPcT7a21XbyMUU+o=",[m
 			"dev": true[m
 		},[m
 		"in-publish": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/in-publish/-/in-publish-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/in-publish/-/in-publish-2.0.0.tgz",[m
 			"integrity": "sha1-4g/146KvwmkDILbcVSaCqcf631E=",[m
 			"dev": true[m
 		},[m
 		"indent-string": {[m
 			"version": "2.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/indent-string/-/indent-string-2.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/indent-string/-/indent-string-2.1.0.tgz",[m
 			"integrity": "sha1-ji1INIdCEhtKghi3oTfppSBJ3IA=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4861,19 +4840,19 @@[m
 		},[m
 		"indexes-of": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/indexes-of/-/indexes-of-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/indexes-of/-/indexes-of-1.0.1.tgz",[m
 			"integrity": "sha1-8w9xbI4r00bHtn0985FVZqfAVgc=",[m
 			"dev": true[m
 		},[m
 		"indexof": {[m
 			"version": "0.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/indexof/-/indexof-0.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/indexof/-/indexof-0.0.1.tgz",[m
 			"integrity": "sha1-gtwzbSMrkGIXnQWrMpOmYFn9Q10=",[m
 			"dev": true[m
 		},[m
 		"inflight": {[m
 			"version": "1.0.6",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/inflight/-/inflight-1.0.6.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/inflight/-/inflight-1.0.6.tgz",[m
 			"integrity": "sha1-Sb1jMdfQLQwJvJEKEHW6gWW1bfk=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4883,7 +4862,7 @@[m
 		},[m
 		"inherits": {[m
 			"version": "2.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/inherits/-/inherits-2.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/inherits/-/inherits-2.0.3.tgz",[m
 			"integrity": "sha1-Yzwsg+PaQqUC9SRmAiSA9CCCYd4="[m
 		},[m
 		"ini": {[m
[36m@@ -4900,7 +4879,7 @@[m
 		},[m
 		"invariant": {[m
 			"version": "2.2.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/invariant/-/invariant-2.2.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/invariant/-/invariant-2.2.4.tgz",[m
 			"integrity": "sha512-phJfQVBuaJM5raOpJjSfkiD6BpbCE4Ns//LaXl6wGYtUBY83nWS6Rf9tXm2e8VaK60JEjYldbPif/A2B1C2gNA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4909,7 +4888,7 @@[m
 		},[m
 		"invert-kv": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/invert-kv/-/invert-kv-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/invert-kv/-/invert-kv-1.0.0.tgz",[m
 			"integrity": "sha1-EEqOSqym09jNFXqO+L+rLXo//bY=",[m
 			"dev": true[m
 		},[m
[36m@@ -4920,13 +4899,13 @@[m
 		},[m
 		"is-absolute-url": {[m
 			"version": "2.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-absolute-url/-/is-absolute-url-2.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-absolute-url/-/is-absolute-url-2.1.0.tgz",[m
 			"integrity": "sha1-UFMN+4T8yap9vnhS6Do3uTufKqY=",[m
 			"dev": true[m
 		},[m
 		"is-accessor-descriptor": {[m
 			"version": "0.1.6",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-0.1.6.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-0.1.6.tgz",[m
 			"integrity": "sha1-qeEss66Nh2cn7u84Q/igiXtcmNY=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4935,13 +4914,13 @@[m
 		},[m
 		"is-arrayish": {[m
 			"version": "0.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-arrayish/-/is-arrayish-0.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-arrayish/-/is-arrayish-0.2.1.tgz",[m
 			"integrity": "sha1-d8mYQFJ6qOyxqLppe4BkWnqSap0=",[m
 			"dev": true[m
 		},[m
 		"is-binary-path": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-binary-path/-/is-binary-path-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-binary-path/-/is-binary-path-1.0.1.tgz",[m
 			"integrity": "sha1-dfFmQrSA8YenEcgUFh/TpKdlWJg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4950,19 +4929,19 @@[m
 		},[m
 		"is-buffer": {[m
 			"version": "1.1.6",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-buffer/-/is-buffer-1.1.6.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-buffer/-/is-buffer-1.1.6.tgz",[m
 			"integrity": "sha512-NcdALwpXkTm5Zvvbk7owOUSvVvBKDgKP5/ewfXEznmQFfs4ZRmanOeKBTjRVjka3QFoN6XJ+9F3USqfHqTaU5w==",[m
 			"dev": true[m
 		},[m
 		"is-callable": {[m
 			"version": "1.1.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-callable/-/is-callable-1.1.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-callable/-/is-callable-1.1.4.tgz",[m
 			"integrity": "sha512-r5p9sxJjYnArLjObpjA4xu5EKI3CuKHkJXMhT7kwbpUyIFD1n5PMAsoPvWnvtZiNz7LjkYDRZhd7FlI0eMijEA==",[m
 			"dev": true[m
 		},[m
 		"is-color-stop": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-color-stop/-/is-color-stop-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-color-stop/-/is-color-stop-1.1.0.tgz",[m
 			"integrity": "sha1-z/9HGu5N1cnhWFmPvhKWe1za00U=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4976,7 +4955,7 @@[m
 		},[m
 		"is-data-descriptor": {[m
 			"version": "0.1.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-0.1.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-0.1.4.tgz",[m
 			"integrity": "sha1-C17mSDiOLIYCgueT8YVv7D8wG1Y=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -4985,13 +4964,13 @@[m
 		},[m
 		"is-date-object": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-date-object/-/is-date-object-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-date-object/-/is-date-object-1.0.1.tgz",[m
 			"integrity": "sha1-mqIOtq7rv/d/vTPnTKAbM1gdOhY=",[m
 			"dev": true[m
 		},[m
 		"is-descriptor": {[m
 			"version": "0.1.6",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-0.1.6.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-0.1.6.tgz",[m
 			"integrity": "sha512-avDYr0SB3DwO9zsMov0gKCESFYqCnE4hq/4z3TdUlukEy5t9C0YRq7HLrsN52NAcqXKaepeCD0n+B0arnVG3Hg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5002,7 +4981,7 @@[m
 			"dependencies": {[m
 				"kind-of": {[m
 					"version": "5.1.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-5.1.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-5.1.0.tgz",[m
 					"integrity": "sha512-NGEErnH6F2vUuXDh+OlbcKW7/wOcfdRHaZ7VWtqCztfHri/++YKmP51OdWeGPuqCOba6kk2OTe5d02VmTB80Pw==",[m
 					"dev": true[m
 				}[m
[36m@@ -5010,19 +4989,19 @@[m
 		},[m
 		"is-directory": {[m
 			"version": "0.3.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-directory/-/is-directory-0.3.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-directory/-/is-directory-0.3.1.tgz",[m
 			"integrity": "sha1-YTObbyR1/Hcv2cnYP1yFddwVSuE=",[m
 			"dev": true[m
 		},[m
 		"is-dotfile": {[m
 			"version": "1.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-dotfile/-/is-dotfile-1.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-dotfile/-/is-dotfile-1.0.3.tgz",[m
 			"integrity": "sha1-pqLzL/0t+wT1yiXs0Pa4PPeYoeE=",[m
 			"dev": true[m
 		},[m
 		"is-equal-shallow": {[m
 			"version": "0.1.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-equal-shallow/-/is-equal-shallow-0.1.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-equal-shallow/-/is-equal-shallow-0.1.3.tgz",[m
 			"integrity": "sha1-IjgJj8Ih3gvPpdnqxMRdY4qhxTQ=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5031,19 +5010,19 @@[m
 		},[m
 		"is-extendable": {[m
 			"version": "0.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-extendable/-/is-extendable-0.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-extendable/-/is-extendable-0.1.1.tgz",[m
 			"integrity": "sha1-YrEQ4omkcUGOPsNqYX1HLjAd/Ik=",[m
 			"dev": true[m
 		},[m
 		"is-extglob": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-extglob/-/is-extglob-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-extglob/-/is-extglob-1.0.0.tgz",[m
 			"integrity": "sha1-rEaBd8SUNAWgkvyPKXYMb/xiBsA=",[m
 			"dev": true[m
 		},[m
 		"is-finite": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-finite/-/is-finite-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-finite/-/is-finite-1.0.2.tgz",[m
 			"integrity": "sha1-zGZ3aVYCvlUO8R6LSqYwU0K20Ko=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5052,7 +5031,7 @@[m
 		},[m
 		"is-fullwidth-code-point": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-fullwidth-code-point/-/is-fullwidth-code-point-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-fullwidth-code-point/-/is-fullwidth-code-point-1.0.0.tgz",[m
 			"integrity": "sha1-754xOG8DGn8NZDr4L95QxFfvAMs=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5061,7 +5040,7 @@[m
 		},[m
 		"is-glob": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-glob/-/is-glob-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-glob/-/is-glob-2.0.1.tgz",[m
 			"integrity": "sha1-0Jb5JqPe1WAPP9/ZEZjLCIjC2GM=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5070,7 +5049,7 @@[m
 		},[m
 		"is-number": {[m
 			"version": "2.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-number/-/is-number-2.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-number/-/is-number-2.1.0.tgz",[m
 			"integrity": "sha1-Afy7s5NGOlSPL0ZszhbezknbkI8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5079,13 +5058,13 @@[m
 		},[m
 		"is-obj": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-obj/-/is-obj-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/is-obj/-/is-obj-1.0.1.tgz",[m
 			"integrity": "sha1-PkcprB9f3gJc19g6iW2rn09n2w8=",[m
 			"dev": true[m
 		},[m
 		"is-plain-object": {[m
 			"version": "2.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-plain-object/-/is-plain-object-2.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-plain-object/-/is-plain-object-2.0.4.tgz",[m
 			"integrity": "sha512-h5PpgXkWitc38BBMYawTYMWJHFZJVnBquFE57xFpjB8pJFiF6gZ+bU+WyI/yqXiFR5mdLsgYNaPe8uao6Uv9Og==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5094,7 +5073,7 @@[m
 			"dependencies": {[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				}[m
[36m@@ -5102,19 +5081,19 @@[m
 		},[m
 		"is-posix-bracket": {[m
 			"version": "0.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-posix-bracket/-/is-posix-bracket-0.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-posix-bracket/-/is-posix-bracket-0.1.1.tgz",[m
 			"integrity": "sha1-MzTceXdDaOkvAW5vvAqI9c1ua8Q=",[m
 			"dev": true[m
 		},[m
 		"is-primitive": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-primitive/-/is-primitive-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-primitive/-/is-primitive-2.0.0.tgz",[m
 			"integrity": "sha1-IHurkWOEmcB7Kt8kCkGochADRXU=",[m
 			"dev": true[m
 		},[m
 		"is-regex": {[m
 			"version": "1.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-regex/-/is-regex-1.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-regex/-/is-regex-1.0.4.tgz",[m
 			"integrity": "sha1-VRdIm1RwkbCTDglWVM7SXul+lJE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5123,19 +5102,19 @@[m
 		},[m
 		"is-resolvable": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-resolvable/-/is-resolvable-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-resolvable/-/is-resolvable-1.1.0.tgz",[m
 			"integrity": "sha512-qgDYXFSR5WvEfuS5dMj6oTMEbrrSaM0CrFk2Yiq/gXnBvD9pMa2jGXxyhGLfvhZpuMZe18CJpFxAt3CRs42NMg==",[m
 			"dev": true[m
 		},[m
 		"is-stream": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-stream/-/is-stream-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-stream/-/is-stream-1.1.0.tgz",[m
 			"integrity": "sha1-EtSj3U5o4Lec6428hBc66A2RykQ=",[m
 			"dev": true[m
 		},[m
 		"is-svg": {[m
 			"version": "3.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-svg/-/is-svg-3.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-svg/-/is-svg-3.0.0.tgz",[m
 			"integrity": "sha512-gi4iHK53LR2ujhLVVj+37Ykh9GLqYHX6JOVXbLAucaG/Cqw9xwdFOjDM2qeifLs1sF1npXXFvDu0r5HNgCMrzQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5144,7 +5123,7 @@[m
 		},[m
 		"is-symbol": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-symbol/-/is-symbol-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-symbol/-/is-symbol-1.0.2.tgz",[m
 			"integrity": "sha512-HS8bZ9ox60yCJLH9snBpIwv9pYUAkcuLhSA1oero1UB5y9aiQpRA8y2ex945AOtCZL1lJDeIk3G5LthswI46Lw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5153,37 +5132,37 @@[m
 		},[m
 		"is-typedarray": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-typedarray/-/is-typedarray-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-typedarray/-/is-typedarray-1.0.0.tgz",[m
 			"integrity": "sha1-5HnICFjfDBsR3dppQPlgEfzaSpo=",[m
 			"dev": true[m
 		},[m
 		"is-utf8": {[m
 			"version": "0.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-utf8/-/is-utf8-0.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-utf8/-/is-utf8-0.2.1.tgz",[m
 			"integrity": "sha1-Sw2hRCEE0bM2NA6AeX6GXPOffXI=",[m
 			"dev": true[m
 		},[m
 		"is-windows": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-windows/-/is-windows-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/is-windows/-/is-windows-1.0.2.tgz",[m
 			"integrity": "sha512-eXK1UInq2bPmjyX6e3VHIzMLobc4J94i4AWn+Hpq3OU5KkrRC96OAcR3PRJ/pGu6m8TRnBHP9dkXQVsT/COVIA==",[m
 			"dev": true[m
 		},[m
 		"isarray": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isarray/-/isarray-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/isarray/-/isarray-1.0.0.tgz",[m
 			"integrity": "sha1-u5NdSFgsuhaMBoNJV6VKPgcSTxE=",[m
 			"dev": true[m
 		},[m
 		"isexe": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isexe/-/isexe-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/isexe/-/isexe-2.0.0.tgz",[m
 			"integrity": "sha1-6PvzdNxVb/iUehDcsFctYz8s+hA=",[m
 			"dev": true[m
 		},[m
 		"isobject": {[m
 			"version": "2.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-2.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-2.1.0.tgz",[m
 			"integrity": "sha1-8GVWEJaj8dou9GJy+BXIQNh+DIk=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5192,7 +5171,7 @@[m
 		},[m
 		"isstream": {[m
 			"version": "0.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isstream/-/isstream-0.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/isstream/-/isstream-0.1.2.tgz",[m
 			"integrity": "sha1-R+Y/evVa+m+S4VAOaQ64uFKcCZo=",[m
 			"dev": true[m
 		},[m
[36m@@ -5209,7 +5188,7 @@[m
 		},[m
 		"js-tokens": {[m
 			"version": "3.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/js-tokens/-/js-tokens-3.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/js-tokens/-/js-tokens-3.0.2.tgz",[m
 			"integrity": "sha1-mGbfOVECEw449/mWvOtlRDIJwls=",[m
 			"dev": true[m
 		},[m
[36m@@ -5225,49 +5204,49 @@[m
 		},[m
 		"jsbn": {[m
 			"version": "0.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/jsbn/-/jsbn-0.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/jsbn/-/jsbn-0.1.1.tgz",[m
 			"integrity": "sha1-peZUwuWi3rXyAdls77yoDA7y9RM=",[m
 			"dev": true[m
 		},[m
 		"jsesc": {[m
 			"version": "1.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/jsesc/-/jsesc-1.3.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/jsesc/-/jsesc-1.3.0.tgz",[m
 			"integrity": "sha1-RsP+yMGJKxKwgz25vHYiF226s0s=",[m
 			"dev": true[m
 		},[m
 		"json-parse-better-errors": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/json-parse-better-errors/-/json-parse-better-errors-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/json-parse-better-errors/-/json-parse-better-errors-1.0.2.tgz",[m
 			"integrity": "sha512-mrqyZKfX5EhL7hvqcV6WG1yYjnjeuYDzDhhcAAUrq8Po85NBQBJP+ZDUT75qZQ98IkUoBqdkExkukOU7Ts2wrw==",[m
 			"dev": true[m
 		},[m
 		"json-schema": {[m
 			"version": "0.2.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/json-schema/-/json-schema-0.2.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/json-schema/-/json-schema-0.2.3.tgz",[m
 			"integrity": "sha1-tIDIkuWaLwWVTOcnvT8qTogvnhM=",[m
 			"dev": true[m
 		},[m
 		"json-schema-traverse": {[m
 			"version": "0.4.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/json-schema-traverse/-/json-schema-traverse-0.4.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/json-schema-traverse/-/json-schema-traverse-0.4.1.tgz",[m
 			"integrity": "sha512-xbbCH5dCYU5T8LcEhhuh7HJ88HXuW3qsI3Y0zOZFKfZEHcpWiHU/Jxzk629Brsab/mMiHQti9wMP+845RPe3Vg==",[m
 			"dev": true[m
 		},[m
 		"json-stringify-safe": {[m
 			"version": "5.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/json-stringify-safe/-/json-stringify-safe-5.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/json-stringify-safe/-/json-stringify-safe-5.0.1.tgz",[m
 			"integrity": "sha1-Epai1Y/UXxmg9s4B1lcB4sc1tus=",[m
 			"dev": true[m
 		},[m
 		"json5": {[m
 			"version": "0.5.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/json5/-/json5-0.5.1.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/json5/-/json5-0.5.1.tgz",[m
 			"integrity": "sha1-Hq3nrMASA0rYTiOWdn6tn6VJWCE=",[m
 			"dev": true[m
 		},[m
 		"jsonify": {[m
 			"version": "0.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/jsonify/-/jsonify-0.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/jsonify/-/jsonify-0.0.0.tgz",[m
 			"integrity": "sha1-LHS27kHZPKUbe1qu6PUDYx0lKnM=",[m
 			"dev": true[m
 		},[m
[36m@@ -5290,14 +5269,14 @@[m
 			"dependencies": {[m
 				"ms": {[m
 					"version": "2.1.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ms/-/ms-2.1.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ms/-/ms-2.1.1.tgz",[m
 					"integrity": "sha512-tgp+dl5cGk28utYktBsrFqA7HKgrhgPsg6Z/EfhWI4gl1Hwq8B/GmY/0oXZ6nF8hDVesS/FpnYaD/kOWhYQvyg=="[m
 				}[m
 			}[m
 		},[m
 		"jsprim": {[m
 			"version": "1.4.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/jsprim/-/jsprim-1.4.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/jsprim/-/jsprim-1.4.1.tgz",[m
 			"integrity": "sha1-MT5mvB5cwG5Di8G3SZwuXFastqI=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5328,13 +5307,13 @@[m
 		},[m
 		"kareem": {[m
 			"version": "2.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kareem/-/kareem-2.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/kareem/-/kareem-2.3.0.tgz",[m
 			"integrity": "sha512-6hHxsp9e6zQU8nXsP+02HGWXwTkOEw6IROhF2ZA28cYbUk4eJ6QbtZvdqZOdD9YPKghG3apk5eOCvs+tLl3lRg==",[m
 			"dev": true[m
 		},[m
 		"kind-of": {[m
 			"version": "3.2.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 			"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5343,7 +5322,7 @@[m
 		},[m
 		"last-call-webpack-plugin": {[m
 			"version": "3.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/last-call-webpack-plugin/-/last-call-webpack-plugin-3.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/last-call-webpack-plugin/-/last-call-webpack-plugin-3.0.0.tgz",[m
 			"integrity": "sha512-7KI2l2GIZa9p2spzPIVZBYyNKkN+e/SQPpnjlTiPhdbDW3F86tdKKELxKpzJ5sgU19wQWsACULZmpTPYHeWO5w==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5353,7 +5332,7 @@[m
 		},[m
 		"lcid": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lcid/-/lcid-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lcid/-/lcid-1.0.0.tgz",[m
 			"integrity": "sha1-MIrMr6C8SDo4Z7S28rlQYlHRuDU=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5362,7 +5341,7 @@[m
 		},[m
 		"load-json-file": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/load-json-file/-/load-json-file-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/load-json-file/-/load-json-file-1.1.0.tgz",[m
 			"integrity": "sha1-lWkFcI1YtLq0wiYbBPWfMcmTdMA=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5375,7 +5354,7 @@[m
 			"dependencies": {[m
 				"pify": {[m
 					"version": "2.3.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pify/-/pify-2.3.0.tgz",[m
[32m+[m					[32m"resolved": "http://registry.npmjs.org/pify/-/pify-2.3.0.tgz",[m
 					"integrity": "sha1-7RQaasBDqEnqWISY59yosVMw6Qw=",[m
 					"dev": true[m
 				}[m
[36m@@ -5417,7 +5396,7 @@[m
 		},[m
 		"locate-path": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/locate-path/-/locate-path-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/locate-path/-/locate-path-2.0.0.tgz",[m
 			"integrity": "sha1-K1aLJl7slExtnA3pw9u7ygNUzY4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5427,43 +5406,43 @@[m
 		},[m
 		"lodash": {[m
 			"version": "4.17.11",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lodash/-/lodash-4.17.11.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lodash/-/lodash-4.17.11.tgz",[m
 			"integrity": "sha512-cQKh8igo5QUhZ7lg38DYWAxMvjSAKG0A8wGSVimP07SIUEK2UO+arSRKbRZWtelMtN5V0Hkwh5ryOto/SshYIg=="[m
 		},[m
 		"lodash.debounce": {[m
 			"version": "4.0.8",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lodash.debounce/-/lodash.debounce-4.0.8.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lodash.debounce/-/lodash.debounce-4.0.8.tgz",[m
 			"integrity": "sha1-gteb/zCmfEAF/9XiUVMArZyk168=",[m
 			"dev": true[m
 		},[m
 		"lodash.includes": {[m
 			"version": "4.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lodash.includes/-/lodash.includes-4.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lodash.includes/-/lodash.includes-4.3.0.tgz",[m
 			"integrity": "sha1-YLuYqHy5I8aMoeUTJUgzFISfVT8="[m
 		},[m
 		"lodash.isboolean": {[m
 			"version": "3.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lodash.isboolean/-/lodash.isboolean-3.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lodash.isboolean/-/lodash.isboolean-3.0.3.tgz",[m
 			"integrity": "sha1-bC4XHbKiV82WgC/UOwGyDV9YcPY="[m
 		},[m
 		"lodash.isinteger": {[m
 			"version": "4.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lodash.isinteger/-/lodash.isinteger-4.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lodash.isinteger/-/lodash.isinteger-4.0.4.tgz",[m
 			"integrity": "sha1-YZwK89A/iwTDH1iChAt3sRzWg0M="[m
 		},[m
 		"lodash.isnumber": {[m
 			"version": "3.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lodash.isnumber/-/lodash.isnumber-3.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lodash.isnumber/-/lodash.isnumber-3.0.3.tgz",[m
 			"integrity": "sha1-POdoEMWSjQM1IwGsKHMX8RwLH/w="[m
 		},[m
 		"lodash.isplainobject": {[m
 			"version": "4.0.6",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lodash.isplainobject/-/lodash.isplainobject-4.0.6.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lodash.isplainobject/-/lodash.isplainobject-4.0.6.tgz",[m
 			"integrity": "sha1-fFJqUtibRcRcxpC4gWO+BJf1UMs="[m
 		},[m
 		"lodash.isstring": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lodash.isstring/-/lodash.isstring-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lodash.isstring/-/lodash.isstring-4.0.1.tgz",[m
 			"integrity": "sha1-1SfftUVuynzJu5XV2ur4i6VKVFE="[m
 		},[m
 		"lodash.memoize": {[m
[36m@@ -5474,24 +5453,24 @@[m
 		},[m
 		"lodash.once": {[m
 			"version": "4.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lodash.once/-/lodash.once-4.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lodash.once/-/lodash.once-4.1.1.tgz",[m
 			"integrity": "sha1-DdOXEhPHxW34gJd9UEyI+0cal6w="[m
 		},[m
 		"lodash.tail": {[m
 			"version": "4.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lodash.tail/-/lodash.tail-4.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lodash.tail/-/lodash.tail-4.1.1.tgz",[m
 			"integrity": "sha1-0jM6NtnncXyK0vfKyv7HwytERmQ=",[m
 			"dev": true[m
 		},[m
 		"lodash.uniq": {[m
 			"version": "4.5.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lodash.uniq/-/lodash.uniq-4.5.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/lodash.uniq/-/lodash.uniq-4.5.0.tgz",[m
 			"integrity": "sha1-0CJTc662Uq3BvILklFM5qEJ1R3M=",[m
 			"dev": true[m
 		},[m
 		"loose-envify": {[m
 			"version": "1.4.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/loose-envify/-/loose-envify-1.4.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/loose-envify/-/loose-envify-1.4.0.tgz",[m
 			"integrity": "sha512-lyuxPGr/Wfhrlem2CL/UcnUc1zcqKAImBDzukY7Y5F/yQiNdko6+fRLevlw1HgMySw7f611UIY408EtxRSoK3Q==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5500,7 +5479,7 @@[m
 		},[m
 		"loud-rejection": {[m
 			"version": "1.6.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/loud-rejection/-/loud-rejection-1.6.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/loud-rejection/-/loud-rejection-1.6.0.tgz",[m
 			"integrity": "sha1-W0b4AUft7leIcPCG0Eghz5mOVR8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5520,7 +5499,7 @@[m
 		},[m
 		"make-dir": {[m
 			"version": "1.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/make-dir/-/make-dir-1.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/make-dir/-/make-dir-1.3.0.tgz",[m
 			"integrity": "sha512-2w31R7SJtieJJnQtGc7RVL2StM2vGYVfqUOvUDxH6bC6aJTxPxTF0GnIgCyu7tjockiUWAYQRbxa7vKn34s5sQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5535,7 +5514,7 @@[m
 		},[m
 		"map-age-cleaner": {[m
 			"version": "0.1.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/map-age-cleaner/-/map-age-cleaner-0.1.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/map-age-cleaner/-/map-age-cleaner-0.1.3.tgz",[m
 			"integrity": "sha512-bJzx6nMoP6PDLPBFmg7+xRKeFZvFboMrGlxmNj9ClvX53KrmvM5bXFXEWjbz4cz1AFn+jWJ9z/DJSz7hrs0w3w==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5544,19 +5523,19 @@[m
 		},[m
 		"map-cache": {[m
 			"version": "0.2.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/map-cache/-/map-cache-0.2.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/map-cache/-/map-cache-0.2.2.tgz",[m
 			"integrity": "sha1-wyq9C9ZSXZsFFkW7TyasXcmKDb8=",[m
 			"dev": true[m
 		},[m
 		"map-obj": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/map-obj/-/map-obj-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/map-obj/-/map-obj-1.0.1.tgz",[m
 			"integrity": "sha1-2TPOuSBdgr3PSIb2dCvcK03qFG0=",[m
 			"dev": true[m
 		},[m
 		"map-visit": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/map-visit/-/map-visit-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/map-visit/-/map-visit-1.0.0.tgz",[m
 			"integrity": "sha1-7Nyo8TFE5mDxtb1B8S80edmN+48=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5571,7 +5550,7 @@[m
 		},[m
 		"md5.js": {[m
 			"version": "1.3.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/md5.js/-/md5.js-1.3.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/md5.js/-/md5.js-1.3.5.tgz",[m
 			"integrity": "sha512-xitP+WxNPcTTOgnTJcrhM0xvdPepipPSf3I8EIpGKeFLjt3PlJLIDG3u8EX53ZIubkb+5U2+3rELYpEhHhzdkg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5582,13 +5561,13 @@[m
 		},[m
 		"mdn-data": {[m
 			"version": "1.1.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/mdn-data/-/mdn-data-1.1.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/mdn-data/-/mdn-data-1.1.4.tgz",[m
 			"integrity": "sha512-FSYbp3lyKjyj3E7fMl6rYvUdX0FBXaluGqlFoYESWQlyUTq8R+wp0rkFxoYFqZlHCvsUXGjyJmLQSnXToYhOSA==",[m
 			"dev": true[m
 		},[m
 		"media-typer": {[m
 			"version": "0.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/media-typer/-/media-typer-0.3.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/media-typer/-/media-typer-0.3.0.tgz",[m
 			"integrity": "sha1-hxDXrwqmJvj/+hzgAWhUUmMlV0g="[m
 		},[m
 		"mem": {[m
[36m@@ -5604,7 +5583,7 @@[m
 		},[m
 		"memory-fs": {[m
 			"version": "0.4.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/memory-fs/-/memory-fs-0.4.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/memory-fs/-/memory-fs-0.4.1.tgz",[m
 			"integrity": "sha1-OpoguEYlI+RHz7x+i7gO1me/xVI=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5621,13 +5600,13 @@[m
 		},[m
 		"memorystream": {[m
 			"version": "0.3.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/memorystream/-/memorystream-0.3.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/memorystream/-/memorystream-0.3.1.tgz",[m
 			"integrity": "sha1-htcJCzDORV1j+64S3aUaR93K+bI=",[m
 			"dev": true[m
 		},[m
 		"meow": {[m
 			"version": "3.7.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/meow/-/meow-3.7.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/meow/-/meow-3.7.0.tgz",[m
 			"integrity": "sha1-cstmi0JSKCkKu/qFaJJYcwioAfs=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5645,7 +5624,7 @@[m
 			"dependencies": {[m
 				"minimist": {[m
 					"version": "1.2.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/minimist/-/minimist-1.2.0.tgz",[m
[32m+[m					[32m"resolved": "http://registry.npmjs.org/minimist/-/minimist-1.2.0.tgz",[m
 					"integrity": "sha1-o1AIsg9BOD7sH7kU9M1d95omQoQ=",[m
 					"dev": true[m
 				}[m
[36m@@ -5653,17 +5632,17 @@[m
 		},[m
 		"merge-descriptors": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/merge-descriptors/-/merge-descriptors-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/merge-descriptors/-/merge-descriptors-1.0.1.tgz",[m
 			"integrity": "sha1-sAqqVW3YtEVoFQ7J0blT8/kMu2E="[m
 		},[m
 		"methods": {[m
 			"version": "1.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/methods/-/methods-1.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/methods/-/methods-1.1.2.tgz",[m
 			"integrity": "sha1-VSmk1nZUE07cxSZmVoNbD4Ua/O4="[m
 		},[m
 		"micromatch": {[m
 			"version": "2.3.11",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/micromatch/-/micromatch-2.3.11.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/micromatch/-/micromatch-2.3.11.tgz",[m
 			"integrity": "sha1-hmd8l9FyCzY0MdBNDRUpO9OMFWU=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5684,7 +5663,7 @@[m
 		},[m
 		"miller-rabin": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/miller-rabin/-/miller-rabin-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/miller-rabin/-/miller-rabin-4.0.1.tgz",[m
 			"integrity": "sha512-115fLhvZVqWwHPbClyntxEVfVDfl9DLLTuJvq3g2O/Oxi8AiNouAHvDSzHS0viUJc+V5vm3eq91Xwqn9dp4jRA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5694,7 +5673,7 @@[m
 		},[m
 		"mime": {[m
 			"version": "1.4.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/mime/-/mime-1.4.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/mime/-/mime-1.4.1.tgz",[m
 			"integrity": "sha512-KI1+qOZu5DcW6wayYHSzR/tXKCDC5Om4s1z2QJjDULzLcmf3DvzS7oluY4HCTrc+9FiKmWUgeNLg7W3uIQvxtQ=="[m
 		},[m
 		"mime-db": {[m
[36m@@ -5718,7 +5697,7 @@[m
 		},[m
 		"mini-css-extract-plugin": {[m
 			"version": "0.4.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/mini-css-extract-plugin/-/mini-css-extract-plugin-0.4.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/mini-css-extract-plugin/-/mini-css-extract-plugin-0.4.5.tgz",[m
 			"integrity": "sha512-dqBanNfktnp2hwL2YguV9Jh91PFX7gu7nRLs4TGsbAfAG6WOtlynFRYzwDwmmeSb5uIwHo9nx1ta0f7vAZVp2w==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5729,19 +5708,19 @@[m
 		},[m
 		"minimalistic-assert": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/minimalistic-assert/-/minimalistic-assert-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/minimalistic-assert/-/minimalistic-assert-1.0.1.tgz",[m
 			"integrity": "sha512-UtJcAD4yEaGtjPezWuO9wC4nwUnVH/8/Im3yEHQP4b67cXlD/Qr9hdITCU1xDbSEXg2XKNaP8jsReV7vQd00/A==",[m
 			"dev": true[m
 		},[m
 		"minimalistic-crypto-utils": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/minimalistic-crypto-utils/-/minimalistic-crypto-utils-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/minimalistic-crypto-utils/-/minimalistic-crypto-utils-1.0.1.tgz",[m
 			"integrity": "sha1-9sAMHAsIIkblxNmd+4x8CDsrWCo=",[m
 			"dev": true[m
 		},[m
 		"minimatch": {[m
 			"version": "3.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/minimatch/-/minimatch-3.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/minimatch/-/minimatch-3.0.4.tgz",[m
 			"integrity": "sha512-yJHVQEhyqPLUTgt9B83PXu6W3rx4MvvHvSUvToogpwoGDOUQ+yDrR0HRot+yOCdCO7u4hX3pWft6kWBBcqh0UA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5750,7 +5729,7 @@[m
 		},[m
 		"minimist": {[m
 			"version": "0.0.8",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/minimist/-/minimist-0.0.8.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/minimist/-/minimist-0.0.8.tgz",[m
 			"integrity": "sha1-hX/Kv8M5fSYluCKCYuhqp6ARsF0=",[m
 			"dev": true[m
 		},[m
[36m@@ -5783,7 +5762,7 @@[m
 		},[m
 		"mississippi": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/mississippi/-/mississippi-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/mississippi/-/mississippi-2.0.0.tgz",[m
 			"integrity": "sha512-zHo8v+otD1J10j/tC+VNoGK9keCuByhKovAvdn74dmxJl9+mWHnx6EMsDN4lgRoMI/eYo2nchAxniIbUPb5onw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5801,7 +5780,7 @@[m
 		},[m
 		"mixin-deep": {[m
 			"version": "1.3.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/mixin-deep/-/mixin-deep-1.3.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/mixin-deep/-/mixin-deep-1.3.1.tgz",[m
 			"integrity": "sha512-8ZItLHeEgaqEvd5lYBXfm4EZSFCX29Jb9K+lAHhDKzReKBQKj3R+7NOF6tjqYi9t4oI8VUfaWITJQm86wnXGNQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5811,7 +5790,7 @@[m
 			"dependencies": {[m
 				"is-extendable": {[m
 					"version": "1.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-extendable/-/is-extendable-1.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-extendable/-/is-extendable-1.0.1.tgz",[m
 					"integrity": "sha512-arnXMxT1hhoKo9k1LZdmlNyJdDDfy2v0fXjFlmok4+i8ul/6WlbVge9bhM74OpNPQPMGUToDtz+KXa1PneJxOA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -5822,7 +5801,7 @@[m
 		},[m
 		"mixin-object": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/mixin-object/-/mixin-object-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/mixin-object/-/mixin-object-2.0.1.tgz",[m
 			"integrity": "sha1-T7lJRB2rGCVA8f4DW6YOGUel5X4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5832,7 +5811,7 @@[m
 			"dependencies": {[m
 				"for-in": {[m
 					"version": "0.1.8",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/for-in/-/for-in-0.1.8.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/for-in/-/for-in-0.1.8.tgz",[m
 					"integrity": "sha1-2Hc5COMSVhCZUrH9ubP6hn0ndeE=",[m
 					"dev": true[m
 				}[m
[36m@@ -5840,7 +5819,7 @@[m
 		},[m
 		"mkdirp": {[m
 			"version": "0.5.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/mkdirp/-/mkdirp-0.5.1.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/mkdirp/-/mkdirp-0.5.1.tgz",[m
 			"integrity": "sha1-MAV0OOrGz3+MR2fzhkjWaX11yQM=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5900,13 +5879,13 @@[m
 		},[m
 		"mongoose-legacy-pluralize": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/mongoose-legacy-pluralize/-/mongoose-legacy-pluralize-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/mongoose-legacy-pluralize/-/mongoose-legacy-pluralize-1.0.2.tgz",[m
 			"integrity": "sha512-Yo/7qQU4/EyIS8YDFSeenIvXxZN+ld7YdV9LqFVQJzTLye8unujAWPZ4NWKfFA+RNjh+wvTWKY9Z3E5XM6ZZiQ==",[m
 			"dev": true[m
 		},[m
 		"move-concurrently": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/move-concurrently/-/move-concurrently-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/move-concurrently/-/move-concurrently-1.0.1.tgz",[m
 			"integrity": "sha1-viwAX9oy4LKa8fBdfEszIUxwH5I=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5926,7 +5905,7 @@[m
 		},[m
 		"mquery": {[m
 			"version": "3.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/mquery/-/mquery-3.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/mquery/-/mquery-3.2.0.tgz",[m
 			"integrity": "sha512-qPJcdK/yqcbQiKoemAt62Y0BAc0fTEKo1IThodBD+O5meQRJT/2HSe5QpBNwaa4CjskoGrYWsEyjkqgiE0qjhg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5939,13 +5918,13 @@[m
 			"dependencies": {[m
 				"bluebird": {[m
 					"version": "3.5.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/bluebird/-/bluebird-3.5.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/bluebird/-/bluebird-3.5.1.tgz",[m
 					"integrity": "sha512-MKiLiV+I1AA596t9w1sQJ8jkiSr5+ZKi0WKrYGUn6d1Fx+Ij4tIj+m2WMQSGczs5jZVxV339chE8iwk6F64wjA==",[m
 					"dev": true[m
 				},[m
 				"debug": {[m
 					"version": "3.1.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/debug/-/debug-3.1.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/debug/-/debug-3.1.0.tgz",[m
 					"integrity": "sha512-OX8XqP7/1a9cqkxYw2yXss15f26NKWBpDXQd0/uK/KPqdQhxbPa994hnzjcE2VqQpDslf55723cKPUOGSmMY3g==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -5956,12 +5935,12 @@[m
 		},[m
 		"ms": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ms/-/ms-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/ms/-/ms-2.0.0.tgz",[m
 			"integrity": "sha1-VgiurfwAvmwpAd9fmGF4jeDVl8g="[m
 		},[m
 		"multer": {[m
 			"version": "1.4.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/multer/-/multer-1.4.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/multer/-/multer-1.4.1.tgz",[m
 			"integrity": "sha512-zzOLNRxzszwd+61JFuAo0fxdQfvku12aNJgnla0AQ+hHxFmfc/B7jBVuPr5Rmvu46Jze/iJrFpSOsD7afO8SDw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -5983,7 +5962,7 @@[m
 		},[m
 		"nanomatch": {[m
 			"version": "1.2.13",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/nanomatch/-/nanomatch-1.2.13.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/nanomatch/-/nanomatch-1.2.13.tgz",[m
 			"integrity": "sha512-fpoe2T0RbHwBTBUOftAfBPaDEi06ufaUai0mE6Yn1kacc3SnTErfb/h+X94VXzI64rKFHYImXSvdwGGCmwOqCA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6002,19 +5981,19 @@[m
 			"dependencies": {[m
 				"arr-diff": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/arr-diff/-/arr-diff-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/arr-diff/-/arr-diff-4.0.0.tgz",[m
 					"integrity": "sha1-1kYQdP6/7HHn4VI1dhoyml3HxSA=",[m
 					"dev": true[m
 				},[m
 				"array-unique": {[m
 					"version": "0.3.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-unique/-/array-unique-0.3.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/array-unique/-/array-unique-0.3.2.tgz",[m
 					"integrity": "sha1-qJS3XUvE9s1nnvMkSp/Y9Gri1Cg=",[m
 					"dev": true[m
 				},[m
 				"kind-of": {[m
 					"version": "6.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-6.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-6.0.2.tgz",[m
 					"integrity": "sha512-s5kLOcnH0XqDO+FvuaLX8DDjZ18CGFk7VygH40QoKPUQhW4e2rvM0rwUq0t8IQDOwYSeLK01U90OjzBTme2QqA==",[m
 					"dev": true[m
 				}[m
[36m@@ -6022,24 +6001,24 @@[m
 		},[m
 		"negotiator": {[m
 			"version": "0.6.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/negotiator/-/negotiator-0.6.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/negotiator/-/negotiator-0.6.1.tgz",[m
 			"integrity": "sha1-KzJxhOiZIQEXeyhWP7XnECrNDKk="[m
 		},[m
 		"neo-async": {[m
 			"version": "2.6.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/neo-async/-/neo-async-2.6.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/neo-async/-/neo-async-2.6.0.tgz",[m
 			"integrity": "sha512-MFh0d/Wa7vkKO3Y3LlacqAEeHK0mckVqzDieUKTT+KGxi+zIpeVsFxymkIiRpbpDziHc290Xr9A1O4Om7otoRA==",[m
 			"dev": true[m
 		},[m
 		"nice-try": {[m
 			"version": "1.0.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/nice-try/-/nice-try-1.0.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/nice-try/-/nice-try-1.0.5.tgz",[m
 			"integrity": "sha512-1nh45deeb5olNY7eX82BkPO7SSxR5SSYJiPTrTdFUVYwAl8CKMA5N9PjTYkHiRjisVcxcQ1HXdLhx2qxxJzLNQ==",[m
 			"dev": true[m
 		},[m
 		"node-gyp": {[m
 			"version": "3.8.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/node-gyp/-/node-gyp-3.8.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/node-gyp/-/node-gyp-3.8.0.tgz",[m
 			"integrity": "sha512-3g8lYefrRRzvGeSowdJKAKyks8oUpLEd/DyPV4eMhVlhJ0aNaZqIrNUIPuEWWTAoPqyFkfGrM67MC69baqn6vA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6057,21 +6036,44 @@[m
 				"which": "1"[m
 			},[m
 			"dependencies": {[m
[32m+[m				[32m"fstream": {[m
[32m+[m					[32m"version": "1.0.12",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/fstream/-/fstream-1.0.12.tgz",[m
[32m+[m					[32m"integrity": "sha512-WvJ193OHa0GHPEL+AycEJgxvBEwyfRkN1vhjca23OaPVMCaLCXTd5qAu82AjTcgP1UJmytkOKb63Ypde7raDIg==",[m
[32m+[m					[32m"dev": true,[m
[32m+[m					[32m"requires": {[m
[32m+[m						[32m"graceful-fs": "^4.1.2",[m
[32m+[m						[32m"inherits": "~2.0.0",[m
[32m+[m						[32m"mkdirp": ">=0.5 0",[m
[32m+[m						[32m"rimraf": "2"[m
[32m+[m					[32m}[m
[32m+[m				[32m},[m
 				"semver": {[m
 					"version": "5.3.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/semver/-/semver-5.3.0.tgz",[m
[32m+[m					[32m"resolved": "http://registry.npmjs.org/semver/-/semver-5.3.0.tgz",[m
 					"integrity": "sha1-myzl094C0XxgEq0yaqa00M9U+U8=",[m
 					"dev": true[m
 				},[m
 				"tar": {[m
[31m-					"version": "2.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/tar/-/tar-2.2.1.tgz",[m
[31m-					"integrity": "sha1-jk0qJWwOIYXGsYrWlK7JaLg8sdE=",[m
[32m+[m					[32m"version": "2.2.2",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/tar/-/tar-2.2.2.tgz",[m
[32m+[m					[32m"integrity": "sha512-FCEhQ/4rE1zYv9rYXJw/msRqsnmlje5jHP6huWeBZ704jUTy02c5AZyWujpMR1ax6mVw9NyJMfuK2CMDWVIfgA==",[m
 					"dev": true,[m
 					"requires": {[m
 						"block-stream": "*",[m
[31m-						"fstream": "^1.0.2",[m
[32m+[m						[32m"fstream": "^1.0.12",[m
 						"inherits": "2"[m
[32m+[m					[32m},[m
[32m+[m					[32m"dependencies": {[m
[32m+[m						[32m"block-stream": {[m
[32m+[m							[32m"version": "0.0.9",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/block-stream/-/block-stream-0.0.9.tgz",[m
[32m+[m							[32m"integrity": "sha1-E+v+d4oDIFz+A3UUgeu0szAMEmo=",[m
[32m+[m							[32m"dev": true,[m
[32m+[m							[32m"requires": {[m
[32m+[m								[32m"inherits": "~2.0.0"[m
[32m+[m							[32m}[m
[32m+[m						[32m}[m
 					}[m
 				}[m
 			}[m
[36m@@ -6151,12 +6153,12 @@[m
 		},[m
 		"nodemailer": {[m
 			"version": "4.7.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/nodemailer/-/nodemailer-4.7.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/nodemailer/-/nodemailer-4.7.0.tgz",[m
 			"integrity": "sha512-IludxDypFpYw4xpzKdMAozBSkzKHmNBvGanUREjJItgJ2NYcK/s8+PggVhj7c2yGFQykKsnnmv1+Aqo0ZfjHmw=="[m
 		},[m
 		"nopt": {[m
 			"version": "3.0.6",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/nopt/-/nopt-3.0.6.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/nopt/-/nopt-3.0.6.tgz",[m
 			"integrity": "sha1-xkZdvwirzU2zWTF/eaxopkayj/k=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6177,7 +6179,7 @@[m
 		},[m
 		"normalize-path": {[m
 			"version": "2.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/normalize-path/-/normalize-path-2.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/normalize-path/-/normalize-path-2.1.1.tgz",[m
 			"integrity": "sha1-GrKLVW4Zg2Oowab35vogE3/mrtk=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6186,7 +6188,7 @@[m
 		},[m
 		"normalize-url": {[m
 			"version": "3.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/normalize-url/-/normalize-url-3.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/normalize-url/-/normalize-url-3.3.0.tgz",[m
 			"integrity": "sha512-U+JJi7duF1o+u2pynbp2zXDW2/PADgC30f0GsHZtRh+HOcXHnw137TrNlyxxRvWW5fjKd3bcLHPxofWuCjaeZg==",[m
 			"dev": true[m
 		},[m
[36m@@ -6209,7 +6211,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6229,7 +6231,7 @@[m
 				},[m
 				"cross-spawn": {[m
 					"version": "6.0.5",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cross-spawn/-/cross-spawn-6.0.5.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/cross-spawn/-/cross-spawn-6.0.5.tgz",[m
 					"integrity": "sha512-eTVLrBSt7fjbDygz805pMnstIs2VTBNkRm0qxZd+M7A5XDdxVRWO5MxGBXZhjY4cqLYLdtrGqRf8mBPmzwSpWQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6242,7 +6244,7 @@[m
 				},[m
 				"load-json-file": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/load-json-file/-/load-json-file-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/load-json-file/-/load-json-file-4.0.0.tgz",[m
 					"integrity": "sha1-L19Fq5HjMhYjT9U62rZo607AmTs=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6254,7 +6256,7 @@[m
 				},[m
 				"parse-json": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/parse-json/-/parse-json-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/parse-json/-/parse-json-4.0.0.tgz",[m
 					"integrity": "sha1-vjX1Qlvh9/bHRxhPmKeIy5lHfuA=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6264,7 +6266,7 @@[m
 				},[m
 				"read-pkg": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/read-pkg/-/read-pkg-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/read-pkg/-/read-pkg-3.0.0.tgz",[m
 					"integrity": "sha1-nLxoaXj+5l0WwA4rGcI3/Pbjg4k=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6275,13 +6277,13 @@[m
 				},[m
 				"strip-bom": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/strip-bom/-/strip-bom-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/strip-bom/-/strip-bom-3.0.0.tgz",[m
 					"integrity": "sha1-IzTBjpx1n3vdVv3vfprj1YjmjtM=",[m
 					"dev": true[m
 				},[m
 				"supports-color": {[m
 					"version": "5.5.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/supports-color/-/supports-color-5.5.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/supports-color/-/supports-color-5.5.0.tgz",[m
 					"integrity": "sha512-QjVjwdXIt408MIiAqCX4oUKsgU2EqAGzs2Ppkm4aQYbjm+ZEWEcW4SfFNTr4uMNZma0ey4f5lgLrkB0aX0QMow==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6292,7 +6294,7 @@[m
 		},[m
 		"npm-run-path": {[m
 			"version": "2.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/npm-run-path/-/npm-run-path-2.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/npm-run-path/-/npm-run-path-2.0.2.tgz",[m
 			"integrity": "sha1-NakjLfo11wZ7TLLd8jV7GHFTbF8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6301,7 +6303,7 @@[m
 		},[m
 		"npmlog": {[m
 			"version": "4.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/npmlog/-/npmlog-4.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/npmlog/-/npmlog-4.1.2.tgz",[m
 			"integrity": "sha512-2uUqazuKlTaSI/dC8AzicUck7+IrEaOnN/e0jd3Xtt1KcGpwx30v50mL7oPyr/h9bL3E4aZccVwpwP+5W9Vjkg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6313,7 +6315,7 @@[m
 		},[m
 		"nth-check": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/nth-check/-/nth-check-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/nth-check/-/nth-check-1.0.2.tgz",[m
 			"integrity": "sha512-WeBOdju8SnzPN5vTUJYxYUxLeXpCaVP5i5e0LF8fg7WORF2Wd7wFX/pk0tYZk7s8T+J7VLy0Da6J1+wCT0AtHg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6322,25 +6324,25 @@[m
 		},[m
 		"number-is-nan": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/number-is-nan/-/number-is-nan-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/number-is-nan/-/number-is-nan-1.0.1.tgz",[m
 			"integrity": "sha1-CXtgK1NCKlIsGvuHkDGDNpQaAR0=",[m
 			"dev": true[m
 		},[m
 		"oauth-sign": {[m
 			"version": "0.9.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/oauth-sign/-/oauth-sign-0.9.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/oauth-sign/-/oauth-sign-0.9.0.tgz",[m
 			"integrity": "sha512-fexhUFFPTGV8ybAtSIGbV6gOkSv8UtRbDBnAyLQw4QPKkgNlsH2ByPGtMUqdWkos6YCRmAqViwgZrJc/mRDzZQ==",[m
 			"dev": true[m
 		},[m
 		"object-assign": {[m
 			"version": "4.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/object-assign/-/object-assign-4.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/object-assign/-/object-assign-4.1.1.tgz",[m
 			"integrity": "sha1-IQmtx5ZYh8/AXLvUQsrIv7s2CGM=",[m
 			"dev": true[m
 		},[m
 		"object-copy": {[m
 			"version": "0.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/object-copy/-/object-copy-0.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/object-copy/-/object-copy-0.1.0.tgz",[m
 			"integrity": "sha1-fn2Fi3gb18mRpBupde04EnVOmYw=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6351,7 +6353,7 @@[m
 			"dependencies": {[m
 				"define-property": {[m
 					"version": "0.2.5",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-0.2.5.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-0.2.5.tgz",[m
 					"integrity": "sha1-w1se+RjsPJkPmlvFe+BKrOxcgRY=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6368,7 +6370,7 @@[m
 		},[m
 		"object-visit": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/object-visit/-/object-visit-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/object-visit/-/object-visit-1.0.1.tgz",[m
 			"integrity": "sha1-95xEk68MU3e1n+OdOV5BBC3QRbs=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6377,7 +6379,7 @@[m
 			"dependencies": {[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				}[m
[36m@@ -6385,7 +6387,7 @@[m
 		},[m
 		"object.getownpropertydescriptors": {[m
 			"version": "2.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/object.getownpropertydescriptors/-/object.getownpropertydescriptors-2.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/object.getownpropertydescriptors/-/object.getownpropertydescriptors-2.0.3.tgz",[m
 			"integrity": "sha1-h1jIRvW0B62rDyNuCYbxSwUcqhY=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6395,7 +6397,7 @@[m
 		},[m
 		"object.omit": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/object.omit/-/object.omit-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/object.omit/-/object.omit-2.0.1.tgz",[m
 			"integrity": "sha1-Gpx0SCnznbuFjHbKNXmuKlTr0fo=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6405,7 +6407,7 @@[m
 		},[m
 		"object.pick": {[m
 			"version": "1.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/object.pick/-/object.pick-1.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/object.pick/-/object.pick-1.3.0.tgz",[m
 			"integrity": "sha1-h6EKxMFpS9Lhy/U1kaZhQftd10c=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6414,7 +6416,7 @@[m
 			"dependencies": {[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				}[m
[36m@@ -6434,7 +6436,7 @@[m
 		},[m
 		"on-finished": {[m
 			"version": "2.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/on-finished/-/on-finished-2.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/on-finished/-/on-finished-2.3.0.tgz",[m
 			"integrity": "sha1-IPEzZIGwg811M3mSoWlxqi2QaUc=",[m
 			"requires": {[m
 				"ee-first": "1.1.1"[m
[36m@@ -6447,7 +6449,7 @@[m
 		},[m
 		"once": {[m
 			"version": "1.4.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/once/-/once-1.4.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/once/-/once-1.4.0.tgz",[m
 			"integrity": "sha1-WDsap3WWHUsROsF9nFC6753Xa9E=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6471,7 +6473,7 @@[m
 			"dependencies": {[m
 				"anymatch": {[m
 					"version": "2.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/anymatch/-/anymatch-2.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/anymatch/-/anymatch-2.0.0.tgz",[m
 					"integrity": "sha512-5teOsQWABXHHBFP9y3skS5P3d/WfWXpv3FUpy+LorMrNYaT9pI4oLMQX7jzQ2KklNpGpWHzdCXTDT2Y3XGlZBw==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6492,19 +6494,19 @@[m
 				},[m
 				"arr-diff": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/arr-diff/-/arr-diff-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/arr-diff/-/arr-diff-4.0.0.tgz",[m
 					"integrity": "sha1-1kYQdP6/7HHn4VI1dhoyml3HxSA=",[m
 					"dev": true[m
 				},[m
 				"array-unique": {[m
 					"version": "0.3.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-unique/-/array-unique-0.3.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/array-unique/-/array-unique-0.3.2.tgz",[m
 					"integrity": "sha1-qJS3XUvE9s1nnvMkSp/Y9Gri1Cg=",[m
 					"dev": true[m
 				},[m
 				"braces": {[m
 					"version": "2.3.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/braces/-/braces-2.3.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/braces/-/braces-2.3.2.tgz",[m
 					"integrity": "sha512-aNdbnj9P8PjdXU4ybaWLK2IF3jc/EoDYbC7AazW6to3TRsfXxscC9UXOB5iDiEQrkyIbWp2SLQda4+QAa7nc3w==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6522,7 +6524,7 @@[m
 					"dependencies": {[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -6553,7 +6555,7 @@[m
 				},[m
 				"cross-spawn": {[m
 					"version": "6.0.5",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cross-spawn/-/cross-spawn-6.0.5.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/cross-spawn/-/cross-spawn-6.0.5.tgz",[m
 					"integrity": "sha512-eTVLrBSt7fjbDygz805pMnstIs2VTBNkRm0qxZd+M7A5XDdxVRWO5MxGBXZhjY4cqLYLdtrGqRf8mBPmzwSpWQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6566,7 +6568,7 @@[m
 				},[m
 				"expand-brackets": {[m
 					"version": "2.1.4",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/expand-brackets/-/expand-brackets-2.1.4.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/expand-brackets/-/expand-brackets-2.1.4.tgz",[m
 					"integrity": "sha1-t3c14xXOMPa27/D4OwQVGiJEliI=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6581,7 +6583,7 @@[m
 					"dependencies": {[m
 						"define-property": {[m
 							"version": "0.2.5",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-0.2.5.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-0.2.5.tgz",[m
 							"integrity": "sha1-w1se+RjsPJkPmlvFe+BKrOxcgRY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -6590,7 +6592,7 @@[m
 						},[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -6599,7 +6601,7 @@[m
 						},[m
 						"is-accessor-descriptor": {[m
 							"version": "0.1.6",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-0.1.6.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-0.1.6.tgz",[m
 							"integrity": "sha1-qeEss66Nh2cn7u84Q/igiXtcmNY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -6608,7 +6610,7 @@[m
 							"dependencies": {[m
 								"kind-of": {[m
 									"version": "3.2.2",[m
[31m-									"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m									[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 									"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 									"dev": true,[m
 									"requires": {[m
[36m@@ -6619,7 +6621,7 @@[m
 						},[m
 						"is-data-descriptor": {[m
 							"version": "0.1.4",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-0.1.4.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-0.1.4.tgz",[m
 							"integrity": "sha1-C17mSDiOLIYCgueT8YVv7D8wG1Y=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -6628,7 +6630,7 @@[m
 							"dependencies": {[m
 								"kind-of": {[m
 									"version": "3.2.2",[m
[31m-									"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m									[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 									"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 									"dev": true,[m
 									"requires": {[m
[36m@@ -6639,7 +6641,7 @@[m
 						},[m
 						"is-descriptor": {[m
 							"version": "0.1.6",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-0.1.6.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-0.1.6.tgz",[m
 							"integrity": "sha512-avDYr0SB3DwO9zsMov0gKCESFYqCnE4hq/4z3TdUlukEy5t9C0YRq7HLrsN52NAcqXKaepeCD0n+B0arnVG3Hg==",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -6650,7 +6652,7 @@[m
 						},[m
 						"kind-of": {[m
 							"version": "5.1.0",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-5.1.0.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-5.1.0.tgz",[m
 							"integrity": "sha512-NGEErnH6F2vUuXDh+OlbcKW7/wOcfdRHaZ7VWtqCztfHri/++YKmP51OdWeGPuqCOba6kk2OTe5d02VmTB80Pw==",[m
 							"dev": true[m
 						}[m
[36m@@ -6658,7 +6660,7 @@[m
 				},[m
 				"extglob": {[m
 					"version": "2.0.4",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extglob/-/extglob-2.0.4.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/extglob/-/extglob-2.0.4.tgz",[m
 					"integrity": "sha512-Nmb6QXkELsuBr24CJSkilo6UHHgbekK5UiZgfE6UHD3Eb27YC6oD+bhcT+tJ6cl8dmsgdQxnWlcry8ksBIBLpw==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6674,7 +6676,7 @@[m
 					"dependencies": {[m
 						"define-property": {[m
 							"version": "1.0.0",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-1.0.0.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-1.0.0.tgz",[m
 							"integrity": "sha1-dp66rz9KY6rTr56NMEybvnm/sOY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -6683,7 +6685,7 @@[m
 						},[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -6694,7 +6696,7 @@[m
 				},[m
 				"fill-range": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fill-range/-/fill-range-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/fill-range/-/fill-range-4.0.0.tgz",[m
 					"integrity": "sha1-1USBHUKPmOsGpj3EAtJAPDKMOPc=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6706,7 +6708,7 @@[m
 					"dependencies": {[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -6717,7 +6719,7 @@[m
 				},[m
 				"glob-parent": {[m
 					"version": "3.1.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/glob-parent/-/glob-parent-3.1.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/glob-parent/-/glob-parent-3.1.0.tgz",[m
 					"integrity": "sha1-nmr2KZ2NO9K9QEMIMr0RPfkGxa4=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6727,7 +6729,7 @@[m
 					"dependencies": {[m
 						"is-glob": {[m
 							"version": "3.1.0",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-glob/-/is-glob-3.1.0.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-glob/-/is-glob-3.1.0.tgz",[m
 							"integrity": "sha1-e6WuJCF4BKxwcHuWkiVnSGzD6Eo=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -6738,7 +6740,7 @@[m
 				},[m
 				"is-accessor-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-m5hnHTkcVsPfqx3AKlyttIPb7J+XykHvJP2B9bZDjlhLIoEq4XoK64Vg7boZlVWYK6LUY94dYPEE7Lh0ZkZKcQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6747,7 +6749,7 @@[m
 				},[m
 				"is-data-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-jbRXy1FmtAoCjQkVmIVYwuuqDFUbaOeDjmed1tOGPrsMhtJA4rD9tkgA0F1qJ3gRFRXcHYVkdeaP50Q5rE/jLQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6756,7 +6758,7 @@[m
 				},[m
 				"is-descriptor": {[m
 					"version": "1.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
 					"integrity": "sha512-2eis5WqQGV7peooDyLmNEPUrps9+SXX5c9pL3xEB+4e9HnGuDa7mB7kHxHw4CbqS9k1T2hOH3miL8n8WtiYVtg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6767,7 +6769,7 @@[m
 				},[m
 				"is-extglob": {[m
 					"version": "2.1.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-extglob/-/is-extglob-2.1.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-extglob/-/is-extglob-2.1.1.tgz",[m
 					"integrity": "sha1-qIwCU1eR8C7TfHahueqXc8gz+MI=",[m
 					"dev": true[m
 				},[m
[36m@@ -6782,7 +6784,7 @@[m
 				},[m
 				"is-number": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-number/-/is-number-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-number/-/is-number-3.0.0.tgz",[m
 					"integrity": "sha1-JP1iAaR4LPUFYcgQJ2r8fRLXEZU=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6791,7 +6793,7 @@[m
 					"dependencies": {[m
 						"kind-of": {[m
 							"version": "3.2.2",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 							"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -6802,19 +6804,19 @@[m
 				},[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				},[m
 				"kind-of": {[m
 					"version": "6.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-6.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-6.0.2.tgz",[m
 					"integrity": "sha512-s5kLOcnH0XqDO+FvuaLX8DDjZ18CGFk7VygH40QoKPUQhW4e2rvM0rwUq0t8IQDOwYSeLK01U90OjzBTme2QqA==",[m
 					"dev": true[m
 				},[m
 				"micromatch": {[m
 					"version": "3.1.10",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/micromatch/-/micromatch-3.1.10.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/micromatch/-/micromatch-3.1.10.tgz",[m
 					"integrity": "sha512-MWikgl9n9M3w+bpsY3He8L+w9eF9338xRl8IAO5viDizwSzziFEyUzo2xrrloB64ADbTf8uA8vRqqttDTOmccg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -6835,7 +6837,7 @@[m
 				},[m
 				"minimist": {[m
 					"version": "1.2.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/minimist/-/minimist-1.2.0.tgz",[m
[32m+[m					[32m"resolved": "http://registry.npmjs.org/minimist/-/minimist-1.2.0.tgz",[m
 					"integrity": "sha1-o1AIsg9BOD7sH7kU9M1d95omQoQ=",[m
 					"dev": true[m
 				},[m
[36m@@ -6858,7 +6860,7 @@[m
 		},[m
 		"optimize-css-assets-webpack-plugin": {[m
 			"version": "5.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/optimize-css-assets-webpack-plugin/-/optimize-css-assets-webpack-plugin-5.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/optimize-css-assets-webpack-plugin/-/optimize-css-assets-webpack-plugin-5.0.1.tgz",[m
 			"integrity": "sha512-Rqm6sSjWtx9FchdP0uzTQDc7GXDKnwVEGoSxjezPkzMewx7gEWE9IMUYKmigTRC4U3RaNSwYVnUDLuIdtTpm0A==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6868,19 +6870,19 @@[m
 		},[m
 		"os-browserify": {[m
 			"version": "0.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/os-browserify/-/os-browserify-0.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/os-browserify/-/os-browserify-0.3.0.tgz",[m
 			"integrity": "sha1-hUNzx/XCMVkU/Jv8a9gjj92h7Cc=",[m
 			"dev": true[m
 		},[m
 		"os-homedir": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/os-homedir/-/os-homedir-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/os-homedir/-/os-homedir-1.0.2.tgz",[m
 			"integrity": "sha1-/7xJiDNuDoM94MFox+8VISGqf7M=",[m
 			"dev": true[m
 		},[m
 		"os-locale": {[m
 			"version": "1.4.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/os-locale/-/os-locale-1.4.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/os-locale/-/os-locale-1.4.0.tgz",[m
 			"integrity": "sha1-IPnxeuKe00XoveWDsT0gCYA8FNk=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6889,13 +6891,13 @@[m
 		},[m
 		"os-tmpdir": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/os-tmpdir/-/os-tmpdir-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/os-tmpdir/-/os-tmpdir-1.0.2.tgz",[m
 			"integrity": "sha1-u+Z0BseaqFxc/sdm/lc0VV36EnQ=",[m
 			"dev": true[m
 		},[m
 		"osenv": {[m
 			"version": "0.1.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/osenv/-/osenv-0.1.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/osenv/-/osenv-0.1.5.tgz",[m
 			"integrity": "sha512-0CWcCECdMVc2Rw3U5w9ZjqX6ga6ubk1xDVKxtBQPK7wis/0F2r9T6k4ydGYhecl7YUBxBVxhL5oisPsNxAPe2g==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6905,7 +6907,7 @@[m
 		},[m
 		"output-file-sync": {[m
 			"version": "1.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/output-file-sync/-/output-file-sync-1.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/output-file-sync/-/output-file-sync-1.1.2.tgz",[m
 			"integrity": "sha1-0KM+7+YaIF+suQCS6CZZjVJFznY=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6916,13 +6918,13 @@[m
 		},[m
 		"p-defer": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/p-defer/-/p-defer-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/p-defer/-/p-defer-1.0.0.tgz",[m
 			"integrity": "sha1-n26xgvbJqozXQwBKfU+WsZaw+ww=",[m
 			"dev": true[m
 		},[m
 		"p-finally": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/p-finally/-/p-finally-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/p-finally/-/p-finally-1.0.0.tgz",[m
 			"integrity": "sha1-P7z7FbiZpEEjs0ttzBi3JDNqLK4=",[m
 			"dev": true[m
 		},[m
[36m@@ -6934,7 +6936,7 @@[m
 		},[m
 		"p-limit": {[m
 			"version": "1.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/p-limit/-/p-limit-1.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/p-limit/-/p-limit-1.3.0.tgz",[m
 			"integrity": "sha512-vvcXsLAJ9Dr5rQOPk7toZQZJApBl2K4J6dANSsEuh6QI41JYcsS/qhTGa9ErIUUgK3WNQoJYvylxvjqmiqEA9Q==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6943,7 +6945,7 @@[m
 		},[m
 		"p-locate": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/p-locate/-/p-locate-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/p-locate/-/p-locate-2.0.0.tgz",[m
 			"integrity": "sha1-IKAQOyIqcMj9OcwuWAaA893l7EM=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6952,7 +6954,7 @@[m
 		},[m
 		"p-try": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/p-try/-/p-try-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/p-try/-/p-try-1.0.0.tgz",[m
 			"integrity": "sha1-y8ec26+P1CKOE/Yh8rGiN8GyB7M=",[m
 			"dev": true[m
 		},[m
[36m@@ -6964,7 +6966,7 @@[m
 		},[m
 		"parallel-transform": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/parallel-transform/-/parallel-transform-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/parallel-transform/-/parallel-transform-1.1.0.tgz",[m
 			"integrity": "sha1-1BDwZbBdojCB/NEPKIVMKb2jOwY=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -6989,7 +6991,7 @@[m
 		},[m
 		"parse-glob": {[m
 			"version": "3.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/parse-glob/-/parse-glob-3.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/parse-glob/-/parse-glob-3.0.4.tgz",[m
 			"integrity": "sha1-ssN2z7EfNVE7rdFz7wu246OIORw=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7001,7 +7003,7 @@[m
 		},[m
 		"parse-json": {[m
 			"version": "2.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/parse-json/-/parse-json-2.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/parse-json/-/parse-json-2.2.0.tgz",[m
 			"integrity": "sha1-9ID0BDTvgHQfhGkJn43qGPVaTck=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7021,13 +7023,13 @@[m
 		},[m
 		"pascalcase": {[m
 			"version": "0.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pascalcase/-/pascalcase-0.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/pascalcase/-/pascalcase-0.1.1.tgz",[m
 			"integrity": "sha1-s2PlXoAGym/iF4TS2yK9FdeRfxQ=",[m
 			"dev": true[m
 		},[m
 		"passport": {[m
 			"version": "0.4.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/passport/-/passport-0.4.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/passport/-/passport-0.4.0.tgz",[m
 			"integrity": "sha1-xQlWkTR71a07XhgCOMORTRbwWBE=",[m
 			"requires": {[m
 				"passport-strategy": "1.x.x",[m
[36m@@ -7036,7 +7038,7 @@[m
 		},[m
 		"passport-jwt": {[m
 			"version": "4.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/passport-jwt/-/passport-jwt-4.0.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/passport-jwt/-/passport-jwt-4.0.0.tgz",[m
 			"integrity": "sha512-BwC0n2GP/1hMVjR4QpnvqA61TxenUMlmfNjYNgK0ZAs0HK4SOQkHcSv4L328blNTLtHq7DbmvyNJiH+bn6C5Mg==",[m
 			"requires": {[m
 				"jsonwebtoken": "^8.2.0",[m
[36m@@ -7045,7 +7047,7 @@[m
 		},[m
 		"passport-local": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/passport-local/-/passport-local-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/passport-local/-/passport-local-1.0.0.tgz",[m
 			"integrity": "sha1-H+YyaMkudWBmJkN+O5BmYsFbpu4=",[m
 			"requires": {[m
 				"passport-strategy": "1.x.x"[m
[36m@@ -7053,7 +7055,7 @@[m
 		},[m
 		"passport-strategy": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/passport-strategy/-/passport-strategy-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/passport-strategy/-/passport-strategy-1.0.0.tgz",[m
 			"integrity": "sha1-tVOaqPwiWj0a0XlHbd8ja0QPUuQ="[m
 		},[m
 		"path-browserify": {[m
[36m@@ -7064,25 +7066,25 @@[m
 		},[m
 		"path-dirname": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/path-dirname/-/path-dirname-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/path-dirname/-/path-dirname-1.0.2.tgz",[m
 			"integrity": "sha1-zDPSTVJeCZpTiMAzbG4yuRYGCeA=",[m
 			"dev": true[m
 		},[m
 		"path-exists": {[m
 			"version": "3.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/path-exists/-/path-exists-3.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/path-exists/-/path-exists-3.0.0.tgz",[m
 			"integrity": "sha1-zg6+ql94yxiSXqfYENe1mwEP1RU=",[m
 			"dev": true[m
 		},[m
 		"path-is-absolute": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/path-is-absolute/-/path-is-absolute-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/path-is-absolute/-/path-is-absolute-1.0.1.tgz",[m
 			"integrity": "sha1-F0uSaHNVNP+8es5r9TpanhtcX18=",[m
 			"dev": true[m
 		},[m
 		"path-key": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/path-key/-/path-key-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/path-key/-/path-key-2.0.1.tgz",[m
 			"integrity": "sha1-QRyttXTFoUDTpLGRDUDYDMn0C0A=",[m
 			"dev": true[m
 		},[m
[36m@@ -7094,12 +7096,12 @@[m
 		},[m
 		"path-to-regexp": {[m
 			"version": "0.1.7",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/path-to-regexp/-/path-to-regexp-0.1.7.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/path-to-regexp/-/path-to-regexp-0.1.7.tgz",[m
 			"integrity": "sha1-32BBeABfUi8V60SQ5yR6G/qmf4w="[m
 		},[m
 		"path-type": {[m
 			"version": "3.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/path-type/-/path-type-3.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/path-type/-/path-type-3.0.0.tgz",[m
 			"integrity": "sha512-T2ZUsdZFHgA3u4e5PfPbjd7HDDpxPnQb5jN0SrDsjNSuVXHJqtwTnWqG0B1jZrgmJ/7lj1EmVIByWt1gxGkWvg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7108,12 +7110,12 @@[m
 		},[m
 		"pause": {[m
 			"version": "0.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pause/-/pause-0.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/pause/-/pause-0.0.1.tgz",[m
 			"integrity": "sha1-HUCLP9t2kjuVQ9lvtMnf1TXZy10="[m
 		},[m
 		"pbkdf2": {[m
 			"version": "3.0.17",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pbkdf2/-/pbkdf2-3.0.17.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/pbkdf2/-/pbkdf2-3.0.17.tgz",[m
 			"integrity": "sha512-U/il5MsrZp7mGg3mSQfn742na2T+1/vHDCG5/iTI3X9MKUuYUZVLQhyRsg06mCgDBTd57TxzgZt7P+fYfjRLtA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7126,7 +7128,7 @@[m
 		},[m
 		"performance-now": {[m
 			"version": "2.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/performance-now/-/performance-now-2.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/performance-now/-/performance-now-2.1.0.tgz",[m
 			"integrity": "sha1-Ywn04OX6kT7BxpMHrjZLSzd8nns=",[m
 			"dev": true[m
 		},[m
[36m@@ -7138,19 +7140,19 @@[m
 		},[m
 		"pify": {[m
 			"version": "3.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pify/-/pify-3.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/pify/-/pify-3.0.0.tgz",[m
 			"integrity": "sha1-5aSs0sEB/fPZpNB/DbxNtJ3SgXY=",[m
 			"dev": true[m
 		},[m
 		"pinkie": {[m
 			"version": "2.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pinkie/-/pinkie-2.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/pinkie/-/pinkie-2.0.4.tgz",[m
 			"integrity": "sha1-clVrgM+g1IqXToDnckjoDtT3+HA=",[m
 			"dev": true[m
 		},[m
 		"pinkie-promise": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pinkie-promise/-/pinkie-promise-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/pinkie-promise/-/pinkie-promise-2.0.1.tgz",[m
 			"integrity": "sha1-ITXW36ejWMBprJsXh3YogihFD/o=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7159,7 +7161,7 @@[m
 		},[m
 		"pkg-dir": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pkg-dir/-/pkg-dir-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/pkg-dir/-/pkg-dir-2.0.0.tgz",[m
 			"integrity": "sha1-9tXREJ4Z1j7fQo4L1X4Sd3YVM0s=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7168,13 +7170,13 @@[m
 		},[m
 		"posix-character-classes": {[m
 			"version": "0.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/posix-character-classes/-/posix-character-classes-0.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/posix-character-classes/-/posix-character-classes-0.1.1.tgz",[m
 			"integrity": "sha1-AerA/jta9xoqbAL+q7jB/vfgDqs=",[m
 			"dev": true[m
 		},[m
 		"postcss": {[m
 			"version": "6.0.23",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss/-/postcss-6.0.23.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss/-/postcss-6.0.23.tgz",[m
 			"integrity": "sha512-soOk1h6J3VMTZtVeVpv15/Hpdl2cBLX3CAw4TAbkpTJiNPk9YP/zWcD1ND+xEtvyuuvKzbxliTOIyvkSeSJ6ag==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7185,7 +7187,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7205,13 +7207,13 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
 				"supports-color": {[m
 					"version": "5.5.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/supports-color/-/supports-color-5.5.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/supports-color/-/supports-color-5.5.0.tgz",[m
 					"integrity": "sha512-QjVjwdXIt408MIiAqCX4oUKsgU2EqAGzs2Ppkm4aQYbjm+ZEWEcW4SfFNTr4uMNZma0ey4f5lgLrkB0aX0QMow==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7222,7 +7224,7 @@[m
 		},[m
 		"postcss-calc": {[m
 			"version": "7.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-calc/-/postcss-calc-7.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-calc/-/postcss-calc-7.0.1.tgz",[m
 			"integrity": "sha512-oXqx0m6tb4N3JGdmeMSc/i91KppbYsFZKdH0xMOqK8V1rJlzrKlTdokz8ozUXLVejydRN6u2IddxpcijRj2FqQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7234,7 +7236,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7276,7 +7278,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -7306,7 +7308,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7359,7 +7361,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -7376,7 +7378,7 @@[m
 		},[m
 		"postcss-convert-values": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-convert-values/-/postcss-convert-values-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-convert-values/-/postcss-convert-values-4.0.1.tgz",[m
 			"integrity": "sha512-Kisdo1y77KUC0Jmn0OXU/COOJbzM8cImvw1ZFsBgBgMgb1iL23Zs/LXRe3r+EZqM3vGYKdQ2YJVQ5VkJI+zEJQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7386,7 +7388,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7428,7 +7430,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -7454,7 +7456,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7496,7 +7498,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -7513,7 +7515,7 @@[m
 		},[m
 		"postcss-discard-duplicates": {[m
 			"version": "4.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-discard-duplicates/-/postcss-discard-duplicates-4.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-discard-duplicates/-/postcss-discard-duplicates-4.0.2.tgz",[m
 			"integrity": "sha512-ZNQfR1gPNAiXZhgENFfEglF93pciw0WxMkJeVmw8eF+JZBbMD7jp6C67GqJAXVZP2BWbOztKfbsdmMp/k8c6oQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7522,7 +7524,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7564,7 +7566,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -7581,7 +7583,7 @@[m
 		},[m
 		"postcss-discard-empty": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-discard-empty/-/postcss-discard-empty-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-discard-empty/-/postcss-discard-empty-4.0.1.tgz",[m
 			"integrity": "sha512-B9miTzbznhDjTfjvipfHoqbWKwd0Mj+/fL5s1QOz06wufguil+Xheo4XpOnc4NqKYBCNqqEzgPv2aPBIJLox0w==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7590,7 +7592,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7632,7 +7634,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -7649,7 +7651,7 @@[m
 		},[m
 		"postcss-discard-overridden": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-discard-overridden/-/postcss-discard-overridden-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-discard-overridden/-/postcss-discard-overridden-4.0.1.tgz",[m
 			"integrity": "sha512-IYY2bEDD7g1XM1IDEsUT4//iEYCxAmP5oDSFMVU/JVvT7gh+l4fmjciLqGgwjdWpQIdb0Che2VX00QObS5+cTg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7658,7 +7660,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7700,7 +7702,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -7729,7 +7731,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7771,7 +7773,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -7802,7 +7804,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7855,7 +7857,7 @@[m
 				},[m
 				"postcss-selector-parser": {[m
 					"version": "3.1.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-selector-parser/-/postcss-selector-parser-3.1.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/postcss-selector-parser/-/postcss-selector-parser-3.1.1.tgz",[m
 					"integrity": "sha1-T4dfSvsMllc9XPTXQBGu4lCn6GU=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7866,7 +7868,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -7883,7 +7885,7 @@[m
 		},[m
 		"postcss-minify-font-values": {[m
 			"version": "4.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-minify-font-values/-/postcss-minify-font-values-4.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-minify-font-values/-/postcss-minify-font-values-4.0.2.tgz",[m
 			"integrity": "sha512-j85oO6OnRU9zPf04+PZv1LYIYOprWm6IA6zkXkrJXyRveDEuQggG6tvoy8ir8ZwjLxLuGfNkCZEQG7zan+Hbtg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -7893,7 +7895,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -7935,7 +7937,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -7964,7 +7966,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8006,7 +8008,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8037,7 +8039,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8090,7 +8092,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8119,7 +8121,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8161,7 +8163,7 @@[m
 				},[m
 				"postcss-selector-parser": {[m
 					"version": "3.1.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-selector-parser/-/postcss-selector-parser-3.1.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/postcss-selector-parser/-/postcss-selector-parser-3.1.1.tgz",[m
 					"integrity": "sha1-T4dfSvsMllc9XPTXQBGu4lCn6GU=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8172,7 +8174,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8189,7 +8191,7 @@[m
 		},[m
 		"postcss-modules-extract-imports": {[m
 			"version": "1.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-modules-extract-imports/-/postcss-modules-extract-imports-1.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-modules-extract-imports/-/postcss-modules-extract-imports-1.2.1.tgz",[m
 			"integrity": "sha512-6jt9XZwUhwmRUhb/CkyJY020PYaPJsCyt3UjbaWo6XEbH/94Hmv6MP7fG2C5NDU/BcHzyGYxNtHvM+LTf9HrYw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -8198,7 +8200,7 @@[m
 		},[m
 		"postcss-modules-local-by-default": {[m
 			"version": "1.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-modules-local-by-default/-/postcss-modules-local-by-default-1.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-modules-local-by-default/-/postcss-modules-local-by-default-1.2.0.tgz",[m
 			"integrity": "sha1-99gMOYxaOT+nlkRmvRlQCn1hwGk=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -8208,7 +8210,7 @@[m
 		},[m
 		"postcss-modules-scope": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-modules-scope/-/postcss-modules-scope-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-modules-scope/-/postcss-modules-scope-1.1.0.tgz",[m
 			"integrity": "sha1-1upkmUx5+XtipytCb75gVqGUu5A=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -8218,7 +8220,7 @@[m
 		},[m
 		"postcss-modules-values": {[m
 			"version": "1.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-modules-values/-/postcss-modules-values-1.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-modules-values/-/postcss-modules-values-1.3.0.tgz",[m
 			"integrity": "sha1-7P+p1+GSUYOJ9CrQ6D9yrsRW6iA=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -8228,7 +8230,7 @@[m
 		},[m
 		"postcss-normalize-charset": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-normalize-charset/-/postcss-normalize-charset-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-normalize-charset/-/postcss-normalize-charset-4.0.1.tgz",[m
 			"integrity": "sha512-gMXCrrlWh6G27U0hF3vNvR3w8I1s2wOBILvA87iNXaPvSNo5uZAMYsZG7XjCUf1eVxuPfyL4TJ7++SGZLc9A3g==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -8237,7 +8239,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8279,7 +8281,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8307,7 +8309,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8349,7 +8351,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8378,7 +8380,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8420,7 +8422,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8449,7 +8451,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8491,7 +8493,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8519,7 +8521,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8561,7 +8563,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8589,7 +8591,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8631,7 +8633,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8648,7 +8650,7 @@[m
 		},[m
 		"postcss-normalize-unicode": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-normalize-unicode/-/postcss-normalize-unicode-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-normalize-unicode/-/postcss-normalize-unicode-4.0.1.tgz",[m
 			"integrity": "sha512-od18Uq2wCYn+vZ/qCOeutvHjB5jm57ToxRaMeNuf0nWVHaP9Hua56QyMF6fs/4FSUnVIw0CBPsU0K4LnBPwYwg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -8659,7 +8661,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8712,7 +8714,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8729,7 +8731,7 @@[m
 		},[m
 		"postcss-normalize-url": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-normalize-url/-/postcss-normalize-url-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-normalize-url/-/postcss-normalize-url-4.0.1.tgz",[m
 			"integrity": "sha512-p5oVaF4+IHwu7VpMan/SSpmpYxcJMtkGppYf0VbdH5B6hN8YNmVyJLuY9FmLQTzY3fag5ESUUHDqM+heid0UVA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -8741,7 +8743,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8783,7 +8785,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8810,7 +8812,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8852,7 +8854,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8880,7 +8882,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -8922,7 +8924,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -8951,7 +8953,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9004,7 +9006,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -9033,7 +9035,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9075,7 +9077,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -9103,7 +9105,7 @@[m
 			"dependencies": {[m
 				"cssesc": {[m
 					"version": "2.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cssesc/-/cssesc-2.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/cssesc/-/cssesc-2.0.0.tgz",[m
 					"integrity": "sha512-MsCAG1z9lPdoO/IUMLSBWBSVxVtJ1395VGIQ+Fc2gNdkQ1hNDnQdw3YhA71WJCBW1vdwA0cAnk/DnW6bqoEUYg==",[m
 					"dev": true[m
 				}[m
[36m@@ -9123,7 +9125,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9165,7 +9167,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -9182,7 +9184,7 @@[m
 		},[m
 		"postcss-unique-selectors": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-unique-selectors/-/postcss-unique-selectors-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-unique-selectors/-/postcss-unique-selectors-4.0.1.tgz",[m
 			"integrity": "sha512-+JanVaryLo9QwZjKrmJgkI4Fn8SBgRO6WXQBJi7KiAVPlmxikB5Jzc4EvXMT2H0/m0RjrVVm9rGNhZddm/8Spg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9193,7 +9195,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9235,7 +9237,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -9252,37 +9254,37 @@[m
 		},[m
 		"postcss-value-parser": {[m
 			"version": "3.3.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-value-parser/-/postcss-value-parser-3.3.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/postcss-value-parser/-/postcss-value-parser-3.3.1.tgz",[m
 			"integrity": "sha512-pISE66AbVkp4fDQ7VHBwRNXzAAKJjw4Vw7nWI/+Q3vuly7SNfgYXvm6i5IgFylHGK5sP/xHAbB7N49OS4gWNyQ==",[m
 			"dev": true[m
 		},[m
 		"preserve": {[m
 			"version": "0.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/preserve/-/preserve-0.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/preserve/-/preserve-0.2.0.tgz",[m
 			"integrity": "sha1-gV7R9uvGWSb4ZbMQwHE7yzMVzks=",[m
 			"dev": true[m
 		},[m
 		"private": {[m
 			"version": "0.1.8",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/private/-/private-0.1.8.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/private/-/private-0.1.8.tgz",[m
 			"integrity": "sha512-VvivMrbvd2nKkiG38qjULzlc+4Vx4wm/whI9pQD35YrARNnhxeiRktSOhSukRLFNlzg6Br/cJPet5J/u19r/mg==",[m
 			"dev": true[m
 		},[m
 		"process": {[m
 			"version": "0.11.10",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/process/-/process-0.11.10.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/process/-/process-0.11.10.tgz",[m
 			"integrity": "sha1-czIwDoQBYb2j5podHZGn1LwW8YI=",[m
 			"dev": true[m
 		},[m
 		"process-nextick-args": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/process-nextick-args/-/process-nextick-args-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/process-nextick-args/-/process-nextick-args-2.0.0.tgz",[m
 			"integrity": "sha512-MtEC1TqN0EU5nephaJ4rAtThHtC86dNN9qCuEhtshvpVBkAW5ZO7BASN9REnF9eoXGcRub+pFuKEpOHE+HbEMw==",[m
 			"dev": true[m
 		},[m
 		"promise-inflight": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/promise-inflight/-/promise-inflight-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/promise-inflight/-/promise-inflight-1.0.1.tgz",[m
 			"integrity": "sha1-mEcocL8igTL8vdhoEputEsPAKeM=",[m
 			"dev": true[m
 		},[m
[36m@@ -9297,13 +9299,13 @@[m
 		},[m
 		"prr": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/prr/-/prr-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/prr/-/prr-1.0.1.tgz",[m
 			"integrity": "sha1-0/wRS6BplaRexok/SEzrHXj19HY=",[m
 			"dev": true[m
 		},[m
 		"pseudomap": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pseudomap/-/pseudomap-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/pseudomap/-/pseudomap-1.0.2.tgz",[m
 			"integrity": "sha1-8FKijacOYYkX7wqKw0wa5aaChrM=",[m
 			"dev": true[m
 		},[m
[36m@@ -9315,7 +9317,7 @@[m
 		},[m
 		"public-encrypt": {[m
 			"version": "4.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/public-encrypt/-/public-encrypt-4.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/public-encrypt/-/public-encrypt-4.0.3.tgz",[m
 			"integrity": "sha512-zVpa8oKZSz5bTMTFClc1fQOnyyEzpl5ozpi1B5YcvBrdohMjH2rfsBtyXcuNuwjsDIXmBYlF2N5FlJYhR29t8Q==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9329,7 +9331,7 @@[m
 		},[m
 		"pump": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pump/-/pump-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/pump/-/pump-2.0.1.tgz",[m
 			"integrity": "sha512-ruPMNRkN3MHP1cWJc9OWr+T/xDP0jhXYCLfJcBuX54hhfIBnaQmAUMfDcG4DM5UMWByBbJY69QSphm3jtDKIkA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9339,7 +9341,7 @@[m
 		},[m
 		"pumpify": {[m
 			"version": "1.5.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pumpify/-/pumpify-1.5.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/pumpify/-/pumpify-1.5.1.tgz",[m
 			"integrity": "sha512-oClZI37HvuUJJxSKKrC17bZ9Cu0ZYhEAGPsPUy9KlMUmv9dKX2o77RUmq7f3XjIxbwyGwYzbzQ1L2Ks8sIradQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9356,7 +9358,7 @@[m
 		},[m
 		"q": {[m
 			"version": "1.5.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/q/-/q-1.5.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/q/-/q-1.5.1.tgz",[m
 			"integrity": "sha1-fjL3W0E4EpHQRhHxvxQQmsAGUdc=",[m
 			"dev": true[m
 		},[m
[36m@@ -9367,24 +9369,24 @@[m
 		},[m
 		"querystring": {[m
 			"version": "0.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/querystring/-/querystring-0.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/querystring/-/querystring-0.2.0.tgz",[m
 			"integrity": "sha1-sgmEkgO7Jd+CDadW50cAWHhSFiA=",[m
 			"dev": true[m
 		},[m
 		"querystring-es3": {[m
 			"version": "0.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/querystring-es3/-/querystring-es3-0.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/querystring-es3/-/querystring-es3-0.2.1.tgz",[m
 			"integrity": "sha1-nsYfeQSYdXB9aUFFlv2Qek1xHnM=",[m
 			"dev": true[m
 		},[m
 		"random-bytes": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/random-bytes/-/random-bytes-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/random-bytes/-/random-bytes-1.0.0.tgz",[m
 			"integrity": "sha1-T2ih3Arli9P7lYSMMDJNt11kNgs="[m
 		},[m
 		"randomatic": {[m
 			"version": "3.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/randomatic/-/randomatic-3.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/randomatic/-/randomatic-3.1.1.tgz",[m
 			"integrity": "sha512-TuDE5KxZ0J461RVjrJZCJc+J+zCkTb1MbH9AQUq68sMhOMcy9jLcb3BrZKgp9q9Ncltdg4QVqWrH02W2EFFVYw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9395,13 +9397,13 @@[m
 			"dependencies": {[m
 				"is-number": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-number/-/is-number-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-number/-/is-number-4.0.0.tgz",[m
 					"integrity": "sha512-rSklcAIlf1OmFdyAqbnWTLVelsQ58uvZ66S/ZyawjWqIviTWCjg2PzVGw8WUA+nNuPTqb4wgA+NszrJ+08LlgQ==",[m
 					"dev": true[m
 				},[m
 				"kind-of": {[m
 					"version": "6.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-6.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-6.0.2.tgz",[m
 					"integrity": "sha512-s5kLOcnH0XqDO+FvuaLX8DDjZ18CGFk7VygH40QoKPUQhW4e2rvM0rwUq0t8IQDOwYSeLK01U90OjzBTme2QqA==",[m
 					"dev": true[m
 				}[m
[36m@@ -9418,7 +9420,7 @@[m
 		},[m
 		"randomfill": {[m
 			"version": "1.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/randomfill/-/randomfill-1.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/randomfill/-/randomfill-1.0.4.tgz",[m
 			"integrity": "sha512-87lcbR8+MhcWcUiQ+9e+Rwx8MyR2P7qnt15ynUlbm3TU/fjbgz4GsvfSUDTemtCCtVCqb4ZcEFlyPNTh9bBTLw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9428,7 +9430,7 @@[m
 		},[m
 		"range-parser": {[m
 			"version": "1.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/range-parser/-/range-parser-1.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/range-parser/-/range-parser-1.2.0.tgz",[m
 			"integrity": "sha1-9JvmtIeJTdxA3MlKMi9hEJLgDV4="[m
 		},[m
 		"raw-body": {[m
[36m@@ -9444,7 +9446,7 @@[m
 		},[m
 		"read-pkg": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/read-pkg/-/read-pkg-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/read-pkg/-/read-pkg-1.1.0.tgz",[m
 			"integrity": "sha1-9f+qXs0pyzHAR0vKfXVra7KePyg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9455,7 +9457,7 @@[m
 			"dependencies": {[m
 				"path-type": {[m
 					"version": "1.1.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/path-type/-/path-type-1.1.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/path-type/-/path-type-1.1.0.tgz",[m
 					"integrity": "sha1-WcRPfuSR2nBNpBXaWkBwuk+P5EE=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9466,7 +9468,7 @@[m
 				},[m
 				"pify": {[m
 					"version": "2.3.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pify/-/pify-2.3.0.tgz",[m
[32m+[m					[32m"resolved": "http://registry.npmjs.org/pify/-/pify-2.3.0.tgz",[m
 					"integrity": "sha1-7RQaasBDqEnqWISY59yosVMw6Qw=",[m
 					"dev": true[m
 				}[m
[36m@@ -9474,7 +9476,7 @@[m
 		},[m
 		"read-pkg-up": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/read-pkg-up/-/read-pkg-up-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/read-pkg-up/-/read-pkg-up-1.0.1.tgz",[m
 			"integrity": "sha1-nWPBMnbAZZGNV/ACpX9AobZD+wI=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9484,7 +9486,7 @@[m
 			"dependencies": {[m
 				"find-up": {[m
 					"version": "1.1.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/find-up/-/find-up-1.1.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/find-up/-/find-up-1.1.2.tgz",[m
 					"integrity": "sha1-ay6YIrGizgpgq2TWEOzK1TyyTQ8=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9494,7 +9496,7 @@[m
 				},[m
 				"path-exists": {[m
 					"version": "2.1.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/path-exists/-/path-exists-2.1.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/path-exists/-/path-exists-2.1.0.tgz",[m
 					"integrity": "sha1-D+tsZPD8UY2adU3V77YscCJ2H0s=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9505,7 +9507,7 @@[m
 		},[m
 		"readable-stream": {[m
 			"version": "2.3.6",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/readable-stream/-/readable-stream-2.3.6.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/readable-stream/-/readable-stream-2.3.6.tgz",[m
 			"integrity": "sha512-tQtKA9WIAhBF3+VLAseyMqZeBjW0AHJoxOtYqSUZNJxauErmLbVm2FW1y+J/YA9dUrAC39ITejlZWhVIwawkKw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9520,7 +9522,7 @@[m
 		},[m
 		"readdirp": {[m
 			"version": "2.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/readdirp/-/readdirp-2.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/readdirp/-/readdirp-2.2.1.tgz",[m
 			"integrity": "sha512-1JU/8q+VgFZyxwrJ+SVIOsh+KywWGpds3NTqikiKpDMZWScmAYyKIgqkO+ARvNWJfXeXR1zxz7aHF4u4CyH6vQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9531,19 +9533,19 @@[m
 			"dependencies": {[m
 				"arr-diff": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/arr-diff/-/arr-diff-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/arr-diff/-/arr-diff-4.0.0.tgz",[m
 					"integrity": "sha1-1kYQdP6/7HHn4VI1dhoyml3HxSA=",[m
 					"dev": true[m
 				},[m
 				"array-unique": {[m
 					"version": "0.3.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-unique/-/array-unique-0.3.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/array-unique/-/array-unique-0.3.2.tgz",[m
 					"integrity": "sha1-qJS3XUvE9s1nnvMkSp/Y9Gri1Cg=",[m
 					"dev": true[m
 				},[m
 				"braces": {[m
 					"version": "2.3.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/braces/-/braces-2.3.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/braces/-/braces-2.3.2.tgz",[m
 					"integrity": "sha512-aNdbnj9P8PjdXU4ybaWLK2IF3jc/EoDYbC7AazW6to3TRsfXxscC9UXOB5iDiEQrkyIbWp2SLQda4+QAa7nc3w==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9561,7 +9563,7 @@[m
 					"dependencies": {[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -9572,7 +9574,7 @@[m
 				},[m
 				"expand-brackets": {[m
 					"version": "2.1.4",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/expand-brackets/-/expand-brackets-2.1.4.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/expand-brackets/-/expand-brackets-2.1.4.tgz",[m
 					"integrity": "sha1-t3c14xXOMPa27/D4OwQVGiJEliI=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9587,7 +9589,7 @@[m
 					"dependencies": {[m
 						"define-property": {[m
 							"version": "0.2.5",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-0.2.5.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-0.2.5.tgz",[m
 							"integrity": "sha1-w1se+RjsPJkPmlvFe+BKrOxcgRY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -9596,7 +9598,7 @@[m
 						},[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -9605,7 +9607,7 @@[m
 						},[m
 						"is-accessor-descriptor": {[m
 							"version": "0.1.6",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-0.1.6.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-0.1.6.tgz",[m
 							"integrity": "sha1-qeEss66Nh2cn7u84Q/igiXtcmNY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -9614,7 +9616,7 @@[m
 							"dependencies": {[m
 								"kind-of": {[m
 									"version": "3.2.2",[m
[31m-									"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m									[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 									"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 									"dev": true,[m
 									"requires": {[m
[36m@@ -9625,7 +9627,7 @@[m
 						},[m
 						"is-data-descriptor": {[m
 							"version": "0.1.4",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-0.1.4.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-0.1.4.tgz",[m
 							"integrity": "sha1-C17mSDiOLIYCgueT8YVv7D8wG1Y=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -9634,7 +9636,7 @@[m
 							"dependencies": {[m
 								"kind-of": {[m
 									"version": "3.2.2",[m
[31m-									"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m									[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 									"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 									"dev": true,[m
 									"requires": {[m
[36m@@ -9645,7 +9647,7 @@[m
 						},[m
 						"is-descriptor": {[m
 							"version": "0.1.6",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-0.1.6.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-0.1.6.tgz",[m
 							"integrity": "sha512-avDYr0SB3DwO9zsMov0gKCESFYqCnE4hq/4z3TdUlukEy5t9C0YRq7HLrsN52NAcqXKaepeCD0n+B0arnVG3Hg==",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -9656,7 +9658,7 @@[m
 						},[m
 						"kind-of": {[m
 							"version": "5.1.0",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-5.1.0.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-5.1.0.tgz",[m
 							"integrity": "sha512-NGEErnH6F2vUuXDh+OlbcKW7/wOcfdRHaZ7VWtqCztfHri/++YKmP51OdWeGPuqCOba6kk2OTe5d02VmTB80Pw==",[m
 							"dev": true[m
 						}[m
[36m@@ -9664,7 +9666,7 @@[m
 				},[m
 				"extglob": {[m
 					"version": "2.0.4",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extglob/-/extglob-2.0.4.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/extglob/-/extglob-2.0.4.tgz",[m
 					"integrity": "sha512-Nmb6QXkELsuBr24CJSkilo6UHHgbekK5UiZgfE6UHD3Eb27YC6oD+bhcT+tJ6cl8dmsgdQxnWlcry8ksBIBLpw==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9680,7 +9682,7 @@[m
 					"dependencies": {[m
 						"define-property": {[m
 							"version": "1.0.0",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-1.0.0.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-1.0.0.tgz",[m
 							"integrity": "sha1-dp66rz9KY6rTr56NMEybvnm/sOY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -9689,7 +9691,7 @@[m
 						},[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -9700,7 +9702,7 @@[m
 				},[m
 				"fill-range": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fill-range/-/fill-range-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/fill-range/-/fill-range-4.0.0.tgz",[m
 					"integrity": "sha1-1USBHUKPmOsGpj3EAtJAPDKMOPc=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9712,7 +9714,7 @@[m
 					"dependencies": {[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -9723,7 +9725,7 @@[m
 				},[m
 				"is-accessor-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-m5hnHTkcVsPfqx3AKlyttIPb7J+XykHvJP2B9bZDjlhLIoEq4XoK64Vg7boZlVWYK6LUY94dYPEE7Lh0ZkZKcQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9732,7 +9734,7 @@[m
 				},[m
 				"is-data-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-jbRXy1FmtAoCjQkVmIVYwuuqDFUbaOeDjmed1tOGPrsMhtJA4rD9tkgA0F1qJ3gRFRXcHYVkdeaP50Q5rE/jLQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9741,7 +9743,7 @@[m
 				},[m
 				"is-descriptor": {[m
 					"version": "1.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
 					"integrity": "sha512-2eis5WqQGV7peooDyLmNEPUrps9+SXX5c9pL3xEB+4e9HnGuDa7mB7kHxHw4CbqS9k1T2hOH3miL8n8WtiYVtg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9752,7 +9754,7 @@[m
 				},[m
 				"is-number": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-number/-/is-number-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-number/-/is-number-3.0.0.tgz",[m
 					"integrity": "sha1-JP1iAaR4LPUFYcgQJ2r8fRLXEZU=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9761,7 +9763,7 @@[m
 					"dependencies": {[m
 						"kind-of": {[m
 							"version": "3.2.2",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 							"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -9772,19 +9774,19 @@[m
 				},[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				},[m
 				"kind-of": {[m
 					"version": "6.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-6.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-6.0.2.tgz",[m
 					"integrity": "sha512-s5kLOcnH0XqDO+FvuaLX8DDjZ18CGFk7VygH40QoKPUQhW4e2rvM0rwUq0t8IQDOwYSeLK01U90OjzBTme2QqA==",[m
 					"dev": true[m
 				},[m
 				"micromatch": {[m
 					"version": "3.1.10",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/micromatch/-/micromatch-3.1.10.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/micromatch/-/micromatch-3.1.10.tgz",[m
 					"integrity": "sha512-MWikgl9n9M3w+bpsY3He8L+w9eF9338xRl8IAO5viDizwSzziFEyUzo2xrrloB64ADbTf8uA8vRqqttDTOmccg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -9807,7 +9809,7 @@[m
 		},[m
 		"redent": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/redent/-/redent-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/redent/-/redent-1.0.0.tgz",[m
 			"integrity": "sha1-z5Fqsf1fHxbfsggi3W7H9zDCr94=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9817,19 +9819,19 @@[m
 		},[m
 		"regenerate": {[m
 			"version": "1.4.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/regenerate/-/regenerate-1.4.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/regenerate/-/regenerate-1.4.0.tgz",[m
 			"integrity": "sha512-1G6jJVDWrt0rK99kBjvEtziZNCICAuvIPkSiUFIQxVP06RCVpq3dmDo2oi6ABpYaDYaTRr67BEhL8r1wgEZZKg==",[m
 			"dev": true[m
 		},[m
 		"regenerator-runtime": {[m
 			"version": "0.11.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/regenerator-runtime/-/regenerator-runtime-0.11.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/regenerator-runtime/-/regenerator-runtime-0.11.1.tgz",[m
 			"integrity": "sha512-MguG95oij0fC3QV3URf4V2SDYGJhJnJGqvIIgdECeODCT98wSWDAJ94SSuVpYQUoTcGUIL6L4yNB7j1DFFHSBg==",[m
 			"dev": true[m
 		},[m
 		"regenerator-transform": {[m
 			"version": "0.10.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/regenerator-transform/-/regenerator-transform-0.10.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/regenerator-transform/-/regenerator-transform-0.10.1.tgz",[m
 			"integrity": "sha512-PJepbvDbuK1xgIgnau7Y90cwaAmO/LCLMI2mPvaXq2heGMR3aWW5/BQvYrhJ8jgmQjXewXvBjzfqKcVOmhjZ6Q==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9840,7 +9842,7 @@[m
 		},[m
 		"regex-cache": {[m
 			"version": "0.4.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/regex-cache/-/regex-cache-0.4.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/regex-cache/-/regex-cache-0.4.4.tgz",[m
 			"integrity": "sha512-nVIZwtCjkC9YgvWkpM55B5rBhBYRZhAaJbgcFYXXsHnbZ9UZI9nnVWYZpBlCqv9ho2eZryPnWrZGsOdPwVWXWQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9849,7 +9851,7 @@[m
 		},[m
 		"regex-not": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/regex-not/-/regex-not-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/regex-not/-/regex-not-1.0.2.tgz",[m
 			"integrity": "sha512-J6SDjUgDxQj5NusnOtdFxDwN/+HWykR8GELwctJ7mdqhcyy1xEc4SRFHUXvxTp661YaVKAjfRLZ9cCqS6tn32A==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9859,13 +9861,13 @@[m
 		},[m
 		"regexp-clone": {[m
 			"version": "0.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/regexp-clone/-/regexp-clone-0.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/regexp-clone/-/regexp-clone-0.0.1.tgz",[m
 			"integrity": "sha1-p8LgmJH9vzj7sQ03b7cwA+aKxYk=",[m
 			"dev": true[m
 		},[m
 		"regexpu-core": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/regexpu-core/-/regexpu-core-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/regexpu-core/-/regexpu-core-2.0.0.tgz",[m
 			"integrity": "sha1-SdA4g3uNz4v6W5pCE5k45uoq4kA=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9876,13 +9878,13 @@[m
 		},[m
 		"regjsgen": {[m
 			"version": "0.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/regjsgen/-/regjsgen-0.2.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/regjsgen/-/regjsgen-0.2.0.tgz",[m
 			"integrity": "sha1-bAFq3qxVT3WCP+N6wFuS1aTtsfc=",[m
 			"dev": true[m
 		},[m
 		"regjsparser": {[m
 			"version": "0.1.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/regjsparser/-/regjsparser-0.1.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/regjsparser/-/regjsparser-0.1.5.tgz",[m
 			"integrity": "sha1-fuj4Tcb6eS0/0K4ijSS9lJ6tIFw=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9891,7 +9893,7 @@[m
 			"dependencies": {[m
 				"jsesc": {[m
 					"version": "0.5.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/jsesc/-/jsesc-0.5.0.tgz",[m
[32m+[m					[32m"resolved": "http://registry.npmjs.org/jsesc/-/jsesc-0.5.0.tgz",[m
 					"integrity": "sha1-597mbjXW/Bb3EP6R1c9p9w8IkR0=",[m
 					"dev": true[m
 				}[m
[36m@@ -9899,25 +9901,25 @@[m
 		},[m
 		"remove-trailing-separator": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/remove-trailing-separator/-/remove-trailing-separator-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/remove-trailing-separator/-/remove-trailing-separator-1.1.0.tgz",[m
 			"integrity": "sha1-wkvOKig62tW8P1jg1IJJuSN52O8=",[m
 			"dev": true[m
 		},[m
 		"repeat-element": {[m
 			"version": "1.1.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/repeat-element/-/repeat-element-1.1.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/repeat-element/-/repeat-element-1.1.3.tgz",[m
 			"integrity": "sha512-ahGq0ZnV5m5XtZLMb+vP76kcAM5nkLqk0lpqAuojSKGgQtn4eRi4ZZGm2olo2zKFH+sMsWaqOCW1dqAnOru72g==",[m
 			"dev": true[m
 		},[m
 		"repeat-string": {[m
 			"version": "1.6.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/repeat-string/-/repeat-string-1.6.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/repeat-string/-/repeat-string-1.6.1.tgz",[m
 			"integrity": "sha1-jcrkcOHIirwtYA//Sndihtp15jc=",[m
 			"dev": true[m
 		},[m
 		"repeating": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/repeating/-/repeating-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/repeating/-/repeating-2.0.1.tgz",[m
 			"integrity": "sha1-UhTFOpJtNVJwdSf7q0FdvAjQbdo=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9926,7 +9928,7 @@[m
 		},[m
 		"request": {[m
 			"version": "2.88.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/request/-/request-2.88.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/request/-/request-2.88.0.tgz",[m
 			"integrity": "sha512-NAqBSrijGLZdM0WZNsInLJpkJokL72XYjUpnB0iwsRgxh7dB6COrHnTBNwN0E+lHDAJzu7kLAkDeY08z2/A0hg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9962,19 +9964,19 @@[m
 		},[m
 		"require-directory": {[m
 			"version": "2.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/require-directory/-/require-directory-2.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/require-directory/-/require-directory-2.1.1.tgz",[m
 			"integrity": "sha1-jGStX9MNqxyXbiNE/+f3kqam30I=",[m
 			"dev": true[m
 		},[m
 		"require-main-filename": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/require-main-filename/-/require-main-filename-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/require-main-filename/-/require-main-filename-1.0.1.tgz",[m
 			"integrity": "sha1-l/cXtp1IeE9fUmpsWqj/3aBVpNE=",[m
 			"dev": true[m
 		},[m
 		"require_optional": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/require_optional/-/require_optional-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/require_optional/-/require_optional-1.0.1.tgz",[m
 			"integrity": "sha512-qhM/y57enGWHAe3v/NcwML6a3/vfESLe/sGM2dII+gEO0BpKRUkWZow/tyloNqJyN6kXSl3RyyM8Ll5D/sJP8g==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -9993,7 +9995,7 @@[m
 		},[m
 		"resolve-cwd": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/resolve-cwd/-/resolve-cwd-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/resolve-cwd/-/resolve-cwd-2.0.0.tgz",[m
 			"integrity": "sha1-AKn3OHVW4nA46uIyyqNypqWbZlo=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10002,7 +10004,7 @@[m
 			"dependencies": {[m
 				"resolve-from": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/resolve-from/-/resolve-from-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/resolve-from/-/resolve-from-3.0.0.tgz",[m
 					"integrity": "sha1-six699nWiBvItuZTM17rywoYh0g=",[m
 					"dev": true[m
 				}[m
[36m@@ -10020,31 +10022,31 @@[m
 		},[m
 		"resolve-from": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/resolve-from/-/resolve-from-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/resolve-from/-/resolve-from-2.0.0.tgz",[m
 			"integrity": "sha1-lICrIOlP+h2egKgEx+oUdhGWa1c=",[m
 			"dev": true[m
 		},[m
 		"resolve-url": {[m
 			"version": "0.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/resolve-url/-/resolve-url-0.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/resolve-url/-/resolve-url-0.2.1.tgz",[m
 			"integrity": "sha1-LGN/53yJOv0qZj/iGqkIAGjiBSo=",[m
 			"dev": true[m
 		},[m
 		"ret": {[m
 			"version": "0.1.15",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ret/-/ret-0.1.15.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/ret/-/ret-0.1.15.tgz",[m
 			"integrity": "sha512-TTlYpa+OL+vMMNG24xSlQGEJ3B/RzEfUlLct7b5G/ytav+wPrplCpVMFuwzXbkecJrb6IYo1iFb0S9v37754mg==",[m
 			"dev": true[m
 		},[m
 		"rgb-regex": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/rgb-regex/-/rgb-regex-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/rgb-regex/-/rgb-regex-1.0.1.tgz",[m
 			"integrity": "sha1-wODWiC3w4jviVKR16O3UGRX+rrE=",[m
 			"dev": true[m
 		},[m
 		"rgba-regex": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/rgba-regex/-/rgba-regex-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/rgba-regex/-/rgba-regex-1.0.0.tgz",[m
 			"integrity": "sha1-QzdOLiyglosO8VI0YLfXMP8i7rM=",[m
 			"dev": true[m
 		},[m
[36m@@ -10059,7 +10061,7 @@[m
 		},[m
 		"ripemd160": {[m
 			"version": "2.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ripemd160/-/ripemd160-2.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/ripemd160/-/ripemd160-2.0.2.tgz",[m
 			"integrity": "sha512-ii4iagi25WusVoiC4B4lq7pbXfAp3D9v5CwfkY33vffw2+pkDjY1D8GaN7spsxvCSx8dkPqOZCEZyfxcmJG2IA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10069,7 +10071,7 @@[m
 		},[m
 		"run-queue": {[m
 			"version": "1.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/run-queue/-/run-queue-1.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/run-queue/-/run-queue-1.0.3.tgz",[m
 			"integrity": "sha1-6Eg5bwV9Ij8kOGkkYY4laUFh7Ec=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10078,12 +10080,12 @@[m
 		},[m
 		"safe-buffer": {[m
 			"version": "5.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/safe-buffer/-/safe-buffer-5.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/safe-buffer/-/safe-buffer-5.1.2.tgz",[m
 			"integrity": "sha512-Gd2UZBJDkXlY7GbJxfsE8/nvKkUEU1G38c1siN6QP6a9PT9MmHB8GnpscSmMJSoF8LOIrt8ud/wPtojys4G6+g=="[m
 		},[m
 		"safe-regex": {[m
 			"version": "1.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/safe-regex/-/safe-regex-1.1.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/safe-regex/-/safe-regex-1.1.0.tgz",[m
 			"integrity": "sha1-QKNmnzsHfR6UPURinhV91IAjvy4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10092,12 +10094,12 @@[m
 		},[m
 		"safer-buffer": {[m
 			"version": "2.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/safer-buffer/-/safer-buffer-2.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/safer-buffer/-/safer-buffer-2.1.2.tgz",[m
 			"integrity": "sha512-YZo3K82SD7Riyi0E1EQPojLz7kpepnSQI9IyPbHHg1XXXevb5dJI7tpyN2ADxGcQbHG7vcyRHk0cbwqcQriUtg=="[m
 		},[m
 		"saslprep": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/saslprep/-/saslprep-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/saslprep/-/saslprep-1.0.2.tgz",[m
 			"integrity": "sha512-4cDsYuAjXssUSjxHKRe4DTZC0agDwsCqcMqtJAQPzC74nJ7LfAJflAtC1Zed5hMzEQKj82d3tuzqdGNRsLJ4Gw==",[m
 			"dev": true,[m
 			"optional": true,[m
[36m@@ -10107,7 +10109,7 @@[m
 		},[m
 		"sass-graph": {[m
 			"version": "2.2.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/sass-graph/-/sass-graph-2.2.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/sass-graph/-/sass-graph-2.2.4.tgz",[m
 			"integrity": "sha1-E/vWPNHK8JCLn9k0dq1DpR0eC0k=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10119,7 +10121,7 @@[m
 		},[m
 		"sass-loader": {[m
 			"version": "7.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/sass-loader/-/sass-loader-7.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/sass-loader/-/sass-loader-7.1.0.tgz",[m
 			"integrity": "sha512-+G+BKGglmZM2GUSfT9TLuEp6tzehHPjAMoRRItOojWIqIGPloVCMhNIQuG639eJ+y033PaGTSjLaTHts8Kw79w==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10133,7 +10135,7 @@[m
 		},[m
 		"sax": {[m
 			"version": "1.2.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/sax/-/sax-1.2.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/sax/-/sax-1.2.4.tgz",[m
 			"integrity": "sha512-NqVDv9TpANUjFm0N8uM5GxL36UgKi9/atZw+x7YFnQ8ckwFGKrl4xX4yWtrey3UJm5nP1kUbnYgLopqWNSRhWw==",[m
 			"dev": true[m
 		},[m
[36m@@ -10150,7 +10152,7 @@[m
 		},[m
 		"scss-tokenizer": {[m
 			"version": "0.2.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/scss-tokenizer/-/scss-tokenizer-0.2.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/scss-tokenizer/-/scss-tokenizer-0.2.3.tgz",[m
 			"integrity": "sha1-jrBtualyMzOCTT9VMGQRSYR85dE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10160,7 +10162,7 @@[m
 			"dependencies": {[m
 				"source-map": {[m
 					"version": "0.4.4",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.4.4.tgz",[m
[32m+[m					[32m"resolved": "http://registry.npmjs.org/source-map/-/source-map-0.4.4.tgz",[m
 					"integrity": "sha1-66T12pwNyZneaAMti092FzZSA2s=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -10176,7 +10178,7 @@[m
 		},[m
 		"send": {[m
 			"version": "0.16.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/send/-/send-0.16.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/send/-/send-0.16.2.tgz",[m
 			"integrity": "sha512-E64YFPUssFHEFBvpbbjr44NCLtI1AohxQ8ZSiJjQLskAdKuriYEP6VyGEsRDH8ScozGpkaX1BGvhanqCwkcEZw==",[m
 			"requires": {[m
 				"debug": "2.6.9",[m
[36m@@ -10212,7 +10214,7 @@[m
 				},[m
 				"statuses": {[m
 					"version": "1.4.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/statuses/-/statuses-1.4.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/statuses/-/statuses-1.4.0.tgz",[m
 					"integrity": "sha512-zhSCtt8v2NDrRlPQpCNtw/heZLtfUDqxBM1udqikb/Hbk52LK4nQSwr10u77iopCW5LsyHpuXS0GnEc48mLeew=="[m
 				}[m
 			}[m
[36m@@ -10225,7 +10227,7 @@[m
 		},[m
 		"serve-static": {[m
 			"version": "1.13.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/serve-static/-/serve-static-1.13.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/serve-static/-/serve-static-1.13.2.tgz",[m
 			"integrity": "sha512-p/tdJrO4U387R9oMjb1oj7qSMaMfmOyd4j9hOFoxZe2baQszgHcSWjuya/CiT5kgZZKRudHNOA0pYXOl8rQ5nw==",[m
 			"requires": {[m
 				"encodeurl": "~1.0.2",[m
[36m@@ -10236,13 +10238,13 @@[m
 		},[m
 		"set-blocking": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/set-blocking/-/set-blocking-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/set-blocking/-/set-blocking-2.0.0.tgz",[m
 			"integrity": "sha1-BF+XgtARrppoA93TgrJDkrPYkPc=",[m
 			"dev": true[m
 		},[m
 		"set-value": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/set-value/-/set-value-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/set-value/-/set-value-2.0.0.tgz",[m
 			"integrity": "sha512-hw0yxk9GT/Hr5yJEYnHNKYXkIA8mVJgd9ditYZCe16ZczcaELYYcfvaXesNACk2O8O0nTiPQcQhGUQj8JLzeeg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10254,7 +10256,7 @@[m
 			"dependencies": {[m
 				"extend-shallow": {[m
 					"version": "2.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 					"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -10265,7 +10267,7 @@[m
 		},[m
 		"setimmediate": {[m
 			"version": "1.0.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/setimmediate/-/setimmediate-1.0.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/setimmediate/-/setimmediate-1.0.5.tgz",[m
 			"integrity": "sha1-KQy7Iy4waULX1+qbg3Mqt4VvgoU=",[m
 			"dev": true[m
 		},[m
[36m@@ -10276,7 +10278,7 @@[m
 		},[m
 		"sha.js": {[m
 			"version": "2.4.11",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/sha.js/-/sha.js-2.4.11.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/sha.js/-/sha.js-2.4.11.tgz",[m
 			"integrity": "sha512-QMEp5B7cftE7APOjk5Y6xgrbWu+WkLVQwk8JNjZ8nKRciZaByEW6MubieAiToS7+dwvrjGhH8jRXz3MVd0AYqQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10286,7 +10288,7 @@[m
 		},[m
 		"shallow-clone": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/shallow-clone/-/shallow-clone-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/shallow-clone/-/shallow-clone-1.0.0.tgz",[m
 			"integrity": "sha512-oeXreoKR/SyNJtRJMAKPDSvd28OqEwG4eR/xc856cRGBII7gX9lvAqDxusPm0846z/w/hWYjI1NpKwJ00NHzRA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10297,7 +10299,7 @@[m
 			"dependencies": {[m
 				"kind-of": {[m
 					"version": "5.1.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-5.1.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-5.1.0.tgz",[m
 					"integrity": "sha512-NGEErnH6F2vUuXDh+OlbcKW7/wOcfdRHaZ7VWtqCztfHri/++YKmP51OdWeGPuqCOba6kk2OTe5d02VmTB80Pw==",[m
 					"dev": true[m
 				}[m
[36m@@ -10305,7 +10307,7 @@[m
 		},[m
 		"shebang-command": {[m
 			"version": "1.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/shebang-command/-/shebang-command-1.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/shebang-command/-/shebang-command-1.2.0.tgz",[m
 			"integrity": "sha1-RKrGW2lbAzmJaMOfNj/uXer98eo=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10314,13 +10316,13 @@[m
 		},[m
 		"shebang-regex": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/shebang-regex/-/shebang-regex-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/shebang-regex/-/shebang-regex-1.0.0.tgz",[m
 			"integrity": "sha1-2kL0l0DAtC2yypcoVxyxkMmO/qM=",[m
 			"dev": true[m
 		},[m
 		"shell-quote": {[m
 			"version": "1.6.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/shell-quote/-/shell-quote-1.6.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/shell-quote/-/shell-quote-1.6.1.tgz",[m
 			"integrity": "sha1-9HgZSczkAmlxJ0MOo7PFR29IF2c=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10338,13 +10340,13 @@[m
 		},[m
 		"signal-exit": {[m
 			"version": "3.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/signal-exit/-/signal-exit-3.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/signal-exit/-/signal-exit-3.0.2.tgz",[m
 			"integrity": "sha1-tf3AjxKH6hF4Yo5BXiUTK3NkbG0=",[m
 			"dev": true[m
 		},[m
 		"simple-swizzle": {[m
 			"version": "0.2.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/simple-swizzle/-/simple-swizzle-0.2.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/simple-swizzle/-/simple-swizzle-0.2.2.tgz",[m
 			"integrity": "sha1-pNprY1/8zMoz9w0Xy5JZLeleVXo=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10353,7 +10355,7 @@[m
 			"dependencies": {[m
 				"is-arrayish": {[m
 					"version": "0.3.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-arrayish/-/is-arrayish-0.3.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-arrayish/-/is-arrayish-0.3.2.tgz",[m
 					"integrity": "sha512-eVRqCvVlZbuw3GrM63ovNSNAeA1K16kaR/LRY/92w0zxQ5/1YzwblUX652i4Xs9RwAGjW9d9y6X88t8OaAJfWQ==",[m
 					"dev": true[m
 				}[m
[36m@@ -10361,19 +10363,19 @@[m
 		},[m
 		"slash": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/slash/-/slash-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/slash/-/slash-1.0.0.tgz",[m
 			"integrity": "sha1-xB8vbDn8FtHNF61LXYlhFK5HDVU=",[m
 			"dev": true[m
 		},[m
 		"sliced": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/sliced/-/sliced-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/sliced/-/sliced-1.0.1.tgz",[m
 			"integrity": "sha1-CzpmK10Ewxd7GSa+qCsD+Dei70E=",[m
 			"dev": true[m
 		},[m
 		"snapdragon": {[m
 			"version": "0.8.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/snapdragon/-/snapdragon-0.8.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/snapdragon/-/snapdragon-0.8.2.tgz",[m
 			"integrity": "sha512-FtyOnWN/wCHTVXOMwvSv26d+ko5vWlIDD6zoUJ7LW8vh+ZBC8QdljveRP+crNrtBwioEUWy/4dMtbBjA4ioNlg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10389,7 +10391,7 @@[m
 			"dependencies": {[m
 				"define-property": {[m
 					"version": "0.2.5",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-0.2.5.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-0.2.5.tgz",[m
 					"integrity": "sha1-w1se+RjsPJkPmlvFe+BKrOxcgRY=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -10398,7 +10400,7 @@[m
 				},[m
 				"extend-shallow": {[m
 					"version": "2.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 					"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -10409,7 +10411,7 @@[m
 		},[m
 		"snapdragon-node": {[m
 			"version": "2.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/snapdragon-node/-/snapdragon-node-2.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/snapdragon-node/-/snapdragon-node-2.1.1.tgz",[m
 			"integrity": "sha512-O27l4xaMYt/RSQ5TR3vpWCAB5Kb/czIcqUFOM/C4fYcLnbZUc1PkjTAMjof2pBWaSTwOUd6qUHcFGVGj7aIwnw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10420,7 +10422,7 @@[m
 			"dependencies": {[m
 				"define-property": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-1.0.0.tgz",[m
 					"integrity": "sha1-dp66rz9KY6rTr56NMEybvnm/sOY=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -10429,7 +10431,7 @@[m
 				},[m
 				"is-accessor-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-m5hnHTkcVsPfqx3AKlyttIPb7J+XykHvJP2B9bZDjlhLIoEq4XoK64Vg7boZlVWYK6LUY94dYPEE7Lh0ZkZKcQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -10438,7 +10440,7 @@[m
 				},[m
 				"is-data-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-jbRXy1FmtAoCjQkVmIVYwuuqDFUbaOeDjmed1tOGPrsMhtJA4rD9tkgA0F1qJ3gRFRXcHYVkdeaP50Q5rE/jLQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -10447,7 +10449,7 @@[m
 				},[m
 				"is-descriptor": {[m
 					"version": "1.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
 					"integrity": "sha512-2eis5WqQGV7peooDyLmNEPUrps9+SXX5c9pL3xEB+4e9HnGuDa7mB7kHxHw4CbqS9k1T2hOH3miL8n8WtiYVtg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -10458,13 +10460,13 @@[m
 				},[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				},[m
 				"kind-of": {[m
 					"version": "6.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-6.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-6.0.2.tgz",[m
 					"integrity": "sha512-s5kLOcnH0XqDO+FvuaLX8DDjZ18CGFk7VygH40QoKPUQhW4e2rvM0rwUq0t8IQDOwYSeLK01U90OjzBTme2QqA==",[m
 					"dev": true[m
 				}[m
[36m@@ -10472,7 +10474,7 @@[m
 		},[m
 		"snapdragon-util": {[m
 			"version": "3.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/snapdragon-util/-/snapdragon-util-3.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/snapdragon-util/-/snapdragon-util-3.0.1.tgz",[m
 			"integrity": "sha512-mbKkMdQKsjX4BAL4bRYTj21edOf8cN7XHdYUJEe+Zn99hVEYcMvKPct1IqNe7+AZPirn8BCDOQBHQZknqmKlZQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10481,19 +10483,19 @@[m
 		},[m
 		"source-list-map": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-list-map/-/source-list-map-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/source-list-map/-/source-list-map-2.0.1.tgz",[m
 			"integrity": "sha512-qnQ7gVMxGNxsiL4lEuJwe/To8UnK7fAnmbGEEH8RpLouuKbeEm0lhbQVFIrNSuB+G7tVrAlVsZgETT5nljf+Iw==",[m
 			"dev": true[m
 		},[m
 		"source-map": {[m
 			"version": "0.5.7",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.5.7.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.5.7.tgz",[m
 			"integrity": "sha1-igOdLRAh0i0eoUyA2OpGi6LvP8w=",[m
 			"dev": true[m
 		},[m
 		"source-map-resolve": {[m
 			"version": "0.5.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map-resolve/-/source-map-resolve-0.5.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/source-map-resolve/-/source-map-resolve-0.5.2.tgz",[m
 			"integrity": "sha512-MjqsvNwyz1s0k81Goz/9vRBe9SZdB09Bdw+/zYyO+3CuPk6fouTaxscHkgtE8jKvf01kVfl8riHzERQ/kefaSA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10506,7 +10508,7 @@[m
 		},[m
 		"source-map-support": {[m
 			"version": "0.4.18",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map-support/-/source-map-support-0.4.18.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/source-map-support/-/source-map-support-0.4.18.tgz",[m
 			"integrity": "sha512-try0/JqxPLF9nOjvSta7tVondkP5dwgyLDjVoyMDlmjugT2lRZ1OfsrYTkCd2hkDnJTKRbO/Rl3orm8vlsUzbA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10515,13 +10517,13 @@[m
 		},[m
 		"source-map-url": {[m
 			"version": "0.4.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map-url/-/source-map-url-0.4.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/source-map-url/-/source-map-url-0.4.0.tgz",[m
 			"integrity": "sha1-PpNdfd1zYxuXZZlW1VEo6HtQhKM=",[m
 			"dev": true[m
 		},[m
 		"sparse-bitfield": {[m
 			"version": "3.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/sparse-bitfield/-/sparse-bitfield-3.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/sparse-bitfield/-/sparse-bitfield-3.0.3.tgz",[m
 			"integrity": "sha1-/0rm5oZWBWuks+eSqzM004JzyhE=",[m
 			"dev": true,[m
 			"optional": true,[m
[36m@@ -10541,13 +10543,13 @@[m
 		},[m
 		"spdx-exceptions": {[m
 			"version": "2.2.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/spdx-exceptions/-/spdx-exceptions-2.2.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/spdx-exceptions/-/spdx-exceptions-2.2.0.tgz",[m
 			"integrity": "sha512-2XQACfElKi9SlVb1CYadKDXvoajPgBVPn/gOQLrTvHdElaVhr7ZEbqJaRnJLVNeaI4cMEAgVCeBMKF6MWRDCRA==",[m
 			"dev": true[m
 		},[m
 		"spdx-expression-parse": {[m
 			"version": "3.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/spdx-expression-parse/-/spdx-expression-parse-3.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/spdx-expression-parse/-/spdx-expression-parse-3.0.0.tgz",[m
 			"integrity": "sha512-Yg6D3XpRD4kkOmTpdgbUiEJFKghJH03fiC1OPll5h/0sO6neh2jqRDVHOQ4o/LMea0tgCkbMgea5ip/e+MkWyg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10563,7 +10565,7 @@[m
 		},[m
 		"split-string": {[m
 			"version": "3.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/split-string/-/split-string-3.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/split-string/-/split-string-3.1.0.tgz",[m
 			"integrity": "sha512-NzNVhJDYpwceVVii8/Hu6DKfD2G+NrQHlS/V/qgv763EYudVwEcMQNxd2lh+0VrUByXN/oJkl5grOhYWvQUYiw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10572,7 +10574,7 @@[m
 		},[m
 		"sprintf-js": {[m
 			"version": "1.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/sprintf-js/-/sprintf-js-1.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/sprintf-js/-/sprintf-js-1.0.3.tgz",[m
 			"integrity": "sha1-BOaSb2YolTVPPdAVIDYzuFcpfiw=",[m
 			"dev": true[m
 		},[m
[36m@@ -10595,7 +10597,7 @@[m
 		},[m
 		"ssri": {[m
 			"version": "5.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ssri/-/ssri-5.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/ssri/-/ssri-5.3.0.tgz",[m
 			"integrity": "sha512-XRSIPqLij52MtgoQavH/x/dU1qVKtWUAAZeOHsR9c2Ddi4XerFy3mc1alf+dLJKl9EUIm/Ht+EowFkTUOA6GAQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10604,13 +10606,13 @@[m
 		},[m
 		"stable": {[m
 			"version": "0.1.8",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/stable/-/stable-0.1.8.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/stable/-/stable-0.1.8.tgz",[m
 			"integrity": "sha512-ji9qxRnOVfcuLDySj9qzhGSEFVobyt1kIOSkj1qZzYLzq7Tos/oUUWvotUPQLlrsidqsK6tBH89Bc9kL5zHA6w==",[m
 			"dev": true[m
 		},[m
 		"static-extend": {[m
 			"version": "0.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/static-extend/-/static-extend-0.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/static-extend/-/static-extend-0.1.2.tgz",[m
 			"integrity": "sha1-YICcOcv/VTNyJv1eC1IPNB8ftcY=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10620,7 +10622,7 @@[m
 			"dependencies": {[m
 				"define-property": {[m
 					"version": "0.2.5",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-0.2.5.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-0.2.5.tgz",[m
 					"integrity": "sha1-w1se+RjsPJkPmlvFe+BKrOxcgRY=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -10631,12 +10633,12 @@[m
 		},[m
 		"statuses": {[m
 			"version": "1.5.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/statuses/-/statuses-1.5.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/statuses/-/statuses-1.5.0.tgz",[m
 			"integrity": "sha1-Fhx9rBd2Wf2YEfQ3cfqZOBR4Yow="[m
 		},[m
 		"stdout-stream": {[m
 			"version": "1.4.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/stdout-stream/-/stdout-stream-1.4.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/stdout-stream/-/stdout-stream-1.4.1.tgz",[m
 			"integrity": "sha512-j4emi03KXqJWcIeF8eIXkjMFN1Cmb8gUlDYGeBALLPo5qdyTfA9bOtl8m33lRoC+vFMkP3gl0WsDr6+gzxbbTA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10655,7 +10657,7 @@[m
 		},[m
 		"stream-each": {[m
 			"version": "1.2.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/stream-each/-/stream-each-1.2.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/stream-each/-/stream-each-1.2.3.tgz",[m
 			"integrity": "sha512-vlMC2f8I2u/bZGqkdfLQW/13Zihpej/7PmSiMQsbYddxuTsJp8vRe2x2FvVExZg7FaOds43ROAuFJwPR4MTZLw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10665,7 +10667,7 @@[m
 		},[m
 		"stream-http": {[m
 			"version": "2.8.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/stream-http/-/stream-http-2.8.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/stream-http/-/stream-http-2.8.3.tgz",[m
 			"integrity": "sha512-+TSkfINHDo4J+ZobQLWiMouQYB+UVYFttRA94FpEzzJ7ZdqcL4uUUQ7WkdkI4DSozGmgBUE/a47L+38PenXhUw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10678,19 +10680,19 @@[m
 		},[m
 		"stream-shift": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/stream-shift/-/stream-shift-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/stream-shift/-/stream-shift-1.0.0.tgz",[m
 			"integrity": "sha1-1cdSgl5TZ+eG944Y5EXqIjoVWVI=",[m
 			"dev": true[m
 		},[m
 		"streamsearch": {[m
 			"version": "0.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/streamsearch/-/streamsearch-0.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/streamsearch/-/streamsearch-0.1.2.tgz",[m
 			"integrity": "sha1-gIudDlb8Jz2Am6VzOOkpkZoanxo=",[m
 			"dev": true[m
 		},[m
 		"string-width": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/string-width/-/string-width-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/string-width/-/string-width-1.0.2.tgz",[m
 			"integrity": "sha1-EYvfW4zcUaKn5w0hHgfisLmxB9M=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10701,7 +10703,7 @@[m
 		},[m
 		"string.prototype.padend": {[m
 			"version": "3.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/string.prototype.padend/-/string.prototype.padend-3.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/string.prototype.padend/-/string.prototype.padend-3.0.0.tgz",[m
 			"integrity": "sha1-86rvfBcZ8XDF6rHDK/eA2W4h8vA=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10712,7 +10714,7 @@[m
 		},[m
 		"string_decoder": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/string_decoder/-/string_decoder-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/string_decoder/-/string_decoder-1.1.1.tgz",[m
 			"integrity": "sha512-n/ShnvDi6FHbbVfviro+WojiFzv+s8MPMHBczVePfUpDJLwoLT0ht1l4YwBCbi8pJAveEEdnkHyPyTP/mzRfwg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10721,7 +10723,7 @@[m
 		},[m
 		"strip-ansi": {[m
 			"version": "3.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/strip-ansi/-/strip-ansi-3.0.1.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/strip-ansi/-/strip-ansi-3.0.1.tgz",[m
 			"integrity": "sha1-ajhfuIU9lS1f8F0Oiq+UJ43GPc8=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10730,7 +10732,7 @@[m
 		},[m
 		"strip-bom": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/strip-bom/-/strip-bom-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/strip-bom/-/strip-bom-2.0.0.tgz",[m
 			"integrity": "sha1-YhmoVhZSBJHzV4i9vxRHqZx+aw4=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10739,13 +10741,13 @@[m
 		},[m
 		"strip-eof": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/strip-eof/-/strip-eof-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/strip-eof/-/strip-eof-1.0.0.tgz",[m
 			"integrity": "sha1-u0P/VZim6wXYm1n80SnJgzE2Br8=",[m
 			"dev": true[m
 		},[m
 		"strip-indent": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/strip-indent/-/strip-indent-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/strip-indent/-/strip-indent-1.0.1.tgz",[m
 			"integrity": "sha1-DHlipq3vp7vUrDZkYKY4VSrhoKI=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10754,7 +10756,7 @@[m
 		},[m
 		"style-loader": {[m
 			"version": "0.23.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/style-loader/-/style-loader-0.23.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/style-loader/-/style-loader-0.23.1.tgz",[m
 			"integrity": "sha512-XK+uv9kWwhZMZ1y7mysB+zoihsEj4wneFWAS5qoiLwzW0WzSqMrrsIy+a3zkQJq0ipFtBpX5W3MqyRIBF/WFGg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -10775,7 +10777,7 @@[m
 			"dependencies": {[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -10828,7 +10830,7 @@[m
 				},[m
 				"postcss-selector-parser": {[m
 					"version": "3.1.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/postcss-selector-parser/-/postcss-selector-parser-3.1.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/postcss-selector-parser/-/postcss-selector-parser-3.1.1.tgz",[m
 					"integrity": "sha1-T4dfSvsMllc9XPTXQBGu4lCn6GU=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -10839,7 +10841,7 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -10856,7 +10858,7 @@[m
 		},[m
 		"supports-color": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/supports-color/-/supports-color-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/supports-color/-/supports-color-2.0.0.tgz",[m
 			"integrity": "sha1-U10EXOa2Nj+kARcIRimZXp3zJMc=",[m
 			"dev": true[m
 		},[m
[36m@@ -10955,7 +10957,7 @@[m
 			"dependencies": {[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
[36m@@ -11022,7 +11024,7 @@[m
 				},[m
 				"find-up": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/find-up/-/find-up-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/find-up/-/find-up-3.0.0.tgz",[m
 					"integrity": "sha512-1yD6RmLI1XBfxugvORwlck6f75tYL+iR0jqwsOrOxMZyGYqUuDhJ0l4AXdO1iX/FTs9cBAMEk1gWSEx1kSbylg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11031,7 +11033,7 @@[m
 				},[m
 				"locate-path": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/locate-path/-/locate-path-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/locate-path/-/locate-path-3.0.0.tgz",[m
 					"integrity": "sha512-7AO748wWnIhNqAuaty2ZWHkQHRSNfPVIsPIfwEOWO22AmaoVrWavlOcMR5nzTLNYvp36X220/maaRsrec1G65A==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11060,7 +11062,7 @@[m
 				},[m
 				"mississippi": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/mississippi/-/mississippi-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/mississippi/-/mississippi-3.0.0.tgz",[m
 					"integrity": "sha512-x471SsVjUtBRtcvd4BzKE9kFC+/2TeWgKCgw0bZcw1b9l2X3QX5vCWgF+KaZaYm87Ss//rHnWryupDrgLvmSkA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11087,7 +11089,7 @@[m
 				},[m
 				"p-locate": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/p-locate/-/p-locate-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/p-locate/-/p-locate-3.0.0.tgz",[m
 					"integrity": "sha512-x+12w/To+4GFfgJhBEpiDcLozRJGegY+Ei7/z0tSLkMmxGZNybVMSfWj9aJn8Z5Fc7dBUNJOOVgPv2H7IwulSQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11108,7 +11110,7 @@[m
 				},[m
 				"pkg-dir": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pkg-dir/-/pkg-dir-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/pkg-dir/-/pkg-dir-3.0.0.tgz",[m
 					"integrity": "sha512-/E57AYkoeQ25qkxMj5PBOVgF8Kiu/h7cYS30Z5+R7WaiCCBfLq58ZI/dSeaEKb9WVJV5n/03QwrN3IeWIFllvw==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11117,7 +11119,7 @@[m
 				},[m
 				"pump": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pump/-/pump-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/pump/-/pump-3.0.0.tgz",[m
 					"integrity": "sha512-LwZy+p3SFs1Pytd/jYct4wpv49HiYCqd9Rlc5ZVdk0V+8Yzv6jR5Blk3TRmPL1ft69TxP0IMZGJ+WPFU2BFhww==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11127,13 +11129,13 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
 				"ssri": {[m
 					"version": "6.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ssri/-/ssri-6.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ssri/-/ssri-6.0.1.tgz",[m
 					"integrity": "sha512-3Wge10hNcT1Kur4PDFwEieXSCMCJs/7WvSACcrMYrNp+b8kDL1/0wJch5Ni2WrtwEa2IO8OsVfeKIciKCDx/QA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11150,7 +11152,7 @@[m
 		},[m
 		"through2": {[m
 			"version": "2.0.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/through2/-/through2-2.0.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/through2/-/through2-2.0.5.tgz",[m
 			"integrity": "sha512-/mrRod8xqpA+IHSLyGCQ2s8SPHiCDEeQJSep1jqLYeEUClOFG2Qsh+4FU6G9VeqpZnGW/Su8LQGc4YKni5rYSQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11169,25 +11171,25 @@[m
 		},[m
 		"timsort": {[m
 			"version": "0.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/timsort/-/timsort-0.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/timsort/-/timsort-0.3.0.tgz",[m
 			"integrity": "sha1-QFQRqOfmM5/mTbmiNN4R3DHgK9Q=",[m
 			"dev": true[m
 		},[m
 		"to-arraybuffer": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/to-arraybuffer/-/to-arraybuffer-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/to-arraybuffer/-/to-arraybuffer-1.0.1.tgz",[m
 			"integrity": "sha1-fSKbH8xjfkZsoIEYCDanqr/4P0M=",[m
 			"dev": true[m
 		},[m
 		"to-fast-properties": {[m
 			"version": "1.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/to-fast-properties/-/to-fast-properties-1.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/to-fast-properties/-/to-fast-properties-1.0.3.tgz",[m
 			"integrity": "sha1-uDVx+k2MJbguIxsG46MFXeTKGkc=",[m
 			"dev": true[m
 		},[m
 		"to-object-path": {[m
 			"version": "0.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/to-object-path/-/to-object-path-0.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/to-object-path/-/to-object-path-0.3.0.tgz",[m
 			"integrity": "sha1-KXWIt7Dn4KwI4E5nL4XB9JmeF68=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11196,7 +11198,7 @@[m
 		},[m
 		"to-regex": {[m
 			"version": "3.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/to-regex/-/to-regex-3.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/to-regex/-/to-regex-3.0.2.tgz",[m
 			"integrity": "sha512-FWtleNAtZ/Ki2qtqej2CXTOayOH9bHDQF+Q48VpWyDXjbYxA4Yz8iDB31zXOBUlOHHKidDbqGVrTUvQMPmBGBw==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11208,7 +11210,7 @@[m
 		},[m
 		"to-regex-range": {[m
 			"version": "2.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/to-regex-range/-/to-regex-range-2.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/to-regex-range/-/to-regex-range-2.1.1.tgz",[m
 			"integrity": "sha1-fIDBe53+vlmeJzZ+DU3VWQFB2zg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11218,7 +11220,7 @@[m
 			"dependencies": {[m
 				"is-number": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-number/-/is-number-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-number/-/is-number-3.0.0.tgz",[m
 					"integrity": "sha1-JP1iAaR4LPUFYcgQJ2r8fRLXEZU=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11234,7 +11236,7 @@[m
 		},[m
 		"tough-cookie": {[m
 			"version": "2.4.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/tough-cookie/-/tough-cookie-2.4.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/tough-cookie/-/tough-cookie-2.4.3.tgz",[m
 			"integrity": "sha512-Q5srk/4vDM54WJsJio3XNn6K2sCG+CQ8G5Wz6bZhRZoAe/+TxjWB/GlFAnYEbkYVlON9FMk/fE3h2RLpPXo4lQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11252,25 +11254,25 @@[m
 		},[m
 		"tree-kill": {[m
 			"version": "1.2.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/tree-kill/-/tree-kill-1.2.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/tree-kill/-/tree-kill-1.2.1.tgz",[m
 			"integrity": "sha512-4hjqbObwlh2dLyW4tcz0Ymw0ggoaVDMveUB9w8kFSQScdRLo0gxO9J7WFcUBo+W3C1TLdFIEwNOWebgZZ0RH9Q==",[m
 			"dev": true[m
 		},[m
 		"trim-newlines": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/trim-newlines/-/trim-newlines-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/trim-newlines/-/trim-newlines-1.0.0.tgz",[m
 			"integrity": "sha1-WIeWa7WCpFA6QetST301ARgVphM=",[m
 			"dev": true[m
 		},[m
 		"trim-right": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/trim-right/-/trim-right-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/trim-right/-/trim-right-1.0.1.tgz",[m
 			"integrity": "sha1-yy4SAwZ+DI3h9hQJS5/kVwTqYAM=",[m
 			"dev": true[m
 		},[m
 		"true-case-path": {[m
 			"version": "1.0.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/true-case-path/-/true-case-path-1.0.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/true-case-path/-/true-case-path-1.0.3.tgz",[m
 			"integrity": "sha512-m6s2OdQe5wgpFMC+pAJ+q9djG82O2jcHPOI6RNg1yy9rCYR+WD6Nbpl32fDpfC56nirdRy+opFa/Vk7HYhqaew==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11279,7 +11281,7 @@[m
 		},[m
 		"tslib": {[m
 			"version": "1.9.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/tslib/-/tslib-1.9.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/tslib/-/tslib-1.9.3.tgz",[m
 			"integrity": "sha512-4krF8scpejhaOgqzBEcGM7yDIEfi0/8+8zDRZhNZZ2kjmHJ4hv3zCbQWxoJGz1iw5U0Jl0nma13xzHXcncMavQ==",[m
 			"dev": true[m
 		},[m
[36m@@ -11291,7 +11293,7 @@[m
 		},[m
 		"tunnel-agent": {[m
 			"version": "0.6.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/tunnel-agent/-/tunnel-agent-0.6.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/tunnel-agent/-/tunnel-agent-0.6.0.tgz",[m
 			"integrity": "sha1-J6XeoGs2sEoKmWZ3SykIaPD8QP0=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11300,7 +11302,7 @@[m
 		},[m
 		"tweetnacl": {[m
 			"version": "0.14.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/tweetnacl/-/tweetnacl-0.14.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/tweetnacl/-/tweetnacl-0.14.5.tgz",[m
 			"integrity": "sha1-WuaBd/GS1EViadEIr6k/+HQ/T2Q=",[m
 			"dev": true[m
 		},[m
[36m@@ -11315,7 +11317,7 @@[m
 		},[m
 		"typedarray": {[m
 			"version": "0.0.6",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/typedarray/-/typedarray-0.0.6.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/typedarray/-/typedarray-0.0.6.tgz",[m
 			"integrity": "sha1-hnrHTjhkGHsdPUfZlqeOxciDB3c=",[m
 			"dev": true[m
 		},[m
[36m@@ -11388,7 +11390,7 @@[m
 				},[m
 				"find-up": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/find-up/-/find-up-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/find-up/-/find-up-3.0.0.tgz",[m
 					"integrity": "sha512-1yD6RmLI1XBfxugvORwlck6f75tYL+iR0jqwsOrOxMZyGYqUuDhJ0l4AXdO1iX/FTs9cBAMEk1gWSEx1kSbylg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11397,7 +11399,7 @@[m
 				},[m
 				"locate-path": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/locate-path/-/locate-path-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/locate-path/-/locate-path-3.0.0.tgz",[m
 					"integrity": "sha512-7AO748wWnIhNqAuaty2ZWHkQHRSNfPVIsPIfwEOWO22AmaoVrWavlOcMR5nzTLNYvp36X220/maaRsrec1G65A==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11426,7 +11428,7 @@[m
 				},[m
 				"mississippi": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/mississippi/-/mississippi-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/mississippi/-/mississippi-3.0.0.tgz",[m
 					"integrity": "sha512-x471SsVjUtBRtcvd4BzKE9kFC+/2TeWgKCgw0bZcw1b9l2X3QX5vCWgF+KaZaYm87Ss//rHnWryupDrgLvmSkA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11453,7 +11455,7 @@[m
 				},[m
 				"p-locate": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/p-locate/-/p-locate-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/p-locate/-/p-locate-3.0.0.tgz",[m
 					"integrity": "sha512-x+12w/To+4GFfgJhBEpiDcLozRJGegY+Ei7/z0tSLkMmxGZNybVMSfWj9aJn8Z5Fc7dBUNJOOVgPv2H7IwulSQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11474,7 +11476,7 @@[m
 				},[m
 				"pkg-dir": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pkg-dir/-/pkg-dir-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/pkg-dir/-/pkg-dir-3.0.0.tgz",[m
 					"integrity": "sha512-/E57AYkoeQ25qkxMj5PBOVgF8Kiu/h7cYS30Z5+R7WaiCCBfLq58ZI/dSeaEKb9WVJV5n/03QwrN3IeWIFllvw==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11483,7 +11485,7 @@[m
 				},[m
 				"pump": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/pump/-/pump-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/pump/-/pump-3.0.0.tgz",[m
 					"integrity": "sha512-LwZy+p3SFs1Pytd/jYct4wpv49HiYCqd9Rlc5ZVdk0V+8Yzv6jR5Blk3TRmPL1ft69TxP0IMZGJ+WPFU2BFhww==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11493,13 +11495,13 @@[m
 				},[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				},[m
 				"ssri": {[m
 					"version": "6.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ssri/-/ssri-6.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ssri/-/ssri-6.0.1.tgz",[m
 					"integrity": "sha512-3Wge10hNcT1Kur4PDFwEieXSCMCJs/7WvSACcrMYrNp+b8kDL1/0wJch5Ni2WrtwEa2IO8OsVfeKIciKCDx/QA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11516,7 +11518,7 @@[m
 		},[m
 		"uid-safe": {[m
 			"version": "2.1.5",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/uid-safe/-/uid-safe-2.1.5.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/uid-safe/-/uid-safe-2.1.5.tgz",[m
 			"integrity": "sha512-KPHm4VL5dDXKz01UuEd88Df+KzynaohSL9fBh096KWAxSKZQDI2uBrVqtvRM4rwrIrRRKsdLNML/lnaaVSRioA==",[m
 			"requires": {[m
 				"random-bytes": "~1.0.0"[m
[36m@@ -11524,7 +11526,7 @@[m
 		},[m
 		"union-value": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/union-value/-/union-value-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/union-value/-/union-value-1.0.0.tgz",[m
 			"integrity": "sha1-XHHDTLW61dzr4+oM0IIHulqhrqQ=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11536,7 +11538,7 @@[m
 			"dependencies": {[m
 				"extend-shallow": {[m
 					"version": "2.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 					"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11545,7 +11547,7 @@[m
 				},[m
 				"set-value": {[m
 					"version": "0.4.3",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/set-value/-/set-value-0.4.3.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/set-value/-/set-value-0.4.3.tgz",[m
 					"integrity": "sha1-fbCPnT0i3H945Trzw79GZuzfzPE=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11559,19 +11561,19 @@[m
 		},[m
 		"uniq": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/uniq/-/uniq-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/uniq/-/uniq-1.0.1.tgz",[m
 			"integrity": "sha1-sxxa6CVIRKOoKBVBzisEuGWnNP8=",[m
 			"dev": true[m
 		},[m
 		"uniqs": {[m
 			"version": "2.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/uniqs/-/uniqs-2.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/uniqs/-/uniqs-2.0.0.tgz",[m
 			"integrity": "sha1-/+3ks2slKQaW5uFl1KWe25mOawI=",[m
 			"dev": true[m
 		},[m
 		"unique-filename": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/unique-filename/-/unique-filename-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/unique-filename/-/unique-filename-1.1.1.tgz",[m
 			"integrity": "sha512-Vmp0jIp2ln35UTXuryvjzkjGdRyf9b2lTXuSYUiPmzRcl3FDtYqAwOnTJkAngD9SWhnoJzDbTKwaOrZ+STtxNQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11580,7 +11582,7 @@[m
 		},[m
 		"unique-slug": {[m
 			"version": "2.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/unique-slug/-/unique-slug-2.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/unique-slug/-/unique-slug-2.0.1.tgz",[m
 			"integrity": "sha512-n9cU6+gITaVu7VGj1Z8feKMmfAjEAQGhwD9fE3zvpRRa0wEIx8ODYkVGfSc94M2OX00tUFV8wH3zYbm1I8mxFg==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11589,18 +11591,18 @@[m
 		},[m
 		"unpipe": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/unpipe/-/unpipe-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/unpipe/-/unpipe-1.0.0.tgz",[m
 			"integrity": "sha1-sr9O6FFKrmFltIF4KdIbLvSZBOw="[m
 		},[m
 		"unquote": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/unquote/-/unquote-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/unquote/-/unquote-1.1.1.tgz",[m
 			"integrity": "sha1-j97XMk7G6IoP+LkF58CYzcCG1UQ=",[m
 			"dev": true[m
 		},[m
 		"unset-value": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/unset-value/-/unset-value-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/unset-value/-/unset-value-1.0.0.tgz",[m
 			"integrity": "sha1-g3aHP30jNRef+x5vw6jtDfyKtVk=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11610,7 +11612,7 @@[m
 			"dependencies": {[m
 				"has-value": {[m
 					"version": "0.3.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/has-value/-/has-value-0.3.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/has-value/-/has-value-0.3.1.tgz",[m
 					"integrity": "sha1-ex9YutpiyoJ+wKIHgCVlSEWZXh8=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11621,7 +11623,7 @@[m
 					"dependencies": {[m
 						"isobject": {[m
 							"version": "2.1.0",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-2.1.0.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-2.1.0.tgz",[m
 							"integrity": "sha1-8GVWEJaj8dou9GJy+BXIQNh+DIk=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -11632,13 +11634,13 @@[m
 				},[m
 				"has-values": {[m
 					"version": "0.1.4",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/has-values/-/has-values-0.1.4.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/has-values/-/has-values-0.1.4.tgz",[m
 					"integrity": "sha1-bWHeldkd/Km5oCCJrThL/49it3E=",[m
 					"dev": true[m
 				},[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				}[m
[36m@@ -11652,7 +11654,7 @@[m
 		},[m
 		"uri-js": {[m
 			"version": "4.2.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/uri-js/-/uri-js-4.2.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/uri-js/-/uri-js-4.2.2.tgz",[m
 			"integrity": "sha512-KY9Frmirql91X2Qgjry0Wd4Y+YTdrdZheS8TFwvkbLWf/G5KNJDCh6pKL5OZctEW4+0Baa5idK2ZQuELRwPznQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11661,13 +11663,13 @@[m
 		},[m
 		"urix": {[m
 			"version": "0.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/urix/-/urix-0.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/urix/-/urix-0.1.0.tgz",[m
 			"integrity": "sha1-2pN/emLiH+wf0Y1Js1wpNQZ6bHI=",[m
 			"dev": true[m
 		},[m
 		"url": {[m
 			"version": "0.11.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/url/-/url-0.11.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/url/-/url-0.11.0.tgz",[m
 			"integrity": "sha1-ODjpfPxgUh63PFJajlW/3Z4uKPE=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11677,7 +11679,7 @@[m
 			"dependencies": {[m
 				"punycode": {[m
 					"version": "1.3.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/punycode/-/punycode-1.3.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/punycode/-/punycode-1.3.2.tgz",[m
 					"integrity": "sha1-llOgNvt8HuQjQvIyXM7v6jkmxI0=",[m
 					"dev": true[m
 				}[m
[36m@@ -11685,13 +11687,13 @@[m
 		},[m
 		"use": {[m
 			"version": "3.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/use/-/use-3.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/use/-/use-3.1.1.tgz",[m
 			"integrity": "sha512-cwESVXlO3url9YWlFW/TA9cshCEhtu7IKJ/p5soJ/gGpj7vbvFrAY/eIioQ6Dw23KjZhYgiIo8HOs1nQ2vr/oQ==",[m
 			"dev": true[m
 		},[m
 		"user-home": {[m
 			"version": "1.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/user-home/-/user-home-1.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/user-home/-/user-home-1.1.1.tgz",[m
 			"integrity": "sha1-K1viOjK2Onyd640PKNSFcko98ZA=",[m
 			"dev": true[m
 		},[m
[36m@@ -11706,13 +11708,13 @@[m
 		},[m
 		"util-deprecate": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/util-deprecate/-/util-deprecate-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/util-deprecate/-/util-deprecate-1.0.2.tgz",[m
 			"integrity": "sha1-RQ1Nyfpw3nMnYvvS1KKJgUGaDM8=",[m
 			"dev": true[m
 		},[m
 		"util.promisify": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/util.promisify/-/util.promisify-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/util.promisify/-/util.promisify-1.0.0.tgz",[m
 			"integrity": "sha512-i+6qA2MPhvoKLuxnJNpXAGhg7HphQOSUq2LKMZD0m15EiskXUkMvKdF4Uui0WYeCUGea+o2cw/ZuwehtfsrNkA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11722,24 +11724,24 @@[m
 		},[m
 		"utils-merge": {[m
 			"version": "1.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/utils-merge/-/utils-merge-1.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/utils-merge/-/utils-merge-1.0.1.tgz",[m
 			"integrity": "sha1-n5VxD1CiZ5R7LMwSR0HBAoQn5xM="[m
 		},[m
 		"uuid": {[m
 			"version": "3.3.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/uuid/-/uuid-3.3.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/uuid/-/uuid-3.3.2.tgz",[m
 			"integrity": "sha512-yXJmeNaw3DnnKAOKJE51sL/ZaYfWJRl1pK9dr19YFCu0ObS231AB1/LbqTKRAQ5kw8A90rA6fr4riOUpTZvQZA==",[m
 			"dev": true[m
 		},[m
 		"v8-compile-cache": {[m
 			"version": "2.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/v8-compile-cache/-/v8-compile-cache-2.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/v8-compile-cache/-/v8-compile-cache-2.0.2.tgz",[m
 			"integrity": "sha512-1wFuMUIM16MDJRCrpbpuEPTUGmM5QMUg0cr3KFwra2XgOgFcPGDQHDh3CszSCD2Zewc/dh/pamNEW8CbfDebUw==",[m
 			"dev": true[m
 		},[m
 		"v8flags": {[m
 			"version": "2.1.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/v8flags/-/v8flags-2.1.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/v8flags/-/v8flags-2.1.1.tgz",[m
 			"integrity": "sha1-qrGh+jDUX4jdMhFIh1rALAtV5bQ=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11748,7 +11750,7 @@[m
 		},[m
 		"validate-npm-package-license": {[m
 			"version": "3.0.4",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/validate-npm-package-license/-/validate-npm-package-license-3.0.4.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/validate-npm-package-license/-/validate-npm-package-license-3.0.4.tgz",[m
 			"integrity": "sha512-DpKm2Ui/xN7/HQKCtpZxoRWBhZ9Z0kqtygG8XCgNQ8ZlDnxuQmWhj566j8fN4Cu3/JmbhsDo7fcAJq4s9h27Ew==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11763,18 +11765,18 @@[m
 		},[m
 		"vary": {[m
 			"version": "1.1.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/vary/-/vary-1.1.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/vary/-/vary-1.1.2.tgz",[m
 			"integrity": "sha1-IpnwLG3tMNSllhsLn3RSShj2NPw="[m
 		},[m
 		"vendors": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/vendors/-/vendors-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/vendors/-/vendors-1.0.2.tgz",[m
 			"integrity": "sha512-w/hry/368nO21AN9QljsaIhb9ZiZtZARoVH5f3CsFbawdLdayCgKRPup7CggujvySMxx0I91NOyxdVENohprLQ==",[m
 			"dev": true[m
 		},[m
 		"verror": {[m
 			"version": "1.10.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/verror/-/verror-1.10.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/verror/-/verror-1.10.0.tgz",[m
 			"integrity": "sha1-OhBcoXBTr1XW4nDB+CiGguGNpAA=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11794,7 +11796,7 @@[m
 		},[m
 		"watchpack": {[m
 			"version": "1.6.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/watchpack/-/watchpack-1.6.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/watchpack/-/watchpack-1.6.0.tgz",[m
 			"integrity": "sha512-i6dHe3EyLjMmDlU1/bGQpEw25XSjkJULPuAVKCbNRefQVq48yXKUpwg538F7AZTf9kyr57zj++pQFltUa5H7yA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -11805,7 +11807,7 @@[m
 			"dependencies": {[m
 				"anymatch": {[m
 					"version": "2.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/anymatch/-/anymatch-2.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/anymatch/-/anymatch-2.0.0.tgz",[m
 					"integrity": "sha512-5teOsQWABXHHBFP9y3skS5P3d/WfWXpv3FUpy+LorMrNYaT9pI4oLMQX7jzQ2KklNpGpWHzdCXTDT2Y3XGlZBw==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11826,19 +11828,19 @@[m
 				},[m
 				"arr-diff": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/arr-diff/-/arr-diff-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/arr-diff/-/arr-diff-4.0.0.tgz",[m
 					"integrity": "sha1-1kYQdP6/7HHn4VI1dhoyml3HxSA=",[m
 					"dev": true[m
 				},[m
 				"array-unique": {[m
 					"version": "0.3.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-unique/-/array-unique-0.3.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/array-unique/-/array-unique-0.3.2.tgz",[m
 					"integrity": "sha1-qJS3XUvE9s1nnvMkSp/Y9Gri1Cg=",[m
 					"dev": true[m
 				},[m
 				"braces": {[m
 					"version": "2.3.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/braces/-/braces-2.3.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/braces/-/braces-2.3.2.tgz",[m
 					"integrity": "sha512-aNdbnj9P8PjdXU4ybaWLK2IF3jc/EoDYbC7AazW6to3TRsfXxscC9UXOB5iDiEQrkyIbWp2SLQda4+QAa7nc3w==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11856,7 +11858,7 @@[m
 					"dependencies": {[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -11887,7 +11889,7 @@[m
 				},[m
 				"expand-brackets": {[m
 					"version": "2.1.4",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/expand-brackets/-/expand-brackets-2.1.4.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/expand-brackets/-/expand-brackets-2.1.4.tgz",[m
 					"integrity": "sha1-t3c14xXOMPa27/D4OwQVGiJEliI=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11902,7 +11904,7 @@[m
 					"dependencies": {[m
 						"define-property": {[m
 							"version": "0.2.5",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-0.2.5.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-0.2.5.tgz",[m
 							"integrity": "sha1-w1se+RjsPJkPmlvFe+BKrOxcgRY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -11911,7 +11913,7 @@[m
 						},[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -11920,7 +11922,7 @@[m
 						},[m
 						"is-accessor-descriptor": {[m
 							"version": "0.1.6",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-0.1.6.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-0.1.6.tgz",[m
 							"integrity": "sha1-qeEss66Nh2cn7u84Q/igiXtcmNY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -11929,7 +11931,7 @@[m
 							"dependencies": {[m
 								"kind-of": {[m
 									"version": "3.2.2",[m
[31m-									"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m									[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 									"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 									"dev": true,[m
 									"requires": {[m
[36m@@ -11940,7 +11942,7 @@[m
 						},[m
 						"is-data-descriptor": {[m
 							"version": "0.1.4",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-0.1.4.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-0.1.4.tgz",[m
 							"integrity": "sha1-C17mSDiOLIYCgueT8YVv7D8wG1Y=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -11949,7 +11951,7 @@[m
 							"dependencies": {[m
 								"kind-of": {[m
 									"version": "3.2.2",[m
[31m-									"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m									[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 									"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 									"dev": true,[m
 									"requires": {[m
[36m@@ -11960,7 +11962,7 @@[m
 						},[m
 						"is-descriptor": {[m
 							"version": "0.1.6",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-0.1.6.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-0.1.6.tgz",[m
 							"integrity": "sha512-avDYr0SB3DwO9zsMov0gKCESFYqCnE4hq/4z3TdUlukEy5t9C0YRq7HLrsN52NAcqXKaepeCD0n+B0arnVG3Hg==",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -11971,7 +11973,7 @@[m
 						},[m
 						"kind-of": {[m
 							"version": "5.1.0",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-5.1.0.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-5.1.0.tgz",[m
 							"integrity": "sha512-NGEErnH6F2vUuXDh+OlbcKW7/wOcfdRHaZ7VWtqCztfHri/++YKmP51OdWeGPuqCOba6kk2OTe5d02VmTB80Pw==",[m
 							"dev": true[m
 						}[m
[36m@@ -11979,7 +11981,7 @@[m
 				},[m
 				"extglob": {[m
 					"version": "2.0.4",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extglob/-/extglob-2.0.4.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/extglob/-/extglob-2.0.4.tgz",[m
 					"integrity": "sha512-Nmb6QXkELsuBr24CJSkilo6UHHgbekK5UiZgfE6UHD3Eb27YC6oD+bhcT+tJ6cl8dmsgdQxnWlcry8ksBIBLpw==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -11995,7 +11997,7 @@[m
 					"dependencies": {[m
 						"define-property": {[m
 							"version": "1.0.0",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-1.0.0.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-1.0.0.tgz",[m
 							"integrity": "sha1-dp66rz9KY6rTr56NMEybvnm/sOY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12004,7 +12006,7 @@[m
 						},[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12015,7 +12017,7 @@[m
 				},[m
 				"fill-range": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fill-range/-/fill-range-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/fill-range/-/fill-range-4.0.0.tgz",[m
 					"integrity": "sha1-1USBHUKPmOsGpj3EAtJAPDKMOPc=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12027,7 +12029,7 @@[m
 					"dependencies": {[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12038,7 +12040,7 @@[m
 				},[m
 				"glob-parent": {[m
 					"version": "3.1.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/glob-parent/-/glob-parent-3.1.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/glob-parent/-/glob-parent-3.1.0.tgz",[m
 					"integrity": "sha1-nmr2KZ2NO9K9QEMIMr0RPfkGxa4=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12048,7 +12050,7 @@[m
 					"dependencies": {[m
 						"is-glob": {[m
 							"version": "3.1.0",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-glob/-/is-glob-3.1.0.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-glob/-/is-glob-3.1.0.tgz",[m
 							"integrity": "sha1-e6WuJCF4BKxwcHuWkiVnSGzD6Eo=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12059,7 +12061,7 @@[m
 				},[m
 				"is-accessor-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-m5hnHTkcVsPfqx3AKlyttIPb7J+XykHvJP2B9bZDjlhLIoEq4XoK64Vg7boZlVWYK6LUY94dYPEE7Lh0ZkZKcQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12068,7 +12070,7 @@[m
 				},[m
 				"is-data-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-jbRXy1FmtAoCjQkVmIVYwuuqDFUbaOeDjmed1tOGPrsMhtJA4rD9tkgA0F1qJ3gRFRXcHYVkdeaP50Q5rE/jLQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12077,7 +12079,7 @@[m
 				},[m
 				"is-descriptor": {[m
 					"version": "1.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
 					"integrity": "sha512-2eis5WqQGV7peooDyLmNEPUrps9+SXX5c9pL3xEB+4e9HnGuDa7mB7kHxHw4CbqS9k1T2hOH3miL8n8WtiYVtg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12088,7 +12090,7 @@[m
 				},[m
 				"is-extglob": {[m
 					"version": "2.1.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-extglob/-/is-extglob-2.1.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-extglob/-/is-extglob-2.1.1.tgz",[m
 					"integrity": "sha1-qIwCU1eR8C7TfHahueqXc8gz+MI=",[m
 					"dev": true[m
 				},[m
[36m@@ -12103,7 +12105,7 @@[m
 				},[m
 				"is-number": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-number/-/is-number-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-number/-/is-number-3.0.0.tgz",[m
 					"integrity": "sha1-JP1iAaR4LPUFYcgQJ2r8fRLXEZU=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12112,7 +12114,7 @@[m
 					"dependencies": {[m
 						"kind-of": {[m
 							"version": "3.2.2",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 							"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12123,19 +12125,19 @@[m
 				},[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				},[m
 				"kind-of": {[m
 					"version": "6.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-6.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-6.0.2.tgz",[m
 					"integrity": "sha512-s5kLOcnH0XqDO+FvuaLX8DDjZ18CGFk7VygH40QoKPUQhW4e2rvM0rwUq0t8IQDOwYSeLK01U90OjzBTme2QqA==",[m
 					"dev": true[m
 				},[m
 				"micromatch": {[m
 					"version": "3.1.10",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/micromatch/-/micromatch-3.1.10.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/micromatch/-/micromatch-3.1.10.tgz",[m
 					"integrity": "sha512-MWikgl9n9M3w+bpsY3He8L+w9eF9338xRl8IAO5viDizwSzziFEyUzo2xrrloB64ADbTf8uA8vRqqttDTOmccg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12196,19 +12198,19 @@[m
 			"dependencies": {[m
 				"arr-diff": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/arr-diff/-/arr-diff-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/arr-diff/-/arr-diff-4.0.0.tgz",[m
 					"integrity": "sha1-1kYQdP6/7HHn4VI1dhoyml3HxSA=",[m
 					"dev": true[m
 				},[m
 				"array-unique": {[m
 					"version": "0.3.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/array-unique/-/array-unique-0.3.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/array-unique/-/array-unique-0.3.2.tgz",[m
 					"integrity": "sha1-qJS3XUvE9s1nnvMkSp/Y9Gri1Cg=",[m
 					"dev": true[m
 				},[m
 				"braces": {[m
 					"version": "2.3.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/braces/-/braces-2.3.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/braces/-/braces-2.3.2.tgz",[m
 					"integrity": "sha512-aNdbnj9P8PjdXU4ybaWLK2IF3jc/EoDYbC7AazW6to3TRsfXxscC9UXOB5iDiEQrkyIbWp2SLQda4+QAa7nc3w==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12226,7 +12228,7 @@[m
 					"dependencies": {[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12237,7 +12239,7 @@[m
 				},[m
 				"expand-brackets": {[m
 					"version": "2.1.4",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/expand-brackets/-/expand-brackets-2.1.4.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/expand-brackets/-/expand-brackets-2.1.4.tgz",[m
 					"integrity": "sha1-t3c14xXOMPa27/D4OwQVGiJEliI=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12252,7 +12254,7 @@[m
 					"dependencies": {[m
 						"define-property": {[m
 							"version": "0.2.5",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-0.2.5.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-0.2.5.tgz",[m
 							"integrity": "sha1-w1se+RjsPJkPmlvFe+BKrOxcgRY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12261,7 +12263,7 @@[m
 						},[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12270,7 +12272,7 @@[m
 						},[m
 						"is-accessor-descriptor": {[m
 							"version": "0.1.6",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-0.1.6.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-0.1.6.tgz",[m
 							"integrity": "sha1-qeEss66Nh2cn7u84Q/igiXtcmNY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12279,7 +12281,7 @@[m
 							"dependencies": {[m
 								"kind-of": {[m
 									"version": "3.2.2",[m
[31m-									"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m									[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 									"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 									"dev": true,[m
 									"requires": {[m
[36m@@ -12290,7 +12292,7 @@[m
 						},[m
 						"is-data-descriptor": {[m
 							"version": "0.1.4",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-0.1.4.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-0.1.4.tgz",[m
 							"integrity": "sha1-C17mSDiOLIYCgueT8YVv7D8wG1Y=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12299,7 +12301,7 @@[m
 							"dependencies": {[m
 								"kind-of": {[m
 									"version": "3.2.2",[m
[31m-									"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m									[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 									"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 									"dev": true,[m
 									"requires": {[m
[36m@@ -12310,7 +12312,7 @@[m
 						},[m
 						"is-descriptor": {[m
 							"version": "0.1.6",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-0.1.6.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-0.1.6.tgz",[m
 							"integrity": "sha512-avDYr0SB3DwO9zsMov0gKCESFYqCnE4hq/4z3TdUlukEy5t9C0YRq7HLrsN52NAcqXKaepeCD0n+B0arnVG3Hg==",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12321,7 +12323,7 @@[m
 						},[m
 						"kind-of": {[m
 							"version": "5.1.0",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-5.1.0.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-5.1.0.tgz",[m
 							"integrity": "sha512-NGEErnH6F2vUuXDh+OlbcKW7/wOcfdRHaZ7VWtqCztfHri/++YKmP51OdWeGPuqCOba6kk2OTe5d02VmTB80Pw==",[m
 							"dev": true[m
 						}[m
[36m@@ -12329,7 +12331,7 @@[m
 				},[m
 				"extglob": {[m
 					"version": "2.0.4",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extglob/-/extglob-2.0.4.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/extglob/-/extglob-2.0.4.tgz",[m
 					"integrity": "sha512-Nmb6QXkELsuBr24CJSkilo6UHHgbekK5UiZgfE6UHD3Eb27YC6oD+bhcT+tJ6cl8dmsgdQxnWlcry8ksBIBLpw==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12345,7 +12347,7 @@[m
 					"dependencies": {[m
 						"define-property": {[m
 							"version": "1.0.0",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/define-property/-/define-property-1.0.0.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/define-property/-/define-property-1.0.0.tgz",[m
 							"integrity": "sha1-dp66rz9KY6rTr56NMEybvnm/sOY=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12354,7 +12356,7 @@[m
 						},[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12365,7 +12367,7 @@[m
 				},[m
 				"fill-range": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/fill-range/-/fill-range-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/fill-range/-/fill-range-4.0.0.tgz",[m
 					"integrity": "sha1-1USBHUKPmOsGpj3EAtJAPDKMOPc=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12377,7 +12379,7 @@[m
 					"dependencies": {[m
 						"extend-shallow": {[m
 							"version": "2.0.1",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/extend-shallow/-/extend-shallow-2.0.1.tgz",[m
 							"integrity": "sha1-Ua99YUrZqfYQ6huvu5idaxxWiQ8=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12388,7 +12390,7 @@[m
 				},[m
 				"is-accessor-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-accessor-descriptor/-/is-accessor-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-m5hnHTkcVsPfqx3AKlyttIPb7J+XykHvJP2B9bZDjlhLIoEq4XoK64Vg7boZlVWYK6LUY94dYPEE7Lh0ZkZKcQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12397,7 +12399,7 @@[m
 				},[m
 				"is-data-descriptor": {[m
 					"version": "1.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-data-descriptor/-/is-data-descriptor-1.0.0.tgz",[m
 					"integrity": "sha512-jbRXy1FmtAoCjQkVmIVYwuuqDFUbaOeDjmed1tOGPrsMhtJA4rD9tkgA0F1qJ3gRFRXcHYVkdeaP50Q5rE/jLQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12406,7 +12408,7 @@[m
 				},[m
 				"is-descriptor": {[m
 					"version": "1.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-descriptor/-/is-descriptor-1.0.2.tgz",[m
 					"integrity": "sha512-2eis5WqQGV7peooDyLmNEPUrps9+SXX5c9pL3xEB+4e9HnGuDa7mB7kHxHw4CbqS9k1T2hOH3miL8n8WtiYVtg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12417,7 +12419,7 @@[m
 				},[m
 				"is-number": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-number/-/is-number-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-number/-/is-number-3.0.0.tgz",[m
 					"integrity": "sha1-JP1iAaR4LPUFYcgQJ2r8fRLXEZU=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12426,7 +12428,7 @@[m
 					"dependencies": {[m
 						"kind-of": {[m
 							"version": "3.2.2",[m
[31m-							"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-3.2.2.tgz",[m
[32m+[m							[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-3.2.2.tgz",[m
 							"integrity": "sha1-MeohpzS6ubuw8yRm2JOupR5KPGQ=",[m
 							"dev": true,[m
 							"requires": {[m
[36m@@ -12437,19 +12439,19 @@[m
 				},[m
 				"isobject": {[m
 					"version": "3.0.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/isobject/-/isobject-3.0.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/isobject/-/isobject-3.0.1.tgz",[m
 					"integrity": "sha1-TkMekrEalzFjaqH5yNHMvP2reN8=",[m
 					"dev": true[m
 				},[m
 				"kind-of": {[m
 					"version": "6.0.2",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/kind-of/-/kind-of-6.0.2.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/kind-of/-/kind-of-6.0.2.tgz",[m
 					"integrity": "sha512-s5kLOcnH0XqDO+FvuaLX8DDjZ18CGFk7VygH40QoKPUQhW4e2rvM0rwUq0t8IQDOwYSeLK01U90OjzBTme2QqA==",[m
 					"dev": true[m
 				},[m
 				"micromatch": {[m
 					"version": "3.1.10",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/micromatch/-/micromatch-3.1.10.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/micromatch/-/micromatch-3.1.10.tgz",[m
 					"integrity": "sha512-MWikgl9n9M3w+bpsY3He8L+w9eF9338xRl8IAO5viDizwSzziFEyUzo2xrrloB64ADbTf8uA8vRqqttDTOmccg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12491,13 +12493,13 @@[m
 			"dependencies": {[m
 				"ansi-regex": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-regex/-/ansi-regex-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-regex/-/ansi-regex-3.0.0.tgz",[m
 					"integrity": "sha1-7QMXwyIGT3lGbAKWa922Bas32Zg=",[m
 					"dev": true[m
 				},[m
 				"ansi-styles": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/ansi-styles/-/ansi-styles-3.2.1.tgz",[m
 					"integrity": "sha512-VT0ZI6kZRdTh8YyJw3SMbYm/u+NqfsAxEpWO0Pf9sq8/e94WxxOpPKx9FR1FlyCtOVDNOQ+8ntlqFxiRc+r5qA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12523,7 +12525,7 @@[m
 				},[m
 				"cliui": {[m
 					"version": "4.1.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cliui/-/cliui-4.1.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/cliui/-/cliui-4.1.0.tgz",[m
 					"integrity": "sha512-4FG+RSG9DL7uEwRUZXZn3SS34DiDPfzP0VOiEwtUWlE+AR2EIg+hSyvrIgUUfhdgR/UkAeW2QHgeP+hWrXs7jQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12534,7 +12536,7 @@[m
 				},[m
 				"cross-spawn": {[m
 					"version": "6.0.5",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/cross-spawn/-/cross-spawn-6.0.5.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/cross-spawn/-/cross-spawn-6.0.5.tgz",[m
 					"integrity": "sha512-eTVLrBSt7fjbDygz805pMnstIs2VTBNkRm0qxZd+M7A5XDdxVRWO5MxGBXZhjY4cqLYLdtrGqRf8mBPmzwSpWQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12547,7 +12549,7 @@[m
 				},[m
 				"find-up": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/find-up/-/find-up-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/find-up/-/find-up-3.0.0.tgz",[m
 					"integrity": "sha512-1yD6RmLI1XBfxugvORwlck6f75tYL+iR0jqwsOrOxMZyGYqUuDhJ0l4AXdO1iX/FTs9cBAMEk1gWSEx1kSbylg==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12556,19 +12558,19 @@[m
 				},[m
 				"invert-kv": {[m
 					"version": "2.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/invert-kv/-/invert-kv-2.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/invert-kv/-/invert-kv-2.0.0.tgz",[m
 					"integrity": "sha512-wPVv/y/QQ/Uiirj/vh3oP+1Ww+AWehmi1g5fFWGPF6IpCBCDVrhgHRMvrLfdYcwDh3QJbGXDW4JAuzxElLSqKA==",[m
 					"dev": true[m
 				},[m
 				"is-fullwidth-code-point": {[m
 					"version": "2.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/is-fullwidth-code-point/-/is-fullwidth-code-point-2.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/is-fullwidth-code-point/-/is-fullwidth-code-point-2.0.0.tgz",[m
 					"integrity": "sha1-o7MKXE8ZkYMWeqq5O+764937ZU8=",[m
 					"dev": true[m
 				},[m
 				"lcid": {[m
 					"version": "2.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/lcid/-/lcid-2.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/lcid/-/lcid-2.0.0.tgz",[m
 					"integrity": "sha512-avPEb8P8EGnwXKClwsNUgryVjllcRqtMYa49NTsbQagYuT1DcXnl1915oxWjoyGrXR6zH/Y0Zc96xWsPcoDKeA==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12577,7 +12579,7 @@[m
 				},[m
 				"locate-path": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/locate-path/-/locate-path-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/locate-path/-/locate-path-3.0.0.tgz",[m
 					"integrity": "sha512-7AO748wWnIhNqAuaty2ZWHkQHRSNfPVIsPIfwEOWO22AmaoVrWavlOcMR5nzTLNYvp36X220/maaRsrec1G65A==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12607,7 +12609,7 @@[m
 				},[m
 				"p-locate": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/p-locate/-/p-locate-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/p-locate/-/p-locate-3.0.0.tgz",[m
 					"integrity": "sha512-x+12w/To+4GFfgJhBEpiDcLozRJGegY+Ei7/z0tSLkMmxGZNybVMSfWj9aJn8Z5Fc7dBUNJOOVgPv2H7IwulSQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12622,7 +12624,7 @@[m
 				},[m
 				"string-width": {[m
 					"version": "2.1.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/string-width/-/string-width-2.1.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/string-width/-/string-width-2.1.1.tgz",[m
 					"integrity": "sha512-nOqH59deCq9SRHlxq1Aw85Jnt4w6KvLKqWVik6oA9ZklXLNIOlqg4F2yrT1MVaTjAqvVwdfeZ7w7aCvJD7ugkw==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12632,7 +12634,7 @@[m
 				},[m
 				"strip-ansi": {[m
 					"version": "4.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/strip-ansi/-/strip-ansi-4.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/strip-ansi/-/strip-ansi-4.0.0.tgz",[m
 					"integrity": "sha1-qEeQIusaw2iocTibY1JixQXuNo8=",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12641,7 +12643,7 @@[m
 				},[m
 				"supports-color": {[m
 					"version": "5.5.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/supports-color/-/supports-color-5.5.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/supports-color/-/supports-color-5.5.0.tgz",[m
 					"integrity": "sha512-QjVjwdXIt408MIiAqCX4oUKsgU2EqAGzs2Ppkm4aQYbjm+ZEWEcW4SfFNTr4uMNZma0ey4f5lgLrkB0aX0QMow==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12650,13 +12652,13 @@[m
 				},[m
 				"which-module": {[m
 					"version": "2.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/which-module/-/which-module-2.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/which-module/-/which-module-2.0.0.tgz",[m
 					"integrity": "sha1-2e8H3Od7mQK4o6j6SzHD4/fm6Ho=",[m
 					"dev": true[m
 				},[m
 				"yargs": {[m
 					"version": "12.0.5",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/yargs/-/yargs-12.0.5.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/yargs/-/yargs-12.0.5.tgz",[m
 					"integrity": "sha512-Lhz8TLaYnxq/2ObqHDql8dX8CJi97oHxrjUcYtzKbbykPtVW9WB+poxI+NM2UIzsMgNCZTIf0AQwsjK5yMAqZw==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12676,7 +12678,7 @@[m
 				},[m
 				"yargs-parser": {[m
 					"version": "11.1.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/yargs-parser/-/yargs-parser-11.1.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/yargs-parser/-/yargs-parser-11.1.1.tgz",[m
 					"integrity": "sha512-C6kB/WJDiaxONLJQnF8ccx9SEeoTTLek8RVbaOIsrAUS8VrBEXfmeSnCZxygc+XC2sNMBIwOOnfcxiynjHsVSQ==",[m
 					"dev": true,[m
 					"requires": {[m
[36m@@ -12697,13 +12699,13 @@[m
 		},[m
 		"webpack-node-externals": {[m
 			"version": "1.7.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/webpack-node-externals/-/webpack-node-externals-1.7.2.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/webpack-node-externals/-/webpack-node-externals-1.7.2.tgz",[m
 			"integrity": "sha512-ajerHZ+BJKeCLviLUUmnyd5B4RavLF76uv3cs6KNuO8W+HuQaEs0y0L7o40NQxdPy5w0pcv8Ew7yPUAQG0UdCg==",[m
 			"dev": true[m
 		},[m
 		"webpack-sources": {[m
 			"version": "1.3.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/webpack-sources/-/webpack-sources-1.3.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/webpack-sources/-/webpack-sources-1.3.0.tgz",[m
 			"integrity": "sha512-OiVgSrbGu7NEnEvQJJgdSFPl2qWKkWq5lHMhgiToIiN9w34EBnjYzSYs+VbL5KoYiLNtFFa7BZIKxRED3I32pA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -12713,7 +12715,7 @@[m
 			"dependencies": {[m
 				"source-map": {[m
 					"version": "0.6.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/source-map/-/source-map-0.6.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/source-map/-/source-map-0.6.1.tgz",[m
 					"integrity": "sha512-UjgapumWlbMhkBgzT7Ykc5YXUT46F0iKu8SGXq0bcwP5dz/h0Plj6enJqjz1Zbq2l5WaqYnrVbwWOWMyF3F47g==",[m
 					"dev": true[m
 				}[m
[36m@@ -12721,7 +12723,7 @@[m
 		},[m
 		"which": {[m
 			"version": "1.3.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/which/-/which-1.3.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/which/-/which-1.3.1.tgz",[m
 			"integrity": "sha512-HxJdYWq1MTIQbJ3nw0cqssHoTNU267KlrDuGZ1WYlxDStUtKUhOaJmh112/TZmHxxUfuJqPXSOm7tDyas0OSIQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -12730,13 +12732,13 @@[m
 		},[m
 		"which-module": {[m
 			"version": "1.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/which-module/-/which-module-1.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/which-module/-/which-module-1.0.0.tgz",[m
 			"integrity": "sha1-u6Y8qGGUiZT/MHc2CJ47lgJsKk8=",[m
 			"dev": true[m
 		},[m
 		"wide-align": {[m
 			"version": "1.1.3",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/wide-align/-/wide-align-1.1.3.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/wide-align/-/wide-align-1.1.3.tgz",[m
 			"integrity": "sha512-QGkOQc8XL6Bt5PwnsExKBPuMKBxnGxWWW3fU55Xt4feHozMUhdUMaBCk290qpm/wG5u/RSKzwdAC4i51YigihA==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -12745,7 +12747,7 @@[m
 		},[m
 		"worker-farm": {[m
 			"version": "1.6.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/worker-farm/-/worker-farm-1.6.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/worker-farm/-/worker-farm-1.6.0.tgz",[m
 			"integrity": "sha512-6w+3tHbM87WnSWnENBUvA2pxJPLhQUg5LKwUQHq3r+XPhIM+Gh2R5ycbwPCyuGbNg+lPgdcnQUhuC02kJCvffQ==",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -12754,7 +12756,7 @@[m
 		},[m
 		"wrap-ansi": {[m
 			"version": "2.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/wrap-ansi/-/wrap-ansi-2.1.0.tgz",[m
[32m+[m			[32m"resolved": "http://registry.npmjs.org/wrap-ansi/-/wrap-ansi-2.1.0.tgz",[m
 			"integrity": "sha1-2Pw9KE3QV5T+hJc8rs3Rz4JP3YU=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -12764,19 +12766,19 @@[m
 		},[m
 		"wrappy": {[m
 			"version": "1.0.2",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/wrappy/-/wrappy-1.0.2.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/wrappy/-/wrappy-1.0.2.tgz",[m
 			"integrity": "sha1-tSQ9jz7BqjXxNkYFvA0QNuMKtp8=",[m
 			"dev": true[m
 		},[m
 		"xtend": {[m
 			"version": "4.0.1",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/xtend/-/xtend-4.0.1.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/xtend/-/xtend-4.0.1.tgz",[m
 			"integrity": "sha1-pcbVMr5lbiPbgg77lDofBJmNY68=",[m
 			"dev": true[m
 		},[m
 		"y18n": {[m
 			"version": "4.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/y18n/-/y18n-4.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/y18n/-/y18n-4.0.0.tgz",[m
 			"integrity": "sha512-r9S/ZyXu/Xu9q1tYlpsLIsa3EeLXXk0VwlxqTcFRfg9EhMW+17kbt9G0NrgCmhGb5vT2hyhJZLfDGx+7+5Uj/w==",[m
 			"dev": true[m
 		},[m
[36m@@ -12788,7 +12790,7 @@[m
 		},[m
 		"yargs": {[m
 			"version": "7.1.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/yargs/-/yargs-7.1.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/yargs/-/yargs-7.1.0.tgz",[m
 			"integrity": "sha1-a6MY6xaWFyf10oT46gA+jWFU0Mg=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -12809,13 +12811,13 @@[m
 			"dependencies": {[m
 				"camelcase": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/camelcase/-/camelcase-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/camelcase/-/camelcase-3.0.0.tgz",[m
 					"integrity": "sha1-MvxLn82vhF/N9+c7uXysImHwqwo=",[m
 					"dev": true[m
 				},[m
 				"y18n": {[m
 					"version": "3.2.1",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/y18n/-/y18n-3.2.1.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/y18n/-/y18n-3.2.1.tgz",[m
 					"integrity": "sha1-bRX7qITAhnnA136I53WegR4H+kE=",[m
 					"dev": true[m
 				}[m
[36m@@ -12823,7 +12825,7 @@[m
 		},[m
 		"yargs-parser": {[m
 			"version": "5.0.0",[m
[31m-			"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/yargs-parser/-/yargs-parser-5.0.0.tgz",[m
[32m+[m			[32m"resolved": "https://registry.npmjs.org/yargs-parser/-/yargs-parser-5.0.0.tgz",[m
 			"integrity": "sha1-J17PDX/+Bcd+ZOfIbkzZS/DhIoo=",[m
 			"dev": true,[m
 			"requires": {[m
[36m@@ -12832,7 +12834,7 @@[m
 			"dependencies": {[m
 				"camelcase": {[m
 					"version": "3.0.0",[m
[31m-					"resolved": "http://nexus.currencyfair.com/nexus/repository/npm-all/camelcase/-/camelcase-3.0.0.tgz",[m
[32m+[m					[32m"resolved": "https://registry.npmjs.org/camelcase/-/camelcase-3.0.0.tgz",[m
 					"integrity": "sha1-MvxLn82vhF/N9+c7uXysImHwqwo=",[m
 					"dev": true[m
 				}[m
[1mdiff --git a/src/public/js/client.js b/src/public/js/client.js[m
[1mindex 4db6401..f0beacd 100644[m
[1m--- a/src/public/js/client.js[m
[1m+++ b/src/public/js/client.js[m
[36m@@ -145,6 +145,18 @@[m [meval("\n\nvar messages = document.querySelector('#messages');\n\nif (messages) {[m
 [m
 /***/ }),[m
 [m
[32m+[m[32m/***/ "./src/public/js/client/navigation.js":[m
[32m+[m[32m/*!********************************************!*\[m
[32m+[m[32m  !*** ./src/public/js/client/navigation.js ***![m
[32m+[m[32m  \********************************************/[m
[32m+[m[32m/*! no static exports found */[m
[32m+[m[32m/***/ (function(module, exports, __webpack_require__) {[m
[32m+[m
[32m+[m[32m"use strict";[m
[32m+[m[32meval("\n\nvar $ = __webpack_require__(/*! jquery */ \"./node_modules/jquery/dist/jquery.js\");\nvar tabletSize = 900;\n\nvar menuBtn = void 0;\nvar navGroup = void 0;\nvar footerBtn = void 0;\n\nwindow.onresize = getWindowSize;\n\n$(document).ready(function () {\n  menuBtn = document.querySelector(\"#menu-btn\");\n  navGroup = document.querySelector('.navigation__group');\n  footerBtn = document.querySelector('#footer-icon-btn');\n  menuBtn.addEventListener('click', showMenu);\n  footerBtn.addEventListener('click', scrollTop);\n});\n\nfunction getWindowSize() {\n  if (window.innerWidth > tabletSize) {\n    navGroup.classList.remove('menu-is-active');\n  }\n}\n\nfunction showMenu() {\n  navGroup.classList.toggle('menu-is-active');\n}\n\nvar scrollTop = function scrollTop(e) {\n  window.scroll({\n    top: 0,\n    left: 0,\n    behavior: 'smooth'\n  });\n};\n\n//# sourceURL=webpack:///./src/public/js/client/navigation.js?");[m
[32m+[m
[32m+[m[32m/***/ }),[m
[32m+[m
 /***/ "./src/public/js/client/pagination.js":[m
 /*!********************************************!*\[m
   !*** ./src/public/js/client/pagination.js ***![m
[36m@@ -182,13 +194,13 @@[m [meval("\n\nvar $ = __webpack_require__(/*! jquery */ \"./node_modules/jquery/dist[m
 /***/ }),[m
 [m
 /***/ 0:[m
[31m-/*!***********************************************************************************************************************************************************************************************************************************************************************************!*\[m
[31m-  !*** multi ./src/public/js/client/delete-items.js ./src/public/js/client/image-upload.js ./src/public/js/client/messages.js ./src/public/js/client/filters-ls.js ./src/public/js/client/sort-dropdown.js ./src/public/js/client/pagination.js ./src/public/js/client/redirect.js ***![m
[31m-  \***********************************************************************************************************************************************************************************************************************************************************************************/[m
[32m+[m[32m/*!************************************************************************************************************************************************************************************************************************************************************************************************************************!*\[m
[32m+[m[32m  !*** multi ./src/public/js/client/delete-items.js ./src/public/js/client/image-upload.js ./src/public/js/client/messages.js ./src/public/js/client/filters-ls.js ./src/public/js/client/sort-dropdown.js ./src/public/js/client/pagination.js ./src/public/js/client/redirect.js ./src/public/js/client/navigation.js ***![m
[32m+[m[32m  \************************************************************************************************************************************************************************************************************************************************************************************************************************/[m
 /*! no static exports found */[m
 /***/ (function(module, exports, __webpack_require__) {[m
 [m
[31m-eval("__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\delete-items.js */\"./src/public/js/client/delete-items.js\");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\image-upload.js */\"./src/public/js/client/image-upload.js\");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\messages.js */\"./src/public/js/client/messages.js\");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\filters-ls.js */\"./src/public/js/client/filters-ls.js\");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\sort-dropdown.js */\"./src/public/js/client/sort-dropdown.js\");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\pagination.js */\"./src/public/js/client/pagination.js\");\nmodule.exports = __webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\redirect.js */\"./src/public/js/client/redirect.js\");\n\n\n//# sourceURL=webpack:///multi_./src/public/js/client/delete-items.js_./src/public/js/client/image-upload.js_./src/public/js/client/messages.js_./src/public/js/client/filters-ls.js_./src/public/js/client/sort-dropdown.js_./src/public/js/client/pagination.js_./src/public/js/client/redirect.js?");[m
[32m+[m[32meval("__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\delete-items.js */\"./src/public/js/client/delete-items.js\");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\image-upload.js */\"./src/public/js/client/image-upload.js\");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\messages.js */\"./src/public/js/client/messages.js\");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\filters-ls.js */\"./src/public/js/client/filters-ls.js\");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\sort-dropdown.js */\"./src/public/js/client/sort-dropdown.js\");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\pagination.js */\"./src/public/js/client/pagination.js\");\n__webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\redirect.js */\"./src/public/js/client/redirect.js\");\nmodule.exports = __webpack_require__(/*! C:\\Users\\jamie\\Documents\\Projects\\Github\\copsewood-aviaries\\src\\public\\js\\client\\navigation.js */\"./src/public/js/client/navigation.js\");\n\n\n//# sourceURL=webpack:///multi_./src/public/js/client/delete-items.js_./src/public/js/client/image-upload.js_./src/public/js/client/messages.js_./src/public/js/client/filters-ls.js_./src/public/js/client/sort-dropdown.js_./src/public/js/client/pagination.js_./src/public/js/client/redirect.js_./src/public/js/client/navigation.js?");[m
 [m
 /***/ })[m
 [m
[1mdiff --git a/src/public/styles/index.css b/src/public/styles/index.css[m
[1mindex f710830..21d61d3 100644[m
[1m--- a/src/public/styles/index.css[m
[1m+++ b/src/public/styles/index.css[m
[36m@@ -19,10 +19,10 @@[m
   font-family: 'Raleway', sans-serif;[m
   color: #efe9df; }[m
   .banner-header-font, .banner__text h1 {[m
[31m-    font-size: 42px; }[m
[32m+[m[32m    font-size: 36px; }[m
     @media screen and (min-width: 500px) {[m
       .banner-header-font, .banner__text h1 {[m
[31m-        font-size: calc( 42px + 6 * ((100vw - 500px) / 950)); } }[m
[32m+[m[32m        font-size: calc( 36px + 12 * ((100vw - 500px) / 950)); } }[m
     @media screen and (min-width: 1450px) {[m
       .banner-header-font, .banner__text h1 {[m
         font-size: 48px; } }[m
[36m@@ -32,10 +32,10 @@[m
   font-weight: 600;[m
   color: #efe9df; }[m
   .banner-sub-header-font, .banner__text h2 {[m
[31m-    font-size: 26px; }[m
[32m+[m[32m    font-size: 24px; }[m
     @media screen and (min-width: 500px) {[m
       .banner-sub-header-font, .banner__text h2 {[m
[31m-        font-size: calc( 26px + 6 * ((100vw - 500px) / 950)); } }[m
[32m+[m[32m        font-size: calc( 24px + 8 * ((100vw - 500px) / 950)); } }[m
     @media screen and (min-width: 1450px) {[m
       .banner-sub-header-font, .banner__text h2 {[m
         font-size: 32px; } }[m
[36m@@ -169,6 +169,9 @@[m
       .header .menu button:hover {[m
         cursor: pointer; }[m
 [m
[32m+[m[32mnav {[m
[32m+[m[32m  display: inline; }[m
[32m+[m
 .nav-padding, .navigation .navigation__group, .navigation .navigation__item {[m
   padding: 8px 0px; }[m
 [m
[36m@@ -176,6 +179,8 @@[m
   background-color: #efe9df;[m
   grid-column: 1 / span 12;[m
   grid-row: 2 / span 1; }[m
[32m+[m[32m  .navigation .navigation__group {[m
[32m+[m[32m    list-style: none; }[m
   .navigation .navigation__item {[m
     color: #191512;[m
     text-decoration: none;[m
[36m@@ -192,7 +197,7 @@[m
   grid-row: 6 / span 1; }[m
   .footer .wrapper:after {[m
     content: "";[m
[31m-    margin: 40px 40px 0 40px;[m
[32m+[m[32m    margin: 40px 40px 0 0;[m
     background: #ffffff;[m
     width: 100%;[m
     height: 2px;[m
[36m@@ -200,7 +205,6 @@[m
   .footer .wrapper {[m
     background-color: #191512;[m
     padding-top: 50px;[m
[31m-    padding-bottom: 50px;[m
     justify-content: space-between; }[m
     .footer .wrapper h3 {[m
       margin-bottom: 10px; }[m
[36m@@ -227,6 +231,16 @@[m
     margin-top: 0; }[m
   .footer .footer__contact .contact-details div {[m
     margin: 20px 0; }[m
[32m+[m[32m  .footer .footer__icon {[m
[32m+[m[32m    justify-content: center;[m
[32m+[m[32m    padding: 20px 0;[m
[32m+[m[32m    align-content: center;[m
[32m+[m[32m    background-color: #191512; }[m
[32m+[m[32m    .footer .footer__icon .icon__btn {[m
[32m+[m[32m      cursor: pointer;[m
[32m+[m[32m      background: none;[m
[32m+[m[32m      border: none;[m
[32m+[m[32m      outline: none; }[m
 [m
 .banner-height, .banner, .banner__image, .banner__image img {[m
   height: 500px; }[m
[36m@@ -335,6 +349,10 @@[m
 .unhighlight-upload .st0 {[m
   stroke: black; }[m
 [m
[32m+[m[32m.menu-is-active {[m
[32m+[m[32m  display: block !important;[m
[32m+[m[32m  flex-direction: column; }[m
[32m+[m
 #sortby-input,[m
 #page-input {[m
   display: none; }[m
[36m@@ -398,19 +416,28 @@[m
   .bird-categories .wrapper .card {[m
     align-self: center; }[m
   .banner {[m
[31m-    grid-row: 3 / span 1; }[m
[32m+[m[32m    grid-row: 4 / span 1; }[m
   .banner-height, .banner, .banner__image, .banner__image img {[m
     height: 380px; }[m
   .banner__text {[m
     grid-column: 1 / span 12;[m
[31m-    grid-row: 3 / span 1; }[m
[32m+[m[32m    grid-row: 4 / span 1; }[m
[32m+[m[32m    .banner__text h2 {[m
[32m+[m[32m      margin: 35px 0; }[m
   .banner__image {[m
     grid-column: 1 / span 12;[m
[31m-    grid-row: 2 / span 1; }[m
[31m-  .footer .wrapper:after {[m
[31m-    margin: 50px 0px 0px 0px; } }[m
[32m+[m[32m    grid-row: 3 / span 1; }[m
[32m+[m[32m  .welcome {[m
[32m+[m[32m    grid-row: 5 / span 1; }[m
[32m+[m[32m  .bird-categories {[m
[32m+[m[32m    grid-row: 6 / span 1; }[m
[32m+[m[32m  .footer {[m
[32m+[m[32m    grid-row: 7 / span 1; }[m
[32m+[m[32m    .footer .wrapper:after {[m
[32m+[m[32m      margin: 50px 0px 0px 0px; } }[m
 [m
 /* Tablet - Between */[m
[32m+[m[32m/* Tablet - Above */[m
 /* Phone - Below */[m
 @media (max-width: 500px) {[m
   .wrapper {[m
[1mdiff --git a/src/public/styles/scss/pages/partials/_footer.scss b/src/public/styles/scss/pages/partials/_footer.scss[m
[1mindex eba9478..0686827 100644[m
[1m--- a/src/public/styles/scss/pages/partials/_footer.scss[m
[1m+++ b/src/public/styles/scss/pages/partials/_footer.scss[m
[36m@@ -4,7 +4,7 @@[m
 [m
 	.wrapper:after { [m
 		content:""; [m
[31m-		margin: 40px 40px 0 40px; [m
[32m+[m		[32mmargin: 40px 40px 0 0;[m[41m [m
 		background: $white; [m
 		width: 100%; [m
 		height: 2px; [m
[36m@@ -14,7 +14,6 @@[m
 	.wrapper {[m
 		background-color: $dark;[m
 		padding-top: 50px;[m
[31m-		padding-bottom: 50px;[m
 		justify-content: space-between;[m
 [m
 		h3 {[m
[36m@@ -86,4 +85,18 @@[m
 			}[m
 		}[m
 	}[m
[32m+[m
[32m+[m	[32m.footer__icon {[m
[32m+[m		[32mjustify-content: center;[m
[32m+[m		[32mpadding: 20px 0;[m
[32m+[m		[32malign-content: center;[m
[32m+[m		[32mbackground-color: $dark;[m
[32m+[m
[32m+[m		[32m.icon__btn {[m
[32m+[m			[32mcursor: pointer;[m
[32m+[m			[32mbackground: none;[m
[32m+[m			[32mborder: none;[m
[32m+[m			[32moutline: none;[m
[32m+[m		[32m}[m
[32m+[m	[32m}[m
 }[m
[1mdiff --git a/src/public/styles/scss/pages/partials/_navigation.scss b/src/public/styles/scss/pages/partials/_navigation.scss[m
[1mindex d1eef34..7e10c52 100644[m
[1m--- a/src/public/styles/scss/pages/partials/_navigation.scss[m
[1m+++ b/src/public/styles/scss/pages/partials/_navigation.scss[m
[36m@@ -1,3 +1,7 @@[m
[32m+[m[32mnav {[m
[32m+[m	[32mdisplay: inline;[m
[32m+[m[32m}[m
[32m+[m
 .nav-padding {[m
 	padding: 8px 0px;[m
 }[m
[36m@@ -9,6 +13,7 @@[m
 [m
 	.navigation__group {[m
 		@extend .nav-padding;[m
[32m+[m		[32mlist-style: none;[m
 	}[m
 [m
 	.navigation__item {[m
[1mdiff --git a/src/public/styles/scss/settings/_fonts.scss b/src/public/styles/scss/settings/_fonts.scss[m
[1mindex e0d7786..d2d6833 100644[m
[1m--- a/src/public/styles/scss/settings/_fonts.scss[m
[1m+++ b/src/public/styles/scss/settings/_fonts.scss[m
[36m@@ -12,13 +12,13 @@[m
 }[m
 [m
 .banner-header-font {[m
[31m-	@include fluid-type($phone-bp, $desktop-bp, 42px, 48px);[m
[32m+[m	[32m@include fluid-type($phone-bp, $desktop-bp, 36px, 48px);[m
 	font-family: 'Raleway', sans-serif;[m
 	color: $light;[m
 }[m
 [m
 .banner-sub-header-font {[m
[31m-	@include fluid-type($phone-bp, $desktop-bp, 26px, 32px);[m
[32m+[m	[32m@include fluid-type($phone-bp, $desktop-bp, 24px, 32px);[m
 	font-family: 'Open Sans', sans-serif;[m
 	font-weight: 600;[m
 	color: $light;[m
[1mdiff --git a/src/public/styles/scss/utilities/_helpers.scss b/src/public/styles/scss/utilities/_helpers.scss[m
[1mindex a3747d7..f8b2093 100644[m
[1m--- a/src/public/styles/scss/utilities/_helpers.scss[m
[1m+++ b/src/public/styles/scss/utilities/_helpers.scss[m
[36m@@ -40,6 +40,11 @@[m
 	}[m
 }[m
 [m
[32m+[m[32m.menu-is-active {[m
[32m+[m	[32mdisplay: block !important;[m
[32m+[m	[32mflex-direction: column;[m
[32m+[m[32m}[m
[32m+[m
 #sortby-input,[m
 #page-input {[m
 	display: none;[m
[1mdiff --git a/src/public/styles/scss/utilities/index/_tablet.scss b/src/public/styles/scss/utilities/index/_tablet.scss[m
[1mindex 44f9270..74ffe4f 100644[m
[1m--- a/src/public/styles/scss/utilities/index/_tablet.scss[m
[1m+++ b/src/public/styles/scss/utilities/index/_tablet.scss[m
[36m@@ -36,7 +36,7 @@[m
 	} // bird-categories[m
 [m
 	.banner {[m
[31m-		grid-row: 3 / span 1;[m
[32m+[m		[32mgrid-row: 4 / span 1;[m
 	}[m
 	[m
 	.banner-height {[m
[36m@@ -45,16 +45,30 @@[m
 [m
 	.banner__text {[m
 		grid-column: 1 / span 12;[m
[31m-		grid-row: 3 / span 1;[m
[32m+[m		[32mgrid-row: 4 / span 1;[m
[32m+[m
[32m+[m		[32mh2 {[m
[32m+[m			[32mmargin: 35px 0;[m
[32m+[m		[32m}[m
 	}[m
 [m
 	.banner__image {[m
 		grid-column: 1 / span 12;[m
[31m-		grid-row: 2 / span 1;[m
[32m+[m		[32mgrid-row: 3 / span 1;[m
 	}[m
 	// banner[m
 [m
[32m+[m	[32m.welcome {[m
[32m+[m		[32mgrid-row: 5 / span 1;[m
[32m+[m	[32m}[m
[32m+[m
[32m+[m	[32m.bird-categories {[m
[32m+[m		[32mgrid-row: 6 / span 1;[m
[32m+[m	[32m}[m
[32m+[m
 	.footer {[m
[32m+[m		[32mgrid-row: 7 / span 1;[m
[32m+[m
 		.wrapper:after { [m
 			margin: 50px 0px 0px 0px; [m
 		}[m
[36m@@ -65,3 +79,8 @@[m
 @media (min-width: $tablet-bp) and (max-width: $laptop-bp) {[m
 [m
 }[m
[32m+[m
[32m+[m[32m/* Tablet - Above */[m
[32m+[m[32m@media (min-width: $tablet-bp) {[m
[32m+[m
[32m+[m[32m}[m
[1mdiff --git a/src/views/partials/footer.ejs b/src/views/partials/footer.ejs[m
[1mindex 82bbaaa..deabc28 100644[m
[1m--- a/src/views/partials/footer.ejs[m
[1m+++ b/src/views/partials/footer.ejs[m
[36m@@ -43,8 +43,9 @@[m
             </div>[m
           </div>[m
         </div>[m
[31m-[m
[31m-[m
[32m+[m[32m      </div>[m
[32m+[m[32m      <div class="footer__icon row">[m
[32m+[m[32m        <button class="icon__btn" id="footer-icon-btn"><img src="images/icons/up_arrow.svg"></button>[m
       </div>[m
     </footer>[m
   </div> <!-- grid -->[m
[1mdiff --git a/src/views/partials/header.ejs b/src/views/partials/header.ejs[m
[1mindex 4424c4d..7055ca0 100644[m
[1m--- a/src/views/partials/header.ejs[m
[1m+++ b/src/views/partials/header.ejs[m
[36m@@ -14,14 +14,14 @@[m
       <div class="row wrapper">[m
         <a class="brand">[m
           <img src='images/icons/Logo-Placeholder.png'>[m
[31m-          <div>Copsewood Aviaries</div>[m
[32m+[m[32m          <div class="test">Copsewood Aviaries</div>[m
         </a>[m
 [m
         <div class="phone">[m
           <span class="phone__text">Call Us - 086 2620201</span>[m
         </div>[m
 [m
[31m-        <div class="menu">[m
[32m+[m[32m        <div class="menu" id="menu-btn">[m
           <button>[m
             <img src="images/icons/menu-icon.png" />[m
           </button>[m
[1mdiff --git a/src/views/partials/navbar.ejs b/src/views/partials/navbar.ejs[m
[1mindex 9720464..b8ac6e5 100644[m
[1m--- a/src/views/partials/navbar.ejs[m
[1m+++ b/src/views/partials/navbar.ejs[m
[36m@@ -1,15 +1,25 @@[m
 <nav class="navigation wrapper">[m
[31m-  <div class="navigation__group">[m
[31m-    <a class="navigation__item" href="/">Home</a>[m
[31m-    <a class="navigation__item" href="/about">About</a>[m
[31m-    <a class="navigation__item" href="/birds">Birds for Sale</a>[m
[31m-    <a class="navigation__item" href="/contact">Contact</a>[m
[31m-    <% if (user) { %>[m
[31m-      <a class="navigation__item" href="/admin-panel">Admin Panel</a>[m
[31m-      <a class="navigation__item" href="/admins">Admins</a>[m
[31m-      <a class="navigation__item" href="/api/admins/logout">Logout</a>[m
[31m-    <% } else { %>[m
[31m-      <a class="navigation__item" href="/login">Login</a>[m
[31m-    <% } %>[m
[31m-  </div>[m
[32m+[m[32m  <ul class="navigation__group row">[m
[32m+[m[32m    <li>[m
[32m+[m[32m      <a class="navigation__item" href="/">Home</a>[m
[32m+[m[32m    </li>[m
[32m+[m[32m    <li>[m
[32m+[m[32m      <a class="navigation__item" href="/about">About</a>[m
[32m+[m[32m    </li>[m
[32m+[m[32m    <li>[m
[32m+[m[32m      <a class="navigation__item" href="/birds">Birds for Sale</a>[m
[32m+[m[32m    </li>[m
[32m+[m[32m    <li>[m
[32m+[m[32m      <a class="navigation__item" href="/contact">Contact</a>[m
[32m+[m[32m    </li>[m
[32m+[m[32m    <li>[m
[32m+[m[32m      <% if (user) { %>[m
[32m+[m[32m        <a class="navigation__item" href="/admin-panel">Admin Panel</a>[m
[32m+[m[32m        <a class="navigation__item" href="/admins">Admins</a>[m
[32m+[m[32m        <a class="navigation__item" href="/api/admins/logout">Logout</a>[m
[32m+[m[32m      <% } else { %>[m
[32m+[m[32m        <a class="navigation__item" href="/login">Login</a>[m
[32m+[m[32m      <% } %>[m
[32m+[m[32m    </li>[m
[32m+[m[32m  </ul>[m
 </nav>[m
\ No newline at end of file[m
[1mdiff --git a/webpack.dev.js b/webpack.dev.js[m
[1mindex 3bc9c58..c4d720d 100644[m
[1m--- a/webpack.dev.js[m
[1m+++ b/webpack.dev.js[m
[36m@@ -13,7 +13,8 @@[m [mmodule.exports = {[m
       path.resolve(__dirname, './src/public/js/client/filters-ls.js'),[m
       path.resolve(__dirname, './src/public/js/client/sort-dropdown.js'),[m
       path.resolve(__dirname, './src/public/js/client/pagination.js'),[m
[31m-      path.resolve(__dirname, './src/public/js/client/redirect.js')[m
[32m+[m[32m      path.resolve(__dirname, './src/public/js/client/redirect.js'),[m
[32m+[m[32m      path.resolve(__dirname, './src/public/js/client/navigation.js')[m
     ][m
   },[m
   output: {[m
